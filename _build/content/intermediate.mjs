export default {
id:'intermediate', ink:'--int',
kicker:{en:'Track two', ar:'المستوى التاني'},
name:{en:'Intermediate', ar:'متوسط'},
dek:{
  en:`Where an app stops being a demo. Routing and code splitting, talking to a server, forms that survive real validation, and the two chapters that decide whether your app is fast: effects and change detection.`,
  ar:`هنا التطبيق بيبطّل يبقى demo. Routing وتقسيم الكود، والكلام مع السيرفر، وforms تستحمل validation حقيقي، والفصلين اللي بيحددوا هل تطبيقك سريع ولا لأ: الـ effects والـ change detection.`
},
arts:[

/* 06 ------------------------------------------------------------------ */
{
  slug:'06-routing', num:'06',
  badge:{en:'Routing', ar:'التنقل'},
  title:{en:'Routes are data, guards are functions', ar:'الـ routes مجرد داتا، والـ guards مجرد functions'},
  lead:{
    en:`A route config is a plain array you read top to bottom. Lazy loading is one property. Guards are functions that can <code>inject()</code> anything — and the difference between <code>canMatch</code> and <code>canActivate</code> decides whether an unauthorised user <b>downloads your admin bundle</b>.`,
    ar:`ملف الـ routes مصفوفة عادية تقراها من فوق لتحت. الـ lazy loading سطر واحد. والـ guards functions تقدر تعمل <code>inject()</code> لأي حاجة — والفرق بين <code>canMatch</code> و<code>canActivate</code> هو اللي بيحدد هل اللي مش مصرّح له <b>هينزّل bundle الأدمن</b> ولا لأ.`
  },
  nodes:[
    [{id:'cfg',role:{en:'the map',ar:'الخريطة'},file:'app.routes',ext:'.ts',kind:'cfg'},
     {id:'guard',role:{en:'the gate',ar:'البوابة'},file:'auth',ext:'.guard.ts',kind:'cfg'}],
    [{id:'chunk',role:{en:'its own bundle',ar:'bundle لوحده'},file:'order-page.component',ext:'.ts',kind:'ts'},
     {id:'boot',role:{en:'wires it up',ar:'بيوصّل'},file:'app.config',ext:'.ts',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'boot',to:'cfg',a:'top',b:'bottom',label:'provideRouter(routes)',packet:'routes'},
    {id:'e2',from:'cfg',to:'guard',a:'right',b:'left',label:'canActivate',packet:'/orders/42'},
    {id:'e3',from:'guard',to:'chunk',a:'bottom',b:'right',label:'import()',packet:'chunk.js'}
  ],
  files:[
    {id:'cfg',name:'app.routes.ts',lang:'ts',tag:{en:'the map',ar:'الخريطة'},code:[
      "import { Routes } from '@angular/router';",
      "import { authGuard, adminMatch } from './auth.guard';",
      "",
      "export const routes: Routes = [",
      "  { path: '', pathMatch: 'full', redirectTo: 'orders' },",
      "",
      "  {",
      "    path: 'orders/:id',",
      "    title: 'Order details',",
      "    loadComponent: () => import('./orders/order-page.component').then(m => m.OrderPage),",
      "    canActivate: [authGuard],",
      "    data: { section: 'sales' },",
      "  },",
      "",
      "  {",
      "    path: 'admin',",
      "    canMatch: [adminMatch],        // checked BEFORE the chunk downloads",
      "    loadChildren: () => import('./admin/admin.routes').then(m => m.ADMIN_ROUTES),",
      "  },",
      "",
      "  { path: '**', loadComponent: () => import('./not-found.component').then(m => m.NotFound) },",
      "];"]},
    {id:'guard',name:'auth.guard.ts',lang:'ts',tag:{en:'plain functions',ar:'functions عادية'},code:[
      "import { inject } from '@angular/core';",
      "import { CanActivateFn, CanMatchFn, Router } from '@angular/router';",
      "",
      "export const authGuard: CanActivateFn = (route, state) => {",
      "  const session = inject(Session);",
      "  if (session.isSignedIn()) return true;",
      "",
      "  // a UrlTree redirects instead of merely blocking",
      "  return inject(Router).createUrlTree(['/sign-in'], {",
      "    queryParams: { next: state.url },",
      "  });",
      "};",
      "",
      "// false here makes the router skip the route and keep matching —",
      "// the lazy chunk is never requested at all",
      "export const adminMatch: CanMatchFn = () => inject(Session).isAdmin();"]},
    {id:'chunk',name:'order-page.component.ts',lang:'ts',tag:{en:'the page',ar:'الصفحة'},code:[
      "export class OrderPage {",
      "  // withComponentInputBinding() fills these from the URL",
      "  readonly id  = input.required<string>();   // :id",
      "  readonly tab = input('items');             // ?tab=",
      "  readonly section = input('');              // data: { section }",
      "",
      "  // no ActivatedRoute, no snapshot, no paramMap subscription.",
      "  // navigating /orders/1 → /orders/2 reuses the component and",
      "  // id() simply becomes '2'.",
      "  readonly order = httpResource<Order>(() => `/api/orders/${this.id()}`);",
      "}"]},
    {id:'boot',name:'app.config.ts',lang:'ts',tag:{en:'wiring',ar:'التوصيل'},code:[
      "import { provideRouter, withComponentInputBinding,",
      "         withInMemoryScrolling, withPreloading, PreloadAllModules } from '@angular/router';",
      "",
      "export const appConfig: ApplicationConfig = {",
      "  providers: [",
      "    provideRouter(",
      "      routes,",
      "      withComponentInputBinding(),",
      "      withInMemoryScrolling({ scrollPositionRestoration: 'enabled', anchorScrolling: 'enabled' }),",
      "      withPreloading(PreloadAllModules),   // fetch lazy chunks once idle",
      "    ),",
      "  ],",
      "};"]}
  ],
  steps:[
    {t:{en:'Bootstrap hands the route array to the router. <code class="i">withComponentInputBinding()</code> is the flag that turns URL segments into component inputs.',
        ar:'الإقلاع بيسلّم مصفوفة الراوتس للراوتر. و<code class="i">withComponentInputBinding()</code> هو السطر اللي بيحوّل أجزاء الـ URL لـ inputs في الـ component.'},
     edge:'e1',hl:{boot:[7,8]}},
    {t:{en:'A navigation to <code class="i">/orders/42</code> matches the second route. Its guards run before anything renders.',
        ar:'التنقل لـ <code class="i">/orders/42</code> بيطابق الراوت التاني. والـ guards بتاعته بتشتغل قبل ما أي حاجة تترسم.'},
     edge:'e2',hl:{cfg:[8,11]}},
    {t:{en:'The guard is a function with no class and no provider entry. It runs in an injection context, so it can reach the session and the router directly.',
        ar:'الـ guard function من غير كلاس ولا provider. بتشتغل جوه injection context، فبتوصل للـ session وللراوتر على طول.'},
     hl:{guard:[4,5,6,9]}},
    {t:{en:'Only once the gate opens does <code class="i">loadComponent</code> run the dynamic <code class="i">import()</code> and pull the chunk over the network.',
        ar:'وبس لما البوابة تفتح، <code class="i">loadComponent</code> بينفّذ الـ <code class="i">import()</code> الديناميكي وبينزّل الـ chunk من الشبكة.'},
     edge:'e3',hl:{cfg:[10]}},
    {t:{en:'The page reads <code class="i">id()</code> as an input. Navigating from <code class="i">/orders/1</code> to <code class="i">/orders/2</code> reuses the component — the signal just changes, and the resource refetches.',
        ar:'الصفحة بتقرا <code class="i">id()</code> كـ input. والتنقل من <code class="i">/orders/1</code> لـ <code class="i">/orders/2</code> بيعيد استخدام نفس الـ component — الـ signal بتتغير بس، والـ resource بيجيب من جديد.'},
     hl:{chunk:[3,10]}}
  ],
  example:{
    name:{en:'A list page that keeps its filters in the URL', ar:'صفحة لستة محتفظة بالفلاتر في الـ URL'},
    what:{
      en:'Filters live in query params, so the back button works, a refresh keeps the view, and a colleague can paste the link into chat and see exactly what you were looking at. The component still just reads inputs.',
      ar:'الفلاتر عايشة في الـ query params، فزرار الرجوع بيشتغل، والـ refresh مبيضيّعش العرض، وزميلك يقدر يلزق اللينك في الشات ويشوف اللي إنت كنت شايفه بالظبط. والـ component لسه بيقرا inputs وبس.'},
    files:[
      {id:'r1',name:'orders-list.component.ts',lang:'ts',tag:{en:'reads the URL',ar:'بيقرا الـ URL'},code:[
        "export class OrdersList {",
        "  private readonly router = inject(Router);",
        "  private readonly route = inject(ActivatedRoute);",
        "",
        "  readonly status = input<'all' | 'paid' | 'failed'>('all');   // ?status=",
        "  readonly page = input(1, { transform: numberAttribute });     // ?page=",
        "",
        "  readonly data = httpResource<Page<Order>>(() =>",
        "    `/api/orders?status=${this.status()}&page=${this.page()}`);",
        "",
        "  setStatus(status: string) {",
        "    // change the URL — the inputs follow, the resource refetches",
        "    this.router.navigate([], {",
        "      relativeTo: this.route,",
        "      queryParams: { status, page: 1 },",
        "      queryParamsHandling: 'merge',",
        "    });",
        "  }",
        "}"]},
      {id:'r2',name:'orders-list.component.html',lang:'html',tag:{en:'the controls',ar:'التحكم'},code:[
        "@for (s of ['all', 'paid', 'failed']; track s) {",
        "  <button (click)=\"setStatus(s)\" [class.on]=\"status() === s\">{{ s }}</button>",
        "}",
        "",
        "@if (data.isLoading()) { <app-skeleton /> }",
        "@else { @for (o of data.value()?.items ?? []; track o.id) { <app-order-row [order]=\"o\" /> } }"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">canActivate</code> runs <b>after</b> the lazy chunk downloads. For role-gated features use <code class="i">canMatch</code> so the bundle is never even requested.',
     ar:'<code class="i">canActivate</code> بيشتغل <b>بعد</b> ما الـ chunk ينزل. للحاجات المربوطة بصلاحيات استعمل <code class="i">canMatch</code> عشان الـ bundle ميتطلبش أصلاً.'},
    {en:'Route order matters. <code class="i">**</code> must be last, and a <code class="i">path: \'\'</code> without <code class="i">pathMatch: \'full\'</code> matches everything.',
     ar:'ترتيب الراوتس مهم. <code class="i">**</code> لازم تبقى الأخيرة، و<code class="i">path: \'\'</code> من غير <code class="i">pathMatch: \'full\'</code> بيطابق كل حاجة.'},
    {en:'Reading <code class="i">route.snapshot.params</code> once is the classic bug: navigating <code class="i">/user/1</code> → <code class="i">/user/2</code> reuses the component and the snapshot never changes. Use input binding or <code class="i">paramMap</code>.',
     ar:'قراية <code class="i">route.snapshot.params</code> مرة واحدة دي الباج الكلاسيكي: التنقل من <code class="i">/user/1</code> لـ <code class="i">/user/2</code> بيعيد استخدام نفس الـ component والـ snapshot مبيتغيرش. استعمل input binding أو <code class="i">paramMap</code>.'},
    {en:'<code class="i">withComponentInputBinding()</code> only fills inputs whose <b>name matches</b> the param, query param or data key. A typo is silent.',
     ar:'<code class="i">withComponentInputBinding()</code> بيملا بس الـ inputs اللي <b>اسمها مطابق</b> للـ param أو الـ query param أو مفتاح الـ data. أي غلطة إملائية بتعدي بالسكوت.'},
    {en:'Query params arrive as strings. <code class="i">input(1, { transform: numberAttribute })</code> saves you a pile of <code class="i">+x</code> conversions.',
     ar:'الـ query params بتوصل strings. و<code class="i">input(1, { transform: numberAttribute })</code> بيوفر عليك كومة تحويلات <code class="i">+x</code>.'}
  ]
},

/* 07 ------------------------------------------------------------------ */
{
  slug:'07-http', num:'07',
  badge:{en:'Data', ar:'الداتا'},
  title:{en:'HTTP without the <code>subscribe</code>', ar:'HTTP من غير <code>subscribe</code>'},
  lead:{
    en:`Interceptors are functions. Requests can be <b>declared</b> instead of triggered — and the loading flag, the error branch and the race condition you keep hand-writing already exist in the box.`,
    ar:`الـ interceptors بقت functions. والريكويست تقدر <b>تعلن</b> عنه بدل ما تطلقه — والـ loading flag وفرع الايرور وحالة السباق اللي بتكتبها بإيدك كل مرة، كلهم موجودين جاهزين.`
  },
  nodes:[
    [{id:'cfg',role:{en:'registers them',ar:'بيسجّلهم'},file:'app.config',ext:'.ts',kind:'cfg'},
     {id:'int',role:{en:'adds the token',ar:'بيضيف التوكن'},file:'auth',ext:'.interceptor.ts',kind:'cfg'}],
    [{id:'cmp',role:{en:'declares the read',ar:'بيعلن القراية'},file:'user-page.component',ext:'.ts',kind:'ts'},
     {id:'tpl',role:{en:'renders 3 states',ar:'بيعرض 3 حالات'},file:'user-page.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'cfg',to:'int',a:'right',b:'left',label:'withInterceptors([...])',packet:'chain'},
    {id:'e2',from:'cmp',to:'int',a:'top',b:'bottom',label:'GET /api/users/42',packet:'+ Bearer'},
    {id:'e3',from:'cmp',to:'tpl',a:'right',b:'left',label:'user.value()',packet:'{ name }'}
  ],
  files:[
    {id:'cfg',name:'app.config.ts',lang:'ts',tag:{en:'setup',ar:'الإعداد'},code:[
      "import { provideHttpClient, withFetch, withInterceptors } from '@angular/common/http';",
      "",
      "providers: [",
      "  provideHttpClient(",
      "    withFetch(),                          // needed for SSR streaming",
      "    withInterceptors([authInterceptor, errorInterceptor]),",
      "  ),",
      "]"]},
    {id:'int',name:'auth.interceptor.ts',lang:'ts',tag:{en:'a function',ar:'مجرد function'},code:[
      "import { inject } from '@angular/core';",
      "import { HttpInterceptorFn } from '@angular/common/http';",
      "",
      "export const authInterceptor: HttpInterceptorFn = (req, next) => {",
      "  const token = inject(Session).token();",
      "  if (!token) return next(req);",
      "",
      "  // HttpRequest is immutable — clone it, never mutate it",
      "  return next(req.clone({",
      "    setHeaders: { Authorization: `Bearer ${token}` },",
      "  }));",
      "};"]},
    {id:'cmp',name:'user-page.component.ts',lang:'ts',tag:{en:'declarative',ar:'إعلاني'},code:[
      "import { httpResource } from '@angular/common/http';",
      "",
      "export class UserPage {",
      "  readonly id = input.required<string>();",
      "",
      "  // reads id() → refetches when it changes, aborts the stale request",
      "  readonly user = httpResource<User>(() => `/api/users/${this.id()}`);",
      "",
      "  //  user.value()     → User | undefined",
      "  //  user.isLoading() → boolean",
      "  //  user.error()     → unknown",
      "  //  user.reload()",
      "}"]},
    {id:'tpl',name:'user-page.component.html',lang:'html',tag:{en:'three states',ar:'تلات حالات'},code:[
      "@if (user.isLoading()) {",
      "  <app-skeleton />",
      "} @else if (user.error()) {",
      "  <p class=\"err\">",
      "    Could not load this user.",
      "    <button (click)=\"user.reload()\">Try again</button>",
      "  </p>",
      "} @else if (user.value(); as u) {",
      "  <h1>{{ u.name }}</h1>",
      "  <p>{{ u.email }}</p>",
      "}"]},
    {id:'stable',name:'the stable equivalent',lang:'ts',tag:{en:'no preview APIs',ar:'من غير APIs تجريبية'},code:[
      "// same behaviour with HttpClient only — switchMap is the",
      "// part that cancels the request for the id you just left",
      "readonly user = toSignal(",
      "  toObservable(this.id).pipe(",
      "    switchMap(id => this.http.get<User>(`/api/users/${id}`)),",
      "  ),",
      "  { initialValue: undefined },",
      ");"]}
  ],
  steps:[
    {t:{en:'One provider call sets up the client. <code class="i">withFetch()</code> swaps XHR for the Fetch API — smaller, and required if you want streamed SSR.',
        ar:'نداء provider واحد بيجهّز الكلاينت. و<code class="i">withFetch()</code> بيبدّل XHR بـ Fetch API — أصغر، ومطلوب لو عايز SSR بالبث.'},
     edge:'e1',hl:{cfg:[5,6]}},
    {t:{en:'Each interceptor is a function in a chain. It runs in an injection context, so <code class="i">inject(Session)</code> works with no constructor anywhere.',
        ar:'كل interceptor function في سلسلة. وبتشتغل جوه injection context، فـ <code class="i">inject(Session)</code> شغالة من غير أي constructor.'},
     hl:{int:[4,5,9,10]}},
    {t:{en:'The component <b>declares</b> a read. Nothing fires until something reads the resource, and the URL function makes <code class="i">id()</code> a dependency.',
        ar:'الـ component <b>بيعلن</b> عن قراية. مفيش حاجة بتشتغل لحد ما حد يقرا الـ resource، ودالة الـ URL بتخلي <code class="i">id()</code> dependency.'},
     edge:'e2',hl:{cmp:[7]}},
    {t:{en:'Change <code class="i">id()</code> and the previous request is <b>aborted</b>, not just ignored. That is the race condition — slow response for the old id landing after the new one — solved by default.',
        ar:'غيّر <code class="i">id()</code> والريكويست القديم بيتلغي <b>فعلاً</b>، مش بس بيتتجاهل. وده حل جاهز لحالة السباق — رد الـ id القديم البطيء بيوصل بعد الجديد.'},
     hl:{cmp:[6,7]}},
    {t:{en:'The template renders the three states from one object. No <code class="i">loading</code> boolean, no <code class="i">error</code> field, no <code class="i">ngOnDestroy</code>.',
        ar:'التمبلت بيعرض التلات حالات من object واحد. مفيش <code class="i">loading</code> ولا <code class="i">error</code> ولا <code class="i">ngOnDestroy</code>.'},
     edge:'e3',hl:{tpl:[1,3,8]}},
    {t:{en:'If you would rather not depend on a preview API, this is the same behaviour with stable pieces only. <code class="i">switchMap</code> is the part doing the cancelling.',
        ar:'ولو مش عايز تعتمد على API لسه preview، ده نفس السلوك بحاجات مستقرة بس. و<code class="i">switchMap</code> هو الجزء اللي بيعمل الإلغاء.'},
     hl:{stable:[3,4,5,7]}}
  ],
  example:{
    name:{en:'A search box that does not spam the server', ar:'خانة بحث مش بتدوّخ السيرفر'},
    what:{
      en:'Every keystroke changes a signal, but you do not want a request per keystroke. The debounce lives on the way in, and cancellation of the request you no longer care about is automatic.',
      ar:'كل حرف بيغيّر signal، بس إنت مش عايز ريكويست لكل حرف. الـ debounce بيتحط على المدخل، وإلغاء الريكويست اللي مبقاش يهمك بيحصل لوحده.'},
    files:[
      {id:'s1',name:'product-search.component.ts',lang:'ts',tag:{en:'debounced',ar:'مبطّأ'},code:[
        "export class ProductSearch {",
        "  readonly term = signal('');",
        "",
        "  // debounce on the way in, so the resource only sees settled values",
        "  private readonly settled = toSignal(",
        "    toObservable(this.term).pipe(debounceTime(300), distinctUntilChanged()),",
        "    { initialValue: '' },",
        "  );",
        "",
        "  readonly results = httpResource<Product[]>(() => {",
        "    const q = this.settled().trim();",
        "    return q.length < 2 ? undefined : `/api/products?q=${encodeURIComponent(q)}`;",
        "  });",
        "  // returning undefined means: do not request anything yet",
        "}"]},
      {id:'s2',name:'save-flow.ts',lang:'ts',tag:{en:'mutations stay imperative',ar:'التعديلات بتفضل يدوية'},code:[
        "// a POST caused by a click is an EVENT, not a derivation.",
        "// resources model reading; keep writing explicit.",
        "async save(draft: Product) {",
        "  await firstValueFrom(this.http.post<Product>('/api/products', draft));",
        "  this.results.reload();      // now the read is stale — refresh it",
        "  this.toasts.show('Saved');",
        "}"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">resource</code>, <code class="i">rxResource</code> and <code class="i">httpResource</code> are <b>developer preview</b> and their option names have already changed once (<code class="i">request</code> → <code class="i">params</code>, <code class="i">loader</code> → <code class="i">stream</code>). Fine in an app you control; be careful in a published library.',
     ar:'<code class="i">resource</code> و<code class="i">rxResource</code> و<code class="i">httpResource</code> لسه <b>developer preview</b> وأسامي الـ options اتغيرت مرة بالفعل (<code class="i">request</code> بقت <code class="i">params</code>، و<code class="i">loader</code> بقت <code class="i">stream</code>). عادي في تطبيق إنت مسيطر عليه؛ خد بالك في library هتنشرها.'},
    {en:'An <code class="i">HttpRequest</code> is immutable. <code class="i">req.headers.set(...)</code> returns a new object and changes nothing — you must <code class="i">req.clone({...})</code>.',
     ar:'الـ <code class="i">HttpRequest</code> ثابت. <code class="i">req.headers.set(...)</code> بيرجّع object جديد ومبيغيّرش حاجة — لازم <code class="i">req.clone({...})</code>.'},
    {en:'Interceptor order is the array order, and it applies outward on the response. Put logging first and auth last if you want to log the final request.',
     ar:'ترتيب الـ interceptors هو ترتيب المصفوفة، وبيتطبق بالعكس على الرد. حط الـ logging الأول والـ auth الآخر لو عايز تسجّل الريكويست النهائي.'},
    {en:'<code class="i">HttpClient</code> completes after one value, so a forgotten <code class="i">subscribe</code> is not a leak — but it <b>is</b> an untracked side effect. Prefer the <code class="i">async</code> pipe, <code class="i">toSignal</code>, or a resource.',
     ar:'<code class="i">HttpClient</code> بيقفل بعد قيمة واحدة، فالـ <code class="i">subscribe</code> اللي نسيته مش تسريب — بس هو <b>فعلاً</b> side effect مش متتبَّع. الأفضل <code class="i">async</code> pipe أو <code class="i">toSignal</code> أو resource.'},
    {en:'Returning <code class="i">undefined</code> from the URL function means “do not request yet”. That is how you express a search that waits for two characters.',
     ar:'لو دالة الـ URL رجّعت <code class="i">undefined</code> معناها “متطلبش حاجة دلوقتي”. وكده تعبّر عن بحث مستني حرفين.'}
  ]
},

/* 08 ------------------------------------------------------------------ */
{
  slug:'08-forms', num:'08',
  badge:{en:'Forms', ar:'الفورمز'},
  title:{en:'Typed forms, and the <code>null</code> that surprises everyone', ar:'فورمز بأنواع حقيقية، والـ <code>null</code> اللي بتفاجئ الكل'},
  lead:{
    en:`Reactive forms carry real types now. The catch: a control resets to <code>null</code> unless you say otherwise, so your <code>string</code> is quietly a <code>string | null</code> — and that is where the flood of <code>?.</code> and <code>!</code> in form code comes from.`,
    ar:`الـ reactive forms بقى ليها أنواع حقيقية. المشكلة: الـ control بيرجع لـ <code>null</code> إلا لو قلت له لأ، فالـ <code>string</code> بتاعك في الحقيقة <code>string | null</code> — ومن هنا بييجي طوفان الـ <code>?.</code> والـ <code>!</code> في كود الفورمز.`
  },
  nodes:[
    [{id:'ts',role:{en:'declares the shape',ar:'بيعلن الشكل'},file:'signup.component',ext:'.ts',kind:'ts'},
     {id:'val',role:{en:'cross-field rule',ar:'قاعدة بين الحقول'},file:'match-fields',ext:'.ts',kind:'cfg'}],
    [{id:'tpl',role:{en:'binds & shows errors',ar:'بيربط ويعرض الأخطاء'},file:'signup.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'val',a:'right',b:'left',label:'validators: [...]',packet:'group'},
    {id:'e2',from:'ts',to:'tpl',a:'bottom',b:'top',label:'[formGroup]="form"',packet:'form'},
    {id:'e3',from:'tpl',to:'ts',a:'right',b:'right',label:'(ngSubmit)',packet:'getRawValue()'}
  ],
  files:[
    {id:'ts',name:'signup.component.ts',lang:'ts',tag:{en:'the shape',ar:'الشكل'},code:[
      "import { FormBuilder, Validators, ReactiveFormsModule } from '@angular/forms';",
      "",
      "export class Signup {",
      "  private readonly fb = inject(FormBuilder);",
      "",
      "  // nonNullable is doing the real work here",
      "  readonly form = this.fb.nonNullable.group({",
      "    name:     ['', [Validators.required, Validators.minLength(2)]],",
      "    email:    ['', [Validators.required, Validators.email]],",
      "    password: ['', [Validators.required, Validators.minLength(8)]],",
      "    confirm:  ['', Validators.required],",
      "    plan:     ['free' as 'free' | 'pro'],",
      "  }, { validators: matchFields('password', 'confirm') });",
      "",
      "  submit() {",
      "    if (this.form.invalid) { this.form.markAllAsTouched(); return; }",
      "    const value = this.form.getRawValue();   // fully typed, no nulls",
      "    this.api.signup(value);",
      "  }",
      "}"]},
    {id:'val',name:'match-fields.ts',lang:'ts',tag:{en:'a validator',ar:'فاليديتور'},code:[
      "import { AbstractControl, ValidationErrors, ValidatorFn } from '@angular/forms';",
      "",
      "// a rule about two fields lives on the GROUP, not on a control",
      "export function matchFields(a: string, b: string): ValidatorFn {",
      "  return (group: AbstractControl): ValidationErrors | null => {",
      "    const va = group.get(a)?.value;",
      "    const vb = group.get(b)?.value;",
      "    return va === vb ? null : { mismatch: true };",
      "  };",
      "}",
      "",
      "// async validators return an Observable or a Promise",
      "export function emailTaken(api: Api): AsyncValidatorFn {",
      "  return c => api.checkEmail(c.value).pipe(",
      "    map(taken => (taken ? { taken: true } : null)),",
      "  );",
      "}"]},
    {id:'tpl',name:'signup.component.html',lang:'html',tag:{en:'the view',ar:'العرض'},code:[
      "<form [formGroup]=\"form\" (ngSubmit)=\"submit()\">",
      "  <label>",
      "    Email",
      "    <input formControlName=\"email\" type=\"email\" autocomplete=\"email\">",
      "  </label>",
      "",
      "  @let email = form.controls.email;",
      "  @if (email.touched && email.errors; as e) {",
      "    <p class=\"err\">",
      "      @if (e['required']) { Email is required. }",
      "      @else if (e['email']) { That does not look like an email address. }",
      "    </p>",
      "  }",
      "",
      "  @if (form.touched && form.errors?.['mismatch']) {",
      "    <p class=\"err\">The two passwords do not match.</p>",
      "  }",
      "",
      "  <button [disabled]=\"form.invalid\">Create account</button>",
      "</form>"]},
    {id:'sig',name:'bridging into signals',lang:'ts',tag:{en:'optional',ar:'اختياري'},code:[
      "readonly value  = toSignal(this.form.valueChanges,",
      "                           { initialValue: this.form.getRawValue() });",
      "readonly status = toSignal(this.form.statusChanges,",
      "                           { initialValue: this.form.status });",
      "",
      "readonly canSubmit = computed(() => this.status() === 'VALID');",
      "readonly showPro   = computed(() => this.value().plan === 'pro');"]}
  ],
  steps:[
    {t:{en:'<code class="i">fb.nonNullable</code> is the difference between a <code class="i">string</code> and a <code class="i">string | null</code>. It also makes <code class="i">reset()</code> return controls to <code class="i">\'\'</code>, not to <code class="i">null</code>.',
        ar:'<code class="i">fb.nonNullable</code> هو الفرق بين <code class="i">string</code> و<code class="i">string | null</code>. وكمان بيخلي <code class="i">reset()</code> يرجّع الـ controls لـ <code class="i">\'\'</code> مش لـ <code class="i">null</code>.'},
     hl:{ts:[6,7]}},
    {t:{en:'A rule involving two fields cannot live on either one. It goes on the group, as a second argument.',
        ar:'القاعدة اللي بتخص حقلين مينفعش تعيش في واحد منهم. بتتحط على الـ group، كمعامل تاني.'},
     edge:'e1',hl:{ts:[13],val:[4,5,8]}},
    {t:{en:'<code class="i">[formGroup]</code> connects the object to the markup; <code class="i">formControlName</code> connects each field by name. A typo here fails at runtime, not at compile time.',
        ar:'<code class="i">[formGroup]</code> بيوصّل الـ object بالماركب؛ و<code class="i">formControlName</code> بيوصّل كل حقل بالاسم. وأي غلطة إملائية هنا بتظهر وقت التشغيل مش وقت الكمبايل.'},
     edge:'e2',hl:{tpl:[1,4]}},
    {t:{en:'Errors are shown only after <code class="i">touched</code> — never while someone is typing their first character. <code class="i">@let</code> keeps the template from repeating <code class="i">form.controls.email</code> four times.',
        ar:'الأخطاء بتتعرض بس بعد <code class="i">touched</code> — عمرك ما توريها وهو لسه بيكتب أول حرف. و<code class="i">@let</code> بيمنع التمبلت إنه يكرر <code class="i">form.controls.email</code> أربع مرات.'},
     hl:{tpl:[7,8,10,11]}},
    {t:{en:'On submit, <code class="i">getRawValue()</code> — not <code class="i">value</code>. A disabled control is excluded from <code class="i">value</code>, which makes its type a <code class="i">Partial</code>.',
        ar:'وقت الإرسال، استعمل <code class="i">getRawValue()</code> مش <code class="i">value</code>. الـ control المعطّل مش بيتحسب في <code class="i">value</code>، وده بيخلي نوعها <code class="i">Partial</code>.'},
     edge:'e3',hl:{ts:[16,17,18]}},
    {t:{en:'A form is an RxJS object, so bridging it into signals is two <code class="i">toSignal</code> calls. After that the rest of the component reads plain values.',
        ar:'الفورم object بتاع RxJS، فتوصيله بالـ signals ندائين <code class="i">toSignal</code>. وبعد كده باقي الـ component بيقرا قيم عادية.'},
     hl:{sig:[1,3,6,7]}}
  ],
  example:{
    name:{en:'An address form that changes shape by country', ar:'فورم عنوان بيتغير شكله حسب البلد'},
    what:{
      en:'Picking Egypt shows a governorate field and hides the postcode; picking Germany does the reverse. Validators are added and removed at runtime, and updateValueAndValidity is the line everybody forgets.',
      ar:'لما تختار مصر بيظهر حقل المحافظة ويختفي الرقم البريدي؛ ولما تختار ألمانيا بيحصل العكس. الفاليديتورز بتتضاف وتتشال وقت التشغيل، وسطر updateValueAndValidity هو اللي الكل بينساه.'},
    files:[
      {id:'a1',name:'address-form.component.ts',lang:'ts',tag:{en:'dynamic rules',ar:'قواعد متغيرة'},code:[
        "export class AddressForm {",
        "  readonly form = this.fb.nonNullable.group({",
        "    country:     ['EG'],",
        "    street:      ['', Validators.required],",
        "    governorate: [''],",
        "    postcode:    [''],",
        "  });",
        "",
        "  constructor() {",
        "    this.form.controls.country.valueChanges",
        "      .pipe(takeUntilDestroyed())",
        "      .subscribe(c => this.applyCountryRules(c));",
        "  }",
        "",
        "  private applyCountryRules(country: string) {",
        "    const gov = this.form.controls.governorate;",
        "    const post = this.form.controls.postcode;",
        "",
        "    gov.setValidators(country === 'EG' ? [Validators.required] : []);",
        "    post.setValidators(country === 'EG' ? [] : [Validators.required]);",
        "",
        "    // without these two lines the form keeps the OLD validity",
        "    gov.updateValueAndValidity();",
        "    post.updateValueAndValidity();",
        "  }",
        "}"]},
      {id:'a2',name:'address-form.component.html',lang:'html',tag:{en:'conditional fields',ar:'حقول مشروطة'},code:[
        "<select formControlName=\"country\">",
        "  <option value=\"EG\">Egypt</option>",
        "  <option value=\"DE\">Germany</option>",
        "</select>",
        "",
        "@if (form.value.country === 'EG') {",
        "  <input formControlName=\"governorate\" placeholder=\"Governorate\">",
        "} @else {",
        "  <input formControlName=\"postcode\" placeholder=\"Postcode\">",
        "}"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">form.value</code> excludes disabled controls and is therefore a <code class="i">Partial</code>. Use <code class="i">getRawValue()</code> when you submit.',
     ar:'<code class="i">form.value</code> بيستبعد الـ controls المعطلة وعشان كده نوعها <code class="i">Partial</code>. استعمل <code class="i">getRawValue()</code> وقت الإرسال.'},
    {en:'<code class="i">setValidators()</code> does not re-run validation. Follow it with <code class="i">updateValueAndValidity()</code> or the form keeps its old verdict.',
     ar:'<code class="i">setValidators()</code> مبيعيدش التحقق. لازم تتبعه بـ <code class="i">updateValueAndValidity()</code> وإلا الفورم هيفضل على حكمه القديم.'},
    {en:'Subscribing to <code class="i">valueChanges</code> without <code class="i">takeUntilDestroyed()</code> is a real leak — unlike HTTP, this stream never completes.',
     ar:'الـ subscribe لـ <code class="i">valueChanges</code> من غير <code class="i">takeUntilDestroyed()</code> ده تسريب حقيقي — عكس الـ HTTP، الستريم ده عمره ما بيقفل.'},
    {en:'<code class="i">disabled: true</code> inside the control config is a known trap in reactive forms. Set it with <code class="i">control.disable()</code> instead.',
     ar:'<code class="i">disabled: true</code> جوه إعداد الـ control فخ معروف في الـ reactive forms. استعمل <code class="i">control.disable()</code> بدالها.'},
    {en:'“Invalid input” tells nobody anything. Say what is wrong <b>and</b> what fixes it: <em>Password needs at least 8 characters.</em>',
     ar:'“Invalid input” مش بتقول لحد أي حاجة. قول إيه الغلط <b>و</b>إيه اللي يصلّحه: <em>الباسورد لازم 8 حروف على الأقل.</em>'}
  ]
},

/* 09 ------------------------------------------------------------------ */
{
  slug:'09-effects-vs-computed', num:'09',
  badge:{en:'Reactivity', ar:'التفاعلية'},
  title:{en:'<code>effect()</code> is not a cheaper <code>computed()</code>', ar:'<code>effect()</code> مش نسخة أرخص من <code>computed()</code>'},
  lead:{
    en:`The single most common mistake in signal code. <code>computed()</code> derives a value and hands it back; <code>effect()</code> derives nothing and exists only to push something into the world outside the graph. <b>If what you want at the end is a value, you do not want an effect.</b>`,
    ar:`أشهر غلطة في الكود اللي بيستخدم signals. <code>computed()</code> بيشتق قيمة ويرجّعهالك؛ و<code>effect()</code> مبيشتقش حاجة وموجود بس عشان يدفع حاجة لبرّه الرسم البياني. <b>لو اللي عايزه في الآخر قيمة، يبقى إنت مش عايز effect.</b>`
  },
  nodes:[
    [{id:'src',role:{en:'the state',ar:'الحالة'},file:'theme.service',ext:'.ts',kind:'svc'}],
    [{id:'good',role:{en:'derives a value',ar:'بيشتق قيمة'},file:'computed',ext:'.ts',kind:'ts'},
     {id:'fx',role:{en:'leaves the graph',ar:'بيخرج بره الرسم'},file:'effect',ext:'.ts',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'src',to:'good',a:'bottom',b:'top',label:'read → depend',packet:'dark'},
    {id:'e2',from:'src',to:'fx',a:'right',b:'top',label:'read → depend',packet:'dark'},
    {id:'e3',from:'fx',to:'src',a:'right',b:'right',label:'DO NOT write back',packet:'loop!'}
  ],
  files:[
    {id:'bad',name:'the mistake',lang:'ts',tag:{en:'avoid',ar:'تجنّب'},code:[
      "readonly items = signal<Item[]>([]);",
      "readonly total = signal(0);",
      "",
      "constructor() {",
      "  effect(() => {",
      "    this.total.set(this.items().reduce((s, i) => s + i.price, 0));",
      "  });",
      "}",
      "",
      "// total is now stored twice. There is a moment where items has",
      "// updated and total has not — two truths that can disagree.",
      "// Two effects writing each other's signals will ping-pong",
      "// until Angular gives up, and the stack trace will not name your bug."]},
    {id:'good',name:'the fix',lang:'ts',tag:{en:'do this',ar:'اعمل كده'},code:[
      "readonly items = signal<Item[]>([]);",
      "readonly total = computed(() =>",
      "  this.items().reduce((s, i) => s + i.price, 0));",
      "",
      "// one source of truth. lazy, cached, never out of date,",
      "// and it declared its own dependency just by reading items()."]},
    {id:'fx',name:'theme.service.ts',lang:'ts',tag:{en:'a real effect',ar:'effect حقيقي'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class ThemeService {",
      "  readonly theme = signal<'light' | 'dark'>('light');",
      "",
      "  constructor() {",
      "    effect(onCleanup => {",
      "      const value = this.theme();                         // tracked",
      "",
      "      // everything below leaves the reactive graph — that is the point",
      "      document.documentElement.dataset['theme'] = value;",
      "      localStorage.setItem('theme', value);",
      "",
      "      onCleanup(() => console.debug('replacing', value));",
      "    });",
      "  }",
      "}"]},
    {id:'unt',name:'untracked()',lang:'ts',tag:{en:'read without depending',ar:'اقرا من غير اعتماد'},code:[
      "effect(() => {",
      "  const page = this.page();                       // re-run when this changes",
      "  const uid  = untracked(() => this.session().id); // read, but do not depend",
      "",
      "  analytics.track('page_view', { page, uid });",
      "});",
      "",
      "// without untracked, signing in would fire a bogus page_view."]}
  ],
  steps:[
    {t:{en:'Both a <code class="i">computed</code> and an <code class="i">effect</code> become readers the moment they read a signal. That part is identical.',
        ar:'الـ <code class="i">computed</code> والـ <code class="i">effect</code> الاتنين بيبقوا قرّاء أول ما يقروا signal. الجزء ده واحد.'},
     edge:'e1',hl:{good:[2,3]}},
    {t:{en:'The difference is what they return. A computed gives a value back <b>into</b> the graph. An effect returns nothing and touches the DOM, storage, analytics — the world outside.',
        ar:'الفرق في اللي بيرجّعوه. الـ computed بيرجّع قيمة <b>جوه</b> الرسم البياني. والـ effect مبيرجّعش حاجة وبيلمس الـ DOM والتخزين والـ analytics — العالم اللي بره.'},
     edge:'e2',hl:{fx:[6,7,10,11]}},
    {t:{en:'Writing a signal from inside an effect is where it goes wrong. You have created a second copy of a value that was already derivable — and a window in which the two disagree.',
        ar:'الكتابة في signal من جوه effect هي اللي بتبوّظ الدنيا. إنت كده عملت نسخة تانية من قيمة كانت أصلاً قابلة للاشتقاق — وفتحت شباك بيختلفوا فيه.'},
     edge:'e3',hl:{bad:[5,6,10,11]}},
    {t:{en:'Ask what value the effect is really computing. Nine times out of ten the answer is a <code class="i">computed</code> you have not written yet.',
        ar:'اسأل نفسك الـ effect بيحسب إيه بالظبط. تسعة من عشرة الإجابة <code class="i">computed</code> إنت لسه مكتبتهاش.'},
     hl:{good:[1,2,3,5,6]}},
    {t:{en:'<code class="i">untracked()</code> reads a value without becoming its reader — for the id you only want in a log line, not as a trigger.',
        ar:'<code class="i">untracked()</code> بتقرا قيمة من غير ما تبقى قارئ ليها — للـ id اللي عايزه في سطر log بس، مش كمشغّل.'},
     hl:{unt:[2,3,8]}}
  ],
  example:{
    name:{en:'Draft autosave that does not fight itself', ar:'حفظ تلقائي للمسودة مش بيتخانق مع نفسه'},
    what:{
      en:'Everything derivable is a computed: whether the draft is dirty, whether it can be saved, what the status line says. Exactly one effect exists, and it does the one thing the graph cannot do — write to storage.',
      ar:'كل حاجة تقدر تُشتق بقت computed: هل المسودة اتغيرت، هل ينفع تتحفظ، سطر الحالة بيقول إيه. وفيه effect واحد بس، وبيعمل الحاجة الوحيدة اللي الرسم البياني مش بيعملها — الكتابة في التخزين.'},
    files:[
      {id:'d1',name:'draft.store.ts',lang:'ts',tag:{en:'derive everything',ar:'اشتق كل حاجة'},code:[
        "export class DraftStore {",
        "  readonly draft = signal<Draft>(EMPTY);",
        "  readonly saved = signal<Draft>(EMPTY);",
        "",
        "  // all three are derived — none of them is stored",
        "  readonly dirty = computed(() =>",
        "    JSON.stringify(this.draft()) !== JSON.stringify(this.saved()));",
        "  readonly canSave = computed(() => this.dirty() && this.draft().title.length > 0);",
        "  readonly status = computed(() => this.dirty() ? 'Unsaved changes' : 'All changes saved');",
        "",
        "  constructor() {",
        "    // the ONE effect: persistence is outside the graph",
        "    effect(() => {",
        "      const d = this.draft();",
        "      localStorage.setItem('draft', JSON.stringify(d));",
        "    });",
        "  }",
        "}"]},
      {id:'d2',name:'draft-bar.component.html',lang:'html',tag:{en:'reads only',ar:'قراية بس'},code:[
        "<span class=\"status\" [class.dirty]=\"dirty()\">{{ status() }}</span>",
        "<button [disabled]=\"!canSave()\" (click)=\"save()\">Save</button>"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">effect()</code> needs an injection context — a field initializer or a constructor. To create one later, pass <code class="i">{ injector }</code> explicitly.',
     ar:'<code class="i">effect()</code> محتاج injection context — عند تعريف field أو جوه constructor. ولو عايز تعمله بعدين، مرّر <code class="i">{ injector }</code> بنفسك.'},
    {en:'Effects are destroyed with their component or service. You do not clean them up by hand; that is what <code class="i">onCleanup</code> is for <b>between runs</b>.',
     ar:'الـ effects بتتدمّر مع الـ component أو الـ service بتاعتها. مش بتنضفها بإيدك؛ و<code class="i">onCleanup</code> ده للتنضيف <b>بين التشغيلات</b>.'},
    {en:'Everything read inside an effect becomes a dependency — including a read hidden inside a helper method you call.',
     ar:'أي حاجة بتتقرا جوه effect بتبقى dependency — حتى القراية المخبية جوه method مساعدة إنت بتناديها.'},
    {en:'A <code class="i">computed</code> must be pure. Calling an API or writing another signal inside one produces a value that depends on who looked and when.',
     ar:'الـ <code class="i">computed</code> لازم تبقى نضيفة. لو نادیت API أو كتبت في signal تانية من جواها، القيمة هتعتمد على مين بصّ وإمتى.'},
    {en:'Legitimate effect work: persistence, analytics, focus management, and driving an imperative third-party library — a chart, a map, a canvas.',
     ar:'الشغل المشروع للـ effect: التخزين، والـ analytics، وإدارة الـ focus، وتشغيل library خارجية بتاخد أوامر — chart أو map أو canvas.'}
  ]
},

/* 10 ------------------------------------------------------------------ */
{
  slug:'10-change-detection', num:'10',
  badge:{en:'Rendering', ar:'الرندرة'},
  title:{en:'What actually makes Angular re-render', ar:'إيه اللي بيخلي أنجولار يرندر تاني فعلاً'},
  lead:{
    en:`Change detection is neither magic nor continuous. Something schedules a check, Angular walks the component tree, and each component is either <b>dirty</b> or <b>skipped</b>. Knowing exactly five things mark a component dirty is the whole subject.`,
    ar:`الـ change detection لا سحر ولا شغال على طول. حاجة بتجدول فحص، وأنجولار بيمشي على شجرة الـ components، وكل واحد يا <b>dirty</b> يا <b>بيتعدّى</b>. ومعرفة إن خمس حاجات بالظبط هي اللي بتعلّمه dirty دي الموضوع كله.`
  },
  nodes:[
    [{id:'root',role:{en:'checked first',ar:'بيتفحص الأول'},file:'app.component',ext:'.ts',kind:'ts'}],
    [{id:'push',role:{en:'OnPush — skipped',ar:'OnPush — بيتعدّى'},file:'list.component',ext:'.ts',kind:'ts'},
     {id:'leaf',role:{en:'reads a signal',ar:'بيقرا signal'},file:'row.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'root',to:'push',a:'bottom',b:'top',label:'tree walk',packet:'check'},
    {id:'e2',from:'push',to:'leaf',a:'right',b:'left',label:'[row]="r"',packet:'ref change'},
    {id:'e3',from:'leaf',to:'root',a:'top',b:'right',label:'signal → mark path',packet:'dirty'}
  ],
  files:[
    {id:'push',name:'list.component.ts',lang:'ts',tag:{en:'the strategy',ar:'الاستراتيجية'},code:[
      "@Component({",
      "  selector: 'app-list',",
      "  changeDetection: ChangeDetectionStrategy.OnPush,",
      "  template: `",
      "    @for (r of rows(); track r.id) { <app-row [row]=\"r\" /> }",
      "  `,",
      "})",
      "export class List {",
      "  readonly rows = input<Row[]>([]);",
      "}"]},
    {id:'bad',name:'the silent break',lang:'ts',tag:{en:'avoid',ar:'تجنّب'},code:[
      "@Component({ changeDetection: ChangeDetectionStrategy.OnPush })",
      "export class List {",
      "  @Input() rows: Row[] = [];",
      "",
      "  addRow(r: Row) {",
      "    this.rows.push(r);      // SAME array reference",
      "  }",
      "}",
      "",
      "// OnPush compares the input by reference. Nothing changed,",
      "// so nothing is marked dirty, so the screen never updates."]},
    {id:'fix',name:'either fix',lang:'ts',tag:{en:'do this',ar:'اعمل كده'},code:[
      "// 1. give it a new reference",
      "this.rows = [...this.rows, r];",
      "",
      "// 2. or stop having the problem:",
      "//    a signal notifies its readers regardless of reference",
      "readonly rows = input<Row[]>([]);",
      "addRow(r: Row) { this.store.rows.update(l => [...l, r]); }"]},
    {id:'leaf',name:'row.component.ts',lang:'ts',tag:{en:'targeted',ar:'موجّه'},code:[
      "@Component({",
      "  selector: 'app-row',",
      "  changeDetection: ChangeDetectionStrategy.OnPush,",
      "  template: `",
      "    <span>{{ row().title }}</span>",
      "    <b>{{ store.priceOf(row().id) }}</b>",
      "  `,",
      "})",
      "export class RowCmp {",
      "  readonly row = input.required<Row>();",
      "  readonly store = inject(PriceStore);",
      "",
      "  // when a signal read by THIS template changes, only the path",
      "  // from the root down to THIS component is marked — not siblings.",
      "}"]}
  ],
  steps:[
    {t:{en:'Classically <code class="i">zone.js</code> patches <code class="i">setTimeout</code>, <code class="i">addEventListener</code> and XHR. Any of them firing says “something might have happened” and a full tree walk begins.',
        ar:'كلاسيكياً <code class="i">zone.js</code> بيلبّس نفسه على <code class="i">setTimeout</code> و<code class="i">addEventListener</code> والـ XHR. وأول ما أي واحد فيهم يشتغل بيقول “يمكن حصلت حاجة” وتبدأ لفة كاملة على الشجرة.'},
     edge:'e1'},
    {t:{en:'An <code class="i">OnPush</code> component is <b>skipped</b> during that walk unless it was marked dirty. That is the opt-out, and it is why the strategy is a performance tool.',
        ar:'الـ component اللي عليه <code class="i">OnPush</code> <b>بيتعدّى</b> في اللفة دي إلا لو كان اتعلّم dirty. دي طريقة الخروج، وعشان كده الاستراتيجية دي أداة أداء.'},
     hl:{push:[3]}},
    {t:{en:'Exactly five things mark it dirty: a DOM event bound in its own template, a new input <b>reference</b>, an <code class="i">async</code> pipe emitting, a signal its template reads changing, or an explicit <code class="i">markForCheck()</code>.',
        ar:'خمس حاجات بالظبط بتعلّمه dirty: حدث DOM متربوط في تمبلته، أو <b>reference</b> جديدة لـ input، أو <code class="i">async</code> pipe بيطلّع قيمة، أو signal بيقراها تمبلته بتتغير، أو <code class="i">markForCheck()</code> صريح.'},
     edge:'e2',hl:{push:[9]}},
    {t:{en:'This is the classic silent break: mutating the array in place. The reference did not change, so <code class="i">OnPush</code> never notices and the screen quietly lies.',
        ar:'ودي الكسرة الصامتة الكلاسيكية: تعديل الـ array في مكانها. الـ reference متغيرتش، فـ <code class="i">OnPush</code> مبيلاحظش والشاشة بتكدب في هدوء.'},
     hl:{bad:[6,10,11]}},
    {t:{en:'Two ways out: replace the reference so <code class="i">OnPush</code> sees a change, or hold the value in a signal so the reference stops mattering at all.',
        ar:'مخرجين: تبدّل الـ reference عشان <code class="i">OnPush</code> يشوف تغيير، أو تحط القيمة في signal فالـ reference تبطّل يبقى ليها لازمة أصلاً.'},
     hl:{fix:[2,6,7]}},
    {t:{en:'The fourth trigger is the important one. A signal read in a template marks <b>only the components that read it</b> — not their siblings. That is targeted invalidation, and it is what makes signal apps fast with no tuning.',
        ar:'المشغّل الرابع هو المهم. الـ signal اللي بتتقرا في تمبلت بتعلّم <b>الـ components اللي قرتها بس</b> — مش إخواتهم. ده اسمه targeted invalidation، وهو اللي بيخلي تطبيقات الـ signals سريعة من غير أي ظبط.'},
     edge:'e3',hl:{leaf:[5,6,13,14]}}
  ],
  example:{
    name:{en:'A live price ticker in a 500-row table', ar:'مؤشر أسعار حي في جدول 500 صف'},
    what:{
      en:'A websocket pushes a price every 200 ms. With zone.js and default change detection that is a full tree walk five times a second. With a signal store and OnPush rows, only the cells whose price actually moved are touched.',
      ar:'الـ websocket بيبعت سعر كل 200 مللي ثانية. مع zone.js والـ change detection الافتراضي دي لفة كاملة على الشجرة خمس مرات في الثانية. ومع signal store وصفوف OnPush، الخلايا اللي سعرها اتغير فعلاً هي بس اللي بتتلمس.'},
    files:[
      {id:'p1',name:'price.store.ts',lang:'ts',tag:{en:'one signal per symbol',ar:'signal لكل رمز'},code:[
        "@Injectable({ providedIn: 'root' })",
        "export class PriceStore {",
        "  private readonly map = new Map<string, WritableSignal<number>>();",
        "",
        "  priceOf(symbol: string) {",
        "    if (!this.map.has(symbol)) this.map.set(symbol, signal(0));",
        "    return this.map.get(symbol)!();",
        "  }",
        "",
        "  // called from outside Angular — no NgZone.run needed,",
        "  // a signal write schedules its own targeted check",
        "  push(symbol: string, price: number) {",
        "    this.map.get(symbol)?.set(price);",
        "  }",
        "}"]},
      {id:'p2',name:'ticker-row.component.ts',lang:'ts',tag:{en:'one cell updates',ar:'خلية واحدة بتتحدث'},code:[
        "@Component({",
        "  selector: 'app-ticker-row',",
        "  changeDetection: ChangeDetectionStrategy.OnPush,",
        "  template: `<td>{{ symbol() }}</td><td>{{ store.priceOf(symbol()) }}</td>`,",
        "})",
        "export class TickerRow {",
        "  readonly symbol = input.required<string>();",
        "  readonly store = inject(PriceStore);",
        "}",
        "",
        "// 500 rows on screen, one price ticks → one row re-renders."]}
    ]
  },
  gotchas:[
    {en:'<code class="i">push()</code>, <code class="i">splice()</code> and property mutation are invisible to <code class="i">OnPush</code>. Replace the reference, or hold the value in a signal.',
     ar:'<code class="i">push()</code> و<code class="i">splice()</code> وتعديل الخصائص كلهم مش مرئيين لـ <code class="i">OnPush</code>. بدّل الـ reference، أو حط القيمة في signal.'},
    {en:'A third-party callback (a chart library, a map SDK) runs outside Angular. Set a signal from it and the update lands — no <code class="i">NgZone.run</code> required.',
     ar:'أي callback من library خارجية (chart أو map SDK) بيشتغل بره أنجولار. حط signal من جواه والتحديث هيوصل — من غير <code class="i">NgZone.run</code>.'},
    {en:'<code class="i">OnPush</code> everywhere plus signals for state is not an optimisation you apply later. It is the shape the framework now expects.',
     ar:'<code class="i">OnPush</code> في كل حتة مع signals للحالة دي مش تحسينة بتعملها بعدين. دي الشكل اللي الفريمورك بقى متوقعه.'},
    {en:'Measure instead of guessing: <code class="i">ng.profiler.timeChangeDetection()</code> in the console, or record a profile in Angular DevTools.',
     ar:'قِس بدل ما تخمّن: <code class="i">ng.profiler.timeChangeDetection()</code> في الكونسول، أو سجّل profile في Angular DevTools.'},
    {en:'A <code class="i">setInterval</code> you forgot about costs a full tree walk on every tick under zone.js — a common cause of a mysteriously warm laptop.',
     ar:'<code class="i">setInterval</code> نسيته بيكلّفك لفة كاملة على الشجرة كل تكة تحت zone.js — سبب شائع للاب توب اللي بيسخن من غير سبب واضح.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'queries',
  badge:{en:'Reaching in', ar:'الوصول للداخل'},
  title:{en:'<code>viewChild</code> and <code>contentChild</code>', ar:'<code>viewChild</code> و <code>contentChild</code>'},
  lead:{
    en:`Sometimes you need the element itself — to call <code>focus()</code>, to measure it, to hand it to a chart library. Queries find it for you. As signals they solve the oldest bug in Angular: <b>the child that was undefined when you looked</b>.`,
    ar:`أحياناً بتحتاج العنصر نفسه — عشان تنادي <code>focus()</code>، أو تقيسه، أو تديه لمكتبة رسوم. الـ queries بتلاقيه لك. وكـ signals بيحلوا أقدم باج في أنجولار: <b>الابن اللي كان undefined لما بصيت</b>.`
  },
  nodes:[
    [{id:'ts',role:{en:'asks for it',ar:'بيطلبه'},file:'search-panel',ext:'.ts',kind:'ts'}],
    [{id:'tpl',role:{en:'owns the element',ar:'مالك العنصر'},file:'search-panel',ext:'.html',kind:'html'},
     {id:'child',role:{en:'a child component',ar:'component ابن'},file:'result-list',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'tpl',a:'bottom',b:'top',label:"viewChild('box')",packet:'#box'},
    {id:'e2',from:'tpl',to:'child',a:'right',b:'left',label:'<app-result-list>',packet:'instance'},
    {id:'e3',from:'child',to:'ts',a:'top',b:'right',label:'.scrollToTop()',packet:'call'}
  ],
  files:[
    {id:'ts',name:'search-panel.ts',lang:'ts',tag:{en:'every form',ar:'كل الأشكال'},code:[
      "import { Component, viewChild, viewChildren, contentChild,",
      "         ElementRef, afterNextRender } from '@angular/core';",
      "",
      "export class SearchPanel {",
      "  // may be undefined — Signal<ElementRef | undefined>",
      "  readonly box = viewChild<ElementRef<HTMLInputElement>>('box');",
      "",
      "  // guaranteed — Signal<ElementRef>. throws at startup if missing.",
      "  readonly input = viewChild.required<ElementRef<HTMLInputElement>>('box');",
      "",
      "  // a child COMPONENT instance, by its class",
      "  readonly list = viewChild(ResultList);",
      "",
      "  // all of them — Signal<readonly ElementRef[]>",
      "  readonly chips = viewChildren<ElementRef>('chip');",
      "",
      "  // the DOM element of a component tag, not the component instance",
      "  readonly listEl = viewChild(ResultList, { read: ElementRef });",
      "",
      "  clearAndFocus() {",
      "    this.input().nativeElement.value = '';",
      "    this.input().nativeElement.focus();",
      "    this.list()?.scrollToTop();",
      "  }",
      "}"]},
    {id:'tpl',name:'search-panel.html',lang:'html',tag:{en:'the targets',ar:'الأهداف'},code:[
      "<!-- #box is a TEMPLATE REFERENCE VARIABLE. it is what the",
      "     string in viewChild('box') is matching against.        -->",
      "<input #box (input)=\"onType($event)\">",
      "",
      "<button (click)=\"clearAndFocus()\">Clear</button>",
      "",
      "@for (t of tags(); track t) {",
      "  <span #chip class=\"chip\">{{ t }}</span>",
      "}",
      "",
      "<app-result-list [results]=\"results()\" />"]},
    {id:'child',name:'result-list.ts',lang:'ts',tag:{en:'the child',ar:'الابن'},code:[
      "export class ResultList {",
      "  readonly results = input<Result[]>([]);",
      "  private readonly host = inject(ElementRef<HTMLElement>);",
      "",
      "  // a public method the parent is allowed to call.",
      "  // keep this surface small — it is an API, not an escape hatch.",
      "  scrollToTop() {",
      "    this.host.nativeElement.scrollTo({ top: 0, behavior: 'smooth' });",
      "  }",
      "}"]},
    {id:'when',name:'when is it actually there?',lang:'ts',tag:{en:'the old bug',ar:'الباج القديم'},code:[
      "// THE OLD DECORATOR API — the classic beginner trap",
      "@ViewChild('box') box!: ElementRef;",
      "",
      "constructor()   { this.box.nativeElement; }   // ✗ undefined",
      "ngOnInit()      { this.box.nativeElement; }   // ✗ still undefined",
      "ngAfterViewInit() { this.box.nativeElement; } // ✓ finally",
      "",
      "// …and undefined AGAIN whenever the element sits inside a",
      "// false @if, with no warning that it went away.",
      "",
      "// THE SIGNAL API — you cannot read it at the wrong time,",
      "// because reading it is just calling a signal:",
      "readonly box = viewChild<ElementRef>('box');",
      "",
      "// undefined while absent, present when present. an effect or",
      "// a computed simply re-runs when that changes.",
      "constructor() {",
      "  effect(() => this.box()?.nativeElement.focus());",
      "}"]},
    {id:'content',name:'viewChild vs contentChild',lang:'ts',tag:{en:'the difference',ar:'الفرق'},code:[
      "// viewChild    → something in MY OWN template",
      "// contentChild → something PROJECTED INTO me by whoever used me",
      "",
      "@Component({",
      "  selector: 'app-tabs',",
      "  template: `",
      "    <nav>@for (t of tabs(); track t.id) { <button>{{ t.label() }}</button> }</nav>",
      "    <ng-content />",
      "  `,",
      "})",
      "export class Tabs {",
      "  // the <app-tab> elements the CALLER wrote between our tags",
      "  readonly tabs = contentChildren(Tab);",
      "}",
      "",
      "// <app-tabs>",
      "//   <app-tab label=\"One\">…</app-tab>     ← contentChildren finds these",
      "//   <app-tab label=\"Two\">…</app-tab>",
      "// </app-tabs>"]}
  ],
  steps:[
    {t:{en:'<code class="i">#box</code> in the template is a <b>template reference variable</b>. The string you pass to <code class="i">viewChild(\'box\')</code> is matching that name.',
        ar:'<code class="i">#box</code> اللي في التمبلت اسمه <b>template reference variable</b>. والنص اللي بتمرره لـ <code class="i">viewChild(\'box\')</code> بيطابق الاسم ده.'},
     edge:'e1',hl:{tpl:[1,2,3],ts:[6]}},
    {t:{en:'<code class="i">.required</code> removes the <code class="i">undefined</code> from the type and fails loudly at startup if the element is genuinely missing. Use it whenever the element is unconditionally in your template.',
        ar:'<code class="i">.required</code> بتشيل الـ <code class="i">undefined</code> من النوع وبتفشل بصوت عالي عند التشغيل لو العنصر ناقص فعلاً. استعملها كل ما العنصر يكون موجود في تمبلتك من غير شروط.'},
     hl:{ts:[8,9]}},
    {t:{en:'Query for a class instead of a string and you get the <b>component instance</b> — so you can call its public methods. Add <code class="i">{ read: ElementRef }</code> if you wanted its DOM element instead.',
        ar:'دوّر بكلاس بدل نص وهتاخد <b>نسخة الـ component</b> — فتقدر تنادي methods العامة بتاعته. وضيف <code class="i">{ read: ElementRef }</code> لو كنت عايز عنصر الـ DOM بتاعه بدل كده.'},
     edge:'e2',hl:{ts:[11,12,17,18],child:[7,8]}},
    {t:{en:'Because a query is a signal, an <code class="i">effect</code> or <code class="i">computed</code> that reads it simply re-runs when the element appears or disappears. The whole “undefined at the wrong moment” category of bug is gone.',
        ar:'وعشان الـ query دي signal، أي <code class="i">effect</code> أو <code class="i">computed</code> بيقراها بيعيد الشغل لما العنصر يظهر أو يختفي. ونوعية باج «undefined في الوقت الغلط» كلها راحت.'},
     edge:'e3',hl:{when:[12,17,18]}},
    {t:{en:'<code class="i">viewChild</code> looks in <b>your own</b> template. <code class="i">contentChild</code> looks at what was <b>projected into you</b>. Mixing them up is the second most common mistake here.',
        ar:'<code class="i">viewChild</code> بيبص في <b>تمبلتك إنت</b>. و<code class="i">contentChild</code> بيبص في اللي <b>اتسقط جواك</b>. وخلطهم ببعض تاني أكتر غلطة هنا.'},
     hl:{content:[1,2,12,13]}}
  ],
  example:[
    {
      name:{en:'Tabs that discover their own children', ar:'تابات بتكتشف أبناءها لوحدها'},
      what:{
        en:'The tab strip never receives a list of tabs as an input. It finds them with contentChildren, reads their labels, and drives which one is visible. Adding a tab is writing one more tag — no array to keep in sync.',
        ar:'شريط التابات عمره ما بياخد لستة تابات كـ input. هو بيلاقيهم بـ contentChildren، ويقرا عناوينهم، ويتحكم في اللي ظاهر. وإضافة تاب معناها تكتب tag زيادة — من غير array تفضل تزامنها.'},
      files:[
        {id:'t1',name:'tabs.ts',lang:'ts',tag:{en:'the container',ar:'الحاوية'},code:[
          "@Component({",
          "  selector: 'app-tabs',",
          "  template: `",
          "    <nav role=\"tablist\">",
          "      @for (t of tabs(); track t.id; let i = $index) {",
          "        <button role=\"tab\"",
          "                [attr.aria-selected]=\"active() === i\"",
          "                (click)=\"active.set(i)\">{{ t.label() }}</button>",
          "      }",
          "    </nav>",
          "    <ng-content />",
          "  `,",
          "})",
          "export class Tabs {",
          "  readonly tabs = contentChildren(Tab);",
          "  readonly active = signal(0);",
          "",
          "  constructor() {",
          "    // tell each child whether it is the visible one",
          "    effect(() => this.tabs().forEach((t, i) => t.visible.set(i === this.active())));",
          "  }",
          "}"]},
        {id:'t2',name:'tab.ts',lang:'ts',tag:{en:'the child',ar:'الابن'},code:[
          "@Component({",
          "  selector: 'app-tab',",
          "  template: `@if (visible()) { <ng-content /> }`,",
          "})",
          "export class Tab {",
          "  readonly id = crypto.randomUUID();",
          "  readonly label = input.required<string>();",
          "  readonly visible = signal(false);",
          "}"]},
        {id:'t3',name:'settings.html',lang:'html',tag:{en:'the caller',ar:'المستخدم'},code:[
          "<app-tabs>",
          "  <app-tab label=\"Profile\">   <app-profile-form /></app-tab>",
          "  <app-tab label=\"Billing\">   <app-billing-form /></app-tab>",
          "  <app-tab label=\"Security\">  <app-security-form /></app-tab>",
          "</app-tabs>",
          "",
          "<!-- no array of tab definitions anywhere. -->"]}
      ]
    }
  ],
  gotchas:[
    {en:'A query only finds elements in the template of the component that declared it. It does not search the whole page, and it never crosses into a child component&rsquo;s own template.',
     ar:'الـ query بتلاقي بس العناصر اللي في تمبلت الـ component اللي أعلنها. مش بتدوّر في الصفحة كلها، وعمرها ما بتدخل جوه تمبلت component ابن.'},
    {en:'An element inside a false <code class="i">&#64;if</code> does not exist. With signal queries that is simply <code class="i">undefined</code> — handle it with <code class="i">?.</code> rather than <code class="i">.required</code>.',
     ar:'العنصر اللي جوه <code class="i">&#64;if</code> بقيمة false مش موجود. ومع الـ signal queries دي ببساطة <code class="i">undefined</code> — اتعامل معاها بـ <code class="i">?.</code> مش بـ <code class="i">.required</code>.'},
    {en:'Reaching into a child to poke at its internals couples them permanently. Call a small public method — or better, pass an input down and let the child decide.',
     ar:'إنك تمد إيدك جوه ابن وتلعب في تفاصيله بيربطهم ببعض للأبد. نادي method عامة صغيرة — أو الأحسن، مرّر input لتحت وسيب الابن يقرر.'},
    {en:'<code class="i">{ read: ElementRef }</code> is how you get the DOM node of a component tag. Without it you get the component instance, which has no <code class="i">nativeElement</code>.',
     ar:'<code class="i">{ read: ElementRef }</code> هي إزاي تجيب الـ DOM node بتاع تاج component. من غيرها بتاخد نسخة الـ component، واللي مالهاش <code class="i">nativeElement</code>.'},
    {en:'Prefer a template binding to a query every time. If <code class="i">[value]</code> or <code class="i">&#64;if</code> can express it, a query is more machinery for the same result.',
     ar:'فضّل الربط في التمبلت على الـ query في كل مرة. لو <code class="i">[value]</code> أو <code class="i">&#64;if</code> يقدروا يعبّروا عنها، فالـ query مجرد تعقيد زيادة لنفس النتيجة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'rxjs-operators',
  badge:{en:'The four maps', ar:'الأربع maps'},
  title:{en:'<code>switchMap</code>, <code>mergeMap</code>, <code>concatMap</code>, <code>exhaustMap</code>', ar:'<code>switchMap</code> و <code>mergeMap</code> و <code>concatMap</code> و <code>exhaustMap</code>'},
  lead:{
    en:`These four look identical and behave completely differently. Picking the wrong one is the single most expensive RxJS mistake — it causes duplicate orders, flickering search results, and race conditions that only appear on slow connections. <b>The question they answer is: what happens when a new event arrives while the previous one is still running?</b>`,
    ar:`الأربعة دول شكلهم واحد وبيتصرفوا بشكل مختلف تماماً. واختيار الغلط فيهم هو أغلى غلطة في RxJS — بيسبب أوردرات مكررة، ونتايج بحث بترف، وحالات سباق مبتظهرش غير على نت بطيء. <b>والسؤال اللي بيجاوبوا عليه: إيه اللي بيحصل لما حدث جديد يوصل والقديم لسه شغال؟</b>`
  },
  nodes:[
    [{id:'src',role:{en:'events over time',ar:'أحداث عبر الزمن'},file:'source$',ext:'.ts',kind:'svc'}],
    [{id:'op',role:{en:'the decision',ar:'القرار'},file:'the four operators',ext:'.ts',kind:'cfg'},
     {id:'out',role:{en:'what reaches you',ar:'اللي بيوصلك'},file:'result$',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'src',to:'op',a:'bottom',b:'top',label:'a new event arrives',packet:'"ang"'},
    {id:'e2',from:'op',to:'out',a:'right',b:'left',label:'keep / cancel / queue',packet:'result'}
  ],
  files:[
    {id:'op',name:'the four, in one sentence each',lang:'ts',tag:{en:'memorise this',ar:'احفظ دي'},code:[
      "// switchMap   CANCEL the previous, keep only the newest",
      "//             → search boxes, route param changes, filters",
      "",
      "// exhaustMap  IGNORE the new one while the previous is running",
      "//             → save buttons, login, anything you must not do twice",
      "",
      "// concatMap   QUEUE them, run strictly in order, one at a time",
      "//             → ordered writes, audit logs, a sequence of steps",
      "",
      "// mergeMap    RUN THEM ALL at once, results arrive in any order",
      "//             → independent parallel work, uploading 10 files",
      "",
      "// if you cannot decide, switchMap is right for reads and",
      "// exhaustMap is right for writes. that covers 90% of real cases."]},
    {id:'switch',name:'switchMap — a search box',lang:'ts',tag:{en:'reads',ar:'قراية'},code:[
      "// the user types \"a\", \"an\", \"ang\". three requests start.",
      "// the answer for \"a\" is now worthless — cancel it.",
      "",
      "readonly results = toSignal(",
      "  toObservable(this.term).pipe(",
      "    debounceTime(300),",
      "    switchMap(q => this.http.get<Product[]>(`/api/search?q=${q}`)),",
      "  ),",
      "  { initialValue: [] },",
      ");",
      "",
      "// WITHOUT switchMap: the slow response for \"a\" can arrive AFTER",
      "// the fast one for \"ang\", and the screen shows results for a",
      "// query the user already finished typing. classic race condition."]},
    {id:'exhaust',name:'exhaustMap — a save button',lang:'ts',tag:{en:'writes',ar:'كتابة'},code:[
      "// the user double-clicks Save. that must be ONE order, not two.",
      "",
      "this.saveClicks$.pipe(",
      "  exhaustMap(() => this.http.post('/api/orders', this.draft())),",
      "  takeUntilDestroyed(),",
      ").subscribe(order => this.router.navigate(['/orders', order.id]));",
      "",
      "// while the POST is in flight, further clicks are DROPPED.",
      "// switchMap here would cancel the first request — but the server",
      "// may already have created the order. exhaustMap is the safe one."]},
    {id:'concat',name:'concatMap / mergeMap',lang:'ts',tag:{en:'order vs speed',ar:'الترتيب مقابل السرعة'},code:[
      "// concatMap — order matters more than speed.",
      "// each request waits for the previous one to finish.",
      "this.edits$.pipe(",
      "  concatMap(edit => this.http.patch(`/api/doc/${edit.id}`, edit)),",
      ").subscribe();",
      "// edit 1 lands before edit 2. always. even if 1 is slow.",
      "",
      "// mergeMap — they are independent, so run them all now.",
      "from(files).pipe(",
      "  mergeMap(file => this.upload(file), 3),   // 3 = max concurrent",
      ").subscribe();",
      "// 10 files, 3 at a time, results arrive in whatever order.",
      "",
      "// mergeMap on a search box is the WORST option: every keystroke",
      "// fires, nothing is cancelled, and results arrive out of order."]},
    {id:'rest',name:'the rest of the useful ones',lang:'ts',tag:{en:'the short list',ar:'اللستة القصيرة'},code:[
      "map(x => x.name)                  // transform each value",
      "filter(x => x.active)             // drop values you do not want",
      "tap(x => console.log(x))          // side effect, changes nothing",
      "",
      "debounceTime(300)                 // wait for a pause",
      "distinctUntilChanged()            // ignore a repeat of the same value",
      "startWith(null)                   // emit immediately, then carry on",
      "",
      "catchError(e => of([]))           // recover — MUST return an observable",
      "retry({ count: 3, delay: 1000 })  // try again on failure",
      "finalize(() => this.busy.set(false))  // runs on complete OR error",
      "",
      "combineLatest([a$, b$])           // latest of each, whenever either fires",
      "forkJoin([a$, b$])                // wait for ALL to complete, once",
      "",
      "takeUntilDestroyed()              // Angular's unsubscribe. always."]}
  ],
  steps:[
    {t:{en:'All four take a value and start something async with it. The difference is only what they do when the <b>next</b> value arrives before the first has finished.',
        ar:'الأربعة بياخدوا قيمة ويبدأوا بيها حاجة async. والفرق بس في اللي بيعملوه لما القيمة <b>اللي بعدها</b> توصل قبل ما الأولى تخلص.'},
     edge:'e1',hl:{op:[1,4,7,10]}},
    {t:{en:'<code class="i">switchMap</code> cancels the previous one. For a <b>read</b>, the old answer is worthless the moment you ask a new question — so this is almost always right.',
        ar:'<code class="i">switchMap</code> بيلغي اللي قبله. وفي حالة <b>القراية</b>، الإجابة القديمة مبقالهاش قيمة أول ما تسأل سؤال جديد — فدي تقريباً دايماً الصح.'},
     edge:'e2',hl:{switch:[7,12,13,14]}},
    {t:{en:'<code class="i">exhaustMap</code> ignores the new one. For a <b>write</b>, cancelling is dangerous — the server may already have acted on the request you cancelled.',
        ar:'<code class="i">exhaustMap</code> بيتجاهل الجديد. وفي حالة <b>الكتابة</b>، الإلغاء خطر — السيرفر يمكن يكون نفّذ الريكويست اللي إنت ألغيته.'},
     hl:{exhaust:[4,9,10]}},
    {t:{en:'<code class="i">concatMap</code> queues them so order is guaranteed. <code class="i">mergeMap</code> runs everything at once, which is fastest and gives you no ordering at all.',
        ar:'<code class="i">concatMap</code> بيصفّهم فالترتيب مضمون. و<code class="i">mergeMap</code> بيشغّل كل حاجة مرة واحدة، وده الأسرع وبيديك صفر ترتيب.'},
     hl:{concat:[4,6,10,12]}},
    {t:{en:'The rest of RxJS is a long list, but this short one covers nearly all real Angular code. Learn these thirteen and you can read almost any pipe you meet.',
        ar:'باقي RxJS لستة طويلة، بس اللستة القصيرة دي بتغطي تقريباً كل كود أنجولار حقيقي. اتعلم التلتاشر دول وهتقدر تقرا أي pipe هتقابله.'},
     hl:{rest:[1,4,8,14]}}
  ],
  example:[
    {
      name:{en:'The same page, both operators', ar:'نفس الصفحة، والـ operator الاتنين'},
      what:{
        en:'A checkout page reads and writes. The address lookup is a read that should follow the newest keystroke; the Place Order button is a write that must happen at most once. Same file, two different operators, for exactly the reason above.',
        ar:'صفحة الدفع بتقرا وبتكتب. البحث عن العنوان قراية المفروض تتبع آخر حرف اتكتب؛ وزرار تنفيذ الأوردر كتابة لازم تحصل مرة واحدة بالكتير. نفس الملف، operator مختلفين، بالظبط للسبب اللي فوق.'},
      files:[
        {id:'c1',name:'checkout.component.ts',lang:'ts',tag:{en:'read + write',ar:'قراية + كتابة'},code:[
          "export class Checkout {",
          "  readonly postcode = signal('');",
          "  private readonly place$ = new Subject<void>();",
          "",
          "  // READ → switchMap. only the newest postcode matters.",
          "  readonly addresses = toSignal(",
          "    toObservable(this.postcode).pipe(",
          "      debounceTime(400),",
          "      filter(p => p.length >= 3),",
          "      distinctUntilChanged(),",
          "      switchMap(p => this.api.lookup(p)),",
          "    ),",
          "    { initialValue: [] },",
          "  );",
          "",
          "  // WRITE → exhaustMap. a double-click must not double-charge.",
          "  constructor() {",
          "    this.place$.pipe(",
          "      exhaustMap(() => this.api.placeOrder(this.draft()).pipe(",
          "        finalize(() => this.busy.set(false)),",
          "      )),",
          "      takeUntilDestroyed(),",
          "    ).subscribe(o => this.router.navigate(['/orders', o.id]));",
          "  }",
          "",
          "  place() { this.busy.set(true); this.place$.next(); }",
          "}"]}
      ]
    },
    {
      name:{en:'What goes wrong with the wrong one', ar:'إيه اللي بيبوظ مع الغلط'},
      what:{
        en:'These are not theoretical. Each line below is a bug that has shipped to production in a real application, and each one is a single word away from being correct.',
        ar:'دي مش نظرية. كل سطر تحت ده باج اتنشر فعلاً في تطبيق حقيقي، وكل واحد فيهم كلمة واحدة بينه وبين الصح.'},
      files:[
        {id:'w1',name:'the four failures',lang:'ts',tag:{en:'real bugs',ar:'باجات حقيقية'},code:[
          "// mergeMap on a search box",
          "//   → results for \"a\" overwrite results for \"ang\". flickering,",
          "//     wrong data on screen, only on slow connections.",
          "",
          "// switchMap on a save button",
          "//   → the first POST is cancelled client-side, but the server",
          "//     already created the order. duplicate orders, no error.",
          "",
          "// concatMap on a search box",
          "//   → every keystroke queues. type 10 characters and you wait",
          "//     for 10 sequential requests before seeing anything.",
          "",
          "// exhaustMap on a retry button",
          "//   → the user clicks Retry, nothing happens, because a slow",
          "//     failing request is still 'in flight'. feels broken."]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>Reads use <code class="i">switchMap</code>, writes use <code class="i">exhaustMap</code>.</b> If you remember one sentence from this page, that is the one.',
     ar:'<b>القراية بـ <code class="i">switchMap</code>، والكتابة بـ <code class="i">exhaustMap</code>.</b> لو هتفتكر جملة واحدة من الصفحة دي، تبقى دي.'},
    {en:'<code class="i">catchError</code> must return an <b>observable</b> — <code class="i">of([])</code>, not <code class="i">[]</code>. And catching at the outer level ends the stream for good; catch <b>inside</b> the inner pipe to keep the outer one alive.',
     ar:'<code class="i">catchError</code> لازم يرجّع <b>observable</b> — <code class="i">of([])</code> مش <code class="i">[]</code>. والمسك في المستوى البرّاني بينهي الستريم للأبد؛ امسك <b>جوه</b> الـ pipe الجوّاني عشان البرّاني يفضل عايش.'},
    {en:'Nesting <code class="i">subscribe</code> inside <code class="i">subscribe</code> is what these operators exist to replace. If you see it, one of the four is the fix.',
     ar:'إنك تحط <code class="i">subscribe</code> جوه <code class="i">subscribe</code> ده بالظبط اللي الـ operators دي موجودة عشان تستبدله. لو شفته، واحد من الأربعة هو الحل.'},
    {en:'<code class="i">forkJoin</code> waits for every source to <b>complete</b>. Give it a stream that never completes and it never emits at all — a very quiet way to hang a page.',
     ar:'<code class="i">forkJoin</code> بيستنى كل المصادر <b>تقفل</b>. وادّيله ستريم عمره ما بيقفل وهو عمره ما هيطلّع حاجة — طريقة هادية جداً إنك تعلّق صفحة.'},
    {en:'Order inside <code class="i">pipe()</code> matters. <code class="i">debounceTime</code> before <code class="i">switchMap</code> saves requests; after it, you have already made them.',
     ar:'الترتيب جوه <code class="i">pipe()</code> مهم. <code class="i">debounceTime</code> قبل <code class="i">switchMap</code> بيوفر ريكويستات؛ وبعده، تبقى عملتهم خلاص.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'testing',
  badge:{en:'Confidence', ar:'الثقة'},
  title:{en:'Testing a component without a browser', ar:'تختبر component من غير براوزر'},
  lead:{
    en:`A test is a second opinion about whether your code does what you think. Angular gives you a real component, a real template and a fake everything-else. Start with three kinds of test and ignore the rest until you need them.`,
    ar:`التست ده رأي تاني في إن كودك بيعمل اللي إنت فاكره ولا لأ. أنجولار بيديك component حقيقي وتمبلت حقيقي وكل حاجة تانية مزيفة. ابدأ بتلات أنواع تستات وتجاهل الباقي لحد ما تحتاجه.`
  },
  nodes:[
    [{id:'spec',role:{en:'the test',ar:'التست'},file:'cart.component.spec',ext:'.ts',kind:'cfg'}],
    [{id:'cmp',role:{en:'the real component',ar:'الـ component الحقيقي'},file:'cart.component',ext:'.ts',kind:'ts'},
     {id:'fake',role:{en:'the fake service',ar:'الـ service المزيفة'},file:'cart.store',ext:'.ts',kind:'svc'}]
  ],
  edges:[
    {id:'e1',from:'spec',to:'fake',a:'bottom',b:'right',label:'providers: [...]',packet:'stub'},
    {id:'e2',from:'spec',to:'cmp',a:'bottom',b:'top',label:'createComponent',packet:'fixture'},
    {id:'e3',from:'cmp',to:'spec',a:'left',b:'left',label:'nativeElement',packet:'the DOM'}
  ],
  files:[
    {id:'spec',name:'cart.component.spec.ts',lang:'ts',tag:{en:'the shape of every test',ar:'شكل أي تست'},code:[
      "import { TestBed } from '@angular/core/testing';",
      "import { Cart } from './cart.component';",
      "import { CartStore } from './cart.store';",
      "",
      "describe('Cart', () => {",
      "  // a fake store: same shape, no real logic",
      "  const store = {",
      "    items: signal<Item[]>([]),",
      "    add: jasmine.createSpy('add'),",
      "  };",
      "",
      "  beforeEach(() => TestBed.configureTestingModule({",
      "    imports: [Cart],                                  // the real component",
      "    providers: [{ provide: CartStore, useValue: store }],  // the fake service",
      "  }));",
      "",
      "  it('shows the item count', async () => {",
      "    store.items.set([{ id: '1', title: 'Mouse', price: 100 }]);",
      "",
      "    const fixture = TestBed.createComponent(Cart);",
      "    await fixture.whenStable();          // zoneless-safe. not detectChanges().",
      "",
      "    const text = fixture.nativeElement.textContent;",
      "    expect(text).toContain('Cart (1)');",
      "  });",
      "});"]},
    {id:'kinds',name:'the three tests worth writing',lang:'ts',tag:{en:'start here',ar:'ابدأ من هنا'},code:[
      "// 1. A PURE FUNCTION — no TestBed, no Angular, instant.",
      "//    computed values, validators, formatters, reducers.",
      "it('adds VAT', () => {",
      "  expect(withVat(100)).toBe(114);",
      "});",
      "",
      "// 2. A COMPONENT'S OUTPUT — given this state, what is on screen?",
      "it('shows the empty message', async () => {",
      "  store.items.set([]);",
      "  const f = TestBed.createComponent(Cart);",
      "  await f.whenStable();",
      "  expect(f.nativeElement.textContent).toContain('Your cart is empty');",
      "});",
      "",
      "// 3. AN INTERACTION — given this click, what happened?",
      "it('adds an item when the button is clicked', async () => {",
      "  const f = TestBed.createComponent(Cart);",
      "  await f.whenStable();",
      "",
      "  f.nativeElement.querySelector('button.add').click();",
      "  await f.whenStable();",
      "",
      "  expect(store.add).toHaveBeenCalled();",
      "});"]},
    {id:'inputs',name:'setting inputs and reading outputs',lang:'ts',tag:{en:'the component API',ar:'واجهة الـ component'},code:[
      "it('renders the product title', async () => {",
      "  const f = TestBed.createComponent(ProductCard);",
      "",
      "  // set a signal input from the outside",
      "  f.componentRef.setInput('product', { id: '1', title: 'Mouse', price: 100 });",
      "  await f.whenStable();",
      "",
      "  expect(f.nativeElement.querySelector('h3').textContent).toBe('Mouse');",
      "});",
      "",
      "it('emits when removed', async () => {",
      "  const f = TestBed.createComponent(ProductCard);",
      "  f.componentRef.setInput('product', product);",
      "",
      "  let emitted: string | undefined;",
      "  f.componentInstance.removed.subscribe(id => emitted = id);",
      "",
      "  f.nativeElement.querySelector('.remove').click();",
      "  expect(emitted).toBe('1');",
      "});"]},
    {id:'http',name:'testing something that calls the server',lang:'ts',tag:{en:'no real network',ar:'من غير شبكة حقيقية'},code:[
      "import { provideHttpClient } from '@angular/common/http';",
      "import { provideHttpClientTesting, HttpTestingController }",
      "  from '@angular/common/http/testing';",
      "",
      "beforeEach(() => TestBed.configureTestingModule({",
      "  providers: [provideHttpClient(), provideHttpClientTesting()],",
      "}));",
      "",
      "it('loads the user', () => {",
      "  const api = TestBed.inject(UserApi);",
      "  const http = TestBed.inject(HttpTestingController);",
      "",
      "  let result: User | undefined;",
      "  api.load('u1').subscribe(u => result = u);",
      "",
      "  // assert the request, then answer it yourself",
      "  const req = http.expectOne('/api/users/u1');",
      "  expect(req.request.method).toBe('GET');",
      "  req.flush({ id: 'u1', name: 'Mona' });",
      "",
      "  expect(result?.name).toBe('Mona');",
      "  http.verify();      // fails if any request went unanswered",
      "});"]},
    {id:'fake',name:'what to fake, and what not to',lang:'ts',tag:{en:'judgement',ar:'الحكم'},code:[
      "// FAKE: anything slow, remote, random or outside your control.",
      "//   HTTP, the router, timers, Date.now, localStorage, analytics.",
      "",
      "// DO NOT FAKE: your own components, your own pipes, your own",
      "//   computed values. A test full of fakes tests only the fakes.",
      "",
      "// a spy remembers how it was called",
      "const store = { add: jasmine.createSpy('add') };",
      "expect(store.add).toHaveBeenCalledWith(item);",
      "",
      "// the router, faked",
      "providers: [{ provide: Router, useValue: { navigate: jasmine.createSpy() } }]"]}
  ],
  steps:[
    {t:{en:'<code class="i">TestBed.configureTestingModule</code> builds a miniature app: the real component under test, and stand-ins for everything it depends on.',
        ar:'<code class="i">TestBed.configureTestingModule</code> بيبني تطبيق مصغّر: الـ component الحقيقي اللي بتختبره، وبدائل لكل حاجة بيعتمد عليها.'},
     edge:'e1',hl:{spec:[12,13,14]}},
    {t:{en:'Because the service is provided by a token, swapping it for a fake is one line. This is dependency injection paying for itself.',
        ar:'وعشان الـ service بتيجي من خلال token، استبدالها بواحدة مزيفة سطر واحد. ودي الـ dependency injection بترد تكلفتها.'},
     hl:{spec:[7,8,9,10]}},
    {t:{en:'<code class="i">createComponent</code> gives you a <b>fixture</b> — the component instance plus its real rendered DOM.',
        ar:'<code class="i">createComponent</code> بيديك <b>fixture</b> — نسخة الـ component مع الـ DOM الحقيقي المرسوم بتاعها.'},
     edge:'e2',hl:{spec:[20,21]}},
    {t:{en:'Then you assert on what a <b>user</b> would see. Query the DOM, read the text, click the button — not the private fields.',
        ar:'وبعدين بتتأكد من اللي <b>المستخدم</b> هيشوفه. دوّر في الـ DOM، اقرا النص، دوس الزرار — مش الحقول الخاصة.'},
     edge:'e3',hl:{spec:[23,24],kinds:[19,20,21,23]}},
    {t:{en:'Set inputs with <code class="i">componentRef.setInput()</code>, and let <code class="i">HttpTestingController</code> answer requests so no real network is involved.',
        ar:'حط الـ inputs بـ <code class="i">componentRef.setInput()</code>، وسيب <code class="i">HttpTestingController</code> يرد على الريكويستات فمفيش شبكة حقيقية في الموضوع.'},
     hl:{inputs:[5,6],http:[17,19,22]}},
    {t:{en:'The judgement call is what to fake. Fake what is slow, remote or random; keep your own code real, or the test proves nothing.',
        ar:'والقرار هنا هو تزيّف إيه. زيّف اللي بطيء أو بعيد أو عشوائي؛ وسيب كودك حقيقي، وإلا التست مش بيثبت حاجة.'},
     hl:{fake:[1,2,4,5,8]}}
  ],
  example:[
    {
      name:{en:'The first test worth writing today', ar:'أول تست يستاهل تكتبه النهاردة'},
      what:{
        en:'Do not start by testing components. Start with the pure functions — validators, formatters, computed logic. They need no TestBed, run in milliseconds, and catch the bugs that actually bite.',
        ar:'متبدأش بتست الـ components. ابدأ بالدوال النضيفة — الفاليديتورز والمنسّقات والمنطق المحسوب. مش محتاجة TestBed، وبتشتغل في أجزاء من الثانية، وبتمسك الباجات اللي بتوجع فعلاً.'},
      files:[
        {id:'e1',name:'money.ts',lang:'ts',tag:{en:'the code',ar:'الكود'},code:[
          "export function withVat(amount: number, rate = 0.14): number {",
          "  return Math.round(amount * (1 + rate) * 100) / 100;",
          "}",
          "",
          "export function matchFields(a: string, b: string): ValidatorFn {",
          "  return g => g.get(a)?.value === g.get(b)?.value ? null : { mismatch: true };",
          "}"]},
        {id:'e2',name:'money.spec.ts',lang:'ts',tag:{en:'no Angular at all',ar:'من غير أنجولار خالص'},code:[
          "describe('withVat', () => {",
          "  it('adds 14% by default', () => {",
          "    expect(withVat(100)).toBe(114);",
          "  });",
          "",
          "  it('rounds to two decimals', () => {",
          "    expect(withVat(99.99)).toBe(113.99);",
          "  });",
          "",
          "  it('handles zero', () => {",
          "    expect(withVat(0)).toBe(0);",
          "  });",
          "});",
          "",
          "// three tests, no TestBed, runs in under a millisecond.",
          "// this is the highest value per line of test code you will write."]}
      ]
    }
  ],
  gotchas:[
    {en:'Test what a user can observe — rendered text, clicks, emitted events. Tests that assert on private fields break every time you refactor and prove nothing.',
     ar:'اختبر اللي المستخدم يقدر يشوفه — النص المرسوم، والكليكات، والأحداث المبعوتة. التستات اللي بتتأكد من الحقول الخاصة بتتكسر مع كل إعادة هيكلة ومش بتثبت حاجة.'},
    {en:'Under zoneless, <code class="i">await fixture.whenStable()</code> replaces <code class="i">fixture.detectChanges()</code>. And <code class="i">fakeAsync</code> / <code class="i">tick()</code> depend on zone.js — they do not work zoneless.',
     ar:'مع الـ zoneless، <code class="i">await fixture.whenStable()</code> بتحل محل <code class="i">fixture.detectChanges()</code>. و<code class="i">fakeAsync</code> و<code class="i">tick()</code> بيعتمدوا على zone.js — فمش شغالين zoneless.'},
    {en:'A test that fakes everything tests only the fakes. Fake what is slow, remote or random; keep your own code real.',
     ar:'التست اللي بيزيّف كل حاجة بيختبر المزيّف بس. زيّف اللي بطيء أو بعيد أو عشوائي؛ وسيب كودك إنت حقيقي.'},
    {en:'<code class="i">http.verify()</code> in an <code class="i">afterEach</code> catches requests you never answered — a common source of tests that pass individually and fail together.',
     ar:'<code class="i">http.verify()</code> في <code class="i">afterEach</code> بتمسك الريكويستات اللي مردتش عليها — سبب شائع لتستات بتنجح لوحدها وبتفشل مع بعض.'},
    {en:'Do not chase a coverage percentage. Ten tests on the logic that handles money are worth more than two hundred on getters.',
     ar:'متجريش ورا نسبة تغطية. عشر تستات على المنطق اللي بيتعامل مع الفلوس أهم من ميتين تست على getters.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'error-handling',
  badge:{en:'When things fail', ar:'لما الحاجات تفشل'},
  title:{en:'Failing well: retry, report, recover', ar:'تفشل بشكل كويس: إعادة، إبلاغ، تعافي'},
  lead:{
    en:`Networks drop, servers return 500, and tokens expire. The difference between an app that feels solid and one that feels broken is almost never the happy path — it is <b>what happens on the sad one</b>, and whether the person can see it and do something about it.`,
    ar:`الشبكة بتقطع، والسيرفر بيرجّع 500، والتوكنز بتنتهي. والفرق بين تطبيق حاسس إنه متين وتطبيق حاسس إنه بايظ تقريباً عمره ما بيكون المسار السعيد — الفرق في <b>اللي بيحصل في المسار الحزين</b>، وهل الشخص يقدر يشوفه ويعمل حاجة بخصوصه.`
  },
  nodes:[
    [{id:'int',role:{en:'catches HTTP failures',ar:'بيمسك أخطاء HTTP'},file:'error',ext:'.interceptor.ts',kind:'cfg'},
     {id:'glob',role:{en:'catches everything else',ar:'بيمسك الباقي'},file:'global-error',ext:'.handler.ts',kind:'cfg'}],
    [{id:'toast',role:{en:'tells the person',ar:'بيقول للشخص'},file:'toast.service',ext:'.ts',kind:'svc'},
     {id:'cmp',role:{en:'offers a way out',ar:'بيدي مخرج'},file:'orders.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'int',to:'toast',a:'bottom',b:'top',label:'retry then report',packet:'500'},
    {id:'e2',from:'glob',to:'toast',a:'bottom',b:'right',label:'unexpected error',packet:'crash'},
    {id:'e3',from:'toast',to:'cmp',a:'right',b:'left',label:'and a retry button',packet:'recover'}
  ],
  files:[
    {id:'int',name:'error.interceptor.ts',lang:'ts',tag:{en:'one place for all HTTP',ar:'مكان واحد لكل HTTP'},code:[
      "export const errorInterceptor: HttpInterceptorFn = (req, next) => {",
      "  const toasts = inject(Toasts);",
      "  const session = inject(Session);",
      "",
      "  return next(req).pipe(",
      "    // retry only what is worth retrying: transient failures.",
      "    retry({",
      "      count: 2,",
      "      delay: (err, n) => {",
      "        const retryable = err.status === 0 || err.status >= 500;",
      "        if (!retryable) throw err;              // 400s will never succeed",
      "        return timer(500 * Math.pow(2, n));     // 500ms, 1s — back off",
      "      },",
      "    }),",
      "",
      "    catchError((err: HttpErrorResponse) => {",
      "      if (err.status === 401) { session.signOut(); }",
      "      else if (err.status === 0)   toasts.show('You appear to be offline.');",
      "      else if (err.status >= 500)  toasts.show('Our server had a problem. Try again shortly.');",
      "      else if (err.status === 404) { /* the component will handle this */ }",
      "      else toasts.show(err.error?.message ?? 'Something went wrong.');",
      "",
      "      return throwError(() => err);   // let the caller decide too",
      "    }),",
      "  );",
      "};"]},
    {id:'glob',name:'global-error.handler.ts',lang:'ts',tag:{en:'the last resort',ar:'الملاذ الأخير'},code:[
      "import { ErrorHandler, Injectable, inject } from '@angular/core';",
      "",
      "@Injectable()",
      "export class GlobalErrorHandler implements ErrorHandler {",
      "  private readonly toasts = inject(Toasts);",
      "",
      "  handleError(error: unknown) {",
      "    // this catches what nothing else did: a thrown error in a",
      "    // component, a bad computed, a null dereference in a template.",
      "    console.error(error);",
      "    reportToSentry(error);",
      "    this.toasts.show('Something unexpected happened.', 'error');",
      "  }",
      "}",
      "",
      "// register it:",
      "// providers: [{ provide: ErrorHandler, useClass: GlobalErrorHandler }]"]},
    {id:'cmp',name:'orders.component.html',lang:'html',tag:{en:'a way out',ar:'مخرج'},code:[
      "@if (orders.isLoading()) {",
      "  <app-skeleton />",
      "} @else if (orders.error()) {",
      "  <div class=\"error-state\" role=\"alert\">",
      "    <h3>We could not load your orders</h3>",
      "    <p>This is usually temporary.</p>",
      "    <button (click)=\"orders.reload()\">Try again</button>",
      "  </div>",
      "} @else if (!orders.value()?.length) {",
      "  <div class=\"empty-state\">",
      "    <h3>No orders yet</h3>",
      "    <a routerLink=\"/shop\">Start shopping</a>",
      "  </div>",
      "} @else {",
      "  @for (o of orders.value()!; track o.id) { <app-order-row [order]=\"o\" /> }",
      "}"]},
    {id:'msg',name:'what to actually say',lang:'ts',tag:{en:'the words matter',ar:'الكلام مهم'},code:[
      "// BAD: tells the person nothing, and blames them",
      "'Error 500'",
      "'Invalid input'",
      "'Oops! Something went wrong :('",
      "",
      "// GOOD: what happened, whose fault, what to do next",
      "'We could not save your changes — your draft is still here. Try again.'",
      "'That email is already registered. Sign in instead?'",
      "'You appear to be offline. We will retry when you reconnect.'",
      "",
      "// three rules:",
      "//   1. never show a stack trace or an error code to a user",
      "//   2. say whether their work is safe",
      "//   3. give them exactly one obvious next action"]},
    {id:'toast',name:'toast.service.ts',lang:'ts',tag:{en:'one place to report',ar:'مكان واحد للإبلاغ'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class Toasts {",
      "  private readonly _all = signal<Toast[]>([]);",
      "  readonly all = this._all.asReadonly();",
      "",
      "  show(text: string, kind: Toast['kind'] = 'info', ms = 5000) {",
      "    const id = crypto.randomUUID();",
      "    this._all.update(l => [...l, { id, text, kind }]);",
      "    if (ms) setTimeout(() => this.dismiss(id), ms);",
      "  }",
      "",
      "  dismiss(id: string) { this._all.update(l => l.filter(t => t.id !== id)); }",
      "}"]}
  ],
  steps:[
    {t:{en:'Retry first — but only what can succeed on a second try. A network blip or a 503 is worth retrying; a 400 or a 404 will fail identically forever.',
        ar:'أعد المحاولة الأول — بس بس اللي ممكن ينجح في المرة التانية. قطعة نت أو 503 تستاهل إعادة؛ أما 400 أو 404 فهيفشلوا بنفس الشكل للأبد.'},
     hl:{int:[7,8,10,11]}},
    {t:{en:'Back off between attempts. Three immediate retries against a struggling server make the outage worse; <code class="i">500ms</code>, then <code class="i">1s</code>, gives it room.',
        ar:'خفّف بين المحاولات. تلات إعادات فورية على سيرفر تعبان بتزوّد العطل؛ أما <code class="i">500ms</code> وبعدين <code class="i">1s</code> فبتديه مساحة.'},
     hl:{int:[12]}},
    {t:{en:'One interceptor turns every HTTP failure in the app into a message a human can read — written once, applied to every request automatically.',
        ar:'interceptor واحد بيحوّل أي فشل HTTP في التطبيق لرسالة إنسان يقدر يقراها — مكتوبة مرة، ومتطبقة على كل ريكويست لوحدها.'},
     edge:'e1',hl:{int:[17,19,20,21]}},
    {t:{en:'A global <code class="i">ErrorHandler</code> catches what nothing else did — a thrown error in a component, a null dereference in a template. Without it those crash silently in production.',
        ar:'الـ <code class="i">ErrorHandler</code> العام بيمسك اللي محدش مسكه — ايرور اترمى في component، أو null اتقرا في تمبلت. ومن غيره دول بيقعوا في صمت في الإنتاج.'},
     edge:'e2',hl:{glob:[7,10,11,12]}},
    {t:{en:'Then give the person a way out. An error state with a <b>Try again</b> button is the difference between “broken” and “busy”.',
        ar:'وبعدين ادي الشخص مخرج. حالة الايرور اللي فيها زرار <b>حاول تاني</b> هي الفرق بين «بايظ» و«مشغول».'},
     edge:'e3',hl:{cmp:[3,5,7],msg:[7,8,12,13]}},
    {t:{en:'All of it reports through one small service, so there is exactly one place that decides how a message looks.',
        ar:'وكل ده بيبلّغ من خلال service صغيرة واحدة، فيبقى فيه مكان واحد بالظبط بيقرر شكل الرسالة.'},
     hl:{toast:[6,7,8]}}
  ],
  example:[
    {
      name:{en:'The four states every screen has', ar:'الأربع حالات اللي في كل شاشة'},
      what:{
        en:'Beginners build the success state and stop. Every screen that loads anything actually has four, and the other three are where an app earns trust. Write them in this order and you will never ship a blank screen again.',
        ar:'المبتدئين بيبنوا حالة النجاح ويقفوا. وأي شاشة بتحمّل حاجة عندها أربع حالات فعلاً، والتلاتة التانيين هما اللي التطبيق بيكسب فيهم الثقة. اكتبهم بالترتيب ده وعمرك ما هتنشر شاشة بيضا تاني.'},
      files:[
        {id:'st1',name:'the checklist',lang:'html',tag:{en:'every list screen',ar:'كل شاشة لستة'},code:[
          "@if (data.isLoading())      { <!-- 1. LOADING  — a skeleton, not a spinner --> }",
          "@else if (data.error())     { <!-- 2. ERROR    — what happened + a retry  --> }",
          "@else if (!data.value()?.length) { <!-- 3. EMPTY — why, and what to do   --> }",
          "@else                       { <!-- 4. SUCCESS  — the actual content      --> }",
          "",
          "<!-- a skeleton beats a spinner: it holds the layout, so nothing",
          "     jumps when the data lands. -->",
          "",
          "<!-- an EMPTY state is not an error. \"No orders yet — start shopping\"",
          "     is a different message from \"We could not load your orders\". -->"]}
      ]
    }
  ],
  gotchas:[
    {en:'Do not retry a <code class="i">POST</code> blindly. If the server already created the order, a retry creates a second one. Retry reads freely; retry writes only when the endpoint is idempotent.',
     ar:'متعيدش <code class="i">POST</code> بشكل أعمى. لو السيرفر عمل الأوردر خلاص، الإعادة هتعمل واحد تاني. أعد القراية براحتك؛ وأعد الكتابة بس لما الـ endpoint يكون idempotent.'},
    {en:'Swallowing an error with <code class="i">catchError(() =&gt; of(null))</code> and no message is worse than crashing — the person sees nothing happen and cannot tell whether it worked.',
     ar:'إنك تبلع الايرور بـ <code class="i">catchError(() =&gt; of(null))</code> من غير رسالة أوحش من الكراش — الشخص مش بيشوف حاجة حصلت ومش عارف نجحت ولا لأ.'},
    {en:'An empty state is not an error state. “No results for that search” and “We could not reach the server” need different words and different buttons.',
     ar:'الحالة الفاضية مش حالة ايرور. «مفيش نتايج للبحث ده» و«مقدرناش نوصل للسيرفر» محتاجين كلام مختلف وزراير مختلفة.'},
    {en:'Never put a stack trace or an error code in front of a user. Log it, report it to your monitoring, and show a sentence a human wrote.',
     ar:'عمرك ما تحط stack trace أو كود ايرور قدام مستخدم. سجّله، وابعته للمراقبة، وورّي جملة إنسان كتبها.'},
    {en:'A global <code class="i">ErrorHandler</code> is the only thing that catches errors thrown inside templates and effects. Without one, they vanish into the console in production.',
     ar:'الـ <code class="i">ErrorHandler</code> العام هو الحاجة الوحيدة اللي بتمسك الأخطاء اللي بتترمي جوه التمبلتس والـ effects. ومن غيره بيختفوا في الكونسول في الإنتاج.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'rxjs-fundamentals',
  badge:{en:'Streams', ar:'الستريمز'},
  title:{en:'Observable, Subject, BehaviorSubject', ar:'Observable و Subject و BehaviorSubject'},
  lead:{
    en:`RxJS models <b>values arriving over time</b>. An Observable is a recipe that does nothing until someone subscribes. A Subject is an Observable you can also push into. Understanding those two sentences explains ninety percent of the RxJS you will meet in Angular.`,
    ar:`RxJS بيمثّل <b>قيم بتوصل عبر الزمن</b>. الـ Observable وصفة مبتعملش حاجة لحد ما حد يعمل subscribe. والـ Subject هو Observable تقدر تدفع فيه كمان. وفهم الجملتين دول بيشرح تسعين في المية من الـ RxJS اللي هتقابله في أنجولار.`
  },
  nodes:[
    [{id:'obs',role:{en:'a recipe',ar:'وصفة'},file:'Observable',ext:'',kind:'svc'}],
    [{id:'sub',role:{en:'runs it',ar:'بيشغّلها'},file:'subscribe()',ext:'',kind:'ts'},
     {id:'subj',role:{en:'you can push into it',ar:'تقدر تدفع فيه'},file:'Subject',ext:'',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'sub',to:'obs',a:'top',b:'bottom',label:'subscribe()',packet:'start'},
    {id:'e2',from:'obs',to:'sub',a:'left',b:'left',label:'next / error / complete',packet:'value'},
    {id:'e3',from:'subj',to:'obs',a:'top',b:'right',label:'.next(v)',packet:'push'}
  ],
  files:[
    {id:'obs',name:'what an Observable actually is',lang:'ts',tag:{en:'the recipe',ar:'الوصفة'},code:[
      "import { Observable } from 'rxjs';",
      "",
      "// nothing in here runs until somebody subscribes.",
      "const clock$ = new Observable<number>(observer => {",
      "  let n = 0;",
      "  const id = setInterval(() => observer.next(n++), 1000);",
      "",
      "  // the teardown. runs on unsubscribe, error or complete.",
      "  return () => clearInterval(id);",
      "});",
      "",
      "// three callbacks, all optional",
      "const sub = clock$.subscribe({",
      "  next:     v => console.log('tick', v),",
      "  error:    e => console.error(e),        // ends the stream",
      "  complete: () => console.log('done'),    // also ends it",
      "});",
      "",
      "sub.unsubscribe();     // runs the teardown above",
      "",
      "// CONVENTION: a variable holding an observable ends in $"]},
    {id:'cold',name:'cold vs hot — the one that surprises people',lang:'ts',tag:{en:'important',ar:'مهم'},code:[
      "// COLD: each subscriber gets its OWN independent run.",
      "const request$ = this.http.get<User[]>('/api/users');",
      "",
      "request$.subscribe();      // HTTP request #1",
      "request$.subscribe();      // HTTP request #2 — a second, separate call",
      "",
      "// this is why two  | async  on the same observable = two requests.",
      "",
      "// HOT: one source, shared by everyone. a Subject is hot.",
      "const clicks$ = new Subject<void>();",
      "clicks$.subscribe(() => console.log('a'));",
      "clicks$.subscribe(() => console.log('b'));",
      "clicks$.next();            // logs 'a' AND 'b' — one push, two listeners",
      "",
      "// make a cold one shared:",
      "const shared$ = request$.pipe(shareReplay({ bufferSize: 1, refCount: true }));"]},
    {id:'subj',name:'the three Subjects',lang:'ts',tag:{en:'pick the right one',ar:'اختار الصح'},code:[
      "import { Subject, BehaviorSubject, ReplaySubject } from 'rxjs';",
      "",
      "// Subject — no memory. subscribe late and you miss everything.",
      "const events$ = new Subject<string>();",
      "events$.next('one');",
      "events$.subscribe(v => console.log(v));   // logs NOTHING. 'one' is gone.",
      "",
      "// BehaviorSubject — holds the CURRENT value. needs an initial one.",
      "const user$ = new BehaviorSubject<User | null>(null);",
      "user$.next(mona);",
      "user$.subscribe(v => console.log(v));     // logs mona immediately",
      "user$.value;                              // and you can read it directly",
      "",
      "// ReplaySubject — remembers the last N values",
      "const log$ = new ReplaySubject<string>(3);",
      "",
      "// THE RULE: for STATE use BehaviorSubject (or, better, a signal).",
      "//           for EVENTS use Subject.",
      "// 'my subscriber gets nothing' is almost always a plain Subject",
      "// being used where BehaviorSubject was needed."]},
    {id:'ang',name:'where you meet them in Angular',lang:'ts',tag:{en:'in practice',ar:'في الشغل'},code:[
      "// COLD, completes after one value — no unsubscribe needed",
      "this.http.get<User>('/api/me')",
      "",
      "// HOT, never completes — you MUST unsubscribe",
      "this.form.valueChanges",
      "this.router.events",
      "fromEvent(window, 'resize')",
      "",
      "// the safe pattern for anything that never completes:",
      "this.form.valueChanges",
      "  .pipe(takeUntilDestroyed())",
      "  .subscribe(v => this.autosave(v));",
      "",
      "// …or avoid the question entirely:",
      "readonly value = toSignal(this.form.valueChanges, { initialValue: null });",
      "",
      "// modern Angular guidance: signals for STATE, RxJS for EVENTS",
      "// AND for anything about time, order or cancellation."]}
  ],
  steps:[
    {t:{en:'An Observable is a <b>recipe</b>. Creating one runs no code. This is why an <code class="i">http.get()</code> you never subscribed to makes no request at all.',
        ar:'الـ Observable <b>وصفة</b>. وإنشاؤه مش بيشغّل كود. وعشان كده <code class="i">http.get()</code> اللي معملتش له subscribe مش بيعمل ريكويست أصلاً.'},
     hl:{obs:[3,4,5,6]}},
    {t:{en:'<code class="i">subscribe()</code> starts it and returns a <code class="i">Subscription</code>. The function you return from the constructor is the <b>teardown</b> — that is where cleanup belongs.',
        ar:'<code class="i">subscribe()</code> بيشغّله وبيرجّع <code class="i">Subscription</code>. والدالة اللي بترجّعها من الـ constructor هي <b>التنضيف</b> — وده مكان تنضيف الموارد.'},
     edge:'e1',hl:{obs:[8,9,13,19]}},
    {t:{en:'Values arrive through <code class="i">next</code>. <code class="i">error</code> and <code class="i">complete</code> both <b>end</b> the stream permanently — after either, nothing more ever arrives.',
        ar:'القيم بتوصل عن طريق <code class="i">next</code>. و<code class="i">error</code> و<code class="i">complete</code> الاتنين بي<b>نهوا</b> الستريم للأبد — وبعد أي واحد فيهم مفيش حاجة تانية بتوصل.'},
     edge:'e2',hl:{obs:[14,15,16]}},
    {t:{en:'A <b>cold</b> observable runs separately per subscriber — two subscribes to an HTTP call means two requests. A <b>hot</b> one is shared.',
        ar:'الـ observable الـ<b>بارد</b> بيشتغل لكل مشترك لوحده — اتنين subscribe لنداء HTTP معناها ريكويستين. أما الـ<b>ساخن</b> فمشترك.'},
     hl:{cold:[2,4,5,7,10,13]}},
    {t:{en:'A <code class="i">Subject</code> is hot and has no memory; a <code class="i">BehaviorSubject</code> holds the current value. “My subscriber got nothing” is nearly always a Subject where a BehaviorSubject was needed.',
        ar:'الـ <code class="i">Subject</code> ساخن ومالوش ذاكرة؛ والـ <code class="i">BehaviorSubject</code> بيمسك القيمة الحالية. و«المشترك بتاعي مجاش له حاجة» دي تقريباً دايماً Subject في مكان كان محتاج BehaviorSubject.'},
     edge:'e3',hl:{subj:[4,5,6,9,10,11]}},
    {t:{en:'In practice: HTTP completes so it needs no cleanup; <code class="i">valueChanges</code> never does, so it needs <code class="i">takeUntilDestroyed()</code>.',
        ar:'عملياً: الـ HTTP بيقفل فمش محتاج تنضيف؛ و<code class="i">valueChanges</code> عمره ما بيقفل، فمحتاج <code class="i">takeUntilDestroyed()</code>.'},
     hl:{ang:[2,5,6,10,11,17]}}
  ],
  example:[
    {
      name:{en:'The same store, three generations', ar:'نفس الـ store، تلات أجيال'},
      what:{
        en:'The BehaviorSubject service is what every Angular tutorial written before 2023 shows you, and you will meet it in real codebases for years. The signal version does the same job with less ceremony. Being able to read both is the point.',
        ar:'الـ service اللي بـ BehaviorSubject دي اللي كل شرح أنجولار اتكتب قبل 2023 بيوريهالك، وهتقابلها في كود حقيقي لسنين. ونسخة الـ signals بتعمل نفس الشغل بمراسم أقل. والمهم إنك تعرف تقرا الاتنين.'},
      files:[
        {id:'g1',name:'the RxJS store',lang:'ts',tag:{en:'you will meet this',ar:'هتقابل دي'},code:[
          "@Injectable({ providedIn: 'root' })",
          "export class CartStore {",
          "  private readonly _items = new BehaviorSubject<Item[]>([]);",
          "",
          "  readonly items$ = this._items.asObservable();      // read-only view",
          "  readonly count$ = this.items$.pipe(map(i => i.length));",
          "  readonly total$ = this.items$.pipe(",
          "    map(i => i.reduce((s, x) => s + x.price, 0)));",
          "",
          "  add(item: Item) {",
          "    this._items.next([...this._items.value, item]);",
          "  }",
          "}",
          "",
          "// template:  {{ count$ | async }}"]},
        {id:'g2',name:'the signal store',lang:'ts',tag:{en:'write this',ar:'اكتب دي'},code:[
          "@Injectable({ providedIn: 'root' })",
          "export class CartStore {",
          "  private readonly _items = signal<Item[]>([]);",
          "",
          "  readonly items = this._items.asReadonly();",
          "  readonly count = computed(() => this._items().length);",
          "  readonly total = computed(() =>",
          "    this._items().reduce((s, x) => s + x.price, 0));",
          "",
          "  add(item: Item) {",
          "    this._items.update(l => [...l, item]);",
          "  }",
          "}",
          "",
          "// template:  {{ count() }}      — no pipe, no subscription"]}
      ]
    }
  ],
  gotchas:[
    {en:'A plain <code class="i">Subject</code> has no current value. Subscribe after the value was pushed and you get nothing, forever. Use <code class="i">BehaviorSubject</code> for state.',
     ar:'الـ <code class="i">Subject</code> العادي مالوش قيمة حالية. واعمل subscribe بعد ما القيمة اتدفعت وهتاخد لا حاجة، للأبد. استعمل <code class="i">BehaviorSubject</code> للحالة.'},
    {en:'An <code class="i">error</code> kills the stream permanently. Recover <b>inside</b> the pipe with <code class="i">catchError</code>, before the error reaches the outer subscriber.',
     ar:'الـ <code class="i">error</code> بيقتل الستريم للأبد. تعافى <b>جوه</b> الـ pipe بـ <code class="i">catchError</code>، قبل ما الايرور يوصل للمشترك البرّاني.'},
    {en:'Streams that never complete — <code class="i">valueChanges</code>, <code class="i">router.events</code>, <code class="i">fromEvent</code> — leak unless you use <code class="i">takeUntilDestroyed()</code>.',
     ar:'الستريمز اللي عمرها ما بتقفل — <code class="i">valueChanges</code> و<code class="i">router.events</code> و<code class="i">fromEvent</code> — بتسرّب إلا لو استعملت <code class="i">takeUntilDestroyed()</code>.'},
    {en:'Never expose the Subject itself. Publish <code class="i">.asObservable()</code>, or anyone can call <code class="i">.next()</code> on your state from anywhere.',
     ar:'عمرك ما تعرض الـ Subject نفسه. انشر <code class="i">.asObservable()</code>، وإلا أي حد يقدر ينادي <code class="i">.next()</code> على حالتك من أي مكان.'},
    {en:'For new code, reach for signals first. Use RxJS when the question is about <b>time, order or cancellation</b> — debounce, retry, switchMap, websockets.',
     ar:'للكود الجديد، ابدأ بالـ signals. واستعمل RxJS لما السؤال يكون عن <b>الزمن أو الترتيب أو الإلغاء</b> — debounce وretry وswitchMap والـ websockets.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'ui-libraries',
  badge:{en:'Not from scratch', ar:'مش من الصفر'},
  title:{en:'Angular Material and the CDK', ar:'Angular Material والـ CDK'},
  lead:{
    en:`Do not hand-build a date picker, a data table with sorting, or an accessible dialog. Material gives you finished components; the <b>CDK</b> underneath it gives you the hard behaviour — overlays, focus traps, virtual scrolling — with <b>no styling at all</b>, so you can put your own design on top.`,
    ar:`متبنيش بإيدك date picker ولا جدول بترتيب ولا dialog متاح للجميع. Material بيديك components جاهزة؛ والـ <b>CDK</b> اللي تحتيه بيديك السلوك الصعب — الـ overlays وحصر الـ focus والـ virtual scrolling — <b>من غير أي تنسيق</b>، فتقدر تحط تصميمك إنت فوقه.`
  },
  nodes:[
    [{id:'cli',role:{en:'installs and themes it',ar:'بيثبّت ويعمل الثيم'},file:'ng add @angular/material',ext:'',kind:'cfg'}],
    [{id:'mat',role:{en:'finished components',ar:'components جاهزة'},file:'Material',ext:'',kind:'ts'},
     {id:'cdk',role:{en:'behaviour, no styles',ar:'سلوك من غير استايل'},file:'CDK',ext:'',kind:'svc'}]
  ],
  edges:[
    {id:'e1',from:'cli',to:'mat',a:'bottom',b:'top',label:'components + theme',packet:'mat-*'},
    {id:'e2',from:'mat',to:'cdk',a:'right',b:'left',label:'built on',packet:'a11y'}
  ],
  files:[
    {id:'cli',name:'getting it',lang:'bash',tag:{en:'one command',ar:'أمر واحد'},code:[
      "ng add @angular/material",
      "",
      "# it asks you three questions and then:",
      "#   - installs @angular/material and @angular/cdk",
      "#   - adds a prebuilt theme (or generates a custom one)",
      "#   - adds the Material Symbols font to index.html",
      "#   - sets up typography and global styles",
      "",
      "# the CDK on its own, if you want behaviour without the look:",
      "npm i @angular/cdk"]},
    {id:'mat',name:'using a component',lang:'ts',tag:{en:'import per component',ar:'import لكل component'},code:[
      "import { MatButtonModule } from '@angular/material/button';",
      "import { MatFormFieldModule } from '@angular/material/form-field';",
      "import { MatInputModule } from '@angular/material/input';",
      "import { MatDatepickerModule } from '@angular/material/datepicker';",
      "import { MatDialog } from '@angular/material/dialog';",
      "",
      "@Component({",
      "  selector: 'app-booking',",
      "  imports: [",
      "    MatButtonModule, MatFormFieldModule,",
      "    MatInputModule, MatDatepickerModule, ReactiveFormsModule,",
      "  ],",
      "  templateUrl: './booking.component.html',",
      "})",
      "export class Booking {",
      "  private readonly dialog = inject(MatDialog);",
      "",
      "  async confirm() {",
      "    const ref = this.dialog.open(ConfirmDialog, { data: { name: 'Mona' } });",
      "    const result = await firstValueFrom(ref.afterClosed());",
      "    if (result) this.save();",
      "  }",
      "}"]},
    {id:'tpl',name:'booking.component.html',lang:'html',tag:{en:'the markup',ar:'الماركب'},code:[
      "<mat-form-field>",
      "  <mat-label>Check-in date</mat-label>",
      "  <input matInput [matDatepicker]=\"picker\" formControlName=\"checkIn\">",
      "  <mat-datepicker-toggle matIconSuffix [for]=\"picker\" />",
      "  <mat-datepicker #picker />",
      "  @if (form.controls.checkIn.hasError('required')) {",
      "    <mat-error>Pick a date</mat-error>",
      "  }",
      "</mat-form-field>",
      "",
      "<button mat-flat-button color=\"primary\" (click)=\"confirm()\">Book</button>",
      "",
      "<!-- keyboard navigation, screen-reader labels, focus management",
      "     and date localisation all come for free. writing that",
      "     yourself correctly is weeks of work. -->"]},
    {id:'cdk',name:'the CDK — behaviour without opinions',lang:'html',tag:{en:'the useful half',ar:'النص المفيد'},code:[
      "<!-- VIRTUAL SCROLL — render 20 rows out of 100,000 -->",
      "<cdk-virtual-scroll-viewport itemSize=\"56\" class=\"list\">",
      "  <div *cdkVirtualFor=\"let row of rows\" class=\"row\">{{ row.name }}</div>",
      "</cdk-virtual-scroll-viewport>",
      "",
      "<!-- FOCUS TRAP — keyboard cannot escape your modal -->",
      "<div cdkTrapFocus cdkTrapFocusAutoCapture role=\"dialog\">…</div>",
      "",
      "<!-- DRAG AND DROP -->",
      "<div cdkDropList (cdkDropListDropped)=\"reorder($event)\">",
      "  @for (t of tasks(); track t.id) { <div cdkDrag>{{ t.title }}</div> }",
      "</div>",
      "",
      "<!-- OVERLAY — position anything against anything -->",
      "<button cdkOverlayOrigin #trigger=\"cdkOverlayOrigin\">Menu</button>",
      "<ng-template cdkConnectedOverlay [cdkConnectedOverlayOrigin]=\"trigger\"",
      "             [cdkConnectedOverlayOpen]=\"open()\">",
      "  <div class=\"my-own-menu\">…</div>",
      "</ng-template>",
      "",
      "<!-- plus: LiveAnnouncer, BreakpointObserver, clipboard, a11y -->"]},
    {id:'pick',name:'which library?',lang:'ts',tag:{en:'the decision',ar:'القرار'},code:[
      "// ANGULAR MATERIAL",
      "//   ✓ first-party, tracks every Angular release, superb a11y",
      "//   ✓ the safe default for an internal tool or a dashboard",
      "//   ✗ looks like Google unless you invest in theming",
      "",
      "// PRIMENG / NG-ZORRO / KENDO",
      "//   ✓ far more components — tables, charts, org charts, editors",
      "//   ✗ third-party: an Angular major release can strand you",
      "",
      "// TAILWIND + THE CDK",
      "//   ✓ total design freedom, tiny runtime, no component API to learn",
      "//   ✗ you build every component yourself (the CDK does the hard parts)",
      "",
      "// A REAL RULE: whatever you pick, pick ONE. two component",
      "// libraries in one app means two themes, two sets of a11y bugs,",
      "// and two focus-management strategies fighting each other."]}
  ],
  steps:[
    {t:{en:'<code class="i">ng add</code> is not <code class="i">npm install</code> — it runs a schematic that installs, themes, and edits your config files for you.',
        ar:'<code class="i">ng add</code> مش <code class="i">npm install</code> — ده بيشغّل schematic بيثبّت ويعمل الثيم ويعدّل ملفات الإعدادات بدالك.'},
     edge:'e1',hl:{cli:[1,4,5,6]}},
    {t:{en:'You import only the components you use. Each one is a separate entry point, so unused Material never reaches your bundle.',
        ar:'بتعمل import للـ components اللي بتستخدمها بس. وكل واحدة entry point لوحدها، فأي Material مش مستخدم عمره ما يوصل للـ bundle.'},
     hl:{mat:[1,2,3,9,10,11]}},
    {t:{en:'What you get for free is the part that is genuinely hard: keyboard navigation, ARIA wiring, focus management and date localisation.',
        ar:'واللي بتاخده ببلاش هو الجزء الصعب فعلاً: التنقل بالكيبورد، وتوصيل ARIA، وإدارة الـ focus، وتعريب التواريخ.'},
     hl:{tpl:[3,4,13,14,15]}},
    {t:{en:'The <b>CDK</b> is Material with the styling removed. Virtual scrolling, focus traps, drag-drop and overlays — the behaviour, none of the look.',
        ar:'الـ <b>CDK</b> هو Material من غير تنسيق. Virtual scrolling وحصر focus وسحب وإفلات وoverlays — السلوك، من غير أي شكل.'},
     edge:'e2',hl:{cdk:[2,3,7,15,16]}},
    {t:{en:'Pick one library and commit. Two component libraries in one app means two themes and two sets of focus bugs fighting each other.',
        ar:'اختار مكتبة واحدة والتزم بيها. مكتبتين components في تطبيق واحد معناها ثيمين ومجموعتين باجات focus بيتخانقوا مع بعض.'},
     hl:{pick:[13,14,15,16]}}
  ],
  example:[
    {
      name:{en:'A 100,000-row table that stays smooth', ar:'جدول 100,000 صف وفاضل سلس'},
      what:{
        en:'Rendering a hundred thousand rows freezes the browser. Virtual scrolling renders only the twenty that fit on screen and recycles them as you scroll — and it is one wrapper element plus one directive.',
        ar:'رسم مية ألف صف بيجمّد البراوزر. والـ virtual scrolling بيرسم العشرين اللي داخلين في الشاشة بس ويعيد استخدامهم وإنت بتنزل — وده عنصر لف واحد وdirective واحد.'},
      files:[
        {id:'v1',name:'big-list.component.ts',lang:'ts',tag:{en:'CDK only, no Material',ar:'CDK بس، من غير Material'},code:[
          "import { ScrollingModule } from '@angular/cdk/scrolling';",
          "",
          "@Component({",
          "  selector: 'app-big-list',",
          "  imports: [ScrollingModule],",
          "  template: `",
          "    <cdk-virtual-scroll-viewport itemSize=\"56\" class=\"viewport\">",
          "      <div *cdkVirtualFor=\"let row of rows(); templateCacheSize: 40\"",
          "           class=\"row\">",
          "        <strong>{{ row.name }}</strong>",
          "        <span>{{ row.email }}</span>",
          "      </div>",
          "    </cdk-virtual-scroll-viewport>",
          "  `,",
          "  styles: `.viewport { height: 600px } .row { height: 56px }`,",
          "})",
          "export class BigList {",
          "  readonly rows = signal<Row[]>(loadHundredThousand());",
          "}",
          "",
          "// itemSize MUST match the real row height, or scrolling drifts."]}
      ]
    }
  ],
  gotchas:[
    {en:'<code class="i">itemSize</code> on a virtual scroll viewport must match the <b>actual</b> rendered row height in pixels. If it does not, the scrollbar and content drift apart.',
     ar:'<code class="i">itemSize</code> في الـ virtual scroll لازم يطابق ارتفاع الصف <b>الحقيقي</b> بالبكسل. ولو مش مطابق، الـ scrollbar والمحتوى بيبعدوا عن بعض.'},
    {en:'The viewport needs an explicit <code class="i">height</code>. Without one it collapses and renders nothing — the most common first-try failure.',
     ar:'الـ viewport محتاج <code class="i">height</code> صريح. ومن غيره بينهار ومبيرسمش حاجة — وده أشهر فشل من أول مرة.'},
    {en:'Material components come pre-styled. Fighting them with <code class="i">::ng-deep</code> and <code class="i">!important</code> is a losing game — use the theming API, or use the CDK and style it yourself.',
     ar:'مكونات Material بتيجي منسّقة. والخناق معاها بـ <code class="i">::ng-deep</code> و<code class="i">!important</code> معركة خاسرة — استعمل الـ theming API، أو استعمل الـ CDK ونسّق بنفسك.'},
    {en:'Third-party libraries are a real upgrade risk. Before adopting one, check when it last released and whether it supports the current Angular major.',
     ar:'المكتبات الخارجية خطر حقيقي وقت الترقية. قبل ما تعتمد واحدة، شوف آخر إصدار ليها إمتى وهل بتدعم إصدار أنجولار الحالي.'},
    {en:'Do not import a whole library barrel just for one button. Import per component, or your first bundle jumps by hundreds of kilobytes.',
     ar:'متعملش import لمكتبة كاملة عشان زرار واحد. اعمل import لكل component، وإلا أول bundle هيقفز بمئات الكيلوبايتات.'}
  ]
},

]};
