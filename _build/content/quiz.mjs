/* ---------------------------------------------------------------
   Check yourself — four questions per topic.

   Shape, per question:
     q     the question, bilingual
     code  optional snippets shown under it: {name, lang:'ts'|'html', lines:[]}
     opts  exactly four answers, bilingual
     a     index of the right one (0-3)
     why   the explanation revealed after answering, bilingual

   Rules that keep these worth doing:
     - the right answer is never the longest or the most hedged one
     - every wrong answer is a mistake somebody actually makes
     - the `why` teaches the rule, it does not just say "correct"

   Keys are the *base* slug (no number), same as plain.mjs.
   A topic missing from this file simply builds without the block.
   --------------------------------------------------------------- */
export default {

/* ============================ beginner ============================ */

'how-an-app-starts': [
  { q: {en:"How many HTML pages does a finished Angular app serve?",
        ar:"تطبيق أنجولار كامل بيقدّم كام صفحة HTML؟"},
    opts:[{en:"One — <code>index.html</code>, and Angular keeps editing it",
           ar:"واحدة — <code>index.html</code>، وأنجولار بيفضل يعدّل فيها"},
          {en:"One per component", ar:"واحدة لكل component"},
          {en:"One per route",     ar:"واحدة لكل route"},
          {en:"None; Angular builds pages on the server every time",
           ar:"ولا واحدة؛ أنجولار بيبني الصفحات على السيرفر كل مرة"}],
    a:0,
    why:{en:"This is what <b>single page application</b> means. The browser loads <code>index.html</code> once, and from then on Angular edits the DOM in place. Routing changes what is <i>inside</i> the page, never the page itself.",
         ar:"ده معنى <b>single page application</b>. المتصفح بيحمّل <code>index.html</code> مرة واحدة، وبعد كده أنجولار بيعدّل الـ DOM في مكانه. الـ routing بيغيّر اللي <i>جوه</i> الصفحة، مش الصفحة نفسها."} },

  { q: {en:"Which file hands your root component to Angular?",
        ar:"أنهي ملف بيسلّم الـ component الرئيسي لأنجولار؟"},
    code:[{name:'main.ts', lang:'ts', lines:[
      "import { bootstrapApplication } from '@angular/platform-browser';",
      "import { App } from './app/app';",
      "import { appConfig } from './app/app.config';",
      "",
      "bootstrapApplication(App, appConfig);"]}],
    opts:[{en:"<code>main.ts</code>", ar:"<code>main.ts</code>"},
          {en:"<code>index.html</code>", ar:"<code>index.html</code>"},
          {en:"<code>angular.json</code>", ar:"<code>angular.json</code>"},
          {en:"<code>app.config.ts</code>", ar:"<code>app.config.ts</code>"}],
    a:0,
    why:{en:"<code>main.ts</code> is the starting line: it calls <code>bootstrapApplication()</code>. <code>app.config.ts</code> only <i>describes</i> the settings; <code>angular.json</code> is build configuration and never runs in the browser.",
         ar:"<code>main.ts</code> هو خط البداية: هو اللي بينادي <code>bootstrapApplication()</code>. <code>app.config.ts</code> بس <i>بيوصف</i> الإعدادات، و<code>angular.json</code> ده إعداد بناء ومبيشتغلش في المتصفح أصلاً."} },

  { q: {en:"Your app builds with no errors but the browser shows a blank white page. What do you check first?",
        ar:"التطبيق بُني من غير أخطاء بس المتصفح بيوريك صفحة بيضا فاضية. تشيّك على إيه الأول؟"},
    opts:[{en:"The browser console — a runtime error stops the first render",
           ar:"كونسول المتصفح — أي خطأ وقت التشغيل بيوقف أول رسم"},
          {en:"Restart the CLI", ar:"اقفل الـ CLI وشغّله تاني"},
          {en:"Clear the browser cache", ar:"امسح الكاش"},
          {en:"Reinstall <code>node_modules</code>", ar:"نزّل <code>node_modules</code> تاني"}],
    a:0,
    why:{en:"A blank page nearly always means Angular threw while bootstrapping — a missing provider, a bad import, an error in a field initialiser. The build cannot see those; the console can. Read it before touching anything else.",
         ar:"الصفحة الفاضية معناها في الغالب إن أنجولار رمى خطأ وهو بيبدأ — provider ناقص، أو import غلط، أو خطأ في تهيئة متغيّر. البناء مش بيشوف الحاجات دي، بس الكونسول بيشوفها. اقراه قبل أي حاجة."} },

  { q: {en:"What is <code>&lt;app-root&gt;</code> in <code>index.html</code>?",
        ar:"إيه هو <code>&lt;app-root&gt;</code> اللي في <code>index.html</code>؟"},
    opts:[{en:"The empty spot Angular fills with your root component",
           ar:"المكان الفاضي اللي أنجولار بيملاه بالـ component الرئيسي"},
          {en:"A standard HTML element", ar:"عنصر HTML قياسي"},
          {en:"A comment the compiler removes", ar:"تعليق الـ compiler بيشيله"},
          {en:"The name of the project", ar:"اسم المشروع"}],
    a:0,
    why:{en:"It is your root component's <b>selector</b>, sitting in the page as a placeholder. Change the selector in the component and you must change the tag here too — otherwise Angular has nowhere to render and you get a blank page.",
         ar:"ده الـ <b>selector</b> بتاع الـ component الرئيسي، وموجود في الصفحة كمكان محفوظ. لو غيّرت الـ selector في الـ component لازم تغيّر التاج هنا كمان — وإلا أنجولار مش هيلاقي مكان يرسم فيه وهتطلعلك صفحة فاضية."} },
],

'cli-and-project-files': [
  { q: {en:"<code>ng generate component cart</code> — which file holds the HTML?",
        ar:"<code>ng generate component cart</code> — أنهي ملف فيه الـ HTML؟"},
    opts:[{en:"<code>cart.html</code>", ar:"<code>cart.html</code>"},
          {en:"<code>cart.ts</code>", ar:"<code>cart.ts</code>"},
          {en:"<code>cart.css</code>", ar:"<code>cart.css</code>"},
          {en:"<code>cart.spec.ts</code>", ar:"<code>cart.spec.ts</code>"}],
    a:0,
    why:{en:"A component is a small family of files with one name: <code>.ts</code> is the logic, <code>.html</code> is the picture, <code>.css</code> is the styling, <code>.spec.ts</code> is the test. Knowing which is which is most of finding your way around a project.",
         ar:"الـ component عبارة عن مجموعة ملفات صغيرة بنفس الاسم: الـ <code>.ts</code> المنطق، والـ <code>.html</code> الشكل، والـ <code>.css</code> التنسيق، والـ <code>.spec.ts</code> الاختبار. إنك تعرف مين فيهم إيه، ده معظم حكاية إنك تعرف تتحرك في المشروع."} },

  { q: {en:"Why use <code>ng generate</code> instead of creating the files by hand?",
        ar:"ليه نستخدم <code>ng generate</code> بدل ما نعمل الملفات بإيدينا؟"},
    opts:[{en:"It writes the boilerplate and the naming correctly every time",
           ar:"بيكتب الكود المتكرر والتسمية صح في كل مرة"},
          {en:"Hand-made files do not compile", ar:"الملفات اليدوية مبتتترجمش"},
          {en:"It is the only way to register a component", ar:"دي الطريقة الوحيدة لتسجيل component"},
          {en:"It makes the app faster", ar:"بيخلي التطبيق أسرع"}],
    a:0,
    why:{en:"Nothing stops you writing the files yourself — the CLI is a time saver, not a requirement. What it buys you is consistency: the same file names, the same selector convention, the same class name, so the next person can guess where things are.",
         ar:"مفيش حاجة تمنعك تكتب الملفات بنفسك — الـ CLI بيوفّر وقت، مش شرط. اللي بتكسبه هو الاتساق: نفس أسماء الملفات، ونفس عُرف الـ selector، ونفس اسم الكلاس، فاللي بعديك يقدر يتوقّع كل حاجة فين."} },

  { q: {en:"What does <code>ng serve</code> do that <code>ng build</code> does not?",
        ar:"<code>ng serve</code> بيعمل إيه و<code>ng build</code> مبيعملهوش؟"},
    opts:[{en:"Runs a dev server and reloads the page as you save",
           ar:"بيشغّل سيرفر تطوير وبيحدّث الصفحة وإنت بتحفظ"},
          {en:"Writes optimised files into <code>dist/</code>", ar:"بيكتب ملفات محسّنة في <code>dist/</code>"},
          {en:"Runs the tests", ar:"بيشغّل الاختبارات"},
          {en:"Installs dependencies", ar:"بينزّل المكتبات"}],
    a:0,
    why:{en:"<code>ng serve</code> is for working: it keeps everything in memory and rebuilds on save. <code>ng build</code> is for shipping: it writes the real optimised output into <code>dist/</code>. Never deploy what <code>ng serve</code> produced.",
         ar:"<code>ng serve</code> للشغل: بيحتفظ بكل حاجة في الميموري وبيعيد البناء أول ما تحفظ. و<code>ng build</code> للنشر: بيكتب الناتج الحقيقي المحسّن في <code>dist/</code>. متنشر خالص اللي <code>ng serve</code> عمله."} },

  { q: {en:"Where do you put a package's settings so they apply to the whole app?",
        ar:"بتحط إعدادات مكتبة فين علشان تسري على التطبيق كله؟"},
    opts:[{en:"The <code>providers</code> array in <code>app.config.ts</code>",
           ar:"مصفوفة <code>providers</code> في <code>app.config.ts</code>"},
          {en:"<code>angular.json</code>", ar:"<code>angular.json</code>"},
          {en:"<code>package.json</code>", ar:"<code>package.json</code>"},
          {en:"<code>index.html</code>", ar:"<code>index.html</code>"}],
    a:0,
    why:{en:"<code>app.config.ts</code> is the app-wide settings list — <code>provideRouter()</code>, <code>provideHttpClient()</code> and friends all go in its <code>providers</code> array. <code>angular.json</code> configures the <i>build</i>; <code>package.json</code> lists dependencies.",
         ar:"<code>app.config.ts</code> هو قائمة إعدادات التطبيق كله — <code>provideRouter()</code> و<code>provideHttpClient()</code> وأصحابهم كلهم بيتحطوا في مصفوفة <code>providers</code> بتاعته. <code>angular.json</code> بيظبط <i>البناء</i>، و<code>package.json</code> بيسرد المكتبات."} },
],

'components': [
  { q: {en:"A <code>&lt;app-user-card&gt;</code> tag renders nothing and nothing is thrown. What is missing?",
        ar:"تاج <code>&lt;app-user-card&gt;</code> مش بيعرض أي حاجة ومفيش خطأ. إيه الناقص؟"},
    opts:[{en:"<code>UserCard</code> is not in the parent's <code>imports</code>",
           ar:"<code>UserCard</code> مش موجود في <code>imports</code> بتاع الأب"},
          {en:"The child has no <code>@Input()</code>", ar:"الابن مفيهوش <code>@Input()</code>"},
          {en:"The selector must start with a capital letter", ar:"الـ selector لازم يبدأ بحرف كابيتال"},
          {en:"The child needs a constructor", ar:"الابن محتاج constructor"}],
    a:0,
    why:{en:"With standalone components, a tag you have not imported is just unknown HTML: nothing renders and nothing crashes. Dev mode logs an <i>is not a known element</i> warning that is easy to scroll past — so check the parent's <code>imports: []</code> first, every time.",
         ar:"مع الـ standalone components، أي تاج مش عامله import بيعتبر HTML مش معروف: مفيش حاجة بتترسم ومفيش حاجة بتقع. وضع التطوير بيسجّل تحذير <i>is not a known element</i> وسهل جداً تعدّي عليه — فشيّك على <code>imports: []</code> بتاع الأب الأول، في كل مرة."} },

  { q: {en:"What is a component's <code>selector</code> for?",
        ar:"الـ <code>selector</code> بتاع الـ component بيعمل إيه؟"},
    opts:[{en:"It is the tag name you write to place the component",
           ar:"ده اسم التاج اللي بتكتبه علشان تحط الـ component"},
          {en:"It picks which CSS file to use", ar:"بيختار أنهي ملف CSS يستخدم"},
          {en:"It selects elements inside the template", ar:"بيختار عناصر جوه الـ template"},
          {en:"It names the class in the compiled output", ar:"بيسمّي الكلاس في ناتج الترجمة"}],
    a:0,
    why:{en:"<code>selector: 'app-user-card'</code> means you write <code>&lt;app-user-card&gt;</code>. The <code>app-</code> prefix is a convention that keeps your tags from ever colliding with real HTML elements or another library's.",
         ar:"<code>selector: 'app-user-card'</code> معناها إنك بتكتب <code>&lt;app-user-card&gt;</code>. والبادئة <code>app-</code> ده عُرف بيمنع التاجات بتاعتك إنها تتعارض مع عناصر HTML الحقيقية أو مع مكتبة تانية."} },

  { q: {en:"Where does a component's logic live?",
        ar:"منطق الـ component بيعيش فين؟"},
    opts:[{en:"In the exported class — its fields and methods",
           ar:"في الكلاس المُصدَّر — المتغيّرات والدوال بتاعته"},
          {en:"Inside <code>@Component({...})</code>", ar:"جوه <code>@Component({...})</code>"},
          {en:"In the template", ar:"في الـ template"},
          {en:"In <code>main.ts</code>", ar:"في <code>main.ts</code>"}],
    a:0,
    why:{en:"<code>@Component({...})</code> is only a label that tells Angular how to wire the class up — selector, template, imports. The behaviour is the plain TypeScript class underneath it, which is why you can test it like any other class.",
         ar:"<code>@Component({...})</code> ده مجرد لافتة بتقول لأنجولار يوصّل الكلاس إزاي — selector وtemplate وimports. أما السلوك فهو كلاس TypeScript عادي تحتيها، وعلشان كده تقدر تختبره زي أي كلاس تاني."} },

  { q: {en:"Which is the smallest correct way to show a component's own HTML?",
        ar:"إيه أصغر طريقة صح لعرض HTML بتاع component؟"},
    code:[{name:'badge.ts', lang:'ts', lines:[
      "@Component({",
      "  selector: 'app-badge',",
      "  template: '<span class=\"badge\">New</span>',",
      "})",
      "export class Badge {}"]}],
    opts:[{en:"<code>template</code> for short markup, <code>templateUrl</code> for a separate file",
           ar:"<code>template</code> للماركب القصير، و<code>templateUrl</code> لملف منفصل"},
          {en:"Only <code>templateUrl</code> is allowed", ar:"<code>templateUrl</code> بس اللي مسموح"},
          {en:"Only <code>template</code> is allowed", ar:"<code>template</code> بس اللي مسموح"},
          {en:"Both at once, and Angular joins them", ar:"الاتنين مع بعض، وأنجولار بيلزقهم"}],
    a:0,
    why:{en:"Both work, and you pick by size: inline <code>template</code> for a few lines, <code>templateUrl</code> once it grows. Using both on the same component is an error — Angular needs exactly one source for the markup.",
         ar:"الاتنين يشتغلوا، وإنت بتختار على أساس الحجم: <code>template</code> مباشر لكام سطر، و<code>templateUrl</code> لما يكبر. أما إنك تستخدم الاتنين على نفس الـ component فده خطأ — أنجولار محتاج مصدر واحد بالظبط للماركب."} },
],

'control-flow': [
  { q: {en:"What does <code>track</code> do in an <code>@for</code> block?",
        ar:"<code>track</code> بتعمل إيه جوه <code>@for</code>؟"},
    code:[{name:'orders.html', lang:'html', lines:[
      "@for (order of orders(); track order.id) {",
      "  <app-order-row [order]=\"order\" />",
      "}"]}],
    opts:[{en:"Tells Angular which row is which, so it moves DOM instead of rebuilding it",
           ar:"بتقول لأنجولار مين هو أنهي صف، فبيحرّك الـ DOM بدل ما يبنيه من الأول"},
          {en:"Sorts the list", ar:"بترتّب القائمة"},
          {en:"Counts the iterations", ar:"بتعد اللفّات"},
          {en:"Filters out duplicates", ar:"بتشيل المكرر"}],
    a:0,
    why:{en:"<code>track</code> is an identity, not an index. Given a stable id, Angular can tell that a row merely moved and keep its DOM — which preserves focus, scroll position and input values. It is required in <code>@for</code> for exactly this reason.",
         ar:"<code>track</code> دي هوية، مش ترتيب. لما يكون فيه id ثابت، أنجولار يعرف إن الصف بس اتحرك ويحتفظ بالـ DOM بتاعه — وده بيحافظ على الفوكس ومكان الـ scroll والقيم المكتوبة. وعلشان كده بالظبط هي مطلوبة في <code>@for</code>."} },

  { q: {en:"Which block runs when a list is empty?",
        ar:"أنهي بلوك بيشتغل لما القائمة تكون فاضية؟"},
    opts:[{en:"<code>@empty</code>", ar:"<code>@empty</code>"},
          {en:"<code>@else</code>", ar:"<code>@else</code>"},
          {en:"<code>@default</code>", ar:"<code>@default</code>"},
          {en:"<code>@placeholder</code>", ar:"<code>@placeholder</code>"}],
    a:0,
    why:{en:"<code>@empty</code> attaches to <code>@for</code> and renders when there is nothing to loop over. It is the tidy replacement for a second <code>@if (list.length === 0)</code> sitting next to your loop.",
         ar:"<code>@empty</code> بتلزق في <code>@for</code> وبتترسم لما ميكونش فيه حاجة نلف عليها. وهي البديل المرتب لـ <code>@if (list.length === 0)</code> تانية جنب اللوب."} },

  { q: {en:"What is the modern replacement for <code>*ngIf</code>?",
        ar:"إيه البديل الحديث لـ <code>*ngIf</code>؟"},
    opts:[{en:"<code>@if</code>, built into the template — no import needed",
           ar:"<code>@if</code>، مدمجة في الـ template — مش محتاجة import"},
          {en:"<code>[ngIf]</code>", ar:"<code>[ngIf]</code>"},
          {en:"<code>@show</code>", ar:"<code>@show</code>"},
          {en:"<code>@when</code>", ar:"<code>@when</code>"}],
    a:0,
    why:{en:"Since Angular 17 the control flow is part of the template language itself, so there is nothing to import and no <code>CommonModule</code>. <code>*ngIf</code> still works, but new code should use <code>@if</code> / <code>@for</code> / <code>@switch</code>.",
         ar:"من أنجولار 17 التحكم في المسار بقى جزء من لغة الـ template نفسها، فمفيش حاجة تعمل لها import ولا <code>CommonModule</code>. <code>*ngIf</code> لسه بتشتغل، بس الكود الجديد يستخدم <code>@if</code> و<code>@for</code> و<code>@switch</code>."} },

  { q: {en:"You want a value from an <code>@if</code> condition inside the block. How?",
        ar:"عايز القيمة اللي في شرط <code>@if</code> تستخدمها جوه البلوك. إزاي؟"},
    opts:[{en:"<code>@if (user(); as u) { {{ u.name }} }</code>",
           ar:"<code>@if (user(); as u) { {{ u.name }} }</code>"},
          {en:"<code>@if (user()) { let u = user() }</code>", ar:"<code>@if (user()) { let u = user() }</code>"},
          {en:"<code>@if (user() as u)</code>", ar:"<code>@if (user() as u)</code>"},
          {en:"You cannot; call the signal again", ar:"مش ممكن؛ نادي الـ signal تاني"}],
    a:0,
    why:{en:"The <code>; as</code> form stores the checked value under a name, so you read it once and TypeScript knows it is not null inside the block. Calling the signal again also works, but you lose that narrowing.",
         ar:"صيغة <code>; as</code> بتخزّن القيمة اللي اتشيّك عليها باسم، فبتقراها مرة واحدة وTypeScript يعرف إنها مش null جوه البلوك. إنك تنادي الـ signal تاني برضه ينفع، بس بتخسر التضييق ده."} },
],

'signals': [
  { q: {en:"How do you read a signal's value?",
        ar:"بتقرا قيمة الـ signal إزاي؟"},
    opts:[{en:"Call it: <code>count()</code>", ar:"نادي عليها: <code>count()</code>"},
          {en:"<code>count</code>", ar:"<code>count</code>"},
          {en:"<code>count.value</code>", ar:"<code>count.value</code>"},
          {en:"<code>count.get()</code>", ar:"<code>count.get()</code>"}],
    a:0,
    why:{en:"A signal <i>is</i> a function. Calling it does two things at once: it hands you the value, and it quietly records that whoever is reading now depends on it. That recording is how the screen knows to redraw later.",
         ar:"الـ signal <i>هي</i> دالة. لما تنادي عليها بتعمل حاجتين في نفس الوقت: بتعطيك القيمة، وبتسجّل بهدوء إن اللي بيقرا دلوقتي بقى متعلّق بيها. والتسجيل ده هو اللي بيخلي الشاشة تعرف تعيد الرسم بعدين."} },

  { q: {en:"The screen shows one item. What renders after <code>add()</code>?",
        ar:"الشاشة بتوري عنصر واحد. بعد <code>add()</code> هيترسم إيه؟"},
    code:[{name:'list.ts', lang:'ts', lines:[
      "items = signal<string[]>(['a']);",
      "",
      "add() {",
      "  this.items().push('b');",
      "}"]}],
    opts:[{en:"Still one item — the signal never changed",
           ar:"لسه عنصر واحد — الـ signal ما اتغيّرتش"},
          {en:"Two items", ar:"عنصرين"},
          {en:"Two items, after the next click anywhere", ar:"عنصرين، بعد أي كليك تاني"},
          {en:"It throws — signals are read-only", ar:"بيرمي خطأ — الـ signals للقراءة بس"}],
    a:0,
    why:{en:"<code>items()</code> hands you the array itself, and pushing into it leaves the signal holding the same reference — so it tells nobody. Replace the value instead: <code>this.items.update(a =&gt; [...a, 'b'])</code>.",
         ar:"<code>items()</code> بتعطيك المصفوفة نفسها، ولما تعمل push جواها الـ signal بيفضل شايل نفس المرجع — فمش بيبلّغ حد. استبدل القيمة: <code>this.items.update(a =&gt; [...a, 'b'])</code>."} },

  { q: {en:"When should a value be a <code>computed()</code> rather than a <code>signal()</code>?",
        ar:"إمتى القيمة تبقى <code>computed()</code> بدل <code>signal()</code>؟"},
    opts:[{en:"When it can always be worked out from other signals",
           ar:"لما تقدر دايماً تحسبها من signals تانية"},
          {en:"When it changes often", ar:"لما تتغيّر كتير"},
          {en:"When it is a number", ar:"لما تكون رقم"},
          {en:"When more than one component reads it", ar:"لما أكتر من component يقراها"}],
    a:0,
    why:{en:"A total, a filtered list, a <i>can the user submit</i> flag — all derived. Storing them in their own signal means two places to update and one chance to forget. <code>computed()</code> recalculates itself and caches the result until an input changes.",
         ar:"الإجمالي، والقائمة المفلترة، وعلامة <i>ينفع يبعت ولا لأ</i> — كلها مشتقّة. لما تخزّنها في signal لوحدها يبقى عندك مكانين تحدّثهم وفرصة واحدة تنسى. <code>computed()</code> بتحسب نفسها وبتحتفظ بالنتيجة لحد ما مدخلاتها تتغيّر."} },

  { q: {en:"Which one replaces the whole value based on the old one?",
        ar:"أنهي واحدة بتستبدل القيمة كلها بناءً على القديمة؟"},
    opts:[{en:"<code>count.update(n =&gt; n + 1)</code>", ar:"<code>count.update(n =&gt; n + 1)</code>"},
          {en:"<code>count.set(n =&gt; n + 1)</code>", ar:"<code>count.set(n =&gt; n + 1)</code>"},
          {en:"<code>count.mutate(n =&gt; n + 1)</code>", ar:"<code>count.mutate(n =&gt; n + 1)</code>"},
          {en:"<code>count.next(n =&gt; n + 1)</code>", ar:"<code>count.next(n =&gt; n + 1)</code>"}],
    a:0,
    why:{en:"<code>set()</code> takes a finished value; <code>update()</code> takes a function of the old one. <code>next()</code> belongs to RxJS subjects, and <code>mutate()</code> was removed precisely because mutating in place does not notify anybody.",
         ar:"<code>set()</code> بتاخد قيمة جاهزة، و<code>update()</code> بتاخد دالة على القيمة القديمة. <code>next()</code> بتاعة RxJS subjects، و<code>mutate()</code> اتشالت بالظبط علشان التعديل في المكان مبيبلّغش حد."} },
],

'inputs-outputs': [
  { q: {en:"A parent binds <code>(remove)=\"drop($event)\"</code> and <code>$event</code> is always undefined. Why?",
        ar:"الأب بيربط <code>(remove)=\"drop($event)\"</code> و<code>$event</code> دايماً undefined. ليه؟"},
    opts:[{en:"The child calls <code>emit()</code> with no argument",
           ar:"الابن بينادي <code>emit()</code> من غير أي قيمة"},
          {en:"<code>$event</code> only works on DOM events", ar:"<code>$event</code> بتشتغل بس على أحداث الـ DOM"},
          {en:"The output needs a type parameter", ar:"الـ output محتاج نوع"},
          {en:"The handler must be <code>async</code>", ar:"الدالة لازم تكون <code>async</code>"}],
    a:0,
    why:{en:"<code>$event</code> <b>is</b> whatever you hand to emit. <code>emit()</code> sends nothing; <code>emit(this.line.id)</code> sends the id. Typing the output — <code>output&lt;string&gt;()</code> — makes the compiler catch this for you.",
         ar:"<code>$event</code> <b>هو</b> اللي بتسلّمه لـ emit. <code>emit()</code> مش بتبعت حاجة، و<code>emit(this.line.id)</code> بتبعت الـ id. ولما تحدّد نوع الـ output — <code>output&lt;string&gt;()</code> — الـ compiler هو اللي يمسكلك الغلطة دي."} },

  { q: {en:"What is the difference between <code>title=\"Hello\"</code> and <code>[title]=\"Hello\"</code>?",
        ar:"إيه الفرق بين <code>title=\"Hello\"</code> و<code>[title]=\"Hello\"</code>؟"},
    opts:[{en:"The first passes the text; the second looks for a property named <code>Hello</code>",
           ar:"الأولى بتمرر النص، والتانية بتدوّر على متغيّر اسمه <code>Hello</code>"},
          {en:"They are identical", ar:"الاتنين نفس الحاجة"},
          {en:"The second is invalid syntax", ar:"التانية صيغة غلط"},
          {en:"The first is invalid for components", ar:"الأولى غلط مع الـ components"}],
    a:0,
    why:{en:"Square brackets mean <i>evaluate this as an expression</i>. Without them you are passing a literal string. Both are legal, which is why a missing bracket is such a quiet bug — you get the word <code>Hello</code> instead of your value.",
         ar:"الأقواس المربعة معناها <i>احسب اللي جوايا كتعبير</i>. من غيرها إنت بتمرر نص حرفي. والاتنين قانونيين، وعلشان كده نسيان القوس بيبقى باج هادي جداً — بتاخد كلمة <code>Hello</code> بدل قيمتك."} },

  { q: {en:"Which pair is the modern, signal-based version of <code>@Input()</code> and <code>@Output()</code>?",
        ar:"أنهي زوج هو النسخة الحديثة القايمة على signals من <code>@Input()</code> و<code>@Output()</code>؟"},
    opts:[{en:"<code>input()</code> and <code>output()</code>", ar:"<code>input()</code> و<code>output()</code>"},
          {en:"<code>signalInput()</code> and <code>signalOutput()</code>", ar:"<code>signalInput()</code> و<code>signalOutput()</code>"},
          {en:"<code>model()</code> and <code>emit()</code>", ar:"<code>model()</code> و<code>emit()</code>"},
          {en:"<code>prop()</code> and <code>event()</code>", ar:"<code>prop()</code> و<code>event()</code>"}],
    a:0,
    why:{en:"<code>name = input.required&lt;string&gt;()</code> is read as <code>name()</code> and is reactive everywhere. <code>model()</code> is the two-way variant — it is an input and an output at once, which is what <code>[(value)]</code> needs.",
         ar:"<code>name = input.required&lt;string&gt;()</code> بتتقرا كـ <code>name()</code> وبتبقى تفاعلية في كل مكان. و<code>model()</code> هي النسخة ذات الاتجاهين — input وoutput في نفس الوقت، وده اللي <code>[(value)]</code> محتاجاه."} },

  { q: {en:"When can you read an <code>@Input()</code> for the first time?",
        ar:"إمتى تقدر تقرا <code>@Input()</code> لأول مرة؟"},
    opts:[{en:"In <code>ngOnInit</code> — inputs are set after the constructor",
           ar:"في <code>ngOnInit</code> — الـ inputs بتتحدد بعد الـ constructor"},
          {en:"In the constructor", ar:"في الـ constructor"},
          {en:"Only in <code>ngAfterViewInit</code>", ar:"في <code>ngAfterViewInit</code> بس"},
          {en:"Any time; they are set before the class is built", ar:"في أي وقت؛ بتتحدد قبل بناء الكلاس"}],
    a:0,
    why:{en:"Angular builds the class first and assigns the inputs afterwards, so the constructor always sees <code>undefined</code>. Read them in <code>ngOnInit</code>, react to later changes in <code>ngOnChanges</code>, or use signal inputs with an <code>effect()</code>.",
         ar:"أنجولار بيبني الكلاس الأول وبيحدد الـ inputs بعدين، فالـ constructor دايماً بيشوف <code>undefined</code>. اقراهم في <code>ngOnInit</code>، واستجيب للتغييرات بعدها في <code>ngOnChanges</code>، أو استخدم signal inputs مع <code>effect()</code>."} },
],

'services-di': [
  { q: {en:"Two sibling components inject the same service but see different values. Why?",
        ar:"اتنين components أشقّاء بيعملوا inject لنفس الـ service بس بيشوفوا قيم مختلفة. ليه؟"},
    opts:[{en:"One of them lists the service in its own <code>providers</code>",
           ar:"واحد منهم بيسجّل الـ service في <code>providers</code> بتاعه"},
          {en:"Services cannot hold state", ar:"الـ services مش بتحتفظ بحالة"},
          {en:"They need the same parent", ar:"لازم يكون لهم نفس الأب"},
          {en:"The service is missing <code>@Injectable()</code>", ar:"الـ service ناقصها <code>@Injectable()</code>"}],
    a:0,
    why:{en:"A service in a component's <code>providers</code> is created fresh for that component and its children, shadowing the root one. Remove the line and let <code>@Injectable({ providedIn: 'root' })</code> give everyone the single shared instance.",
         ar:"الـ service اللي في <code>providers</code> بتاع component بتتعمل من جديد للـ component ده وأولاده، وبتحجب اللي في الـ root. شيل السطر وخلي <code>@Injectable({ providedIn: 'root' })</code> تدّي الكل نسخة واحدة مشتركة."} },

  { q: {en:"What does <code>providedIn: 'root'</code> mean?",
        ar:"<code>providedIn: 'root'</code> معناها إيه؟"},
    opts:[{en:"One instance for the whole app, created the first time it is asked for",
           ar:"نسخة واحدة للتطبيق كله، بتتعمل أول مرة حد يطلبها"},
          {en:"A new instance for every component", ar:"نسخة جديدة لكل component"},
          {en:"It is created at startup, always", ar:"بتتعمل عند بداية التشغيل دايماً"},
          {en:"It can only be injected in the root component", ar:"ينفع inject بس في الـ component الرئيسي"}],
    a:0,
    why:{en:"It is a single shared instance, created lazily — if nothing injects it, it is never created and the bundler can even drop it. That laziness is why <code>providedIn: 'root'</code> is preferred over listing services in <code>app.config.ts</code>.",
         ar:"دي نسخة واحدة مشتركة، وبتتعمل وقت الحاجة — لو مفيش حاجة عملت لها inject، مش هتتعمل خالص والـ bundler يقدر يشيلها. والتأجيل ده هو السبب إن <code>providedIn: 'root'</code> أفضل من إنك تسرد الـ services في <code>app.config.ts</code>."} },

  { q: {en:"Which is the modern way to get a service inside a component?",
        ar:"إيه الطريقة الحديثة للحصول على service جوه component؟"},
    opts:[{en:"<code>private api = inject(UserApi);</code>", ar:"<code>private api = inject(UserApi);</code>"},
          {en:"<code>new UserApi()</code>", ar:"<code>new UserApi()</code>"},
          {en:"<code>UserApi.getInstance()</code>", ar:"<code>UserApi.getInstance()</code>"},
          {en:"Import it and call its methods statically", ar:"اعمل import ونادي دواله بشكل ثابت"}],
    a:0,
    why:{en:"<code>inject()</code> asks Angular's injector for the instance, so you get the shared one and it stays swappable in tests. <code>new UserApi()</code> builds your own private copy and skips dependency injection entirely — the state will not be shared.",
         ar:"<code>inject()</code> بتطلب النسخة من injector أنجولار، فبتاخد المشتركة وتفضل قابلة للاستبدال في الاختبارات. أما <code>new UserApi()</code> فبتبني نسخة خاصة بيك وبتتخطى الـ dependency injection خالص — والحالة مش هتكون مشتركة."} },

  { q: {en:"Why put shared state in a service instead of passing it component to component?",
        ar:"ليه نحط الحالة المشتركة في service بدل ما نمررها من component لـ component؟"},
    opts:[{en:"Components far apart have no template between them to bind through",
           ar:"الـ components البعيدة عن بعضها مفيش template بينها تربط من خلاله"},
          {en:"Services are faster", ar:"الـ services أسرع"},
          {en:"Inputs have a size limit", ar:"الـ inputs لها حد أقصى للحجم"},
          {en:"Angular forbids more than three inputs", ar:"أنجولار بيمنع أكتر من ٣ inputs"}],
    a:0,
    why:{en:"Bindings only exist between a parent and the child written inside it. For a header and a page five levels apart there is no wire — a service lives in the injector rather than the tree, so both sides simply ask for it.",
         ar:"الروابط موجودة بس بين الأب والابن المكتوب جواه. أما header وصفحة بينهم خمس مستويات فمفيش سلك بينهم — والـ service بتعيش في الـ injector مش في الشجرة، فالطرفين بيطلبوها وخلاص."} },
],

'lifecycle': [
  { q: {en:"You need the real DOM element of a child. Which hook is the earliest safe one?",
        ar:"محتاج عنصر الـ DOM الحقيقي لابن. أنهي hook أول مكان آمن؟"},
    opts:[{en:"<code>ngAfterViewInit</code>", ar:"<code>ngAfterViewInit</code>"},
          {en:"<code>ngOnInit</code>", ar:"<code>ngOnInit</code>"},
          {en:"The constructor", ar:"الـ constructor"},
          {en:"<code>ngOnChanges</code>", ar:"<code>ngOnChanges</code>"}],
    a:0,
    why:{en:"<code>ngOnInit</code> runs before the template has been created, so view queries are still <code>undefined</code> there. <code>ngAfterViewInit</code> is the first moment the DOM exists — or use <code>afterNextRender()</code>, which is also safe on the server.",
         ar:"<code>ngOnInit</code> بتشتغل قبل إنشاء الـ template، فاستعلامات الـ view لسه <code>undefined</code> فيها. <code>ngAfterViewInit</code> هي أول لحظة الـ DOM بيبقى موجود — أو استخدم <code>afterNextRender()</code>، وهي آمنة على السيرفر كمان."} },

  { q: {en:"Where do you undo a subscription or a third-party widget?",
        ar:"بتلغي subscription أو widget من مكتبة خارجية فين؟"},
    opts:[{en:"<code>ngOnDestroy</code>, or a <code>DestroyRef</code> callback",
           ar:"<code>ngOnDestroy</code>، أو callback من <code>DestroyRef</code>"},
          {en:"<code>ngAfterViewChecked</code>", ar:"<code>ngAfterViewChecked</code>"},
          {en:"Angular cleans everything up itself", ar:"أنجولار بينضّف كل حاجة بنفسه"},
          {en:"In the constructor of the next component", ar:"في constructor الـ component اللي بعده"}],
    a:0,
    why:{en:"Angular destroys its own bindings but knows nothing about your <code>setInterval</code>, your chart library or a manual <code>subscribe()</code>. Left behind, they keep running against a component that no longer exists — the classic memory leak.",
         ar:"أنجولار بيهدم الروابط بتاعته، بس مبيعرفش حاجة عن <code>setInterval</code> بتاعك ولا مكتبة الرسم ولا <code>subscribe()</code> يدوي. ولو سيبتهم، بيفضلوا شغالين على component مش موجود خلاص — وده الـ memory leak الكلاسيكي."} },

  { q: {en:"Which hook tells you an input just changed to a new value?",
        ar:"أنهي hook بتقولك إن input اتغيّر لقيمة جديدة؟"},
    opts:[{en:"<code>ngOnChanges</code>", ar:"<code>ngOnChanges</code>"},
          {en:"<code>ngOnInit</code>", ar:"<code>ngOnInit</code>"},
          {en:"<code>ngDoCheck</code>", ar:"<code>ngDoCheck</code>"},
          {en:"<code>ngAfterContentInit</code>", ar:"<code>ngAfterContentInit</code>"}],
    a:0,
    why:{en:"<code>ngOnChanges</code> runs before every <code>ngOnInit</code> and again on every new input value, with the old and new values in hand. With signal inputs you would use <code>computed()</code> or <code>effect()</code> instead and skip the hook entirely.",
         ar:"<code>ngOnChanges</code> بتشتغل قبل كل <code>ngOnInit</code> وبتتكرر مع كل قيمة input جديدة، والقيمة القديمة والجديدة في إيدك. ومع signal inputs هتستخدم <code>computed()</code> أو <code>effect()</code> وتستغنى عن الـ hook خالص."} },

  { q: {en:"Why is <code>afterNextRender()</code> often better than <code>ngAfterViewInit</code>?",
        ar:"ليه <code>afterNextRender()</code> أحسن غالباً من <code>ngAfterViewInit</code>؟"},
    opts:[{en:"It only runs in the browser, so server rendering does not break",
           ar:"بتشتغل في المتصفح بس، فالرندر على السيرفر مبيخربش"},
          {en:"It runs earlier", ar:"بتشتغل أبدر"},
          {en:"It runs on every change", ar:"بتشتغل مع كل تغيير"},
          {en:"It replaces <code>ngOnDestroy</code>", ar:"بتستبدل <code>ngOnDestroy</code>"}],
    a:0,
    why:{en:"During server-side rendering there is no <code>window</code> and no real DOM, but <code>ngAfterViewInit</code> still runs — so DOM code there crashes the render. <code>afterNextRender()</code> is skipped on the server by design.",
         ar:"وقت الرندر على السيرفر مفيش <code>window</code> ولا DOM حقيقي، ومع ذلك <code>ngAfterViewInit</code> بتشتغل — فكود الـ DOM هناك بيوقّع الرندر. أما <code>afterNextRender()</code> فبتتخطى على السيرفر بحكم التصميم."} },
],

'directives-and-pipes': [
  { q: {en:"You wrote a pipe and the template says it is an unknown pipe. What is missing?",
        ar:"عملت pipe والـ template بيقول إنها pipe مش معروفة. إيه الناقص؟"},
    opts:[{en:"The pipe class is not in the component's <code>imports</code>",
           ar:"كلاس الـ pipe مش موجود في <code>imports</code> بتاع الـ component"},
          {en:"Pipes must be declared in <code>app.config.ts</code>", ar:"الـ pipes لازم تتسجّل في <code>app.config.ts</code>"},
          {en:"The pipe name must start with <code>ng</code>", ar:"اسم الـ pipe لازم يبدأ بـ <code>ng</code>"},
          {en:"Pipes need <code>providedIn: 'root'</code>", ar:"الـ pipes محتاجة <code>providedIn: 'root'</code>"}],
    a:0,
    why:{en:"Standalone pipes and directives are imported exactly like components: add the class to <code>imports: []</code> where you use it. Unlike a missing component this one <i>does</i> fail loudly, which makes it the easier of the two mistakes.",
         ar:"الـ pipes والـ directives المستقلة بتتعمل import بالظبط زي الـ components: ضيف الكلاس في <code>imports: []</code> في المكان اللي بتستخدمه فيه. وعلى عكس الـ component الناقص، دي <i>بتفشل بصوت عالي</i>، وعلشان كده هي أسهل الغلطتين."} },

  { q: {en:"What is the difference between a directive and a component?",
        ar:"إيه الفرق بين الـ directive والـ component؟"},
    opts:[{en:"A directive adds behaviour to an existing element; a component brings its own template",
           ar:"الـ directive بتضيف سلوك لعنصر موجود، والـ component بيجيب template بتاعه"},
          {en:"A directive cannot be injected into", ar:"الـ directive مينفعش تعمل لها inject"},
          {en:"A directive can only change styles", ar:"الـ directive بتغيّر التنسيقات بس"},
          {en:"There is no difference in modern Angular", ar:"مفيش فرق في أنجولار الحديث"}],
    a:0,
    why:{en:"They are the same machinery with one difference: a component has a template, a directive does not. So <code>appAutofocus</code> sits on an <code>&lt;input&gt;</code> you already have and gives it one extra behaviour, without changing the markup.",
         ar:"هما نفس الآلية بفرق واحد: الـ component عنده template، والـ directive لأ. فـ <code>appAutofocus</code> بتقعد على <code>&lt;input&gt;</code> موجود عندك أصلاً وبتديه سلوك زيادة، من غير ما تغيّر الماركب."} },

  { q: {en:"Why should a pipe be <code>pure</code> (the default)?",
        ar:"ليه الـ pipe تكون <code>pure</code> (وهو الوضع الافتراضي)؟"},
    opts:[{en:"So it only recalculates when its input changes, instead of on every check",
           ar:"علشان تعيد الحساب بس لما مدخلها يتغيّر، مش مع كل فحص"},
          {en:"So it can be used in more than one component", ar:"علشان تستخدمها في أكتر من component"},
          {en:"So it can return objects", ar:"علشان ترجّع كائنات"},
          {en:"So it works on the server", ar:"علشان تشتغل على السيرفر"}],
    a:0,
    why:{en:"An impure pipe runs on <i>every</i> change detection pass — many times per second while you scroll. A pure pipe runs only when its arguments change. If you find yourself needing <code>pure: false</code>, the value probably wants to be a <code>computed()</code>.",
         ar:"الـ pipe الـ impure بتشتغل مع <i>كل</i> دورة فحص تغييرات — يعني مرات كتير في الثانية وإنت بتعمل scroll. أما الـ pure فبتشتغل بس لما مدخلاتها تتغيّر. ولو لقيت نفسك محتاج <code>pure: false</code>، فالقيمة غالباً عايزة تكون <code>computed()</code>."} },

  { q: {en:"Where does a directive get the element it is sitting on?",
        ar:"الـ directive بتجيب العنصر اللي قاعدة عليه منين؟"},
    opts:[{en:"<code>inject(ElementRef)</code>", ar:"<code>inject(ElementRef)</code>"},
          {en:"<code>@Input() element</code>", ar:"<code>@Input() element</code>"},
          {en:"<code>document.querySelector()</code>", ar:"<code>document.querySelector()</code>"},
          {en:"<code>this.host</code>", ar:"<code>this.host</code>"}],
    a:0,
    why:{en:"Angular injects the host element as an <code>ElementRef</code>, and <code>el.nativeElement</code> is the real DOM node. Prefer <code>host: {...}</code> bindings or <code>Renderer2</code> where you can — those keep the directive working outside a browser.",
         ar:"أنجولار بيعمل inject للعنصر المضيف على شكل <code>ElementRef</code>، و<code>el.nativeElement</code> هو عقدة الـ DOM الحقيقية. وفضّل روابط <code>host: {...}</code> أو <code>Renderer2</code> لما تقدر — دول بيخلّوا الـ directive تشتغل بره المتصفح كمان."} },
],

'content-projection': [
  { q: {en:"What does <code>&lt;ng-content&gt;</code> do?",
        ar:"<code>&lt;ng-content&gt;</code> بتعمل إيه؟"},
    opts:[{en:"Marks where markup written between the component's tags should land",
           ar:"بتحدد فين الماركب المكتوب بين تاجات الـ component يستقر"},
          {en:"Loads content over HTTP", ar:"بتحمّل محتوى عن طريق HTTP"},
          {en:"Renders a child component", ar:"بترسم component ابن"},
          {en:"Repeats its contents", ar:"بتكرر اللي جواها"}],
    a:0,
    why:{en:"It is a hole in the wrapper's template. That is what lets one <code>&lt;app-panel&gt;</code> hold a table on one page and a form on another, without the panel knowing anything about either.",
         ar:"دي فتحة في template الغلاف. وده اللي بيخلي <code>&lt;app-panel&gt;</code> واحد يشيل جدول في صفحة وفورم في صفحة تانية، من غير ما الـ panel يعرف حاجة عن أي واحد فيهم."} },

  { q: {en:"Which component owns the click handler in projected markup?",
        ar:"مين الـ component اللي بيملك دالة الكليك في ماركب متمرَّر؟"},
    code:[{name:'settings.html', lang:'html', lines:[
      "<app-panel>",
      "  <button (click)=\"save()\">Save</button>",
      "</app-panel>"]}],
    opts:[{en:"<code>SettingsComponent</code> — the one that wrote the markup",
           ar:"<code>SettingsComponent</code> — اللي كتب الماركب"},
          {en:"<code>PanelComponent</code>", ar:"<code>PanelComponent</code>"},
          {en:"Whichever declares <code>save()</code>", ar:"اللي معرّف فيه <code>save()</code>"},
          {en:"Neither; it must be an output", ar:"ولا واحد؛ لازم تكون output"}],
    a:0,
    why:{en:"Projected content is compiled in the component that <i>wrote</i> it, so <code>save()</code> is looked up on <code>SettingsComponent</code>. This is the single most useful fact about <code>&lt;ng-content&gt;</code>: the markup moves, but its bindings stay home.",
         ar:"المحتوى المتمرَّر بيتترجم في الـ component اللي <i>كتبه</i>، فـ <code>save()</code> بيتم البحث عنها في <code>SettingsComponent</code>. وده أهم معلومة عن <code>&lt;ng-content&gt;</code>: الماركب بيتحرك، بس روابطه بتفضل في بيتها."} },

  { q: {en:"How do you give a wrapper more than one slot?",
        ar:"بتدّي الغلاف أكتر من فتحة إزاي؟"},
    opts:[{en:"<code>&lt;ng-content select=\"[slot=actions]\"&gt;</code>",
           ar:"<code>&lt;ng-content select=\"[slot=actions]\"&gt;</code>"},
          {en:"<code>&lt;ng-content name=\"actions\"&gt;</code>", ar:"<code>&lt;ng-content name=\"actions\"&gt;</code>"},
          {en:"<code>&lt;ng-content #actions&gt;</code>", ar:"<code>&lt;ng-content #actions&gt;</code>"},
          {en:"You cannot; one slot per component", ar:"مش ممكن؛ فتحة واحدة لكل component"}],
    a:0,
    why:{en:"<code>select</code> takes a CSS selector, so you can route content by attribute, class or tag. Keep one plain <code>&lt;ng-content&gt;</code> last as the catch-all — anything that matches no selector goes there, and content matching nothing at all is dropped silently.",
         ar:"<code>select</code> بتاخد CSS selector، فتقدر توجّه المحتوى بالسمة أو الكلاس أو التاج. وخلي <code>&lt;ng-content&gt;</code> عادية في الآخر كمستقبِل عام — أي حاجة مش مطابقة لأي selector بتروح هناك، واللي مش مطابق لحاجة خالص بيتشال بهدوء."} },

  { q: {en:"When should you reach for <code>&lt;ng-template&gt;</code> with a context instead of <code>&lt;ng-content&gt;</code>?",
        ar:"إمتى تستخدم <code>&lt;ng-template&gt;</code> مع context بدل <code>&lt;ng-content&gt;</code>؟"},
    opts:[{en:"When the wrapper has to hand data back to the markup — a row per item",
           ar:"لما الغلاف يكون لازم يرجّع بيانات للماركب — صف لكل عنصر"},
          {en:"When there is more than one slot", ar:"لما يكون فيه أكتر من فتحة"},
          {en:"When the content is long", ar:"لما المحتوى يكون طويل"},
          {en:"When the wrapper is lazy loaded", ar:"لما الغلاف يتحمّل بشكل مؤجل"}],
    a:0,
    why:{en:"<code>&lt;ng-content&gt;</code> re-places finished markup and cannot supply values. A template is a recipe the wrapper can run once per row, passing the row in through the context — which is exactly how every table library you have used works.",
         ar:"<code>&lt;ng-content&gt;</code> بتعيد تحديد مكان ماركب جاهز ومش بتقدر تدّي قيم. أما الـ template فهي وصفة الغلاف يقدر ينفّذها مرة لكل صف، ويمرر الصف من خلال الـ context — وده بالظبط اللي كل مكتبة جداول استخدمتها بتعمله."} },
],

'component-styles': [
  { q: {en:"A rule in <code>card.css</code> does not affect a child component's element. Why?",
        ar:"قاعدة في <code>card.css</code> مش مأثّرة على عنصر جوه component ابن. ليه؟"},
    opts:[{en:"Component styles are scoped to that component's own template",
           ar:"تنسيقات الـ component محدودة بالـ template بتاعه"},
          {en:"The CSS file was not imported", ar:"ملف الـ CSS ما اتعملّهوش import"},
          {en:"Child components ignore CSS", ar:"الـ components الأبناء بيتجاهلوا الـ CSS"},
          {en:"The selector needs <code>!important</code>", ar:"الـ selector محتاج <code>!important</code>"}],
    a:0,
    why:{en:"Angular adds a unique attribute to your elements and rewrites your selectors to match only those — so styles cannot leak out, and equally cannot reach in. Style the child from inside the child, or hand it a class through an input.",
         ar:"أنجولار بيضيف سمة فريدة لعناصرك وبيعيد كتابة الـ selectors علشان تطابقها بس — فالتنسيقات مبتخرجش، وبالمثل مبتوصلش لجوه. نسّق الابن من جوه الابن، أو مرّر له كلاس من خلال input."} },

  { q: {en:"Which file's styles apply everywhere with no scoping?",
        ar:"تنسيقات أنهي ملف بتسري في كل مكان من غير تحديد نطاق؟"},
    opts:[{en:"<code>styles.css</code>, the global stylesheet", ar:"<code>styles.css</code>، ملف التنسيقات العام"},
          {en:"Every <code>.component.css</code>", ar:"كل <code>.component.css</code>"},
          {en:"The root component's CSS", ar:"CSS الـ component الرئيسي"},
          {en:"None; everything is scoped", ar:"ولا واحد؛ كل حاجة محدودة النطاق"}],
    a:0,
    why:{en:"<code>styles.css</code> is the one unscoped place: resets, fonts, CSS custom properties, <code>body</code> rules. Everything visual that belongs to one component should stay in that component's own file, where it cannot surprise anybody.",
         ar:"<code>styles.css</code> هو المكان الوحيد غير محدود النطاق: الـ resets والخطوط ومتغيّرات CSS وقواعد <code>body</code>. وأي حاجة بصرية بتخص component واحد لازم تفضل في ملفه، حيث مش هتفاجئ حد."} },

  { q: {en:"How do you turn a class on and off from the component?",
        ar:"بتشغّل وتقفل كلاس من الـ component إزاي؟"},
    opts:[{en:"<code>[class.sold-out]=\"stock() === 0\"</code>", ar:"<code>[class.sold-out]=\"stock() === 0\"</code>"},
          {en:"<code>class=\"{{ soldOut }}\"</code>", ar:"<code>class=\"{{ soldOut }}\"</code>"},
          {en:"<code>(class)=\"soldOut\"</code>", ar:"<code>(class)=\"soldOut\"</code>"},
          {en:"<code>#soldOut</code>", ar:"<code>#soldOut</code>"}],
    a:0,
    why:{en:"<code>[class.name]=\"expression\"</code> adds the class when the expression is truthy and removes it otherwise, leaving any other classes on the element alone. Interpolating into <code>class</code> replaces the whole attribute — which quietly wipes your static classes.",
         ar:"<code>[class.name]=\"expression\"</code> بتضيف الكلاس لما التعبير يكون صح وبتشيله لما يكون غلط، وبتسيب أي كلاسات تانية على العنصر زي ما هي. أما الإدراج جوه <code>class</code> فبيستبدل السمة كلها — وده بيمسح كلاساتك الثابتة بهدوء."} },

  { q: {en:"What does <code>:host</code> select in a component stylesheet?",
        ar:"<code>:host</code> بتختار إيه في ملف تنسيقات component؟"},
    opts:[{en:"The component's own tag — <code>&lt;app-card&gt;</code> itself",
           ar:"تاج الـ component نفسه — <code>&lt;app-card&gt;</code>"},
          {en:"The parent component", ar:"الـ component الأب"},
          {en:"The <code>body</code> element", ar:"عنصر <code>body</code>"},
          {en:"The first element in the template", ar:"أول عنصر في الـ template"}],
    a:0,
    why:{en:"Your template's elements sit <i>inside</i> the custom tag, and that tag is not styled by your other rules. <code>:host { display: block }</code> is often the first line a component needs — custom elements are <code>display: inline</code> by default.",
         ar:"عناصر الـ template بتاعك قاعدة <i>جوه</i> التاج المخصص، والتاج ده مش بتنسّقه قواعدك التانية. و<code>:host { display: block }</code> غالباً بيبقى أول سطر الـ component محتاجه — لأن العناصر المخصصة افتراضياً <code>display: inline</code>."} },
],

'template-syntax': [
  { q: {en:"What does <code>{{ }}</code> do?",
        ar:"<code>{{ }}</code> بتعمل إيه؟"},
    opts:[{en:"Reads an expression and writes the result as text",
           ar:"بتقرا تعبير وبتكتب النتيجة كنص"},
          {en:"Runs a statement", ar:"بتنفّذ أمر"},
          {en:"Binds an event", ar:"بتربط حدث"},
          {en:"Declares a variable", ar:"بتعرّف متغيّر"}],
    a:0,
    why:{en:"Interpolation is read-only and one-way: expression in, text out. It also escapes what it writes, which is why <code>{{ userInput }}</code> can never inject HTML — a fact worth remembering when you are tempted by <code>[innerHTML]</code>.",
         ar:"الإدراج للقراءة بس وفي اتجاه واحد: تعبير داخل، نص خارج. وكمان بيأمّن اللي بيكتبه، وعلشان كده <code>{{ userInput }}</code> مستحيل يحقن HTML — وهي حقيقة تستحق التذكر لما تتحمس لـ <code>[innerHTML]</code>."} },

  { q: {en:"Which of these is <b>not</b> allowed in a template expression?",
        ar:"أنهي واحدة من دول <b>مش</b> مسموحة في تعبير template؟"},
    opts:[{en:"<code>{{ items.length++ }}</code>", ar:"<code>{{ items.length++ }}</code>"},
          {en:"<code>{{ a + b }}</code>", ar:"<code>{{ a + b }}</code>"},
          {en:"<code>{{ user?.name }}</code>", ar:"<code>{{ user?.name }}</code>"},
          {en:"<code>{{ total() | currency }}</code>", ar:"<code>{{ total() | currency }}</code>"}],
    a:0,
    why:{en:"Templates allow expressions, not statements — no assignment, no <code>++</code>, no <code>new</code>. The restriction is deliberate: rendering must not change state, or the next render would read something different and you would get an infinite loop.",
         ar:"الـ templates بتسمح بتعبيرات، مش أوامر — مفيش إسناد ولا <code>++</code> ولا <code>new</code>. والقيد ده مقصود: الرسم مينفعش يغيّر الحالة، وإلا الرسم اللي بعده هيقرا حاجة مختلفة وتدخل في لفة لا نهائية."} },

  { q: {en:"What is <code>#box</code> in <code>&lt;input #box&gt;</code>?",
        ar:"إيه <code>#box</code> في <code>&lt;input #box&gt;</code>؟"},
    opts:[{en:"A template reference variable — the element, usable elsewhere in the template",
           ar:"متغيّر مرجعي في الـ template — العنصر نفسه، تقدر تستخدمه في مكان تاني في الـ template"},
          {en:"An id attribute", ar:"سمة id"},
          {en:"A CSS selector", ar:"محدّد CSS"},
          {en:"A one-way binding", ar:"ربط في اتجاه واحد"}],
    a:0,
    why:{en:"<code>#box</code> names the element so the rest of the template can use it: <code>box.value</code>, <code>box.focus()</code>. Put it on a component tag instead and you get that component's <i>instance</i>, which is how a parent calls a child's method.",
         ar:"<code>#box</code> بتسمّي العنصر علشان باقي الـ template يستخدمه: <code>box.value</code> و<code>box.focus()</code>. ولو حطّيتها على تاج component بتاخد <i>نسخة</i> الـ component، وده إزاي الأب بينادي دالة في الابن."} },

  { q: {en:"Why does the template render before your HTTP data arrives?",
        ar:"ليه الـ template بيترسم قبل ما بيانات الـ HTTP توصل؟"},
    opts:[{en:"Rendering is synchronous; the request is not",
           ar:"الرسم متزامن، والطلب لأ"},
          {en:"The template is cached", ar:"الـ template متخزّن في الكاش"},
          {en:"HTTP is always slow", ar:"الـ HTTP دايماً بطيء"},
          {en:"Change detection is disabled during requests", ar:"فحص التغييرات بيتوقف وقت الطلبات"}],
    a:0,
    why:{en:"Angular draws once, immediately, with whatever the fields hold — usually nothing. That is why <code>{{ user.name }}</code> throws while <code>{{ user?.name }}</code> does not. Better still, guard the block with <code>@if (user())</code> and give the field a safe default.",
         ar:"أنجولار بيرسم مرة واحدة، فوراً، بأي حاجة المتغيّرات شايلاها — وغالباً مفيش حاجة. وعلشان كده <code>{{ user.name }}</code> بترمي خطأ و<code>{{ user?.name }}</code> لأ. والأحسن إنك تحمي البلوك بـ <code>@if (user())</code> وتدّي المتغيّر قيمة ابتدائية آمنة."} },
],

'data-binding': [
  { q: {en:"Match the syntax to the direction: <code>[value]</code>, <code>(input)</code>, <code>[(ngModel)]</code>.",
        ar:"وصّل الصيغة بالاتجاه: <code>[value]</code> و<code>(input)</code> و<code>[(ngModel)]</code>."},
    opts:[{en:"down, up, both ways", ar:"لتحت، لفوق، الاتجاهين"},
          {en:"up, down, both ways", ar:"لفوق، لتحت، الاتجاهين"},
          {en:"both ways, down, up", ar:"الاتجاهين، لتحت، لفوق"},
          {en:"down, both ways, up", ar:"لتحت، الاتجاهين، لفوق"}],
    a:0,
    why:{en:"Square brackets push a value <i>in</i>, parentheses listen for something coming <i>out</i>, and <code>[( )]</code> — the banana in a box — is simply both written together. Once you see the shape you can read any Angular template.",
         ar:"الأقواس المربعة بتدفع قيمة <i>لجوه</i>، والأقواس العادية بتسمع لحاجة طالعة <i>لبره</i>، و<code>[( )]</code> — الموزة في الصندوق — هي بس الاتنين مكتوبين مع بعض. وأول ما تشوف الشكل ده هتقدر تقرا أي template في أنجولار."} },

  { q: {en:"<code>[(value)]=\"x\"</code> on your own component. What must the component expose?",
        ar:"<code>[(value)]=\"x\"</code> على component من عندك. الـ component لازم يوفّر إيه؟"},
    opts:[{en:"<code>value = model()</code> — or an input <code>value</code> plus an output <code>valueChange</code>",
           ar:"<code>value = model()</code> — أو input اسمه <code>value</code> وoutput اسمه <code>valueChange</code>"},
          {en:"An input called <code>value</code> only", ar:"input اسمه <code>value</code> بس"},
          {en:"An output called <code>value</code> only", ar:"output اسمه <code>value</code> بس"},
          {en:"An input <code>value</code> and an output <code>changed</code>", ar:"input اسمه <code>value</code> وoutput اسمه <code>changed</code>"}],
    a:0,
    why:{en:"Angular expands <code>[(value)]</code> into <code>[value]</code> and <code>(valueChange)</code>. The output name is the input name plus the word <code>Change</code>, spelled exactly — and <code>model()</code> creates both halves for you in one line.",
         ar:"أنجولار بيفك <code>[(value)]</code> لـ <code>[value]</code> و<code>(valueChange)</code>. واسم الـ output هو اسم الـ input زائد كلمة <code>Change</code> بالحرف — و<code>model()</code> بتعمل النصين في سطر واحد."} },

  { q: {en:"What is the difference between <code>[value]</code> and <code>value=</code> on an input element?",
        ar:"إيه الفرق بين <code>[value]</code> و<code>value=</code> على عنصر input؟"},
    opts:[{en:"<code>[value]</code> sets the live DOM property; <code>value=</code> sets the initial HTML attribute",
           ar:"<code>[value]</code> بتحدد خاصية الـ DOM الحيّة، و<code>value=</code> بتحدد سمة HTML الابتدائية"},
          {en:"They are the same thing", ar:"الاتنين نفس الحاجة"},
          {en:"<code>[value]</code> only works on components", ar:"<code>[value]</code> بتشتغل على الـ components بس"},
          {en:"<code>value=</code> is deprecated", ar:"<code>value=</code> مهجورة"}],
    a:0,
    why:{en:"Attributes are the starting state written in the HTML; properties are the live object in memory. Angular binds to properties, which is why <code>[value]</code> keeps updating as your data changes while a plain attribute is set once and forgotten.",
         ar:"السمات هي الحالة الابتدائية المكتوبة في الـ HTML، والخصائص هي الكائن الحيّ في الميموري. وأنجولار بيربط بالخصائص، وعلشان كده <code>[value]</code> بتفضل بتحدّث مع تغيّر بياناتك، أما السمة العادية بتتحدد مرة وتُنسى."} },

  { q: {en:"Your <code>(click)</code> handler runs but the screen does not update. Most likely cause?",
        ar:"دالة <code>(click)</code> بتشتغل بس الشاشة مش بتتحدّث. أرجح سبب؟"},
    opts:[{en:"You mutated an object or array instead of replacing it",
           ar:"عدّلت كائن أو مصفوفة في مكانها بدل ما تستبدلها"},
          {en:"The handler needs to be <code>async</code>", ar:"الدالة محتاجة تكون <code>async</code>"},
          {en:"<code>(click)</code> does not trigger rendering", ar:"<code>(click)</code> مش بتشغّل الرسم"},
          {en:"The template needs a manual refresh call", ar:"الـ template محتاج نداء تحديث يدوي"}],
    a:0,
    why:{en:"With signals and with <code>OnPush</code>, change is detected by comparing references. <code>arr.push(x)</code> leaves the same array, so nothing looks different. Build a new one: <code>this.items.update(a =&gt; [...a, x])</code>.",
         ar:"مع الـ signals ومع <code>OnPush</code>، التغيير بيتكشف بمقارنة المراجع. و<code>arr.push(x)</code> بتسيب نفس المصفوفة، فمفيش حاجة باينة مختلفة. ابني واحدة جديدة: <code>this.items.update(a =&gt; [...a, x])</code>."} },
],

'built-in-directives': [
  { q: {en:"Which needs no import at all?",
        ar:"أنهي واحدة مش محتاجة import خالص؟"},
    opts:[{en:"<code>[class.active]</code> and <code>[style.width.px]</code>",
           ar:"<code>[class.active]</code> و<code>[style.width.px]</code>"},
          {en:"<code>ngClass</code>", ar:"<code>ngClass</code>"},
          {en:"<code>ngStyle</code>", ar:"<code>ngStyle</code>"},
          {en:"<code>NgIf</code>", ar:"<code>NgIf</code>"}],
    a:0,
    why:{en:"<code>[class.x]</code> and <code>[style.x]</code> are core template syntax — nothing to import. <code>ngClass</code> and <code>ngStyle</code> are directives from <code>CommonModule</code>, and for a single conditional class the plain syntax is both shorter and faster.",
         ar:"<code>[class.x]</code> و<code>[style.x]</code> دول صيغة أساسية في الـ template — مفيش حاجة تعمل لها import. أما <code>ngClass</code> و<code>ngStyle</code> فدول directives من <code>CommonModule</code>، ولكلاس شرطي واحد الصيغة العادية أقصر وأسرع."} },

  { q: {en:"When is <code>ngClass</code> still the better choice?",
        ar:"إمتى <code>ngClass</code> تفضل الاختيار الأحسن؟"},
    opts:[{en:"When a whole set of classes comes from one object or expression",
           ar:"لما مجموعة كلاسات كاملة تيجي من كائن أو تعبير واحد"},
          {en:"Whenever there is more than one class", ar:"في أي وقت يكون فيه أكتر من كلاس"},
          {en:"When the class name is static", ar:"لما اسم الكلاس يكون ثابت"},
          {en:"Never; it is deprecated", ar:"أبداً؛ هي مهجورة"}],
    a:0,
    why:{en:"<code>[ngClass]=\"statusClasses()\"</code> reads well when a <code>computed()</code> already returns the map. For one or two independent toggles, three <code>[class.x]</code> bindings are clearer than building an object to describe them.",
         ar:"<code>[ngClass]=\"statusClasses()\"</code> بتتقرا حلو لما يكون فيه <code>computed()</code> بيرجّع الخريطة أصلاً. أما لتبديل أو اتنين مستقلين، تلات روابط <code>[class.x]</code> أوضح من إنك تبني كائن يوصفهم."} },

  { q: {en:"What replaced <code>*ngFor</code>, <code>*ngIf</code> and <code>*ngSwitch</code>?",
        ar:"إيه اللي حلّ مكان <code>*ngFor</code> و<code>*ngIf</code> و<code>*ngSwitch</code>؟"},
    opts:[{en:"<code>@for</code>, <code>@if</code> and <code>@switch</code> — template syntax, no imports",
           ar:"<code>@for</code> و<code>@if</code> و<code>@switch</code> — صيغة template، من غير imports"},
          {en:"Nothing; they are still the only option", ar:"مفيش؛ لسه هما الاختيار الوحيد"},
          {en:"Signals", ar:"الـ signals"},
          {en:"<code>CommonModule</code>", ar:"<code>CommonModule</code>"}],
    a:0,
    why:{en:"The built-in control flow ships with the compiler, so there is nothing to import and nothing to forget. It is also faster and gives better type narrowing. The old star directives keep working, so migration can be gradual.",
         ar:"التحكم المدمج في المسار بييجي مع الـ compiler، فمفيش حاجة تعمل لها import ومفيش حاجة تنساها. وكمان أسرع وبيدّي تضييق أنواع أحسن. والـ directives القديمة بالنجمة لسه شغالة، فالانتقال يبقى بالتدريج."} },

  { q: {en:"<code>[style.width]=\"w\"</code> where <code>w</code> is the number 40. What happens?",
        ar:"<code>[style.width]=\"w\"</code> و<code>w</code> رقم قيمته 40. إيه اللي بيحصل؟"},
    opts:[{en:"Nothing visible — CSS needs a unit, so use <code>[style.width.px]</code>",
           ar:"مفيش حاجة تبان — الـ CSS محتاجة وحدة، فاستخدم <code>[style.width.px]</code>"},
          {en:"It becomes 40px", ar:"بتبقى 40px"},
          {en:"It becomes 40%", ar:"بتبقى 40%"},
          {en:"It throws a type error", ar:"بترمي خطأ في النوع"}],
    a:0,
    why:{en:"A bare number is not a valid CSS length, so the browser ignores it silently. Angular gives you the unit suffix for exactly this: <code>[style.width.px]=\"w\"</code>, and the same trick works for <code>.em</code>, <code>.%</code> and the rest.",
         ar:"الرقم لوحده مش طول CSS صحيح، فالمتصفح بيتجاهله بهدوء. وأنجولار بيدّيك لاحقة الوحدة علشان ده بالظبط: <code>[style.width.px]=\"w\"</code>، ونفس الحيلة بتشتغل مع <code>.em</code> و<code>.%</code> وغيرهم."} },
],

'built-in-pipes': [
  { q: {en:"What does a pipe change?",
        ar:"الـ pipe بتغيّر إيه؟"},
    opts:[{en:"Only how the value is displayed; the value itself is untouched",
           ar:"بس طريقة عرض القيمة؛ القيمة نفسها ما بتتلمسش"},
          {en:"The value in the component", ar:"القيمة اللي في الـ component"},
          {en:"The value in the database", ar:"القيمة اللي في قاعدة البيانات"},
          {en:"The type of the field", ar:"نوع المتغيّر"}],
    a:0,
    why:{en:"<code>{{ price | currency }}</code> is a display decision made in the template. Your field stays the plain number it was, which is what you want for maths, sorting and sending back to the server.",
         ar:"<code>{{ price | currency }}</code> ده قرار عرض بيتاخد في الـ template. والمتغيّر بتاعك بيفضل الرقم العادي زي ما كان، وده اللي إنت عايزه للحسابات والترتيب والإرسال للسيرفر."} },

  { q: {en:"How do you pass an argument to a pipe?",
        ar:"بتمرر معامل للـ pipe إزاي؟"},
    opts:[{en:"After a colon: <code>{{ total | currency:'EGP' }}</code>",
           ar:"بعد النقطتين: <code>{{ total | currency:'EGP' }}</code>"},
          {en:"In parentheses: <code>currency('EGP')</code>", ar:"بين قوسين: <code>currency('EGP')</code>"},
          {en:"As a second pipe", ar:"كـ pipe تانية"},
          {en:"Pipes take no arguments", ar:"الـ pipes مبتاخدش معاملات"}],
    a:0,
    why:{en:"Colons separate the arguments, and you can chain several: <code>{{ d | date:'medium' }}</code>, <code>{{ x | number:'1.2-2' }}</code>. Pipes also chain left to right — <code>{{ s | slice:0:20 | uppercase }}</code>.",
         ar:"النقطتين بتفصل المعاملات، وتقدر تحط أكتر من واحد: <code>{{ d | date:'medium' }}</code> و<code>{{ x | number:'1.2-2' }}</code>. والـ pipes بتتسلسل من الشمال لليمين كمان — <code>{{ s | slice:0:20 | uppercase }}</code>."} },

  { q: {en:"What does the <code>async</code> pipe do?",
        ar:"الـ pipe اسمها <code>async</code> بتعمل إيه؟"},
    opts:[{en:"Subscribes to an observable, shows the latest value, and unsubscribes on destroy",
           ar:"بتعمل subscribe لـ observable، وبتعرض آخر قيمة، وبتفصل عند التدمير"},
          {en:"Makes the template render asynchronously", ar:"بتخلي الـ template يترسم بشكل غير متزامن"},
          {en:"Delays rendering until data arrives", ar:"بتأجّل الرسم لحد ما البيانات توصل"},
          {en:"Converts a promise to a signal", ar:"بتحوّل promise لـ signal"}],
    a:0,
    why:{en:"It is the tidy way to render a stream: no <code>subscribe()</code> in your class and no <code>ngOnDestroy</code> to remember. In signal-based code <code>toSignal()</code> does the same job and reads more naturally in the template.",
         ar:"دي الطريقة المرتبة لرسم stream: مفيش <code>subscribe()</code> في الكلاس ومفيش <code>ngOnDestroy</code> تفتكره. وفي الكود القايم على signals، <code>toSignal()</code> بتعمل نفس الشغل وبتتقرا أطبع في الـ template."} },

  { q: {en:"Why avoid a pipe that filters or sorts a list?",
        ar:"ليه نتجنّب pipe بتفلتر أو بترتّب قائمة؟"},
    opts:[{en:"It hides work that runs on every render — a <code>computed()</code> is clearer and cached",
           ar:"بتخبّي شغل بيتنفّذ مع كل رسم — و<code>computed()</code> أوضح ومحفوظة"},
          {en:"Pipes cannot return arrays", ar:"الـ pipes مبترجّعش مصفوفات"},
          {en:"Pipes cannot take arguments", ar:"الـ pipes مبتاخدش معاملات"},
          {en:"Filtering must happen on the server", ar:"الفلترة لازم تحصل على السيرفر"}],
    a:0,
    why:{en:"The Angular team deliberately ships no <code>filter</code> or <code>orderBy</code> pipe. A pure pipe that receives a new array reference every render recomputes every render, and it also blocks tree-shaking of the sort logic. Derive the list with <code>computed()</code> instead.",
         ar:"فريق أنجولار متعمّد إنه مبيقدّمش pipe لـ <code>filter</code> أو <code>orderBy</code>. الـ pipe الـ pure اللي بتاخد مرجع مصفوفة جديد مع كل رسم بتعيد الحساب مع كل رسم، وكمان بتمنع الـ tree-shaking لمنطق الترتيب. اشتق القائمة بـ <code>computed()</code> بدالها."} },
],

'basic-routing': [
  { q: {en:"Which one navigates without reloading the page?",
        ar:"أنهي واحدة بتنقل من غير إعادة تحميل الصفحة؟"},
    opts:[{en:"<code>&lt;a routerLink=\"/about\"&gt;</code>", ar:"<code>&lt;a routerLink=\"/about\"&gt;</code>"},
          {en:"<code>&lt;a href=\"/about\"&gt;</code>", ar:"<code>&lt;a href=\"/about\"&gt;</code>"},
          {en:"<code>window.location = '/about'</code>", ar:"<code>window.location = '/about'</code>"},
          {en:"<code>location.reload()</code>", ar:"<code>location.reload()</code>"}],
    a:0,
    why:{en:"<code>routerLink</code> hands the click to the Angular router, which swaps the component inside <code>&lt;router-outlet&gt;</code>. A plain <code>href</code> makes the browser fetch the whole app again — you will see it flash, and all your state is gone.",
         ar:"<code>routerLink</code> بتسلّم الكليك لـ router أنجولار، وهو بيبدّل الـ component اللي جوه <code>&lt;router-outlet&gt;</code>. أما <code>href</code> العادية فبتخلي المتصفح يجيب التطبيق كله تاني — هتشوفه بيرمش، وكل حالتك ضاعت."} },

  { q: {en:"What does <code>&lt;router-outlet&gt;</code> mark?",
        ar:"<code>&lt;router-outlet&gt;</code> بتحدد إيه؟"},
    opts:[{en:"The spot where the matched route's component is rendered",
           ar:"المكان اللي بيترسم فيه component الـ route المطابق"},
          {en:"The list of routes", ar:"قائمة المسارات"},
          {en:"The default route", ar:"المسار الافتراضي"},
          {en:"A navigation menu", ar:"قائمة تنقّل"}],
    a:0,
    why:{en:"Everything around the outlet — header, sidebar, footer — stays put while the outlet's contents change. That is the whole shape of a routed Angular app, and nesting outlets is how you get layouts inside layouts.",
         ar:"كل حاجة حوالين الـ outlet — الهيدر والسايدبار والفوتر — بتفضل مكانها وبس اللي جواه بيتغيّر. وده شكل تطبيق أنجولار الموجّه بالمسارات كله، ولما تعشّش outlets بتاخد تخطيطات جوه تخطيطات."} },

  { q: {en:"How does a page read the <code>7</code> out of <code>/user/7</code>?",
        ar:"إزاي صفحة تقرا الرقم <code>7</code> من <code>/user/7</code>؟"},
    opts:[{en:"A route param — <code>path: 'user/:id'</code>, then read <code>id</code>",
           ar:"معامل مسار — <code>path: 'user/:id'</code>، وبعدين اقرا <code>id</code>"},
          {en:"An <code>@Input()</code> from the router outlet", ar:"<code>@Input()</code> من الـ router outlet"},
          {en:"A global variable set before navigating", ar:"متغيّر عام بيتحدد قبل التنقل"},
          {en:"<code>localStorage</code>", ar:"<code>localStorage</code>"}],
    a:0,
    why:{en:"The URL is the channel between two pages, because there is no template between them to bind through. It also makes the page refreshable, bookmarkable and shareable — none of which a variable gives you.",
         ar:"الـ URL هو القناة بين صفحتين، لأن مفيش template بينهم تربط من خلاله. وكمان بيخلي الصفحة قابلة للتحديث والحفظ والمشاركة — وولا حاجة من دول المتغيّر بيدّيها لك."} },

  { q: {en:"What does <code>loadComponent</code> in a route do?",
        ar:"<code>loadComponent</code> في الـ route بتعمل إيه؟"},
    opts:[{en:"Downloads that page's code only when the route is first visited",
           ar:"بتنزّل كود الصفحة دي بس لما حد يزور الـ route لأول مرة"},
          {en:"Loads it during startup", ar:"بتحمّلها وقت بداية التشغيل"},
          {en:"Preloads every route", ar:"بتحمّل كل المسارات مقدماً"},
          {en:"Caches the rendered HTML", ar:"بتخزّن الـ HTML المرسوم"}],
    a:0,
    why:{en:"<code>loadComponent: () =&gt; import('./about')</code> splits that page into its own chunk, so the first load only ships what the first screen needs. It is the cheapest performance win in an Angular app.",
         ar:"<code>loadComponent: () =&gt; import('./about')</code> بتفصل الصفحة دي في حزمة لوحدها، فأول تحميل بينزّل بس اللي أول شاشة محتاجاه. وده أرخص مكسب أداء في تطبيق أنجولار."} },
],

'template-driven-forms': [
  { q: {en:"What must be imported for <code>ngModel</code> to work?",
        ar:"لازم نعمل import لإيه علشان <code>ngModel</code> تشتغل؟"},
    opts:[{en:"<code>FormsModule</code>", ar:"<code>FormsModule</code>"},
          {en:"<code>ReactiveFormsModule</code>", ar:"<code>ReactiveFormsModule</code>"},
          {en:"<code>CommonModule</code>", ar:"<code>CommonModule</code>"},
          {en:"Nothing; it is built in", ar:"مفيش؛ هي مدمجة"}],
    a:0,
    why:{en:"<code>FormsModule</code> is for template-driven forms (<code>ngModel</code>, <code>ngForm</code>); <code>ReactiveFormsModule</code> is for <code>formGroup</code> and <code>formControlName</code>. Importing the wrong one gives you the confusing <i>can't bind to ngModel</i> error.",
         ar:"<code>FormsModule</code> للفورمز اللي بتتقاد من الـ template (<code>ngModel</code> و<code>ngForm</code>)، و<code>ReactiveFormsModule</code> لـ <code>formGroup</code> و<code>formControlName</code>. ولو عملت import للغلط هتطلعلك رسالة <i>can't bind to ngModel</i> المحيّرة."} },

  { q: {en:"When is a template-driven form the right choice?",
        ar:"إمتى الفورم اللي بتتقاد من الـ template تكون الاختيار الصح؟"},
    opts:[{en:"Small, mostly static forms with simple validation",
           ar:"فورمز صغيرة، ثابتة في الغالب، وبتحقّق بسيط"},
          {en:"Forms with dynamic fields added at runtime", ar:"فورمز فيها خانات بتتضاف وقت التشغيل"},
          {en:"Forms needing cross-field validation", ar:"فورمز محتاجة تحقّق بين الخانات"},
          {en:"Any form; reactive forms are deprecated", ar:"أي فورم؛ الـ reactive forms مهجورة"}],
    a:0,
    why:{en:"A login box or a contact form is faster to write this way. Once you need fields that appear and disappear, validators that compare two fields, or a form you want to unit test without a DOM, reactive forms are the better tool.",
         ar:"خانة تسجيل دخول أو فورم تواصل بتتكتب أسرع بالطريقة دي. وأول ما تحتاج خانات تظهر وتختفي، أو تحقّق بيقارن خانتين، أو فورم عايز تختبرها من غير DOM، الـ reactive forms تبقى الأداة الأحسن."} },

  { q: {en:"What is <code>#f=\"ngForm\"</code> for?",
        ar:"<code>#f=\"ngForm\"</code> بتستخدم لإيه؟"},
    opts:[{en:"It gives you the form's state — <code>f.valid</code>, <code>f.value</code>, <code>f.dirty</code>",
           ar:"بتدّيك حالة الفورم — <code>f.valid</code> و<code>f.value</code> و<code>f.dirty</code>"},
          {en:"It names the submit handler", ar:"بتسمّي دالة الإرسال"},
          {en:"It registers the form with Angular", ar:"بتسجّل الفورم في أنجولار"},
          {en:"It sets the form's id attribute", ar:"بتحدد سمة id للفورم"}],
    a:0,
    why:{en:"The <code>=\"ngForm\"</code> part asks for the directive instance rather than the plain element, which is where all the tracking lives. That is how <code>&lt;button [disabled]=\"f.invalid\"&gt;</code> works with no code in the class at all.",
         ar:"جزء <code>=\"ngForm\"</code> بيطلب نسخة الـ directive بدل العنصر العادي، وهي المكان اللي فيه كل التتبّع. وده إزاي <code>&lt;button [disabled]=\"f.invalid\"&gt;</code> بتشتغل من غير أي كود في الكلاس خالص."} },

  { q: {en:"An error message shows before the user has typed anything. What is the usual fix?",
        ar:"رسالة خطأ ظاهرة قبل إن المستخدم يكتب أي حاجة. إيه الحل المعتاد؟"},
    opts:[{en:"Also check <code>touched</code> or <code>dirty</code>, not just <code>invalid</code>",
           ar:"شيّك على <code>touched</code> أو <code>dirty</code> كمان، مش <code>invalid</code> بس"},
          {en:"Remove the <code>required</code> validator", ar:"شيل مُحقّق <code>required</code>"},
          {en:"Set the field's initial value", ar:"حدد قيمة ابتدائية للخانة"},
          {en:"Validate only on submit", ar:"تحقّق عند الإرسال بس"}],
    a:0,
    why:{en:"An empty required field is invalid from the very first render — that is correct, it just should not be shouted about yet. <code>@if (email.invalid &amp;&amp; email.touched)</code> waits until the user has actually visited the field.",
         ar:"الخانة المطلوبة والفاضية بتكون غير صالحة من أول رسم — وده صح، بس مش وقته إننا نزعّق. و<code>@if (email.invalid &amp;&amp; email.touched)</code> بتستنى لحد ما المستخدم يزور الخانة فعلاً."} },
],

'http-basics': [
  { q: {en:"Where do you switch <code>HttpClient</code> on?",
        ar:"بتشغّل <code>HttpClient</code> فين؟"},
    opts:[{en:"<code>provideHttpClient()</code> in <code>app.config.ts</code>",
           ar:"<code>provideHttpClient()</code> في <code>app.config.ts</code>"},
          {en:"Import <code>HttpClientModule</code> in every component", ar:"اعمل import لـ <code>HttpClientModule</code> في كل component"},
          {en:"<code>inject(HttpClient)</code> is enough", ar:"<code>inject(HttpClient)</code> تكفي"},
          {en:"It is on by default", ar:"شغالة افتراضياً"}],
    a:0,
    why:{en:"Without that one line, <code>inject(HttpClient)</code> throws a <i>no provider</i> error. It goes in the app-wide <code>providers</code> array once — and interceptors are added right there too, via <code>withInterceptors([...])</code>.",
         ar:"من غير السطر ده، <code>inject(HttpClient)</code> بترمي خطأ <i>no provider</i>. بتتحط في مصفوفة <code>providers</code> بتاعة التطبيق مرة واحدة — والـ interceptors بتتضاف في نفس المكان عن طريق <code>withInterceptors([...])</code>."} },

  { q: {en:"You call <code>this.http.get(url)</code> and nothing happens. Why?",
        ar:"بتنادي <code>this.http.get(url)</code> ومفيش حاجة بتحصل. ليه؟"},
    opts:[{en:"Nobody subscribed — the observable is cold until someone does",
           ar:"محدش عمل subscribe — الـ observable بارد لحد ما حد يعمل"},
          {en:"The URL must be absolute", ar:"الـ URL لازم يكون مطلق"},
          {en:"<code>get()</code> needs <code>await</code>", ar:"<code>get()</code> محتاجة <code>await</code>"},
          {en:"It only works inside <code>ngOnInit</code>", ar:"بتشتغل بس جوه <code>ngOnInit</code>"}],
    a:0,
    why:{en:"An <code>HttpClient</code> observable does nothing until it is subscribed — by <code>subscribe()</code>, by the <code>async</code> pipe, or by <code>toSignal()</code>. This laziness is also why every subscription fires its own request.",
         ar:"observable الـ <code>HttpClient</code> مبيعملش حاجة لحد ما حد يعمل له subscribe — بـ <code>subscribe()</code>، أو بـ pipe الـ <code>async</code>، أو بـ <code>toSignal()</code>. والتأجيل ده كمان هو السبب إن كل subscribe بيطلق طلب لوحده."} },

  { q: {en:"Where should the <code>http.get</code> call live?",
        ar:"نداء <code>http.get</code> يعيش فين؟"},
    opts:[{en:"In a service the components inject", ar:"في service الـ components بتعمل لها inject"},
          {en:"In each component that needs the data", ar:"في كل component محتاج البيانات"},
          {en:"In <code>app.config.ts</code>", ar:"في <code>app.config.ts</code>"},
          {en:"In the template", ar:"في الـ template"}],
    a:0,
    why:{en:"A service gives you one place for the URL, the types and the error handling, and lets you swap in a fake for tests. Components should ask for data, not know how it is fetched.",
         ar:"الـ service بتدّيك مكان واحد للـ URL والأنواع ومعالجة الأخطاء، وبتخليك تحط بديل مزيّف في الاختبارات. والـ components المفروض تطلب البيانات، مش تعرف بتتجاب إزاي."} },

  { q: {en:"The template throws <i>cannot read properties of undefined</i> right after loading. Best fix?",
        ar:"الـ template بيرمي <i>cannot read properties of undefined</i> بعد التحميل فوراً. أحسن حل؟"},
    opts:[{en:"Give the field a safe default and guard the block with <code>@if</code>",
           ar:"دّي المتغيّر قيمة ابتدائية آمنة واحمِ البلوك بـ <code>@if</code>"},
          {en:"Wrap the call in <code>setTimeout</code>", ar:"لُف النداء في <code>setTimeout</code>"},
          {en:"Make the component <code>async</code>", ar:"خلي الـ component <code>async</code>"},
          {en:"Add <code>!</code> to every property in the template", ar:"ضيف <code>!</code> لكل خاصية في الـ template"}],
    a:0,
    why:{en:"The first render happens before the response arrives, with the field still empty. Start from <code>items: Item[] = []</code>, or wrap the block in <code>@if (user())</code>. <code>setTimeout</code> happens to work sometimes, which makes it a trap.",
         ar:"أول رسم بيحصل قبل ما الرد يوصل، والمتغيّر لسه فاضي. ابدأ بـ <code>items: Item[] = []</code>، أو لُف البلوك في <code>@if (user())</code>. أما <code>setTimeout</code> فبتشتغل بالعافية أحياناً، وعلشان كده هي فخ."} },
],

/* ========================== intermediate ========================== */

'routing': [
  { q: {en:"A guard returns <code>false</code>. What happens?",
        ar:"guard رجّع <code>false</code>. إيه اللي بيحصل؟"},
    opts:[{en:"The navigation is cancelled and the URL stays where it was",
           ar:"التنقل بيتلغي والـ URL بيفضل مكانه"},
          {en:"The route loads but the component is empty", ar:"المسار بيتحمّل بس الـ component بيبقى فاضي"},
          {en:"Angular redirects to the first route", ar:"أنجولار بيوجّه لأول مسار"},
          {en:"The guard runs again", ar:"الـ guard بيشتغل تاني"}],
    a:0,
    why:{en:"A falsy guard result aborts the navigation entirely. Returning a <code>UrlTree</code> instead — <code>return router.parseUrl('/login')</code> — cancels it <i>and</i> sends the user somewhere useful, which is almost always what you want.",
         ar:"لو الـ guard رجّع قيمة كاذبة، التنقل بيتلغي بالكامل. ولو رجّعت <code>UrlTree</code> بدالها — <code>return router.parseUrl('/login')</code> — بتلغيه <i>وكمان</i> بتوصّل المستخدم لمكان مفيد، وده اللي إنت عايزه في الغالب."} },

  { q: {en:"Where must the wildcard route <code>{ path: '**' }</code> go?",
        ar:"مسار الـ wildcard <code>{ path: '**' }</code> لازم يكون فين؟"},
    opts:[{en:"Last — routes are matched in order, first match wins",
           ar:"في الآخر — المسارات بتتطابق بالترتيب، وأول تطابق بيفوز"},
          {en:"First", ar:"في الأول"},
          {en:"Anywhere; Angular sorts them", ar:"في أي مكان؛ أنجولار بيرتّبهم"},
          {en:"In a separate array", ar:"في مصفوفة منفصلة"}],
    a:0,
    why:{en:"The router walks the array top to bottom and stops at the first match. Put <code>'**'</code> anywhere but last and it swallows every route below it — your app becomes one big 404 page.",
         ar:"الـ router بيمشي في المصفوفة من فوق لتحت وبيوقف عند أول تطابق. ولو حطيت <code>'**'</code> في أي مكان غير الآخر، هيبلع كل المسارات اللي تحته — وتطبيقك يبقى صفحة 404 كبيرة."} },

  { q: {en:"<code>loadComponent</code> versus <code>loadChildren</code> — which is which?",
        ar:"<code>loadComponent</code> مقابل <code>loadChildren</code> — مين فيهم إيه؟"},
    opts:[{en:"One lazy component versus a lazy group of routes",
           ar:"component واحد مؤجّل مقابل مجموعة مسارات مؤجّلة"},
          {en:"Standalone versus module-based, otherwise identical", ar:"standalone مقابل module، وغير كده نفس الحاجة"},
          {en:"<code>loadChildren</code> is deprecated", ar:"<code>loadChildren</code> مهجورة"},
          {en:"<code>loadComponent</code> preloads, <code>loadChildren</code> does not", ar:"<code>loadComponent</code> بتحمّل مقدماً و<code>loadChildren</code> لأ"}],
    a:0,
    why:{en:"<code>loadComponent</code> lazily loads one page. <code>loadChildren</code> lazily loads a whole child route array — a settings area with five tabs, say — so the entire section becomes one chunk.",
         ar:"<code>loadComponent</code> بتحمّل صفحة واحدة عند الحاجة. و<code>loadChildren</code> بتحمّل مصفوفة مسارات فرعية كاملة — زي قسم إعدادات فيه خمس تابات — فالقسم كله يبقى حزمة واحدة."} },

  { q: {en:"Navigating <code>/user/1</code> → <code>/user/2</code> does not reload the data. Why?",
        ar:"التنقل من <code>/user/1</code> لـ <code>/user/2</code> مش بيعيد تحميل البيانات. ليه؟"},
    opts:[{en:"Same route, same component instance — <code>ngOnInit</code> does not run again",
           ar:"نفس المسار ونفس نسخة الـ component — <code>ngOnInit</code> مش بتشتغل تاني"},
          {en:"The router caches responses", ar:"الـ router بيخزّن الردود"},
          {en:"The guard blocked it", ar:"الـ guard منعه"},
          {en:"Params only update on a full reload", ar:"المعاملات بتتحدّث بس مع إعادة تحميل كاملة"}],
    a:0,
    why:{en:"Angular reuses the component rather than destroying it, so a <code>route.snapshot</code> read in <code>ngOnInit</code> happens once and never again. Subscribe to <code>route.paramMap</code>, or use route input binding with an <code>effect()</code>.",
         ar:"أنجولار بيستخدم الـ component تاني بدل ما يهدمه، فقراية <code>route.snapshot</code> في <code>ngOnInit</code> بتحصل مرة واحدة وخلاص. اعمل subscribe لـ <code>route.paramMap</code>، أو استخدم ربط الـ inputs بالمسار مع <code>effect()</code>."} },
],

'http': [
  { q: {en:"Where is an interceptor registered?",
        ar:"الـ interceptor بيتسجّل فين؟"},
    opts:[{en:"<code>provideHttpClient(withInterceptors([authInterceptor]))</code>",
           ar:"<code>provideHttpClient(withInterceptors([authInterceptor]))</code>"},
          {en:"In each service that calls the API", ar:"في كل service بتنادي الـ API"},
          {en:"<code>@Injectable({ providedIn: 'root' })</code> is enough", ar:"<code>@Injectable({ providedIn: 'root' })</code> تكفي"},
          {en:"In <code>angular.json</code>", ar:"في <code>angular.json</code>"}],
    a:0,
    why:{en:"Functional interceptors are listed once, next to <code>provideHttpClient()</code>, and every request in the app then passes through them in order. That is the whole appeal: the token and the error handling live in one file.",
         ar:"الـ interceptors الوظيفية بتتسجّل مرة واحدة جنب <code>provideHttpClient()</code>، وبعدها كل طلب في التطبيق بيعدّي منها بالترتيب. وده كل جمالها: التوكن ومعالجة الأخطاء في ملف واحد."} },

  { q: {en:"Why does an interceptor <code>clone()</code> the request?",
        ar:"ليه الـ interceptor بيعمل <code>clone()</code> للطلب؟"},
    code:[{name:'auth.interceptor.ts', lang:'ts', lines:[
      "export const authInterceptor: HttpInterceptorFn = (req, next) => {",
      "  const withToken = req.clone({",
      "    setHeaders: { Authorization: 'Bearer ' + token() },",
      "  });",
      "  return next(withToken);",
      "};"]}],
    opts:[{en:"<code>HttpRequest</code> is immutable — you cannot edit it in place",
           ar:"<code>HttpRequest</code> غير قابل للتغيير — مينفعش تعدّله في مكانه"},
          {en:"To send the request twice", ar:"علشان تبعت الطلب مرتين"},
          {en:"To keep it in the cache", ar:"علشان تحتفظ بيه في الكاش"},
          {en:"Cloning is optional style", ar:"الاستنساخ ده أسلوب اختياري"}],
    a:0,
    why:{en:"Requests are deliberately immutable so that a retry replays exactly what was sent. <code>clone()</code> hands you a modified copy; assigning to <code>req.headers</code> directly does nothing at all.",
         ar:"الطلبات غير قابلة للتغيير بشكل مقصود علشان إعادة المحاولة تبعت نفس اللي اتبعت بالظبط. و<code>clone()</code> بتدّيك نسخة معدّلة، أما إنك تكتب في <code>req.headers</code> مباشرة فمش بيعمل حاجة خالص."} },

  { q: {en:"<code>catchError</code> must return what?",
        ar:"<code>catchError</code> لازم ترجّع إيه؟"},
    opts:[{en:"An observable — a fallback value, or <code>throwError</code> to rethrow",
           ar:"observable — قيمة بديلة، أو <code>throwError</code> لإعادة الرمي"},
          {en:"A plain value", ar:"قيمة عادية"},
          {en:"Nothing", ar:"مفيش حاجة"},
          {en:"A promise", ar:"promise"}],
    a:0,
    why:{en:"The stream has to continue with <i>something</i>. <code>of([])</code> recovers with an empty list; <code>throwError(() =&gt; err)</code> passes the problem on. Returning a bare value is a type error and a common first mistake.",
         ar:"الـ stream لازم يكمل بـ <i>حاجة</i>. <code>of([])</code> بتتعافى بقائمة فاضية، و<code>throwError(() =&gt; err)</code> بتمرر المشكلة لبعدين. وإنك ترجّع قيمة عادية ده خطأ نوع، وغلطة أولى شائعة."} },

  { q: {en:"Two components both subscribe to the same <code>http.get()</code>. How many requests?",
        ar:"اتنين components بيعملوا subscribe لنفس <code>http.get()</code>. كام طلب؟"},
    opts:[{en:"Two — each subscription fires its own request",
           ar:"اتنين — كل subscribe بيطلق طلب لوحده"},
          {en:"One, it is shared", ar:"واحد، بيتشارك"},
          {en:"One, then it is cached", ar:"واحد، وبعدها بيتخزّن"},
          {en:"None until change detection runs", ar:"ولا حاجة لحد ما فحص التغييرات يشتغل"}],
    a:0,
    why:{en:"HTTP observables are cold: subscribing is what starts the work. To share one response add <code>shareReplay(1)</code>, or hold the result in a signal in the service so every reader sees the same value.",
         ar:"observables الـ HTTP باردة: الـ subscribe هو اللي بيبدأ الشغل. وعلشان تشارك رد واحد ضيف <code>shareReplay(1)</code>، أو احتفظ بالنتيجة في signal جوه الـ service فكل اللي بيقرا يشوف نفس القيمة."} },
],

'forms': [
  { q: {en:"A validator compares two fields. Where does it go?",
        ar:"مُحقّق بيقارن خانتين. بيتحط فين؟"},
    opts:[{en:"On the <code>FormGroup</code> — only it can see both controls",
           ar:"على الـ <code>FormGroup</code> — هو الوحيد اللي شايف الخانتين"},
          {en:"On each control", ar:"على كل خانة"},
          {en:"On the second control only", ar:"على الخانة التانية بس"},
          {en:"In the template", ar:"في الـ template"}],
    a:0,
    why:{en:"A control validator only ever receives its own value. Cross-field rules — password confirmation, end-after-start — belong on the group, where both values are in hand.",
         ar:"مُحقّق الخانة بياخد قيمته هو بس. أما القواعد اللي بين الخانات — زي تأكيد كلمة السر أو تاريخ نهاية بعد البداية — فمكانها على الـ group، حيث القيمتين في إيدك."} },

  { q: {en:"<code>form.value</code> is missing a field you know exists. Why?",
        ar:"<code>form.value</code> ناقص خانة إنت متأكد إنها موجودة. ليه؟"},
    opts:[{en:"That control is disabled — use <code>getRawValue()</code>",
           ar:"الخانة دي معطّلة — استخدم <code>getRawValue()</code>"},
          {en:"It has no validator", ar:"مفيهاش مُحقّق"},
          {en:"It was never touched", ar:"محدش لمسها"},
          {en:"It is invalid", ar:"هي غير صالحة"}],
    a:0,
    why:{en:"<code>value</code> deliberately omits disabled controls, on the grounds that the user could not edit them. <code>getRawValue()</code> returns everything. This bites hardest when a disabled id silently stops reaching your API.",
         ar:"<code>value</code> بتستبعد الخانات المعطّلة بشكل مقصود، على أساس إن المستخدم مقدرش يعدّلها. و<code>getRawValue()</code> بترجّع كل حاجة. والمشكلة دي بتوجع أكتر لما id معطّل يبطّل يوصل للـ API بهدوء."} },

  { q: {en:"<code>setValue</code> versus <code>patchValue</code>?",
        ar:"<code>setValue</code> مقابل <code>patchValue</code>؟"},
    opts:[{en:"<code>setValue</code> needs every control; <code>patchValue</code> updates only the keys you pass",
           ar:"<code>setValue</code> عايزة كل الخانات، و<code>patchValue</code> بتحدّث المفاتيح اللي بعتها بس"},
          {en:"They are identical", ar:"الاتنين نفس الحاجة"},
          {en:"<code>patchValue</code> also runs validators", ar:"<code>patchValue</code> بتشغّل المُحقّقات كمان"},
          {en:"<code>setValue</code> is for single controls only", ar:"<code>setValue</code> للخانات المفردة بس"}],
    a:0,
    why:{en:"<code>setValue</code> is strict and will throw if a key is missing — useful when you want that safety. <code>patchValue</code> is forgiving, which makes it right for filling part of a form from a server response.",
         ar:"<code>setValue</code> صارمة وبترمي خطأ لو مفتاح ناقص — وده مفيد لما تعوز الأمان ده. و<code>patchValue</code> متسامحة، وعلشان كده هي الصح لما تملا جزء من فورم من رد السيرفر."} },

  { q: {en:"Which module do reactive forms need?",
        ar:"الـ reactive forms محتاجة أنهي module؟"},
    opts:[{en:"<code>ReactiveFormsModule</code>", ar:"<code>ReactiveFormsModule</code>"},
          {en:"<code>FormsModule</code>", ar:"<code>FormsModule</code>"},
          {en:"<code>CommonModule</code>", ar:"<code>CommonModule</code>"},
          {en:"None", ar:"ولا واحد"}],
    a:0,
    why:{en:"<code>ReactiveFormsModule</code> brings <code>formGroup</code>, <code>formControlName</code> and <code>formArrayName</code>. <code>FormsModule</code> is the template-driven one, with <code>ngModel</code> — importing the wrong one produces a puzzling <i>can't bind to</i> error.",
         ar:"<code>ReactiveFormsModule</code> بتجيب <code>formGroup</code> و<code>formControlName</code> و<code>formArrayName</code>. و<code>FormsModule</code> هي بتاعة الـ template مع <code>ngModel</code> — ولو عملت import للغلط هتطلعلك رسالة <i>can't bind to</i> محيّرة."} },
],

'effects-vs-computed': [
  { q: {en:"Which one gives you back a value?",
        ar:"أنهي واحدة بترجّعلك قيمة؟"},
    opts:[{en:"<code>computed()</code> — an <code>effect()</code> returns nothing",
           ar:"<code>computed()</code> — و<code>effect()</code> مبترجّعش حاجة"},
          {en:"<code>effect()</code>", ar:"<code>effect()</code>"},
          {en:"Both", ar:"الاتنين"},
          {en:"Neither", ar:"ولا واحدة"}],
    a:0,
    why:{en:"That single sentence decides which to use. Need a number, a list, a flag? <code>computed()</code>. Need something to <i>happen</i> — a log, a <code>localStorage</code> write, a chart redraw? <code>effect()</code>.",
         ar:"الجملة الواحدة دي بتحدد إنت هتستخدم مين. محتاج رقم أو قائمة أو علامة؟ <code>computed()</code>. محتاج حاجة <i>تحصل</i> — سجل، أو كتابة في <code>localStorage</code>، أو إعادة رسم شارت؟ <code>effect()</code>."} },

  { q: {en:"An effect writes to a signal it also reads. What happens?",
        ar:"effect بيكتب في signal وبيقراها كمان. إيه اللي بيحصل؟"},
    opts:[{en:"It re-triggers itself — a loop Angular will warn you about",
           ar:"بيعيد تشغيل نفسه — لفة أنجولار هيحذّرك منها"},
          {en:"It runs exactly twice", ar:"بيشتغل مرتين بالظبط"},
          {en:"Nothing; writes are ignored inside effects", ar:"مفيش حاجة؛ الكتابة بتتجاهل جوه الـ effects"},
          {en:"The signal becomes read-only", ar:"الـ signal بتبقى للقراءة بس"}],
    a:0,
    why:{en:"Reading registers a dependency, so writing to that same signal schedules the effect again. If you genuinely need it, read with <code>untracked()</code> — but a value derived from other values almost always wants <code>computed()</code> instead.",
         ar:"القراية بتسجّل تبعية، فالكتابة في نفس الـ signal بتجدول الـ effect تاني. ولو محتاج ده فعلاً، اقرا بـ <code>untracked()</code> — بس القيمة المشتقّة من قيم تانية بتعوز <code>computed()</code> في الغالب."} },

  { q: {en:"Which of these is the right job for an <code>effect()</code>?",
        ar:"أنهي واحدة من دول شغل مناسب لـ <code>effect()</code>؟"},
    opts:[{en:"Saving the current theme to <code>localStorage</code>",
           ar:"حفظ الثيم الحالي في <code>localStorage</code>"},
          {en:"Working out a cart total", ar:"حساب إجمالي السلة"},
          {en:"Filtering a list for display", ar:"فلترة قائمة للعرض"},
          {en:"Deciding whether a button is disabled", ar:"تحديد إن الزرار معطّل ولا لأ"}],
    a:0,
    why:{en:"The other three are values, and values belong in <code>computed()</code> — cached, pure, easy to test. An effect is for reaching outside Angular: storage, analytics, a third-party chart, the document title.",
         ar:"التلاتة التانيين قيم، والقيم مكانها <code>computed()</code> — محفوظة ونقية وسهلة الاختبار. أما الـ effect فهو للوصول لبره أنجولار: التخزين والتحليلات وشارت من مكتبة خارجية وعنوان الصفحة."} },

  { q: {en:"When is an <code>effect()</code> cleaned up?",
        ar:"الـ <code>effect()</code> بيتنضّف إمتى؟"},
    opts:[{en:"When its injection context is destroyed — usually the component",
           ar:"لما سياق الـ injection بتاعه يتهدم — وغالباً ده الـ component"},
          {en:"Never; you must stop it by hand", ar:"أبداً؛ لازم توقّفه بإيدك"},
          {en:"After it runs once", ar:"بعد ما يشتغل مرة"},
          {en:"On the next navigation", ar:"عند التنقل اللي بعده"}],
    a:0,
    why:{en:"Created in a constructor or a field initialiser, an effect is tied to that component's lifetime and disposed with it. Created outside an injection context you must pass an injector — or keep the <code>EffectRef</code> and call <code>destroy()</code>.",
         ar:"لو اتعمل في constructor أو تهيئة متغيّر، الـ effect بيبقى مربوط بعمر الـ component وبيتخلص معاه. ولو اتعمل بره سياق injection لازم تمرر injector — أو تحتفظ بـ <code>EffectRef</code> وتنادي <code>destroy()</code>."} },
],

'queries': [
  { q: {en:"<code>viewChild</code> versus <code>contentChild</code>?",
        ar:"<code>viewChild</code> مقابل <code>contentChild</code>؟"},
    opts:[{en:"Your own template versus markup projected into you",
           ar:"الـ template بتاعك مقابل ماركب متمرَّر لجواك"},
          {en:"Direct child versus any descendant", ar:"ابن مباشر مقابل أي سليل"},
          {en:"Element versus component", ar:"عنصر مقابل component"},
          {en:"Signal versus decorator", ar:"signal مقابل decorator"}],
    a:0,
    why:{en:"<code>viewChild</code> searches the template you wrote. <code>contentChild</code> searches what somebody else wrote between your tags and projected in. Using the wrong one returns <code>undefined</code> and reads like a timing bug.",
         ar:"<code>viewChild</code> بتدوّر في الـ template اللي إنت كتبته. و<code>contentChild</code> بتدوّر في اللي حد تاني كتبه بين تاجاتك وتمرّر لجواك. ولو استخدمت الغلط بترجّع <code>undefined</code> وتبان كأنها مشكلة توقيت."} },

  { q: {en:"A <code>@ViewChild</code> is <code>undefined</code> in <code>ngOnInit</code>. Why?",
        ar:"<code>@ViewChild</code> قيمته <code>undefined</code> في <code>ngOnInit</code>. ليه؟"},
    opts:[{en:"The view has not been created yet — read it in <code>ngAfterViewInit</code>",
           ar:"الـ view لسه ما اتعملش — اقراه في <code>ngAfterViewInit</code>"},
          {en:"The selector is wrong", ar:"الـ selector غلط"},
          {en:"It needs <code>static: false</code>", ar:"محتاج <code>static: false</code>"},
          {en:"Queries only work on components", ar:"الاستعلامات بتشتغل على الـ components بس"}],
    a:0,
    why:{en:"View queries are filled in after the template is built. The signal form — <code>box = viewChild('box')</code> — sidesteps the whole timing question: read it in an <code>effect()</code> and it runs once the element exists.",
         ar:"استعلامات الـ view بتتملى بعد بناء الـ template. والصيغة القايمة على signal — <code>box = viewChild('box')</code> — بتتخطى حكاية التوقيت كلها: اقراه في <code>effect()</code> وهو هيشتغل أول ما العنصر يبقى موجود."} },

  { q: {en:"A queried child sits inside an <code>@if</code>. What should you expect?",
        ar:"ابن مُستعلَم عنه قاعد جوه <code>@if</code>. توقّع إيه؟"},
    opts:[{en:"It is undefined while the condition is false, and appears when it turns true",
           ar:"بيكون undefined طالما الشرط كاذب، وبيظهر لما يبقى صح"},
          {en:"It is always available", ar:"متاح دايماً"},
          {en:"The query throws", ar:"الاستعلام بيرمي خطأ"},
          {en:"It caches the first instance forever", ar:"بيخزّن أول نسخة للأبد"}],
    a:0,
    why:{en:"A conditional child genuinely does not exist while the condition is false, and it is a <i>new</i> instance each time it comes back. This is exactly why signal queries are better here — they update, where a one-time read goes stale.",
         ar:"الابن الشرطي فعلاً مش موجود طالما الشرط كاذب، وكل مرة يرجع بيبقى نسخة <i>جديدة</i>. وعلشان كده بالظبط استعلامات الـ signal أحسن هنا — هي بتتحدّث، أما القراية لمرة واحدة فبتقدم."} },

  { q: {en:"You need every projected tab, not just the first. Which?",
        ar:"محتاج كل التابات المتمرَّرة، مش أول واحدة بس. أنهي؟"},
    opts:[{en:"<code>contentChildren()</code>", ar:"<code>contentChildren()</code>"},
          {en:"<code>contentChild()</code> in a loop", ar:"<code>contentChild()</code> في لوب"},
          {en:"<code>viewChildren()</code>", ar:"<code>viewChildren()</code>"},
          {en:"<code>querySelectorAll</code>", ar:"<code>querySelectorAll</code>"}],
    a:0,
    why:{en:"The plural query returns them all, and the signal version keeps the list current as tabs are added or removed. <code>viewChildren</code> would search your own template, where the tabs are not.",
         ar:"الاستعلام بصيغة الجمع بيرجّعهم كلهم، والنسخة القايمة على signal بتحافظ على القائمة محدّثة وإنت بتضيف أو بتشيل تابات. أما <code>viewChildren</code> فهيدوّر في الـ template بتاعك، واللي التابات مش فيه."} },
],

'error-handling': [
  { q: {en:"Which catches a failed HTTP call before your component sees it?",
        ar:"أنهي واحد بيمسك نداء HTTP فاشل قبل ما الـ component يشوفه؟"},
    opts:[{en:"An <code>HttpInterceptorFn</code>", ar:"<code>HttpInterceptorFn</code>"},
          {en:"<code>ErrorHandler</code>", ar:"<code>ErrorHandler</code>"},
          {en:"<code>window.onerror</code>", ar:"<code>window.onerror</code>"},
          {en:"A route guard", ar:"route guard"}],
    a:0,
    why:{en:"Interceptors sit on the request pipeline, so they are the right place to retry a 503 or turn a 401 into a redirect. <code>ErrorHandler</code> is the last-resort net for everything <i>unexpected</i>, anywhere in the app.",
         ar:"الـ interceptors قاعدة على مسار الطلب، فهي المكان الصح لإعادة محاولة 503 أو تحويل 401 لتوجيه. أما <code>ErrorHandler</code> فهو الشبكة الأخيرة لكل حاجة <i>غير متوقّعة</i>، في أي مكان في التطبيق."} },

  { q: {en:"What should the user see when a request fails?",
        ar:"المستخدم المفروض يشوف إيه لما طلب يفشل؟"},
    opts:[{en:"A plain message and a way to retry",
           ar:"رسالة واضحة وطريقة يحاول تاني"},
          {en:"The raw error object", ar:"كائن الخطأ الخام"},
          {en:"Nothing; log it silently", ar:"مفيش حاجة؛ سجّله بهدوء"},
          {en:"A full page reload", ar:"إعادة تحميل الصفحة كلها"}],
    a:0,
    why:{en:"A stack trace helps nobody and can leak internals; silence makes the app look broken. The pattern that works is a short human sentence plus a retry button — and the technical detail sent to your logging service.",
         ar:"تتبّع المكدس مش بيساعد حد وممكن يفضح تفاصيل داخلية، والصمت بيخلي التطبيق يبان مكسور. والنمط اللي بينفع هو جملة قصيرة بلغة البشر وزرار إعادة محاولة — والتفاصيل التقنية تتبعت لخدمة التسجيل بتاعتك."} },

  { q: {en:"<code>retry({ count: 3, delay: 1000 })</code> on a 404. Good idea?",
        ar:"<code>retry({ count: 3, delay: 1000 })</code> على خطأ 404. فكرة حلوة؟"},
    opts:[{en:"No — retry only what might succeed later, like a 503 or a network drop",
           ar:"لأ — أعِد المحاولة بس لللي ممكن ينجح بعدين، زي 503 أو انقطاع الشبكة"},
          {en:"Yes, always retry", ar:"أيوه، دايماً أعِد المحاولة"},
          {en:"Yes, but only once", ar:"أيوه، بس مرة واحدة"},
          {en:"It makes no difference", ar:"مفيش فرق"}],
    a:0,
    why:{en:"A 404 means the thing is not there, and asking three more times will not conjure it — you have just made the user wait three seconds for the same answer. Filter by status, and retry only the transient failures.",
         ar:"الـ 404 معناها إن الحاجة مش موجودة، وإنك تسأل تلات مرات تاني مش هيخلّقها — إنت بس خلّيت المستخدم يستنى تلات ثواني لنفس الرد. فلتر حسب الحالة، وأعِد المحاولة للأخطاء المؤقتة بس."} },

  { q: {en:"What does a custom <code>ErrorHandler</code> replace?",
        ar:"الـ <code>ErrorHandler</code> المخصص بيستبدل إيه؟"},
    opts:[{en:"Angular's default \"log it to the console\" behaviour for uncaught errors",
           ar:"سلوك أنجولار الافتراضي \"سجّله في الكونسول\" للأخطاء غير الملتقطة"},
          {en:"Every <code>try/catch</code> in your code", ar:"كل <code>try/catch</code> في كودك"},
          {en:"HTTP error responses", ar:"ردود أخطاء الـ HTTP"},
          {en:"Template type errors", ar:"أخطاء أنواع الـ template"}],
    a:0,
    why:{en:"Provide your own and every uncaught error in the app flows through one function — the natural place to send it to Sentry and show one toast. It does not replace local handling where you can actually recover.",
         ar:"لما توفّر واحد من عندك، كل خطأ غير ملتقط في التطبيق بيمر من دالة واحدة — وده المكان الطبيعي إنك تبعته لـ Sentry وتعرض toast واحد. وهو مش بديل للمعالجة المحلية في الأماكن اللي بتقدر تتعافى فيها فعلاً."} },
],

'rxjs-fundamentals': [
  { q: {en:"An observable is created but nothing runs. Why?",
        ar:"observable اتعمل ومفيش حاجة بتشتغل. ليه؟"},
    opts:[{en:"It is cold — nothing happens until something subscribes",
           ar:"هو بارد — مفيش حاجة بتحصل لحد ما حاجة تعمل subscribe"},
          {en:"It needs <code>await</code>", ar:"محتاج <code>await</code>"},
          {en:"Observables must be provided", ar:"الـ observables لازم تتوفّر كـ providers"},
          {en:"It only runs after change detection", ar:"بيشتغل بعد فحص التغييرات بس"}],
    a:0,
    why:{en:"An observable is a recipe, not a running process. <code>subscribe()</code>, the <code>async</code> pipe or <code>toSignal()</code> is what starts it — and each of those starts its own separate run.",
         ar:"الـ observable وصفة، مش عملية شغالة. و<code>subscribe()</code> أو pipe الـ <code>async</code> أو <code>toSignal()</code> هو اللي بيبدأه — وكل واحد فيهم بيبدأ تشغيل منفصل لوحده."} },

  { q: {en:"<code>Subject</code> versus <code>BehaviorSubject</code>?",
        ar:"<code>Subject</code> مقابل <code>BehaviorSubject</code>؟"},
    opts:[{en:"<code>BehaviorSubject</code> keeps the latest value and gives it to new subscribers",
           ar:"<code>BehaviorSubject</code> بيحتفظ بآخر قيمة وبيدّيها لأي subscriber جديد"},
          {en:"<code>Subject</code> is typed, <code>BehaviorSubject</code> is not", ar:"<code>Subject</code> مُنمَّط و<code>BehaviorSubject</code> لأ"},
          {en:"<code>Subject</code> can have many subscribers, the other cannot", ar:"<code>Subject</code> ينفع له subscribers كتير والتاني لأ"},
          {en:"They behave identically", ar:"الاتنين نفس السلوك"}],
    a:0,
    why:{en:"A plain <code>Subject</code> has no memory: subscribe after the emit and you get nothing at all. That is the number one cause of <i>my service has the value but my component sees nothing</i>.",
         ar:"الـ <code>Subject</code> العادي مفيهوش ذاكرة: لو عملت subscribe بعد الإرسال مش هتاخد حاجة خالص. وده السبب الأول لمشكلة <i>الـ service فيها القيمة بس الـ component مش شايف حاجة</i>."} },

  { q: {en:"After an observable errors, what comes next?",
        ar:"بعد ما الـ observable يرمي خطأ، إيه اللي بعده؟"},
    opts:[{en:"Nothing — the stream is finished, error is terminal",
           ar:"مفيش — الـ stream خلص، الخطأ نهائي"},
          {en:"It retries automatically", ar:"بيعيد المحاولة تلقائياً"},
          {en:"It emits <code>undefined</code> then continues", ar:"بيرسل <code>undefined</code> وبيكمل"},
          {en:"<code>complete</code> also fires", ar:"<code>complete</code> بيشتغل كمان"}],
    a:0,
    why:{en:"<code>error</code> and <code>complete</code> both end the stream, and only one of them ever fires. That is why a search box that errors once goes dead — you need <code>catchError</code> inside the <code>switchMap</code>, not outside it.",
         ar:"<code>error</code> و<code>complete</code> الاتنين بينهّوا الـ stream، وواحد بس فيهم بيحصل. وعلشان كده خانة بحث لو رمت خطأ مرة بتموت — إنت محتاج <code>catchError</code> جوه الـ <code>switchMap</code>، مش بره."} },

  { q: {en:"What is the tidiest way to stop subscribing when a component dies?",
        ar:"إيه أنضف طريقة توقّف الـ subscribe لما الـ component يموت؟"},
    opts:[{en:"<code>takeUntilDestroyed()</code>, or let the <code>async</code> pipe do it",
           ar:"<code>takeUntilDestroyed()</code>، أو خلي pipe الـ <code>async</code> تعملها"},
          {en:"Store every subscription in an array", ar:"خزّن كل subscription في مصفوفة"},
          {en:"Angular unsubscribes automatically", ar:"أنجولار بيفصل تلقائياً"},
          {en:"Call <code>complete()</code> on the source", ar:"نادي <code>complete()</code> على المصدر"}],
    a:0,
    why:{en:"Manual bookkeeping works but is easy to forget. <code>takeUntilDestroyed()</code> ties the subscription to the component's lifetime in one line, and the <code>async</code> pipe or <code>toSignal()</code> removes the question altogether.",
         ar:"المسك اليدوي ينفع بس سهل تنساه. و<code>takeUntilDestroyed()</code> بتربط الـ subscription بعمر الـ component في سطر واحد، وpipe الـ <code>async</code> أو <code>toSignal()</code> بيشيلوا السؤال من أصله."} },
],

'di-host-directives': [
  { q: {en:"Why use an <code>InjectionToken</code> instead of a class?",
        ar:"ليه نستخدم <code>InjectionToken</code> بدل كلاس؟"},
    opts:[{en:"To inject something that has no class — a config object, a string, a URL",
           ar:"علشان تعمل inject لحاجة مالهاش كلاس — كائن إعدادات، أو نص، أو URL"},
          {en:"It is faster", ar:"أسرع"},
          {en:"Classes cannot be injected", ar:"الكلاسات مينفعش تتعمل inject"},
          {en:"To avoid <code>providedIn: 'root'</code>", ar:"علشان تتجنب <code>providedIn: 'root'</code>"}],
    a:0,
    why:{en:"A class is its own key, but a plain object has no identity to look up. <code>new InjectionToken&lt;AppConfig&gt;('app.config')</code> creates that key — typed, unique, and safe against name collisions.",
         ar:"الكلاس بيبقى مفتاح لنفسه، أما الكائن العادي فمالهوش هوية تدوّر بيها. و<code>new InjectionToken&lt;AppConfig&gt;('app.config')</code> بتعمل المفتاح ده — مُنمَّط وفريد وآمن ضد تعارض الأسماء."} },

  { q: {en:"What do <code>hostDirectives</code> do?",
        ar:"<code>hostDirectives</code> بتعمل إيه؟"},
    opts:[{en:"Attach a directive's behaviour to every instance of your component automatically",
           ar:"بتلزق سلوك directive على كل نسخة من الـ component بشكل تلقائي"},
          {en:"Let a directive host other components", ar:"بتخلي الـ directive يستضيف components تانية"},
          {en:"Replace <code>@HostBinding</code>", ar:"بتستبدل <code>@HostBinding</code>"},
          {en:"Register global directives", ar:"بتسجّل directives عامة"}],
    a:0,
    why:{en:"Instead of asking every caller to remember <code>appAnalytics</code> on the tag, the component declares it once and every use gets it. Composition without inheritance — and the caller cannot forget.",
         ar:"بدل ما تطلب من كل واحد بيستخدمه إنه يفتكر يحط <code>appAnalytics</code> على التاج، الـ component بيعلنها مرة وكل استخدام بياخدها. تركيب من غير وراثة — واللي بيستخدم مش هينسى."} },

  { q: {en:"A service listed in a component's <code>providers</code> — who shares it?",
        ar:"service مسجّلة في <code>providers</code> بتاع component — مين بيشاركها؟"},
    opts:[{en:"That component and everything inside it, and nobody else",
           ar:"الـ component ده وكل اللي جواه، ومحدش تاني"},
          {en:"The whole app", ar:"التطبيق كله"},
          {en:"Only that component", ar:"الـ component ده بس"},
          {en:"Its siblings too", ar:"وأشقاؤه كمان"}],
    a:0,
    why:{en:"It creates a fresh instance for that branch of the tree, shadowing the root one. Deliberately, this is how you give each wizard or each tab group its own private state — accidentally, it is why your service \"forgot\" a value.",
         ar:"بتعمل نسخة جديدة للفرع ده من الشجرة، وبتحجب اللي في الـ root. ولما تكون بقصد، دي طريقتك إنك تدّي كل wizard أو كل مجموعة تابات حالة خاصة — ولما تكون بالغلط، دي سبب إن الـ service \"نسيت\" قيمة."} },

  { q: {en:"<code>inject()</code> throws \"can only be used within an injection context\". When?",
        ar:"<code>inject()</code> بترمي \"can only be used within an injection context\". إمتى؟"},
    opts:[{en:"Called outside a constructor or field initialiser — for example inside a click handler",
           ar:"لما تتنادى بره الـ constructor أو تهيئة متغيّر — مثلاً جوه دالة كليك"},
          {en:"When the service is not <code>providedIn: 'root'</code>", ar:"لما الـ service مش <code>providedIn: 'root'</code>"},
          {en:"In a standalone component", ar:"في component مستقل"},
          {en:"When there are two providers", ar:"لما يكون فيه اتنين providers"}],
    a:0,
    why:{en:"Angular only knows which injector to use while it is constructing your class. Call <code>inject()</code> in a field initialiser or the constructor and store the result — or pass an injector explicitly with <code>runInInjectionContext</code>.",
         ar:"أنجولار بيعرف أنهي injector يستخدم وهو بيبني الكلاس بتاعك بس. فنادي <code>inject()</code> في تهيئة متغيّر أو في الـ constructor واحتفظ بالنتيجة — أو مرّر injector صريح بـ <code>runInInjectionContext</code>."} },
],

'ui-libraries': [
  { q: {en:"What is the CDK, next to Angular Material?",
        ar:"إيه هو الـ CDK، جنب Angular Material؟"},
    opts:[{en:"The unstyled behaviour underneath — overlays, drag and drop, a11y, virtual scroll",
           ar:"السلوك بدون تنسيق اللي تحتيه — overlays وسحب وإفلات وإتاحة وvirtual scroll"},
          {en:"A theme pack", ar:"حزمة ثيمات"},
          {en:"The Material icon set", ar:"مجموعة أيقونات Material"},
          {en:"A CLI plugin", ar:"إضافة للـ CLI"}],
    a:0,
    why:{en:"Material is the CDK plus a look. When a designer hands you something that is not Material, reach for the CDK on its own — you keep the hard parts (positioning, focus trapping, keyboard support) and supply your own styling.",
         ar:"Material هو الـ CDK زائد مظهر. ولما مصمّم يسلّمك حاجة مش Material، استخدم الـ CDK لوحده — بتحتفظ بالحاجات الصعبة (تحديد المواقع وحصر الفوكس ودعم الكيبورد) وتحط تنسيقك إنت."} },

  { q: {en:"Why <code>ng add @angular/material</code> rather than <code>npm install</code>?",
        ar:"ليه <code>ng add @angular/material</code> بدل <code>npm install</code>؟"},
    opts:[{en:"It installs <i>and</i> wires up the theme, typography and animations for you",
           ar:"بتنزّل <i>وكمان</i> بتوصّل الثيم والخطوط والحركات علشانك"},
          {en:"It is faster", ar:"أسرع"},
          {en:"<code>npm install</code> does not work for Angular packages", ar:"<code>npm install</code> مش بتنفع لحزم أنجولار"},
          {en:"It pins the version", ar:"بتثبّت الإصدار"}],
    a:0,
    why:{en:"<code>ng add</code> runs the package's schematic: it picks a version that matches your Angular, adds the theme CSS, sets up typography and touches <code>angular.json</code>. Doing that by hand is where the missing-styles bugs come from.",
         ar:"<code>ng add</code> بتشغّل الـ schematic بتاع الحزمة: بتختار إصدار متوافق مع أنجولار بتاعك، وبتضيف CSS الثيم، وبتظبط الخطوط، وبتعدّل <code>angular.json</code>. وإنك تعمل ده بإيدك هو مصدر مشاكل التنسيقات الناقصة."} },

  { q: {en:"A Material component looks unstyled. Most likely cause?",
        ar:"component من Material باين بدون تنسيق. أرجح سبب؟"},
    opts:[{en:"No theme is included in the global stylesheet",
           ar:"مفيش ثيم مضاف في ملف التنسيقات العام"},
          {en:"The component is not imported", ar:"الـ component مش معمول له import"},
          {en:"Animations are disabled", ar:"الحركات معطّلة"},
          {en:"The CDK is missing", ar:"الـ CDK ناقص"}],
    a:0,
    why:{en:"Material components carry structure, not colours — the palette comes from a theme in <code>styles.css</code>. A missing import throws loudly; a missing theme fails quietly, which is why it is the better first guess.",
         ar:"components الـ Material شايلة البنية، مش الألوان — واللوحة بتيجي من ثيم في <code>styles.css</code>. والـ import الناقص بيرمي خطأ بصوت عالي، أما الثيم الناقص فبيفشل بهدوء، وعلشان كده هو أول تخمين أحسن."} },

  { q: {en:"When should you <i>not</i> pull in a UI library?",
        ar:"إمتى <i>ماينفعش</i> تجيب مكتبة UI؟"},
    opts:[{en:"When the design is bespoke and you would fight the library's styles",
           ar:"لما التصميم يكون خاص وهتتعارك مع تنسيقات المكتبة"},
          {en:"When the app is large", ar:"لما التطبيق يكون كبير"},
          {en:"When you need accessibility", ar:"لما تحتاج إتاحة"},
          {en:"When you use signals", ar:"لما تستخدم signals"}],
    a:0,
    why:{en:"Overriding a component library's internals costs more than writing the component. But do not give up the behaviour with the styling — take the CDK, keep the accessibility and keyboard work, and paint it yourself.",
         ar:"إنك تتغلّب على تفاصيل مكتبة components داخلياً بيكلّف أكتر من إنك تكتب الـ component. بس ماتتنازلش عن السلوك مع التنسيق — خُد الـ CDK، واحتفظ بالإتاحة وشغل الكيبورد، وانت لوّنه بنفسك."} },
],

/* =========================== advanced ============================ */

'change-detection': [
  { q: {en:"Under <code>OnPush</code>, which of these does <b>not</b> mark a component for check?",
        ar:"مع <code>OnPush</code>، أنهي واحدة من دول <b>مش</b> بتعلّم الـ component للفحص؟"},
    opts:[{en:"Mutating an object that was already passed in",
           ar:"تعديل كائن كان متمرَّر أصلاً"},
          {en:"A new input reference", ar:"مرجع input جديد"},
          {en:"An event from the template", ar:"حدث من الـ template"},
          {en:"A signal read in the template changing", ar:"تغيّر signal مقروءة في الـ template"}],
    a:0,
    why:{en:"<code>OnPush</code> compares references. <code>user.name = 'x'</code> leaves the same object, so nothing looks different and the view is skipped. Replace instead: <code>user = { ...user, name: 'x' }</code>.",
         ar:"<code>OnPush</code> بيقارن المراجع. و<code>user.name = 'x'</code> بتسيب نفس الكائن، فمفيش حاجة باينة مختلفة والـ view بيتخطى. استبدل بدالها: <code>user = { ...user, name: 'x' }</code>."} },

  { q: {en:"Where does a change detection pass start?",
        ar:"دورة فحص التغييرات بتبدأ منين؟"},
    opts:[{en:"At the root, walking down the component tree",
           ar:"من الـ root، وبتنزل في شجرة الـ components"},
          {en:"At the component that changed", ar:"من الـ component اللي اتغيّر"},
          {en:"At the deepest component", ar:"من أعمق component"},
          {en:"In no fixed order", ar:"من غير ترتيب ثابت"}],
    a:0,
    why:{en:"It always sweeps from the top down, once per pass. That is why an expensive getter in a template is dangerous — and why <code>OnPush</code> helps so much, since it lets Angular skip whole branches.",
         ar:"دايماً بتكنس من فوق لتحت، مرة كل دورة. وعلشان كده أي getter مكلّف في template بيبقى خطر — وعلشان كده <code>OnPush</code> بيساعد كتير، لأنه بيخلي أنجولار يتخطى فروع كاملة."} },

  { q: {en:"A signal changes deep in the tree under <code>OnPush</code>. What does Angular do?",
        ar:"signal اتغيّرت في عمق الشجرة مع <code>OnPush</code>. أنجولار بيعمل إيه؟"},
    opts:[{en:"Marks the path from that component up to the root, then checks only that path",
           ar:"بيعلّم الطريق من الـ component ده لحد الـ root، وبعدين بيفحص الطريق ده بس"},
          {en:"Checks the whole tree", ar:"بيفحص الشجرة كلها"},
          {en:"Checks only that component", ar:"بيفحص الـ component ده بس"},
          {en:"Nothing until an event fires", ar:"مفيش حاجة لحد ما حدث يحصل"}],
    a:0,
    why:{en:"Signals know exactly which views read them, so they can mark a precise path dirty. This is why signals plus <code>OnPush</code> is the fast combination, and why it makes zoneless possible at all.",
         ar:"الـ signals عارفة بالظبط أنهي views بتقراها، فبتقدر تعلّم طريق محدد كمتّسخ. وعلشان كده الـ signals مع <code>OnPush</code> هما التركيبة السريعة، وعلشان كده الـ zoneless بقى ممكن أصلاً."} },

  { q: {en:"Why is a method call in a template a performance risk?",
        ar:"ليه نداء دالة في template بيبقى خطر على الأداء؟"},
    opts:[{en:"It runs on every check, not only when its inputs change",
           ar:"بيشتغل مع كل فحص، مش بس لما مدخلاته تتغيّر"},
          {en:"Templates cannot call methods", ar:"الـ templates مبتقدرش تنادي دوال"},
          {en:"It breaks type checking", ar:"بيكسر فحص الأنواع"},
          {en:"It disables <code>OnPush</code>", ar:"بيوقّف <code>OnPush</code>"}],
    a:0,
    why:{en:"Angular cannot know whether the result changed, so it calls the method every single pass — many times a second while you scroll. A <code>computed()</code> caches until a dependency actually changes.",
         ar:"أنجولار مبيعرفش لو النتيجة اتغيّرت، فبينادي الدالة في كل دورة — يعني مرات كتير في الثانية وإنت بتعمل scroll. أما <code>computed()</code> فبتحفظ النتيجة لحد ما تبعية تتغيّر فعلاً."} },
],

'zoneless': [
  { q: {en:"In a zoneless app, what tells Angular to redraw?",
        ar:"في تطبيق zoneless، إيه اللي بيقول لأنجولار يعيد الرسم؟"},
    opts:[{en:"A signal being set, or an explicit <code>markForCheck</code>",
           ar:"تحديد قيمة signal، أو <code>markForCheck</code> صريح"},
          {en:"<code>setTimeout</code> finishing", ar:"انتهاء <code>setTimeout</code>"},
          {en:"Any promise resolving", ar:"أي promise بيتحقق"},
          {en:"An interval tick", ar:"نبضة interval"}],
    a:0,
    why:{en:"That is the whole change: without <code>zone.js</code> nothing is monkey-patched, so Angular no longer hears about every timer and callback. Signals report their own changes — plain class fields do not.",
         ar:"ده كل التغيير: من غير <code>zone.js</code> مفيش حاجة متعدّلة تحت السطح، فأنجولار مبقى يسمع عن كل timer وcallback. والـ signals بتبلّغ عن تغييرها بنفسها — أما المتغيّرات العادية فلأ."} },

  { q: {en:"An old component assigns to a plain field after a <code>setTimeout</code>. Zoneless result?",
        ar:"component قديم بيكتب في متغيّر عادي بعد <code>setTimeout</code>. النتيجة في zoneless؟"},
    opts:[{en:"The value changes but the screen does not",
           ar:"القيمة بتتغيّر بس الشاشة لأ"},
          {en:"It throws", ar:"بيرمي خطأ"},
          {en:"It works as before", ar:"بيشتغل زي الأول"},
          {en:"The timer never fires", ar:"الـ timer مش بيشتغل خالص"}],
    a:0,
    why:{en:"This is the migration hazard in one sentence: silence, not a crash. Convert the field to a <code>signal()</code> — that is the whole fix, and it works in both modes.",
         ar:"ده خطر الانتقال في جملة واحدة: صمت، مش انهيار. حوّل المتغيّر لـ <code>signal()</code> — وده كل الحل، وبيشتغل في الوضعين."} },

  { q: {en:"What do you remove to go zoneless?",
        ar:"بتشيل إيه علشان تبقى zoneless؟"},
    opts:[{en:"<code>zone.js</code> from polyfills, and switch to <code>provideZonelessChangeDetection()</code>",
           ar:"<code>zone.js</code> من الـ polyfills، وتحوّل لـ <code>provideZonelessChangeDetection()</code>"},
          {en:"<code>OnPush</code> from every component", ar:"<code>OnPush</code> من كل component"},
          {en:"All your effects", ar:"كل الـ effects بتاعتك"},
          {en:"<code>provideRouter</code>", ar:"<code>provideRouter</code>"}],
    a:0,
    why:{en:"Two edits: the provider in <code>app.config.ts</code>, and dropping <code>zone.js</code> from the polyfills in <code>angular.json</code>. Leaving the polyfill in just ships 30-odd kilobytes doing nothing.",
         ar:"تعديلين: الـ provider في <code>app.config.ts</code>، وشيل <code>zone.js</code> من الـ polyfills في <code>angular.json</code>. ولو سيبت الـ polyfill هتنزّل حوالي ٣٠ كيلوبايت مش بيعملوا حاجة."} },

  { q: {en:"Which benefit is <b>not</b> a reason to go zoneless?",
        ar:"أنهي فايدة <b>مش</b> سبب إنك تبقى zoneless؟"},
    opts:[{en:"Your components need fewer inputs",
           ar:"الـ components بتاعتك هتحتاج inputs أقل"},
          {en:"A smaller bundle", ar:"حزمة أصغر"},
          {en:"Fewer, more targeted checks", ar:"فحوصات أقل وأدق"},
          {en:"Cleaner stack traces", ar:"تتبّع مكدس أنضف"}],
    a:0,
    why:{en:"Inputs have nothing to do with it. The real wins are size, precision and debuggability — <code>zone.js</code> used to wrap every async call, which is what made those stack traces so long.",
         ar:"الـ inputs مالهاش علاقة بالموضوع. المكاسب الحقيقية هي الحجم والدقة وسهولة التتبع — <code>zone.js</code> كان بيلف كل نداء غير متزامن، وده اللي كان بيخلي تتبّع المكدس طويل كده."} },
],

'rxjs-operators': [
  { q: {en:"A search box fires on every keystroke. Which operator keeps only the newest request?",
        ar:"خانة بحث بتطلق مع كل حرف. أنهي operator بيخلي أحدث طلب بس؟"},
    opts:[{en:"<code>switchMap</code> — it cancels the previous one",
           ar:"<code>switchMap</code> — بيلغي اللي قبله"},
          {en:"<code>mergeMap</code>", ar:"<code>mergeMap</code>"},
          {en:"<code>concatMap</code>", ar:"<code>concatMap</code>"},
          {en:"<code>exhaustMap</code>", ar:"<code>exhaustMap</code>"}],
    a:0,
    why:{en:"<code>switchMap</code> unsubscribes from the in-flight request when a new value arrives, so a slow early response can never overwrite a fast late one. That out-of-order overwrite is the classic search-box bug.",
         ar:"<code>switchMap</code> بيفصل عن الطلب اللي في الطريق أول ما قيمة جديدة توصل، فرد بطيء قديم مستحيل يستبدل رد سريع جديد. والاستبدال ده خارج الترتيب هو الباج الكلاسيكي لخانة البحث."} },

  { q: {en:"A save button that must not double-submit. Which one?",
        ar:"زرار حفظ ماينفعش يبعت مرتين. أنهي واحد؟"},
    opts:[{en:"<code>exhaustMap</code> — ignore clicks while one is in flight",
           ar:"<code>exhaustMap</code> — يتجاهل الكليكات طالما فيه واحد شغال"},
          {en:"<code>switchMap</code>", ar:"<code>switchMap</code>"},
          {en:"<code>mergeMap</code>", ar:"<code>mergeMap</code>"},
          {en:"<code>concatMap</code>", ar:"<code>concatMap</code>"}],
    a:0,
    why:{en:"<code>switchMap</code> would cancel the save you already started — exactly wrong for a write. <code>exhaustMap</code> drops the extra clicks until the first save finishes.",
         ar:"<code>switchMap</code> كان هيلغي الحفظ اللي بدأته أصلاً — وده غلط بالظبط لعملية كتابة. أما <code>exhaustMap</code> فبيرمي الكليكات الزيادة لحد ما أول حفظ يخلص."} },

  { q: {en:"You need every request to run, in order, none dropped.",
        ar:"محتاج كل طلب يتنفّذ، بالترتيب، ومفيش حاجة تتشال."},
    opts:[{en:"<code>concatMap</code>", ar:"<code>concatMap</code>"},
          {en:"<code>mergeMap</code>", ar:"<code>mergeMap</code>"},
          {en:"<code>switchMap</code>", ar:"<code>switchMap</code>"},
          {en:"<code>exhaustMap</code>", ar:"<code>exhaustMap</code>"}],
    a:0,
    why:{en:"<code>concatMap</code> queues: each request waits for the one before it. <code>mergeMap</code> also drops nothing but runs them in parallel, so the responses can arrive in any order.",
         ar:"<code>concatMap</code> بيصُف: كل طلب بيستنى اللي قبله. و<code>mergeMap</code> برضه مش بيشيل حاجة بس بينفّذهم بالتوازي، فالردود تقدر توصل بأي ترتيب."} },

  { q: {en:"Where must <code>catchError</code> go so one failure does not kill the search?",
        ar:"<code>catchError</code> لازم يكون فين علشان فشل واحد مايقتلش البحث؟"},
    opts:[{en:"Inside the <code>switchMap</code>, on the inner request",
           ar:"جوه الـ <code>switchMap</code>، على الطلب الداخلي"},
          {en:"After the <code>switchMap</code>", ar:"بعد الـ <code>switchMap</code>"},
          {en:"On the subscription", ar:"على الـ subscription"},
          {en:"In the interceptor only", ar:"في الـ interceptor بس"}],
    a:0,
    why:{en:"An error on the outer stream is terminal — the search box goes permanently dead. Catch it on the inner observable and the outer stream never learns anything went wrong.",
         ar:"الخطأ على الـ stream الخارجي نهائي — وخانة البحث بتموت للأبد. امسكه على الـ observable الداخلي والـ stream الخارجي مش هيعرف إن حاجة غلطت."} },
],

'rxjs-combination': [
  { q: {en:"<code>combineLatest</code> emits when?",
        ar:"<code>combineLatest</code> بيرسل إمتى؟"},
    opts:[{en:"Once every source has emitted, then on every later emission from any of them",
           ar:"أول ما كل مصدر يرسل مرة، وبعدها مع كل إرسال من أي واحد فيهم"},
          {en:"As soon as any source emits", ar:"أول ما أي مصدر يرسل"},
          {en:"Only when all of them complete", ar:"بس لما كلهم يخلصوا"},
          {en:"Once, then never again", ar:"مرة، وبعدها خلاص"}],
    a:0,
    why:{en:"The <i>every source first</i> rule is what surprises people: one silent stream keeps the whole combination silent. Give each source a starting value — a <code>BehaviorSubject</code>, or <code>startWith()</code>.",
         ar:"قاعدة <i>كل مصدر يرسل الأول</i> هي اللي بتفاجئ الناس: stream واحد ساكت بيخلي التركيبة كلها ساكتة. دّي كل مصدر قيمة بداية — <code>BehaviorSubject</code>، أو <code>startWith()</code>."} },

  { q: {en:"You need two calls to finish, then one result. Which?",
        ar:"محتاج نداءين يخلصوا، وبعدين نتيجة واحدة. أنهي؟"},
    opts:[{en:"<code>forkJoin</code>", ar:"<code>forkJoin</code>"},
          {en:"<code>combineLatest</code>", ar:"<code>combineLatest</code>"},
          {en:"<code>merge</code>", ar:"<code>merge</code>"},
          {en:"<code>zip</code>", ar:"<code>zip</code>"}],
    a:0,
    why:{en:"<code>forkJoin</code> is <code>Promise.all</code> for observables: it waits for every source to <i>complete</i> and emits the last values once. HTTP calls complete on their own, which makes this the natural fit.",
         ar:"<code>forkJoin</code> هو <code>Promise.all</code> للـ observables: بيستنى كل مصدر <i>يخلص</i> وبيرسل آخر القيم مرة واحدة. ونداءات الـ HTTP بتخلص بنفسها، وعلشان كده هو الأنسب هنا."} },

  { q: {en:"One source in a <code>forkJoin</code> never completes. What do you get?",
        ar:"مصدر في <code>forkJoin</code> مش بيخلص خالص. هتاخد إيه؟"},
    opts:[{en:"Nothing, ever", ar:"مفيش حاجة، خلاص"},
          {en:"The others, without it", ar:"الباقيين، من غيره"},
          {en:"An error", ar:"خطأ"},
          {en:"A partial result", ar:"نتيجة جزئية"}],
    a:0,
    why:{en:"<code>forkJoin</code> waits for completion, so a <code>Subject</code> or a signal-derived stream holds it open forever. Add <code>take(1)</code> to force completion, or use <code>combineLatest</code> instead.",
         ar:"<code>forkJoin</code> بيستنى الاكتمال، فأي <code>Subject</code> أو stream مشتق من signal بيسيبه مفتوح للأبد. ضيف <code>take(1)</code> علشان تفرض الاكتمال، أو استخدم <code>combineLatest</code> بدالها."} },

  { q: {en:"<code>merge</code> versus <code>combineLatest</code> — what comes out?",
        ar:"<code>merge</code> مقابل <code>combineLatest</code> — بيخرج إيه؟"},
    opts:[{en:"<code>merge</code> passes each value through as it arrives; <code>combineLatest</code> emits a combined snapshot",
           ar:"<code>merge</code> بيمرر كل قيمة زي ما هي وقت وصولها، و<code>combineLatest</code> بيرسل لقطة مجمّعة"},
          {en:"They are the same", ar:"الاتنين نفس الحاجة"},
          {en:"<code>merge</code> waits for all sources", ar:"<code>merge</code> بيستنى كل المصادر"},
          {en:"<code>combineLatest</code> only takes two sources", ar:"<code>combineLatest</code> بياخد مصدرين بس"}],
    a:0,
    why:{en:"Use <code>merge</code> for events from several places funnelled into one handler; use <code>combineLatest</code> when the view needs several current values at the same time.",
         ar:"استخدم <code>merge</code> لأحداث من أماكن مختلفة بتتجمّع في دالة واحدة، واستخدم <code>combineLatest</code> لما الشاشة تحتاج أكتر من قيمة حالية في نفس الوقت."} },
],

'rxjs-interop': [
  { q: {en:"<code>toSignal()</code> on an HTTP observable — what is the value before the response?",
        ar:"<code>toSignal()</code> على observable بتاع HTTP — القيمة قبل الرد إيه؟"},
    opts:[{en:"<code>undefined</code>, unless you pass <code>initialValue</code>",
           ar:"<code>undefined</code>، إلا لو مرّرت <code>initialValue</code>"},
          {en:"<code>null</code>", ar:"<code>null</code>"},
          {en:"It throws", ar:"بترمي خطأ"},
          {en:"It blocks until the response arrives", ar:"بتستنى لحد ما الرد يوصل"}],
    a:0,
    why:{en:"The signal must have <i>something</i> to return on the first read. Pass <code>{ initialValue: [] }</code> and the type stops including <code>undefined</code> — which removes a lot of template guards.",
         ar:"الـ signal لازم يكون معاها <i>حاجة</i> ترجّعها في أول قراية. مرّر <code>{ initialValue: [] }</code> والنوع مش هيشمل <code>undefined</code> — وده بيشيل حراسات كتير من الـ template."} },

  { q: {en:"Why convert a signal to an observable at all?",
        ar:"ليه نحوّل signal لـ observable من الأصل؟"},
    opts:[{en:"To use time-based operators — <code>debounceTime</code>, <code>switchMap</code>, <code>retry</code>",
           ar:"علشان نستخدم operators الوقت — <code>debounceTime</code> و<code>switchMap</code> و<code>retry</code>"},
          {en:"Signals cannot be read in services", ar:"الـ signals مينفعش تتقرا في الـ services"},
          {en:"Observables are faster", ar:"الـ observables أسرع"},
          {en:"To share state across components", ar:"علشان نشارك الحالة بين الـ components"}],
    a:0,
    why:{en:"Signals model <i>state right now</i>; observables model <i>events over time</i>. Debouncing a search box and cancelling the previous request is squarely the second job, so you cross over, then come back with <code>toSignal()</code>.",
         ar:"الـ signals بتمثّل <i>الحالة دلوقتي</i>، والـ observables بتمثّل <i>أحداث على مدى الوقت</i>. وتأخير خانة البحث وإلغاء الطلب اللي قبله ده الشغل التاني بالظبط، فبتعدّي هناك، وبترجع بـ <code>toSignal()</code>."} },

  { q: {en:"<code>toObservable()</code> and <code>toSignal()</code> need what?",
        ar:"<code>toObservable()</code> و<code>toSignal()</code> محتاجين إيه؟"},
    opts:[{en:"An injection context, or an explicit injector",
           ar:"سياق injection، أو injector صريح"},
          {en:"<code>zone.js</code>", ar:"<code>zone.js</code>"},
          {en:"<code>OnPush</code>", ar:"<code>OnPush</code>"},
          {en:"An <code>async</code> function", ar:"دالة <code>async</code>"}],
    a:0,
    why:{en:"Both need to know when to clean up, which they learn from the injection context. Call them in a field initialiser or constructor — or pass <code>{ injector }</code> explicitly.",
         ar:"الاتنين محتاجين يعرفوا إمتى ينضّفوا، وده بياخدوه من سياق الـ injection. فناديهم في تهيئة متغيّر أو في الـ constructor — أو مرّر <code>{ injector }</code> صريح."} },

  { q: {en:"Do you still need <code>takeUntilDestroyed()</code> with <code>toSignal()</code>?",
        ar:"لسه محتاج <code>takeUntilDestroyed()</code> مع <code>toSignal()</code>؟"},
    opts:[{en:"No — <code>toSignal()</code> unsubscribes when its context is destroyed",
           ar:"لأ — <code>toSignal()</code> بتفصل لما سياقها يتهدم"},
          {en:"Yes, always", ar:"أيوه، دايماً"},
          {en:"Only for HTTP", ar:"للـ HTTP بس"},
          {en:"Only in zoneless apps", ar:"في تطبيقات zoneless بس"}],
    a:0,
    why:{en:"That is one of the main reasons to use it: the subscription's lifetime is handled for you. You still need <code>takeUntilDestroyed()</code> for a manual <code>subscribe()</code> that does side effects.",
         ar:"ده واحد من أهم أسباب استخدامها: عمر الـ subscription بيتظبط علشانك. وبتفضل محتاج <code>takeUntilDestroyed()</code> لأي <code>subscribe()</code> يدوي بيعمل تأثيرات جانبية."} },
],

'signal-state': [
  { q: {en:"Which belongs in a signal store, and which should be derived?",
        ar:"أنهي حاجة مكانها الـ signal store، وأنهي واحدة تبقى مشتقّة؟"},
    opts:[{en:"Store the todos and the filter; derive the visible list",
           ar:"خزّن المهام والفلتر؛ واشتق القائمة الظاهرة"},
          {en:"Store all three", ar:"خزّن التلاتة"},
          {en:"Store the visible list; derive the rest", ar:"خزّن القائمة الظاهرة؛ واشتق الباقي"},
          {en:"Store nothing; compute everything", ar:"ماتخزّنش حاجة؛ احسب كل حاجة"}],
    a:0,
    why:{en:"Store the smallest set of facts you cannot work out, and derive everything else with <code>computed()</code>. Storing the visible list too means two things to keep in step, and one of them will drift.",
         ar:"خزّن أصغر مجموعة حقائق مش بتقدر تحسبها، واشتق كل حاجة تانية بـ <code>computed()</code>. ولو خزّنت القائمة الظاهرة كمان يبقى عندك حاجتين لازم يفضلوا متوافقين، وواحدة فيهم هتنحرف."} },

  { q: {en:"How should a store expose its state to components?",
        ar:"الـ store المفروض يعرض حالته للـ components إزاي؟"},
    opts:[{en:"Read-only signals out, methods in",
           ar:"signals للقراءة بس بتخرج، ودوال بتدخل"},
          {en:"Writable signals, so any component can set them", ar:"signals قابلة للكتابة، فأي component يقدر يحددها"},
          {en:"Plain public fields", ar:"متغيّرات عامة عادية"},
          {en:"A single big object", ar:"كائن واحد كبير"}],
    a:0,
    why:{en:"Expose <code>asReadonly()</code> (or a <code>computed()</code>) and keep the <code>WritableSignal</code> private. Then every change goes through a named method — and when a value is wrong you know exactly which few lines could have done it.",
         ar:"اعرض <code>asReadonly()</code> (أو <code>computed()</code>) وخلي الـ <code>WritableSignal</code> خاص. وساعتها كل تغيير بيمر من دالة لها اسم — ولما قيمة تطلع غلط تكون عارف بالظبط أنهي كام سطر ممكن يكونوا عملوها."} },

  { q: {en:"When is a signal store enough, without NgRx?",
        ar:"إمتى الـ signal store يكفي، من غير NgRx؟"},
    opts:[{en:"Most apps — until you need time-travel debugging or a strict action log",
           ar:"معظم التطبيقات — لحد ما تحتاج تتبع رجعي أو سجل أحداث صارم"},
          {en:"Only tiny apps", ar:"التطبيقات الصغيرة جداً بس"},
          {en:"Never; global state needs a library", ar:"أبداً؛ الحالة العامة محتاجة مكتبة"},
          {en:"Only when there is no HTTP", ar:"بس لما ميكونش فيه HTTP"}],
    a:0,
    why:{en:"A service holding signals plus <code>computed()</code> covers a lot of ground with no ceremony. Reach for NgRx when you actually want the things it sells: a serialisable action history, devtools, undo.",
         ar:"service شايلة signals مع <code>computed()</code> بتغطي مساحة كبيرة من غير طقوس. واستخدم NgRx لما تكون عايز فعلاً اللي بيقدّمه: سجل أحداث قابل للتخزين، وdevtools، وتراجع."} },

  { q: {en:"<code>update()</code> versus <code>set()</code> for a nested object?",
        ar:"<code>update()</code> مقابل <code>set()</code> لكائن متداخل؟"},
    opts:[{en:"Either, as long as you build a new object rather than editing the old one",
           ar:"أي واحدة، بشرط تبني كائن جديد بدل ما تعدّل القديم"},
          {en:"<code>set()</code> only", ar:"<code>set()</code> بس"},
          {en:"<code>update()</code> only", ar:"<code>update()</code> بس"},
          {en:"Neither; use <code>mutate()</code>", ar:"ولا واحدة؛ استخدم <code>mutate()</code>"}],
    a:0,
    why:{en:"The rule is about references, not which method you pick. <code>update(s =&gt; ({ ...s, open: true }))</code> is fine; <code>update(s =&gt; { s.open = true; return s; })</code> returns the same object and changes nothing on screen.",
         ar:"القاعدة عن المراجع، مش عن أنهي دالة تختار. <code>update(s =&gt; ({ ...s, open: true }))</code> تمام، أما <code>update(s =&gt; { s.open = true; return s; })</code> فبترجّع نفس الكائن ومش بتغيّر حاجة على الشاشة."} },
],

'global-state': [
  { q: {en:"In the NgRx pattern, who talks to the API?",
        ar:"في نمط NgRx، مين اللي بيتكلم مع الـ API؟"},
    opts:[{en:"An effect, listening for the action", ar:"effect، بيسمع للـ action"},
          {en:"The reducer", ar:"الـ reducer"},
          {en:"The component", ar:"الـ component"},
          {en:"The selector", ar:"الـ selector"}],
    a:0,
    why:{en:"Reducers must be pure — same input, same output, no side effects — because that purity is what makes the state predictable and replayable. Anything async lives in an effect.",
         ar:"الـ reducers لازم تكون نقية — نفس المدخل، نفس المخرج، ومفيش تأثيرات جانبية — لأن النقاء ده هو اللي بيخلي الحالة متوقّعة وقابلة للإعادة. وأي حاجة غير متزامنة مكانها effect."} },

  { q: {en:"A component dispatches <code>load()</code>. What does it know about the result?",
        ar:"component بيرسل <code>load()</code>. بيعرف إيه عن النتيجة؟"},
    opts:[{en:"Nothing directly — it reads the store and re-renders when it changes",
           ar:"مفيش حاجة مباشرة — بيقرا الـ store وبيعيد الرسم لما تتغيّر"},
          {en:"It gets a promise back", ar:"بياخد promise"},
          {en:"It gets the data returned", ar:"بياخد البيانات مرجّعة"},
          {en:"It must subscribe to the effect", ar:"لازم يعمل subscribe للـ effect"}],
    a:0,
    why:{en:"Dispatching is fire-and-forget: an announcement that something should happen. The component's only job afterwards is reading state — which is why this scales, and why it feels indirect at first.",
         ar:"الإرسال ده \"ابعت وانسى\": إعلان إن حاجة المفروض تحصل. وشغل الـ component بعدها هو قراية الحالة بس — وعلشان كده النمط ده بيتوسّع، وعلشان كده بيبان غير مباشر في الأول."} },

  { q: {en:"When is a full global store the wrong choice?",
        ar:"إمتى الـ store العام الكامل يكون اختيار غلط؟"},
    opts:[{en:"When the state belongs to one screen and nothing else reads it",
                      ar:"لما الحالة تخص شاشة واحدة ومحدش تاني بيقراها"},
          {en:"When there are many components", ar:"لما يكون فيه components كتير"},
          {en:"When you use HTTP", ar:"لما تستخدم HTTP"},
          {en:"When the app is a SPA", ar:"لما التطبيق يكون SPA"}],
    a:0,
    why:{en:"Global state has a real cost in ceremony: actions, reducers, effects, selectors. A wizard's current step or a table's sort column is local — keep it in the component, or in a small service scoped to that route.",
         ar:"الحالة العامة لها تكلفة حقيقية في الطقوس: actions وreducers وeffects وselectors. أما خطوة الـ wizard الحالية أو عمود ترتيب الجدول فدي محلية — سيبها في الـ component، أو في service صغيرة محدودة بالمسار ده."} },

  { q: {en:"What is a selector for?",
        ar:"الـ selector بيستخدم لإيه؟"},
    opts:[{en:"Reading a derived slice of the store, memoised",
           ar:"قراية جزء مشتق من الـ store، محفوظ"},
          {en:"Choosing which reducer runs", ar:"اختيار أنهي reducer يشتغل"},
          {en:"Selecting DOM elements", ar:"اختيار عناصر الـ DOM"},
          {en:"Dispatching actions", ar:"إرسال actions"}],
    a:0,
    why:{en:"A selector is <code>computed()</code> for a store: it derives a view of the state and caches until its inputs change. Components should read selectors, never reach into the raw state shape.",
         ar:"الـ selector هو <code>computed()</code> بتاع الـ store: بيشتق منظر من الحالة وبيحفظه لحد ما مدخلاته تتغيّر. والـ components المفروض تقرا selectors، ومتلمسش شكل الحالة الخام."} },
],

'dynamic-components': [
  { q: {en:"Which sets an input on a component you created in code?",
        ar:"أنهي واحدة بتحدد input على component عملته بالكود؟"},
    opts:[{en:"<code>ref.setInput('title', 'Sales')</code>", ar:"<code>ref.setInput('title', 'Sales')</code>"},
          {en:"<code>ref.instance.title = 'Sales'</code>", ar:"<code>ref.instance.title = 'Sales'</code>"},
          {en:"<code>ref.inputs.title = 'Sales'</code>", ar:"<code>ref.inputs.title = 'Sales'</code>"},
          {en:"<code>ref.setAttribute('title', 'Sales')</code>", ar:"<code>ref.setAttribute('title', 'Sales')</code>"}],
    a:0,
    why:{en:"<code>setInput()</code> writes the value <b>and</b> marks the component dirty, so it re-renders under <code>OnPush</code> and <code>ngOnChanges</code> fires. Assigning to <code>ref.instance</code> does neither.",
         ar:"<code>setInput()</code> بتكتب القيمة <b>وكمان</b> بتعلّم الـ component كمتّسخ، فبيعيد الرسم مع <code>OnPush</code> و<code>ngOnChanges</code> بتشتغل. أما الكتابة في <code>ref.instance</code> فمش بتعمل ولا واحدة."} },

  { q: {en:"How do you get events out of a dynamically created component?",
        ar:"بتاخد أحداث من component اتعمل ديناميكياً إزاي؟"},
    opts:[{en:"Subscribe to the output on <code>ref.instance</code>",
           ar:"اعمل subscribe للـ output على <code>ref.instance</code>"},
          {en:"An <code>(event)</code> binding on the container", ar:"ربط <code>(event)</code> على الحاوية"},
          {en:"<code>ngComponentOutletOutputs</code>", ar:"<code>ngComponentOutletOutputs</code>"},
          {en:"You cannot", ar:"مش ممكن"}],
    a:0,
    why:{en:"There is no template to write a binding in, so you subscribe by hand — and unsubscribe when you destroy the component. If you only ever need inputs, <code>ngComponentOutlet</code> is much less code.",
         ar:"مفيش template تكتب فيه ربط، فبتعمل subscribe بإيدك — وبتفصل لما تهدم الـ component. ولو محتاج inputs بس، <code>ngComponentOutlet</code> بيبقى كود أقل بكتير."} },

  { q: {en:"When is a dynamic component the wrong tool?",
        ar:"إمتى الـ dynamic component يكون الأداة الغلط؟"},
    opts:[{en:"When you already know the two possible components — use <code>@if</code> or <code>@switch</code>",
           ar:"لما تكون عارف الـ componentين المحتملين — استخدم <code>@if</code> أو <code>@switch</code>"},
          {en:"When the list comes from an API", ar:"لما القائمة تيجي من API"},
          {en:"When the components have inputs", ar:"لما الـ components يكون لها inputs"},
          {en:"Inside an <code>@for</code>", ar:"جوه <code>@for</code>"}],
    a:0,
    why:{en:"You give up type safety and readability, so only pay that when the set of components genuinely is not known until runtime — a CMS, a plugin system, a dashboard the user arranges.",
         ar:"إنت بتتنازل عن أمان الأنواع وسهولة القراية، فادفع التكلفة دي بس لما مجموعة الـ components فعلاً مش معروفة قبل التشغيل — زي CMS، أو نظام إضافات، أو dashboard المستخدم بيرتّبه."} },

  { q: {en:"A registry maps a string to a component class. Why is that useful?",
        ar:"سجل بيربط نص بكلاس component. ليه ده مفيد؟"},
    opts:[{en:"The server can name a component, and adding a type is one line",
           ar:"السيرفر يقدر يسمّي component، وإضافة نوع جديد بتبقى سطر واحد"},
          {en:"It makes rendering faster", ar:"بيخلي الرسم أسرع"},
          {en:"It avoids lazy loading", ar:"بيتجنب التحميل المؤجّل"},
          {en:"It removes the need for inputs", ar:"بيلغي الحاجة للـ inputs"}],
    a:0,
    why:{en:"The registry is the seam between data and code: JSON says <code>\"hero\"</code>, the map turns that into <code>HeroBlock</code>. New block type, one entry — and no <code>@switch</code> to grow forever.",
         ar:"السجل هو الوصلة بين البيانات والكود: الـ JSON بيقول <code>\"hero\"</code>، والخريطة بتحوّلها لـ <code>HeroBlock</code>. نوع بلوك جديد، مدخل واحد — ومفيش <code>@switch</code> بيكبر للأبد."} },
],

'control-value-accessor': [
  { q: {en:"Angular throws <i>No value accessor for form control</i>. What is missing?",
        ar:"أنجولار بيرمي <i>No value accessor for form control</i>. إيه الناقص؟"},
    opts:[{en:"The component does not provide <code>NG_VALUE_ACCESSOR</code>",
           ar:"الـ component مش موفّر <code>NG_VALUE_ACCESSOR</code>"},
          {en:"The <code>FormGroup</code> is missing", ar:"الـ <code>FormGroup</code> ناقص"},
          {en:"<code>formControlName</code> should be <code>formControl</code>", ar:"<code>formControlName</code> المفروض تكون <code>formControl</code>"},
          {en:"The component is not standalone", ar:"الـ component مش standalone"}],
    a:0,
    why:{en:"A form control needs a translator, and your component volunteers by adding <code>{ provide: NG_VALUE_ACCESSOR, useExisting: forwardRef(() =&gt; MyComponent), multi: true }</code> to its providers.",
         ar:"الـ form control محتاج مترجم، والـ component بتاعك بيتقدّم للمهمة لما تضيف <code>{ provide: NG_VALUE_ACCESSOR, useExisting: forwardRef(() =&gt; MyComponent), multi: true }</code> في الـ providers بتاعته."} },

  { q: {en:"Which method tells the form the user changed something?",
        ar:"أنهي دالة بتقول للفورم إن المستخدم غيّر حاجة؟"},
    opts:[{en:"The callback you stored in <code>registerOnChange</code>",
           ar:"الـ callback اللي خزّنتها في <code>registerOnChange</code>"},
          {en:"<code>writeValue()</code>", ar:"<code>writeValue()</code>"},
          {en:"<code>ngOnChanges()</code>", ar:"<code>ngOnChanges()</code>"},
          {en:"<code>setDisabledState()</code>", ar:"<code>setDisabledState()</code>"}],
    a:0,
    why:{en:"<code>writeValue</code> is the form talking to you; the <code>onChange</code> callback is you talking back. Calling <code>onChange</code> from inside <code>writeValue</code> is the classic infinite loop.",
         ar:"<code>writeValue</code> هي الفورم بيتكلم معاك، والـ callback بتاعة <code>onChange</code> هي إنت بتتكلم معاه. وإنك تنادي <code>onChange</code> من جوه <code>writeValue</code> ده الحلقة اللا نهائية الكلاسيكية."} },

  { q: {en:"Why is <code>forwardRef</code> needed in the provider?",
        ar:"ليه <code>forwardRef</code> مطلوبة في الـ provider؟"},
    opts:[{en:"The class is referenced inside its own decorator, before it exists",
           ar:"الكلاس بيتم الإشارة له جوه الـ decorator بتاعه، قبل ما يكون موجود"},
          {en:"To make it injectable", ar:"علشان يبقى قابل للـ inject"},
          {en:"To support lazy loading", ar:"لدعم التحميل المؤجّل"},
          {en:"To allow more than one instance", ar:"علشان يسمح بأكتر من نسخة"}],
    a:0,
    why:{en:"The decorator runs while the class is still being defined, so the identifier is not bound yet. <code>forwardRef</code> defers the lookup to the moment it is actually needed.",
         ar:"الـ decorator بيشتغل والكلاس لسه بيتعرّف، فالاسم لسه مش مربوط. و<code>forwardRef</code> بتأجّل البحث للحظة اللي بتحتاجه فيها فعلاً."} },

  { q: {en:"What do you get for free once the interface is implemented?",
        ar:"بتاخد إيه ببلاش أول ما تنفّذ الواجهة؟"},
    opts:[{en:"Validators, <code>touched</code>/<code>dirty</code> state, <code>patchValue</code>, <code>reset</code>, <code>disable</code>",
           ar:"المُحقّقات، وحالة <code>touched</code>/<code>dirty</code>، و<code>patchValue</code> و<code>reset</code> و<code>disable</code>"},
          {en:"Only two-way binding", ar:"الربط ذو الاتجاهين بس"},
          {en:"Automatic accessibility", ar:"إتاحة تلقائية"},
          {en:"Server-side validation", ar:"تحقّق على السيرفر"}],
    a:0,
    why:{en:"Your widget becomes a first-class control, so the entire forms API applies to it. Do add <code>setDisabledState()</code> though — leave it out and <code>form.disable()</code> silently does nothing to your component.",
         ar:"الويدجت بتاعك بيبقى control من الدرجة الأولى، فكل واجهة الفورمز بتسري عليه. بس ضيف <code>setDisabledState()</code> — لو سيبتها، <code>form.disable()</code> مش هتعمل حاجة في الـ component بتاعك بهدوء."} },
],

'renderer2': [
  { q: {en:"Why use <code>Renderer2</code> instead of <code>el.nativeElement.style</code>?",
        ar:"ليه نستخدم <code>Renderer2</code> بدل <code>el.nativeElement.style</code>؟"},
    opts:[{en:"It works where there is no DOM — server rendering, for instance",
           ar:"بيشتغل في الأماكن اللي مفيهاش DOM — زي الرندر على السيرفر"},
          {en:"It is faster", ar:"أسرع"},
          {en:"It is the only way to set styles", ar:"دي الطريقة الوحيدة لتحديد التنسيقات"},
          {en:"It avoids change detection", ar:"بيتجنب فحص التغييرات"}],
    a:0,
    why:{en:"<code>nativeElement</code> assumes a browser. <code>Renderer2</code> goes through an abstraction, so the same directive survives server-side rendering — and it is a sanitisation boundary too.",
         ar:"<code>nativeElement</code> بيفترض وجود متصفح. أما <code>Renderer2</code> فبيمر من طبقة تجريد، فنفس الـ directive بينجو من الرندر على السيرفر — وكمان هو حدود تنظيف."} },

  { q: {en:"Which is usually better than either, for styling?",
        ar:"أنهي واحدة أحسن من الاتنين عادةً، للتنسيق؟"},
    opts:[{en:"A class toggled with <code>host</code> bindings, and the styling in CSS",
           ar:"كلاس بيتبدّل بروابط <code>host</code>، والتنسيق في CSS"},
          {en:"Inline styles from TypeScript", ar:"تنسيقات مباشرة من TypeScript"},
          {en:"<code>document.styleSheets</code>", ar:"<code>document.styleSheets</code>"},
          {en:"<code>!important</code> everywhere", ar:"<code>!important</code> في كل حاجة"}],
    a:0,
    why:{en:"Bind a class and let the stylesheet decide what it looks like. Designers can then change the appearance without touching TypeScript, and you keep one place per concern.",
         ar:"اربط كلاس وخلي ملف التنسيقات يحدد شكله. وساعتها المصمّمين يقدروا يغيّروا المظهر من غير ما يلمسوا TypeScript، وإنت بتحتفظ بمكان واحد لكل مسؤولية."} },

  { q: {en:"When is reaching for the DOM directly genuinely justified?",
        ar:"إمتى الوصول للـ DOM مباشرة يكون مبرَّر فعلاً؟"},
    opts:[{en:"Measuring — <code>getBoundingClientRect</code>, scroll position, focus",
           ar:"القياس — <code>getBoundingClientRect</code> ومكان الـ scroll والفوكس"},
          {en:"Setting text content", ar:"تحديد نص العنصر"},
          {en:"Adding elements", ar:"إضافة عناصر"},
          {en:"Changing colours", ar:"تغيير الألوان"}],
    a:0,
    why:{en:"Angular has no template syntax for <i>how tall is this</i>. Reading measurements is fine; writing content or structure is what templates and bindings are for.",
         ar:"أنجولار مفيهوش صيغة template لسؤال <i>ده طوله كام</i>. فقراية القياسات تمام، أما كتابة المحتوى أو البنية فده شغل الـ templates والروابط."} },

  { q: {en:"Where should DOM measuring code run?",
        ar:"كود قياس الـ DOM المفروض يشتغل فين؟"},
    opts:[{en:"<code>afterNextRender()</code> — browser only, after layout",
           ar:"<code>afterNextRender()</code> — في المتصفح بس، بعد التخطيط"},
          {en:"The constructor", ar:"في الـ constructor"},
          {en:"<code>ngOnInit</code>", ar:"في <code>ngOnInit</code>"},
          {en:"A <code>computed()</code>", ar:"في <code>computed()</code>"}],
    a:0,
    why:{en:"There is no layout to measure until the browser has done one, and on the server there is no layout at all. <code>afterNextRender()</code> handles both facts for you.",
         ar:"مفيش تخطيط تقيسه لحد ما المتصفح يعمل واحد، وعلى السيرفر مفيش تخطيط أصلاً. و<code>afterNextRender()</code> بتتعامل مع الحقيقتين علشانك."} },
],

'lazy-loading': [
  { q: {en:"What does a preloading strategy change?",
        ar:"استراتيجية التحميل المسبق بتغيّر إيه؟"},
    opts:[{en:"<i>When</i> a lazy chunk is fetched, not whether it is separate",
           ar:"<i>إمتى</i> الحزمة المؤجّلة تتحمّل، مش إنها منفصلة ولا لأ"},
          {en:"Whether the route is lazy at all", ar:"إن المسار مؤجّل من الأصل ولا لأ"},
          {en:"The size of the chunk", ar:"حجم الحزمة"},
          {en:"The order of the routes", ar:"ترتيب المسارات"}],
    a:0,
    why:{en:"The chunk is still separate — the first paint stays small. Preloading just fetches it during idle time, so by the time the user clicks it is already in cache and the navigation feels instant.",
         ar:"الحزمة بتفضل منفصلة — وأول رسم بيفضل صغير. والتحميل المسبق بس بيجيبها وقت الفراغ، فلما المستخدم يدوس تكون في الكاش أصلاً والتنقل يحس إنه فوري."} },

  { q: {en:"Which is the biggest cheap win for first-load performance?",
        ar:"أنهي أكبر مكسب رخيص لأداء أول تحميل؟"},
    opts:[{en:"Lazy loading the routes the first screen does not need",
           ar:"تأجيل تحميل المسارات اللي أول شاشة مش محتاجاها"},
          {en:"Switching to <code>OnPush</code>", ar:"التحويل لـ <code>OnPush</code>"},
          {en:"Using signals", ar:"استخدام الـ signals"},
          {en:"Minifying twice", ar:"التصغير مرتين"}],
    a:0,
    why:{en:"<code>OnPush</code> and signals make the app <i>run</i> faster; they do nothing about how much JavaScript arrives first. Route-level code splitting is the one change that shrinks the initial download.",
         ar:"<code>OnPush</code> والـ signals بيخلوا التطبيق <i>يشتغل</i> أسرع، بس مش بيعملوا حاجة في كمية الجافاسكريبت اللي بتوصل الأول. وتقسيم الكود على مستوى المسارات هو التغيير الوحيد اللي بيصغّر أول تحميل."} },

  { q: {en:"Should you preload every route?",
        ar:"ينفع تحمّل كل المسارات مقدماً؟"},
    opts:[{en:"No — that is close to not splitting at all, on a slow connection",
           ar:"لأ — ده قريب من إنك مقسّمتش خالص، على اتصال بطيء"},
          {en:"Yes, always", ar:"أيوه، دايماً"},
          {en:"Yes, if the app is small", ar:"أيوه، لو التطبيق صغير"},
          {en:"Only in development", ar:"في التطوير بس"}],
    a:0,
    why:{en:"<code>PreloadAllModules</code> is fine for a handful of small routes. Beyond that, preload selectively — mark the likely next routes in route <code>data</code> and write a strategy that only fetches those.",
         ar:"<code>PreloadAllModules</code> تمام لعدد قليل من المسارات الصغيرة. وأكتر من كده، حمّل بشكل انتقائي — علّم المسارات المرجّحة في <code>data</code> بتاعة المسار واكتب استراتيجية بتجيب دول بس."} },

  { q: {en:"A lazy route's chunk is huge. What is the usual cause?",
        ar:"حزمة مسار مؤجّل ضخمة. إيه السبب المعتاد؟"},
    opts:[{en:"It imports something shared that pulls a big library in with it",
           ar:"بيعمل import لحاجة مشتركة بتجيب معاها مكتبة كبيرة"},
          {en:"Too many components", ar:"components كتير"},
          {en:"Missing <code>OnPush</code>", ar:"<code>OnPush</code> ناقص"},
          {en:"The route has parameters", ar:"المسار له معاملات"}],
    a:0,
    why:{en:"One import of a date or chart library from a shared barrel file drags the whole thing into that chunk. <code>ng build --stats-json</code> plus a bundle analyser shows you exactly which import did it.",
         ar:"import واحد لمكتبة تواريخ أو رسوم من ملف barrel مشترك بيجرّ الحاجة كلها للحزمة دي. و<code>ng build --stats-json</code> مع محلّل حزم بيوريك بالظبط أنهي import عمل كده."} },
],

'defer': [
  { q: {en:"What does <code>@placeholder</code> show?",
        ar:"<code>@placeholder</code> بتعرض إيه؟"},
    opts:[{en:"Cheap markup shown before the deferred block is even requested",
           ar:"ماركب رخيص بيتعرض قبل إن البلوك المؤجّل يُطلب أصلاً"},
          {en:"A spinner while it downloads", ar:"spinner وهو بينزّل"},
          {en:"An error state", ar:"حالة خطأ"},
          {en:"Nothing; it is optional", ar:"مفيش حاجة؛ هي اختيارية"}],
    a:0,
    why:{en:"<code>@placeholder</code> is the <i>before</i> state; <code>@loading</code> is the <i>while fetching</i> state. Giving the placeholder roughly the right height is what stops the page jumping when the real block swaps in.",
         ar:"<code>@placeholder</code> هي حالة <i>قبل</i>، و<code>@loading</code> هي حالة <i>وهو بيجيب</i>. وإنك تدّي الـ placeholder طول قريب من الحقيقي هو اللي بيمنع الصفحة إنها تنقز لما البلوك الحقيقي يحل مكانه."} },

  { q: {en:"Which trigger fetches the block when it is about to be seen?",
        ar:"أنهي مُشغّل بيجيب البلوك لما يقرب يتشاف؟"},
    opts:[{en:"<code>on viewport</code>", ar:"<code>on viewport</code>"},
          {en:"<code>on idle</code>", ar:"<code>on idle</code>"},
          {en:"<code>on interaction</code>", ar:"<code>on interaction</code>"},
          {en:"<code>on timer</code>", ar:"<code>on timer</code>"}],
    a:0,
    why:{en:"<code>on viewport</code> uses an intersection observer, so reviews at the bottom of a product page cost nothing until the user scrolls. <code>prefetch on idle</code> pairs nicely with it.",
         ar:"<code>on viewport</code> بيستخدم intersection observer، فالمراجعات في آخر صفحة المنتج مش بتكلّف حاجة لحد ما المستخدم ينزل. و<code>prefetch on idle</code> بيتماشى معاه حلو."} },

  { q: {en:"What must be true of a component used in a <code>@defer</code> block?",
        ar:"إيه اللي لازم يكون صح في component مستخدم جوه <code>@defer</code>؟"},
    opts:[{en:"Nothing else in the template may reference it eagerly",
           ar:"مفيش حاجة تانية في الـ template تشير له بشكل فوري"},
          {en:"It must be standalone", ar:"لازم يكون standalone"},
          {en:"It must have no inputs", ar:"لازم ميكونش له inputs"},
          {en:"It must be in a lazy route", ar:"لازم يكون في مسار مؤجّل"}],
    a:0,
    why:{en:"An eager reference elsewhere — a <code>viewChild</code>, or the same tag outside the block — forces the code into the main bundle and the deferral quietly buys you nothing.",
         ar:"أي إشارة فورية في مكان تاني — زي <code>viewChild</code>، أو نفس التاج بره البلوك — بتجبر الكود يدخل الحزمة الرئيسية والتأجيل مش بيجيب لك حاجة بهدوء."} },

  { q: {en:"<code>@defer</code> versus a lazy route?",
        ar:"<code>@defer</code> مقابل مسار مؤجّل؟"},
    opts:[{en:"A block inside a page versus a whole page",
           ar:"بلوك جوه صفحة مقابل صفحة كاملة"},
          {en:"They are the same feature", ar:"الاتنين نفس الميزة"},
          {en:"<code>@defer</code> is for images only", ar:"<code>@defer</code> للصور بس"},
          {en:"Lazy routes are deprecated", ar:"المسارات المؤجّلة مهجورة"}],
    a:0,
    why:{en:"Use lazy routes for whole screens the user may never visit, and <code>@defer</code> for the heavy part of a screen they are already on — a comments section, a map, a chart.",
         ar:"استخدم المسارات المؤجّلة للشاشات الكاملة اللي المستخدم ممكن ميزورهاش خالص، و<code>@defer</code> للجزء التقيل في شاشة هو موجود فيها أصلاً — زي قسم تعليقات، أو خريطة، أو شارت."} },
],

'performance': [
  { q: {en:"You are told the app is slow. What do you do first?",
        ar:"قالولك إن التطبيق بطيء. تعمل إيه الأول؟"},
    opts:[{en:"Measure — decide whether it is load size or runtime work",
           ar:"قِس — حدد لو المشكلة حجم التحميل ولا شغل وقت التشغيل"},
          {en:"Add <code>OnPush</code> everywhere", ar:"ضيف <code>OnPush</code> في كل مكان"},
          {en:"Convert everything to signals", ar:"حوّل كل حاجة لـ signals"},
          {en:"Enable lazy loading everywhere", ar:"شغّل التحميل المؤجّل في كل مكان"}],
    a:0,
    why:{en:"The two problems have opposite cures, and guessing wastes days. <code>ng build --stats-json</code> answers <i>what shipped</i>; the DevTools profiler answers <i>what runs</i>.",
         ar:"المشكلتين لهم علاج معاكس، والتخمين بيضيّع أيام. <code>ng build --stats-json</code> بيجاوب على <i>إيه اللي اتنزّل</i>، وprofiler الـ DevTools بيجاوب على <i>إيه اللي بيشتغل</i>."} },

  { q: {en:"Which of these usually helps least?",
        ar:"أنهي واحدة من دول بتساعد أقل عادةً؟"},
    opts:[{en:"Micro-optimising a function that runs once",
           ar:"تحسين دقيق لدالة بتشتغل مرة واحدة"},
          {en:"Splitting routes", ar:"تقسيم المسارات"},
          {en:"Removing an unused heavy library", ar:"شيل مكتبة تقيلة مش مستخدمة"},
          {en:"Replacing a template method call with <code>computed()</code>", ar:"استبدال نداء دالة في template بـ <code>computed()</code>"}],
    a:0,
    why:{en:"Optimise what runs often or ships large. A one-off function is neither, however satisfying it is to tidy — the other three each remove work that repeats or bytes that travel.",
         ar:"حسّن اللي بيشتغل كتير أو بينزّل كبير. والدالة اللي بتشتغل مرة مش دي ولا دي، مهما كان مريح إنك ترتّبها — أما التلاتة التانيين فكل واحد بيشيل شغل بيتكرر أو بايتات بتتنقل."} },

  { q: {en:"What does <code>NgOptimizedImage</code> do for you?",
        ar:"<code>NgOptimizedImage</code> بيعمل لك إيه؟"},
    opts:[{en:"Lazy loading, srcset, priority hints and layout-shift protection",
           ar:"تحميل مؤجّل وsrcset وتلميحات أولوية وحماية من انتقال التخطيط"},
          {en:"Compresses the image files", ar:"بيضغط ملفات الصور"},
          {en:"Converts to WebP", ar:"بيحوّل لـ WebP"},
          {en:"Caches images offline", ar:"بيخزّن الصور بدون إنترنت"}],
    a:0,
    why:{en:"It does the boring correct things around the image — it does not touch the bytes themselves. Compression stays your job, or your CDN's.",
         ar:"بيعمل الحاجات المملة الصح حوالين الصورة — بس مش بيلمس البايتات نفسها. والضغط بيفضل شغلك، أو شغل الـ CDN بتاعك."} },

  { q: {en:"Which metric best reflects \"the page felt slow to appear\"?",
        ar:"أنهي مقياس بيعبّر أحسن عن \"الصفحة حسّت إنها ظهرت بطيء\"؟"},
    opts:[{en:"Largest Contentful Paint", ar:"Largest Contentful Paint"},
          {en:"Total bundle size", ar:"الحجم الكلي للحزمة"},
          {en:"Number of components", ar:"عدد الـ components"},
          {en:"Change detection count", ar:"عدد دورات فحص التغييرات"}],
    a:0,
    why:{en:"LCP measures when the main content actually became visible — which is what a user means by slow. Bundle size is a cause you can act on, not the experience itself.",
         ar:"الـ LCP بيقيس إمتى المحتوى الرئيسي بان فعلاً — وده اللي المستخدم يقصده بكلمة بطيء. أما حجم الحزمة فده سبب تقدر تتحرك عليه، مش التجربة نفسها."} },
],

'ssr-hydration': [
  { q: {en:"What does hydration avoid?",
        ar:"الـ hydration بيتجنب إيه؟"},
    opts:[{en:"Throwing away the server HTML and rebuilding the DOM",
           ar:"رمي HTML السيرفر وإعادة بناء الـ DOM"},
          {en:"A second HTTP request", ar:"طلب HTTP تاني"},
          {en:"Running JavaScript at all", ar:"تشغيل الجافاسكريبت من الأصل"},
          {en:"Change detection", ar:"فحص التغييرات"}],
    a:0,
    why:{en:"Without it Angular wipes the server-rendered markup and renders again — the visible flicker people complain about. With it, Angular reuses the existing DOM and only attaches behaviour.",
         ar:"من غيره أنجولار بيمسح الماركب اللي جه من السيرفر وبيرسم تاني — وده الرمشة اللي الناس بتشتكي منها. ومعاه، أنجولار بيستخدم الـ DOM الموجود وبيلزق السلوك بس."} },

  { q: {en:"Your code uses <code>window</code> and SSR crashes. Best fix?",
        ar:"كودك بيستخدم <code>window</code> والـ SSR بيقع. أحسن حل؟"},
    opts:[{en:"Move it into <code>afterNextRender()</code>, or guard with <code>isPlatformBrowser</code>",
           ar:"انقله لـ <code>afterNextRender()</code>، أو احمِه بـ <code>isPlatformBrowser</code>"},
          {en:"Wrap it in <code>try/catch</code>", ar:"لُفّه في <code>try/catch</code>"},
          {en:"Define a global <code>window</code> on the server", ar:"عرّف <code>window</code> عام على السيرفر"},
          {en:"Turn hydration off", ar:"اقفل الـ hydration"}],
    a:0,
    why:{en:"On the server there is no <code>window</code>, <code>document</code> or <code>localStorage</code>. <code>afterNextRender()</code> is skipped there by design, which makes it the cleanest home for browser-only code.",
         ar:"على السيرفر مفيش <code>window</code> ولا <code>document</code> ولا <code>localStorage</code>. و<code>afterNextRender()</code> بتتخطى هناك بحكم التصميم، وعلشان كده هي أنضف مكان للكود اللي للمتصفح بس."} },

  { q: {en:"A hydration mismatch warning appears. What causes it?",
        ar:"تحذير عدم تطابق hydration ظهر. إيه اللي بيسببه؟"},
    opts:[{en:"The server and client rendered different DOM — random values, dates, direct DOM edits",
           ar:"السيرفر والعميل رسموا DOM مختلف — قيم عشوائية أو تواريخ أو تعديل DOM مباشر"},
          {en:"A slow network", ar:"شبكة بطيئة"},
          {en:"A missing route", ar:"مسار ناقص"},
          {en:"Too many components", ar:"components كتير"}],
    a:0,
    why:{en:"Hydration assumes the two renders match. <code>Math.random()</code>, <code>new Date()</code> and manual DOM manipulation break that assumption, and Angular falls back to re-rendering that part.",
         ar:"الـ hydration بيفترض إن الرسمتين متطابقتين. و<code>Math.random()</code> و<code>new Date()</code> والتعديل اليدوي للـ DOM بيكسروا الافتراض ده، وأنجولار بيرجع يرسم الجزء ده تاني."} },

  { q: {en:"Why bother with SSR?",
        ar:"ليه نتعب نفسنا بالـ SSR؟"},
    opts:[{en:"Faster first paint and crawlable HTML for search and link previews",
           ar:"أول رسم أسرع وHTML يقدر يزحف عليه البحث ومعاينات اللينكات"},
          {en:"Smaller bundles", ar:"حزم أصغر"},
          {en:"Fewer HTTP requests", ar:"طلبات HTTP أقل"},
          {en:"It removes the need for signals", ar:"بيلغي الحاجة للـ signals"}],
    a:0,
    why:{en:"The browser gets meaningful HTML before any JavaScript runs. That matters most for content people find through search or share as links — and much less for an internal dashboard behind a login.",
         ar:"المتصفح بياخد HTML مفيد قبل إن أي جافاسكريبت يشتغل. وده مهم أكتر للمحتوى اللي الناس بتلاقيه من البحث أو بتشاركه كلينكات — وأقل بكتير لـ dashboard داخلي ورا تسجيل دخول."} },
],

'security': [
  { q: {en:"Is <code>{{ userInput }}</code> safe if the user typed a <code>&lt;script&gt;</code> tag?",
        ar:"<code>{{ userInput }}</code> آمنة لو المستخدم كتب تاج <code>&lt;script&gt;</code>؟"},
    opts:[{en:"Yes — interpolation escapes it, so it shows as text",
           ar:"أيوه — الإدراج بيأمّنها، فبتظهر كنص"},
          {en:"No, it runs", ar:"لأ، بيشتغل"},
          {en:"Only with a sanitiser", ar:"بس مع sanitiser"},
          {en:"Only in production", ar:"في الإنتاج بس"}],
    a:0,
    why:{en:"Angular escapes everything it interpolates, which is why the overwhelming majority of Angular templates are XSS-safe by default. The danger begins the moment you leave that path.",
         ar:"أنجولار بيأمّن كل حاجة بيدرجها، وعلشان كده الغالبية العظمى من templates أنجولار آمنة من XSS افتراضياً. والخطر بيبدأ في اللحظة اللي بتسيب فيها الطريق ده."} },

  { q: {en:"And <code>[innerHTML]=\"userInput\"</code>?",
        ar:"وإيه رأيك في <code>[innerHTML]=\"userInput\"</code>؟"},
    opts:[{en:"Angular strips scripts, but you should still sanitise deliberately",
           ar:"أنجولار بيشيل الـ scripts، بس لسه المفروض تنظّف بشكل مقصود"},
          {en:"It is identical to interpolation", ar:"هي نفس الإدراج بالظبط"},
          {en:"It is always unsafe", ar:"دايماً غير آمنة"},
          {en:"It is blocked at compile time", ar:"ممنوعة وقت الترجمة"}],
    a:0,
    why:{en:"Angular's sanitiser removes scripts and dangerous attributes, so it is not wide open — but it is the point where you have taken responsibility. If the HTML comes from users, sanitise on the server too.",
         ar:"الـ sanitiser بتاع أنجولار بيشيل الـ scripts والسمات الخطرة، فهي مش مفتوحة على البحري — بس دي النقطة اللي إنت بقيت مسؤول فيها. ولو الـ HTML جاي من المستخدمين، نظّف على السيرفر كمان."} },

  { q: {en:"What does <code>bypassSecurityTrustHtml</code> mean in review?",
        ar:"<code>bypassSecurityTrustHtml</code> معناها إيه في المراجعة؟"},
    opts:[{en:"A promise that <i>you</i> proved this string is safe",
           ar:"وعد إن <i>إنت</i> أثبتت إن النص ده آمن"},
          {en:"Angular will sanitise it harder", ar:"أنجولار هينظّفها أكتر"},
          {en:"It is safe for user content", ar:"آمنة لمحتوى المستخدمين"},
          {en:"It only affects development", ar:"بتأثّر في التطوير بس"}],
    a:0,
    why:{en:"It switches the sanitiser off for that value. Fine for a constant you wrote; never for anything that passed through a user or a third-party API.",
         ar:"بتقفل الـ sanitiser للقيمة دي. تمام لثابت إنت كتبته، وممنوع خالص لأي حاجة عدّت من مستخدم أو من API خارجي."} },

  { q: {en:"Which risk does Angular <b>not</b> handle for you?",
        ar:"أنهي خطر أنجولار <b>مش</b> بيتعامل معاه علشانك؟"},
    opts:[{en:"Authorisation — what a signed-in user is allowed to do",
           ar:"التصريح — المستخدم المسجّل مسموح له يعمل إيه"},
          {en:"Escaping interpolated text", ar:"تأمين النص المُدرج"},
          {en:"Sanitising bound HTML", ar:"تنظيف الـ HTML المربوط"},
          {en:"Blocking <code>javascript:</code> URLs", ar:"منع روابط <code>javascript:</code>"}],
    a:0,
    why:{en:"A route guard hides a page; it does not protect the data behind it. Anyone can call your API directly, so authorisation has to be enforced on the server — the client copy is a convenience.",
         ar:"الـ route guard بيخبّي صفحة، بس مش بيحمي البيانات اللي وراها. أي حد يقدر ينادي الـ API بتاعك مباشرة، فالتصريح لازم يتفرض على السيرفر — ونسخة العميل مجرد راحة للمستخدم."} },
],

'testing': [
  { q: {en:"What is the point of <code>providers: [{ provide: CartStore, useValue: fake }]</code> in a spec?",
        ar:"إيه فايدة <code>providers: [{ provide: CartStore, useValue: fake }]</code> في spec؟"},
    opts:[{en:"The component gets your fake instead of the real service",
           ar:"الـ component بياخد البديل المزيّف بدل الـ service الحقيقية"},
          {en:"It speeds the test up", ar:"بتسرّع الاختبار"},
          {en:"It is required for standalone components", ar:"مطلوبة للـ components المستقلة"},
          {en:"It disables change detection", ar:"بتوقّف فحص التغييرات"}],
    a:0,
    why:{en:"Dependency injection is what makes components testable: swap the dependency and the component cannot tell. That is the practical payoff of <code>inject()</code> over <code>new</code>.",
         ar:"الـ dependency injection هو اللي بيخلي الـ components قابلة للاختبار: بدّل التبعية والـ component مش هيفرق. وده المكسب العملي من <code>inject()</code> مقابل <code>new</code>."} },

  { q: {en:"When must you call <code>fixture.detectChanges()</code>?",
        ar:"إمتى لازم تنادي <code>fixture.detectChanges()</code>؟"},
    opts:[{en:"After changing state, before asserting on the DOM",
           ar:"بعد ما تغيّر الحالة، وقبل ما تتأكد على الـ DOM"},
          {en:"Once at the start only", ar:"مرة في الأول بس"},
          {en:"Never in modern Angular", ar:"أبداً في أنجولار الحديث"},
          {en:"Only for <code>OnPush</code> components", ar:"للـ components بتاعة <code>OnPush</code> بس"}],
    a:0,
    why:{en:"Tests run outside the normal render loop, so nothing repaints on its own. Assert on the DOM without it and you are reading the previous frame — a very common source of confusing test failures.",
         ar:"الاختبارات بتشتغل بره حلقة الرسم العادية، فمفيش حاجة بتعيد الرسم بنفسها. ولو اتأكدت على الـ DOM من غيرها بتكون بتقرا الإطار اللي قبله — وده مصدر شائع جداً لفشل اختبارات محيّر."} },

  { q: {en:"What should a component test mostly assert on?",
        ar:"اختبار الـ component المفروض يتأكد على إيه في الغالب؟"},
    opts:[{en:"What the user can see and do — rendered text, clicks, emitted events",
           ar:"اللي المستخدم يشوفه ويعمله — النص المرسوم، والكليكات، والأحداث المُرسلة"},
          {en:"Private methods", ar:"الدوال الخاصة"},
          {en:"The number of change detection runs", ar:"عدد دورات فحص التغييرات"},
          {en:"Field names", ar:"أسماء المتغيّرات"}],
    a:0,
    why:{en:"Tests tied to internals break on every refactor and tell you nothing about whether the feature works. Assert on behaviour and you can rewrite the class freely.",
         ar:"الاختبارات المربوطة بالتفاصيل الداخلية بتكسر مع كل refactor ومبتقولك حاجة عن إن الميزة شغالة ولا لأ. اتأكد على السلوك وهتقدر تعيد كتابة الكلاس بحرية."} },

  { q: {en:"A test passes alone but fails in the suite. Likely cause?",
        ar:"اختبار بينجح لوحده وبيفشل مع المجموعة. أرجح سبب؟"},
    opts:[{en:"Shared state left behind by another test",
           ar:"حالة مشتركة سايبها اختبار تاني"},
          {en:"The suite runs too fast", ar:"المجموعة بتشتغل بسرعة زيادة"},
          {en:"Too many assertions", ar:"تأكيدات كتير"},
          {en:"A missing <code>async</code>", ar:"<code>async</code> ناقصة"}],
    a:0,
    why:{en:"A root-provided service, a module-level variable or a stubbed global that was never reset leaks between tests. Rebuild the state in <code>beforeEach</code> so order never matters.",
         ar:"service متوفّرة في الـ root، أو متغيّر على مستوى الموديول، أو global مزيّف ما اترجّعش لأصله — كل دول بيسربوا بين الاختبارات. أعِد بناء الحالة في <code>beforeEach</code> علشان الترتيب ميفرقش خالص."} },
],

'e2e-testing': [
  { q: {en:"What does an end-to-end test exercise that a unit test does not?",
        ar:"اختبار الطرف للطرف بيجرّب إيه واختبار الوحدة لأ؟"},
    opts:[{en:"The real app in a real browser — routing, HTTP, rendering, all together",
           ar:"التطبيق الحقيقي في متصفح حقيقي — المسارات والـ HTTP والرسم، كلهم مع بعض"},
          {en:"Private methods", ar:"الدوال الخاصة"},
          {en:"Type errors", ar:"أخطاء الأنواع"},
          {en:"Bundle size", ar:"حجم الحزمة"}],
    a:0,
    why:{en:"It is the only level that proves the pieces fit. It is also the slowest and most brittle, which is why you want a few e2e journeys over the critical flows and many fast unit tests underneath.",
         ar:"ده المستوى الوحيد اللي بيثبت إن القطع بتتلاقي مع بعض. وهو كمان الأبطأ والأكثر هشاشة، وعلشان كده إنت عايز كام رحلة e2e على المسارات الحرجة وتحتها اختبارات وحدة كتير وسريعة."} },

  { q: {en:"What is a page object for?",
        ar:"الـ page object بيستخدم لإيه؟"},
    opts:[{en:"One place per screen for its selectors and actions",
           ar:"مكان واحد لكل شاشة فيه selectors وأفعالها"},
          {en:"Rendering the page faster", ar:"رسم الصفحة أسرع"},
          {en:"Mocking HTTP", ar:"تزييف الـ HTTP"},
          {en:"Running tests in parallel", ar:"تشغيل الاختبارات بالتوازي"}],
    a:0,
    why:{en:"When a button's markup changes you fix one file, not twenty tests. It also makes the specs read like the user story rather than a pile of CSS selectors.",
         ar:"لما ماركب زرار يتغيّر بتصلّح ملف واحد، مش عشرين اختبار. وكمان بيخلي الاختبارات تتقرا زي قصة المستخدم مش كومة CSS selectors."} },

  { q: {en:"Which selector is most robust in an e2e test?",
        ar:"أنهي selector أمتن في اختبار e2e؟"},
    opts:[{en:"A role or a <code>data-testid</code>", ar:"role أو <code>data-testid</code>"},
          {en:"A generated CSS class", ar:"كلاس CSS متولّد"},
          {en:"The element's index", ar:"ترتيب العنصر"},
          {en:"A deep child selector", ar:"selector لابن عميق"}],
    a:0,
    why:{en:"Roles and test ids survive restyling, and roles double as an accessibility check. Generated classes and positions change the moment a designer touches the page.",
         ar:"الـ roles وتعريفات الاختبار بتنجو من إعادة التنسيق، والـ roles كمان بتعمل فحص إتاحة في نفس الوقت. أما الكلاسات المتولّدة والمواضع فبتتغير أول ما مصمّم يلمس الصفحة."} },

  { q: {en:"How should an e2e suite handle the backend?",
        ar:"مجموعة اختبارات e2e المفروض تتعامل مع الـ backend إزاي؟"},
    opts:[{en:"A known, seeded state — a test database or intercepted network calls",
           ar:"حالة معروفة ومُجهّزة — قاعدة بيانات اختبار أو طلبات شبكة مُعترضة"},
          {en:"The production API", ar:"الـ API الإنتاجي"},
          {en:"No backend at all", ar:"مفيش backend خالص"},
          {en:"Whatever data happens to be there", ar:"أي بيانات موجودة بالصدفة"}],
    a:0,
    why:{en:"Tests that depend on whatever data exists fail randomly, and a flaky suite gets ignored. Seed the state you need, or intercept the calls and reply with fixtures.",
         ar:"الاختبارات اللي بتعتمد على أي بيانات موجودة بتفشل عشوائياً، والمجموعة المتقلّبة بيتم تجاهلها. جهّز الحالة اللي محتاجها، أو اعترض الطلبات ورُد ببيانات ثابتة."} },
],

'aot-tree-shaking': [
  { q: {en:"What does the AOT compiler produce from your template?",
        ar:"مترجم الـ AOT بيطلع إيه من الـ template بتاعك؟"},
    opts:[{en:"JavaScript render instructions, built before the browser gets it",
           ar:"تعليمات رسم بالجافاسكريبت، متبنية قبل ما المتصفح يستلمها"},
          {en:"An HTML string", ar:"نص HTML"},
          {en:"A virtual DOM tree", ar:"شجرة DOM افتراضية"},
          {en:"A CSS file", ar:"ملف CSS"}],
    a:0,
    why:{en:"Templates are compiled at build time, so the browser never ships a template compiler and template errors surface during <code>ng build</code> rather than at runtime.",
         ar:"الـ templates بتتترجم وقت البناء، فالمتصفح مبياخدش مترجم templates خالص وأخطاء الـ template بتظهر وقت <code>ng build</code> مش وقت التشغيل."} },

  { q: {en:"What stops tree-shaking from removing dead code?",
        ar:"إيه اللي بيمنع الـ tree-shaking من إنه يشيل الكود الميت؟"},
    opts:[{en:"Side effects at module load, and dynamic access the bundler cannot follow",
           ar:"تأثيرات جانبية وقت تحميل الموديول، ووصول ديناميكي الـ bundler مش قادر يتبعه"},
          {en:"Using TypeScript", ar:"استخدام TypeScript"},
          {en:"Too many files", ar:"ملفات كتير"},
          {en:"Standalone components", ar:"الـ components المستقلة"}],
    a:0,
    why:{en:"The bundler must be able to prove nothing is used. Top-level code with side effects, or a lookup like <code>obj[name]</code>, defeats that proof — so everything is kept just in case.",
         ar:"الـ bundler لازم يقدر يثبت إن مفيش حاجة مستخدمة. والكود على المستوى الأعلى اللي له تأثيرات جانبية، أو بحث زي <code>obj[name]</code>، بيهزم الإثبات ده — فبيتم الاحتفاظ بكل حاجة تحوّطاً."} },

  { q: {en:"Why do standalone components help bundle size?",
        ar:"ليه الـ components المستقلة بتساعد في حجم الحزمة؟"},
    opts:[{en:"Dependencies are per component, so nothing arrives via a big shared module",
           ar:"التبعيات بتبقى لكل component، فمفيش حاجة بتوصل من موديول مشترك كبير"},
          {en:"They compile faster", ar:"بتتترجم أسرع"},
          {en:"They skip change detection", ar:"بتتخطى فحص التغييرات"},
          {en:"They are always lazy", ar:"دايماً مؤجّلة"}],
    a:0,
    why:{en:"An <code>NgModule</code> declared everything together, so importing one component dragged its module's whole surface along. With <code>imports</code> on the component, the graph is precise and much easier to shake.",
         ar:"الـ <code>NgModule</code> كان بيعلن كل حاجة مع بعض، فأي import لـ component واحد كان بيجرّ سطح الموديول كله معاه. ومع <code>imports</code> على الـ component، الرسم البياني بيبقى دقيق وأسهل بكتير في التقليم."} },

  { q: {en:"Which tells you what actually ended up in the bundle?",
        ar:"أنهي واحدة بتقولك إيه اللي وصل فعلاً للحزمة؟"},
    opts:[{en:"<code>ng build --stats-json</code> plus a bundle analyser",
           ar:"<code>ng build --stats-json</code> مع محلّل حزم"},
          {en:"The DevTools profiler", ar:"profiler الـ DevTools"},
          {en:"<code>ng test</code>", ar:"<code>ng test</code>"},
          {en:"The network tab alone", ar:"تاب الشبكة لوحده"}],
    a:0,
    why:{en:"The stats file maps every byte back to the module it came from, which is how you find the one date library nobody meant to ship. The profiler answers a different question: what runs.",
         ar:"ملف الإحصائيات بيربط كل بايت بالموديول اللي جه منه، وده إزاي تلاقي مكتبة التواريخ اللي محدش كان قاصد ينشرها. أما الـ profiler فبيجاوب على سؤال تاني: إيه اللي بيشتغل."} },
],

'pwa': [
  { q: {en:"What does the service worker sit between?",
        ar:"الـ service worker قاعد بين إيه وإيه؟"},
    opts:[{en:"Your app and the network", ar:"تطبيقك والشبكة"},
          {en:"Angular and the DOM", ar:"أنجولار والـ DOM"},
          {en:"The server and the database", ar:"السيرفر وقاعدة البيانات"},
          {en:"Two components", ar:"اتنين components"}],
    a:0,
    why:{en:"It intercepts requests, so it can answer from its cache when the network is gone. That single position is what makes offline support and instant repeat loads possible.",
         ar:"بيعترض الطلبات، فيقدر يرد من الكاش بتاعه لما الشبكة تروح. والموقع الواحد ده هو اللي بيخلي الدعم بدون إنترنت والتحميل المتكرر الفوري ممكنين."} },

  { q: {en:"Which strategy suits an API that changes often?",
        ar:"أنهي استراتيجية تناسب API بيتغيّر كتير؟"},
    opts:[{en:"<code>freshness</code> — network first, cache as the fallback",
           ar:"<code>freshness</code> — الشبكة الأول، والكاش بديل"},
          {en:"<code>performance</code> — cache first", ar:"<code>performance</code> — الكاش الأول"},
          {en:"No caching is possible for APIs", ar:"مفيش كاش ممكن للـ APIs"},
          {en:"Cache only", ar:"الكاش بس"}],
    a:0,
    why:{en:"<code>performance</code> is right for things that rarely change — fonts, images, the app shell. Live data wants <code>freshness</code>, so the cache is only used when the network fails.",
         ar:"<code>performance</code> مناسبة للحاجات اللي نادراً ما تتغيّر — الخطوط والصور وهيكل التطبيق. أما البيانات الحيّة فعايزة <code>freshness</code>، فالكاش بيستخدم بس لما الشبكة تفشل."} },

  { q: {en:"A user reports seeing an old version after you deployed. What is happening?",
        ar:"مستخدم بيقول إنه شايف نسخة قديمة بعد النشر. إيه اللي بيحصل؟"},
    opts:[{en:"The old service worker is still serving cached files until it updates",
           ar:"الـ service worker القديم لسه بيقدّم ملفات من الكاش لحد ما يتحدّث"},
          {en:"The CDN is broken", ar:"الـ CDN مكسور"},
          {en:"The build failed", ar:"البناء فشل"},
          {en:"Their browser is unsupported", ar:"متصفحه مش مدعوم"}],
    a:0,
    why:{en:"That staleness is the deal you accepted for offline support. Use <code>SwUpdate</code> to notice a new version and prompt the user to reload — otherwise they may sit on an old build for days.",
         ar:"التقادم ده هو الاتفاق اللي قبلته مقابل الدعم بدون إنترنت. استخدم <code>SwUpdate</code> علشان تلاحظ نسخة جديدة وتطلب من المستخدم يعيد التحميل — وإلا يمكن يفضل على بناء قديم أيام."} },

  { q: {en:"Does the service worker run in <code>ng serve</code>?",
        ar:"الـ service worker بيشتغل في <code>ng serve</code>؟"},
    opts:[{en:"No — it is only active in a production build",
           ar:"لأ — بيبقى نشط في البناء الإنتاجي بس"},
          {en:"Yes, always", ar:"أيوه، دايماً"},
          {en:"Only with a flag", ar:"بس مع flag"},
          {en:"Only in Chrome", ar:"في كروم بس"}],
    a:0,
    why:{en:"Caching during development would be maddening, so it is disabled. Test it by building for production and serving <code>dist/</code> with a static server — <code>http-server</code> is enough.",
         ar:"الكاش وقت التطوير كان هيبقى مجنّن، فهو معطّل. اختبره بإنك تبني للإنتاج وتقدّم <code>dist/</code> بسيرفر ملفات ثابتة — <code>http-server</code> يكفي."} },
],

'schematics': [
  { q: {en:"What is a schematic?",
        ar:"إيه هو الـ schematic؟"},
    opts:[{en:"Code that generates or edits code — what <code>ng generate</code> runs",
           ar:"كود بيولّد أو بيعدّل كود — وده اللي <code>ng generate</code> بيشغّله"},
          {en:"A build configuration", ar:"إعداد بناء"},
          {en:"A component template", ar:"template لـ component"},
          {en:"A type definition", ar:"تعريف نوع"}],
    a:0,
    why:{en:"Every <code>ng generate</code> and every <code>ng add</code> is a schematic. Writing your own is how a team stops copying a folder by hand for the fortieth time.",
         ar:"كل <code>ng generate</code> وكل <code>ng add</code> ده schematic. وإنك تكتب واحد من عندك هو إزاي الفريق يبطّل ينسخ فولدر بإيده للمرة الأربعين."} },

  { q: {en:"What does <code>collection.json</code> do?",
        ar:"<code>collection.json</code> بيعمل إيه؟"},
    opts:[{en:"Maps a command name to the factory function that implements it",
           ar:"بيربط اسم أمر بدالة المصنع اللي بتنفّذه"},
          {en:"Lists the template files", ar:"بيسرد ملفات القوالب"},
          {en:"Declares dependencies", ar:"بيعلن التبعيات"},
          {en:"Configures the build", ar:"بيظبط البناء"}],
    a:0,
    why:{en:"It is the index: <code>ng g my-lib:feature</code> looks up <code>feature</code> there and finds the factory to run, along with the schema describing its options.",
         ar:"ده الفهرس: <code>ng g my-lib:feature</code> بيدوّر على <code>feature</code> هناك وبيلاقي المصنع اللي هيشتغل، ومعاه الـ schema اللي بتوصف خياراته."} },

  { q: {en:"Why are placeholders written as <code>__name@dasherize__</code>?",
        ar:"ليه العناصر البديلة بتتكتب <code>__name@dasherize__</code>؟"},
    opts:[{en:"The file name is substituted and transformed as the template is applied",
                      ar:"اسم الملف بيتم استبداله وتحويله وقت تطبيق القالب"},
          {en:"It is a TypeScript convention", ar:"ده عُرف في TypeScript"},
          {en:"To mark private files", ar:"لتعليم الملفات الخاصة"},
          {en:"To skip compilation", ar:"لتخطي الترجمة"}],
    a:0,
    why:{en:"Both file names and file contents go through the template engine, so one stencil can produce <code>user-profile.component.ts</code> with a <code>UserProfileComponent</code> class inside it.",
         ar:"أسماء الملفات ومحتواها الاتنين بيمروا من محرك القوالب، فقالب واحد يقدر يطلع <code>user-profile.component.ts</code> وجواه كلاس <code>UserProfileComponent</code>."} },

  { q: {en:"When is writing a schematic worth it?",
        ar:"إمتى كتابة schematic تستاهل؟"},
    opts:[{en:"When a team repeats the same multi-file pattern often",
           ar:"لما فريق يكرر نفس النمط متعدد الملفات كتير"},
          {en:"For any new component", ar:"لأي component جديد"},
          {en:"Once, for a single migration", ar:"مرة واحدة، لترحيل واحد"},
          {en:"Only for published libraries", ar:"للمكتبات المنشورة بس"}],
    a:0,
    why:{en:"The value is consistency at scale: a feature folder that always has the same shape. For a one-off change, a small script or an editor snippet costs far less to write.",
         ar:"القيمة هي الاتساق على نطاق واسع: فولدر ميزة بيبقى دايماً بنفس الشكل. أما لتغيير مرة واحدة، فسكربت صغير أو snippet في المحرر بيكلّف أقل بكتير."} },
],

'micro-frontends': [
  { q: {en:"What problem do micro-frontends actually solve?",
        ar:"الـ micro-frontends بتحل مشكلة إيه فعلاً؟"},
    opts:[{en:"Independent deployment by separate teams",
           ar:"نشر مستقل لفرق منفصلة"},
          {en:"Runtime performance", ar:"أداء وقت التشغيل"},
          {en:"Bundle size", ar:"حجم الحزمة"},
          {en:"Type safety", ar:"أمان الأنواع"}],
    a:0,
    why:{en:"The win is organisational: the checkout team ships without waiting for anyone. Technically you pay for it — duplicate framework code, harder debugging, shared-version headaches.",
         ar:"المكسب تنظيمي: فريق الدفع بينشر من غير ما يستنى حد. وتقنياً إنت بتدفع التمن — كود framework مكرر، وتتبع أخطاء أصعب، وصداع إصدارات مشتركة."} },

  { q: {en:"When should you <b>not</b> use them?",
        ar:"إمتى <b>ماينفعش</b> تستخدمهم؟"},
    opts:[{en:"One team, one release train — the overhead buys you nothing",
           ar:"فريق واحد وقطار إصدار واحد — التعقيد مش هيجيب لك حاجة"},
          {en:"When the app is large", ar:"لما التطبيق يكون كبير"},
          {en:"When you use signals", ar:"لما تستخدم signals"},
          {en:"When you need SSR", ar:"لما تحتاج SSR"}],
    a:0,
    why:{en:"With one team, lazy-loaded routes give you the same code splitting with none of the coordination cost. Micro-frontends are an answer to an organisational problem, so reach for them when you have that problem.",
         ar:"مع فريق واحد، المسارات المؤجّلة بتدّيك نفس تقسيم الكود من غير أي تكلفة تنسيق. والـ micro-frontends إجابة لمشكلة تنظيمية، فاستخدمهم لما تكون المشكلة دي عندك."} },

  { q: {en:"What is the main technical risk?",
        ar:"إيه الخطر التقني الأساسي؟"},
    opts:[{en:"Version drift — two remotes on different Angular versions",
           ar:"انحراف الإصدارات — اتنين remotes على إصدارات أنجولار مختلفة"},
          {en:"Too many routes", ar:"مسارات كتير"},
          {en:"CSS specificity", ar:"أولوية الـ CSS"},
          {en:"Missing types", ar:"أنواع ناقصة"}],
    a:0,
    why:{en:"Shared singletons and framework internals assume one version. Drift produces baffling runtime errors, which is why teams end up enforcing a shared version policy — and why the independence is never total.",
         ar:"الـ singletons المشتركة وتفاصيل الـ framework بتفترض إصدار واحد. والانحراف بيطلّع أخطاء وقت تشغيل محيّرة، وعلشان كده الفرق بتنتهي بفرض سياسة إصدار مشترك — وعلشان كده الاستقلال مبيبقى كامل أبداً."} },

  { q: {en:"How should two micro-frontends share state?",
        ar:"إزاي اتنين micro-frontends يشاركوا الحالة؟"},
    opts:[{en:"Through a narrow, explicit contract — the URL, events, or a tiny shared library",
           ar:"من خلال عقد ضيق وصريح — الـ URL، أو أحداث، أو مكتبة مشتركة صغيرة"},
          {en:"A shared global object", ar:"كائن عام مشترك"},
          {en:"Direct imports of each other's services", ar:"import مباشر لخدمات كل واحد من التاني"},
          {en:"<code>localStorage</code> for everything", ar:"<code>localStorage</code> لكل حاجة"}],
    a:0,
    why:{en:"Every shared thing is coupling, and coupling is what you were trying to escape. Keep the surface tiny and explicit, so a remote can change internally without breaking the shell.",
         ar:"كل حاجة مشتركة هي ارتباط، والارتباط هو اللي كنت بتهرب منه. خلي السطح صغير وصريح، علشان أي remote يقدر يتغيّر من جوه من غير ما يكسر الـ shell."} },
],

};
