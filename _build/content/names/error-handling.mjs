/* names for the error handling topic.
   n: the name exactly as typed in the code.   k: ng | mine | pub.
   w: what it is, and what has to change with it.   { en, ar }
   re / only / as: see _build/names.mjs.                                  */
export default {
'error-handling': {
  note: {
    en: '<code>handleError</code> looks like a name you picked, but it is not: <code>ErrorHandler</code> is a contract, and Angular calls exactly that method. The same goes for the keys of <code>retry({ count, delay })</code>. Your service methods like <code>show</code> are yours, but every file that calls them has to follow a rename.',
    ar: '<code>handleError</code> شكله اسم انت اخترته، بس لأ: <code>ErrorHandler</code> ده عقد، وأنجولار بينادي الميثود دي بالاسم ده بالظبط. ونفس الكلام على مفاتيح <code>retry({ count, delay })</code>. أما ميثودز الـ service بتاعتك زي <code>show</code> فبتاعتك، بس أي ملف بيناديها لازم يتغير معاها.'
  },
  names: [
    { n:'HttpInterceptorFn', k:'ng', w:{ en:'Angular’s type for an interceptor function.', ar:'النوع بتاع أنجولار لـ interceptor function.' } },
    { n:'inject', k:'ng', w:{ en:'Angular’s function that hands you a dependency.', ar:'الـ function بتاعة أنجولار اللي بتديك dependency.' } },
    { n:'pipe', k:'ng', w:{ en:'RxJS’s method for chaining operators.', ar:'ميثود RxJS لتوصيل الـ operators ورا بعض.' } },
    { n:'retry', k:'ng', only:['int'], w:{ en:'An RxJS operator: subscribe again after a failure.', ar:'operator من RxJS: اعمل subscribe تاني بعد ما يفشل.' } },
    { n:'count', k:'ng', w:{ en:'An option key <code>retry</code> reads: how many extra tries.', ar:'مفتاح إعداد <code>retry</code> بيقراه: كام محاولة زيادة.' } },
    { n:'delay', k:'ng',
      w:{ en:'An option key <code>retry</code> reads: a function that decides when (or whether) to try again.',
          ar:'مفتاح إعداد <code>retry</code> بيقراه: function بتقرر امتى (أو هل) نجرب تاني.' } },
    { n:'timer', k:'ng', w:{ en:'An RxJS function: emit once after a wait.', ar:'function من RxJS: تبعت قيمة واحدة بعد وقت.' } },
    { n:'catchError', k:'ng', w:{ en:'An RxJS operator that runs when the stream fails.', ar:'operator من RxJS بيشتغل لما الـ stream يفشل.' } },
    { n:'throwError', k:'ng', w:{ en:'An RxJS function that makes a failing stream.', ar:'function من RxJS بتعمل stream فاشل.' } },
    { n:'HttpErrorResponse', k:'ng', w:{ en:'Angular’s type for a failed HTTP response.', ar:'النوع بتاع أنجولار لـ HTTP response فاشل.' } },
    { n:'status', k:'ng',
      w:{ en:'A field on <code>HttpErrorResponse</code>: the HTTP status code. <code>0</code> means no network.',
          ar:'field في <code>HttpErrorResponse</code>: الـ HTTP status code. و<code>0</code> معناها مفيش نت.' } },
    { n:'error', k:'ng', re:'(?<=\\.)error(?![\\w$-])',
      w:{ en:'Not yours in any of its uses: <code>err.error</code> is the body the server sent, <code>console.error</code> is the browser’s, and <code>.error()</code> is a resource signal.',
          ar:'مش بتاعك في أي مكان منهم: <code>err.error</code> هو الـ body اللي السيرفر بعته، و<code>console.error</code> بتاع المتصفح، و<code>.error()</code> signal في الـ resource.' } },
    { n:'ErrorHandler', k:'ng',
      w:{ en:'Angular’s class for the app-wide “last resort” catcher. You replace it with your own.',
          ar:'الكلاس بتاع أنجولار اللي بيمسك أي error محدش مسكه على مستوى التطبيق. وانت بتحط مكانه واحد بتاعك.' } },
    { n:'handleError', k:'ng',
      w:{ en:'Fixed by <code>ErrorHandler</code>: Angular calls exactly this method. Rename it and your handler is never called.',
          ar:'ثابت بسبب <code>ErrorHandler</code>: أنجولار بينادي الميثود دي بالاسم ده بالظبط. لو غيّرته الـ handler بتاعك عمره ما هيتنادى.' } },
    { n:'Injectable', k:'ng', w:{ en:'Angular’s decorator for a service.', ar:'الـ decorator بتاع أنجولار للـ service.' } },
    { n:'providedIn', k:'ng', w:{ en:'An option key Angular reads.', ar:'مفتاح إعداد أنجولار بيقراه.' } },
    { n:'root', k:'ng', w:{ en:'Angular’s value for “one shared instance for the whole app”.', ar:'قيمة أنجولار لـ “نسخة واحدة متشاركة للتطبيق كله”.' } },
    { n:'signal', k:'ng', w:{ en:'Angular’s writable signal.', ar:'الـ signal بتاعة أنجولار اللي بتتكتب.' } },
    { n:'asReadonly', k:'ng', w:{ en:'A signal method: a read-only view of it.', ar:'ميثود في الـ signal: نسخة للقراية بس.' } },
    { n:'update', k:'ng', w:{ en:'A signal method.', ar:'ميثود بتاعة الـ signal.' } },
    { n:'setTimeout', k:'ng', w:{ en:'The browser’s timer.', ar:'التايمر بتاع المتصفح.' } },
    { n:'isLoading', k:'ng', w:{ en:'A signal every resource has.', ar:'signal موجودة في أي resource.' } },
    { n:'value', k:'ng', w:{ en:'A signal every resource has: the loaded data.', ar:'signal موجودة في أي resource: الداتا اللي اتحمّلت.' } },
    { n:'reload', k:'ng', w:{ en:'A method every resource has.', ar:'ميثود موجودة في أي resource.' } },
    { n:'routerLink', k:'ng', w:{ en:'Angular’s link directive. The path in it is yours.', ar:'الـ directive بتاع أنجولار للينكات. الـ path اللي فيه بتاعك.' } },
    { n:'@if', k:'ng', w:{ en:'Angular’s template control flow.', ar:'الـ control flow بتاع أنجولار في التمبلت.' } },
    { n:'@else', k:'ng', w:{ en:'Angular’s template control flow.', ar:'الـ control flow بتاع أنجولار في التمبلت.' } },

    { n:'errorInterceptor', k:'pub',
      w:{ en:'Your interceptor, exported and listed in <code>withInterceptors</code> in the app config.',
          ar:'الـ interceptor بتاعك، متعمله export ومكتوب في <code>withInterceptors</code> في الـ app config.' } },
    { n:'req', k:'mine',
      w:{ en:'The interceptor’s first parameter. Angular passes it by position, so the name is yours.',
          ar:'أول parameter في الـ interceptor. أنجولار بيبعته بالترتيب، فالاسم بتاعك.' } },
    { n:'next', k:'mine',
      w:{ en:'The interceptor’s second parameter. A convention, not a rule.',
          ar:'تاني parameter في الـ interceptor. ده عُرف مش قاعدة.' } },
    { n:'err', k:'mine',
      w:{ en:'A callback parameter (two separate ones, actually). RxJS passes it by position, so the name is yours.',
          ar:'parameter في callback (هما اتنين منفصلين في الحقيقة). RxJS بيبعته بالترتيب، فالاسم بتاعك.' } },
    { n:'retryable', k:'mine', w:{ en:'A local variable.', ar:'variable محلي.' } },
    { n:'toasts', k:'mine',
      w:{ en:'A local name for the injected <code>Toasts</code> service, in each file separately.',
          ar:'اسم محلي للـ service <code>Toasts</code> المتعملها inject، في كل ملف لوحده.' } },
    { n:'session', k:'mine', w:{ en:'A local name for the injected service.', ar:'اسم محلي للـ service المتعملها inject.' } },
    { n:'Session', k:'pub', w:{ en:'Your service class.', ar:'كلاس الـ service بتاعك.' } },
    { n:'signOut', k:'pub', w:{ en:'A method on your <code>Session</code> service, so the service changes with it.', ar:'ميثود في الـ service <code>Session</code> بتاعتك، فالـ service بتتغير معاها.' } },
    { n:'GlobalErrorHandler', k:'pub',
      w:{ en:'Your class. The app config registers it with <code>useClass: GlobalErrorHandler</code>.',
          ar:'الكلاس بتاعك. الـ app config بيسجّله بـ <code>useClass: GlobalErrorHandler</code>.' } },
    { n:'Toasts', k:'pub', w:{ en:'Your service class, injected in two other files here.', ar:'كلاس الـ service بتاعك، متعمله inject في ملفين تانيين هنا.' } },
    { n:'show', k:'pub',
      w:{ en:'Your service method. The interceptor and the error handler both call it, so all three files change together.',
          ar:'ميثود الـ service بتاعتك. الـ interceptor والـ error handler الاتنين بينادوها، فالتلات ملفات بيتغيروا مع بعض.' } },
    { n:'dismiss', k:'pub',
      w:{ en:'A public service method. The toast component’s close button would call it too.',
          ar:'ميثود public في الـ service. زرار الإغلاق في الـ toast component هينادي عليها برضه.' } },
    { n:'_all', k:'mine', w:{ en:'The service’s private signal. Only it can write.', ar:'الـ signal الخاصة بالـ service. هي بس اللي تقدر تكتب فيها.' } },
    { n:'all', k:'pub', w:{ en:'The read-only list other components display.', ar:'القايمة اللي للقراية بس، والـ components التانية بتعرضها.' } },
    { n:'Toast', k:'pub', w:{ en:'Your data type.', ar:'نوع الداتا بتاعك.' } },
    { n:'orders', k:'mine', only:['cmp'], re:'(?<![\\w$-])orders(?=\\.)', w:{ en:'The component’s own resource.', ar:'الـ resource بتاع الـ component نفسه.' } },
    { n:'data', k:'mine', only:['st1'], re:'(?<![\\w$-])data(?=\\.)', w:{ en:'The component’s own resource.', ar:'الـ resource بتاع الـ component نفسه.' } },
  ]
}
};
