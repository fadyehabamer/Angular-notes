/* names for the built-in pipes topic. Format: see inputs-outputs.mjs. */
export default {
'built-in-pipes': {
  note: {
    en: 'In <code>{{ x | pipe:arg }}</code> only the <b>left side</b> is yours. The pipe name after <code>|</code> and the format strings after <code>:</code> are Angular’s vocabulary; spell them wrong and the template will not compile.',
    ar: 'في <code>{{ x | pipe:arg }}</code> <b>الناحية الشمال</b> بس هي اللي بتاعتك. اسم الـ pipe بعد <code>|</code> والـ format strings بعد <code>:</code> دي مفردات أنجولار؛ لو كتبتها غلط التمبلت مش هيعمل compile.'
  },
  names: [
    { n:'date', k:'ng', only:['html'],
      w:{ en:'Angular’s <code>DatePipe</code>, registered under the name <code>date</code>.',
          ar:'الـ <code>DatePipe</code> بتاعة أنجولار، متسجلة باسم <code>date</code>.' } },
    { n:'currency', k:'ng',
      w:{ en:'Angular’s <code>CurrencyPipe</code>. The arguments after it are a currency code, a display style and digits.',
          ar:'الـ <code>CurrencyPipe</code> بتاعة أنجولار. اللي بعدها: كود العملة، وشكل العرض، وعدد الأرقام.' } },
    { n:'number', k:'ng',
      w:{ en:'Angular’s <code>DecimalPipe</code>. Its name in templates is <code>number</code>.',
          ar:'الـ <code>DecimalPipe</code> بتاعة أنجولار. اسمها في التمبلت <code>number</code>.' } },
    { n:'percent', k:'ng',
      w:{ en:'Angular’s <code>PercentPipe</code>: <code>0.125</code> becomes <code>12.5%</code>.',
          ar:'الـ <code>PercentPipe</code> بتاعة أنجولار: <code>0.125</code> بتبقى <code>12.5%</code>.' } },
    { n:'uppercase', k:'ng', w:{ en:'A built-in text pipe from Angular.', ar:'pipe جاهزة للنصوص من أنجولار.' } },
    { n:'lowercase', k:'ng', w:{ en:'A built-in text pipe from Angular.', ar:'pipe جاهزة للنصوص من أنجولار.' } },
    { n:'titlecase', k:'ng', w:{ en:'A built-in text pipe from Angular: capitalises every word.', ar:'pipe جاهزة من أنجولار: بتكبّر أول حرف في كل كلمة.' } },
    { n:'slice', k:'ng',
      w:{ en:'Angular’s <code>SlicePipe</code>. The numbers after it are start and end.',
          ar:'الـ <code>SlicePipe</code> بتاعة أنجولار. الأرقام اللي بعدها هي البداية والنهاية.' } },
    { n:'json', k:'ng', w:{ en:'Angular’s <code>JsonPipe</code>, for debugging.', ar:'الـ <code>JsonPipe</code> بتاعة أنجولار، للـ debugging.' } },
    { n:'async', k:'ng', only:['html'], re:'(?<![\\w$-])(?<!<!-- )async(?![\\w$-])',
      w:{ en:'Angular’s <code>AsyncPipe</code>: subscribes, unwraps and unsubscribes for you.',
          ar:'الـ <code>AsyncPipe</code> بتاعة أنجولار: بتعمل subscribe وتطلّع القيمة وتعمل unsubscribe لوحدها.' } },
    { n:'as', k:'ng', only:['html'],
      w:{ en:'Part of Angular’s <code>@if</code> syntax. The name after it is yours.',
          ar:'جزء من syntax الـ <code>@if</code> بتاع أنجولار. الاسم اللي بعده بتاعك.' } },
    { n:'@if', k:'ng', w:{ en:'Angular’s control flow block.', ar:'بلوك الـ control flow بتاع أنجولار.' } },
    { n:'short', k:'ng',
      w:{ en:'A named date format Angular knows, like <code>medium</code> and <code>mediumDate</code>. Not a name you can invent.',
          ar:'شكل تاريخ جاهز أنجولار عارفه، زي <code>medium</code> و<code>mediumDate</code>. مش اسم تقدر تخترعه.' } },
    { n:'EGP', k:'ng',
      w:{ en:'The ISO currency code for the Egyptian pound. It is a world standard, not yours.',
          ar:'كود العملة العالمي (ISO) للجنيه المصري. ده standard عالمي، مش بتاعك.' } },
    { n:'ar-EG', k:'ng',
      w:{ en:'A standard locale code. Angular ships the data for it under exactly this name.',
          ar:'كود locale قياسي. أنجولار جايب الداتا بتاعته بالاسم ده بالظبط.' } },
    { n:'input', k:'ng', w:{ en:'Angular’s function that creates an input.', ar:'الـ function بتاعة أنجولار اللي بتعمل input.' } },
    { n:'computed', k:'ng', w:{ en:'Angular’s derived signal.', ar:'الـ signal المشتقة بتاعة أنجولار.' } },
    { n:'inject', k:'ng', w:{ en:'Angular’s function that hands you a service or token.', ar:'الـ function بتاعة أنجولار اللي بتديك service أو token.' } },
    { n:'LOCALE_ID', k:'ng',
      w:{ en:'Angular’s injection token for the app’s language. Every locale-aware pipe reads it.',
          ar:'الـ injection token بتاع أنجولار للغة التطبيق. كل pipe بتفرق معاها اللغة بتقراه.' } },
    { n:'DEFAULT_CURRENCY_CODE', k:'ng',
      w:{ en:'Angular’s token for the currency <code>| currency</code> uses when you give it none.',
          ar:'الـ token بتاع أنجولار للعملة اللي <code>| currency</code> بتستخدمها لو مدّتهاش عملة.' } },
    { n:'formatDate', k:'ng',
      w:{ en:'The function behind the <code>date</code> pipe, exported by <code>@angular/common</code> for use in TypeScript.',
          ar:'الـ function اللي ورا الـ pipe <code>date</code>، و<code>@angular/common</code> بيصدّرها عشان تستخدمها في TypeScript.' } },
    { n:'formatCurrency', k:'ng',
      w:{ en:'The function behind the <code>currency</code> pipe, from <code>@angular/common</code>.',
          ar:'الـ function اللي ورا الـ pipe <code>currency</code>، من <code>@angular/common</code>.' } },
    { n:'registerLocaleData', k:'ng',
      w:{ en:'Angular’s function that loads a language’s formatting rules.',
          ar:'الـ function بتاعة أنجولار اللي بتحمّل قواعد التنسيق بتاعة لغة.' } },

    { n:'order', k:'pub',
      w:{ en:'An input. The parent binds it with <code>[order]</code>, so rename it there too.',
          ar:'input. الأب بيربطه بـ <code>[order]</code>، فغيّره هناك كمان.' } },
    { n:'Order', k:'pub',
      w:{ en:'Your data type. Every file that imports it follows a rename.', ar:'نوع الداتا بتاعك. أي ملف بيعمله import بيتغير معاه.' } },
    { n:'placedAt', k:'pub',
      w:{ en:'A field on your <code>Order</code> type. Whatever builds orders (and the API) uses the same name.',
          ar:'field في الـ type <code>Order</code> بتاعك. أي حاجة بتبني orders (والـ API) بتستخدم نفس الاسم.' } },
    { n:'OrderView', k:'pub',
      w:{ en:'The component’s class name. Whoever lists it in <code>imports</code> follows a rename.',
          ar:'اسم كلاس الـ component. أي حد كاتبه في <code>imports</code> بيتغير معاه.' } },
    { n:'total', k:'mine',
      w:{ en:'A computed the component reads in its own template.', ar:'computed الـ component بيقراه في التمبلت بتاعه.' } },
    { n:'user$', k:'mine',
      w:{ en:'The component’s own observable. The <code>$</code> is only a habit, not a rule.',
          ar:'الـ observable بتاع الـ component. الـ <code>$</code> مجرد عادة، مش قاعدة.' } },
    { n:'user', k:'mine', only:['async'],
      w:{ en:'The name you give the unwrapped value after <code>as</code>. It exists only inside that <code>@if</code> block.',
          ar:'الاسم اللي بتديه للقيمة بعد <code>as</code>. موجود بس جوه بلوك الـ <code>@if</code> ده.' } },
    { n:'locale', k:'mine',
      w:{ en:'A private field holding the injected <code>LOCALE_ID</code>.', ar:'field خاص شايل الـ <code>LOCALE_ID</code> اللي اتعمله inject.' } },
    { n:'csvRow', k:'mine', w:{ en:'The component’s own method.', ar:'ميثود الـ component نفسه.' } },
    { n:'localeArEg', k:'mine',
      w:{ en:'A default import, so the name on the left is yours. Only the path on the right is fixed.',
          ar:'default import، فالاسم اللي على الشمال بتاعك. المسار اللي على اليمين بس هو الثابت.' } },
    { n:'line', k:'pub',
      w:{ en:'The invoice row’s input. The parent sets it with <code>[line]</code>.', ar:'الـ input بتاع صف الفاتورة. الأب بيحطه بـ <code>[line]</code>.' } },
  ]
}
};
