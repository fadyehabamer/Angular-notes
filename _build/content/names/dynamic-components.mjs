/* names for the dynamic components topic (NgComponentOutlet, ViewContainerRef).
   n: the name exactly as typed in the code.   k: ng | mine | pub.
   w: what it is, and what has to change with it.   { en, ar }
   re / only / as: see _build/names.mjs.                                  */
export default {
'dynamic-components': {
  note: {
    en: 'When you create a component from code, the compiler can no longer check the names you hand it. The input names in <code>inputs</code> and in <code>setInput(\'…\')</code> are plain strings or keys: they must match the created component’s inputs exactly, and a typo fails silently.',
    ar: 'لما بتعمل component من الكود، الـ compiler مبقاش يقدر يراجع الأسماء اللي بتديهاله. أسماء الـ inputs اللي في <code>inputs</code> وفي <code>setInput(\'…\')</code> مجرد نصوص أو مفاتيح: لازم تطابق الـ inputs بتاعة الـ component اللي اتعمل بالظبط، ولو فيها غلطة مش هتاخد أي error.'
  },
  names: [
    { n:'Type', k:'ng',
      w:{ en:'Angular’s type for “a component class”, not an instance of one.', ar:'النوع بتاع أنجولار اللي معناه “كلاس component”، مش نسخة منه.' } },
    { n:'Record', k:'ng', w:{ en:'A TypeScript built-in type for an object with string keys.', ar:'نوع جاهز في TypeScript لأوبجكت مفاتيحه strings.' } },
    { n:'Promise', k:'ng', w:{ en:'JavaScript’s built-in type for a value that arrives later.', ar:'النوع الجاهز في JavaScript لقيمة هتيجي بعدين.' } },
    { n:'then', k:'ng', w:{ en:'The Promise method that runs once the import has downloaded.', ar:'ميثود الـ Promise اللي بتشتغل أول ما الـ import يخلص تحميل.' } },
    { n:'NgComponentOutlet', k:'ng', w:{ en:'Angular’s directive class. You list it in <code>imports</code>.', ar:'كلاس الـ directive بتاع أنجولار. بتحطه في <code>imports</code>.' } },
    { n:'ngComponentOutlet', k:'ng', w:{ en:'The same directive as it is written in a template, with <code>*</code>.', ar:'نفس الـ directive بالشكل اللي بيتكتب في التمبلت، بـ <code>*</code>.' } },
    { n:'inputs:', k:'ng', re:'(?<![\\w$.-])inputs(?=:)',
      w:{ en:'The outlet’s own option key (short for <code>ngComponentOutletInputs</code>). The value after the colon is yours.',
          ar:'مفتاح الإعداد بتاع الـ outlet نفسه (اختصار لـ <code>ngComponentOutletInputs</code>). القيمة اللي بعد النقطتين بتاعتك.' } },
    { n:'ng-container', k:'ng', w:{ en:'Angular’s invisible wrapper tag: it renders no element of its own.', ar:'تاج أنجولار الخفي: مش بيرسم أي element لنفسه.' } },
    { n:'@Component', k:'ng', w:{ en:'Angular’s decorator that turns a class into a component.', ar:'الـ decorator بتاع أنجولار اللي بيحوّل الكلاس لـ component.' } },
    { n:'input', k:'ng', re:'(?<![\\w$<(-])input(?=[.(<,])', w:{ en:'Angular’s function that creates an input.', ar:'الـ function بتاعة أنجولار اللي بتعمل input.' } },
    { n:'signal', k:'ng', w:{ en:'Angular’s writable signal.', ar:'الـ signal بتاعة أنجولار اللي بتتكتب.' } },
    { n:'computed', k:'ng', w:{ en:'Angular’s derived signal.', ar:'الـ signal المشتقة بتاعة أنجولار.' } },
    { n:'effect', k:'ng', w:{ en:'Angular’s function that re-runs whenever a signal it read changes.', ar:'الـ function بتاعة أنجولار اللي بتشتغل تاني كل ما signal قرتها تتغير.' } },
    { n:'ViewContainerRef', k:'ng', w:{ en:'Angular’s handle on a spot in the page where components can be inserted.', ar:'الـ handle بتاع أنجولار على مكان في الصفحة ينفع تحط فيه components.' } },
    { n:'EnvironmentInjector', k:'ng', w:{ en:'Angular’s injector type that supplies services to the created component.', ar:'نوع الـ injector بتاع أنجولار اللي بيدّي الـ services للـ component اللي اتعمل.' } },
    { n:'createComponent', k:'ng', w:{ en:'Angular’s method that builds the component and returns a <code>ComponentRef</code>.', ar:'الميثود بتاعة أنجولار اللي بتبني الـ component وبترجّع <code>ComponentRef</code>.' } },
    { n:'environmentInjector', k:'ng', w:{ en:'An option key Angular reads (lower-case <code>e</code>).', ar:'مفتاح إعداد أنجولار بيقراه (بحرف <code>e</code> صغير).' } },
    { n:'setInput', k:'ng', w:{ en:'<code>ComponentRef</code>’s method. The string you pass is the input name on the created component.', ar:'ميثود في الـ <code>ComponentRef</code>. النص اللي بتديهوله هو اسم الـ input في الـ component اللي اتعمل.' } },
    { n:'instance', k:'ng', w:{ en:'<code>ComponentRef</code>’s property: the actual component object.', ar:'property في الـ <code>ComponentRef</code>: الـ component نفسه.' } },
    { n:'destroy', k:'ng', w:{ en:'<code>ComponentRef</code>’s method that removes the component. Nobody calls it for you.', ar:'ميثود في الـ <code>ComponentRef</code> بتشيل الـ component. محدش هيناديها بدالك.' } },
    { n:'firstValueFrom', k:'ng', w:{ en:'RxJS’s helper that waits for the first value and gives you a Promise.', ar:'helper من RxJS بيستنى أول قيمة ويديك Promise.' } },

    { n:'WIDGETS', k:'pub', w:{ en:'Your registry, exported and imported by the widget host.', ar:'الـ registry بتاعك، بيتعمله export والـ widget host بيعمله import.' } },
    { n:'chart', k:'pub', re:'(?<![\\w$/.-])chart(?=:)',
      w:{ en:'A registry key. It must match the <code>type</code> string the server sends, so both sides change together.',
          ar:'مفتاح في الـ registry. لازم يطابق نص الـ <code>type</code> اللي السيرفر بيبعته، فالناحيتين بيتغيروا مع بعض.' } },
    { n:'ChartWidget', k:'pub', w:{ en:'The class exported by <code>./widgets/chart</code>. <code>m.ChartWidget</code> must match that export.', ar:'الكلاس اللي <code>./widgets/chart</code> بيعمله export. و<code>m.ChartWidget</code> لازم يطابق الـ export ده.' } },
    { n:'config', k:'pub',
      w:{ en:'An input. The dashboard binds <code>[config]</code>, and the key <code>config:</code> in <code>inputs</code> must match each widget’s own input of that name.',
          ar:'input. الداشبورد بيربط <code>[config]</code>، والمفتاح <code>config:</code> اللي في <code>inputs</code> لازم يطابق الـ input اللي بنفس الاسم في كل widget.' } },
    { n:'table', k:'pub', re:'(?<![\\w$/.-])table(?=:)',
      w:{ en:'Another registry key, matched against the server’s <code>type</code>.', ar:'مفتاح تاني في الـ registry، بيتطابق مع الـ <code>type</code> اللي جاي من السيرفر.' } },
    { n:'TableWidget', k:'pub', w:{ en:'The class exported by <code>./widgets/table</code>.', ar:'الكلاس اللي <code>./widgets/table</code> بيعمله export.' } },
    { n:'metric', k:'pub', re:'(?<![\\w$/.-])metric(?=:)',
      w:{ en:'Another registry key, matched against the server’s <code>type</code>.', ar:'مفتاح تاني في الـ registry، بيتطابق مع الـ <code>type</code> اللي جاي من السيرفر.' } },
    { n:'MetricWidget', k:'pub', w:{ en:'The class exported by <code>./widgets/metric</code>.', ar:'الكلاس اللي <code>./widgets/metric</code> بيعمله export.' } },
    { n:'WidgetConfig', k:'pub', w:{ en:'Your data type, shared with whatever builds the layout.', ar:'نوع الداتا بتاعك، متشارك مع أي حاجة بتبني الـ layout.' } },
    { n:'type', k:'pub', only:['outlet','js1','js2'],
      w:{ en:'A field name in the server’s JSON. Your interface and the server must agree on it.', ar:'اسم field في الـ JSON بتاع السيرفر. الـ interface بتاعك والسيرفر لازم يتفقوا عليه.' } },
    { n:'WidgetHost', k:'pub', w:{ en:'The host’s class name, listed in the dashboard’s <code>imports</code>.', ar:'اسم كلاس الـ host، مكتوب في <code>imports</code> بتاعة الداشبورد.' } },
    { n:'app-widget-host', k:'pub', w:{ en:'Selector: the string here and the tag in the dashboard template must match.', ar:'الـ selector: النص هنا والتاج في تمبلت الداشبورد لازم يبقوا زي بعض.' } },
    { n:'cmp', k:'mine', w:{ en:'The host’s own signal holding the loaded class.', ar:'الـ signal بتاعة الـ host اللي شايلة الكلاس اللي اتحمّل.' } },
    { n:'inputs', k:'mine', re:'(?<![\\w$.-])inputs(?![\\w$:-])',
      w:{ en:'Your computed, read after <code>inputs:</code>. Only the key on the left is the outlet’s; this one you may rename.',
          ar:'الـ computed بتاعك، بيتقري بعد <code>inputs:</code>. المفتاح اللي على الشمال بس هو بتاع الـ outlet؛ ده تقدر تغيّره.' } },
    { n:'load', k:'mine', w:{ en:'A local name for the loader function, in both files.', ar:'اسم محلي للـ function اللي بتحمّل، في الملفين.' } },
    { n:'DialogService', k:'pub', w:{ en:'Your service class, injected wherever a dialog is opened.', ar:'كلاس الـ service بتاعك، بيتعمله inject في أي مكان بيفتح dialog.' } },
    { n:'open', k:'pub', w:{ en:'Your service method. Every caller types <code>dialog.open(…)</code>.', ar:'ميثود الـ service بتاعك. أي حد بيناديها بيكتب <code>dialog.open(…)</code>.' } },
    { n:'vcr', k:'mine', w:{ en:'A private field name for the <code>ViewContainerRef</code>.', ar:'اسم field خاص للـ <code>ViewContainerRef</code>.' } },
    { n:'env', k:'mine', w:{ en:'A private field name for the injector.', ar:'اسم field خاص للـ injector.' } },
    { n:'ref', k:'mine', w:{ en:'A local name for the returned <code>ComponentRef</code>.', ar:'اسم محلي للـ <code>ComponentRef</code> اللي رجع.' } },
    { n:'\'data\'', k:'pub', re:'(?<=\')data(?=\')',
      w:{ en:'The input name on the created component, as a string. The compiler does not check it: rename the input and this string together.',
          ar:'اسم الـ input في الـ component اللي اتعمل، كنص. الـ compiler مش بيراجعه: غيّر الـ input والنص ده مع بعض.' } },
    { n:'data', k:'mine', re:'(?<![\\w$\'.-])data(?![\\w$\'-])',
      w:{ en:'Your method’s parameter. Unlike the quoted <code>\'data\'</code> next to it, it is just a local name.',
          ar:'الـ parameter بتاع الميثود بتاعتك. على عكس <code>\'data\'</code> اللي بين علامات التنصيص جنبه، ده مجرد اسم محلي.' } },
    { n:'closed', k:'pub', w:{ en:'An output on the created component. Rename it there and here together.', ar:'output في الـ component اللي اتعمل. غيّره هناك وهنا مع بعض.' } },
    { n:'layout', k:'mine', re:'(?<![\\w$.-])layout(?=\\()', w:{ en:'The dashboard’s own signal.', ar:'الـ signal بتاعة الداشبورد نفسها.' } },
    { n:'controlFor', k:'mine', w:{ en:'The form component’s own method that picks a class for a field type.', ar:'ميثود الفورم نفسه اللي بتختار كلاس لكل نوع field.' } },
    { n:'field', k:'mine', re:'(?<![\\w$.-])field(?![\\w$:-])',
      w:{ en:'The loop variable. The key <code>field:</code> inside <code>inputs</code> is the created control’s input name and stays.',
          ar:'متغير الـ loop. المفتاح <code>field:</code> اللي جوه <code>inputs</code> ده اسم الـ input في الكنترول اللي اتعمل وبيفضل زي ما هو.' } },
    { n:'key', k:'pub', only:['js1','js2'], w:{ en:'A field name in the server’s JSON, used for <code>track</code>.', ar:'اسم field في الـ JSON بتاع السيرفر، بنستخدمه في <code>track</code>.' } },
    { n:'formGroup', k:'ng', w:{ en:'Angular’s reactive-forms directive.', ar:'الـ directive بتاع الـ reactive forms في أنجولار.' } },
  ]
}
};
