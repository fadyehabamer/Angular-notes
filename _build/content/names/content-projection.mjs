/* names for the content projection topic.
   n: the name exactly as typed in the code.   k: ng | mine | pub.
   w: what it is, and what has to change with it.   { en, ar }
   re / only / as: see _build/names.mjs.                                  */
export default {
'content-projection': {
  note: {
    en: '<code>ng-content</code> and <code>select</code> are Angular’s. What goes <em>inside</em> <code>select="…"</code> is yours, and it is a promise: the parent has to write a matching attribute on what it passes in. Rename the slot on one side only and the content silently lands in the default slot instead.',
    ar: '<code>ng-content</code> و<code>select</code> بتوع أنجولار. اللي بتكتبه <em>جوه</em> <code>select="…"</code> بتاعك، وده وعد: الأب لازم يكتب attribute زيه بالظبط على الحاجة اللي بيبعتها. لو غيّرت اسم الـ slot في ناحية واحدة بس، المحتوى هيروح للـ slot الافتراضي من غير ما حد يقولك.'
  },
  names: [
    { n:'ng-content', k:'ng',
      w:{ en:'Angular’s placeholder tag: “put what the parent wrote between my tags here”.',
          ar:'تاج أنجولار اللي بيحجز مكان: “حط هنا اللي الأب كتبه بين التاجات بتاعتي”.' } },
    { n:'select', k:'ng',
      w:{ en:'The attribute Angular reads on <code>&lt;ng-content&gt;</code>. Its value is a CSS selector you choose.',
          ar:'الـ attribute اللي أنجولار بيقراه على <code>&lt;ng-content&gt;</code>. القيمة بتاعته CSS selector انت اللي بتختاره.' } },
    { n:'ng-container', k:'ng',
      w:{ en:'Angular’s invisible grouping tag. It never reaches the page.',
          ar:'تاج أنجولار اللي بيجمّع حاجات من غير ما يبان. عمره ما بيوصل للصفحة.' } },
    { n:'ng-template', k:'ng',
      w:{ en:'Angular’s tag for markup that is not shown until something renders it.',
          ar:'تاج أنجولار لـ markup مش بيظهر لحد ما حاجة ترسمه.' } },
    { n:'*ngIf', k:'ng',
      w:{ en:'Angular’s older directive for “show if”. The star is part of the syntax. <code>@if</code> replaces it.',
          ar:'الـ directive القديم بتاع أنجولار لـ “اظهر لو”. النجمة جزء من الكتابة. و<code>@if</code> هو البديل.' } },
    { n:'*ngTemplateOutlet', k:'ng',
      w:{ en:'Angular’s directive that renders an <code>ng-template</code> by its reference name.',
          ar:'الـ directive بتاع أنجولار اللي بيرسم <code>ng-template</code> باسم الـ reference بتاعه.' } },
    { n:'@if', k:'ng', w:{ en:'Angular’s template control flow.', ar:'الـ control flow بتاع أنجولار في التمبلت.' } },
    { n:'@else', k:'ng', w:{ en:'Angular’s template control flow.', ar:'الـ control flow بتاع أنجولار في التمبلت.' } },
    { n:'@for', k:'ng', w:{ en:'Angular’s template loop.', ar:'الـ loop بتاع أنجولار في التمبلت.' } },
    { n:'track', k:'ng',
      w:{ en:'A required part of <code>@for</code>. What you track by is yours.',
          ar:'جزء لازم في <code>@for</code>. الحاجة اللي بتعمل بيها track بتاعتك.' } },
    { n:'@Component', k:'ng',
      w:{ en:'Angular’s decorator that turns the class into a component.',
          ar:'الـ decorator بتاع أنجولار اللي بيحوّل الكلاس لـ component.' } },
    { n:'selector', k:'ng', w:{ en:'An option key Angular reads.', ar:'مفتاح إعداد أنجولار بيقراه.' } },
    { n:'input', k:'ng', re:'(?<![\\w$<(-])input(?=[.(<,])',
      w:{ en:'Angular’s function that creates an input.', ar:'الـ function بتاعة أنجولار اللي بتعمل input.' } },
    { n:'required', k:'ng',
      w:{ en:'Part of Angular’s API: <code>input.required</code>.', ar:'جزء من API أنجولار: <code>input.required</code>.' } },
    { n:'signal', k:'ng', w:{ en:'Angular’s writable signal.', ar:'الـ signal بتاعة أنجولار اللي بتتكتب.' } },
    { n:'set', k:'ng', w:{ en:'A signal method.', ar:'ميثود بتاعة الـ signal.' } },
    { n:'emit', k:'ng',
      w:{ en:'The method every output has.', ar:'الميثود اللي موجودة في أي output.' } },
    { n:'click', k:'ng',
      w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'cdkTrapFocus', k:'ng',
      w:{ en:'A directive from the Angular CDK library that keeps keyboard focus inside the modal.',
          ar:'directive من library الـ Angular CDK بيخلي الـ focus بتاع الكيبورد جوه الـ modal.' } },

    { n:'slot', k:'pub',
      w:{ en:'The attribute name this code chose to label slots. <code>select="[slot=…]"</code> and the parent’s <code>slot="…"</code> must agree, so rename both.',
          ar:'اسم الـ attribute اللي الكود ده اختاره عشان يعلّم الـ slots. <code>select="[slot=…]"</code> و<code>slot="…"</code> اللي عند الأب لازم يتفقوا، فغيّر الاتنين.' } },
    { n:'actions', k:'pub', re:'(?<=slot="?)actions(?![\\w$-])',
      w:{ en:'A slot name. The panel selects it and the settings page labels its button with it. (<code>class="actions"</code> is a CSS class, a different thing.)',
          ar:'اسم slot. الـ panel بيختاره وصفحة الإعدادات بتعلّم بيه الزرار. (<code>class="actions"</code> CSS class، حاجة تانية.)' } },
    { n:'footer', k:'pub', re:'(?<=slot="?)footer(?![\\w$-])',
      w:{ en:'A slot name, typed in the modal’s <code>select</code> and on the parent’s buttons. (The <code>&lt;footer&gt;</code> tag is HTML’s.)',
          ar:'اسم slot، مكتوب في <code>select</code> بتاع الـ modal وعلى زراير الأب. (وتاج <code>&lt;footer&gt;</code> بتاع HTML.)' } },
    { n:'title', k:'pub', re:'(?<![\\w$-])(?<!slot="?)title(?![\\w$-])',
      w:{ en:'The panel’s input. The parent sets it with <code>title="…"</code>, so both files change together.',
          ar:'الـ input بتاع الـ panel. الأب بيحطه بـ <code>title="…"</code>، فالملفين بيتغيروا مع بعض.' } },
    { n:'collapsible', k:'pub',
      w:{ en:'An input the parent sets with <code>[collapsible]</code>.', ar:'input الأب بيحطه بـ <code>[collapsible]</code>.' } },
    { n:'open', k:'mine',
      w:{ en:'The panel’s own signal, read and set only by its own template.',
          ar:'الـ signal بتاعة الـ panel نفسه، والتمبلت بتاعه بس اللي بيقراها ويغيّرها.' } },
    { n:'Panel', k:'pub',
      w:{ en:'The class name. Whoever lists it in <code>imports</code> follows a rename.',
          ar:'اسم الكلاس. أي حد كاتبه في <code>imports</code> بيتغير معاه.' } },
    { n:'app-panel', k:'pub',
      w:{ en:'The selector: the string in the class and the tag in the parent must match.',
          ar:'الـ selector: النص في الكلاس والتاج عند الأب لازم يبقوا زي بعض.' } },
    { n:'reset', k:'mine',
      w:{ en:'The settings page’s own method. The panel never sees it; it only moves the button.',
          ar:'ميثود صفحة الإعدادات نفسها. الـ panel عمره ما بيشوفها؛ هو بس بينقل الزرار.' } },
    { n:'app-toggle', k:'pub',
      w:{ en:'Another component’s selector.', ar:'الـ selector بتاع component تاني.' } },
    { n:'checked', k:'pub',
      w:{ en:'A <code>model()</code> on the toggle, bound with <code>[(checked)]</code>. Renaming it means changing the toggle too.',
          ar:'<code>model()</code> في الـ toggle، مربوط بـ <code>[(checked)]</code>. لو غيّرته لازم تغيّر الـ toggle كمان.' } },
    { n:'empty', k:'mine',
      w:{ en:'A template reference variable: <code>#empty</code> names the template and <code>*ngTemplateOutlet</code> uses it, in the same file.',
          ar:'template reference variable: <code>#empty</code> بيسمّي التمبلت و<code>*ngTemplateOutlet</code> بيستخدمه، في نفس الملف.' } },
    { n:'dismiss', k:'pub',
      w:{ en:'The modal’s output. The modal calls <code>dismiss.emit()</code>; the parent listens with <code>(dismiss)</code>.',
          ar:'الـ output بتاع الـ modal. الـ modal بينادي <code>dismiss.emit()</code>؛ والأب بيسمعه بـ <code>(dismiss)</code>.' } },
    { n:'app-modal', k:'pub',
      w:{ en:'The modal’s selector, typed as a tag by the parent.', ar:'الـ selector بتاع الـ modal، والأب بيكتبه كتاج.' } },
    { n:'close', k:'mine',
      w:{ en:'The parent’s own method. The modal does not know or care what it is called.',
          ar:'ميثود الأب نفسه. الـ modal ميعرفش ومش فارق معاه اسمها إيه.' } },
  ]
}
};
