/* names for the template-driven forms topic. Format: see inputs-outputs.mjs. */
export default {
'template-driven-forms': {
  note: {
    en: 'One input line can hold the word <code>email</code> five times, meaning five things. <code>type="email"</code> is the browser’s, the bare <code>email</code> attribute is Angular’s validator, <code>"ngModel"</code> is Angular’s, and <code>#email</code> and <code>model.email</code> are yours. Read each word by <b>where</b> it sits, not by how it is spelled.',
    ar: 'سطر input واحد ممكن يكون فيه كلمة <code>email</code> خمس مرات، وكل مرة معناها حاجة. <code>type="email"</code> بتاعة المتصفح، والـ attribute <code>email</code> لوحده ده الـ validator بتاع أنجولار، و<code>"ngModel"</code> بتاعة أنجولار، أما <code>#email</code> و<code>model.email</code> فبتوعك. اقرا كل كلمة من <b>مكانها</b>، مش من طريقة كتابتها.'
  },
  names: [
    { n:'ngForm', k:'ng',
      w:{ en:'Angular’s export name for the form. <code>#f="ngForm"</code> means: put Angular’s form object in <code>f</code>.',
          ar:'اسم الـ export بتاع أنجولار للفورم. <code>#f="ngForm"</code> معناها: حط الفورم object بتاع أنجولار في <code>f</code>.' } },
    { n:'ngSubmit', k:'ng', w:{ en:'Angular’s form event. Unlike the browser’s <code>submit</code>, it never reloads the page.', ar:'الـ event بتاع أنجولار للفورم. عكس <code>submit</code> بتاع المتصفح، عمره ما بيعمل reload للصفحة.' } },
    { n:'ngModel', k:'ng',
      w:{ en:'Angular’s two-way binding directive, and also its export name in <code>#name="ngModel"</code>.',
          ar:'الـ directive بتاع أنجولار للربط في الاتجاهين، وهو كمان اسم الـ export في <code>#name="ngModel"</code>.' } },
    { n:'name', k:'ng', only:['html'], re:'(?<![\\w$.#"-])name(?==)',
      w:{ en:'The HTML <code>name</code> attribute. <code>ngModel</code> inside a form refuses to work without it. Its value is yours: it becomes the key in <code>f.value</code>.',
          ar:'الـ attribute <code>name</code> بتاع HTML. <code>ngModel</code> جوه فورم مش بيشتغل من غيره. قيمته بتاعتك: بتبقى المفتاح في <code>f.value</code>.' } },
    { n:'required', k:'ng',
      w:{ en:'Angular’s validator attribute, and the fixed error key <code>errors[\'required\']</code> it sets.',
          ar:'الـ attribute بتاع الـ validator في أنجولار، ومفتاح الـ error الثابت <code>errors[\'required\']</code> اللي بيحطه.' } },
    { n:'minlength', k:'ng', w:{ en:'A validator attribute, and its fixed error key.', ar:'attribute بتاع validator، ومفتاح الـ error الثابت بتاعه.' } },
    { n:'email', k:'ng', only:['html'], re:'(?<=required )email(?![\\w$=-])',
      w:{ en:'This bare <code>email</code> attribute is Angular’s email validator. The other <code>email</code>s on the line are the browser’s (<code>type</code>) or yours.',
          ar:'الـ attribute <code>email</code> لوحده ده الـ validator بتاع أنجولار للإيميل. باقي الـ <code>email</code> اللي في السطر يا إما بتوع المتصفح (<code>type</code>) يا إما بتوعك.' } },
    { n:'touched', k:'ng', w:{ en:'A state every control has: true once it was focused and left.', ar:'حالة موجودة في أي control: بتبقى true لما يتعمله focus ويتساب.' } },
    { n:'invalid', k:'ng', re:'(?<![\\w$-])(?<!<!--.*)invalid(?![\\w$-])', w:{ en:'A state every control and form has.', ar:'حالة موجودة في أي control وأي فورم.' } },
    { n:'errors', k:'ng', re:'(?<![\\w$-])(?<!<!--.*)errors(?![\\w$-])', w:{ en:'The object where each failing validator puts its fixed key.', ar:'الـ object اللي كل validator فاشل بيحط فيه مفتاحه الثابت.' } },
    { n:'FormsModule', k:'ng', w:{ en:'Angular’s module that brings <code>ngModel</code> and <code>ngForm</code>.', ar:'الـ module بتاع أنجولار اللي بيجيب <code>ngModel</code> و<code>ngForm</code>.' } },
    { n:'NgForm', k:'ng', w:{ en:'Angular’s type for the form object.', ar:'الـ type بتاع أنجولار للفورم object.' } },
    { n:'markAllAsTouched', k:'ng', w:{ en:'A form method from Angular.', ar:'ميثود في الفورم من أنجولار.' } },
    { n:'resetForm', k:'ng', w:{ en:'An <code>NgForm</code> method from Angular.', ar:'ميثود في <code>NgForm</code> من أنجولار.' } },
    { n:'disabled', k:'ng', w:{ en:'The button’s own DOM property.', ar:'الـ property بتاعة الزرار نفسه في الـ DOM.' } },
    { n:'ng-valid', k:'ng', w:{ en:'A CSS class Angular adds by itself. You style it; you do not name it.', ar:'CSS class أنجولار بيحطها لوحده. انت بتستايلها؛ مش بتسمّيها.' } },
    { n:'ng-invalid', k:'ng', w:{ en:'A CSS class Angular adds by itself.', ar:'CSS class أنجولار بيحطها لوحده.' } },
    { n:'ng-touched', k:'ng', w:{ en:'A CSS class Angular adds by itself.', ar:'CSS class أنجولار بيحطها لوحده.' } },
    { n:'ng-untouched', k:'ng', w:{ en:'A CSS class Angular adds by itself.', ar:'CSS class أنجولار بيحطها لوحده.' } },
    { n:'ng-dirty', k:'ng', w:{ en:'A CSS class Angular adds by itself.', ar:'CSS class أنجولار بيحطها لوحده.' } },
    { n:'ng-pristine', k:'ng', w:{ en:'A CSS class Angular adds by itself.', ar:'CSS class أنجولار بيحطها لوحده.' } },

    { n:'f', k:'mine',
      w:{ en:'The template reference to the form. It is passed into <code>send(f)</code>, whose parameter happens to be called <code>f</code> too.',
          ar:'الـ template reference للفورم. بيتبعت لـ <code>send(f)</code>، والـ parameter بتاعها صدفة اسمه <code>f</code> برضه.' } },
    { n:'#name', k:'mine', only:['tpl'], re:'(?<=#)name(?![\\w$-])|(?<![\\w$.#"-])name(?=\\.\\w)',
      w:{ en:'A template reference to that field’s <code>ngModel</code>, so you can ask <code>name.touched</code>. Rename <code>#name</code> and every <code>name.</code> after it.',
          ar:'template reference للـ <code>ngModel</code> بتاع الخانة دي، عشان تسأل <code>name.touched</code>. غيّر <code>#name</code> وكل <code>name.</code> بعده.' } },
    { n:'#email', k:'mine', only:['tpl'], re:'(?<=#)email(?![\\w$-])|(?<![\\w$.#"-])email(?=\\.\\w)',
      w:{ en:'A template reference to the email field’s <code>ngModel</code>. Only this one and its <code>email.</code> uses are yours to rename freely.',
          ar:'template reference للـ <code>ngModel</code> بتاع خانة الإيميل. ده والـ <code>email.</code> اللي بعده بس اللي تغيّرهم براحتك.' } },
    { n:'model', k:'mine',
      w:{ en:'Your plain object and its fields, bound with <code>[(ngModel)]="model.name"</code>. Rename it in the class and the template.',
          ar:'الـ object العادي بتاعك والـ fields بتاعته، مربوط بـ <code>[(ngModel)]="model.name"</code>. غيّره في الكلاس والتمبلت.' } },
    { n:'send', k:'mine', w:{ en:'The component’s method, called from its own template.', ar:'ميثود الـ component، بتتنادى من التمبلت بتاعه.' } },
    { n:'sending', k:'mine', w:{ en:'The component’s own signal.', ar:'الـ signal بتاعة الـ component نفسه.' } },
    { n:'app-contact', k:'pub', w:{ en:'The selector: the tag a parent types.', ar:'الـ selector: التاج اللي الأب بيكتبه.' } },
    { n:'err', k:'pub', w:{ en:'Your CSS class name. The stylesheet must use the same name.', ar:'اسم الـ CSS class بتاعك. الـ stylesheet لازم يستخدم نفس الاسم.' } },
    { n:'signIn', k:'mine', re:'(?<![\\w$.-])signIn(?![\\w$-])',
      w:{ en:'The login component’s method. <code>auth.signIn</code> is the auth service’s method and is left alone.',
          ar:'ميثود الـ login component. و<code>auth.signIn</code> ميثود الـ auth service ومش بتتلمس.' } },
    { n:'busy', k:'mine', w:{ en:'The login component’s own signal.', ar:'الـ signal بتاعة الـ login component نفسه.' } },
    { n:'error', k:'mine', w:{ en:'The login component’s own signal.', ar:'الـ signal بتاعة الـ login component نفسه.' } },
  ]
}
};
