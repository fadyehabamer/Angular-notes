/* names for the directives & custom pipes topic. Format: see inputs-outputs.mjs. */
export default {
'directives-and-pipes': {
  note: {
    en: 'A directive’s selector and a pipe’s <code>name</code> are words <b>you</b> invent, but every template that uses them types them too, so they are <b>orange</b>. The event names in <code>host</code> (<code>mouseenter</code>, <code>click</code>) and the method name <code>transform</code> are fixed.',
    ar: 'الـ selector بتاع الـ directive والـ <code>name</code> بتاع الـ pipe كلمات <b>انت</b> اللي بتخترعها، بس أي تمبلت بيستخدمهم بيكتبهم برضه، فهما <b>برتقاني</b>. أسماء الـ events في <code>host</code> (<code>mouseenter</code>، <code>click</code>) واسم الميثود <code>transform</code> ثابتين.'
  },
  names: [
    { n:'@Directive', k:'ng', w:{ en:'Angular’s decorator for a directive: a component with no template.', ar:'الـ decorator بتاع أنجولار للـ directive: component من غير تمبلت.' } },
    { n:'selector', k:'ng', w:{ en:'An option key Angular reads. The string you give it is yours.', ar:'مفتاح إعداد أنجولار بيقراه. النص اللي بتديهوله بتاعك.' } },
    { n:'input', k:'ng', re:'(?<![\\w$<(-])input(?=[.(<,])',
      w:{ en:'Angular’s function that creates an input. The HTML <code>&lt;input&gt;</code> tag is a different thing.',
          ar:'الـ function بتاعة أنجولار اللي بتعمل input. وتاج <code>&lt;input&gt;</code> بتاع HTML حاجة تانية.' } },
    { n:'required', k:'ng', w:{ en:'Part of Angular’s API: <code>input.required</code>.', ar:'جزء من API أنجولار: <code>input.required</code>.' } },
    { n:'alias', k:'ng', w:{ en:'An option key Angular reads. The value you give it is the public name.', ar:'مفتاح إعداد أنجولار بيقراه. القيمة اللي بتديهاله هي الاسم الـ public.' } },
    { n:'inject', k:'ng', w:{ en:'Angular’s function that hands you a service.', ar:'الـ function بتاعة أنجولار اللي بتديك service.' } },
    { n:'ElementRef', k:'ng', w:{ en:'Angular’s wrapper around the element the directive sits on.', ar:'الغلاف بتاع أنجولار حوالين العنصر اللي الـ directive قاعد عليه.' } },
    { n:'afterNextRender', k:'ng', w:{ en:'Angular’s function: run once, in the browser, after the element is on the page.', ar:'الـ function بتاعة أنجولار: شغّل مرة واحدة، في المتصفح، بعد ما العنصر يبقى في الصفحة.' } },
    { n:'nativeElement', k:'ng', w:{ en:'The property on <code>ElementRef</code> that holds the real DOM element.', ar:'الـ property في <code>ElementRef</code> اللي شايلة عنصر الـ DOM الحقيقي.' } },
    { n:'focus', k:'ng', w:{ en:'The DOM’s own method on every element.', ar:'الميثود بتاعة الـ DOM نفسه في أي عنصر.' } },
    { n:'host', k:'ng', w:{ en:'An option key Angular reads: bindings and listeners on the element itself.', ar:'مفتاح إعداد أنجولار بيقراه: bindings وlisteners على العنصر نفسه.' } },
    { n:'attr', k:'ng', w:{ en:'Angular’s prefix for binding an HTML attribute: <code>[attr.x]</code>.', ar:'البادئة بتاعة أنجولار لربط attribute في HTML: <code>[attr.x]</code>.' } },
    { n:'class', k:'ng', re:'(?<=\\[)class(?=\\.)',
      w:{ en:'Angular’s prefix for toggling one CSS class: <code>[class.x]</code>. The class name after the dot is yours.',
          ar:'البادئة بتاعة أنجولار لتشغيل وقفل CSS class واحدة: <code>[class.x]</code>. اسم الـ class بعد النقطة بتاعك.' } },
    { n:'mouseenter', k:'ng', w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'mouseleave', k:'ng', w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'focusin', k:'ng', w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'focusout', k:'ng', w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'click', k:'ng', w:{ en:'The browser’s own event name.', ar:'اسم الـ event بتاع المتصفح نفسه.' } },
    { n:'$event', k:'ng', w:{ en:'Angular’s fixed name for the event object inside a listener.', ar:'اسم ثابت من أنجولار لـ object الـ event جوه الـ listener.' } },
    { n:'@Pipe', k:'ng', w:{ en:'Angular’s decorator for a pipe.', ar:'الـ decorator بتاع أنجولار للـ pipe.' } },
    { n:'name', k:'ng', w:{ en:'An option key Angular reads. The string after it is yours.', ar:'مفتاح إعداد أنجولار بيقراه. النص اللي بعده بتاعك.' } },
    { n:'PipeTransform', k:'ng', w:{ en:'Angular’s interface. It makes TypeScript check you wrote <code>transform</code>.', ar:'الـ interface بتاع أنجولار. بيخلّي TypeScript يتأكد إنك كتبت <code>transform</code>.' } },
    { n:'transform', k:'ng', w:{ en:'The method name Angular calls on every pipe. It cannot be renamed.', ar:'اسم الميثود اللي أنجولار بيناديها في أي pipe. مينفعش تتغير.' } },
    { n:'imports', k:'ng', w:{ en:'An option key Angular reads: what this template may use.', ar:'مفتاح إعداد أنجولار بيقراه: التمبلت ده مسموحله يستخدم إيه.' } },

    { n:'appAutofocus', k:'pub',
      w:{ en:'The attribute you invented. The selector, the alias and every template that uses it must spell it the same.',
          ar:'الـ attribute اللي انت اخترعته. الـ selector والـ alias وأي تمبلت بيستخدمه لازم يكتبوه زي بعض.' } },
    { n:'Autofocus', k:'pub', w:{ en:'The directive’s class name, listed in the feed’s <code>imports</code>.', ar:'اسم كلاس الـ directive، مكتوب في <code>imports</code> بتاع الـ feed.' } },
    { n:'delay', k:'mine',
      w:{ en:'The property behind the alias. Outside, it is called <code>appAutofocus</code>, so this name is private.',
          ar:'الـ property اللي ورا الـ alias. برّه اسمه <code>appAutofocus</code>، فالاسم ده خاص.' } },
    { n:'appTooltip', k:'pub',
      w:{ en:'The selector and the input share this name, so one attribute both applies the directive and passes the text.',
          ar:'الـ selector والـ input ليهم نفس الاسم، فـ attribute واحد بيحط الـ directive وبيبعت النص كمان.' } },
    { n:'Tooltip', k:'pub', w:{ en:'The directive’s class name, listed in <code>imports</code>.', ar:'اسم كلاس الـ directive، مكتوب في <code>imports</code>.' } },
    { n:'id', k:'mine', only:['dir2'],
      w:{ en:'The directive’s own field. The string <code>\'id\'</code> in <code>host</code> is an expression that reads it, so both change together.',
          ar:'الـ field بتاع الـ directive نفسه. النص <code>\'id\'</code> في <code>host</code> expression بيقراه، فالاتنين بيتغيروا مع بعض.' } },
    { n:'show', k:'mine', w:{ en:'The directive’s own method, called from its <code>host</code> strings.', ar:'ميثود الـ directive نفسه، بتتنادى من نصوص الـ <code>host</code>.' } },
    { n:'hide', k:'mine', w:{ en:'The directive’s own method, called from its <code>host</code> strings.', ar:'ميثود الـ directive نفسه، بتتنادى من نصوص الـ <code>host</code>.' } },
    { n:'timeAgo', k:'pub',
      w:{ en:'The pipe’s name. The <code>name:</code> string and every <code>| timeAgo</code> in templates must match.',
          ar:'اسم الـ pipe. النص في <code>name:</code> وكل <code>| timeAgo</code> في التمبلتس لازم يطابقوا بعض.' } },
    { n:'TimeAgo', k:'pub', w:{ en:'The pipe’s class name, listed in <code>imports</code>. Templates never type it.', ar:'اسم كلاس الـ pipe، مكتوب في <code>imports</code>. التمبلتس عمرها ما بتكتبه.' } },
    { n:'value', k:'mine', only:['pipe'],
      w:{ en:'The first parameter of <code>transform</code>: whatever sits left of <code>|</code>. Its name is yours.',
          ar:'أول parameter في <code>transform</code>: اللي على شمال الـ <code>|</code>. اسمه بتاعك.' } },
    { n:'posts', k:'mine', w:{ en:'The feed’s own signal, read by its own template.', ar:'الـ signal بتاعة الـ feed، والتمبلت بتاعه بيقراها.' } },
    { n:'appBusy', k:'pub', w:{ en:'The selector and the input share this name; the checkout template types it.', ar:'الـ selector والـ input ليهم نفس الاسم؛ وتمبلت الـ checkout بيكتبه.' } },
    { n:'busy', k:'mine', w:{ en:'The directive’s own signal, read by its <code>host</code> strings.', ar:'الـ signal بتاعة الـ directive، ونصوص الـ <code>host</code> بتقراها.' } },
    { n:'disabledInput', k:'mine',
      w:{ en:'The property behind <code>alias: \'disabled\'</code>. Outside it is called <code>disabled</code>, so this name is private.',
          ar:'الـ property اللي ورا <code>alias: \'disabled\'</code>. برّه اسمه <code>disabled</code>، فالاسم ده خاص.' } },
    { n:'run', k:'mine', w:{ en:'The directive’s own method, wired to <code>(click)</code> in <code>host</code>.', ar:'ميثود الـ directive، متوصلة بـ <code>(click)</code> في <code>host</code>.' } },
  ]
}
};
