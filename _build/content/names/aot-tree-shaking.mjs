/* names for the AOT compilation and tree shaking topic.
   n: the name exactly as typed in the code.   k: ng | mine | pub.
   w: what it is, and what has to change with it.   { en, ar }
   re / only / as: see _build/names.mjs.                                  */
export default {
'aot-tree-shaking': {
  note: {
    en: 'In the compiler’s output, every <code>ɵɵ…</code> word is Angular’s private runtime; you never type them. The only names that survive from <b>your</b> code are the ones you wrote in the template, like <code>title</code> and <code>isOpen</code>. Tree shaking does not care about names at all, only about who imports what.',
    ar: 'في الكود اللي الكومبايلر بيطلّعه، أي كلمة <code>ɵɵ…</code> دي الـ runtime الخاص بأنجولار؛ عمرك ما هتكتبها. والأسماء الوحيدة اللي بتفضل من <b>كودك</b> هي اللي كتبتها في التمبلت، زي <code>title</code> و<code>isOpen</code>. والـ tree shaking مش فارق معاه الأسماء خالص، فارق معاه مين بيعمل import لمين.'
  },
  names: [
    { n:'Card_Template', k:'ng',
      w:{ en:'Generated by the compiler from your class name <code>Card</code>. You never type it; rename the class and it follows by itself.',
          ar:'الكومبايلر بيعمله من اسم الكلاس بتاعك <code>Card</code>. عمرك ما هتكتبه؛ لو غيّرت اسم الكلاس هيتغير لوحده.' } },
    { n:'rf', k:'ng',
      w:{ en:'The compiler’s parameter: render flags. <code>1</code> means create, <code>2</code> means update.',
          ar:'parameter من الكومبايلر: render flags. <code>1</code> يعني create و<code>2</code> يعني update.' } },
    { n:'ctx', k:'ng',
      w:{ en:'The compiler’s parameter: your component instance. Everything after <code>ctx.</code> is a name from your class.',
          ar:'parameter من الكومبايلر: الـ instance بتاع الـ component بتاعك. أي حاجة بعد <code>ctx.</code> اسم من الكلاس بتاعك.' } },
    { n:'ɵɵelementStart', k:'ng', w:{ en:'An Angular runtime instruction: open an element.', ar:'instruction من الـ runtime بتاع أنجولار: افتح element.' } },
    { n:'ɵɵelementEnd', k:'ng', w:{ en:'An Angular runtime instruction: close the element.', ar:'instruction من الـ runtime بتاع أنجولار: اقفل الـ element.' } },
    { n:'ɵɵtext', k:'ng', w:{ en:'An Angular runtime instruction: create a text node.', ar:'instruction من الـ runtime بتاع أنجولار: اعمل text node.' } },
    { n:'ɵɵtextInterpolate', k:'ng', w:{ en:'An Angular runtime instruction: update the text if the value changed. This is <code>{{ }}</code> after compiling.', ar:'instruction من الـ runtime بتاع أنجولار: حدّث النص لو القيمة اتغيرت. ده <code>{{ }}</code> بعد الكومبايل.' } },
    { n:'ɵɵconditionalCreate', k:'ng', w:{ en:'An Angular runtime instruction: prepare an <code>@if</code> block.', ar:'instruction من الـ runtime بتاع أنجولار: جهّز بلوك <code>@if</code>.' } },
    { n:'ɵɵconditional', k:'ng', w:{ en:'An Angular runtime instruction: show or hide the <code>@if</code> branch.', ar:'instruction من الـ runtime بتاع أنجولار: اظهر أو اخفي فرع الـ <code>@if</code>.' } },
    { n:'title', k:'mine',
      w:{ en:'Your component’s signal. The template reads it, and the compiler copies the name as <code>ctx.title()</code>.',
          ar:'الـ signal بتاعة الـ component بتاعك. التمبلت بيقراها، والكومبايلر بينقل الاسم زي ما هو <code>ctx.title()</code>.' } },
    { n:'isOpen', k:'mine', w:{ en:'Your component’s signal, copied into the compiled code as-is.', ar:'الـ signal بتاعة الـ component بتاعك، بتتنقل للكود المتكومبايل زي ما هي.' } },

    { n:'@Injectable', k:'ng', w:{ en:'Angular’s decorator for a service.', ar:'الـ decorator بتاع أنجولار للـ service.' } },
    { n:'providedIn', k:'ng', w:{ en:'An option key Angular reads.', ar:'مفتاح إعداد أنجولار بيقراه.' } },
    { n:'root', k:'ng', only:['ts'],
      w:{ en:'Angular’s value for “one copy for the whole app”. It is also what makes the service tree-shakable.',
          ar:'القيمة بتاعة أنجولار لـ “نسخة واحدة للتطبيق كله”. وهي كمان اللي بتخلّي الـ service ينفع يتشال بالـ tree shaking.' } },
    { n:'providers', k:'ng', only:['ts'], w:{ en:'An option key Angular reads. Listing a class here keeps it in the bundle.', ar:'مفتاح إعداد أنجولار بيقراه. لو كتبت كلاس هنا بيفضل في الـ bundle.' } },
    { n:'ReportService', k:'pub',
      w:{ en:'Your service class. Every file that injects or lists it names it, which is exactly what keeps it in the bundle.',
          ar:'كلاس الـ service بتاعك. أي ملف بيعمله inject أو بيكتبه بيذكر اسمه، وده بالظبط اللي بيخلّيه يفضل في الـ bundle.' } },
    { n:'Button', k:'pub', w:{ en:'Your component class, exported from <code>ui/button</code> and imported by name.', ar:'كلاس الـ component بتاعك، متعمله export من <code>ui/button</code> وبيتعمله import بالاسم.' } },
    { n:'unusedHelper', k:'mine', w:{ en:'Your function. Nothing imports it, so the bundler deletes it.', ar:'الـ function بتاعتك. محدش بيعملها import، فالـ bundler بيمسحها.' } },
    { n:'console.log', k:'ng', w:{ en:'The browser’s console. A top-level call is a side effect.', ar:'الكونسول بتاع المتصفح. لو اتنادى في أول الملف يبقى side effect.' } },
    { n:'window', k:'ng', only:['ts'], w:{ en:'The browser’s global object.', ar:'الأوبجكت العام بتاع المتصفح.' } },
    { n:'myGlobal', k:'mine', w:{ en:'A name you made up, stuck on <code>window</code>. That is the side effect.', ar:'اسم انت اخترعته وحطيته على <code>window</code>. وده الـ side effect.' } },
    { n:'angularCompilerOptions', k:'ng', w:{ en:'The Angular compiler’s section in <code>tsconfig.json</code>.', ar:'القسم بتاع كومبايلر أنجولار في <code>tsconfig.json</code>.' } },
    { n:'strictTemplates', k:'ng', only:['json'], w:{ en:'An Angular compiler option: type-check templates.', ar:'إعداد في كومبايلر أنجولار: اعمل type-check للتمبلتس.' } },
    { n:'strictInjectionParameters', k:'ng', w:{ en:'An Angular compiler option.', ar:'إعداد في كومبايلر أنجولار.' } },
    { n:'sideEffects', k:'ng', only:['json'],
      w:{ en:'A <code>package.json</code> key that bundlers read: “importing my files does nothing by itself”.',
          ar:'مفتاح في <code>package.json</code> الـ bundlers بتقراه: “الملفات بتاعتي مابتعملش حاجة لمجرد إنها اتعملها import”.' } },
  ]
}
};
