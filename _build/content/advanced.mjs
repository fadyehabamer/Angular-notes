export default {
id:'advanced', ink:'--adv',
kicker:{en:'Track three', ar:'المستوى التالت'},
name:{en:'Advanced', ar:'متقدم'},
dek:{
  en:`The things you reach for when the app is already working and now has to be fast, server-rendered, and composable. Removing zone.js, deferring what nobody looked at, hydrating in pieces, meeting RxJS halfway, and bending dependency injection to your shape.`,
  ar:`الحاجات اللي بتلجأ لها لما التطبيق يبقى شغال خلاص، ودلوقتي لازم يبقى سريع، ومترندر من السيرفر، وقابل للتركيب. تشيل zone.js، وتأجّل اللي محدش بصّ عليه، وتعمل hydration على أجزاء، وتقابل RxJS في النص، وتلوي الـ dependency injection على مقاسك.`
},
arts:[

/* 11 ------------------------------------------------------------------ */
{
  slug:'11-zoneless', num:'11',
  badge:{en:'No zone.js', ar:'من غير zone.js'},
  title:{en:'Zoneless: deleting the thing that guessed for you', ar:'Zoneless: تشيل الحاجة اللي كانت بتخمّن بدالك'},
  lead:{
    en:`<code>zone.js</code> monkey-patched every async browser API so it could tell Angular “something might have happened”. Signals removed the need to guess. Going zoneless drops about <b>13 kB</b> of polyfill and replaces guesswork with notification — but it is only safe once your state genuinely lives in signals.`,
    ar:`<code>zone.js</code> كان بيلبّس نفسه على كل async API في البراوزر عشان يقول لأنجولار “يمكن حصلت حاجة”. الـ signals شالت الحاجة للتخمين. وإنك تشتغل zoneless بيشيل حوالي <b>13 كيلوبايت</b> polyfill ويستبدل التخمين بالتبليغ — بس ده مأمون بس لما حالتك تكون فعلاً عايشة في signals.`
  },
  nodes:[
    [{id:'boot',role:{en:'removes the zone',ar:'بيشيل الـ zone'},file:'app.config',ext:'.ts',kind:'cfg'},
     {id:'poly',role:{en:'drops the polyfill',ar:'بيشيل الـ polyfill'},file:'angular',ext:'.json',kind:'cfg'}],
    [{id:'ok',role:{en:'notifies correctly',ar:'بيبلّغ صح'},file:'clock.component',ext:'.ts',kind:'ts'},
     {id:'broke',role:{en:'stops updating',ar:'بيبطّل يتحدث'},file:'legacy.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'boot',to:'poly',a:'right',b:'left',label:'polyfills: []',packet:'−13 kB'},
    {id:'e2',from:'ok',to:'boot',a:'top',b:'bottom',label:'signal.set() → schedule',packet:'render'},
    {id:'e3',from:'broke',to:'boot',a:'top',b:'right',label:'plain field — silence',packet:'nothing'}
  ],
  files:[
    {id:'boot',name:'app.config.ts',lang:'ts',tag:{en:'one provider',ar:'provider واحد'},code:[
      "import { provideZonelessChangeDetection } from '@angular/core';",
      "",
      "export const appConfig: ApplicationConfig = {",
      "  providers: [",
      "    provideZonelessChangeDetection(),",
      "    // v18–v19 called this provideExperimentalZonelessChangeDetection()",
      "    provideRouter(routes),",
      "    provideHttpClient(withFetch()),",
      "  ],",
      "};"]},
    {id:'poly',name:'angular.json',lang:'json',tag:{en:'delete the line',ar:'امسح السطر'},code:[
      "\"architect\": {",
      "  \"build\": {",
      "    \"options\": {",
      "      \"polyfills\": [\"zone.js\"]      ← remove this entry",
      "    }",
      "  }",
      "}"]},
    {id:'ok',name:'clock.component.ts',lang:'ts',tag:{en:'works zoneless',ar:'شغال zoneless'},code:[
      "export class Clock {",
      "  readonly now = signal(new Date());",
      "",
      "  constructor() {",
      "    const id = setInterval(() => this.now.set(new Date()), 1000);",
      "    inject(DestroyRef).onDestroy(() => clearInterval(id));",
      "  }",
      "",
      "  // the signal write is the notification. zone.js is not",
      "  // involved and was never needed here.",
      "}"]},
    {id:'broke',name:'legacy.component.ts',lang:'ts',tag:{en:'breaks silently',ar:'بيكسر في صمت'},code:[
      "export class Legacy {",
      "  now = new Date();          // a plain field: nothing notifies",
      "",
      "  constructor() {",
      "    setInterval(() => {",
      "      this.now = new Date();   // with zone.js this repainted.",
      "    }, 1000);                  // zoneless, the screen freezes.",
      "  }",
      "",
      "  // this is the whole migration risk in one component.",
      "}"]},
    {id:'test',name:'finding them',lang:'bash',tag:{en:'the safe order',ar:'الترتيب الآمن'},code:[
      "# 1. turn OnPush on everywhere first and fix what breaks",
      "# 2. move remaining mutable state into signals",
      "# 3. only then flip the provider",
      "",
      "# a useful intermediate step — same scheduling rules,",
      "# but zone.js is still loaded as a safety net:",
      "provideZoneChangeDetection({ eventCoalescing: true })"]}
  ],
  steps:[
    {t:{en:'One provider replaces the zone. Angular now schedules a check when a signal is written, when a template event fires, or when <code class="i">markForCheck()</code> is called — and at no other time.',
        ar:'provider واحد بيستبدل الـ zone. وأنجولار بقى يجدول فحص لما signal تتكتب، أو حدث في تمبلت يشتغل، أو <code class="i">markForCheck()</code> تتنادى — ومفيش وقت تاني.'},
     hl:{boot:[5]}},
    {t:{en:'Then the polyfill comes out of the build. This is where the 13 kB and the patched-every-API startup cost disappear.',
        ar:'وبعدين الـ polyfill بيطلع من الـ build. وهنا الـ 13 كيلوبايت وتكلفة تلبيس كل API بتختفي.'},
     edge:'e1',hl:{poly:[4]}},
    {t:{en:'A component whose state is a signal is unaffected. The <code class="i">setInterval</code> still fires; the difference is that <code class="i">now.set()</code> is what tells Angular, not the timer.',
        ar:'الـ component اللي حالته signal مش هيتأثر. الـ <code class="i">setInterval</code> لسه بيشتغل؛ الفرق إن <code class="i">now.set()</code> هي اللي بتبلّغ أنجولار، مش التايمر.'},
     edge:'e2',hl:{ok:[2,5]}},
    {t:{en:'A component holding a plain mutable field is where it breaks — and it breaks <b>silently</b>. No error, no warning, just a value that never reaches the screen again.',
        ar:'الـ component اللي ماسك field عادي قابل للتعديل هو اللي بيكسر — و<b>في صمت</b>. مفيش ايرور ولا تحذير، مجرد قيمة عمرها ما هتوصل الشاشة تاني.'},
     edge:'e3',hl:{broke:[2,6,7]}},
    {t:{en:'So migrate in order: OnPush everywhere, then state into signals, then flip the provider last. <code class="i">eventCoalescing</code> is a useful halfway house with the safety net still in place.',
        ar:'فالتحويل بترتيب: OnPush في كل حتة، وبعدين الحالة في signals، وبعدين تقلب الـ provider في الآخر. و<code class="i">eventCoalescing</code> محطة وسط مفيدة والشبكة الأمان لسه موجودة.'},
     hl:{test:[1,2,3,7]}}
  ],
  example:{
    name:{en:'A map SDK that used to need NgZone.run', ar:'مكتبة خرايط كانت محتاجة NgZone.run'},
    what:{
      en:'Third-party libraries fire their callbacks outside Angular. The old fix was to wrap every callback in NgZone.run so the zone would notice. Write a signal instead and the problem stops existing — the same code is correct with the zone and without it.',
      ar:'المكتبات الخارجية بتنادي الـ callbacks بتاعتها بره أنجولار. الحل القديم كان تلف كل callback في NgZone.run عشان الـ zone ياخد باله. اكتب signal بدل كده والمشكلة تختفي — نفس الكود صح مع الـ zone ومن غيره.'},
    files:[
      {id:'m1',name:'the old way',lang:'ts',tag:{en:'zone-dependent',ar:'معتمد على الـ zone'},code:[
        "constructor(private zone: NgZone) {",
        "  map.on('moveend', () => {",
        "    this.zone.run(() => {          // without this the view froze",
        "      this.center = map.getCenter();",
        "    });",
        "  });",
        "}"]},
      {id:'m2',name:'the way that works either way',lang:'ts',tag:{en:'zone-agnostic',ar:'مش فارق معاه'},code:[
        "readonly center = signal<LatLng | null>(null);",
        "",
        "constructor() {",
        "  const map = new MapSdk('#map');",
        "  map.on('moveend', () => this.center.set(map.getCenter()));",
        "",
        "  inject(DestroyRef).onDestroy(() => map.destroy());",
        "}",
        "",
        "// no NgZone injected, no run() wrapper, and it is correct",
        "// under zone.js, under zoneless, and under SSR."]}
    ]
  },
  gotchas:[
    {en:'Zoneless failures are <b>silent</b>. There is no error — a value simply stops reaching the screen. Audit for plain mutable fields before you flip the switch.',
     ar:'أعطال الـ zoneless <b>صامتة</b>. مفيش ايرور — القيمة بتبطل توصل الشاشة وخلاص. راجع الـ fields العادية القابلة للتعديل قبل ما تقلب السويتش.'},
    {en:'Check your dependencies. A UI library that internally relies on zone patching will misbehave; most modern ones no longer do.',
     ar:'راجع المكتبات اللي بتستخدمها. أي UI library بتعتمد جوّاها على تلبيس الـ zone هتغلط؛ ومعظم الحديث منها مبقاش بيعمل كده.'},
    {en:'In tests, <code class="i">fakeAsync</code> and <code class="i">tick()</code> depend on zone.js. Zoneless tests use <code class="i">await fixture.whenStable()</code> instead.',
     ar:'في التستات، <code class="i">fakeAsync</code> و<code class="i">tick()</code> بيعتمدوا على zone.js. تستات الـ zoneless بتستعمل <code class="i">await fixture.whenStable()</code> بدالهم.'},
    {en:'<code class="i">NgZone.run</code> and <code class="i">runOutsideAngular</code> still exist and are harmless, but they become no-ops. Deleting them is cleanup, not a fix.',
     ar:'<code class="i">NgZone.run</code> و<code class="i">runOutsideAngular</code> لسه موجودين ومش ضارين، بس بيبقوا من غير أثر. ومسحهم تنضيف، مش إصلاح.'},
    {en:'You get most of the benefit before you remove the zone: OnPush plus signals already gives targeted invalidation. Zoneless is the last step, not the first.',
     ar:'إنت بتاخد معظم الفايدة قبل ما تشيل الـ zone: OnPush مع الـ signals بيدوك targeted invalidation أصلاً. الـ zoneless آخر خطوة مش أولها.'}
  ]
},

/* 12 ------------------------------------------------------------------ */
{
  slug:'12-defer', num:'12',
  badge:{en:'Loading later', ar:'التحميل بعدين'},
  title:{en:'<code>@defer</code>: shipping less on first paint', ar:'<code>@defer</code>: تشحن أقل في أول رسمة'},
  lead:{
    en:`Everything inside a <code>@defer</code> block — the component, its imports, its transitive dependencies — is cut into a separate chunk at build time and fetched when a trigger fires. It is <code>loadComponent</code> for a piece of a template, and it is the fastest way to shrink a first paint.`,
    ar:`أي حاجة جوه بلوك <code>@defer</code> — الـ component والـ imports بتوعه وكل اللي متعلق بيهم — بتتقص في chunk منفصل وقت الـ build وبتتجاب لما المشغّل يشتغل. ده <code>loadComponent</code> بس لجزء من تمبلت، وهو أسرع طريقة تصغّر بيها أول رسمة.`
  },
  nodes:[
    [{id:'tpl',role:{en:'declares the block',ar:'بيعلن البلوك'},file:'product-page.component',ext:'.html',kind:'html'}],
    [{id:'chunk',role:{en:'its own chunk',ar:'chunk لوحده'},file:'reviews.component',ext:'.ts',kind:'ts'},
     {id:'ph',role:{en:'shown meanwhile',ar:'بيتعرض في الوقت ده'},file:'placeholder',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'tpl',to:'ph',a:'bottom',b:'top',label:'@placeholder',packet:'skeleton'},
    {id:'e2',from:'ph',to:'chunk',a:'right',b:'left',label:'on viewport',packet:'fetch'},
    {id:'e3',from:'chunk',to:'tpl',a:'top',b:'right',label:'swap in',packet:'<app-reviews>'}
  ],
  files:[
    {id:'tpl',name:'product-page.component.html',lang:'html',tag:{en:'the block',ar:'البلوك'},code:[
      "<app-gallery [images]=\"product().images\" />",
      "<app-buy-box [product]=\"product()\" />",
      "",
      "@defer (on viewport; prefetch on idle) {",
      "  <app-reviews [productId]=\"product().id\" />",
      "} @placeholder (minimum 400ms) {",
      "  <div class=\"skeleton\" style=\"height: 320px\"></div>",
      "} @loading (after 100ms; minimum 500ms) {",
      "  <app-spinner />",
      "} @error {",
      "  <p>Reviews are unavailable right now.</p>",
      "}"]},
    {id:'trig',name:'the triggers',lang:'html',tag:{en:'pick one',ar:'اختار واحد'},code:[
      "@defer (on idle)                    <!-- default: when the browser is free -->",
      "@defer (on viewport)                <!-- when the placeholder scrolls into view -->",
      "@defer (on interaction)             <!-- first click or keydown on the placeholder -->",
      "@defer (on hover)                   <!-- pointer enters the placeholder -->",
      "@defer (on timer(3s))               <!-- after a delay -->",
      "@defer (on immediate)               <!-- right after the page renders -->",
      "@defer (when isAdmin())             <!-- any boolean expression, once true -->",
      "",
      "<!-- combine: fetch early, render late -->",
      "@defer (on interaction; prefetch on hover) { … }",
      "",
      "<!-- trigger from another element by reference -->",
      "<button #more>Show reviews</button>",
      "@defer (on interaction(more)) { <app-reviews /> }"]},
    {id:'chunk',name:'reviews.component.ts',lang:'ts',tag:{en:'the payload',ar:'الحمولة'},code:[
      "import { Component, input } from '@angular/core';",
      "import { ChartLib } from 'some-heavy-charting-lib';   // 180 kB",
      "",
      "@Component({",
      "  selector: 'app-reviews',",
      "  imports: [ChartLib, StarRating],",
      "  templateUrl: './reviews.component.html',",
      "})",
      "export class Reviews {",
      "  readonly productId = input.required<string>();",
      "",
      "  // this file AND everything it imports leaves the main bundle,",
      "  // because the only reference to it is inside a @defer block.",
      "}"]},
    {id:'hyd',name:'incremental hydration',lang:'html',tag:{en:'with SSR',ar:'مع الـ SSR'},code:[
      "<!-- rendered on the server, sent as real HTML,",
      "     and its JavaScript is only fetched on interaction -->",
      "@defer (hydrate on interaction) {",
      "  <app-comment-form [postId]=\"post().id\" />",
      "}",
      "",
      "<!-- hydrate never = server-render it and ship no JS at all -->",
      "@defer (hydrate never) {",
      "  <app-legal-footer />",
      "}"]}
  ],
  steps:[
    {t:{en:'The gallery and buy box render immediately — they are what the person came for. Everything below the fold is a candidate for deferring.',
        ar:'الجاليري وبوكس الشراء بيترسموا على طول — دول اللي الشخص جه عشانهم. وأي حاجة تحت حد الشاشة مرشحة إنها تتأجّل.'},
     hl:{tpl:[1,2]}},
    {t:{en:'The placeholder renders in the real layout slot, at the real height. <code class="i">minimum 400ms</code> stops the flash when the chunk arrives almost instantly.',
        ar:'الـ placeholder بيترسم في مكانه الحقيقي وبارتفاعه الحقيقي. و<code class="i">minimum 400ms</code> بيمنع الرفرفة لما الـ chunk يوصل بسرعة جداً.'},
     edge:'e1',hl:{tpl:[6,7]}},
    {t:{en:'<code class="i">prefetch on idle</code> downloads the chunk while the browser is doing nothing; <code class="i">on viewport</code> decides when to <b>render</b> it. Separating fetch from render is the trick that makes this feel instant.',
        ar:'<code class="i">prefetch on idle</code> بينزّل الـ chunk والبراوزر فاضي؛ و<code class="i">on viewport</code> بيحدد إمتى <b>يرسمه</b>. وفصل التنزيل عن الرسم هي الحركة اللي بتخلي ده يحس إنه فوري.'},
     edge:'e2',hl:{trig:[9,10]}},
    {t:{en:'The component and its 180 kB charting dependency were never in the main bundle. The only reference to them sits inside the defer block, so the build cut them out.',
        ar:'الـ component ومكتبة الرسوم اللي 180 كيلوبايت عمرهم ما كانوا في الـ bundle الأساسي. الإشارة الوحيدة ليهم جوه بلوك الـ defer، فالـ build قصّهم بره.'},
     edge:'e3',hl:{chunk:[2,12,13]}},
    {t:{en:'With SSR, <code class="i">hydrate on interaction</code> goes further: the server sends real HTML and the JavaScript is only fetched when somebody actually touches it.',
        ar:'مع الـ SSR، <code class="i">hydrate on interaction</code> بيروح أبعد: السيرفر بيبعت HTML حقيقي، والجافاسكريبت مبتتجابش غير لما حد يلمسها فعلاً.'},
     hl:{hyd:[3,4,8,9]}}
  ],
  example:{
    name:{en:'A dashboard where only one tab is ever opened', ar:'داشبورد محدش بيفتح فيها غير تاب واحد'},
    what:{
      en:'Six tabs, each with its own charts and tables. Most people open one. Deferring on interaction means five of the six chunks are never downloaded — and prefetching on hover means the one they do open feels like it was already there.',
      ar:'ستة تابات، كل واحد بالرسوم والجداول بتاعته. ومعظم الناس بتفتح واحد. الـ defer على الـ interaction معناه إن خمسة من الستة عمرهم ما هينزلوا — والـ prefetch على الـ hover بيخلي اللي بيفتحوه يحس إنه كان موجود أصلاً.'},
    files:[
      {id:'t1',name:'dashboard.component.html',lang:'html',tag:{en:'six chunks',ar:'ستة chunks'},code:[
        "<nav role=\"tablist\">",
        "  @for (t of tabs; track t.id) {",
        "    <button role=\"tab\" #tab",
        "            [attr.aria-selected]=\"active() === t.id\"",
        "            (click)=\"active.set(t.id)\">{{ t.label }}</button>",
        "  }",
        "</nav>",
        "",
        "@if (active() === 'revenue') {",
        "  @defer (on immediate; prefetch on hover) {",
        "    <app-revenue-charts />",
        "  } @placeholder { <div class=\"skeleton tall\"></div> }",
        "}",
        "",
        "@if (active() === 'cohorts') {",
        "  @defer (on immediate) {",
        "    <app-cohort-table />",
        "  } @placeholder { <div class=\"skeleton tall\"></div> }",
        "}"]},
      {id:'t2',name:'what the build produces',lang:'bash',tag:{en:'the payoff',ar:'المكسب'},code:[
        "Initial chunk files   | Names            |  Raw size",
        "main-QK4L2P.js        | main             | 214.60 kB",
        "polyfills-B7NX.js     | polyfills        |  34.58 kB",
        "",
        "Lazy chunk files      | Names            |  Raw size",
        "chunk-REVENUE.js      | revenue-charts   | 186.20 kB   ← not on first paint",
        "chunk-COHORT.js       | cohort-table     |  92.10 kB   ← not on first paint",
        "chunk-FUNNEL.js       | funnel-view      |  77.40 kB   ← not on first paint"]}
    ]
  },
  gotchas:[
    {en:'A deferred component must be used <b>only</b> inside defer blocks. Reference it anywhere eagerly and it lands back in the main bundle — silently undoing the whole thing.',
     ar:'الـ component المؤجّل لازم يتستخدم <b>بس</b> جوه بلوكات الـ defer. لو اتذكر في أي مكان تاني بشكل عادي هيرجع للـ bundle الأساسي — وهيلغي الحكاية كلها في صمت.'},
    {en:'Always give <code class="i">@placeholder</code> the height of what is coming. A placeholder that is the wrong size causes layout shift, which is a Core Web Vital.',
     ar:'دايماً ادي <code class="i">@placeholder</code> ارتفاع الحاجة اللي جاية. الـ placeholder بالحجم الغلط بيسبب layout shift، وده من الـ Core Web Vitals.'},
    {en:'<code class="i">on viewport</code> and <code class="i">on interaction</code> need something to observe. With no <code class="i">@placeholder</code> and no element reference, there is nothing to watch.',
     ar:'<code class="i">on viewport</code> و<code class="i">on interaction</code> محتاجين حاجة يراقبوها. من غير <code class="i">@placeholder</code> ومن غير إشارة لعنصر، مفيش حاجة يتراقب.'},
    {en:'Do not defer above-the-fold content. Deferring the hero image trades a small bundle for a visibly slower page.',
     ar:'متأجّلش المحتوى اللي فوق في الشاشة. تأجيل صورة الـ hero بيبدّل bundle صغير بصفحة أبطأ بشكل واضح.'},
    {en:'<code class="i">@error</code> catches the chunk failing to download — a real case on a flaky connection. Write something useful there, not a blank space.',
     ar:'<code class="i">@error</code> بيمسك فشل تنزيل الـ chunk — وده بيحصل فعلاً على نت ضعيف. اكتب فيه حاجة مفيدة، مش فراغ.'}
  ]
},

/* 13 ------------------------------------------------------------------ */
{
  slug:'13-ssr-hydration', num:'13',
  badge:{en:'Server rendering', ar:'الرندرة على السيرفر'},
  title:{en:'SSR and hydration: the same DOM, twice', ar:'SSR والـ hydration: نفس الـ DOM، مرتين'},
  lead:{
    en:`The server renders your components to HTML and sends it. The browser paints it immediately, then Angular boots and <b>adopts</b> that existing DOM instead of throwing it away. Every bug in this area comes from the two renders disagreeing.`,
    ar:`السيرفر بيرندر الـ components بتاعتك HTML ويبعتها. البراوزر بيرسمها على طول، وبعدين أنجولار بيقلع و<b>يتبنّى</b> الـ DOM الموجود بدل ما يرميه. وكل باج في المنطقة دي سببه إن الرندرتين مش متفقين.`
  },
  nodes:[
    [{id:'server',role:{en:'renders to HTML',ar:'بيرندر HTML'},file:'server',ext:'.ts',kind:'cfg'},
     {id:'cfg',role:{en:'enables hydration',ar:'بيفعّل الـ hydration'},file:'app.config',ext:'.ts',kind:'cfg'}],
    [{id:'cmp',role:{en:'must match',ar:'لازم يطابق'},file:'product.component',ext:'.ts',kind:'ts'},
     {id:'safe',role:{en:'browser-only code',ar:'كود للبراوزر بس'},file:'platform',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'server',to:'cmp',a:'bottom',b:'top',label:'render pass 1',packet:'HTML'},
    {id:'e2',from:'cfg',to:'cmp',a:'bottom',b:'right',label:'provideClientHydration',packet:'adopt'},
    {id:'e3',from:'cmp',to:'safe',a:'right',b:'left',label:'guard the globals',packet:'isBrowser'}
  ],
  files:[
    {id:'cfg',name:'app.config.ts',lang:'ts',tag:{en:'setup',ar:'الإعداد'},code:[
      "import { provideClientHydration, withEventReplay,",
      "         withIncrementalHydration } from '@angular/platform-browser';",
      "",
      "providers: [",
      "  provideClientHydration(",
      "    withEventReplay(),          // replay clicks that happened before boot",
      "    withIncrementalHydration(), // enables @defer (hydrate on …)",
      "  ),",
      "  provideHttpClient(withFetch()),   // recommended for SSR",
      "]"]},
    {id:'cmp',name:'product.component.ts',lang:'ts',tag:{en:'must be deterministic',ar:'لازم يبقى محدد'},code:[
      "export class ProductPage {",
      "  readonly id = input.required<string>();",
      "",
      "  // runs on the server, and the result is transferred to the",
      "  // browser — so the same request is not made twice",
      "  readonly product = httpResource<Product>(() => `/api/products/${this.id()}`);",
      "",
      "  // DANGER: this renders differently on server and client",
      "  // readonly seed = Math.random();",
      "  // readonly now  = new Date().toLocaleTimeString();",
      "}"]},
    {id:'safe',name:'platform.ts',lang:'ts',tag:{en:'the guard',ar:'الحماية'},code:[
      "import { inject, PLATFORM_ID, afterNextRender } from '@angular/core';",
      "import { isPlatformBrowser } from '@angular/common';",
      "",
      "// there is no window, document or localStorage on the server",
      "export function isBrowser() {",
      "  return isPlatformBrowser(inject(PLATFORM_ID));",
      "}",
      "",
      "export class Widget {",
      "  constructor() {",
      "    // the cleanest option: this callback only ever runs client-side",
      "    afterNextRender(() => {",
      "      const w = window.innerWidth;",
      "      new SomeDomLibrary('#chart').render();",
      "    });",
      "  }",
      "}"]},
    {id:'server',name:'server.ts',lang:'ts',tag:{en:'the express side',ar:'ناحية الـ express'},code:[
      "// ng add @angular/ssr generates this",
      "const angularApp = new AngularNodeAppEngine();",
      "",
      "app.use('/**', (req, res, next) => {",
      "  angularApp",
      "    .handle(req)",
      "    .then(response => response ? writeResponseToNodeResponse(response, res) : next())",
      "    .catch(next);",
      "});"]}
  ],
  steps:[
    {t:{en:'The server runs your component tree and produces HTML. The browser paints that immediately — a meaningful first paint before a single line of your JavaScript has executed.',
        ar:'السيرفر بيشغّل شجرة الـ components بتاعتك وبيطلّع HTML. والبراوزر بيرسمها على طول — أول رسمة ليها معنى قبل ما ولا سطر من الجافاسكريبت بتاعك يشتغل.'},
     edge:'e1',hl:{server:[4,5,6]}},
    {t:{en:'<code class="i">provideClientHydration()</code> tells Angular to <b>adopt</b> that DOM instead of destroying and rebuilding it. Without it you get a visible flash as the whole page is thrown away.',
        ar:'<code class="i">provideClientHydration()</code> بيقول لأنجولار <b>يتبنّى</b> الـ DOM ده بدل ما يدمّره ويبنيه من الأول. من غيرها بتشوف رفة واضحة والصفحة كلها بتترمي.'},
     edge:'e2',hl:{cfg:[5,6,7]}},
    {t:{en:'<code class="i">withEventReplay()</code> records clicks that happen before the JavaScript is ready and replays them afterwards — so the button someone pressed at 300 ms is not lost.',
        ar:'<code class="i">withEventReplay()</code> بيسجّل الكليكات اللي بتحصل قبل ما الجافاسكريبت تجهز وبيعيدها بعدين — فالزرار اللي حد داسه عند 300 مللي ثانية مش بيضيع.'},
     hl:{cfg:[6]}},
    {t:{en:'The HTTP response fetched on the server is transferred to the browser, so the same call is not made twice. That transfer cache comes with <code class="i">provideClientHydration()</code>.',
        ar:'الرد اللي اتجاب على السيرفر بيتنقل للبراوزر، فنفس النداء مبيتعملش مرتين. والـ transfer cache ده جاي مع <code class="i">provideClientHydration()</code>.'},
     hl:{cmp:[4,5,6]}},
    {t:{en:'Anything non-deterministic breaks hydration: <code class="i">Math.random()</code>, <code class="i">new Date()</code>, and any use of <code class="i">window</code> or <code class="i">document</code> — which do not exist on the server at all.',
        ar:'أي حاجة نتيجتها مش ثابتة بتكسر الـ hydration: <code class="i">Math.random()</code> و<code class="i">new Date()</code> وأي استخدام لـ <code class="i">window</code> أو <code class="i">document</code> — ودول مش موجودين خالص على السيرفر.'},
     edge:'e3',hl:{cmp:[8,9,10],safe:[12,13,14]}}
  ],
  example:{
    name:{en:'A blog article that ships almost no JavaScript', ar:'مقال مدونة بيشحن جافاسكريبت تقريباً بصفر'},
    what:{
      en:'The article body is text — it never needs to become interactive. The comment form does, but only if somebody scrolls down and clicks it. Incremental hydration lets one page be static and interactive at the same time, per region.',
      ar:'جسم المقال نص — عمره ما هيحتاج يبقى تفاعلي. فورم التعليقات محتاج، بس لو حد نزل ودوس عليه. الـ incremental hydration بيخلي الصفحة الواحدة ثابتة وتفاعلية في نفس الوقت، كل منطقة على حدة.'},
    files:[
      {id:'b1',name:'article.component.html',lang:'html',tag:{en:'per-region hydration',ar:'hydration لكل منطقة'},code:[
        "<article [innerHTML]=\"post().bodyHtml\"></article>",
        "",
        "<!-- server-rendered, and no JS is ever fetched for it -->",
        "@defer (hydrate never) {",
        "  <app-related-posts [tags]=\"post().tags\" />",
        "}",
        "",
        "<!-- server-rendered now, hydrated only if someone engages -->",
        "@defer (hydrate on interaction) {",
        "  <app-comment-form [postId]=\"post().id\" />",
        "} @placeholder {",
        "  <div class=\"comment-box-skeleton\"></div>",
        "}"]},
      {id:'b2',name:'prerendering the routes',lang:'ts',tag:{en:'build time',ar:'وقت الـ build'},code:[
        "// app.routes.server.ts — decide per route",
        "export const serverRoutes: ServerRoute[] = [",
        "  { path: 'blog/:slug', renderMode: RenderMode.Prerender,",
        "    async getPrerenderParams() {",
        "      const posts = await fetch('https://cms/posts').then(r => r.json());",
        "      return posts.map((p: Post) => ({ slug: p.slug }));",
        "    } },",
        "",
        "  { path: 'dashboard/**', renderMode: RenderMode.Client },  // never on the server",
        "  { path: '**', renderMode: RenderMode.Server },            // rendered per request",
        "];"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">window</code>, <code class="i">document</code> and <code class="i">localStorage</code> do not exist on the server. Guard with <code class="i">isPlatformBrowser</code>, or better, use <code class="i">afterNextRender()</code>, which only ever runs in the browser.',
     ar:'<code class="i">window</code> و<code class="i">document</code> و<code class="i">localStorage</code> مش موجودين على السيرفر. احمي نفسك بـ <code class="i">isPlatformBrowser</code>، أو الأحسن استعمل <code class="i">afterNextRender()</code> اللي عمره ما بيشتغل غير في البراوزر.'},
    {en:'<b>NG0500 / hydration mismatch</b> means the two renders produced different DOM. Usual causes: random values, dates, direct DOM manipulation, or invalid HTML nesting the browser silently corrects.',
     ar:'<b>NG0500 / hydration mismatch</b> معناها إن الرندرتين طلعوا DOM مختلف. الأسباب المعتادة: قيم عشوائية، أو تواريخ، أو تعديل مباشر على الـ DOM، أو تداخل HTML غلط البراوزر بيصلحه في صمت.'},
    {en:'Invalid nesting is a sneaky one: a <code class="i">&lt;div&gt;</code> inside a <code class="i">&lt;p&gt;</code> is re-parented by the browser, so the client DOM no longer matches what the server sent.',
     ar:'التداخل الغلط ده خبيث: <code class="i">&lt;div&gt;</code> جوه <code class="i">&lt;p&gt;</code> البراوزر بينقله، فالـ DOM عند الكلاينت مبقاش مطابق للي السيرفر بعته.'},
    {en:'Use <code class="i">provideHttpClient(withFetch())</code> under SSR: Angular recommends the fetch backend on the server and warns in development without it. What stops requests running twice is the transfer cache from <code class="i">provideClientHydration()</code>; drop that and they do run twice.',
     ar:'استخدم <code class="i">provideHttpClient(withFetch())</code> مع الـ SSR: أنجولار بينصح بالـ fetch على السيرفر وبيحذّرك في الـ development من غيرها. واللي بيمنع إن الريكويست يتعمل مرتين هو الـ transfer cache اللي جاي من <code class="i">provideClientHydration()</code>؛ لو شلته هيتعمل مرتين فعلًا.'},
    {en:'You can opt a subtree out entirely with <code class="i">ngSkipHydration</code> on the element. It is an escape hatch for a stubborn third-party widget, not a fix.',
     ar:'تقدر تستثني فرع كامل بـ <code class="i">ngSkipHydration</code> على العنصر. ده مخرج طوارئ لويدجت خارجية عنيدة، مش حل.'}
  ]
},

/* 14 ------------------------------------------------------------------ */
{
  slug:'14-rxjs-interop', num:'14',
  badge:{en:'Interop', ar:'التوافق'},
  title:{en:'Signals and RxJS, meeting in the middle', ar:'الـ Signals و RxJS بيتقابلوا في النص'},
  lead:{
    en:`Signals model <b>state</b>: one current value, always available, synchronous. RxJS models <b>events over time</b>: sequences, with operators for debouncing, retrying and combining. They are not competitors — and two functions carry values across the border.`,
    ar:`الـ Signals بتمثّل <b>الحالة</b>: قيمة واحدة حالية، موجودة دايماً، ومتزامنة. و RxJS بيمثّل <b>الأحداث عبر الزمن</b>: سلاسل، وعليها operators للـ debounce والإعادة والدمج. مش منافسين لبعض — وفيه اتنين functions بينقلوا القيم عبر الحدود.`
  },
  nodes:[
    [{id:'sig',role:{en:'state',ar:'حالة'},file:'search.component',ext:'.ts',kind:'ts'}],
    [{id:'rx',role:{en:'events over time',ar:'أحداث عبر الزمن'},file:'stream',ext:'.ts',kind:'svc'},
     {id:'back',role:{en:'back to state',ar:'رجوع لحالة'},file:'results',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'sig',to:'rx',a:'bottom',b:'top',label:'toObservable(term)',packet:'"ang"'},
    {id:'e2',from:'rx',to:'back',a:'right',b:'left',label:'debounce → switchMap',packet:'Product[]'},
    {id:'e3',from:'back',to:'sig',a:'top',b:'right',label:'toSignal(...)',packet:'results()'}
  ],
  files:[
    {id:'sig',name:'search.component.ts',lang:'ts',tag:{en:'the round trip',ar:'الرحلة كاملة'},code:[
      "import { toSignal, toObservable } from '@angular/core/rxjs-interop';",
      "import { debounceTime, distinctUntilChanged, switchMap } from 'rxjs';",
      "",
      "export class Search {",
      "  private readonly http = inject(HttpClient);",
      "",
      "  // state",
      "  readonly term = signal('');",
      "",
      "  // state → stream → operators → state",
      "  readonly results = toSignal(",
      "    toObservable(this.term).pipe(",
      "      debounceTime(300),",
      "      distinctUntilChanged(),",
      "      switchMap(q => this.http.get<Product[]>(`/api/search?q=${q}`)),",
      "    ),",
      "    { initialValue: [] as Product[] },",
      "  );",
      "}"]},
    {id:'when',name:'which one, when',lang:'ts',tag:{en:'the rule',ar:'القاعدة'},code:[
      "// SIGNAL — there is always a current value and you want to read it",
      "readonly user = signal<User | null>(null);",
      "readonly cart = signal<Item[]>([]);",
      "readonly isOpen = signal(false);",
      "",
      "// RXJS — the question is about time, order or cancellation",
      "//   debounceTime      wait for typing to settle",
      "//   switchMap         cancel the request I no longer care about",
      "//   retry / retryWhen back off and try again",
      "//   combineLatest     merge several independent streams",
      "//   fromEvent         a websocket, a scroll, a drag",
      "",
      "// A rule of thumb: if you would put it in a variable, it is a signal.",
      "// If you would draw it on a timeline, it is an observable."]},
    {id:'back',name:'rxResource',lang:'ts',tag:{en:'both at once',ar:'الاتنين مع بعض'},code:[
      "import { rxResource } from '@angular/core/rxjs-interop';",
      "",
      "readonly page = signal(1);",
      "",
      "readonly orders = rxResource({",
      "  params: () => ({ page: this.page() }),        // signals in",
      "  stream: ({ params }) =>                       // an observable out",
      "    this.http.get<Order[]>(`/api/orders?page=${params.page}`).pipe(",
      "      retry({ count: 2, delay: 500 }),",
      "    ),",
      "});",
      "",
      "// (in earlier versions these two were called request and loader)",
      "// orders.value() / orders.isLoading() / orders.error() / orders.reload()"]},
    {id:'leak',name:'takeUntilDestroyed',lang:'ts',tag:{en:'when you must subscribe',ar:'لما تضطر تعمل subscribe'},code:[
      "import { takeUntilDestroyed } from '@angular/core/rxjs-interop';",
      "",
      "constructor() {",
      "  // valueChanges NEVER completes — this is a genuine leak without it",
      "  this.form.valueChanges",
      "    .pipe(debounceTime(500), takeUntilDestroyed())",
      "    .subscribe(v => this.autosave(v));",
      "}",
      "",
      "// outside an injection context, pass the ref explicitly:",
      "// .pipe(takeUntilDestroyed(this.destroyRef))"]}
  ],
  steps:[
    {t:{en:'The term is state, so it is a signal. Someone typing produces a new current value, and there is always an answer to “what is it now?”.',
        ar:'الـ term حالة، فهو signal. الكتابة بتنتج قيمة حالية جديدة، وفيه دايماً إجابة لسؤال “هو إيه دلوقتي؟”.'},
     hl:{sig:[8]}},
    {t:{en:'<code class="i">toObservable()</code> turns those successive values into a stream, so operators about <b>time</b> become available.',
        ar:'<code class="i">toObservable()</code> بيحوّل القيم المتتابعة دي لستريم، فالـ operators اللي بتتعامل مع <b>الزمن</b> تبقى متاحة.'},
     edge:'e1',hl:{sig:[12]}},
    {t:{en:'This is the part signals genuinely cannot express: wait 300 ms for typing to settle, ignore repeats, and <b>cancel</b> the request whose answer no longer matters.',
        ar:'ودي الحتة اللي الـ signals فعلاً مش بتعبّر عنها: استنى 300 مللي ثانية لحد ما الكتابة تهدا، وتجاهل المكرر، و<b>الغي</b> الريكويست اللي رده مبقاش يهم.'},
     edge:'e2',hl:{sig:[13,14,15]}},
    {t:{en:'<code class="i">toSignal()</code> brings it home. It subscribes for you, unsubscribes when the component is destroyed, and gives the template a plain value to read.',
        ar:'<code class="i">toSignal()</code> بيرجّعها البيت. بيعمل subscribe بدالك، وبيلغيه لما الـ component يتدمّر، وبيدي التمبلت قيمة عادية يقراها.'},
     edge:'e3',hl:{sig:[11,17]}},
    {t:{en:'When you genuinely must subscribe by hand, <code class="i">takeUntilDestroyed()</code> is the seatbelt. <code class="i">valueChanges</code> never completes, so without it you have a real leak.',
        ar:'ولما تضطر فعلاً تعمل subscribe بإيدك، <code class="i">takeUntilDestroyed()</code> هو حزام الأمان. <code class="i">valueChanges</code> عمره ما بيقفل، فمن غيره عندك تسريب حقيقي.'},
     hl:{leak:[4,5,6,7]}},
    {t:{en:'The rule of thumb: if you would store it in a variable, it is a signal. If you would draw it on a timeline, it is an observable.',
        ar:'القاعدة العملية: لو كنت هتحطها في متغير، يبقى دي signal. ولو كنت هترسمها على خط زمن، يبقى دي observable.'},
     hl:{when:[2,3,4,13,14]}},
    {t:{en:'<code class="i">rxResource</code> is the two worlds in one declaration: signals go in as <code class="i">params</code>, an observable comes out as <code class="i">stream</code>, and you get the resource surface on top.',
        ar:'<code class="i">rxResource</code> هو العالمين في إعلان واحد: الـ signals بتدخل كـ <code class="i">params</code>، والـ observable بيخرج كـ <code class="i">stream</code>، وبتاخد واجهة الـ resource فوقهم.'},
     hl:{back:[5,6,7,8]}}
  ],
  example:{
    name:{en:'A live orders feed over a websocket', ar:'تدفق أوردرات حي عبر websocket'},
    what:{
      en:'The socket is a stream — messages arriving over time, with reconnection and back-off. The list on screen is state — one array, always readable. The boundary between them is exactly one toSignal call.',
      ar:'الـ socket ستريم — رسايل بتوصل عبر الزمن، مع إعادة اتصال وتباطؤ. واللستة اللي على الشاشة حالة — array واحدة، بتتقرا في أي وقت. والحد بينهم نداء واحد لـ toSignal.'},
    files:[
      {id:'w1',name:'orders.socket.ts',lang:'ts',tag:{en:'stream side',ar:'ناحية الستريم'},code:[
        "@Injectable({ providedIn: 'root' })",
        "export class OrdersSocket {",
        "  // reconnection, back-off and buffering are all time problems.",
        "  // this is what RxJS is genuinely good at.",
        "  readonly feed$ = webSocket<Order>('wss://api/orders').pipe(",
        "    retry({ delay: 2000 }),",
        "    scan((all: Order[], o: Order) => [o, ...all].slice(0, 200), []),",
        "    shareReplay({ bufferSize: 1, refCount: true }),",
        "  );",
        "}"]},
      {id:'w2',name:'orders-board.component.ts',lang:'ts',tag:{en:'state side',ar:'ناحية الحالة'},code:[
        "export class OrdersBoard {",
        "  // one call, and the rest of the component never mentions RxJS again",
        "  readonly orders = toSignal(inject(OrdersSocket).feed$, { initialValue: [] });",
        "",
        "  readonly urgent = computed(() => this.orders().filter(o => o.priority === 'high'));",
        "  readonly total  = computed(() => this.orders().length);",
        "}"]},
      {id:'w3',name:'orders-board.component.html',lang:'html',tag:{en:'no async pipe',ar:'من غير async pipe'},code:[
        "<h2>{{ total() }} orders — {{ urgent().length }} urgent</h2>",
        "",
        "@for (o of orders(); track o.id) {",
        "  <app-order-card [order]=\"o\" [class.urgent]=\"o.priority === 'high'\" />",
        "}"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">toSignal()</code> subscribes <b>immediately</b> and needs an injection context. Give it an <code class="i">initialValue</code> or its type includes <code class="i">undefined</code>.',
     ar:'<code class="i">toSignal()</code> بيعمل subscribe <b>على طول</b> ومحتاج injection context. ادّيه <code class="i">initialValue</code> وإلا نوعه هيشمل <code class="i">undefined</code>.'},
    {en:'<code class="i">toObservable()</code> emits on a microtask, not synchronously. Two rapid <code class="i">.set()</code> calls in the same tick produce <b>one</b> emission with the last value.',
     ar:'<code class="i">toObservable()</code> بيطلّع القيمة في microtask مش بشكل متزامن. فلو عملت <code class="i">.set()</code> مرتين بسرعة في نفس التكة هيطلع <b>إصدار واحد</b> بآخر قيمة.'},
    {en:'Do not convert back and forth for the sake of it. Each hop costs a subscription and a scheduling boundary; cross once, in one direction, at the edge.',
     ar:'متفضلش تحوّل رايح جاي من غير سبب. كل قفزة بتكلف subscription وحد جدولة؛ اعبر مرة واحدة، في اتجاه واحد، عند الحافة.'},
    {en:'An observable that errors kills the signal derived from it. Handle errors <b>inside</b> the pipe with <code class="i">catchError</code> before <code class="i">toSignal</code>.',
     ar:'أي observable بيرمي error بيقتل الـ signal المشتقة منه. اتعامل مع الأخطاء <b>جوه</b> الـ pipe بـ <code class="i">catchError</code> قبل <code class="i">toSignal</code>.'},
    {en:'<code class="i">shareReplay({ refCount: true })</code> matters for shared streams. Without <code class="i">refCount</code> the subscription outlives every subscriber.',
     ar:'<code class="i">shareReplay({ refCount: true })</code> مهمة للستريمز المشتركة. من غير <code class="i">refCount</code> الـ subscription بيفضل عايش بعد ما كل المشتركين يمشوا.'}
  ]
},

/* 15 ------------------------------------------------------------------ */
{
  slug:'15-di-host-directives', num:'15',
  badge:{en:'Composition', ar:'التركيب'},
  title:{en:'Injection tokens and host directives', ar:'الـ Injection tokens والـ host directives'},
  lead:{
    en:`Two tools for building things other people extend. <code>InjectionToken</code> lets you inject a value that is not a class — config, a strategy, a list of plugins. <code>hostDirectives</code> lets a component <b>wear</b> behaviour it did not write, without inheritance and without a wrapper element.`,
    ar:`أداتين لبناء حاجات ناس تانية هتوسّعها. <code>InjectionToken</code> بيخليك تعمل inject لقيمة مش كلاس — إعدادات، أو استراتيجية، أو لستة plugins. و<code>hostDirectives</code> بيخلي الـ component <b>يلبس</b> سلوك مكتبوش، من غير وراثة ومن غير عنصر ملفوف حواليه.`
  },
  nodes:[
    [{id:'tok',role:{en:'a value, not a class',ar:'قيمة مش كلاس'},file:'tokens',ext:'.ts',kind:'cfg'},
     {id:'dir',role:{en:'the behaviour',ar:'السلوك'},file:'analytics',ext:'.directive.ts',kind:'cfg'}],
    [{id:'cmp',role:{en:'wears it',ar:'بيلبسه'},file:'cta-button.component',ext:'.ts',kind:'ts'},
     {id:'scope',role:{en:'overrides locally',ar:'بيغيّرها محلياً'},file:'checkout.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'tok',to:'cmp',a:'bottom',b:'top',label:'inject(APP_CONFIG)',packet:'{ api }'},
    {id:'e2',from:'dir',to:'cmp',a:'bottom',b:'right',label:'hostDirectives: [...]',packet:'behaviour'},
    {id:'e3',from:'scope',to:'cmp',a:'top',b:'bottom',label:'providers: [...]',packet:'override'}
  ],
  files:[
    {id:'tok',name:'tokens.ts',lang:'ts',tag:{en:'typed config',ar:'إعدادات بأنواع'},code:[
      "import { InjectionToken, inject } from '@angular/core';",
      "",
      "export interface AppConfig { apiUrl: string; pageSize: number; }",
      "",
      "// a class can be injected by its own type. a plain object cannot —",
      "// so you need a token to name it.",
      "export const APP_CONFIG = new InjectionToken<AppConfig>('APP_CONFIG', {",
      "  providedIn: 'root',",
      "  factory: () => ({ apiUrl: '/api', pageSize: 20 }),   // the default",
      "});",
      "",
      "// multi: true collects many providers into one array —",
      "// this is how HTTP_INTERCEPTORS and APP_INITIALIZER work",
      "export const FORMATTERS = new InjectionToken<Formatter[]>('FORMATTERS');"]},
    {id:'dir',name:'analytics.directive.ts',lang:'ts',tag:{en:'reusable behaviour',ar:'سلوك قابل لإعادة الاستخدام'},code:[
      "@Directive({ selector: '[appTrack]' })",
      "export class TrackClicks {",
      "  readonly appTrack = input.required<string>();   // the event name",
      "  private readonly analytics = inject(Analytics);",
      "",
      "  @HostListener('click')",
      "  onClick() { this.analytics.send(this.appTrack()); }",
      "}"]},
    {id:'cmp',name:'cta-button.component.ts',lang:'ts',tag:{en:'composition',ar:'التركيب'},code:[
      "@Component({",
      "  selector: 'app-cta-button',",
      "  hostDirectives: [",
      "    // the component now BEHAVES as if it had this directive on it,",
      "    // with no extra element in the DOM and no inheritance",
      "    { directive: TrackClicks, inputs: ['appTrack: track'] },",
      "    { directive: CdkOverlayOrigin },",
      "  ],",
      "  template: `<button><ng-content /></button>`,",
      "})",
      "export class CtaButton {",
      "  private readonly config = inject(APP_CONFIG);",
      "",
      "  // optional / skipSelf / host change WHERE Angular looks",
      "  private readonly theme = inject(THEME, { optional: true }) ?? 'light';",
      "}"]},
    {id:'scope',name:'checkout.component.ts',lang:'ts',tag:{en:'local override',ar:'تغيير محلي'},code:[
      "@Component({",
      "  selector: 'app-checkout',",
      "  // everything inside this subtree sees THIS config,",
      "  // and nothing outside it is affected",
      "  providers: [",
      "    { provide: APP_CONFIG, useValue: { apiUrl: '/api/v2', pageSize: 5 } },",
      "    { provide: FORMATTERS, useClass: EgpFormatter, multi: true },",
      "  ],",
      "  template: `<app-cta-button track=\"checkout_cta\">Pay now</app-cta-button>`,",
      "})",
      "export class Checkout {}"]}
  ],
  steps:[
    {t:{en:'A class can be injected by its own type. A plain object or a string cannot — an <code class="i">InjectionToken</code> is the name you give it, and the generic carries the type.',
        ar:'الكلاس ينفع يتعمل له inject بنوعه. أما object عادي أو string فلأ — الـ <code class="i">InjectionToken</code> هو الاسم اللي بتديه له، والـ generic شايل النوع.'},
     hl:{tok:[7,8,9]}},
    {t:{en:'A <code class="i">factory</code> on the token supplies the default, so the app runs with no provider configured at all. Overriding it later is opt-in.',
        ar:'الـ <code class="i">factory</code> على التوكن بتوفّر القيمة الافتراضية، فالتطبيق بيشتغل من غير أي provider أصلاً. وتغييرها بعدين اختياري.'},
     edge:'e1',hl:{cmp:[12]}},
    {t:{en:'<code class="i">hostDirectives</code> is composition without inheritance. The component behaves as if the directive were applied to it — no wrapper element, no base class, and the directive stays independently testable.',
        ar:'<code class="i">hostDirectives</code> تركيب من غير وراثة. الـ component بيتصرف كأن الـ directive متطبق عليه — من غير عنصر ملفوف، ومن غير base class، والـ directive بيفضل قابل للاختبار لوحده.'},
     edge:'e2',hl:{cmp:[3,6,7],dir:[1,6,7]}},
    {t:{en:'Inputs can be re-exposed under a friendlier name. The consumer writes <code class="i">track="…"</code> and never learns the directive exists.',
        ar:'الـ inputs ممكن تتعرض باسم أحلى. المستخدم بيكتب <code class="i">track="…"</code> وعمره ما هيعرف إن الـ directive موجود أصلاً.'},
     hl:{cmp:[6],scope:[9]}},
    {t:{en:'Providers on a component create a child injector: everything in that subtree sees the override, nothing outside does. This is scoping, and it is why DI is a tree and not a registry.',
        ar:'الـ providers على component بتعمل injector ابن: كل اللي في الفرع ده بيشوف التغيير، ومحدش بره بيشوفه. ده اسمه scoping، وعشان كده الـ DI شجرة مش سجل.'},
     edge:'e3',hl:{scope:[5,6,7]}}
  ],
  example:{
    name:{en:'A design-system button used in three apps', ar:'زرار في design system مستخدم في تلات تطبيقات'},
    what:{
      en:'Each app needs different analytics, different currency formatting, and different default sizes — but none of them should have to fork the button. Tokens carry the differences; host directives carry the shared behaviour.',
      ar:'كل تطبيق محتاج analytics مختلفة، وتنسيق عملة مختلف، ومقاسات افتراضية مختلفة — وولا واحد فيهم المفروض يعمل fork للزرار. التوكنز بتشيل الاختلافات، والـ host directives بتشيل السلوك المشترك.'},
    files:[
      {id:'k1',name:'button.tokens.ts',lang:'ts',tag:{en:'the seams',ar:'نقاط التوصيل'},code:[
        "export const BUTTON_DEFAULTS = new InjectionToken<ButtonDefaults>('BUTTON_DEFAULTS', {",
        "  providedIn: 'root',",
        "  factory: () => ({ size: 'md', variant: 'solid' }),",
        "});",
        "",
        "// the library depends on the INTERFACE, never on your implementation",
        "export const ANALYTICS = new InjectionToken<AnalyticsPort>('ANALYTICS');"]},
      {id:'k2',name:'app.config.ts — app A',lang:'ts',tag:{en:'wired per app',ar:'موصّل لكل تطبيق'},code:[
        "providers: [",
        "  { provide: BUTTON_DEFAULTS, useValue: { size: 'lg', variant: 'ghost' } },",
        "  { provide: ANALYTICS, useClass: SegmentAnalytics },",
        "]",
        "",
        "// app B ships PlausibleAnalytics, app C ships a no-op for tests.",
        "// The button component itself never changes."]},
      {id:'k3',name:'button.component.spec.ts',lang:'ts',tag:{en:'why it is testable',ar:'ليه قابل للاختبار'},code:[
        "TestBed.configureTestingModule({",
        "  providers: [",
        "    { provide: ANALYTICS, useValue: { send: jasmine.createSpy('send') } },",
        "    { provide: BUTTON_DEFAULTS, useValue: { size: 'sm', variant: 'solid' } },",
        "  ],",
        "});",
        "",
        "// no network, no globals, no mocking of imports — just a different provider."]}
    ]
  },
  gotchas:[
    {en:'Give every token a description string. <code class="i">new InjectionToken(\'APP_CONFIG\')</code> is what appears in the error when a provider is missing — an empty one produces an unreadable message.',
     ar:'ادي كل توكن وصف نصي. <code class="i">new InjectionToken(\'APP_CONFIG\')</code> ده اللي بيظهر في الايرور لما provider يكون ناقص — والفاضي بيطلّع رسالة مش مفهومة.'},
    {en:'A token with a <code class="i">factory</code> is tree-shakable and always has a default. A token without one throws <b>NG0201: No provider</b> the first time somebody injects it.',
     ar:'التوكن اللي معاه <code class="i">factory</code> بيبقى tree-shakable وليه قيمة افتراضية دايماً. واللي من غيرها بيرمي <b>NG0201: No provider</b> أول ما حد يعمل له inject.'},
    {en:'<code class="i">hostDirectives</code> cannot be conditional — they apply to every instance. For opt-in behaviour, use a normal directive the consumer applies themselves.',
     ar:'<code class="i">hostDirectives</code> مينفعش تبقى مشروطة — بتتطبق على كل نسخة. ولو عايز سلوك اختياري، استعمل directive عادي المستخدم بيطبقه بنفسه.'},
    {en:'Inputs of a host directive are <b>not</b> exposed by default. List them in <code class="i">inputs: []</code>, optionally renaming, or the consumer cannot reach them.',
     ar:'الـ inputs بتاعة الـ host directive <b>مش</b> متاحة بشكل افتراضي. حطها في <code class="i">inputs: []</code>، وسمّيها من جديد لو حبيت، وإلا المستخدم مش هيوصلها.'},
    {en:'A service in a component’s <code class="i">providers</code> is created per instance. That is the right tool for state scoped to one dialog — and a bug when you wanted app-wide state.',
     ar:'الـ service اللي في <code class="i">providers</code> بتاعة component بتتعمل لكل نسخة. دي الأداة الصح لحالة مربوطة بـ dialog واحد — وباج لو كنت عايز حالة للتطبيق كله.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'rxjs-combination',
  badge:{en:'Joining streams', ar:'دمج الستريمز'},
  title:{en:'<code>forkJoin</code>, <code>combineLatest</code>, <code>withLatestFrom</code>', ar:'<code>forkJoin</code> و <code>combineLatest</code> و <code>withLatestFrom</code>'},
  lead:{
    en:`Three operators for “I need more than one source at once”, and they differ on two questions: <b>does it wait for everything to finish</b>, and <b>which source is allowed to trigger the output</b>. Get those two wrong and your page either never renders or renders four times.`,
    ar:`تلات operators لـ «أنا محتاج أكتر من مصدر مرة واحدة»، وبيختلفوا في سؤالين: <b>هل بيستنى كل حاجة تخلص</b>، و<b>أنهي مصدر مسموح له يطلق المخرجات</b>. وغلط في الاتنين دول يخلي صفحتك يا عمرها ما بتترسم يا بتترسم أربع مرات.`
  },
  nodes:[
    [{id:'a',role:{en:'source A',ar:'مصدر A'},file:'user$',ext:'',kind:'svc'},
     {id:'b',role:{en:'source B',ar:'مصدر B'},file:'orders$',ext:'',kind:'svc'}],
    [{id:'op',role:{en:'the join',ar:'الدمج'},file:'combination',ext:'',kind:'cfg'},
     {id:'out',role:{en:'one value',ar:'قيمة واحدة'},file:'view$',ext:'',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'a',to:'op',a:'bottom',b:'top',label:'emits',packet:'User'},
    {id:'e2',from:'b',to:'op',a:'bottom',b:'top',label:'emits',packet:'Order[]'},
    {id:'e3',from:'op',to:'out',a:'right',b:'left',label:'[user, orders]',packet:'combined'}
  ],
  files:[
    {id:'op',name:'the three, and when each is right',lang:'ts',tag:{en:'the decision',ar:'القرار'},code:[
      "// forkJoin — wait for ALL to COMPLETE, then emit once. Promise.all.",
      "//   ✓ several independent HTTP calls for one page load",
      "//   ✗ NEVER with a stream that does not complete — it hangs forever",
      "forkJoin({",
      "  user:    this.http.get<User>('/api/me'),",
      "  orders:  this.http.get<Order[]>('/api/orders'),",
      "  settings: this.http.get<Settings>('/api/settings'),",
      "}).subscribe(({ user, orders, settings }) => { … });",
      "",
      "// combineLatest — emit the latest of EACH, whenever ANY of them fires.",
      "//   ✓ filters + sort + page → one query object",
      "//   ✗ emits nothing until EVERY source has fired at least once",
      "combineLatest([this.search$, this.sort$, this.page$]).pipe(",
      "  map(([q, sort, page]) => ({ q, sort, page })),",
      "  switchMap(params => this.api.search(params)),",
      ");",
      "",
      "// withLatestFrom — ONE source drives; the others are just read.",
      "//   ✓ on submit, grab the current form value and the current user",
      "this.submit$.pipe(",
      "  withLatestFrom(this.form$, this.user$),",
      "  switchMap(([, form, user]) => this.api.save(form, user.id)),",
      ");"]},
    {id:'diff',name:'the difference that causes bugs',lang:'ts',tag:{en:'read carefully',ar:'اقرا بتركيز'},code:[
      "// combineLatest fires on EVERY source. three filters changing",
      "// in the same tick = THREE emissions = three HTTP requests.",
      "combineLatest([a$, b$, c$])         // a fires → emit. b fires → emit. c → emit.",
      "",
      "// fix 1: debounce the combined result",
      "combineLatest([a$, b$, c$]).pipe(debounceTime(0));   // one emission per tick",
      "",
      "// fix 2: use withLatestFrom so only ONE source triggers",
      "a$.pipe(withLatestFrom(b$, c$));    // only a fires the output",
      "",
      "// ---------------------------------------------------------------",
      "",
      "// combineLatest emits NOTHING until every source has emitted once.",
      "// a BehaviorSubject or startWith() has a value immediately;",
      "// a plain Subject does not — and your page renders blank forever.",
      "combineLatest([",
      "  this.search$.pipe(startWith('')),      // ← now it has a value",
      "  this.sort$.pipe(startWith('name')),",
      "]);"]},
    {id:'sig',name:'in signal code you rarely need them',lang:'ts',tag:{en:'the modern answer',ar:'الإجابة الحديثة'},code:[
      "// combineLatest exists because observables have no current value.",
      "// signals DO — so 'combining' is just reading them in a computed.",
      "",
      "readonly search = signal('');",
      "readonly sort   = signal<'name' | 'price'>('name');",
      "readonly page   = signal(1);",
      "",
      "// this IS combineLatest, with none of the ceremony:",
      "readonly query = computed(() => ({",
      "  q: this.search(), sort: this.sort(), page: this.page(),",
      "}));",
      "",
      "readonly results = httpResource<Product[]>(() => {",
      "  const { q, sort, page } = this.query();",
      "  return `/api/products?q=${q}&sort=${sort}&page=${page}`;",
      "});",
      "",
      "// forkJoin still has no signal equivalent — for 'fire three",
      "// requests and wait for all of them', it is still the right tool."]},
    {id:'more',name:'the other joins',lang:'ts',tag:{en:'less common',ar:'أقل شيوعاً'},code:[
      "merge(a$, b$)        // pass everything through from either. no pairing.",
      "                     // → a websocket feed plus a manual refresh button",
      "",
      "zip(a$, b$)          // pair them 1-with-1, 2-with-2. waits for both.",
      "                     // rarely what you want — usually you meant combineLatest",
      "",
      "concat(a$, b$)       // all of a, then all of b. strictly in order.",
      "",
      "race(a$, b$)         // whichever emits first wins; the other is dropped",
      "",
      "startWith(x)         // emit x immediately, then carry on.",
      "                     // the fix for 'combineLatest never fires'",
      "",
      "pairwise()           // emit [previous, current] — for diffing"]}
  ],
  steps:[
    {t:{en:'<code class="i">forkJoin</code> is <code class="i">Promise.all</code>. It waits for every source to <b>complete</b>, then emits once. HTTP calls complete, so this is the right tool for a page that needs three of them.',
        ar:'<code class="i">forkJoin</code> هو <code class="i">Promise.all</code>. بيستنى كل مصدر <b>يقفل</b>، وبعدين بيطلّع مرة واحدة. ونداءات HTTP بتقفل، فده الأداة الصح لصفحة محتاجة تلاتة منهم.'},
     edge:'e1',hl:{op:[1,2,4,5,6,7]}},
    {t:{en:'<code class="i">combineLatest</code> never waits for completion. It emits the latest of each, <b>every time any of them fires</b> — which is what you want for live filters.',
        ar:'<code class="i">combineLatest</code> عمره ما بيستنى الإقفال. بيطلّع آخر قيمة من كل واحد، <b>كل مرة أي واحد فيهم يشتغل</b> — وده اللي عايزه مع الفلاتر الحية.'},
     edge:'e2',hl:{op:[10,13,14,15]}},
    {t:{en:'That “every time any of them fires” is the bug factory. Three filters changing together produce three emissions and three requests. <code class="i">debounceTime(0)</code> collapses them.',
        ar:'و«كل مرة أي واحد يشتغل» دي مصنع الباجات. تلات فلاتر بيتغيروا مع بعض بيطلّعوا تلات إصدارات وتلات ريكويستات. و<code class="i">debounceTime(0)</code> بيلمّهم.'},
     hl:{diff:[1,2,3,5,6]}},
    {t:{en:'<code class="i">withLatestFrom</code> fixes the other half: <b>one</b> source triggers the output, the rest are read-only passengers. Exactly right for a submit button.',
        ar:'<code class="i">withLatestFrom</code> بيحل النص التاني: <b>مصدر واحد</b> بيطلق المخرجات، والباقي ركاب للقراية بس. وده مظبوط بالظبط لزرار إرسال.'},
     edge:'e3',hl:{op:[18,20,21,22]}},
    {t:{en:'In signal code most of this disappears — a <code class="i">computed</code> that reads three signals <b>is</b> <code class="i">combineLatest</code>, with no subscription and no startWith.',
        ar:'وفي كود الـ signals معظم ده بيختفي — الـ <code class="i">computed</code> اللي بتقرا تلات signals <b>هي</b> <code class="i">combineLatest</code>، من غير subscription ومن غير startWith.'},
     hl:{sig:[1,2,8,9,10,11]}},
    {t:{en:'The rest are rarer but worth recognising — especially <code class="i">startWith</code>, which is the fix for a <code class="i">combineLatest</code> that never fires.',
        ar:'والباقي أندر بس يستاهل تعرفهم — خصوصاً <code class="i">startWith</code>، اللي هي حل الـ <code class="i">combineLatest</code> اللي عمره ما بيشتغل.'},
     hl:{more:[1,4,7,11,12]}}
  ],
  example:[
    {
      name:{en:'A dashboard that loads four things at once', ar:'داشبورد بتحمّل أربع حاجات مرة واحدة'},
      what:{
        en:'Four independent endpoints, one loading state, one error state. Sequential awaits would take four round trips; forkJoin takes one — the duration of the slowest call.',
        ar:'أربع endpoints مستقلة، وحالة تحميل واحدة، وحالة ايرور واحدة. الـ await المتتابع هياخد أربع رحلات؛ وforkJoin بياخد واحدة — مدة أبطأ نداء فيهم.'},
      files:[
        {id:'fj1',name:'dashboard.component.ts',lang:'ts',tag:{en:'one round trip',ar:'رحلة واحدة'},code:[
          "export class Dashboard {",
          "  readonly data = toSignal(",
          "    forkJoin({",
          "      me:      this.http.get<User>('/api/me'),",
          "      orders:  this.http.get<Order[]>('/api/orders?limit=5'),",
          "      revenue: this.http.get<Revenue>('/api/stats/revenue'),",
          "      alerts:  this.http.get<Alert[]>('/api/alerts').pipe(",
          "                 catchError(() => of([])),   // this one may fail safely",
          "               ),",
          "    }).pipe(",
          "      catchError(err => { this.error.set(true); return EMPTY; }),",
          "    ),",
          "  );",
          "",
          "  readonly error = signal(false);",
          "  readonly loading = computed(() => !this.data() && !this.error());",
          "",
          "  // NOTE the inner catchError on alerts: without it, one failing",
          "  // endpoint takes the ENTIRE forkJoin down and the whole",
          "  // dashboard shows an error because a badge could not load.",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<b><code class="i">forkJoin</code> on a stream that never completes hangs forever</b> and emits nothing. Only use it with sources that complete — HTTP calls do; <code class="i">valueChanges</code> does not.',
     ar:'<b><code class="i">forkJoin</code> مع ستريم عمره ما بيقفل بيعلّق للأبد</b> ومبيطلّعش حاجة. استعمله بس مع مصادر بتقفل — نداءات HTTP بتقفل؛ و<code class="i">valueChanges</code> لأ.'},
    {en:'If <b>any</b> source in a <code class="i">forkJoin</code> errors, the whole thing errors and you lose the successful results too. Add <code class="i">catchError</code> to the optional ones.',
     ar:'لو <b>أي</b> مصدر في <code class="i">forkJoin</code> رمى ايرور، الكل بيرمي ايرور وبتضيّع النتايج الناجحة كمان. ضيف <code class="i">catchError</code> للاختياريين.'},
    {en:'<code class="i">combineLatest</code> emits nothing until <b>every</b> source has emitted once. A plain <code class="i">Subject</code> in the list means a page that stays blank — add <code class="i">startWith()</code>.',
     ar:'<code class="i">combineLatest</code> مبيطلّعش حاجة لحد ما <b>كل</b> مصدر يطلّع مرة. و<code class="i">Subject</code> عادي في اللستة معناه صفحة فاضلة بيضا — ضيف <code class="i">startWith()</code>.'},
    {en:'<code class="i">combineLatest</code> fires once <b>per source change</b>. Changing three filters in one tick fires three times. <code class="i">debounceTime(0)</code> or <code class="i">withLatestFrom</code> is the fix.',
     ar:'<code class="i">combineLatest</code> بيشتغل مرة <b>لكل تغيير مصدر</b>. وتغيير تلات فلاتر في تكة واحدة بيشغّله تلات مرات. والحل <code class="i">debounceTime(0)</code> أو <code class="i">withLatestFrom</code>.'},
    {en:'If everything you are combining is state, use a <code class="i">computed</code> instead. These operators exist to work around observables not having a current value.',
     ar:'لو كل اللي بتدمجه حالة، استعمل <code class="i">computed</code> بدالهم. الـ operators دي موجودة عشان تلتف حوالين إن الـ observables مالهاش قيمة حالية.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'signal-state',
  badge:{en:'State, scaled up', ar:'الحالة، لما تكبر'},
  title:{en:'Signal-based state management', ar:'إدارة الحالة بالـ signals'},
  lead:{
    en:`A service with a private writable signal and public computed views is a complete state solution for most applications. This is what it looks like when you grow it — <b>derived state, entity collections, optimistic updates and undo</b> — before you ever consider a library.`,
    ar:`service فيها signal خاصة قابلة للكتابة وعروض computed عامة — دي حل حالة كامل لمعظم التطبيقات. وده شكلها لما تكبر — <b>حالة مشتقة، ومجموعات entities، وتحديثات متفائلة، وتراجع</b> — قبل ما تفكر في مكتبة أصلاً.`
  },
  nodes:[
    [{id:'store',role:{en:'one writable source',ar:'مصدر واحد قابل للكتابة'},file:'todos.store',ext:'.ts',kind:'svc'}],
    [{id:'derived',role:{en:'computed views',ar:'عروض محسوبة'},file:'computed',ext:'',kind:'ts'},
     {id:'cmp',role:{en:'reads only',ar:'قراية بس'},file:'todo-list',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'store',to:'derived',a:'bottom',b:'top',label:'computed()',packet:'filtered'},
    {id:'e2',from:'derived',to:'cmp',a:'right',b:'left',label:'visible()',packet:'Todo[]'},
    {id:'e3',from:'cmp',to:'store',a:'top',b:'right',label:'toggle(id)',packet:'update'}
  ],
  files:[
    {id:'store',name:'todos.store.ts',lang:'ts',tag:{en:'the whole pattern',ar:'النمط كامل'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class TodosStore {",
      "  // ── ONE writable source of truth ──────────────────────────",
      "  private readonly _todos  = signal<Todo[]>([]);",
      "  private readonly _filter = signal<'all' | 'open' | 'done'>('all');",
      "  private readonly _busy   = signal(false);",
      "",
      "  // ── public read-only views ────────────────────────────────",
      "  readonly todos  = this._todos.asReadonly();",
      "  readonly filter = this._filter.asReadonly();",
      "  readonly busy   = this._busy.asReadonly();",
      "",
      "  // ── everything else is DERIVED, never stored ──────────────",
      "  readonly visible = computed(() => {",
      "    const f = this._filter();",
      "    return this._todos().filter(t =>",
      "      f === 'all' ? true : f === 'done' ? t.done : !t.done);",
      "  });",
      "  readonly openCount = computed(() => this._todos().filter(t => !t.done).length);",
      "  readonly allDone   = computed(() => this._todos().every(t => t.done));",
      "  readonly isEmpty   = computed(() => this._todos().length === 0);",
      "",
      "  // ── the only ways to change it ────────────────────────────",
      "  setFilter(f: 'all' | 'open' | 'done') { this._filter.set(f); }",
      "",
      "  toggle(id: string) {",
      "    this._todos.update(l => l.map(t =>",
      "      t.id === id ? { ...t, done: !t.done } : t));",
      "  }",
      "",
      "  add(title: string) {",
      "    this._todos.update(l => [{ id: crypto.randomUUID(), title, done: false }, ...l]);",
      "  }",
      "}"]},
    {id:'entity',name:'entities: a Map beats an array',lang:'ts',tag:{en:'when it grows',ar:'لما تكبر'},code:[
      "// finding by id in an array is O(n). with 5,000 rows and a",
      "// computed that runs on every keystroke, that is felt.",
      "",
      "private readonly _byId = signal<ReadonlyMap<string, Order>>(new Map());",
      "",
      "readonly all = computed(() => [...this._byId().values()]);",
      "readonly one = (id: string) => computed(() => this._byId().get(id));",
      "",
      "upsert(order: Order) {",
      "  this._byId.update(m => new Map(m).set(order.id, order));",
      "}",
      "",
      "remove(id: string) {",
      "  this._byId.update(m => { const n = new Map(m); n.delete(id); return n; });",
      "}",
      "",
      "// NOTE: a NEW Map every time. mutating the existing one changes",
      "// nothing on screen, exactly like push() on an array."]},
    {id:'linked',name:'linkedSignal — writable, but derived',lang:'ts',tag:{en:'the missing piece',ar:'الحتة الناقصة'},code:[
      "// the problem: a selection that must RESET when the list changes,",
      "// but that the user can also change themselves.",
      "// computed() is read-only. signal() does not react. linkedSignal does both.",
      "",
      "readonly products = input.required<Product[]>();",
      "",
      "readonly selected = linkedSignal(() => this.products()[0]?.id ?? null);",
      "",
      "// - reads like a computed: new products → resets to the first one",
      "// - writes like a signal:  selected.set('p-42') works",
      "//",
      "// before linkedSignal this needed an effect writing a signal,",
      "// which is exactly the anti-pattern the effects chapter warns about."]},
    {id:'optim',name:'optimistic updates and undo',lang:'ts',tag:{en:'feels instant',ar:'بيحس إنه فوري'},code:[
      "async toggleOptimistic(id: string) {",
      "  const before = this._todos();          // snapshot for the rollback",
      "",
      "  this.toggle(id);                       // 1. update the screen NOW",
      "",
      "  try {",
      "    await firstValueFrom(this.api.toggle(id));   // 2. tell the server",
      "  } catch {",
      "    this._todos.set(before);             // 3. roll back on failure",
      "    this.toasts.show('Could not save that. Put it back.');",
      "  }",
      "}",
      "",
      "// undo is the same snapshot idea, kept in a small stack:",
      "private readonly history = signal<Todo[][]>([]);",
      "",
      "private commit(next: Todo[]) {",
      "  this.history.update(h => [...h.slice(-19), this._todos()]);",
      "  this._todos.set(next);",
      "}",
      "",
      "undo() {",
      "  const h = this.history();",
      "  if (!h.length) return;",
      "  this._todos.set(h[h.length - 1]);",
      "  this.history.update(x => x.slice(0, -1));",
      "}"]},
    {id:'cmp',name:'todo-list.component.ts',lang:'ts',tag:{en:'components stay dumb',ar:'الـ components بتفضل بسيطة'},code:[
      "export class TodoList {",
      "  readonly store = inject(TodosStore);",
      "",
      "  // no local copy of the list. no ngOnInit. no subscription.",
      "  // the component reads and calls methods — that is all.",
      "}",
      "",
      "// template:",
      "//   @for (t of store.visible(); track t.id) {",
      "//     <li (click)=\"store.toggle(t.id)\">{{ t.title }}</li>",
      "//   }",
      "//   <p>{{ store.openCount() }} left</p>"]}
  ],
  steps:[
    {t:{en:'One writable signal per genuinely independent piece of state, kept <code class="i">private</code>. Everything a component can reach is <code class="i">readonly</code> or a method.',
        ar:'signal واحدة قابلة للكتابة لكل جزء حالة مستقل فعلاً، ومحفوظة <code class="i">private</code>. وأي حاجة الـ component يقدر يوصلها تبقى <code class="i">readonly</code> أو method.'},
     hl:{store:[4,5,9,10]}},
    {t:{en:'Everything computable is <code class="i">computed</code>. <code class="i">visible</code>, <code class="i">openCount</code>, <code class="i">allDone</code> are never stored — so they can never disagree with the list.',
        ar:'وأي حاجة تقدر تتحسب بتبقى <code class="i">computed</code>. <code class="i">visible</code> و<code class="i">openCount</code> و<code class="i">allDone</code> عمرهم ما بيتخزنوا — فمستحيل يختلفوا مع اللستة.'},
     edge:'e1',hl:{store:[14,19,20,21]}},
    {t:{en:'The component holds <b>no</b> copy of the data. It reads the store and calls its methods — which is why two components on screen can never disagree.',
        ar:'الـ component <b>مش</b> ماسك أي نسخة من الداتا. بيقرا الـ store وينادي methods بتاعته — وعشان كده مستحيل اتنين components على الشاشة يختلفوا.'},
     edge:'e2',hl:{cmp:[2,4,5]}},
    {t:{en:'Past a few thousand rows, switch the collection to a <code class="i">Map</code> keyed by id — and remember to build a <b>new</b> Map, exactly as you build a new array.',
        ar:'وبعد كام ألف صف، حوّل المجموعة لـ <code class="i">Map</code> بمفتاح id — وافتكر تبني Map <b>جديدة</b>، بالظبط زي ما بتبني array جديدة.'},
     hl:{entity:[4,9,10,17,18]}},
    {t:{en:'<code class="i">linkedSignal</code> covers the case <code class="i">computed</code> cannot: a value that is derived <b>and</b> writable. It removes the last common reason to write a signal inside an effect.',
        ar:'<code class="i">linkedSignal</code> بتغطي الحالة اللي <code class="i">computed</code> مش بتغطيها: قيمة مشتقة <b>و</b>قابلة للكتابة. وبتشيل آخر سبب شائع إنك تكتب signal جوه effect.'},
     edge:'e3',hl:{linked:[7,9,10,12,13]}},
    {t:{en:'Optimistic updates are the same store with a snapshot: change the screen first, tell the server, and roll back if it refuses.',
        ar:'والتحديثات المتفائلة هي نفس الـ store مع لقطة: غيّر الشاشة الأول، وقول للسيرفر، وارجع لو رفض.'},
     hl:{optim:[2,4,7,8,9]}}
  ],
  example:[
    {
      name:{en:'When this stops being enough', ar:'إمتى دي تبطّل تكفي'},
      what:{
        en:'A plain signal store handles the overwhelming majority of applications. These are the specific signals that you have outgrown it — not a feeling that the app is "big", but concrete, nameable problems.',
        ar:'الـ signal store العادي بيتعامل مع الغالبية العظمى من التطبيقات. ودي العلامات المحددة إنك كبرت عليه — مش إحساس إن التطبيق «كبير»، لكن مشاكل ملموسة وليها أسماء.'},
      files:[
        {id:'gr1',name:'the honest checklist',lang:'ts',tag:{en:'four signs',ar:'أربع علامات'},code:[
          "// 1. YOU CANNOT ANSWER 'WHY DID THIS CHANGE?'",
          "//    Twelve places call store.set() and you cannot trace which one.",
          "//    → you want actions and a devtools timeline.",
          "",
          "// 2. YOU NEED TIME TRAVEL OR REAL UNDO ACROSS THE WHOLE APP",
          "//    Not one list — every screen, restorable to any past point.",
          "",
          "// 3. THE SAME SERVER ENTITY IS CACHED IN FIVE STORES",
          "//    …and they drift. You want one normalised entity cache.",
          "",
          "// 4. TWENTY DEVELOPERS, AND EVERY STORE LOOKS DIFFERENT",
          "//    The value of a library here is the ENFORCED CONVENTION,",
          "//    not the features.",
          "",
          "// none of these? a signal store is not a compromise —",
          "// it is the correct answer, and it has no bundle cost."]}
      ]
    }
  ],
  gotchas:[
    {en:'Keep the writable signal <code class="i">private</code> and expose <code class="i">asReadonly()</code>. Otherwise any component can <code class="i">.set()</code> your state and you lose every guarantee.',
     ar:'خلي الـ signal القابلة للكتابة <code class="i">private</code> واعرض <code class="i">asReadonly()</code>. غير كده أي component يقدر يعمل <code class="i">.set()</code> لحالتك وبتضيّع كل الضمانات.'},
    {en:'Never store what you can derive. Two fields that must agree will eventually disagree — the only fix is not to have two.',
     ar:'عمرك ما تخزّن حاجة تقدر تشتقها. حقلين لازم يتفقوا هيختلفوا في الآخر — والحل الوحيد إنك متعملش اتنين.'},
    {en:'Immutability is not optional. <code class="i">update(l =&gt; { l.push(x); return l; })</code> returns the same reference and nothing re-renders.',
     ar:'عدم التغيير مش اختياري. <code class="i">update(l =&gt; { l.push(x); return l; })</code> بترجّع نفس الـ reference ومفيش حاجة بتترسم من الأول.'},
    {en:'Do not put a <code class="i">signal</code> inside another signal&rsquo;s value. Nested signals make dependency tracking unpredictable — keep the state flat and derive shapes with <code class="i">computed</code>.',
     ar:'متحطش <code class="i">signal</code> جوه قيمة signal تانية. الـ signals المتداخلة بتخلي تتبع الاعتماديات غير متوقع — خلي الحالة مسطحة واشتق الأشكال بـ <code class="i">computed</code>.'},
    {en:'A store per feature beats one giant <code class="i">AppStore</code>. Global does not mean singular — <code class="i">CartStore</code>, <code class="i">SessionStore</code>, <code class="i">FiltersStore</code>.',
     ar:'store لكل فيتشر أحسن من <code class="i">AppStore</code> واحد عملاق. «عام» مش معناها «واحد» — <code class="i">CartStore</code> و<code class="i">SessionStore</code> و<code class="i">FiltersStore</code>.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'global-state',
  badge:{en:'Libraries', ar:'المكتبات'},
  title:{en:'NgRx, SignalStore and friends', ar:'NgRx و SignalStore وأخواتهم'},
  lead:{
    en:`State libraries buy you two things: an <b>enforced convention</b> across a large team, and a <b>devtools timeline</b> that answers “why did this change?”. They cost bundle size and ceremony. Know what you are buying before you adopt one — and know that most apps genuinely do not need it.`,
    ar:`مكتبات الحالة بتشتري لك حاجتين: <b>convention مفروض</b> على فريق كبير، و<b>خط زمني في الـ devtools</b> بيجاوب على «ليه ده اتغير؟». وتمنهم حجم bundle ومراسم. اعرف بتشتري إيه قبل ما تعتمد واحدة — واعرف إن معظم التطبيقات فعلاً مش محتاجاها.`
  },
  nodes:[
    [{id:'cmp',role:{en:'dispatches an action',ar:'بيبعت action'},file:'component',ext:'.ts',kind:'ts'}],
    [{id:'store',role:{en:'holds the state',ar:'ماسك الحالة'},file:'store',ext:'.ts',kind:'svc'},
     {id:'fx',role:{en:'talks to the server',ar:'بيكلّم السيرفر'},file:'effects',ext:'.ts',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'cmp',to:'store',a:'bottom',b:'top',label:'dispatch(load())',packet:'action'},
    {id:'e2',from:'store',to:'fx',a:'right',b:'left',label:'effect listens',packet:'load'},
    {id:'e3',from:'fx',to:'store',a:'top',b:'right',label:'loadSuccess(data)',packet:'action'}
  ],
  files:[
    {id:'sig',name:'NgRx SignalStore — the modern one',lang:'ts',tag:{en:'start here',ar:'ابدأ من هنا'},code:[
      "import { signalStore, withState, withComputed, withMethods, patchState }",
      "  from '@ngrx/signals';",
      "",
      "export const TodosStore = signalStore(",
      "  { providedIn: 'root' },",
      "",
      "  withState({ todos: [] as Todo[], filter: 'all' as Filter, busy: false }),",
      "",
      "  withComputed(({ todos, filter }) => ({",
      "    visible: computed(() => todos().filter(t =>",
      "      filter() === 'all' ? true : filter() === 'done' ? t.done : !t.done)),",
      "    openCount: computed(() => todos().filter(t => !t.done).length),",
      "  })),",
      "",
      "  withMethods((store, api = inject(TodoApi)) => ({",
      "    setFilter(filter: Filter) { patchState(store, { filter }); },",
      "",
      "    async load() {",
      "      patchState(store, { busy: true });",
      "      const todos = await firstValueFrom(api.getAll());",
      "      patchState(store, { todos, busy: false });",
      "    },",
      "  })),",
      ");",
      "",
      "// usage is identical to a hand-written signal store:",
      "//   readonly store = inject(TodosStore);",
      "//   store.visible()   store.openCount()   store.load()"]},
    {id:'ngrx',name:'classic NgRx — the Redux one',lang:'ts',tag:{en:'you will meet it',ar:'هتقابله'},code:[
      "// 1. ACTIONS — every possible event, named",
      "export const loadTodos        = createAction('[Todos] Load');",
      "export const loadTodosSuccess = createAction('[Todos] Load Success',",
      "                                  props<{ todos: Todo[] }>());",
      "",
      "// 2. REDUCER — a pure function: (state, action) => new state",
      "export const todosReducer = createReducer(",
      "  { todos: [] as Todo[], busy: false },",
      "  on(loadTodos, s => ({ ...s, busy: true })),",
      "  on(loadTodosSuccess, (s, { todos }) => ({ ...s, todos, busy: false })),",
      ");",
      "",
      "// 3. SELECTORS — memoised reads",
      "export const selectOpenCount = createSelector(",
      "  selectTodos, todos => todos.filter(t => !t.done).length);",
      "",
      "// 4. EFFECTS — where side effects live",
      "export const loadEffect = createEffect(",
      "  (actions$ = inject(Actions), api = inject(TodoApi)) =>",
      "    actions$.pipe(",
      "      ofType(loadTodos),",
      "      switchMap(() => api.getAll().pipe(",
      "        map(todos => loadTodosSuccess({ todos })))),",
      "    ),",
      "  { functional: true });",
      "",
      "// four files and ~40 lines for what a signal store does in 10.",
      "// what you bought: a full devtools timeline of every action."]},
    {id:'cmp',name:'reading it from a component',lang:'ts',tag:{en:'both styles',ar:'الأسلوبين'},code:[
      "// SignalStore — it IS signals",
      "export class TodoList {",
      "  readonly store = inject(TodosStore);",
      "  // template:  {{ store.openCount() }}",
      "}",
      "",
      "// classic NgRx — selectors, as signals",
      "export class TodoListClassic {",
      "  private readonly store = inject(Store);",
      "",
      "  readonly todos = this.store.selectSignal(selectTodos);",
      "  readonly open  = this.store.selectSignal(selectOpenCount);",
      "",
      "  ngOnInit() { this.store.dispatch(loadTodos()); }",
      "  toggle(id: string) { this.store.dispatch(toggleTodo({ id })); }",
      "}"]},
    {id:'pick',name:'choosing honestly',lang:'ts',tag:{en:'the real advice',ar:'النصيحة الحقيقية'},code:[
      "// PLAIN SIGNAL SERVICE     0 kB    — the default. most apps.",
      "//   → start here. always. you can migrate later.",
      "",
      "// NGRX SIGNALSTORE      ~ 8 kB    — convention, minimal ceremony",
      "//   → a team that wants one agreed shape for every store",
      "",
      "// NGRX STORE (Redux)    ~ 25 kB   — actions, devtools, time travel",
      "//   → large teams, complex flows, or you genuinely need the timeline",
      "",
      "// NGXS                  ~ 15 kB   — class-based, less boilerplate",
      "//   → smaller community; fine, but check its release cadence",
      "",
      "// THE HONEST ADVICE:",
      "// do not adopt a state library because the app 'feels big'.",
      "// adopt it when you can name the specific problem it solves",
      "// for you. otherwise you have bought ceremony and paid in kB."]}
  ],
  steps:[
    {t:{en:'A component never writes state directly. It <b>dispatches an action</b> — a named description of what happened — and something else decides what that means.',
        ar:'الـ component عمره ما بيكتب في الحالة مباشرة. هو بي<b>بعت action</b> — وصف مسمّى للي حصل — وحاجة تانية بتقرر ده معناه إيه.'},
     edge:'e1',hl:{ngrx:[2,3,4]}},
    {t:{en:'A <b>reducer</b> is a pure function taking the old state and the action and returning new state. Pure means testable, and means the devtools can replay it.',
        ar:'الـ <b>reducer</b> دالة نضيفة بتاخد الحالة القديمة والـ action وترجّع حالة جديدة. ونضيفة معناها قابلة للاختبار، ومعناها الـ devtools تقدر تعيدها.'},
     hl:{ngrx:[7,8,9,10]}},
    {t:{en:'Anything touching the outside world lives in an <b>effect</b>: it listens for one action, calls the server, and dispatches another action with the result.',
        ar:'وأي حاجة بتلمس العالم الخارجي بتعيش في <b>effect</b>: بيسمع لـ action، وينادي السيرفر، ويبعت action تاني بالنتيجة.'},
     edge:'e2',hl:{ngrx:[18,20,21,22,23]}},
    {t:{en:'That round trip is what you are buying: every change is a named event in a timeline you can scrub through. It is also why it is four files instead of one.',
        ar:'والرحلة دي هي اللي بتشتريها: كل تغيير بيبقى حدث مسمّى في خط زمني تقدر تمشي فيه. وهي كمان سبب إنها أربع ملفات بدل واحد.'},
     edge:'e3',hl:{ngrx:[27,28]}},
    {t:{en:'<b>SignalStore</b> keeps the convention and drops most of the ceremony. If you want a library, this is where to start — and a plain signal service is where to start if you do not.',
        ar:'<b>SignalStore</b> بيحافظ على الـ convention ويشيل معظم المراسم. ولو عايز مكتبة، ابدأ من هنا — ولو مش عايز، ابدأ بـ signal service عادية.'},
     hl:{sig:[4,7,9,15],pick:[1,2,14,15,16]}},
    {t:{en:'From a component both styles end up as signals — which is why migrating between them later is genuinely cheap.',
        ar:'ومن ناحية الـ component الأسلوبين بيخلصوا signals — وعشان كده التحويل بينهم بعدين رخيص فعلاً.'},
     hl:{cmp:[2,3,11,12,14]}}
  ],
  example:[
    {
      name:{en:'The same feature, three ways', ar:'نفس الفيتشر، بتلات طرق'},
      what:{
        en:'Loading a list and showing a count. Ten lines, fourteen, or forty — and the forty buy you a devtools timeline. That is the entire trade, stated plainly.',
        ar:'تحميل لستة وعرض عدد. عشر سطور، أو أربعتاشر، أو أربعين — والأربعين بيشتروا لك خط زمني في الـ devtools. وده كل المقايضة، بصراحة.'},
      files:[
        {id:'x1',name:'plain signals — 10 lines',lang:'ts',tag:{en:'0 kB',ar:'0 كيلوبايت'},code:[
          "@Injectable({ providedIn: 'root' })",
          "export class TodosStore {",
          "  private readonly _todos = signal<Todo[]>([]);",
          "  readonly todos = this._todos.asReadonly();",
          "  readonly openCount = computed(() => this._todos().filter(t => !t.done).length);",
          "",
          "  async load() {",
          "    this._todos.set(await firstValueFrom(inject(TodoApi).getAll()));",
          "  }",
          "}"]},
        {id:'x2',name:'SignalStore — 14 lines',lang:'ts',tag:{en:'~8 kB',ar:'~8 كيلوبايت'},code:[
          "export const TodosStore = signalStore(",
          "  { providedIn: 'root' },",
          "  withState({ todos: [] as Todo[] }),",
          "  withComputed(({ todos }) => ({",
          "    openCount: computed(() => todos().filter(t => !t.done).length),",
          "  })),",
          "  withMethods((store, api = inject(TodoApi)) => ({",
          "    async load() {",
          "      patchState(store, { todos: await firstValueFrom(api.getAll()) });",
          "    },",
          "  })),",
          ");"]}
      ]
    }
  ],
  gotchas:[
    {en:'Do not adopt a state library because the app “feels big”. Adopt it when you can <b>name</b> the problem it solves — usually “I cannot trace why this changed”.',
     ar:'متعتمدش مكتبة حالة عشان التطبيق «حاسس إنه كبير». اعتمدها لما تقدر ت<b>سمّي</b> المشكلة اللي بتحلها — وغالباً بتبقى «مش عارف أتتبع ليه ده اتغير».'},
    {en:'Never put derived data in the store. Compute it in a selector or a <code class="i">computed</code> — a stored total is a stored bug.',
     ar:'عمرك ما تحط داتا مشتقة في الـ store. احسبها في selector أو <code class="i">computed</code> — الإجمالي المخزّن ده باج مخزّن.'},
    {en:'Reducers must be <b>pure</b>: no HTTP, no <code class="i">Date.now()</code>, no random. All of that belongs in an effect, or devtools replay lies to you.',
     ar:'الـ reducers لازم تكون <b>نضيفة</b>: مفيش HTTP ولا <code class="i">Date.now()</code> ولا عشوائية. كل ده مكانه الـ effects، وإلا إعادة الـ devtools هتكدب عليك.'},
    {en:'Not all state is global. Form drafts, open/closed panels and hover state belong in the component — putting them in a global store is pure overhead.',
     ar:'مش كل حالة عامة. مسودات الفورمز والبانلز المفتوحة وحالة الـ hover مكانها الـ component — وحطها في store عام ده حِمل زيادة وبس.'},
    {en:'Migrating from a plain signal service to SignalStore later is genuinely easy — the public shape is the same. That is a strong reason to start simple.',
     ar:'التحويل من signal service عادية لـ SignalStore بعدين سهل فعلاً — الشكل العام واحد. وده سبب قوي إنك تبدأ بسيط.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'dynamic-components',
  badge:{en:'Runtime rendering', ar:'الرسم وقت التشغيل'},
  title:{en:'Creating components at runtime', ar:'إنشاء components وقت التشغيل'},
  lead:{
    en:`Sometimes you do not know which component to render until the data arrives — a CMS block, a plugin, a dashboard widget, a dialog. Two tools do this: <code>NgComponentOutlet</code> for the declarative case, and <code>ViewContainerRef</code> when you need the instance back.`,
    ar:`أحياناً مش بتعرف ترسم أنهي component غير لما الداتا توصل — بلوك CMS، أو plugin، أو ويدجت داشبورد، أو dialog. وأداتين بيعملوا كده: <code>NgComponentOutlet</code> للحالة الإعلانية، و<code>ViewContainerRef</code> لما تحتاج النسخة نفسها.`
  },
  nodes:[
    [{id:'map',role:{en:'type → component',ar:'نوع ← component'},file:'widget.registry',ext:'.ts',kind:'cfg'}],
    [{id:'host',role:{en:'picks at runtime',ar:'بيختار وقت التشغيل'},file:'dashboard',ext:'.ts',kind:'ts'},
     {id:'vcr',role:{en:'creates imperatively',ar:'بينشئ بالأوامر'},file:'ViewContainerRef',ext:'',kind:'svc'}]
  ],
  edges:[
    {id:'e1',from:'map',to:'host',a:'bottom',b:'top',label:'registry[type]',packet:'ChartWidget'},
    {id:'e2',from:'host',to:'vcr',a:'right',b:'left',label:'createComponent()',packet:'instance'},
    {id:'e3',from:'vcr',to:'host',a:'top',b:'right',label:'ref.setInput()',packet:'config'}
  ],
  files:[
    {id:'map',name:'widget.registry.ts',lang:'ts',tag:{en:'the lookup',ar:'الجدول'},code:[
      "import { Type } from '@angular/core';",
      "",
      "// a plain object mapping a string from the server to a class.",
      "// the dynamic import keeps each widget in its own chunk.",
      "export const WIDGETS: Record<string, () => Promise<Type<unknown>>> = {",
      "  chart:   () => import('./widgets/chart').then(m => m.ChartWidget),",
      "  table:   () => import('./widgets/table').then(m => m.TableWidget),",
      "  metric:  () => import('./widgets/metric').then(m => m.MetricWidget),",
      "};"]},
    {id:'outlet',name:'NgComponentOutlet — the easy way',lang:'ts',tag:{en:'prefer this',ar:'فضّل دي'},code:[
      "import { NgComponentOutlet } from '@angular/common';",
      "",
      "@Component({",
      "  selector: 'app-widget-host',",
      "  imports: [NgComponentOutlet],",
      "  template: `",
      "    @if (cmp(); as c) {",
      "      <ng-container *ngComponentOutlet=\"c; inputs: inputs()\" />",
      "    } @else {",
      "      <div class=\"skeleton\"></div>",
      "    }",
      "  `,",
      "})",
      "export class WidgetHost {",
      "  readonly config = input.required<WidgetConfig>();",
      "",
      "  readonly cmp = signal<Type<unknown> | null>(null);",
      "  readonly inputs = computed(() => ({ config: this.config() }));",
      "",
      "  constructor() {",
      "    effect(async () => {",
      "      const load = WIDGETS[this.config().type];",
      "      this.cmp.set(load ? await load() : null);",
      "    });",
      "  }",
      "}"]},
    {id:'vcr',name:'ViewContainerRef — when you need the instance',lang:'ts',tag:{en:'imperative',ar:'بالأوامر'},code:[
      "export class DialogService {",
      "  private readonly vcr = inject(ViewContainerRef);",
      "  private readonly env = inject(EnvironmentInjector);",
      "",
      "  async open<T>(load: () => Promise<Type<T>>, data: unknown) {",
      "    const Cmp = await load();",
      "",
      "    const ref = this.vcr.createComponent(Cmp, {",
      "      environmentInjector: this.env,",
      "      // providers scoped to just this component instance",
      "      // projectableNodes: [[…]] for <ng-content>",
      "    });",
      "",
      "    // set inputs AFTER creation — this marks it for check",
      "    ref.setInput('data', data);",
      "",
      "    // read outputs — a real EventEmitter/OutputRef on the instance",
      "    const done = firstValueFrom(ref.instance.closed);",
      "",
      "    // it lives until you destroy it, or its container is destroyed.",
      "    done.finally(() => ref.destroy());",
      "",
      "    return done;",
      "  }",
      "}"]},
    {id:'why',name:'which one, and when neither',lang:'ts',tag:{en:'the decision',ar:'القرار'},code:[
      "// @if / @switch          ← the answer 90% of the time.",
      "//   if you can list the components in the template, DO.",
      "//   it is type-checked, tree-shakable and simpler.",
      "",
      "// NgComponentOutlet      ← the set is open, but you only need to RENDER it",
      "//   CMS blocks, plugin widgets, a form built from a JSON schema",
      "",
      "// ViewContainerRef       ← you need the INSTANCE back",
      "//   dialogs, toasts, tooltips — anything you create from a SERVICE",
      "//   rather than from a template, and must later destroy",
      "",
      "// ❌ do NOT reach for these because a template has three branches.",
      "//    dynamic rendering costs you type-checking on inputs and",
      "//    makes the component graph invisible to the compiler."]},
    {id:'host',name:'dashboard.component.html',lang:'html',tag:{en:'usage',ar:'الاستخدام'},code:[
      "@for (w of layout(); track w.id) {",
      "  <div class=\"grid-cell\" [style.grid-area]=\"w.area\">",
      "    <app-widget-host [config]=\"w\" />",
      "  </div>",
      "}",
      "",
      "<!-- the server decides the layout AND which widget types appear.",
      "     adding a new widget type is: write the component, add one",
      "     line to the registry. no change to this template. -->"]}
  ],
  steps:[
    {t:{en:'A registry maps a string the server sent to a component class. Using dynamic <code class="i">import()</code> keeps every widget in its own lazily-loaded chunk.',
        ar:'الـ registry بيربط نص جاي من السيرفر بكلاس component. واستخدام <code class="i">import()</code> الديناميكي بيخلي كل ويدجت في chunk لوحده بيتحمّل عند الحاجة.'},
     hl:{map:[5,6,7,8]}},
    {t:{en:'<code class="i">NgComponentOutlet</code> renders it declaratively. Angular handles creation, input binding and destruction — you never hold a reference.',
        ar:'<code class="i">NgComponentOutlet</code> بيرسمه بشكل إعلاني. وأنجولار بيتولى الإنشاء وربط الـ inputs والتدمير — وإنت عمرك ما بتمسك reference.'},
     edge:'e1',hl:{outlet:[8,18,19]}},
    {t:{en:'When you need the instance — to call a method, to read an output, to destroy it yourself — <code class="i">ViewContainerRef.createComponent()</code> hands you a <code class="i">ComponentRef</code>.',
        ar:'ولما تحتاج النسخة نفسها — عشان تنادي method، أو تقرا output، أو تدمّرها بنفسك — <code class="i">ViewContainerRef.createComponent()</code> بيديك <code class="i">ComponentRef</code>.'},
     edge:'e2',hl:{vcr:[8,9]}},
    {t:{en:'Inputs are set <b>after</b> creation with <code class="i">setInput()</code>, which also marks the component for check. Outputs are real emitters on <code class="i">ref.instance</code>.',
        ar:'الـ inputs بتتحط <b>بعد</b> الإنشاء بـ <code class="i">setInput()</code>، واللي بتعلّم الـ component للفحص كمان. والـ outputs مُصدِرات حقيقية على <code class="i">ref.instance</code>.'},
     edge:'e3',hl:{vcr:[15,18]}},
    {t:{en:'You now own its lifetime. Nothing destroys a manually created component for you — forget <code class="i">ref.destroy()</code> and you have a leak with a DOM node attached.',
        ar:'وإنت دلوقتي مالك عمره. مفيش حاجة بتدمّر component عملته بإيدك — وتنسى <code class="i">ref.destroy()</code> يبقى عندك تسريب ومعاه DOM node.'},
     hl:{vcr:[20,21],why:[1,2,12,13]}},
    {t:{en:'The payoff: the server decides both the layout and which widget types appear, and this template never changes.',
        ar:'والمكسب: السيرفر بيقرر التخطيط وأنهي أنواع ويدجت تظهر، والتمبلت ده عمره ما بيتغير.'},
     hl:{host:[1,3,7,8]}}
  ],
  example:[
    {
      name:{en:'A form built from a JSON schema', ar:'فورم مبني من JSON schema'},
      what:{
        en:'The backend describes the fields; the frontend renders whichever control each field asks for. Adding a new field type is one component and one registry line — with no frontend deploy needed to change a form.',
        ar:'الباك إند بيوصف الحقول؛ والفرونت إند بيرسم الـ control اللي كل حقل طالبه. وإضافة نوع حقل جديد component واحد وسطر واحد في الـ registry — من غير ما تحتاج تنشر الفرونت إند عشان تغيّر فورم.'},
      files:[
        {id:'js1',name:'the schema from the server',lang:'json',tag:{en:'data, not code',ar:'داتا مش كود'},code:[
          "[",
          "  { \"key\": \"email\",   \"type\": \"text\",   \"label\": \"Email\", \"required\": true },",
          "  { \"key\": \"country\", \"type\": \"select\", \"label\": \"Country\", \"options\": [\"EG\",\"DE\"] },",
          "  { \"key\": \"dob\",     \"type\": \"date\",   \"label\": \"Date of birth\" }",
          "]"]},
        {id:'js2',name:'dynamic-form.component.html',lang:'html',tag:{en:'one template, any form',ar:'تمبلت واحد، أي فورم'},code:[
          "<form [formGroup]=\"form\">",
          "  @for (field of schema(); track field.key) {",
          "    <ng-container",
          "      *ngComponentOutlet=\"controlFor(field.type);",
          "                          inputs: { field: field, form: form }\" />",
          "  }",
          "</form>"]}
      ]
    }
  ],
  gotchas:[
    {en:'Prefer <code class="i">&#64;if</code> or <code class="i">&#64;switch</code> whenever you can list the components. Dynamic rendering costs you compile-time input checking.',
     ar:'فضّل <code class="i">&#64;if</code> أو <code class="i">&#64;switch</code> كل ما تقدر تعدّد الـ components. الرسم الديناميكي بيكلفك التحقق من الـ inputs وقت الكمبايل.'},
    {en:'A component you create with <code class="i">createComponent</code> is <b>yours to destroy</b>. Call <code class="i">ref.destroy()</code>, or you leak the component and its DOM.',
     ar:'الـ component اللي بتعمله بـ <code class="i">createComponent</code> <b>إنت اللي بتدمّره</b>. نادي <code class="i">ref.destroy()</code>، وإلا هتسرّب الـ component والـ DOM بتاعه.'},
    {en:'Use <code class="i">ref.setInput()</code>, not <code class="i">ref.instance.x = value</code>. Direct assignment skips change detection and skips <code class="i">input()</code> signals entirely.',
     ar:'استعمل <code class="i">ref.setInput()</code> مش <code class="i">ref.instance.x = value</code>. الإسناد المباشر بيتخطى الـ change detection وبيتخطى الـ <code class="i">input()</code> signals خالص.'},
    {en:'Referencing components only through a registry object can defeat tree shaking. Use dynamic <code class="i">import()</code> so each stays in its own chunk.',
     ar:'الإشارة للـ components من خلال registry بس ممكن تبطّل الـ tree shaking. استعمل <code class="i">import()</code> ديناميكي عشان كل واحد يفضل في chunk لوحده.'},
    {en:'A dynamically created component sits in the <b>injector</b> you gave it, not the DOM position. Pass an <code class="i">environmentInjector</code> or DI lookups may surprise you.',
     ar:'الـ component المعمول ديناميكياً بيقعد في الـ <b>injector</b> اللي إديته له، مش في مكانه في الـ DOM. مرّر <code class="i">environmentInjector</code> وإلا نتايج الـ DI ممكن تفاجئك.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'control-value-accessor',
  badge:{en:'Custom controls', ar:'كنترولز مخصصة'},
  title:{en:'<code>ControlValueAccessor</code>', ar:'<code>ControlValueAccessor</code>'},
  lead:{
    en:`You built a nice star-rating component. Now someone wants it inside a reactive form with <code>formControlName</code> — and it does not work, because Angular has no idea how to read or write your component. <code>ControlValueAccessor</code> is the four-method contract that teaches it.`,
    ar:`عملت component حلو للتقييم بالنجوم. ودلوقتي حد عايزه جوه reactive form بـ <code>formControlName</code> — ومش شغال، لإن أنجولار مش عارف يقرا ولا يكتب في الـ component بتاعك. و<code>ControlValueAccessor</code> هو العقد بأربع methods اللي بيعلّمه.`
  },
  nodes:[
    [{id:'form',role:{en:'owns the value',ar:'صاحب القيمة'},file:'profile.component',ext:'.ts',kind:'ts'}],
    [{id:'cva',role:{en:'implements the contract',ar:'بينفّذ العقد'},file:'star-rating',ext:'.ts',kind:'ts'},
     {id:'ui',role:{en:'the actual UI',ar:'الواجهة نفسها'},file:'star-rating',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'form',to:'cva',a:'bottom',b:'top',label:'writeValue(4)',packet:'4'},
    {id:'e2',from:'cva',to:'ui',a:'right',b:'left',label:'value.set(4)',packet:'★★★★☆'},
    {id:'e3',from:'ui',to:'form',a:'left',b:'left',label:'onChange(5)',packet:'5'}
  ],
  files:[
    {id:'cva',name:'star-rating.component.ts',lang:'ts',tag:{en:'the four methods',ar:'الأربع methods'},code:[
      "import { ControlValueAccessor, NG_VALUE_ACCESSOR } from '@angular/forms';",
      "",
      "@Component({",
      "  selector: 'app-star-rating',",
      "  templateUrl: './star-rating.component.html',",
      "  providers: [{",
      "    provide: NG_VALUE_ACCESSOR,",
      "    useExisting: forwardRef(() => StarRating),   // forwardRef: the class",
      "    multi: true,                                  // is not defined yet",
      "  }],",
      "})",
      "export class StarRating implements ControlValueAccessor {",
      "  readonly max = input(5);",
      "  protected readonly value = signal(0);",
      "  protected readonly disabled = signal(false);",
      "",
      "  // Angular hands us these two. we call them; we never define them.",
      "  private onChange: (v: number) => void = () => {};",
      "  protected onTouched: () => void = () => {};   // the template calls it",
      "",
      "  // 1. the FORM writes INTO us (initial value, patchValue, reset)",
      "  writeValue(v: number | null) { this.value.set(v ?? 0); }",
      "",
      "  // 2. Angular gives us the callback to report changes",
      "  registerOnChange(fn: (v: number) => void) { this.onChange = fn; }",
      "",
      "  // 3. and the one to report 'the user has been here and left'",
      "  registerOnTouched(fn: () => void) { this.onTouched = fn; }",
      "",
      "  // 4. the form tells us it was disabled",
      "  setDisabledState(isDisabled: boolean) { this.disabled.set(isDisabled); }",
      "",
      "  // and this is us, reporting OUT",
      "  pick(n: number) {",
      "    if (this.disabled()) return;",
      "    this.value.set(n);",
      "    this.onChange(n);      // ← the form control now holds n",
      "    this.onTouched();      // ← and is marked touched",
      "  }",
      "}"]},
    {id:'ui',name:'star-rating.component.html',lang:'html',tag:{en:'ordinary markup',ar:'ماركب عادي'},code:[
      "<div role=\"radiogroup\" [attr.aria-disabled]=\"disabled()\">",
      "  @for (n of [1,2,3,4,5].slice(0, max()); track n) {",
      "    <button type=\"button\"",
      "            role=\"radio\"",
      "            [attr.aria-checked]=\"value() === n\"",
      "            [attr.aria-label]=\"n + ' stars'\"",
      "            [disabled]=\"disabled()\"",
      "            [class.on]=\"n <= value()\"",
      "            (click)=\"pick(n)\"",
      "            (blur)=\"onTouched()\">★</button>",
      "  }",
      "</div>"]},
    {id:'form',name:'profile.component.ts',lang:'ts',tag:{en:'now it just works',ar:'دلوقتي شغالة'},code:[
      "export class Profile {",
      "  readonly form = this.fb.nonNullable.group({",
      "    name:   ['', Validators.required],",
      "    rating: [0, [Validators.required, Validators.min(1)]],",
      "  });",
      "}",
      "",
      "// template — indistinguishable from a native <input>:",
      "//",
      "//   <form [formGroup]=\"form\">",
      "//     <input formControlName=\"name\">",
      "//     <app-star-rating formControlName=\"rating\" [max]=\"5\" />",
      "//   </form>",
      "//",
      "// patchValue, reset, disable, validators, touched, dirty —",
      "// all of it now works on your component for free."]},
    {id:'valid',name:'bonus: validate yourself',lang:'ts',tag:{en:'NG_VALIDATORS',ar:'NG_VALIDATORS'},code:[
      "// a control can also carry its own validation rules,",
      "// so the consumer does not have to know them.",
      "",
      "providers: [",
      "  { provide: NG_VALUE_ACCESSOR, useExisting: forwardRef(() => Iban), multi: true },",
      "  { provide: NG_VALIDATORS,     useExisting: forwardRef(() => Iban), multi: true },",
      "]",
      "",
      "export class Iban implements ControlValueAccessor, Validator {",
      "  validate(c: AbstractControl): ValidationErrors | null {",
      "    return isValidIban(c.value) ? null : { iban: true };",
      "  }",
      "}",
      "",
      "// <app-iban formControlName=\"account\" />  is now self-validating."]}
  ],
  steps:[
    {t:{en:'The <code class="i">NG_VALUE_ACCESSOR</code> provider is the registration. <code class="i">multi: true</code> because it is a collection, and <code class="i">forwardRef</code> because the class is not defined yet on that line.',
        ar:'الـ provider بتاع <code class="i">NG_VALUE_ACCESSOR</code> هو التسجيل. و<code class="i">multi: true</code> لإنها مجموعة، و<code class="i">forwardRef</code> لإن الكلاس لسه مش معرّف في السطر ده.'},
     hl:{cva:[6,7,8,9]}},
    {t:{en:'<code class="i">writeValue</code> is the form pushing a value <b>into</b> you — on init, on <code class="i">patchValue</code>, on <code class="i">reset</code>. Store it; do not report it back.',
        ar:'<code class="i">writeValue</code> هي الفورم بيدفع قيمة <b>جواك</b> — عند البداية، وعند <code class="i">patchValue</code>، وعند <code class="i">reset</code>. خزّنها؛ ومتبلّغش عنها تاني.'},
     edge:'e1',hl:{cva:[21,22]}},
    {t:{en:'<code class="i">registerOnChange</code> and <code class="i">registerOnTouched</code> hand you two callbacks. You store them and call them later — you never write their bodies.',
        ar:'<code class="i">registerOnChange</code> و<code class="i">registerOnTouched</code> بيدوك callbackين. بتخزّنهم وتناديهم بعدين — وعمرك ما بتكتب اللي جواهم.'},
     hl:{cva:[17,18,19,25,28]}},
    {t:{en:'When the user acts, you call <code class="i">onChange(value)</code>. That is the only way the form control learns anything — and forgetting it is why a control stays permanently empty.',
        ar:'ولما المستخدم يتصرف، بتنادي <code class="i">onChange(value)</code>. ودي الطريقة الوحيدة اللي الـ form control بيعرف بيها أي حاجة — ونسيانها هو سبب إن الـ control يفضل فاضي للأبد.'},
     edge:'e3',hl:{cva:[34,36,37]}},
    {t:{en:'<code class="i">setDisabledState</code> is the one everybody forgets. Without it, <code class="i">form.disable()</code> silently does nothing to your control and the user can still edit it.',
        ar:'<code class="i">setDisabledState</code> هي اللي الكل بينساها. ومن غيرها، <code class="i">form.disable()</code> مبتعملش حاجة للـ control بتاعك والمستخدم لسه يقدر يعدّله.'},
     edge:'e2',hl:{cva:[30,31],ui:[7,9]}},
    {t:{en:'And now it is indistinguishable from a native input: <code class="i">formControlName</code>, validators, <code class="i">patchValue</code> and <code class="i">reset</code> all just work.',
        ar:'ودلوقتي مفيش فرق بينه وبين input عادي: <code class="i">formControlName</code> والفاليديتورز و<code class="i">patchValue</code> و<code class="i">reset</code> كلهم شغالين.'},
     hl:{form:[3,4,11,12]}},
    {t:{en:'A control can also carry its own validation, so nobody using it has to know the rules.',
        ar:'والـ control يقدر كمان يشيل الـ validation بتاعه، فمحدش بيستخدمه محتاج يعرف القواعد.'},
     hl:{valid:[5,6,10,11]}}
  ],
  example:[
    {
      name:{en:'A currency input that stores cents', ar:'خانة عملة بتخزّن قروش'},
      what:{
        en:'The user sees "1,250.00"; the form holds 125000 as an integer, because storing money as a float is how rounding bugs get into invoices. The component is the translation layer, and every consumer gets it for free.',
        ar:'المستخدم بيشوف «1,250.00»؛ والفورم ماسك 125000 كعدد صحيح، لإن تخزين الفلوس كـ float هو إزاي باجات التقريب بتدخل الفواتير. والـ component هو طبقة الترجمة، وكل مستخدم بياخدها ببلاش.'},
      files:[
        {id:'cu1',name:'money-input.component.ts',lang:'ts',tag:{en:'the translation',ar:'الترجمة'},code:[
          "export class MoneyInput implements ControlValueAccessor {",
          "  protected readonly text = signal('');",
          "  private onChange: (v: number) => void = () => {};",
          "  private onTouched = () => {};",
          "",
          "  // form (cents) → screen (formatted)",
          "  writeValue(cents: number | null) {",
          "    this.text.set(cents == null ? '' : (cents / 100).toFixed(2));",
          "  }",
          "",
          "  // screen (typed) → form (cents)",
          "  onInput(raw: string) {",
          "    this.text.set(raw);",
          "    const n = Number(raw.replace(/[^0-9.]/g, ''));",
          "    this.onChange(Number.isFinite(n) ? Math.round(n * 100) : 0);",
          "  }",
          "",
          "  onBlur() {",
          "    this.onTouched();",
          "    const n = Number(this.text().replace(/[^0-9.]/g, ''));",
          "    if (Number.isFinite(n)) this.text.set(n.toFixed(2));   // tidy up",
          "  }",
          "",
          "  registerOnChange(fn: (v: number) => void) { this.onChange = fn; }",
          "  registerOnTouched(fn: () => void) { this.onTouched = fn; }",
          "  setDisabledState(d: boolean) { this.disabled.set(d); }",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'Forgetting to call <code class="i">onChange()</code> is the number one bug: the UI updates, the form control stays empty, and validation never passes.',
     ar:'نسيان نداء <code class="i">onChange()</code> هو الباج رقم واحد: الواجهة بتتحدث، والـ form control بيفضل فاضي، والـ validation عمره ما بينجح.'},
    {en:'Never call <code class="i">onChange()</code> from inside <code class="i">writeValue()</code>. That is the form telling <b>you</b>. Reporting it back makes the form think the <i>user</i> changed it, so <code class="i">patchValue()</code> or <code class="i">reset()</code> leaves the control marked dirty.',
     ar:'عمرك ما تنادي <code class="i">onChange()</code> من جوه <code class="i">writeValue()</code>. دي الفورم بيقول <b>لك</b>. لو بلّغته راجع، الفورم هيفتكر إن <i>المستخدم</i> هو اللي غيّرها، فـ <code class="i">patchValue()</code> أو <code class="i">reset()</code> هيسيبوا الـ control متعلّم إنه dirty.'},
    {en:'Implement <code class="i">setDisabledState</code>. Without it <code class="i">form.disable()</code> has no effect on your control, which is a real accessibility and data-integrity bug.',
     ar:'نفّذ <code class="i">setDisabledState</code>. ومن غيرها <code class="i">form.disable()</code> مالهاش تأثير على الـ control بتاعك، وده باج حقيقي في الإتاحة وسلامة البيانات.'},
    {en:'<code class="i">forwardRef</code> is required because the class is referenced on a line inside its own decorator, before it exists. Omitting it throws at startup.',
     ar:'<code class="i">forwardRef</code> مطلوبة لإن الكلاس بيتذكر في سطر جوه الـ decorator بتاعه، قبل ما يبقى موجود. ونسيانها بترمي ايرور عند التشغيل.'},
    {en:'Call <code class="i">onTouched()</code> on blur, not on change. Otherwise validation messages appear while the person is still typing the first character.',
     ar:'نادي <code class="i">onTouched()</code> عند الـ blur، مش عند التغيير. وإلا رسايل الـ validation هتظهر والشخص لسه بيكتب أول حرف.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'security',
  badge:{en:'Not optional', ar:'مش اختياري'},
  title:{en:'XSS, sanitization and the bypass that is usually a bug', ar:'XSS والتعقيم والتجاوز اللي غالباً باج'},
  lead:{
    en:`Angular escapes everything by default, which is why most Angular apps are not vulnerable. The holes are the places you deliberately opened: <code>[innerHTML]</code>, <code>bypassSecurityTrust*</code>, and a URL you built from user input. <b>Every XSS in an Angular app is a line somebody wrote on purpose.</b>`,
    ar:`أنجولار بيهرّب كل حاجة افتراضياً، وعشان كده معظم تطبيقات أنجولار مش مخترقة. والثغرات هي الأماكن اللي إنت فتحتها بقصد: <code>[innerHTML]</code> و<code>bypassSecurityTrust*</code> وURL بنيته من مدخلات المستخدم. <b>وكل XSS في تطبيق أنجولار هو سطر حد كتبه عن قصد.</b>`
  },
  nodes:[
    [{id:'input',role:{en:'untrusted text',ar:'نص غير موثوق'},file:'user input',ext:'',kind:'cfg'}],
    [{id:'safe',role:{en:'escaped automatically',ar:'بيتهرّب لوحده'},file:'{{ }}',ext:'',kind:'ts'},
     {id:'risk',role:{en:'you opened this',ar:'إنت فتحت دي'},file:'[innerHTML]',ext:'',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'input',to:'safe',a:'bottom',b:'top',label:'interpolation',packet:'&lt;b&gt;'},
    {id:'e2',from:'input',to:'risk',a:'bottom',b:'right',label:'innerHTML',packet:'sanitized'}
  ],
  files:[
    {id:'safe',name:'what Angular does for you',lang:'html',tag:{en:'safe by default',ar:'آمن افتراضياً'},code:[
      "<!-- comment is: <img src=x onerror=alert(1)> -->",
      "",
      "<!-- ✓ SAFE. printed as TEXT. the tag is escaped, nothing runs. -->",
      "<p>{{ comment }}</p>",
      "",
      "<!-- ✓ SAFE. Angular sanitizes: it strips the onerror attribute -->",
      "<div [innerHTML]=\"comment\"></div>",
      "",
      "<!-- ✓ SAFE. a javascript: URL is rewritten to unsafe: -->",
      "<a [href]=\"userLink\">click</a>",
      "",
      "<!-- interpolation can NEVER inject HTML. that single fact is why",
      "     Angular apps are mostly safe without anyone thinking about it. -->"]},
    {id:'risk',name:'the four ways people open a hole',lang:'ts',tag:{en:'audit these',ar:'راجع دول'},code:[
      "// 1. BYPASSING THE SANITIZER",
      "//    the method name is a warning. read it out loud.",
      "this.sanitizer.bypassSecurityTrustHtml(commentFromUser);   // ✗ XSS",
      "",
      "//    only ever legitimate for content YOU control:",
      "this.sanitizer.bypassSecurityTrustResourceUrl(",
      "  'https://www.youtube.com/embed/' + encodeURIComponent(videoId));  // ✓",
      "",
      "// 2. TOUCHING THE DOM DIRECTLY — bypasses Angular entirely",
      "el.nativeElement.innerHTML = userText;   // ✗ no sanitizer involved",
      "document.write(userText);                // ✗",
      "",
      "// 3. BUILDING A TEMPLATE FROM DATA",
      "//    a template compiled from a string is code, not content.",
      "//    never construct one from anything a user typed.",
      "",
      "// 4. TRUSTING THE SERVER",
      "//    'it came from our API' is not a safety property — the API",
      "//    stored whatever some other user typed into it."]},
    {id:'ctx',name:'the sanitization contexts',lang:'ts',tag:{en:'five, not one',ar:'خمسة مش واحد'},code:[
      "// Angular sanitizes DIFFERENTLY depending on where the value lands.",
      "",
      "SecurityContext.HTML           // [innerHTML]        strips scripts/handlers",
      "SecurityContext.STYLE          // [style]            strips expressions",
      "SecurityContext.URL            // [href] [src]       blocks javascript:",
      "SecurityContext.RESOURCE_URL   // <iframe src>       CANNOT be sanitized",
      "SecurityContext.SCRIPT         // <script src>       CANNOT be sanitized",
      "",
      "// RESOURCE_URL and SCRIPT have no safe filter — loading a URL as",
      "// executable code is all-or-nothing. that is why an iframe src",
      "// needs an explicit bypass, and why that bypass needs an allowlist:",
      "",
      "const ALLOWED = ['www.youtube.com', 'player.vimeo.com'];",
      "",
      "embed(url: string) {",
      "  const host = new URL(url).hostname;",
      "  if (!ALLOWED.includes(host)) throw new Error('Not an allowed embed host');",
      "  return this.sanitizer.bypassSecurityTrustResourceUrl(url);",
      "}"]},
    {id:'csrf',name:'CSRF / XSRF and CSP',lang:'ts',tag:{en:'the other two',ar:'الاتنين التانيين'},code:[
      "// XSRF — Angular has built-in support, but it needs the server",
      "// to set a readable cookie. it is on by default with HttpClient.",
      "provideHttpClient(",
      "  withXsrfConfiguration({",
      "    cookieName: 'XSRF-TOKEN',      // the server sets this",
      "    headerName: 'X-XSRF-TOKEN',    // Angular echoes it back",
      "  }),",
      ")",
      "// note: it only attaches the header to same-origin mutating requests.",
      "",
      "// CSP — the strongest protection you can add, set as a header:",
      "//   Content-Security-Policy:",
      "//     default-src 'self';",
      "//     script-src 'self';",
      "//     style-src 'self' 'nonce-{random}';",
      "//     object-src 'none'; base-uri 'self';",
      "//",
      "// Angular supports CSP nonces via the ngCspNonce attribute",
      "// on your <app-root>, or the CSP_NONCE injection token."]},
    {id:'list',name:'the practical checklist',lang:'ts',tag:{en:'before you ship',ar:'قبل ما تنشر'},code:[
      "// grep your codebase for these five. every hit needs a reason.",
      "//",
      "//   bypassSecurityTrust",
      "//   innerHTML",
      "//   nativeElement",
      "//   eval(",
      "//   document.write",
      "//",
      "// and these habits:",
      "//   - never put a token in localStorage if an httpOnly cookie will do",
      "//   - never build a URL by concatenation; use encodeURIComponent",
      "//   - never trust a redirect target that came from a query param",
      "//   - run `npm audit` in CI and actually read it",
      "//   - keep Angular current: security fixes ship in patch releases"]}
  ],
  steps:[
    {t:{en:'Interpolation escapes everything. <code class="i">{{ comment }}</code> can never produce a tag — that one property is why most Angular apps have no XSS at all.',
        ar:'الـ interpolation بتهرّب كل حاجة. <code class="i">{{ comment }}</code> مستحيل تطلّع tag — والخاصية دي لوحدها هي سبب إن معظم تطبيقات أنجولار مفيهاش XSS أصلاً.'},
     edge:'e1',hl:{safe:[3,4,12,13]}},
    {t:{en:'<code class="i">[innerHTML]</code> is still sanitized — Angular parses the HTML and strips scripts and event handlers. It is safer than people assume, but it is a bigger surface than text.',
        ar:'<code class="i">[innerHTML]</code> لسه بيتعقّم — أنجولار بيحلل الـ HTML ويشيل السكريبتات ومعالجات الأحداث. وهو أأمن مما الناس فاكرة، بس مساحته أكبر من النص.'},
     edge:'e2',hl:{safe:[6,7]}},
    {t:{en:'The real holes are the deliberate ones. <code class="i">bypassSecurityTrustHtml</code> on user input <b>is</b> the vulnerability — the method name is telling you so.',
        ar:'والثغرات الحقيقية هي المقصودة. <code class="i">bypassSecurityTrustHtml</code> على مدخلات مستخدم <b>هي</b> الثغرة — واسم الـ method بيقولك كده.'},
     hl:{risk:[1,2,3,9,10]}},
    {t:{en:'<code class="i">RESOURCE_URL</code> and <code class="i">SCRIPT</code> cannot be sanitized at all — loading a URL as code is all or nothing. Bypassing those requires a hostname <b>allowlist</b>.',
        ar:'<code class="i">RESOURCE_URL</code> و<code class="i">SCRIPT</code> مينفعش يتعقّموا خالص — تحميل URL ككود إما كله أو لا شيء. وتجاوزهم محتاج <b>قايمة سماح</b> بأسماء المضيفين.'},
     hl:{ctx:[6,7,9,10,14,17,18]}},
    {t:{en:'Then add the layers Angular cannot add for you: an XSRF token on mutating requests, and a Content-Security-Policy header that makes injected script unusable even if something slips through.',
        ar:'وبعدين ضيف الطبقات اللي أنجولار مش هيضيفها بدالك: توكن XSRF على الريكويستات المعدِّلة، وهيدر Content-Security-Policy بيخلي أي سكريبت محقون بلا فايدة حتى لو حاجة عدّت.'},
     hl:{csrf:[3,4,5,6,12,13,14]}},
    {t:{en:'Before shipping, grep for those five. Every hit needs a reason you could defend out loud.',
        ar:'وقبل النشر، دوّر على الخمسة دول. وكل نتيجة محتاجة سبب تقدر تدافع عنه بصوت عالي.'},
     hl:{list:[3,4,5,6,7,11,12]}}
  ],
  example:[
    {
      name:{en:'Rendering rich text from a CMS, safely', ar:'عرض نص منسّق من CMS، بأمان'},
      what:{
        en:'A CMS legitimately produces HTML you must render. The safe route is to sanitize explicitly and allowlist what you accept — never to bypass because "our editors are trusted". Editors get their accounts phished.',
        ar:'الـ CMS بيطلّع HTML لازم ترسمه فعلاً. والطريق الآمن إنك تعقّم صراحة وتحدد اللي بتقبله — مش إنك تتجاوز لإن «المحررين موثوقين». المحررين حساباتهم بتتسرق.'},
      files:[
        {id:'sf1',name:'safe-html.pipe.ts',lang:'ts',tag:{en:'explicit, not bypassed',ar:'صريح مش متجاوز'},code:[
          "@Pipe({ name: 'safeHtml' })",
          "export class SafeHtml implements PipeTransform {",
          "  private readonly sanitizer = inject(DomSanitizer);",
          "",
          "  transform(value: string | null): string {",
          "    if (!value) return '';",
          "    // sanitize() FILTERS. bypass() does not. use this one.",
          "    return this.sanitizer.sanitize(SecurityContext.HTML, value) ?? '';",
          "  }",
          "}",
          "",
          "// <div [innerHTML]=\"post().body | safeHtml\"></div>"]},
        {id:'sf2',name:'the version that is a CVE',lang:'ts',tag:{en:'never do this',ar:'عمرك ما تعمل كده'},code:[
          "@Pipe({ name: 'trustHtml' })",
          "export class TrustHtml implements PipeTransform {",
          "  transform(value: string) {",
          "    // this disables the sanitizer completely.",
          "    // one stored <img onerror> and every visitor is compromised.",
          "    return this.sanitizer.bypassSecurityTrustHtml(value);",
          "  }",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<code class="i">sanitize()</code> filters; <code class="i">bypassSecurityTrust*()</code> disables the filter. They sound similar and do opposite things — read the name every time.',
     ar:'<code class="i">sanitize()</code> بتفلتر؛ و<code class="i">bypassSecurityTrust*()</code> بتقفل الفلتر. الاسمين شبه بعض وبيعملوا العكس — اقرا الاسم كل مرة.'},
    {en:'Writing to <code class="i">nativeElement.innerHTML</code> skips Angular&rsquo;s sanitizer entirely. Use the binding, or <code class="i">Renderer2</code>, or sanitize by hand.',
     ar:'الكتابة في <code class="i">nativeElement.innerHTML</code> بتتخطى معقّم أنجولار خالص. استعمل الـ binding أو <code class="i">Renderer2</code> أو عقّم بإيدك.'},
    {en:'“It came from our API” is not a safety property. Your API stored whatever some other user typed into it.',
     ar:'«دي جاية من الـ API بتاعنا» دي مش ضمانة أمان. الـ API بتاعك خزّن اللي مستخدم تاني كتبه.'},
    {en:'A redirect target taken from a query param is an open-redirect vulnerability. Validate it against your own routes before navigating.',
     ar:'وجهة إعادة التوجيه اللي جاية من query param دي ثغرة open-redirect. تحقق منها قدام الراوتس بتاعتك قبل ما تنقل.'},
    {en:'Angular ships security fixes in <b>patch</b> releases. An app pinned to an old minor is carrying known, published vulnerabilities.',
     ar:'أنجولار بيطلّع إصلاحات أمنية في إصدارات <b>patch</b>. والتطبيق المثبّت على minor قديم شايل ثغرات معروفة ومنشورة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'performance',
  badge:{en:'Measure first', ar:'قِس الأول'},
  title:{en:'Performance: budgets, profiles and the big wins', ar:'الأداء: الميزانيات والقياس والمكاسب الكبيرة'},
  lead:{
    en:`Almost every performance question has already been answered by a measurement you have not taken yet. <b>Measure, find the actual bottleneck, fix that one thing, measure again.</b> Guessing costs more time than profiling, every single time.`,
    ar:`تقريباً كل سؤال عن الأداء إجابته موجودة في قياس إنت لسه معملتوش. <b>قِس، ولاقي الاختناق الحقيقي، وصلّح الحاجة دي، وقِس تاني.</b> والتخمين بيكلّف وقت أكتر من القياس، في كل مرة.`
  },
  nodes:[
    [{id:'measure',role:{en:'find the bottleneck',ar:'لاقي الاختناق'},file:'measure',ext:'',kind:'cfg'}],
    [{id:'bundle',role:{en:'too much JavaScript',ar:'جافاسكريبت كتير'},file:'bundle size',ext:'',kind:'ts'},
     {id:'render',role:{en:'too much rendering',ar:'رندرة كتير'},file:'runtime',ext:'',kind:'svc'}]
  ],
  edges:[
    {id:'e1',from:'measure',to:'bundle',a:'bottom',b:'top',label:'ng build --stats-json',packet:'2.1 MB'},
    {id:'e2',from:'measure',to:'render',a:'bottom',b:'top',label:'DevTools profiler',packet:'340ms'}
  ],
  files:[
    {id:'measure',name:'how to actually measure',lang:'bash',tag:{en:'do this first',ar:'اعمل ده الأول'},code:[
      "# 1. WHAT IS IN MY BUNDLE?",
      "ng build --configuration production --stats-json",
      "npx source-map-explorer dist/**/*.js",
      "#    → an interactive treemap. the surprise is always a library",
      "#      you imported for one function.",
      "",
      "# 2. WHAT IS SLOW AT RUNTIME?",
      "#    Angular DevTools extension → Profiler tab → record → interact",
      "#    it draws a flame chart of change detection per component.",
      "",
      "# 3. WHAT DOES A REAL USER EXPERIENCE?",
      "npx lighthouse https://your-app.com --view",
      "#    LCP  < 2.5s   largest content painted",
      "#    INP  < 200ms  responsiveness to interaction",
      "#    CLS  < 0.1    how much the layout jumps",
      "",
      "# 4. IN THE CONSOLE, ON A LIVE APP",
      "ng.profiler.timeChangeDetection()",
      "#    prints the ms of one full change detection pass"]},
    {id:'budget',name:'angular.json — fail the build, not the user',lang:'json',tag:{en:'automate it',ar:'أتمتتها'},code:[
      "\"budgets\": [",
      "  {",
      "    \"type\": \"initial\",",
      "    \"maximumWarning\": \"500kB\",",
      "    \"maximumError\": \"900kB\"",
      "  },",
      "  {",
      "    \"type\": \"anyComponentStyle\",",
      "    \"maximumWarning\": \"4kB\",",
      "    \"maximumError\": \"8kB\"",
      "  }",
      "]",
      "",
      "// now a pull request that adds 300kB fails CI instead of",
      "// quietly shipping. this single config block prevents more",
      "// performance regressions than any amount of optimising."]},
    {id:'wins',name:'the wins, in order of payoff',lang:'ts',tag:{en:'do them in this order',ar:'اعملهم بالترتيب ده'},code:[
      "// 1. LAZY LOAD ROUTES               — usually halves the first bundle",
      "loadComponent: () => import('./admin').then(m => m.Admin)",
      "",
      "// 2. @defer BELOW THE FOLD         — nobody scrolled, nobody paid",
      "@defer (on viewport) { <app-reviews /> } @placeholder { <div class=\"sk\"></div> }",
      "",
      "// 3. NgOptimizedImage              — lazy, sized, preloaded hero",
      "<img ngSrc=\"/hero.jpg\" width=\"1200\" height=\"600\" priority>",
      "",
      "// 4. OnPush + SIGNALS              — targeted invalidation",
      "changeDetection: ChangeDetectionStrategy.OnPush",
      "",
      "// 5. track BY IDENTITY             — stop rebuilding rows",
      "@for (r of rows(); track r.id) { … }",
      "",
      "// 6. VIRTUAL SCROLL                — 20 rows, not 20,000",
      "<cdk-virtual-scroll-viewport itemSize=\"56\">…</cdk-virtual-scroll-viewport>",
      "",
      "// 7. computed INSTEAD OF METHODS   — cached, not recomputed per CD",
      "readonly total = computed(() => …)",
      "",
      "// 8. ZONELESS                      — removes 13kB and the guessing",
      "provideZonelessChangeDetection()"]},
    {id:'worker',name:'web workers — for genuinely heavy CPU work',lang:'ts',tag:{en:'the last resort',ar:'الملاذ الأخير'},code:[
      "// ng generate web-worker report",
      "",
      "// report.worker.ts — runs on another thread, no DOM, no Angular",
      "addEventListener('message', ({ data }) => {",
      "  const result = crunchTenThousandRows(data);   // 2 seconds of work",
      "  postMessage(result);",
      "});",
      "",
      "// the component — the UI never freezes",
      "export class Report {",
      "  readonly result = signal<Row[] | null>(null);",
      "",
      "  run(rows: Row[]) {",
      "    const w = new Worker(new URL('./report.worker', import.meta.url));",
      "    w.onmessage = ({ data }) => { this.result.set(data); w.terminate(); };",
      "    w.postMessage(rows);",
      "  }",
      "}",
      "",
      "// only worth it for CPU work measured in hundreds of ms.",
      "// the postMessage copy is not free — do not use it for I/O."]},
    {id:'anti',name:'what people optimise that does not matter',lang:'ts',tag:{en:'skip these',ar:'سيبك منهم'},code:[
      "// ✗ trackBy on a list of five items",
      "// ✗ OnPush on a component that renders once",
      "// ✗ memoising a function that runs in 0.01ms",
      "// ✗ splitting a 4kB component into its own chunk",
      "// ✗ replacing a working library to save 3kB",
      "",
      "// ✓ the 400kB date library imported for one format() call",
      "// ✓ the admin module every visitor downloads and never opens",
      "// ✓ the unoptimised 3MB hero image",
      "// ✓ the component that re-renders 60 times a second",
      "//",
      "// the difference between the two lists is that somebody",
      "// MEASURED the second one."]}
  ],
  steps:[
    {t:{en:'Start with the bundle. <code class="i">--stats-json</code> plus <code class="i">source-map-explorer</code> draws a treemap of exactly what you are shipping — and the answer is almost always a library imported for one function.',
        ar:'ابدأ بالـ bundle. <code class="i">--stats-json</code> مع <code class="i">source-map-explorer</code> بيرسم خريطة بالظبط للي بتشحنه — والإجابة تقريباً دايماً مكتبة معمول لها import عشان دالة واحدة.'},
     edge:'e1',hl:{measure:[2,3,4,5]}},
    {t:{en:'Then the runtime. The Angular DevTools profiler shows a flame chart of change detection per component, so you can see which one is running sixty times a second.',
        ar:'وبعدين وقت التشغيل. الـ profiler في Angular DevTools بيوري flame chart للـ change detection لكل component، فتشوف مين بيشتغل ستين مرة في الثانية.'},
     edge:'e2',hl:{measure:[8,9,10]}},
    {t:{en:'Set <b>budgets</b> so the build fails instead of the user. This one config block prevents more regressions than any amount of hand-optimising.',
        ar:'حط <b>ميزانيات</b> عشان الـ build يفشل بدل المستخدم. وبلوك الإعداد ده بيمنع تراجعات أكتر من أي تحسين يدوي.'},
     hl:{budget:[3,4,5,14,15,16]}},
    {t:{en:'Then apply the wins in payoff order. Lazy routes and <code class="i">&#64;defer</code> come first because they remove work entirely; micro-optimisations come last because they usually remove nothing.',
        ar:'وبعدين طبّق المكاسب بترتيب العائد. الراوتس الكسلانة و<code class="i">&#64;defer</code> الأول لإنهم بيشيلوا الشغل خالص؛ والتحسينات الصغيرة في الآخر لإنها غالباً مبتشيلش حاجة.'},
     hl:{wins:[1,2,4,5,7,8]}},
    {t:{en:'And know what <b>not</b> to optimise. The two lists differ by exactly one thing: somebody measured the second one.',
        ar:'واعرف إيه اللي <b>مش</b> هتحسّنه. اللستتين بيختلفوا في حاجة واحدة بالظبط: حد قاس التانية.'},
     hl:{anti:[1,2,7,8,12,13]}},
    {t:{en:'A web worker is the last resort — worth it only for CPU work measured in hundreds of milliseconds.',
        ar:'والـ web worker هو الملاذ الأخير — يستاهل بس مع شغل CPU بمئات المللي ثانية.'},
     hl:{worker:[1,4,5,6,20,21]}}
  ],
  example:[
    {
      name:{en:'A real 2.1 MB → 380 kB reduction', ar:'تقليل حقيقي من 2.1 ميجا لـ 380 كيلو'},
      what:{
        en:'Four changes, in the order a profile suggested them, on an app that felt slow. None of them required rewriting a component — and the fourth one, the one everybody wants to do first, was worth the least.',
        ar:'أربع تغييرات، بالترتيب اللي القياس اقترحه، على تطبيق كان حاسس إنه بطيء. ولا واحد فيهم احتاج إعادة كتابة component — والرابع، اللي الكل عايز يعمله الأول، كان أقلهم قيمة.'},
      files:[
        {id:'pf1',name:'what the treemap showed',lang:'bash',tag:{en:'before',ar:'قبل'},code:[
          "main.js  2,140 kB",
          "  ├─ moment.js + all locales      480 kB   ← imported for one format()",
          "  ├─ lodash (full)                310 kB   ← used debounce and cloneDeep",
          "  ├─ admin feature                420 kB   ← 2% of users ever open it",
          "  ├─ chart library                380 kB   ← on a tab nobody clicks first",
          "  └─ our own code                 550 kB"]},
        {id:'pf2',name:'the four changes',lang:'ts',tag:{en:'after: 380 kB',ar:'بعد: 380 كيلو'},code:[
          "// 1. −480 kB  moment → the built-in date pipe / Intl",
          "{{ order.placedAt | date:'medium' }}",
          "",
          "// 2. −290 kB  lodash → two small functions we already had",
          "//    (debounce came free from RxJS debounceTime)",
          "",
          "// 3. −420 kB  admin behind a lazy route + canMatch",
          "{ path: 'admin', canMatch: [isAdmin],",
          "  loadChildren: () => import('./admin/routes').then(m => m.ADMIN) }",
          "",
          "// 4. −380 kB  chart inside a @defer on its tab",
          "@defer (on interaction; prefetch on hover) { <app-revenue-chart /> }",
          "",
          "// initial bundle: 2,140 kB → 380 kB",
          "// LCP on 3G: 8.4s → 1.9s",
          "// zero components rewritten."]}
      ]
    }
  ],
  gotchas:[
    {en:'Always profile a <b>production</b> build. Dev builds are unminified, untree-shaken and several times larger — measuring one tells you nothing useful.',
     ar:'دايماً قِس build <b>إنتاج</b>. builds التطوير مش مصغّرة ولا متشذّبة وأكبر بمرات — وقياسها مش هيقولك حاجة مفيدة.'},
    {en:'Set budgets on day one, not after the app is slow. Reversing 2 MB of accumulated dependencies is a project; preventing it is one config block.',
     ar:'حط الميزانيات من أول يوم، مش بعد ما التطبيق يبقى بطيء. عكس 2 ميجا اعتماديات متراكمة ده مشروع؛ ومنعها بلوك إعداد واحد.'},
    {en:'Images are usually the real LCP problem, not JavaScript. <code class="i">NgOptimizedImage</code> with <code class="i">priority</code> on the hero often beats every code change combined.',
     ar:'الصور غالباً هي مشكلة الـ LCP الحقيقية، مش الجافاسكريبت. و<code class="i">NgOptimizedImage</code> مع <code class="i">priority</code> على صورة الـ hero بيتفوق على كل تغييرات الكود مجتمعة.'},
    {en:'A web worker is only worth it for CPU work measured in hundreds of milliseconds. The <code class="i">postMessage</code> copy is not free, and it cannot touch the DOM.',
     ar:'الـ web worker يستاهل بس مع شغل CPU بمئات المللي ثانية. ونسخ <code class="i">postMessage</code> مش ببلاش، وهو مش بيلمس الـ DOM.'},
    {en:'Optimising without measuring makes code harder to read and usually slower. If you cannot name the number you improved, you did not improve anything.',
     ar:'التحسين من غير قياس بيخلي الكود أصعب في القراية وغالباً أبطأ. ولو مش قادر تسمّي الرقم اللي حسّنته، يبقى إنت محسّنتش حاجة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'lazy-loading',
  badge:{en:'Ship less', ar:'اشحن أقل'},
  title:{en:'Lazy loading and preloading strategies', ar:'التحميل الكسلان واستراتيجيات التحميل المسبق'},
  lead:{
    en:`Lazy loading splits your bundle so the browser downloads a route&rsquo;s code only when someone goes there. Preloading then fetches those chunks quietly in the background, so the split costs nothing in felt speed. <b>You want both</b> — the split without the preload just moves the wait.`,
    ar:`التحميل الكسلان بيقسّم الـ bundle فالبراوزر مبينزّلش كود الـ route غير لما حد يروح هناك. والتحميل المسبق بيجيب الـ chunks دي في الخلفية بهدوء، فالتقسيم مبيكلفش حاجة في الإحساس بالسرعة. <b>وإنت عايز الاتنين</b> — التقسيم من غير تحميل مسبق بينقل الانتظار بس.`
  },
  nodes:[
    [{id:'routes',role:{en:'declares the splits',ar:'بيعلن التقسيمات'},file:'app.routes',ext:'.ts',kind:'cfg'}],
    [{id:'pre',role:{en:'decides what to fetch',ar:'بيقرر يجيب إيه'},file:'preloading.strategy',ext:'.ts',kind:'svc'},
     {id:'chunk',role:{en:'a separate file',ar:'ملف منفصل'},file:'chunk-ADMIN',ext:'.js',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'routes',to:'pre',a:'bottom',b:'top',label:'withPreloading(...)',packet:'route list'},
    {id:'e2',from:'pre',to:'chunk',a:'right',b:'left',label:'load() when idle',packet:'fetch'},
    {id:'e3',from:'chunk',to:'routes',a:'top',b:'right',label:'ready before you click',packet:'instant'}
  ],
  files:[
    {id:'routes',name:'app.routes.ts',lang:'ts',tag:{en:'three ways to split',ar:'تلات طرق للتقسيم'},code:[
      "export const routes: Routes = [",
      "  // 1. ONE COMPONENT in its own chunk",
      "  {",
      "    path: 'about',",
      "    loadComponent: () => import('./about').then(m => m.About),",
      "  },",
      "",
      "  // 2. A WHOLE FEATURE — its routes and everything they reference",
      "  {",
      "    path: 'admin',",
      "    canMatch: [isAdmin],          // checked BEFORE the chunk downloads",
      "    loadChildren: () => import('./admin/admin.routes').then(m => m.ADMIN_ROUTES),",
      "    data: { preload: false },     // read by our custom strategy below",
      "  },",
      "",
      "  // 3. EAGER — no import(), so it is in the main bundle.",
      "  //    correct for the route people land on first.",
      "  { path: '', component: Home },",
      "];"]},
    {id:'pre',name:'the preloading strategies',lang:'ts',tag:{en:'pick one',ar:'اختار واحدة'},code:[
      "import { withPreloading, PreloadAllModules, NoPreloading } from '@angular/router';",
      "",
      "// NOTHING — the default. every route waits until it is visited.",
      "provideRouter(routes, withPreloading(NoPreloading))",
      "",
      "// EVERYTHING — once the app is idle, quietly fetch every lazy chunk.",
      "// great for an internal tool on good wifi; wasteful on mobile data.",
      "provideRouter(routes, withPreloading(PreloadAllModules))",
      "",
      "// YOUR OWN RULES — the one you actually want in production",
      "provideRouter(routes, withPreloading(SelectivePreload))"]},
    {id:'custom',name:'a strategy worth writing',lang:'ts',tag:{en:'network-aware',ar:'واعية بالشبكة'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class SelectivePreload implements PreloadingStrategy {",
      "",
      "  preload(route: Route, load: () => Observable<unknown>): Observable<unknown> {",
      "    // 1. respect an explicit opt-out on the route",
      "    if (route.data?.['preload'] === false) return EMPTY;",
      "",
      "    // 2. never spend someone else's mobile data",
      "    const conn = (navigator as any).connection;",
      "    if (conn?.saveData) return EMPTY;",
      "    if (/(^|-)2g$/.test(conn?.effectiveType ?? '')) return EMPTY;",
      "",
      "    // 3. wait a moment so preloading never competes with the",
      "    //    first paint or the user's own first navigation",
      "    return timer(2000).pipe(switchMap(() => load()));",
      "  }",
      "}"]},
    {id:'hover',name:'the best strategy: preload on intent',lang:'ts',tag:{en:'feels instant',ar:'بيحس إنه فوري'},code:[
      "// a person hovers a link ~200ms before they click it.",
      "// that is enough time to have the chunk ready.",
      "",
      "@Directive({",
      "  selector: 'a[routerLink][appPrefetch]',",
      "  host: { '(mouseenter)': 'go()', '(focus)': 'go()', '(touchstart)': 'go()' },",
      "})",
      "export class Prefetch {",
      "  private readonly router = inject(Router);",
      "  private readonly link = inject(RouterLink);",
      "  private done = false;",
      "",
      "  go() {",
      "    if (this.done) return;",
      "    this.done = true;",
      "",
      "    const url = this.link.urlTree;",
      "    if (!url) return;",
      "    const route = this.router.parseUrl(url.toString());",
      "    // ask the router to resolve and load the matching config",
      "    this.router.navigateByUrl(route, { skipLocationChange: true, replaceUrl: true })",
      "      .catch(() => {});",
      "  }",
      "}",
      "",
      "// simpler alternative: <a routerLink=\"/x\"> plus @defer(prefetch on hover)",
      "// inside the target page for its heaviest widget."]},
    {id:'check',name:'checking it actually worked',lang:'bash',tag:{en:'verify',ar:'تأكد'},code:[
      "ng build --configuration production",
      "",
      "Initial chunk files   | Names          | Raw size",
      "main-4KQ2P.js         | main           | 214.60 kB",
      "polyfills-B7NX.js     | polyfills      |  34.58 kB",
      "                                       | Initial total | 249.18 kB",
      "",
      "Lazy chunk files      | Names          | Raw size",
      "chunk-ADMIN.js        | admin-routes   | 186.20 kB   ← not on first load",
      "chunk-ABOUT.js        | about          |  12.10 kB",
      "",
      "# if a chunk you expected to be lazy shows under 'Initial',",
      "# something imported it eagerly. search for the class name:",
      "#   grep -rn 'AdminRoutes' src --include=*.ts",
      "# a single eager import undoes the whole split."]}
  ],
  steps:[
    {t:{en:'A dynamic <code class="i">import()</code> is the instruction to the bundler: cut here. <code class="i">loadComponent</code> splits one component; <code class="i">loadChildren</code> splits an entire feature.',
        ar:'الـ <code class="i">import()</code> الديناميكي ده تعليمة للـ bundler: اقص هنا. و<code class="i">loadComponent</code> بيقسّم component واحد؛ و<code class="i">loadChildren</code> بيقسّم فيتشر كامل.'},
     hl:{routes:[5,12]}},
    {t:{en:'<code class="i">canMatch</code> runs <b>before</b> the download. That is the difference between a non-admin merely being blocked and a non-admin never receiving the admin code at all.',
        ar:'<code class="i">canMatch</code> بيشتغل <b>قبل</b> التنزيل. وده الفرق بين إن غير الأدمن يتمنع بس، وبين إنه عمره ما يستلم كود الأدمن أصلاً.'},
     hl:{routes:[11]}},
    {t:{en:'Splitting alone just moves the wait to click time. A preloading strategy fetches the chunks during idle so the split becomes free.',
        ar:'التقسيم لوحده بينقل الانتظار لوقت الكليك. واستراتيجية التحميل المسبق بتجيب الـ chunks وقت الفراغ فالتقسيم يبقى ببلاش.'},
     edge:'e1',hl:{pre:[7,8]}},
    {t:{en:'<code class="i">PreloadAllModules</code> is blunt — it downloads everything, including on a 2G connection with data saver on. A custom strategy that checks the network is a few lines.',
        ar:'<code class="i">PreloadAllModules</code> فظة — بتنزّل كل حاجة، حتى على اتصال 2G وموفّر البيانات شغال. واستراتيجية مخصصة بتشوف الشبكة كام سطر.'},
     edge:'e2',hl:{custom:[6,9,10,11,15]}},
    {t:{en:'Best of all is preloading on <b>intent</b>: someone hovers a link roughly 200 ms before clicking, which is enough time to have the chunk ready.',
        ar:'وأحسن حاجة التحميل المسبق عند <b>النية</b>: الشخص بيعدي على اللينك بالماوس حوالي 200 مللي ثانية قبل ما يدوس، وده وقت كفاية إن الـ chunk يبقى جاهز.'},
     edge:'e3',hl:{hover:[1,2,6]}},
    {t:{en:'Then verify with a production build. A chunk you expected to be lazy showing under “Initial” means something imported it eagerly.',
        ar:'وبعدين تأكد بـ build إنتاج. والـ chunk اللي كنت متوقعه كسلان وظاهر تحت «Initial» معناه إن حاجة عملت له import عادي.'},
     hl:{check:[3,8,9,12,13,15]}}
  ],
  example:[
    {
      name:{en:'The eager import that undid everything', ar:'الـ import العادي اللي لغى كل حاجة'},
      what:{
        en:'The most common lazy-loading failure: the route is lazy, but something else imports the same class normally, so the bundler puts it back in the main chunk. The route config looks perfect and the bundle never shrinks.',
        ar:'أشهر فشل في التحميل الكسلان: الـ route كسلان، بس حاجة تانية بتعمل import عادي لنفس الكلاس، فالـ bundler بيرجّعه للـ chunk الأساسي. وإعداد الـ route شكله مظبوط والـ bundle عمره ما بيصغر.'},
      files:[
        {id:'lz1',name:'the mistake',lang:'ts',tag:{en:'silently eager',ar:'عادي في صمت'},code:[
          "// app.routes.ts — looks correct",
          "{ path: 'admin', loadChildren: () => import('./admin/routes').then(m => m.ADMIN) }",
          "",
          "// …but somewhere else, in a file that IS in the main bundle:",
          "import { AdminDashboard } from './admin/dashboard';   // ✗ eager!",
          "",
          "export const WIDGETS = {",
          "  adminSummary: AdminDashboard,      // now admin is in main.js",
          "};",
          "",
          "// one static import anywhere pulls the whole feature back in."]},
        {id:'lz2',name:'the fix',lang:'ts',tag:{en:'lazy all the way',ar:'كسلان للآخر'},code:[
          "// keep every reference dynamic",
          "export const WIDGETS = {",
          "  adminSummary: () => import('./admin/dashboard').then(m => m.AdminDashboard),",
          "};",
          "",
          "// and verify, do not assume:",
          "//   ng build --configuration production",
          "//   → is the chunk listed under 'Lazy chunk files'?",
          "//",
          "// then guard the route so it is never even requested:",
          "{ path: 'admin', canMatch: [isAdmin],",
          "  loadChildren: () => import('./admin/routes').then(m => m.ADMIN) }"]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>One static <code class="i">import</code> anywhere pulls the whole feature back into the main bundle.</b> Always verify with a production build; never assume the split worked.',
     ar:'<b><code class="i">import</code> عادي واحد في أي مكان بيرجّع الفيتشر كله للـ bundle الأساسي.</b> دايماً تأكد بـ build إنتاج؛ وعمرك ما تفترض إن التقسيم نجح.'},
    {en:'Do not lazy-load the landing route. Splitting the page everyone sees first adds a round trip for zero benefit.',
     ar:'متعملش lazy loading للراوت الرئيسية. تقسيم الصفحة اللي الكل بيشوفها الأول بيضيف رحلة زيادة من غير أي فايدة.'},
    {en:'<code class="i">canActivate</code> runs after the download; <code class="i">canMatch</code> runs before. For anything role-gated, use <code class="i">canMatch</code>.',
     ar:'<code class="i">canActivate</code> بيشتغل بعد التنزيل؛ و<code class="i">canMatch</code> قبله. وأي حاجة مربوطة بصلاحية، استعمل <code class="i">canMatch</code>.'},
    {en:'<code class="i">PreloadAllModules</code> ignores the network. On mobile data that is someone else&rsquo;s money — check <code class="i">navigator.connection.saveData</code>.',
     ar:'<code class="i">PreloadAllModules</code> بتتجاهل الشبكة. وعلى بيانات الموبايل دي فلوس حد تاني — شوف <code class="i">navigator.connection.saveData</code>.'},
    {en:'A lazy chunk that fails to download leaves the user on a dead link. Handle router navigation errors and offer a retry.',
     ar:'الـ chunk الكسلان اللي بيفشل في التنزيل بيسيب المستخدم على لينك ميت. اتعامل مع أخطاء تنقل الراوتر واعرض إعادة محاولة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'renderer2',
  badge:{en:'Touching the DOM', ar:'لمس الـ DOM'},
  title:{en:'<code>Renderer2</code> and safe DOM access', ar:'<code>Renderer2</code> والوصول الآمن للـ DOM'},
  lead:{
    en:`Ninety-five percent of DOM work belongs in a template binding. For the rest — a directive that must set a style, a library that needs an element — <code>Renderer2</code> is the abstraction that keeps your code working <b>on the server, in a web worker, and under a strict CSP</b>, where <code>document</code> does not exist.`,
    ar:`خمسة وتسعين في المية من شغل الـ DOM مكانه binding في التمبلت. وللباقي — directive لازم يحط استايل، أو مكتبة محتاجة عنصر — <code>Renderer2</code> هو التجريد اللي بيخلي كودك شغال <b>على السيرفر، وفي web worker، وتحت CSP صارم</b>، حيث <code>document</code> مش موجود.`
  },
  nodes:[
    [{id:'dir',role:{en:'wants to change an element',ar:'عايز يغيّر عنصر'},file:'ripple.directive',ext:'.ts',kind:'cfg'}],
    [{id:'r2',role:{en:'the safe abstraction',ar:'التجريد الآمن'},file:'Renderer2',ext:'',kind:'svc'},
     {id:'raw',role:{en:'the direct way',ar:'الطريقة المباشرة'},file:'nativeElement',ext:'',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'dir',to:'r2',a:'bottom',b:'top',label:'renderer.setStyle()',packet:'safe'},
    {id:'e2',from:'dir',to:'raw',a:'bottom',b:'right',label:'el.nativeElement.style',packet:'browser only'}
  ],
  files:[
    {id:'order',name:'the order to try things in',lang:'html',tag:{en:'read this first',ar:'اقرا دي الأول'},code:[
      "<!-- 1. A BINDING. always try this first. -->",
      "<div [class.open]=\"isOpen()\" [style.height.px]=\"h()\" [attr.aria-expanded]=\"isOpen()\">",
      "",
      "<!-- 2. A DIRECTIVE with host bindings — declarative, still no DOM code -->",
      "@Directive({",
      "  selector: '[appHighlight]',",
      "  host: { '[style.background]': 'colour()', '(mouseenter)': 'on()' },",
      "})",
      "",
      "<!-- 3. Renderer2 — when you must create or move real nodes -->",
      "<!-- 4. nativeElement — only for read-only measurement, and only",
      "        inside afterNextRender where you know you are in a browser -->",
      "",
      "<!-- if you find yourself at 3 or 4 often, the usual cause is",
      "     fighting the framework rather than a real requirement. -->"]},
    {id:'r2',name:'ripple.directive.ts',lang:'ts',tag:{en:'the full API',ar:'الواجهة كاملة'},code:[
      "import { Directive, Renderer2, ElementRef, inject, afterNextRender } from '@angular/core';",
      "",
      "@Directive({ selector: '[appRipple]', host: { '(click)': 'ripple($event)' } })",
      "export class Ripple {",
      "  private readonly r = inject(Renderer2);",
      "  private readonly host = inject(ElementRef<HTMLElement>);",
      "",
      "  ripple(e: MouseEvent) {",
      "    const span = this.r.createElement('span');",
      "",
      "    this.r.addClass(span, 'ripple');",
      "    this.r.setStyle(span, 'left', e.offsetX + 'px');",
      "    this.r.setStyle(span, 'top', e.offsetY + 'px');",
      "    this.r.setAttribute(span, 'aria-hidden', 'true');",
      "",
      "    this.r.appendChild(this.host.nativeElement, span);",
      "",
      "    // listen() returns its own unlisten function",
      "    const off = this.r.listen(span, 'animationend', () => {",
      "      this.r.removeChild(this.host.nativeElement, span);",
      "      off();",
      "    });",
      "  }",
      "}",
      "",
      "// createElement  createText   appendChild  insertBefore  removeChild",
      "// setAttribute   removeAttribute   addClass   removeClass",
      "// setStyle       removeStyle       setProperty   listen"]},
    {id:'why',name:'why not just use document?',lang:'ts',tag:{en:'the reason',ar:'السبب'},code:[
      "// ✗ this crashes during SSR — there is no document on a server",
      "document.querySelector('.thing')!.classList.add('open');",
      "",
      "// ✗ this works in a browser and nowhere else",
      "this.el.nativeElement.innerHTML = html;   // and skips the sanitizer",
      "",
      "// ✓ Renderer2 is an ABSTRACTION. Angular swaps the implementation:",
      "//     in a browser  → real DOM calls",
      "//     during SSR    → builds the HTML string on the server",
      "//     in a worker   → messages to the main thread",
      "//",
      "// your directive does not change. that is the whole point.",
      "",
      "// ✓ and when you genuinely need to READ the DOM (measure a box),",
      "//   nativeElement is fine — inside afterNextRender, which never",
      "//   runs on the server:",
      "afterNextRender(() => {",
      "  const { width } = this.host.nativeElement.getBoundingClientRect();",
      "  this.width.set(width);",
      "});"]},
    {id:'raw',name:'when nativeElement is the right answer',lang:'ts',tag:{en:'reading, not writing',ar:'قراية مش كتابة'},code:[
      "// Renderer2 has no read API — no getBoundingClientRect, no scrollTop.",
      "// for MEASUREMENT, nativeElement is correct and unavoidable.",
      "",
      "export class StickyHeader {",
      "  private readonly host = inject(ElementRef<HTMLElement>);",
      "  readonly stuck = signal(false);",
      "",
      "  constructor() {",
      "    afterNextRender(() => {",
      "      const io = new IntersectionObserver(",
      "        ([e]) => this.stuck.set(!e.isIntersecting),",
      "        { threshold: [1] },",
      "      );",
      "      io.observe(this.host.nativeElement);",
      "      inject(DestroyRef).onDestroy(() => io.disconnect());",
      "    });",
      "  }",
      "}",
      "",
      "// note: an IntersectionObserver or ResizeObserver is almost always",
      "// better than reading layout on scroll — it does not force reflow."]}
  ],
  steps:[
    {t:{en:'Try a <b>binding</b> first. <code class="i">[class.x]</code>, <code class="i">[style.y]</code> and <code class="i">[attr.z]</code> cover the overwhelming majority of “I need to change this element”.',
        ar:'جرّب <b>binding</b> الأول. <code class="i">[class.x]</code> و<code class="i">[style.y]</code> و<code class="i">[attr.z]</code> بيغطوا الغالبية العظمى من «أنا محتاج أغيّر العنصر ده».'},
     hl:{order:[1,2,4,7]}},
    {t:{en:'When you must create or move real nodes, <code class="i">Renderer2</code> gives you a small, complete API — create, append, remove, style, listen.',
        ar:'ولما تضطر تعمل أو تحرّك nodes حقيقية، <code class="i">Renderer2</code> بيديك API صغيرة وكاملة — إنشاء وإضافة وحذف وتنسيق واستماع.'},
     edge:'e1',hl:{r2:[9,11,12,16]}},
    {t:{en:'<code class="i">listen()</code> returns its own unlisten function. Call it — a listener you added by hand is a leak Angular will not clean up for you.',
        ar:'<code class="i">listen()</code> بترجّع دالة إلغاء بتاعتها. ناديها — أي listener ضفته بإيدك ده تسريب أنجولار مش هينضفه بدالك.'},
     hl:{r2:[19,20,21,22]}},
    {t:{en:'The reason it exists is that Angular swaps the implementation: real DOM in a browser, an HTML string during SSR. Your directive never changes.',
        ar:'وسبب وجوده إن أنجولار بيبدّل التنفيذ: DOM حقيقي في البراوزر، ونص HTML أثناء الـ SSR. والـ directive بتاعك مش بيتغير.'},
     edge:'e2',hl:{why:[1,2,7,8,9,12]}},
    {t:{en:'For <b>reading</b> — measuring a box, observing intersection — <code class="i">nativeElement</code> is correct, because <code class="i">Renderer2</code> has no read API. Wrap it in <code class="i">afterNextRender</code>.',
        ar:'وللـ<b>قراية</b> — قياس صندوق أو مراقبة تقاطع — <code class="i">nativeElement</code> هو الصح، لإن <code class="i">Renderer2</code> مالوش API للقراية. لُفها في <code class="i">afterNextRender</code>.'},
     hl:{raw:[1,2,9,14,15]}}
  ],
  example:[
    {
      name:{en:'A tooltip that works under SSR', ar:'tooltip شغال مع الـ SSR'},
      what:{
        en:'A tooltip must create a node, position it, and remove it. Written with document it crashes the server render; written with Renderer2 the same file runs everywhere, and the server simply produces no tooltip.',
        ar:'الـ tooltip لازم يعمل node ويحدد مكانها ويشيلها. ولو اتكتب بـ document بيكسّر رندر السيرفر؛ ولو اتكتب بـ Renderer2 نفس الملف بيشتغل في كل حتة، والسيرفر ببساطة مش بيطلّع tooltip.'},
      files:[
        {id:'tt1',name:'tooltip.directive.ts',lang:'ts',tag:{en:'portable',ar:'قابل للنقل'},code:[
          "@Directive({",
          "  selector: '[appTooltip]',",
          "  host: { '(mouseenter)': 'show()', '(mouseleave)': 'hide()',",
          "          '(focus)': 'show()', '(blur)': 'hide()' },",
          "})",
          "export class Tooltip {",
          "  readonly appTooltip = input.required<string>();",
          "  private readonly r = inject(Renderer2);",
          "  private readonly host = inject(ElementRef<HTMLElement>);",
          "  private node: HTMLElement | null = null;",
          "",
          "  show() {",
          "    if (this.node) return;",
          "    const n = this.r.createElement('div');",
          "    this.r.addClass(n, 'tooltip');",
          "    this.r.setAttribute(n, 'role', 'tooltip');",
          "    this.r.appendChild(n, this.r.createText(this.appTooltip()));",
          "    this.r.appendChild(this.host.nativeElement, n);",
          "    this.node = n;",
          "  }",
          "",
          "  hide() {",
          "    if (!this.node) return;",
          "    this.r.removeChild(this.host.nativeElement, this.node);",
          "    this.node = null;",
          "  }",
          "",
          "  constructor() { inject(DestroyRef).onDestroy(() => this.hide()); }",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<code class="i">Renderer2</code> has <b>no read API</b>. For measurement you must use <code class="i">nativeElement</code> — inside <code class="i">afterNextRender</code>, so it never runs on the server.',
     ar:'<code class="i">Renderer2</code> <b>مالوش API للقراية</b>. وللقياس لازم تستعمل <code class="i">nativeElement</code> — جوه <code class="i">afterNextRender</code>، عشان عمره ما يشتغل على السيرفر.'},
    {en:'Every <code class="i">listen()</code> returns an unlisten function. Not calling it leaks a listener for the lifetime of the page.',
     ar:'كل <code class="i">listen()</code> بترجّع دالة إلغاء. وعدم نداءها بيسرّب listener طول عمر الصفحة.'},
    {en:'<code class="i">setProperty(el, \'innerHTML\', x)</code> bypasses Angular&rsquo;s sanitizer exactly like direct assignment does. It is not safer for being on the Renderer.',
     ar:'<code class="i">setProperty(el, \'innerHTML\', x)</code> بيتخطى معقّم أنجولار بالظبط زي الإسناد المباشر. ومش بقى أأمن عشان هو على الـ Renderer.'},
    {en:'Reading layout (<code class="i">offsetHeight</code>, <code class="i">getBoundingClientRect</code>) forces a synchronous reflow. In a scroll handler that is a guaranteed jank source — use an observer.',
     ar:'قراية التخطيط (<code class="i">offsetHeight</code> و<code class="i">getBoundingClientRect</code>) بتفرض reflow متزامن. وفي معالج scroll ده مصدر تهنيج مضمون — استعمل observer.'},
    {en:'If you are reaching for <code class="i">Renderer2</code> often, check you are not fighting the framework. Most cases are a binding or a host binding you have not thought of yet.',
     ar:'لو بتلجأ لـ <code class="i">Renderer2</code> كتير، اتأكد إنك مش بتتخانق مع الفريمورك. معظم الحالات دي binding أو host binding إنت لسه مفكرتش فيه.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'e2e-testing',
  badge:{en:'The real thing', ar:'الحاجة الحقيقية'},
  title:{en:'End-to-end testing with Playwright', ar:'اختبار شامل بـ Playwright'},
  lead:{
    en:`A unit test proves a component works. An <b>end-to-end</b> test proves the app works — a real browser, your real build, a real server, clicking real buttons. You want very few of these, and they should cover the journeys that would cost you money if they broke.`,
    ar:`تست الوحدة بيثبت إن الـ component شغال. والتست الـ<b>شامل</b> بيثبت إن التطبيق شغال — براوزر حقيقي، وbuild حقيقي، وسيرفر حقيقي، وكليك على زراير حقيقية. وإنت عايز قليل جداً منهم، ولازم يغطوا الرحلات اللي لو وقعت هتكلفك فلوس.`
  },
  nodes:[
    [{id:'spec',role:{en:'the journey',ar:'الرحلة'},file:'checkout.spec',ext:'.ts',kind:'cfg'}],
    [{id:'po',role:{en:'names the page',ar:'بيسمّي الصفحة'},file:'checkout.page',ext:'.ts',kind:'ts'},
     {id:'app',role:{en:'a real browser',ar:'براوزر حقيقي'},file:'localhost:4200',ext:'',kind:'svc'}]
  ],
  edges:[
    {id:'e1',from:'spec',to:'po',a:'bottom',b:'top',label:'page object',packet:'intent'},
    {id:'e2',from:'po',to:'app',a:'right',b:'left',label:'click / fill',packet:'real DOM'},
    {id:'e3',from:'app',to:'spec',a:'top',b:'right',label:'expect(...)',packet:'pass'}
  ],
  files:[
    {id:'setup',name:'getting started',lang:'bash',tag:{en:'setup',ar:'الإعداد'},code:[
      "npm init playwright@latest",
      "",
      "npx playwright test              # run them all, headless",
      "npx playwright test --ui         # the time-travel debugger. use this.",
      "npx playwright codegen localhost:4200   # records your clicks as code",
      "",
      "# playwright.config.ts — start the app automatically",
      "webServer: {",
      "  command: 'npm run start',",
      "  url: 'http://localhost:4200',",
      "  reuseExistingServer: !process.env.CI,",
      "}"]},
    {id:'spec',name:'checkout.spec.ts',lang:'ts',tag:{en:'one journey',ar:'رحلة واحدة'},code:[
      "import { test, expect } from '@playwright/test';",
      "",
      "test('a visitor can buy a product', async ({ page }) => {",
      "  await page.goto('/products');",
      "",
      "  // find things the way a PERSON does: by visible text and role.",
      "  await page.getByRole('link', { name: 'Wireless mouse' }).click();",
      "  await page.getByRole('button', { name: 'Add to cart' }).click();",
      "",
      "  await expect(page.getByRole('status')).toHaveText('Added to cart');",
      "",
      "  await page.getByRole('link', { name: /cart \\(1\\)/i }).click();",
      "  await page.getByRole('button', { name: 'Checkout' }).click();",
      "",
      "  await page.getByLabel('Email').fill('mona@shop.eg');",
      "  await page.getByLabel('Card number').fill('4242424242424242');",
      "  await page.getByRole('button', { name: 'Pay now' }).click();",
      "",
      "  // Playwright AUTO-WAITS. no sleep, no manual retry.",
      "  await expect(page.getByRole('heading', { name: 'Thank you' })).toBeVisible();",
      "  await expect(page).toHaveURL(/\\/orders\\/[a-z0-9-]+$/);",
      "});"]},
    {id:'po',name:'a page object keeps it readable',lang:'ts',tag:{en:'when it grows',ar:'لما تكبر'},code:[
      "export class CheckoutPage {",
      "  constructor(private readonly page: Page) {}",
      "",
      "  readonly email = () => this.page.getByLabel('Email');",
      "  readonly pay   = () => this.page.getByRole('button', { name: 'Pay now' });",
      "",
      "  async fillCard(number: string) {",
      "    await this.page.getByLabel('Card number').fill(number);",
      "  }",
      "",
      "  async expectSuccess() {",
      "    await expect(this.page.getByRole('heading', { name: 'Thank you' }))",
      "      .toBeVisible();",
      "  }",
      "}",
      "",
      "// the spec now reads like the acceptance criteria:",
      "//   await checkout.email().fill('mona@shop.eg');",
      "//   await checkout.fillCard('4242…');",
      "//   await checkout.pay().click();",
      "//   await checkout.expectSuccess();"]},
    {id:'stable',name:'making them not flaky',lang:'ts',tag:{en:'the hard part',ar:'الجزء الصعب'},code:[
      "// ✗ NEVER. this is the number one source of flaky tests.",
      "await page.waitForTimeout(2000);",
      "",
      "// ✓ assert on the STATE you are waiting for. Playwright retries.",
      "await expect(page.getByRole('heading', { name: 'Thank you' })).toBeVisible();",
      "",
      "// ✗ brittle: any refactor breaks it",
      "await page.click('.mat-mdc-button-base > span:nth-child(2)');",
      "",
      "// ✓ resilient: matches what a user sees, and doubles as an a11y check",
      "await page.getByRole('button', { name: 'Pay now' }).click();",
      "",
      "// ✓ for things with no accessible name, add a test id ON PURPOSE",
      "//   <div data-testid=\"cart-total\">",
      "await expect(page.getByTestId('cart-total')).toHaveText('EGP 1,699.50');",
      "",
      "// ✓ control the network so the test is deterministic",
      "await page.route('**/api/products', route =>",
      "  route.fulfill({ json: FIXTURE_PRODUCTS }));"]},
    {id:'how',name:'how many, and of what',lang:'ts',tag:{en:'the balance',ar:'التوازن'},code:[
      "//        ▲  E2E          5–15 tests. slow, real, expensive.",
      "//       ╱ ╲              only the journeys that cost money:",
      "//      ╱   ╲             sign up · sign in · checkout · the one",
      "//     ╱─────╲            report your biggest customer runs.",
      "//    ╱       ╲",
      "//   ╱ COMPONENT ╲       dozens. TestBed. fast.",
      "//  ╱─────────────╲",
      "// ╱     UNIT       ╲    hundreds. pure functions. instant.",
      "//╱──────────────────╲",
      "",
      "// an E2E suite that takes 40 minutes gets disabled within a month.",
      "// keep it under 5 and people will actually keep it green."]}
  ],
  steps:[
    {t:{en:'Playwright starts your real app, opens a real browser, and drives it. No mocking of Angular, no TestBed — if it passes, the feature genuinely works.',
        ar:'Playwright بيشغّل تطبيقك الحقيقي، ويفتح براوزر حقيقي، ويسوقه. من غير تقليد لأنجولار ولا TestBed — ولو نجح، يبقى الفيتشر شغال فعلاً.'},
     edge:'e1',hl:{setup:[1,7,8,9]}},
    {t:{en:'Find elements the way a <b>person</b> does — by role and visible name. This survives refactors and doubles as an accessibility check.',
        ar:'دوّر على العناصر بالطريقة اللي <b>الشخص</b> بيدوّر بيها — بالدور والاسم الظاهر. ده بيصمد قدام إعادة الهيكلة وبيبقى فحص إتاحة كمان.'},
     edge:'e2',hl:{spec:[6,7,8]}},
    {t:{en:'Playwright <b>auto-waits</b>. Every assertion retries until it passes or times out — which is why you never need <code class="i">waitForTimeout</code>.',
        ar:'Playwright <b>بيستنى لوحده</b>. وكل تأكيد بيتعاد لحد ما ينجح أو الوقت يخلص — وعشان كده عمرك ما هتحتاج <code class="i">waitForTimeout</code>.'},
     edge:'e3',hl:{spec:[19,20,21],stable:[1,2,4,5]}},
    {t:{en:'A hard-coded sleep is the number one cause of flaky suites, and a CSS selector like <code class="i">.mat-mdc-button > span:nth-child(2)</code> is the number two.',
        ar:'الـ sleep الثابت هو السبب رقم واحد للتستات المتقلبة، وselector زي <code class="i">.mat-mdc-button > span:nth-child(2)</code> هو رقم اتنين.'},
     hl:{stable:[7,8,10,11]}},
    {t:{en:'Keep the suite tiny. Five to fifteen journeys, under five minutes. A forty-minute E2E suite gets disabled within a month, and then you have none.',
        ar:'خلي المجموعة صغيرة. من خمسة لخمستاشر رحلة، وأقل من خمس دقايق. المجموعة اللي بتاخد أربعين دقيقة بتتقفل خلال شهر، وساعتها ميبقاش عندك ولا واحد.'},
     hl:{how:[1,2,3,11,12]}},
    {t:{en:'Past a few tests, a page object keeps them readable — the spec then reads like the acceptance criteria.',
        ar:'وبعد كام تست، الـ page object بيخليهم مقروئين — والـ spec بيبقى بيتقرا زي معايير القبول.'},
     hl:{po:[4,5,10,11,18,19]}}
  ],
  example:[
    {
      name:{en:'The five tests worth having', ar:'الخمس تستات اللي تستاهل'},
      what:{
        en:'If you write only five E2E tests, write these. Each one covers a path where a silent break costs real money or real trust — and none of them can be caught by a unit test, because they cross the whole system.',
        ar:'لو هتكتب خمس تستات شاملة بس، اكتب دول. كل واحد بيغطي مسار لو وقع في صمت هيكلّف فلوس حقيقية أو ثقة حقيقية — وولا واحد فيهم تست وحدة يقدر يمسكه، لإنهم بيعدوا النظام كله.'},
      files:[
        {id:'fv1',name:'the list',lang:'ts',tag:{en:'start with these',ar:'ابدأ بدول'},code:[
          "test('a new visitor can sign up')          // your funnel",
          "test('an existing user can sign in')       // and stays signed in on reload",
          "test('a visitor can complete a purchase')  // the money path",
          "test('a signed-out user is redirected from a private page')",
          "test('the main list loads and filters')    // the page most people use",
          "",
          "// notice what is NOT here:",
          "//   ✗ 'the button turns blue on hover'",
          "//   ✗ 'the validation message says the right thing'",
          "//   ✗ 'the date formats correctly'",
          "// those are component or unit tests. they are 100× faster",
          "// there, and they do not break when a class name changes."]},
        {id:'fv2',name:'running them in CI',lang:'bash',tag:{en:'the payoff',ar:'العائد'},code:[
          "# .github/workflows/e2e.yml",
          "- run: npm ci",
          "- run: npx playwright install --with-deps chromium",
          "- run: npx playwright test",
          "- uses: actions/upload-artifact@v4",
          "  if: failure()",
          "  with:",
          "    name: playwright-report",
          "    path: playwright-report/",
          "",
          "# on failure you get a trace: a video, a DOM snapshot per step,",
          "# and the network log. debugging a CI failure stops being guesswork."]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>Never use a fixed <code class="i">waitForTimeout</code>.</b> Assert on the state you are waiting for and let the runner retry — this is the single biggest cause of flaky suites.',
     ar:'<b>عمرك ما تستخدم <code class="i">waitForTimeout</code> ثابت.</b> أكّد على الحالة اللي بتستناها وسيب الـ runner يعيد — ده أكبر سبب للتستات المتقلبة.'},
    {en:'Select by role and visible text, not by CSS class. Class names change with every refactor; a button&rsquo;s accessible name does not.',
     ar:'اختار بالدور والنص الظاهر، مش بـ CSS class. أسماء الـ classes بتتغير مع كل إعادة هيكلة؛ أما الاسم المتاح للزرار فلأ.'},
    {en:'Control the network with <code class="i">page.route()</code> for anything you do not own. A test that depends on a third-party sandbox fails on their schedule, not yours.',
     ar:'تحكّم في الشبكة بـ <code class="i">page.route()</code> لأي حاجة مش بتاعتك. التست اللي بيعتمد على sandbox خارجية بتفشل حسب جدولهم، مش جدولك.'},
    {en:'Each test must set up and clean up its own data. Tests that depend on the order they run in fail the moment someone enables parallelism.',
     ar:'كل تست لازم يجهّز وينضّف الداتا بتاعته. والتستات اللي بتعتمد على ترتيب التشغيل بتفشل أول ما حد يشغّل التوازي.'},
    {en:'E2E tests are slow and expensive by nature. If your suite exceeds five minutes, move assertions down to component tests instead of adding machines.',
     ar:'التستات الشاملة بطيئة وغالية بطبيعتها. ولو مجموعتك عدّت خمس دقايق، انقل التأكيدات لتستات الـ components بدل ما تزوّد أجهزة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'aot-tree-shaking',
  badge:{en:'The build', ar:'الـ build'},
  title:{en:'AOT compilation and tree shaking', ar:'كمبايل AOT والـ tree shaking'},
  lead:{
    en:`Your templates are not HTML at runtime — the compiler turns them into JavaScript functions <b>before</b> the browser ever sees them. Understanding that explains why templates are type-checked, why the Angular compiler is not shipped to users, and why one careless import can defeat tree shaking.`,
    ar:`التمبلتس بتاعتك مش HTML وقت التشغيل — الـ compiler بيحوّلهم لدوال جافاسكريبت <b>قبل</b> ما البراوزر يشوفهم أصلاً. وفهم ده بيشرح ليه التمبلتس بيتفحص نوعها، وليه compiler أنجولار مش بيتشحن للمستخدمين، وليه import واحد مهمل بيلغي الـ tree shaking.`
  },
  nodes:[
    [{id:'src',role:{en:'what you write',ar:'اللي بتكتبه'},file:'component',ext:'.html',kind:'html'}],
    [{id:'aot',role:{en:'compiles it',ar:'بيكمبايله'},file:'AOT compiler',ext:'',kind:'cfg'},
     {id:'out',role:{en:'what ships',ar:'اللي بيتشحن'},file:'main',ext:'.js',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'src',to:'aot',a:'bottom',b:'top',label:'ng build',packet:'template'},
    {id:'e2',from:'aot',to:'out',a:'right',b:'left',label:'render function',packet:'JS'}
  ],
  files:[
    {id:'aot',name:'what the compiler produces',lang:'ts',tag:{en:'roughly',ar:'تقريباً'},code:[
      "// YOU WRITE:",
      "//   <h1>{{ title() }}</h1>",
      "//   @if (isOpen()) { <p>open</p> }",
      "",
      "// THE COMPILER EMITS (simplified, but this is the shape):",
      "function Card_Template(rf, ctx) {",
      "  if (rf & 1) {                       // CREATE pass — runs once",
      "    ɵɵelementStart(0, 'h1');",
      "    ɵɵtext(1);",
      "    ɵɵelementEnd();",
      "    ɵɵconditionalCreate(2, …);",
      "  }",
      "  if (rf & 2) {                       // UPDATE pass — runs on change",
      "    ɵɵtextInterpolate(ctx.title());",
      "    ɵɵconditional(ctx.isOpen() ? 0 : -1);",
      "  }",
      "}",
      "",
      "// consequences of this being AHEAD of time:",
      "//   - your template is TYPE-CHECKED at build time (strictTemplates)",
      "//   - a typo in a binding is a build error, not a runtime surprise",
      "//   - the COMPILER never ships to the browser",
      "//   - the browser gets plain JS it can optimise like any other"]},
    {id:'shake',name:'what tree shaking can and cannot remove',lang:'ts',tag:{en:'the rules',ar:'القواعد'},code:[
      "// tree shaking = 'nothing references this, so delete it'.",
      "// it works on STATIC ESM imports, at build time.",
      "",
      "// ✓ REMOVED — nothing imports it",
      "export function unusedHelper() { … }",
      "",
      "// ✓ REMOVED — providedIn: 'root' is tree-shakable. if no file",
      "//   injects ReportService, the whole class is dropped.",
      "@Injectable({ providedIn: 'root' })",
      "export class ReportService { … }",
      "",
      "// ✗ KEPT — listed in a providers array, so it is referenced",
      "providers: [ReportService]",
      "",
      "// ✗ KEPT — a barrel file imports everything to re-export it",
      "import { Button } from './ui';        // ./ui/index.ts exports 40 things",
      "",
      "// ✓ REMOVED — import the file directly, skip the barrel",
      "import { Button } from './ui/button';",
      "",
      "// ✗ KEPT — a side effect at module top level. the bundler cannot",
      "//   prove removing it is safe, so it keeps the whole module.",
      "console.log('loaded');",
      "window.myGlobal = something;"]},
    {id:'cfg',name:'the settings that matter',lang:'json',tag:{en:'already correct',ar:'مظبوطة أصلاً'},code:[
      "// tsconfig.json",
      "{",
      "  \"angularCompilerOptions\": {",
      "    \"strictTemplates\": true,           // type-check the HTML too",
      "    \"strictInjectionParameters\": true,",
      "    \"strictInputAccessModifiers\": true",
      "  }",
      "}",
      "",
      "// package.json — tells bundlers your code is safe to shake",
      "{ \"sideEffects\": false }",
      "",
      "// angular.json production config (the CLI sets these for you)",
      "//   optimization: true      minify + tree shake",
      "//   sourceMap: false",
      "//   namedChunks: false",
      "//   outputHashing: 'all'    cache busting",
      "//",
      "// AOT is ALWAYS on since v9. there is no JIT switch to forget."]},
    {id:'see',name:'seeing it for yourself',lang:'bash',tag:{en:'verify',ar:'تأكد'},code:[
      "# what does the compiler actually emit for my component?",
      "npx ngc -p tsconfig.json --outDir /tmp/aot",
      "# then read the .js next to your component. it is readable.",
      "",
      "# what survived tree shaking?",
      "ng build --configuration production --stats-json",
      "npx source-map-explorer dist/**/*.js",
      "",
      "# a class you expected to disappear is still there? something",
      "# references it. find out what:",
      "grep -rn 'ReportService' src --include=*.ts",
      "",
      "# most common answers:",
      "#   - it is in a providers: [] array",
      "#   - a barrel file re-exports it",
      "#   - a registry object holds a static reference to it"]}
  ],
  steps:[
    {t:{en:'<code class="i">ng build</code> compiles every template into a JavaScript render function. This happens <b>ahead of time</b>, on your machine or in CI — never in the user&rsquo;s browser.',
        ar:'<code class="i">ng build</code> بيكمبايل كل تمبلت لدالة رسم جافاسكريبت. وده بيحصل <b>مسبقاً</b>، على جهازك أو في الـ CI — وعمره ما بيحصل في براوزر المستخدم.'},
     edge:'e1',hl:{aot:[1,2,6,7]}},
    {t:{en:'Because it happens at build time, your <b>HTML gets type-checked</b>. Binding a string to a number input is a build error — that is what <code class="i">strictTemplates</code> buys you.',
        ar:'وعشان بيحصل وقت الـ build، الـ <b>HTML بتاعك بيتفحص نوعه</b>. وربط نص بـ input نوعه رقم بيبقى ايرور build — وده اللي <code class="i">strictTemplates</code> بيشتريه لك.'},
     edge:'e2',hl:{aot:[20,21,22],cfg:[4]}},
    {t:{en:'The compiler itself never ships. The browser downloads plain JavaScript, which is why an Angular app starts faster than the framework&rsquo;s size suggests.',
        ar:'والـ compiler نفسه عمره ما بيتشحن. البراوزر بينزّل جافاسكريبت عادي، وعشان كده تطبيق أنجولار بيقلع أسرع مما حجم الفريمورك بيوحي.'},
     hl:{aot:[22,23]}},
    {t:{en:'Tree shaking then deletes anything nothing references. <code class="i">providedIn: \'root\'</code> is tree-shakable; a class listed in a <code class="i">providers</code> array is not, because that array references it.',
        ar:'وبعدين الـ tree shaking بيمسح أي حاجة محدش بيشير لها. <code class="i">providedIn: \'root\'</code> قابلة للشذب؛ أما الكلاس اللي في مصفوفة <code class="i">providers</code> فلأ، لإن المصفوفة بتشير له.'},
     hl:{shake:[7,8,9,12,13]}},
    {t:{en:'Two things quietly defeat it: <b>barrel files</b> that re-export everything, and <b>top-level side effects</b> the bundler cannot prove are safe to drop.',
        ar:'وحاجتين بيلغوه في هدوء: <b>ملفات الـ barrel</b> اللي بتعيد تصدير كل حاجة، و<b>الآثار الجانبية</b> في أعلى الملف اللي الـ bundler مش قادر يثبت إن مسحها آمن.'},
     hl:{shake:[15,16,18,19,21,22,23]}},
    {t:{en:'And you can check all of it yourself — read the emitted JavaScript, then prove what survived with a treemap.',
        ar:'وتقدر تتأكد من ده كله بنفسك — اقرا الجافاسكريبت الناتج، وبعدين اثبت اللي فضل بخريطة.'},
     hl:{see:[2,6,7,10,14,15]}}
  ],
  example:[
    {
      name:{en:'The barrel file that cost 180 kB', ar:'ملف الـ barrel اللي كلّف 180 كيلو'},
      what:{
        en:'Barrel files feel tidy and quietly ruin tree shaking. Importing one symbol from a barrel pulls in the whole barrel, and every module it re-exports comes with it — including the chart library you use on one admin page.',
        ar:'ملفات الـ barrel شكلها مرتب وبتخرّب الـ tree shaking في هدوء. وعمل import لرمز واحد من barrel بيجيب الـ barrel كله، وكل module بيعيد تصديره بييجي معاه — بما فيها مكتبة الرسوم اللي بتستخدمها في صفحة أدمن واحدة.'},
      files:[
        {id:'bl1',name:'the tidy-looking version',lang:'ts',tag:{en:'costs 180 kB',ar:'بتكلف 180 كيلو'},code:[
          "// src/app/ui/index.ts — the barrel",
          "export * from './button';",
          "export * from './card';",
          "export * from './chart';      // ← imports a 180 kB charting lib",
          "export * from './table';",
          "",
          "// src/app/home/home.ts",
          "import { Button } from '../ui';    // I only want Button…",
          "",
          "// …but the bundler must evaluate ./ui/index.ts, which imports",
          "// ./chart, which imports the charting library. it is now in",
          "// your main bundle, on the home page, for every visitor."]},
        {id:'bl2',name:'the fix',lang:'ts',tag:{en:'import the file',ar:'اعمل import للملف'},code:[
          "import { Button } from '../ui/button';",
          "",
          "// deep imports look less tidy and are worth it.",
          "// if you must keep a barrel for ergonomics, make sure",
          "// package.json says  \"sideEffects\": false  so the bundler",
          "// is allowed to drop the unused re-exports.",
          "",
          "// then PROVE it:",
          "//   ng build --configuration production --stats-json",
          "//   npx source-map-explorer dist/**/*.js",
          "//   → is the chart library still in main.js?"]}
      ]
    }
  ],
  gotchas:[
    {en:'AOT has been the only mode since Angular 9. If a tutorial talks about “switching on AOT”, it predates 2020 and the rest of it is probably stale too.',
     ar:'الـ AOT هو الوضع الوحيد من أنجولار 9. ولو شرح بيتكلم عن «تشغيل الـ AOT»، يبقى من قبل 2020 وباقيه غالباً قديم كمان.'},
    {en:'Barrel files defeat tree shaking. Import the specific file, or set <code class="i">"sideEffects": false</code> so the bundler is allowed to drop the rest.',
     ar:'ملفات الـ barrel بتلغي الـ tree shaking. اعمل import للملف المحدد، أو حط <code class="i">"sideEffects": false</code> عشان الـ bundler يسمح لنفسه يمسح الباقي.'},
    {en:'A class in a <code class="i">providers</code> array can never be tree-shaken — the array references it. <code class="i">providedIn: \'root\'</code> can be.',
     ar:'الكلاس اللي في مصفوفة <code class="i">providers</code> مستحيل يتشذّب — المصفوفة بتشير له. أما <code class="i">providedIn: \'root\'</code> فينفع.'},
    {en:'Top-level side effects (<code class="i">console.log</code>, assigning to <code class="i">window</code>) make a whole module unshakeable. Keep module scope pure.',
     ar:'الآثار الجانبية في أعلى الملف (<code class="i">console.log</code> أو الإسناد لـ <code class="i">window</code>) بتخلي الـ module كله غير قابل للشذب. خلي نطاق الـ module نضيف.'},
    {en:'Never diagnose bundle size from a dev build. Only <code class="i">--configuration production</code> minifies and tree shakes.',
     ar:'عمرك ما تشخّص حجم الـ bundle من build تطوير. <code class="i">--configuration production</code> بس هو اللي بيصغّر ويشذّب.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'pwa',
  badge:{en:'Offline', ar:'من غير نت'},
  title:{en:'PWAs and the Angular service worker', ar:'الـ PWA وservice worker بتاع أنجولار'},
  lead:{
    en:`A service worker is a script that sits between your app and the network. Angular ships a configurable one, so you declare <b>what to cache in a JSON file</b> instead of writing fetch handlers. The result: the app opens instantly on a repeat visit, and keeps working on a bad connection.`,
    ar:`الـ service worker سكريبت بيقعد بين تطبيقك والشبكة. وأنجولار بييجي بواحد قابل للإعداد، فبتعلن <b>هتخزّن إيه في ملف JSON</b> بدل ما تكتب معالجات fetch. والنتيجة: التطبيق بيفتح فوراً في الزيارة التانية، وبيفضل شغال على نت وحش.`
  },
  nodes:[
    [{id:'cfg',role:{en:'declares the caching',ar:'بيعلن التخزين'},file:'ngsw-config',ext:'.json',kind:'cfg'}],
    [{id:'sw',role:{en:'intercepts requests',ar:'بيعترض الريكويستات'},file:'ngsw-worker',ext:'.js',kind:'svc'},
     {id:'app',role:{en:'unaware of any of it',ar:'مش عارف حاجة'},file:'your app',ext:'',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'app',to:'sw',a:'top',b:'right',label:'GET /api/products',packet:'request'},
    {id:'e2',from:'sw',to:'cfg',a:'top',b:'bottom',label:'which strategy?',packet:'freshness'},
    {id:'e3',from:'sw',to:'app',a:'left',b:'bottom',label:'cache or network',packet:'response'}
  ],
  files:[
    {id:'add',name:'adding it',lang:'bash',tag:{en:'one command',ar:'أمر واحد'},code:[
      "ng add @angular/pwa",
      "",
      "# it:",
      "#   - installs @angular/service-worker",
      "#   - adds provideServiceWorker() to app.config.ts",
      "#   - creates ngsw-config.json",
      "#   - creates manifest.webmanifest and placeholder icons",
      "#   - registers the worker in production builds only",
      "",
      "# IMPORTANT: it does NOT work with `ng serve`.",
      "ng build --configuration production",
      "npx http-server dist/my-app/browser -p 4200",
      "# now open localhost:4200 and check Application → Service Workers"]},
    {id:'cfg',name:'ngsw-config.json',lang:'json',tag:{en:'the whole policy',ar:'السياسة كلها'},code:[
      "{",
      "  \"index\": \"/index.html\",",
      "",
      "  \"assetGroups\": [",
      "    {",
      "      \"name\": \"app\",",
      "      \"installMode\": \"prefetch\",        // download at install time",
      "      \"resources\": { \"files\": [\"/index.html\", \"/*.css\", \"/*.js\"] }",
      "    },",
      "    {",
      "      \"name\": \"assets\",",
      "      \"installMode\": \"lazy\",            // cache on first use",
      "      \"updateMode\": \"prefetch\",",
      "      \"resources\": { \"files\": [\"/media/**\", \"/*.woff2\"] }",
      "    }",
      "  ],",
      "",
      "  \"dataGroups\": [",
      "    {",
      "      \"name\": \"api-fresh\",",
      "      \"urls\": [\"/api/**\"],",
      "      \"cacheConfig\": {",
      "        \"strategy\": \"freshness\",        // try network, fall back to cache",
      "        \"maxSize\": 100,",
      "        \"maxAge\": \"1h\",",
      "        \"timeout\": \"3s\"                 // …after waiting this long",
      "      }",
      "    },",
      "    {",
      "      \"name\": \"api-static\",",
      "      \"urls\": [\"/api/countries\", \"/api/config\"],",
      "      \"cacheConfig\": {",
      "        \"strategy\": \"performance\",      // cache first, do not even ask",
      "        \"maxSize\": 20,",
      "        \"maxAge\": \"7d\"",
      "      }",
      "    }",
      "  ]",
      "}"]},
    {id:'update',name:'telling people a new version exists',lang:'ts',tag:{en:'you must do this',ar:'لازم تعمل دي'},code:[
      "import { SwUpdate, VersionReadyEvent } from '@angular/service-worker';",
      "",
      "@Injectable({ providedIn: 'root' })",
      "export class Updates {",
      "  private readonly sw = inject(SwUpdate);",
      "  private readonly toasts = inject(Toasts);",
      "",
      "  constructor() {",
      "    if (!this.sw.isEnabled) return;",
      "",
      "    // a new version has been downloaded and is waiting",
      "    this.sw.versionUpdates",
      "      .pipe(filter((e): e is VersionReadyEvent => e.type === 'VERSION_READY'),",
      "            takeUntilDestroyed())",
      "      .subscribe(() => {",
      "        this.toasts.action('A new version is available.', 'Reload', () => {",
      "          this.sw.activateUpdate().then(() => location.reload());",
      "        });",
      "      });",
      "",
      "    // check for one every 6 hours for long-lived tabs",
      "    setInterval(() => this.sw.checkForUpdate(), 6 * 60 * 60 * 1000);",
      "  }",
      "}",
      "",
      "// WITHOUT this, a user with the tab open sees your OLD app",
      "// indefinitely — the worker downloads the new one and waits."]},
    {id:'off',name:'knowing you are offline',lang:'ts',tag:{en:'tell the person',ar:'قول للشخص'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class Network {",
      "  readonly online = signal(navigator.onLine);",
      "",
      "  constructor() {",
      "    const on  = () => this.online.set(true);",
      "    const off = () => this.online.set(false);",
      "    addEventListener('online', on);",
      "    addEventListener('offline', off);",
      "    inject(DestroyRef).onDestroy(() => {",
      "      removeEventListener('online', on);",
      "      removeEventListener('offline', off);",
      "    });",
      "  }",
      "}",
      "",
      "// template:",
      "//   @if (!network.online()) {",
      "//     <div class=\"offline-bar\" role=\"status\">",
      "//       You are offline. Showing saved data.",
      "//     </div>",
      "//   }"]},
    {id:'when',name:'is a PWA worth it here?',lang:'ts',tag:{en:'be honest',ar:'كن صادق'},code:[
      "// ✓ WORTH IT",
      "//   - people return daily (a dashboard, a tool, a reader)",
      "//   - connectivity is unreliable (field work, transit, rural)",
      "//   - the data is mostly read and changes slowly",
      "//   - you want an installable icon without an app store",
      "",
      "// ✗ USUALLY NOT WORTH IT",
      "//   - a marketing site people visit once",
      "//   - anything where stale data is dangerous (trading, live stock)",
      "//   - an app that changes every day — you will fight the cache",
      "",
      "// THE COST: a whole extra caching layer to reason about.",
      "// 'why am I seeing the old version?' becomes a support question",
      "// you will answer many times."]}
  ],
  steps:[
    {t:{en:'<code class="i">ng add &#64;angular/pwa</code> wires everything: the worker, the manifest, the icons and the registration. You configure it with JSON rather than code.',
        ar:'<code class="i">ng add &#64;angular/pwa</code> بيوصّل كل حاجة: الـ worker والـ manifest والأيقونات والتسجيل. وبتعدّه بـ JSON مش بكود.'},
     hl:{add:[1,4,5,6,7]}},
    {t:{en:'<b>assetGroups</b> cache your own build output. <code class="i">prefetch</code> downloads at install so the app opens offline; <code class="i">lazy</code> caches on first use.',
        ar:'<b>assetGroups</b> بتخزّن ناتج الـ build بتاعك. <code class="i">prefetch</code> بينزّل عند التثبيت فالتطبيق يفتح من غير نت؛ و<code class="i">lazy</code> بيخزّن عند أول استخدام.'},
     edge:'e2',hl:{cfg:[4,7,11,12]}},
    {t:{en:'<b>dataGroups</b> cache API responses, and the strategy is the whole decision: <code class="i">freshness</code> tries the network first, <code class="i">performance</code> serves the cache without even asking.',
        ar:'<b>dataGroups</b> بتخزّن ردود الـ API، والاستراتيجية هي القرار كله: <code class="i">freshness</code> بتجرّب الشبكة الأول، و<code class="i">performance</code> بتقدّم الكاش من غير ما تسأل أصلاً.'},
     edge:'e1',hl:{cfg:[18,23,24,35]}},
    {t:{en:'A new deployment does <b>not</b> reach an open tab by itself. The worker downloads it and waits — you must offer a reload with <code class="i">SwUpdate</code>.',
        ar:'النشر الجديد <b>مش</b> بيوصل لتاب مفتوح لوحده. الـ worker بينزّله ويستنى — ولازم تعرض إعادة تحميل بـ <code class="i">SwUpdate</code>.'},
     edge:'e3',hl:{update:[12,15,16,17,26,27]}},
    {t:{en:'And tell people when they are offline. A cached page that silently shows yesterday&rsquo;s prices is worse than an honest “you are offline” bar.',
        ar:'وقول للناس لما يكونوا من غير نت. الصفحة المخزّنة اللي بتوري أسعار امبارح في صمت أوحش من شريط صادق بيقول «إنت من غير نت».'},
     hl:{off:[3,18,19,20]}},
    {t:{en:'And be honest about whether it fits. A caching layer you did not need becomes “why am I seeing the old version?” forever.',
        ar:'وكن صادق هل دي مناسبة ولا لأ. طبقة تخزين مكنتش محتاجها بتبقى «ليه بشوف النسخة القديمة؟» للأبد.'},
     hl:{when:[2,3,4,9,10,13,14]}}
  ],
  example:[
    {
      name:{en:'The update problem, and why it bites', ar:'مشكلة التحديث، وليه بتوجع'},
      what:{
        en:'You deploy a fix. A user has had the tab open since yesterday. Without SwUpdate they keep running the broken version indefinitely — and they will tell you the bug is not fixed, and they will be right.',
        ar:'إنت نشرت إصلاح. والمستخدم فاتح التاب من امبارح. من غير SwUpdate هيفضل شغال على النسخة البايظة للأبد — وهيقولك الباج مش متصلّح، وهيبقى معاه حق.'},
      files:[
        {id:'up1',name:'what actually happens',lang:'ts',tag:{en:'the sequence',ar:'التسلسل'},code:[
          "// 1. you deploy. index.html and the JS hashes change.",
          "// 2. the open tab's service worker is STILL serving the old",
          "//    files from cache. the user sees no change at all.",
          "// 3. on the next check, the worker downloads the new version",
          "//    in the background — and installs it as WAITING.",
          "// 4. it activates only when EVERY tab for your origin closes.",
          "//",
          "// a user who never closes the tab never gets the fix.",
          "// this is by design: it stops the app changing under their hands.",
          "//",
          "// your job is step 5: notice, and offer the reload."]},
        {id:'up2',name:'the minimum you must ship',lang:'ts',tag:{en:'do not skip this',ar:'متعديهاش'},code:[
          "constructor() {",
          "  const sw = inject(SwUpdate);",
          "  if (!sw.isEnabled) return;",
          "",
          "  sw.versionUpdates.pipe(",
          "    filter((e): e is VersionReadyEvent => e.type === 'VERSION_READY'),",
          "    takeUntilDestroyed(),",
          "  ).subscribe(async () => {",
          "    // for a critical fix you may reload without asking.",
          "    // for anything else, ASK — reloading loses unsaved work.",
          "    if (confirm('A new version is available. Reload now?')) {",
          "      await sw.activateUpdate();",
          "      location.reload();",
          "    }",
          "  });",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'The service worker <b>does not run under <code class="i">ng serve</code></b>. Test it with a production build served by a static server, or you will conclude it is broken.',
     ar:'الـ service worker <b>مش بيشتغل مع <code class="i">ng serve</code></b>. اختبره بـ build إنتاج مقدّم من سيرفر ثابت، وإلا هتستنتج إنه بايظ.'},
    {en:'Without <code class="i">SwUpdate</code>, a user with an open tab runs your old version indefinitely. Shipping a PWA without update handling is shipping a bug.',
     ar:'من غير <code class="i">SwUpdate</code>، المستخدم اللي فاتح تاب بيفضل على نسختك القديمة للأبد. ونشر PWA من غير التعامل مع التحديث ده نشر باج.'},
    {en:'<code class="i">strategy: "performance"</code> on an endpoint whose data matters will show stale data confidently. Use <code class="i">freshness</code> with a timeout unless the data really is static.',
     ar:'<code class="i">strategy: "performance"</code> على endpoint داتاه مهمة هيوري داتا قديمة بثقة. استعمل <code class="i">freshness</code> مع timeout إلا لو الداتا ثابتة فعلاً.'},
    {en:'Only <code class="i">GET</code> requests are cached. A <code class="i">POST</code> made offline simply fails — real offline writes need a queue you build yourself (IndexedDB plus a sync on reconnect).',
     ar:'الـ <code class="i">GET</code> بس هو اللي بيتخزّن. والـ <code class="i">POST</code> من غير نت بيفشل وخلاص — والكتابة الحقيقية من غير نت محتاجة طابور تبنيه بنفسك (IndexedDB مع مزامنة عند رجوع الاتصال).'},
    {en:'Debug in DevTools → Application → Service Workers. “Update on reload” and “Unregister” will save you hours of confusion during development.',
     ar:'صحّح في DevTools ← Application ← Service Workers. و«Update on reload» و«Unregister» هيوفروا عليك ساعات لخبطة أثناء التطوير.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'schematics',
  badge:{en:'Your own tooling', ar:'أدواتك إنت'},
  title:{en:'Custom schematics and builders', ar:'schematics و builders مخصصة'},
  lead:{
    en:`<code>ng generate component</code> is a schematic — a program that edits your project. You can write your own, so “create a feature” becomes one command instead of a page in a wiki that people follow differently. This is how a convention becomes <b>enforceable</b> instead of aspirational.`,
    ar:`<code>ng generate component</code> ده schematic — برنامج بيعدّل مشروعك. وتقدر تكتب بتاعك، فـ«اعمل فيتشر» تبقى أمر واحد بدل صفحة في wiki كل واحد بيطبقها بطريقة. وكده الـ convention بيبقى <b>قابل للفرض</b> بدل ما يبقى أمنية.`
  },
  nodes:[
    [{id:'coll',role:{en:'declares the command',ar:'بيعلن الأمر'},file:'collection',ext:'.json',kind:'cfg'}],
    [{id:'fac',role:{en:'edits the project',ar:'بيعدّل المشروع'},file:'index',ext:'.ts',kind:'ts'},
     {id:'tpl',role:{en:'the files to write',ar:'الملفات اللي هتتكتب'},file:'files/__name__',ext:'.ts.template',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'coll',to:'fac',a:'bottom',b:'top',label:'ng g my-lib:feature',packet:'options'},
    {id:'e2',from:'fac',to:'tpl',a:'right',b:'left',label:'apply(url("./files"))',packet:'render'}
  ],
  files:[
    {id:'coll',name:'collection.json',lang:'json',tag:{en:'the manifest',ar:'الإعلان'},code:[
      "{",
      "  \"$schema\": \"../node_modules/@angular-devkit/schematics/collection-schema.json\",",
      "  \"schematics\": {",
      "    \"feature\": {",
      "      \"description\": \"Generate a feature folder the way we do it here\",",
      "      \"factory\": \"./feature/index#feature\",",
      "      \"schema\": \"./feature/schema.json\"",
      "    }",
      "  }",
      "}",
      "",
      "// then:  ng generate my-tools:feature checkout"]},
    {id:'fac',name:'feature/index.ts',lang:'ts',tag:{en:'the program',ar:'البرنامج'},code:[
      "import { Rule, Tree, apply, url, applyTemplates, move,",
      "         mergeWith, chain } from '@angular-devkit/schematics';",
      "import { strings } from '@angular-devkit/core';",
      "",
      "export function feature(options: { name: string; path?: string }): Rule {",
      "  return chain([",
      "    // 1. write the files from templates",
      "    mergeWith(apply(url('./files'), [",
      "      applyTemplates({   // fills <%= %> and drops the .template ending",
      "        ...options,",
      "        ...strings,          // gives templates dasherize, classify, camelize",
      "      }),",
      "      move(options.path ?? `src/app/features/${strings.dasherize(options.name)}`),",
      "    ])),",
      "",
      "    // 2. edit an existing file",
      "    addRoute(options.name),",
      "  ]);",
      "}",
      "",
      "function addRoute(name: string): Rule {",
      "  return (tree: Tree) => {",
      "    const path = 'src/app/app.routes.ts';",
      "    const src = tree.read(path)?.toString('utf-8');",
      "    if (!src) return tree;",
      "",
      "    const route = `  { path: '${strings.dasherize(name)}', loadComponent: () =>\\n` +",
      "      `      import('./features/${strings.dasherize(name)}/${strings.dasherize(name)}')\\n` +",
      "      `        .then(m => m.${strings.classify(name)}) },\\n`;",
      "",
      "    tree.overwrite(path, src.replace('routes: Routes = [', 'routes: Routes = [\\n' + route));",
      "    return tree;",
      "  };",
      "}"]},
    {id:'tpl',name:'feature/files/__name@dasherize__.ts.template',lang:'ts',tag:{en:'the template',ar:'القالب'},code:[
      "import { Component, ChangeDetectionStrategy, inject } from '@angular/core';",
      "import { <%= classify(name) %>Store } from './<%= dasherize(name) %>.store';",
      "",
      "@Component({",
      "  selector: 'app-<%= dasherize(name) %>',",
      "  changeDetection: ChangeDetectionStrategy.OnPush,",
      "  templateUrl: './<%= dasherize(name) %>.html',",
      "})",
      "export class <%= classify(name) %> {",
      "  readonly store = inject(<%= classify(name) %>Store);",
      "}",
      "",
      "// the FILENAME is templated too:",
      "//   __name@dasherize__.ts.template  →  checkout.ts",
      "// and every file in ./files gets the same treatment, so your",
      "// team conventions (OnPush, a store, a spec) are the DEFAULT."]},
    {id:'build',name:'builders — custom ng commands',lang:'ts',tag:{en:'the other half',ar:'النص التاني'},code:[
      "// a schematic EDITS files. a builder RUNS something.",
      "// ng build, ng test and ng serve are all builders.",
      "",
      "import { createBuilder, BuilderOutput } from '@angular-devkit/architect';",
      "",
      "export default createBuilder(async (options: { manifest: string }, ctx) => {",
      "  ctx.logger.info('Verifying the release manifest…');",
      "",
      "  const ok = await verify(options.manifest);",
      "  if (!ok) return { success: false, error: 'Manifest check failed' };",
      "",
      "  return { success: true } as BuilderOutput;",
      "});",
      "",
      "// angular.json:",
      "//   \"verify\": { \"builder\": \"my-tools:verify\",",
      "//               \"options\": { \"manifest\": \"release.json\" } }",
      "//",
      "// then:  ng run my-app:verify",
      "",
      "// honestly: most teams need a custom SCHEMATIC and never need",
      "// a custom builder. an npm script usually does the job."]},
    {id:'mig',name:'the ones you already use',lang:'bash',tag:{en:'ng update',ar:'ng update'},code:[
      "# every Angular upgrade runs migration schematics for you",
      "ng update @angular/core @angular/cli",
      "",
      "# and you can run individual ones by hand:",
      "ng generate @angular/core:control-flow          # *ngIf → @if",
      "ng generate @angular/core:signal-inputs         # @Input → input()",
      "ng generate @angular/core:signal-queries        # @ViewChild → viewChild()",
      "ng generate @angular/core:output-migration      # @Output → output()",
      "ng generate @angular/core:inject                # constructor DI → inject()",
      "ng generate @angular/core:standalone            # NgModules → standalone",
      "",
      "# this is the single best argument for schematics: Angular",
      "# migrates YOUR code for you, at scale, correctly."]}
  ],
  steps:[
    {t:{en:'A schematic is registered in <code class="i">collection.json</code>, which maps a command name to a factory function. That is what makes <code class="i">ng generate my-tools:feature</code> exist.',
        ar:'الـ schematic بيتسجّل في <code class="i">collection.json</code>، اللي بيربط اسم أمر بدالة factory. وده اللي بيخلي <code class="i">ng generate my-tools:feature</code> موجود.'},
     hl:{coll:[4,5,6,12]}},
    {t:{en:'The factory returns a <code class="i">Rule</code> — a function that takes a virtual file <code class="i">Tree</code> and returns a modified one. Nothing touches disk until every rule succeeds.',
        ar:'الـ factory بترجّع <code class="i">Rule</code> — دالة بتاخد <code class="i">Tree</code> ملفات افتراضي وترجّع واحد معدّل. ومفيش حاجة بتلمس الديسك لحد ما كل القواعد تنجح.'},
     edge:'e1',hl:{fac:[5,6,22,23]}},
    {t:{en:'Files come from templates. <code class="i">&lt;%= classify(name) %&gt;</code> in the content, and <code class="i">__name&#64;dasherize__</code> in the <b>filename</b>, so both follow your naming.',
        ar:'والملفات بتيجي من قوالب. <code class="i">&lt;%= classify(name) %&gt;</code> في المحتوى، و<code class="i">__name&#64;dasherize__</code> في <b>اسم الملف</b>، فالاتنين بيتبعوا تسميتك.'},
     edge:'e2',hl:{tpl:[5,9,13,14]}},
    {t:{en:'A rule can also <b>edit existing files</b> — reading <code class="i">app.routes.ts</code> and inserting a route is what turns a generator into real automation.',
        ar:'والقاعدة تقدر كمان <b>تعدّل ملفات موجودة</b> — قراية <code class="i">app.routes.ts</code> وإدخال route فيها هي اللي بتحوّل المولّد لأتمتة حقيقية.'},
     hl:{fac:[17,22,24,31]}},
    {t:{en:'You already rely on this: <code class="i">ng update</code> runs migration schematics that rewrite your code across a whole codebase. That is the strongest argument for the mechanism.',
        ar:'وإنت بتعتمد على ده أصلاً: <code class="i">ng update</code> بيشغّل migration schematics بتعيد كتابة كودك في المشروع كله. وده أقوى دليل على فايدة الميكانيزم ده.'},
     hl:{mig:[2,5,6,12,13]}},
    {t:{en:'A builder is the other half — it <em>runs</em> something rather than editing files. Most teams need a schematic and never need one.',
        ar:'والـ builder هو النص التاني — بي<em>شغّل</em> حاجة بدل ما يعدّل ملفات. ومعظم الفرق محتاجة schematic ومش محتاجة واحد أبداً.'},
     hl:{build:[1,2,6,17,18]}}
  ],
  example:[
    {
      name:{en:'When a schematic pays for itself', ar:'إمتى الـ schematic يعوّض تكلفته'},
      what:{
        en:'Writing one costs a day. It pays off when the same folder shape is created often and people keep getting it subtly wrong — because a schematic is a convention that cannot be forgotten, misread, or skipped when someone is in a hurry.',
        ar:'كتابة واحد بتكلف يوم. وبيعوّض لما نفس شكل الفولدر يتعمل كتير والناس بتغلط فيه بشكل بسيط — لإن الـ schematic convention مستحيل يتنسي أو يتقرا غلط أو يتعدّى لما حد يكون مستعجل.'},
      files:[
        {id:'sc1',name:'the wiki page version',lang:'bash',tag:{en:'aspirational',ar:'أمنية'},code:[
          "# \"How to add a feature\" — the wiki page nobody follows exactly",
          "# 1. create src/app/features/<name>/",
          "# 2. add <name>.ts with OnPush",
          "# 3. add <name>.html",
          "# 4. add <name>.store.ts with the private/readonly pattern",
          "# 5. add <name>.spec.ts",
          "# 6. register a lazy route in app.routes.ts",
          "# 7. remember the barrel… no wait, we stopped doing barrels",
          "",
          "# result after six months: eleven features, six shapes,",
          "# three of them missing OnPush."]},
        {id:'sc2',name:'the schematic version',lang:'bash',tag:{en:'enforceable',ar:'مفروض'},code:[
          "ng generate my-tools:feature checkout",
          "",
          "CREATE src/app/features/checkout/checkout.ts",
          "CREATE src/app/features/checkout/checkout.html",
          "CREATE src/app/features/checkout/checkout.store.ts",
          "CREATE src/app/features/checkout/checkout.spec.ts",
          "UPDATE src/app/app.routes.ts",
          "",
          "# eleven features, one shape, OnPush every time.",
          "# and when the convention changes, you change the template",
          "# once instead of asking eleven people to refactor."]}
      ]
    }
  ],
  gotchas:[
    {en:'Test with <code class="i">--dry-run</code> first. It prints every file it would create or change without touching disk.',
     ar:'اختبر بـ <code class="i">--dry-run</code> الأول. بيطبع كل ملف هيعمله أو يغيّره من غير ما يلمس الديسك.'},
    {en:'Template files should end in <code class="i">.template</code> so the TypeScript compiler does not try to build them as source.',
     ar:'ملفات القوالب لازم تنتهي بـ <code class="i">.template</code> عشان الـ TypeScript compiler ميحاولش يبنيها كمصدر.'},
    {en:'Editing an existing file by string replacement is brittle. For anything beyond a simple insert, parse with the TypeScript AST instead of regex.',
     ar:'تعديل ملف موجود باستبدال نصوص ده هش. وأي حاجة أكبر من إدخال بسيط، حلّلها بـ TypeScript AST مش بـ regex.'},
    {en:'A schematic is a build-time tool, so it belongs in <code class="i">devDependencies</code>. Shipping it as a runtime dependency bloats every install.',
     ar:'الـ schematic أداة وقت build، فمكانه <code class="i">devDependencies</code>. وشحنه كاعتمادية وقت تشغيل بيضخّم كل تثبيت.'},
    {en:'Most teams need a schematic and never need a custom <b>builder</b>. If an npm script does the job, write the npm script.',
     ar:'معظم الفرق محتاجة schematic ومش محتاجة <b>builder</b> مخصص أبداً. لو npm script بيعمل الشغل، اكتب الـ npm script.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'micro-frontends',
  badge:{en:'Many teams', ar:'فرق كتير'},
  title:{en:'Micro frontends and Module Federation', ar:'الـ Micro frontends و Module Federation'},
  lead:{
    en:`Micro frontends let separate teams deploy parts of one application independently. This is an <b>organisational</b> solution to an organisational problem — and it is the most over-adopted pattern in frontend. Read the costs before the setup, because they are paid by everyone, forever.`,
    ar:`الـ micro frontends بتخلي فرق منفصلة تنشر أجزاء من تطبيق واحد بشكل مستقل. وده حل <b>تنظيمي</b> لمشكلة تنظيمية — وهو أكتر نمط بيتبنى في غير محله في الفرونت إند. اقرا التكاليف قبل الإعداد، لإن الكل بيدفعها، للأبد.`
  },
  nodes:[
    [{id:'shell',role:{en:'the host',ar:'المضيف'},file:'shell',ext:'',kind:'ts'}],
    [{id:'a',role:{en:'team A deploys this',ar:'فريق A بينشر ده'},file:'checkout',ext:'',kind:'svc'},
     {id:'b',role:{en:'team B deploys this',ar:'فريق B بينشر ده'},file:'catalogue',ext:'',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'shell',to:'a',a:'bottom',b:'top',label:'loadRemoteModule',packet:'at runtime'},
    {id:'e2',from:'shell',to:'b',a:'bottom',b:'top',label:'loadRemoteModule',packet:'at runtime'}
  ],
  files:[
    {id:'first',name:'read this before anything else',lang:'ts',tag:{en:'the honest part',ar:'الجزء الصادق'},code:[
      "// YOU PROBABLY DO NOT NEED THIS.",
      "//",
      "// micro frontends solve exactly one problem: several teams",
      "// that must deploy INDEPENDENTLY, on different schedules,",
      "// without coordinating a release.",
      "//",
      "// they do NOT solve:",
      "//   ✗ 'our bundle is too big'        → lazy loading does that",
      "//   ✗ 'our codebase is messy'        → a micro frontend is the",
      "//                                      same mess, now distributed",
      "//   ✗ 'we want to try React too'     → you want two design systems,",
      "//                                      two a11y baselines and two bugs",
      "//",
      "// WHAT THEY COST, permanently:",
      "//   - shared dependency version skew, at RUNTIME, in production",
      "//   - debugging across independently deployed bundles",
      "//   - duplicated framework code unless sharing is exactly right",
      "//   - integration bugs that no single team's CI can catch",
      "//   - a much harder story for routing, auth and design consistency",
      "//",
      "// A MONOREPO WITH CLEAR LIBRARY BOUNDARIES gives you most of the",
      "// benefit at a fraction of the cost. Try that first. Genuinely."]},
    {id:'setup',name:'if you do need it: Native Federation',lang:'bash',tag:{en:'the Angular way',ar:'طريقة أنجولار'},code:[
      "# Native Federation works with the modern esbuild-based CLI.",
      "# (Webpack Module Federation is the older, webpack-only route.)",
      "",
      "# in the shell (the host):",
      "ng add @angular-architects/native-federation --project shell --type dynamic-host",
      "",
      "# in each remote:",
      "ng add @angular-architects/native-federation --project checkout --type remote --port 4201",
      "",
      "# each app builds and deploys on its own.",
      "# the shell discovers remotes at RUNTIME, not at build time —",
      "# which is the entire point, and the entire risk."]},
    {id:'remote',name:'federation.config.js — the remote',lang:'ts',tag:{en:'what it exposes',ar:'بيعرض إيه'},code:[
      "module.exports = withNativeFederation({",
      "  name: 'checkout',",
      "",
      "  exposes: {",
      "    './routes': './src/app/checkout/checkout.routes.ts',",
      "  },",
      "",
      "  shared: {",
      "    ...shareAll({",
      "      singleton: true,        // ONE copy of Angular across all apps",
      "      strictVersion: true,    // fail loudly on a version mismatch",
      "      requiredVersion: 'auto',",
      "    }),",
      "  },",
      "",
      "  // singleton: true is not optional for @angular/core.",
      "  // two copies of Angular in one page means two injector trees,",
      "  // two zone states, and bugs that make no sense at all.",
      "});"]},
    {id:'shell',name:'the shell loads it at runtime',lang:'ts',tag:{en:'the host',ar:'المضيف'},code:[
      "import { loadRemoteModule } from '@angular-architects/native-federation';",
      "",
      "export const routes: Routes = [",
      "  { path: '', component: Home },",
      "",
      "  {",
      "    path: 'checkout',",
      "    // this code does not exist at build time. it is fetched,",
      "    // from a URL, while the user is waiting.",
      "    loadChildren: () =>",
      "      loadRemoteModule({",
      "        remoteName: 'checkout',",
      "        exposedModule: './routes',",
      "      }).then(m => m.CHECKOUT_ROUTES),",
      "  },",
      "];",
      "",
      "// and because it is a network call to another team's deployment,",
      "// it MUST have a failure path:",
      "//   what does the user see when checkout is down or incompatible?"]},
    {id:'alt',name:'the alternatives, ranked',lang:'ts',tag:{en:'try these first',ar:'جرّب دول الأول'},code:[
      "// 1. ONE APP, LAZY ROUTES              ← almost always the answer",
      "//    independent CODE, one deployment. teams own folders.",
      "",
      "// 2. MONOREPO + LIBRARY BOUNDARIES     ← Nx, or plain workspace libs",
      "//    enforced import rules, shared CI, atomic refactors across",
      "//    every consumer. this is what most 'we need micro frontends'",
      "//    teams actually wanted.",
      "",
      "// 3. BUILD-TIME COMPOSITION            ← publish libs to a registry",
      "//    independent versioning, integration verified at BUILD time",
      "//    instead of in front of a user.",
      "",
      "// 4. RUNTIME FEDERATION                ← only if independent DEPLOY",
      "//    is a hard requirement you can defend in one sentence.",
      "",
      "// if you cannot say WHY 1–3 fail for you, the answer is 1."]}
  ],
  steps:[
    {t:{en:'Start with the question, not the tooling. Micro frontends solve <b>independent deployment</b> by separate teams — and nothing else on the list people usually give.',
        ar:'ابدأ بالسؤال مش بالأدوات. الـ micro frontends بتحل <b>النشر المستقل</b> لفرق منفصلة — ومفيش حاجة تانية من اللستة اللي الناس بتقولها عادةً.'},
     hl:{first:[1,3,4,5,8,9]}},
    {t:{en:'The costs are permanent and paid by everyone: version skew at runtime, cross-bundle debugging, and integration bugs no single team&rsquo;s CI can catch.',
        ar:'والتكاليف دايمة والكل بيدفعها: اختلاف الإصدارات وقت التشغيل، والتصحيح عبر bundles، وباجات تكامل مفيش CI بتاعة فريق واحد تقدر تمسكها.'},
     hl:{first:[15,16,17,18,19,21,22]}},
    {t:{en:'If you still need it, each remote <b>exposes</b> a routes file, and every app declares Angular as a <code class="i">singleton</code>. Two copies of Angular in one page is the classic disaster.',
        ar:'ولو لسه محتاجه، كل remote بي<b>عرض</b> ملف routes، وكل تطبيق بيعلن أنجولار كـ <code class="i">singleton</code>. ونسختين من أنجولار في صفحة واحدة دي الكارثة الكلاسيكية.'},
     edge:'e1',hl:{remote:[4,5,9,10,17,18]}},
    {t:{en:'The shell loads a remote <b>at runtime</b>, over the network, from another team&rsquo;s deployment. That call can fail — so it needs a real failure path, not a blank screen.',
        ar:'والـ shell بيحمّل الـ remote <b>وقت التشغيل</b>، عبر الشبكة، من نشر فريق تاني. والنداء ده ممكن يفشل — فمحتاج مسار فشل حقيقي، مش شاشة بيضا.'},
     edge:'e2',hl:{shell:[8,9,11,19,20]}},
    {t:{en:'Before any of it, try the ladder: lazy routes, then a monorepo with enforced boundaries, then build-time composition. If you cannot say why those fail, the answer is lazy routes.',
        ar:'وقبل ده كله، جرّب السلم: راوتس كسلانة، وبعدين monorepo بحدود مفروضة، وبعدين تركيب وقت الـ build. ولو مش قادر تقول ليه دول مش نافعين، يبقى الإجابة الراوتس الكسلانة.'},
     hl:{alt:[1,4,5,12,16]}},
    {t:{en:'If you genuinely need it, Native Federation is the modern route — each app builds and deploys alone, and the shell discovers it at runtime.',
        ar:'ولو محتاجه فعلاً، Native Federation هو الطريق الحديث — كل تطبيق بيتبني وينتشر لوحده، والـ shell بيكتشفه وقت التشغيل.'},
     hl:{setup:[1,2,5,8,11,12]}}
  ],
  example:[
    {
      name:{en:'What most teams actually needed', ar:'اللي معظم الفرق كانت محتاجاه فعلاً'},
      what:{
        en:'The stated problem is usually "four teams keep blocking each other". The cause is almost never the deployment unit — it is that everything imports everything. Enforced module boundaries fix that without a single runtime dependency.',
        ar:'المشكلة المعلنة عادةً «أربع فرق بيعطّلوا بعض». والسبب تقريباً عمره ما بيكون وحدة النشر — السبب إن كل حاجة بتعمل import لكل حاجة. والحدود المفروضة بتحل ده من غير ولا اعتمادية وقت تشغيل.'},
      files:[
        {id:'mf1',name:'enforced boundaries, one deployment',lang:'json',tag:{en:'lint rule',ar:'قاعدة lint'},code:[
          "// eslint: @nx/enforce-module-boundaries (or a plain import rule)",
          "{",
          "  \"depConstraints\": [",
          "    { \"sourceTag\": \"scope:checkout\",",
          "      \"onlyDependOnLibsWithTags\": [\"scope:checkout\", \"scope:shared\"] },",
          "",
          "    { \"sourceTag\": \"scope:catalogue\",",
          "      \"onlyDependOnLibsWithTags\": [\"scope:catalogue\", \"scope:shared\"] },",
          "",
          "    { \"sourceTag\": \"scope:shared\",",
          "      \"onlyDependOnLibsWithTags\": [\"scope:shared\"] }",
          "  ]",
          "}",
          "",
          "// now team A CANNOT import team B's internals — the build fails.",
          "// teams are isolated, ownership is clear, and there is still",
          "// one deployment, one Angular, one CI and one bundle to debug."]},
        {id:'mf2',name:'the honest test',lang:'ts',tag:{en:'ask this',ar:'اسأل ده'},code:[
          "// answer this in ONE sentence, out loud, before adopting:",
          "//",
          "//   \"We need micro frontends because ____________ ,",
          "//    and a monorepo with enforced boundaries cannot",
          "//    solve it because ____________ .\"",
          "//",
          "// good answers exist. examples:",
          "//   - three companies after an acquisition, separate release",
          "//     approval processes and separate compliance sign-off",
          "//   - a platform where third parties ship UI you never build",
          "//",
          "// if the second blank is hard to fill, you have your answer."]}
      ]
    }
  ],
  gotchas:[
    {en:'Angular <b>must</b> be a <code class="i">singleton</code>. Two copies in one page means two injector trees and bugs that make no sense — this is the number one federation failure.',
     ar:'أنجولار <b>لازم</b> يكون <code class="i">singleton</code>. ونسختين في صفحة واحدة معناها شجرتين injector وباجات مالهاش أي معنى — وده الفشل رقم واحد في الـ federation.'},
    {en:'Version skew is a <b>runtime</b> failure in production. Team A upgrades Angular, team B has not, and the page breaks for users — not in anybody&rsquo;s CI.',
     ar:'اختلاف الإصدارات ده فشل <b>وقت التشغيل</b> في الإنتاج. فريق A بيرقّي أنجولار وفريق B لأ، والصفحة بتتكسر عند المستخدمين — مش في CI حد.'},
    {en:'A remote is a network dependency you do not control. Decide now what the user sees when it is down, and build that screen.',
     ar:'الـ remote اعتمادية شبكة إنت مش متحكم فيها. قرر دلوقتي المستخدم هيشوف إيه لما تقع، وابني الشاشة دي.'},
    {en:'Shared state across remotes is the hardest part. Auth, theme and routing need an explicit contract — usually an injection token exposed by the shell.',
     ar:'الحالة المشتركة بين الـ remotes أصعب جزء. الـ auth والثيم والـ routing محتاجين عقد صريح — وغالباً injection token بيعرضه الـ shell.'},
    {en:'If you cannot explain in one sentence why lazy routes and a monorepo fail for you, you do not need this. That is not a slogan — it is the most common expensive mistake in frontend architecture.',
     ar:'لو مش قادر تشرح في جملة واحدة ليه الراوتس الكسلانة والـ monorepo مش نافعين معاك، يبقى إنت مش محتاج ده. ودي مش شعار — دي أشهر غلطة غالية في معمار الفرونت إند.'}
  ]
},

]};
