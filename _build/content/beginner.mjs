export default {
id:'beginner', ink:'--beg',
kicker:{en:'Track one', ar:'المستوى الأول'},
name:{en:'Beginner', ar:'مبتدئ'},
dek:{
  en:`The five ideas everything else stands on. Write a component, loop a list, hold state in a signal, pass it in and out, share it through a service — and most of an app is already within reach.`,
  ar:`الخمس أفكار اللي كل حاجة تانية واقفة عليها. تكتب component، وتلف على list، وتحط الحالة في signal، وتمررها جوه وبره، وتشاركها من خلال service — وساعتها معظم أي تطبيق يبقى في إيدك.`
},
arts:[

/* A ------------------------------------------------------------------- */
{
  slug:'how-an-app-starts',
  badge:{en:'First principles', ar:'من أول وجديد'},
  title:{en:'How an Angular page actually appears', ar:'إزاي صفحة أنجولار بتظهر فعلاً'},
  lead:{
    en:`Your whole app has <b>one</b> HTML file, and it is almost empty. Everything you see in the browser is JavaScript putting things inside it. Four files do this, always in the same order — and once you can name them, nothing about Angular is mysterious again.`,
    ar:`تطبيقك كله فيه ملف HTML <b>واحد</b>، وهو تقريباً فاضي. وكل اللي بتشوفه في البراوزر ده جافاسكريبت بيحط حاجات جواه. أربع ملفات بيعملوا كده، ودايماً بنفس الترتيب — وأول ما تعرف تسمّيهم، مفيش حاجة في أنجولار هتفضل غامضة.`
  },
  nodes:[
    [{id:'idx',role:{en:'the only real page',ar:'الصفحة الحقيقية الوحيدة'},file:'index',ext:'.html',kind:'html'},
     {id:'main',role:{en:'the ignition',ar:'مفتاح التشغيل'},file:'main',ext:'.ts',kind:'ts'}],
    [{id:'cfg',role:{en:'the settings',ar:'الإعدادات'},file:'app.config',ext:'.ts',kind:'cfg'},
     {id:'root',role:{en:'the first component',ar:'أول component'},file:'app',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'idx',to:'main',a:'right',b:'left',label:'<script src="main.js">',packet:'load'},
    {id:'e2',from:'main',to:'cfg',a:'bottom',b:'right',label:'appConfig',packet:'providers'},
    {id:'e3',from:'main',to:'root',a:'bottom',b:'top',label:'bootstrapApplication(App)',packet:'App'},
    {id:'e4',from:'root',to:'idx',a:'left',b:'bottom',label:'renders into <app-root>',packet:'HTML'}
  ],
  files:[
    {id:'idx',name:'src/index.html',lang:'html',tag:{en:'served to the browser',ar:'اللي بيتبعت للبراوزر'},code:[
      "<!doctype html>",
      "<html lang=\"en\">",
      "<head>",
      "  <meta charset=\"utf-8\">",
      "  <title>MyShop</title>",
      "  <base href=\"/\">",
      "  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">",
      "</head>",
      "<body>",
      "  <app-root></app-root>   <!-- your entire app goes in here -->",
      "</body>",
      "</html>",
      "",
      "<!-- you never add <script> yourself — the build injects it -->"]},
    {id:'main',name:'src/main.ts',lang:'ts',tag:{en:'the first line that runs',ar:'أول سطر بيشتغل'},code:[
      "import { bootstrapApplication } from '@angular/platform-browser';",
      "import { appConfig } from './app/app.config';",
      "import { App } from './app/app';",
      "",
      "// \"start the app, using App as the first component,",
      "//  and appConfig as the settings\"",
      "bootstrapApplication(App, appConfig)",
      "  .catch(err => console.error(err));",
      "",
      "// that is the whole file. you will rarely touch it again."]},
    {id:'cfg',name:'src/app/app.config.ts',lang:'ts',tag:{en:'app-wide settings',ar:'إعدادات التطبيق كله'},code:[
      "import { ApplicationConfig, provideZonelessChangeDetection,",
      "         provideBrowserGlobalErrorListeners } from '@angular/core';",
      "import { provideRouter } from '@angular/router';",
      "import { routes } from './app.routes';",
      "",
      "export const appConfig: ApplicationConfig = {",
      "  providers: [",
      "    provideBrowserGlobalErrorListeners(),",
      "    provideZonelessChangeDetection(),",
      "    provideRouter(routes),        // add provideHttpClient() here later",
      "  ],",
      "};",
      "",
      "// this array is where every app-wide feature gets switched on."]},
    {id:'root',name:'src/app/app.ts',lang:'ts',tag:{en:'the first component',ar:'أول component'},code:[
      "import { Component, signal } from '@angular/core';",
      "import { RouterOutlet } from '@angular/router';",
      "",
      "@Component({",
      "  selector: 'app-root',          // ← matches <app-root> in index.html",
      "  imports: [RouterOutlet],",
      "  templateUrl: './app.html',",
      "  styleUrl: './app.css',",
      "})",
      "export class App {",
      "  protected readonly title = signal('my-shop');",
      "}"]},
    {id:'roothtml',name:'src/app/app.html',lang:'html',tag:{en:'what you see',ar:'اللي بتشوفه'},code:[
      "<h1>{{ title() }}</h1>",
      "",
      "<!-- the router swaps pages in and out right here -->",
      "<router-outlet />"]}
  ],
  steps:[
    {t:{en:'The browser downloads <code class="i">index.html</code>. It contains an empty tag, <code class="i">&lt;app-root&gt;&lt;/app-root&gt;</code>, and a script the build injected. At this instant the page is blank.',
        ar:'البراوزر بينزّل <code class="i">index.html</code>. فيه tag فاضي، <code class="i">&lt;app-root&gt;&lt;/app-root&gt;</code>، وسكريبت الـ build حطه. في اللحظة دي الصفحة بيضا.'},
     edge:'e1',hl:{idx:[10,14]}},
    {t:{en:'That script runs <code class="i">main.ts</code>. This is the first line of <em>your</em> code that executes, and it does exactly one thing: it calls <code class="i">bootstrapApplication</code>.',
        ar:'السكريبت ده بيشغّل <code class="i">main.ts</code>. ده أول سطر من كودك <em>إنت</em> بيتنفّذ، وبيعمل حاجة واحدة بس: بينادي <code class="i">bootstrapApplication</code>.'},
     hl:{main:[7,8]}},
    {t:{en:'It hands over <code class="i">appConfig</code> — the list of features the whole app can use. Routing, HTTP, anything global gets switched on in that one array.',
        ar:'وبيسلّم <code class="i">appConfig</code> — لستة الحاجات اللي التطبيق كله يقدر يستخدمها. الـ routing والـ HTTP وأي حاجة عامة بتتفتح في المصفوفة الواحدة دي.'},
     edge:'e2',hl:{cfg:[7,8,9,10,11]}},
    {t:{en:'And it names <code class="i">App</code> as the first component. Angular reads its <code class="i">selector</code>, <code class="i">\'app-root\'</code>, and goes looking for a matching tag in the page.',
        ar:'وبيسمّي <code class="i">App</code> كأول component. أنجولار بيقرا الـ <code class="i">selector</code> بتاعه، <code class="i">\'app-root\'</code>، ويروح يدوّر على tag مطابق في الصفحة.'},
     edge:'e3',hl:{root:[5,10]}},
    {t:{en:'It finds <code class="i">&lt;app-root&gt;</code>, renders <code class="i">app.html</code> inside it, and the page appears. Every other component in your app is rendered by a component that was itself rendered by this one.',
        ar:'بيلاقي <code class="i">&lt;app-root&gt;</code>، ويرندر <code class="i">app.html</code> جواه، والصفحة تظهر. وأي component تاني في تطبيقك بيترندر بواسطة component اترندر بواسطة ده.'},
     edge:'e4',hl:{roothtml:[1,4]}}
  ],
  example:{
    name:{en:'What “ng serve” is really doing', ar:'«ng serve» بيعمل إيه بالظبط'},
    what:{
      en:'The command starts a small web server, compiles your TypeScript to JavaScript, injects the script tag into index.html, and watches your files. Save a file and it recompiles only what changed and pushes it to the open browser tab — no refresh needed.',
      ar:'الأمر ده بيشغّل سيرفر ويب صغير، وبيحوّل الـ TypeScript بتاعك لجافاسكريبت، وبيحقن الـ script tag في index.html، وبيراقب ملفاتك. تحفظ ملف فيعيد بناء اللي اتغير بس ويبعته للتاب المفتوح — من غير ما تعمل refresh.'},
    files:[
      {id:'s1',name:'terminal',lang:'bash',tag:{en:'run it',ar:'شغّله'},code:[
        "$ ng serve",
        "",
        "Building...",
        "Initial chunk files | Names   | Raw size",
        "main.js             | main    | 24.19 kB",
        "polyfills.js        | polyfill| 34.58 kB",
        "",
        "Watch mode enabled. Watching for file changes...",
        "  ➜  Local:   http://localhost:4200/",
        "",
        "# open that URL. leave this running while you work."]},
      {id:'s2',name:'what the browser receives',lang:'html',tag:{en:'view-source',ar:'كود المصدر'},code:[
        "<body>",
        "  <app-root></app-root>",
        "  <script src=\"main.js\" type=\"module\"></script>",
        "</body>",
        "",
        "<!-- that is genuinely all of it. everything else is built",
        "     by JavaScript after this file loads. -->"]}
    ]
  },
  gotchas:[
    {en:'There is only <b>one</b> HTML file in the whole project. A new “page” is a component plus a route — you never create a second <code class="i">.html</code> page and link to it.',
     ar:'فيه ملف HTML <b>واحد</b> بس في المشروع كله. الـ «صفحة» الجديدة هي component مع route — عمرك ما هتعمل ملف <code class="i">.html</code> تاني وتعمل له لينك.'},
    {en:'A blank white page with no errors almost always means a crash inside a component. Open the browser console first — Angular prints the reason there, not in the terminal.',
     ar:'الصفحة البيضا من غير أخطاء معناها في الغالب كراش جوه component. افتح كونسول البراوزر الأول — أنجولار بيطبع السبب هناك، مش في الترمينال.'},
    {en:'<code class="i">&lt;base href="/"&gt;</code> in <code class="i">index.html</code> is what makes routing work. Deleting it breaks every link in a way that is very hard to diagnose.',
     ar:'<code class="i">&lt;base href="/"&gt;</code> اللي في <code class="i">index.html</code> هو اللي بيخلي الـ routing يشتغل. مسحه بيكسر كل اللينكات بطريقة صعب جداً تكتشفها.'},
    {en:'Do not add <code class="i">&lt;script&gt;</code> tags to <code class="i">index.html</code> by hand. The build injects them, with the right filenames and hashes.',
     ar:'متضيفش <code class="i">&lt;script&gt;</code> في <code class="i">index.html</code> بإيدك. الـ build بيحطهم بالأسماء والهاشات الصح.'},
    {en:'Recent Angular versions generate <code class="i">app.ts</code> with the class <code class="i">App</code>. Older projects and most tutorials use <code class="i">app.component.ts</code> and <code class="i">AppComponent</code>. Both are correct — only the file naming changed.',
     ar:'إصدارات أنجولار الجديدة بتولّد <code class="i">app.ts</code> والكلاس اسمه <code class="i">App</code>. والمشاريع القديمة ومعظم الشروحات بتستخدم <code class="i">app.component.ts</code> و<code class="i">AppComponent</code>. الاتنين صح — اللي اتغير هو تسمية الملفات بس.'}
  ]
},

/* B ------------------------------------------------------------------- */
{
  slug:'cli-and-project-files',
  badge:{en:'The toolbox', ar:'شنطة العدة'},
  title:{en:'The CLI, and what every file is for', ar:'الـ CLI، وكل ملف بيعمل إيه'},
  lead:{
    en:`You will spend your whole Angular life in about six terminal commands. The CLI writes the boilerplate, wires the files together and names things consistently — <b>let it</b>. Hand-created components are where beginners lose an afternoon to a typo.`,
    ar:`هتقضي عمرك في أنجولار على حوالي ستة أوامر في الترمينال. الـ CLI بيكتب الكود المتكرر، ويوصّل الملفات ببعض، ويسمّي الحاجات بنظام — <b>سيبه يعمل كده</b>. الـ components اللي بتتعمل بالإيد هي اللي بتضيّع من المبتدئين نص يوم على غلطة إملائية.`
  },
  nodes:[
    [{id:'term',role:{en:'one command',ar:'أمر واحد'},file:'ng generate component',ext:' cart',kind:'cfg'}],
    [{id:'ts',role:{en:'the class',ar:'الكلاس'},file:'cart',ext:'.ts',kind:'ts'},
     {id:'html',role:{en:'the markup',ar:'الماركب'},file:'cart',ext:'.html',kind:'html'},
     {id:'spec',role:{en:'the test',ar:'التست'},file:'cart.spec',ext:'.ts',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'term',to:'ts',a:'bottom',b:'top',label:'creates',packet:'cart.ts'},
    {id:'e2',from:'term',to:'html',a:'bottom',b:'top',label:'creates',packet:'cart.html'},
    {id:'e3',from:'term',to:'spec',a:'bottom',b:'top',label:'creates',packet:'cart.spec.ts'}
  ],
  files:[
    {id:'term',name:'the six commands you need',lang:'bash',tag:{en:'memorise these',ar:'احفظ دول'},code:[
      "npm install -g @angular/cli      # once, on your machine",
      "",
      "ng new my-shop                   # create a project",
      "ng serve                         # run it at localhost:4200",
      "ng generate component cart       # ng g c cart",
      "ng generate service cart-store   # ng g s cart-store",
      "ng build                         # production build into dist/",
      "ng test                          # run the unit tests",
      "",
      "# tip: `ng g c features/cart` creates it in a subfolder,",
      "#      and creates the folder if it does not exist."]},
    {id:'ts',name:'src/app/cart/cart.ts',lang:'ts',tag:{en:'generated for you',ar:'اتعمل لك'},code:[
      "import { Component } from '@angular/core';",
      "",
      "@Component({",
      "  selector: 'app-cart',",
      "  imports: [],",
      "  templateUrl: './cart.html',",
      "  styleUrl: './cart.css',",
      "})",
      "export class Cart {",
      "",
      "}",
      "",
      "// the CLI matched the class name, the selector, and the",
      "// file names for you. that is the whole value of `ng g c`."]},
    {id:'html',name:'src/app/cart/cart.html',lang:'html',tag:{en:'yours to fill',ar:'دي بتاعتك'},code:[
      "<p>cart works!</p>",
      "",
      "<!-- delete that line and start building -->"]},
    {id:'spec',name:'src/app/cart/cart.spec.ts',lang:'ts',tag:{en:'safe to ignore at first',ar:'عادي تتجاهلها في الأول'},code:[
      "// a starter unit test. it checks the component can be created.",
      "// you can ignore this file until you are ready to learn testing —",
      "// but do not delete it, `ng test` expects it.",
      "",
      "describe('Cart', () => {",
      "  it('should create', async () => {",
      "    /* … */",
      "  });",
      "});"]},
    {id:'map',name:'what everything in the project is',lang:'bash',tag:{en:'the map',ar:'الخريطة'},code:[
      "my-shop/",
      "├─ src/",
      "│  ├─ index.html          the one real page",
      "│  ├─ main.ts             starts the app",
      "│  ├─ styles.css          global styles (fonts, resets, CSS variables)",
      "│  └─ app/",
      "│     ├─ app.ts           the root component",
      "│     ├─ app.html         its template",
      "│     ├─ app.config.ts    app-wide providers",
      "│     ├─ app.routes.ts    the URL → component map",
      "│     └─ cart/            a feature folder — you make these",
      "├─ public/                images and files copied as-is",
      "├─ angular.json           build settings (rarely edited)",
      "├─ package.json           dependencies and npm scripts",
      "├─ tsconfig.json          TypeScript settings",
      "└─ node_modules/          installed packages — never edit, never commit"]}
  ],
  steps:[
    {t:{en:'<code class="i">ng new my-shop</code> creates the project and installs everything. <code class="i">ng serve</code> runs it. Those two get you a working app before you have written a line.',
        ar:'<code class="i">ng new my-shop</code> بيعمل المشروع وينزّل كل حاجة. و<code class="i">ng serve</code> بيشغّله. الاتنين دول بيدوك تطبيق شغال قبل ما تكتب سطر.'},
     hl:{term:[3,4]}},
    {t:{en:'<code class="i">ng generate component cart</code> — or the short <code class="i">ng g c cart</code> — creates a folder with three or four matching files.',
        ar:'<code class="i">ng generate component cart</code> — أو المختصر <code class="i">ng g c cart</code> — بيعمل فولدر فيه تلات أو أربع ملفات متطابقين.'},
     edge:'e1',hl:{term:[5],ts:[4,6,7,9]}},
    {t:{en:'The class name, the <code class="i">selector</code>, and the <code class="i">templateUrl</code> are all generated to match. This is why you use the CLI: three names that must agree, agreed automatically.',
        ar:'اسم الكلاس والـ <code class="i">selector</code> والـ <code class="i">templateUrl</code> كلهم اتولدوا متطابقين. وعشان كده بتستخدم الـ CLI: تلات أسماء لازم يتفقوا، اتفقوا لوحدهم.'},
     edge:'e2',hl:{html:[1]}},
    {t:{en:'A <code class="i">.spec.ts</code> test file comes along too. Ignore it while you are learning — just do not delete it.',
        ar:'وبييجي معاهم ملف تست <code class="i">.spec.ts</code>. تجاهله وإنت بتتعلم — بس متمسحهوش.'},
     edge:'e3',hl:{spec:[1,2,3]}},
    {t:{en:'Everything else in the project has one job each. You will edit <code class="i">app.routes.ts</code> and your feature folders constantly, <code class="i">app.config.ts</code> occasionally, and <code class="i">angular.json</code> almost never.',
        ar:'وباقي حاجات المشروع كل واحدة ليها شغلانة. هتعدّل <code class="i">app.routes.ts</code> وفولدرات الفيتشرز على طول، و<code class="i">app.config.ts</code> من وقت للتاني، و<code class="i">angular.json</code> تقريباً أبداً.'},
     hl:{map:[8,10,11,12,15]}}
  ],
  example:{
    name:{en:'Your first feature, start to finish', ar:'أول فيتشر ليك، من الأول للآخر'},
    what:{
      en:'Three commands and two small edits give you a new page at its own URL. This is the loop you will repeat for every feature you ever build in Angular.',
      ar:'تلات أوامر وتعديلين صغيرين بيدوك صفحة جديدة على URL بتاعها. ودي الدورة اللي هتكررها مع كل فيتشر هتبنيه في أنجولار.'},
    files:[
      {id:'f1',name:'1. generate it',lang:'bash',tag:{en:'terminal',ar:'الترمينال'},code:[
        "ng g c pages/about",
        "",
        "# CREATE src/app/pages/about/about.ts",
        "# CREATE src/app/pages/about/about.html",
        "# CREATE src/app/pages/about/about.css",
        "# CREATE src/app/pages/about/about.spec.ts"]},
      {id:'f2',name:'2. give it a URL',lang:'ts',tag:{en:'app.routes.ts',ar:'app.routes.ts'},code:[
        "import { Routes } from '@angular/router';",
        "",
        "export const routes: Routes = [",
        "  {",
        "    path: 'about',",
        "    loadComponent: () => import('./pages/about/about').then(m => m.About),",
        "  },",
        "];"]},
      {id:'f3',name:'3. link to it',lang:'html',tag:{en:'app.html',ar:'app.html'},code:[
        "<nav>",
        "  <a routerLink=\"/\">Home</a>",
        "  <a routerLink=\"/about\">About</a>",
        "</nav>",
        "",
        "<router-outlet />",
        "",
        "<!-- add RouterLink and RouterOutlet to imports: [] in app.ts -->"]}
    ]
  },
  gotchas:[
    {en:'Use <code class="i">routerLink</code>, never <code class="i">href</code>, for links inside your app. <code class="i">href</code> reloads the whole application and you lose all state.',
     ar:'استعمل <code class="i">routerLink</code> مش <code class="i">href</code> للينكات جوه تطبيقك. <code class="i">href</code> بيعيد تحميل التطبيق كله وبتضيّع كل الحالة.'},
    {en:'Never edit anything in <code class="i">node_modules/</code>, and never commit it. It is rebuilt from <code class="i">package.json</code> by <code class="i">npm install</code>.',
     ar:'عمرك ما تعدّل حاجة في <code class="i">node_modules/</code>، وعمرك ما ترفعها على git. بتتبني من <code class="i">package.json</code> بـ <code class="i">npm install</code>.'},
    {en:'Global styles go in <code class="i">src/styles.css</code>. Styles in a component file only apply to that component — that is deliberate, not a bug.',
     ar:'الاستايلات العامة بتتحط في <code class="i">src/styles.css</code>. والاستايلات اللي في ملف component بتتطبق على الـ component ده بس — وده مقصود مش باج.'},
    {en:'Generating a component does <b>not</b> import it anywhere. You still add it to the <code class="i">imports</code> array of whichever template uses it.',
     ar:'توليد الـ component <b>مش</b> بيعمل له import في أي مكان. لسه لازم تضيفه لمصفوفة <code class="i">imports</code> بتاعة التمبلت اللي هيستخدمه.'},
    {en:'If <code class="i">ng</code> is “command not found”, the global install did not land on your PATH. <code class="i">npx ng serve</code> works from inside the project without any global install.',
     ar:'لو <code class="i">ng</code> طلعلك «command not found»، يبقى التثبيت العام مش على الـ PATH. و<code class="i">npx ng serve</code> بتشتغل من جوه المشروع من غير أي تثبيت عام.'}
  ]
},

/* 01 ------------------------------------------------------------------ */
{
  slug:'01-components', num:'01',
  badge:{en:'The unit', ar:'الوحدة'},
  title:{en:'A component is a class with a label on it', ar:'الـ component كلاس عليه لافتة'},
  lead:{
    en:`No NgModule, no registration ceremony. A class, a decorator saying which tag it answers to, and a list of what its template is allowed to use. Miss that last list and Angular renders <b>nothing</b> — silently.`,
    ar:`مفيش NgModule ولا مراسم تسجيل. كلاس، وdecorator بيقول هو بيرد على أنهي tag، ولستة بالحاجات اللي التمبلت مسموح له يستخدمها. لو نسيت اللستة الأخيرة دي أنجولار مش هيرندر <b>أي حاجة</b> — وهيسكت.`
  },
  nodes:[
    [{id:'pts',role:{en:'owns the data',ar:'صاحب الداتا'},file:'page.component',ext:'.ts',kind:'ts'},
     {id:'phtml',role:{en:'writes the tag',ar:'بيكتب الـ tag'},file:'page.component',ext:'.html',kind:'html'}],
    [{id:'chtml',role:{en:'renders it',ar:'بيرندرها'},file:'user-card.component',ext:'.html',kind:'html'},
     {id:'cts',role:{en:'declares the tag',ar:'بيعلن الـ tag'},file:'user-card.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'pts',to:'phtml',a:'right',b:'left',label:'imports: [UserCard]',packet:'UserCard'},
    {id:'e2',from:'phtml',to:'cts',a:'bottom',b:'top',label:'[name]="p.name"',packet:'Mona'},
    {id:'e3',from:'cts',to:'chtml',a:'left',b:'right',label:'{{ name() }}',packet:'Mona'}
  ],
  files:[
    {id:'pts',name:'page.component.ts',lang:'ts',tag:{en:'source',ar:'المصدر'},code:[
      "import { Component } from '@angular/core';",
      "import { UserCard } from './user-card.component';",
      "",
      "@Component({",
      "  selector: 'app-page',",
      "  imports: [UserCard],          // ← the tag does not exist without this",
      "  templateUrl: './page.component.html',",
      "})",
      "export class Page {",
      "  readonly people = [",
      "    { name: 'Mona',  email: 'mona@shop.eg' },",
      "    { name: 'Karim', email: 'karim@shop.eg' },",
      "  ];",
      "}"]},
    {id:'phtml',name:'page.component.html',lang:'html',tag:{en:'the wire',ar:'السلك'},code:[
      "<!-- now <app-user-card> resolves to the class above -->",
      "@for (p of people; track p.email) {",
      "  <app-user-card [name]=\"p.name\" [email]=\"p.email\" />",
      "}"]},
    {id:'cts',name:'user-card.component.ts',lang:'ts',tag:{en:'receiver',ar:'المستقبِل'},code:[
      "import { Component, input } from '@angular/core';",
      "",
      "@Component({",
      "  selector: 'app-user-card',    // the tag it answers to",
      "  imports: [],                  // this template needs nothing else",
      "  templateUrl: './user-card.component.html',",
      "  styleUrl: './user-card.component.css',",
      "})",
      "export class UserCard {",
      "  readonly name  = input.required<string>();",
      "  readonly email = input('');",
      "}"]},
    {id:'chtml',name:'user-card.component.html',lang:'html',tag:{en:'output',ar:'الناتج'},code:[
      "<article class=\"card\">",
      "  <h3>{{ name() }}</h3>",
      "  <p>{{ email() }}</p>",
      "</article>"]}
  ],
  steps:[
    {t:{en:'The page owns the data and lists <code class="i">UserCard</code> in <code class="i">imports</code>. That one line is what makes the tag exist.',
        ar:'الصفحة صاحبة الداتا، وحاطة <code class="i">UserCard</code> في <code class="i">imports</code>. السطر ده بالذات هو اللي بيخلي الـ tag موجود.'},
     hl:{pts:[6,10,11,12,13]}},
    {t:{en:'Angular compiles the page template. Because the class is imported, <code class="i">&lt;app-user-card&gt;</code> resolves instead of becoming an unknown element.',
        ar:'أنجولار بيكمبايل تمبلت الصفحة. وعشان الكلاس معمول له import، <code class="i">&lt;app-user-card&gt;</code> بيتعرف بدل ما يبقى عنصر مجهول.'},
     edge:'e1',hl:{phtml:[2,3]}},
    {t:{en:'Each <code class="i">[name]</code> binding crosses the component boundary and lands on the <code class="i">input()</code> signal inside the child.',
        ar:'كل <code class="i">[name]</code> بيعدّي حدود الـ component ويوصل للـ <code class="i">input()</code> signal اللي جوه الابن.'},
     edge:'e2',hl:{cts:[10,11]}},
    {t:{en:'The child reads the signal <b>with parentheses</b> and renders. Change <code class="i">people</code> in the page and this row follows by itself.',
        ar:'الابن بيقرا الـ signal <b>بالقوسين</b> ويرندر. غيّر <code class="i">people</code> في الصفحة والصف ده هيتغير لوحده.'},
     edge:'e3',hl:{chtml:[2,3]}}
  ],
  example:{
    name:{en:'One stat tile, three dashboards', ar:'تايل واحد للأرقام، وتلات dashboards'},
    what:{
      en:'The tile knows how to draw a label, a number and a delta. It knows nothing about sales, users or uptime — which is exactly why the same file works on all three pages, and why it is trivial to test.',
      ar:'التايل عارف يرسم عنوان ورقم ونسبة تغيّر. مش عارف حاجة عن المبيعات ولا اليوزرز ولا الـ uptime — وده بالظبط اللي بيخلي نفس الملف شغال في التلات صفحات، وبيخلي اختباره سهل جداً.'},
    files:[
      {id:'x1',name:'stat-tile.component.ts',lang:'ts',tag:{en:'reusable',ar:'قابل للاستخدام'},code:[
        "@Component({",
        "  selector: 'app-stat-tile',",
        "  template: `",
        "    <div class=\"tile\" [class.down]=\"delta() < 0\">",
        "      <span class=\"label\">{{ label() }}</span>",
        "      <strong>{{ value() }}</strong>",
        "      <em>{{ delta() > 0 ? '+' : '' }}{{ delta() }}%</em>",
        "    </div>",
        "  `,",
        "})",
        "export class StatTile {",
        "  readonly label = input.required<string>();",
        "  readonly value = input.required<string>();",
        "  readonly delta = input(0);",
        "}"]},
      {id:'x2',name:'sales-dashboard.component.html',lang:'html',tag:{en:'page A',ar:'صفحة A'},code:[
        "<app-stat-tile label=\"Revenue\"  [value]=\"revenue()\"  [delta]=\"12\" />",
        "<app-stat-tile label=\"Orders\"   [value]=\"orders()\"   [delta]=\"-3\" />"]},
      {id:'x3',name:'ops-dashboard.component.html',lang:'html',tag:{en:'page B',ar:'صفحة B'},code:[
        "<!-- same component, completely different subject -->",
        "<app-stat-tile label=\"Uptime\"   value=\"99.98%\" [delta]=\"0\" />",
        "<app-stat-tile label=\"p95\"      value=\"180ms\"  [delta]=\"-8\" />"]}
    ]
  },
  gotchas:[
    {en:'<b>NG0304: not a known element</b> is almost always a missing entry in <code class="i">imports</code>, not a typo in the selector. Check the array first.',
     ar:'<b>NG0304: not a known element</b> دي في الغالب حاجة ناقصة من <code class="i">imports</code>، مش غلطة إملائية في الـ selector. بص على المصفوفة الأول.'},
    {en:'<code class="i">imports</code> lists what the <b>template</b> uses — components, directives, pipes. Services do not go there; they are injected.',
     ar:'<code class="i">imports</code> بتحط فيها اللي <b>التمبلت</b> بيستخدمه — components وdirectives وpipes. الـ services مالهاش دعوة هنا؛ دي بتتعمل لها inject.'},
    {en:'<code class="i">standalone: true</code> is the default now. If you still write it, it is harmless noise; if you write <code class="i">standalone: false</code> you are opting back into NgModules.',
     ar:'<code class="i">standalone: true</code> بقى هو الافتراضي. لو لسه بتكتبه فهو دوشة مش ضارة؛ إنما لو كتبت <code class="i">standalone: false</code> يبقى إنت راجع لـ NgModules بإرادتك.'},
    {en:'Selector prefixes are not decoration. Keep <code class="i">app-</code> (or your library prefix) so your tags can never collide with a real HTML element.',
     ar:'بادئة الـ selector مش زينة. خليك على <code class="i">app-</code> (أو بادئة المكتبة بتاعتك) عشان الـ tags بتاعتك عمرها ما تتصادم مع عنصر HTML حقيقي.'},
    {en:'Inline <code class="i">template</code> is fine while it fits on a screen. Past that, <code class="i">templateUrl</code> — a 300-line string is a file wearing a disguise.',
     ar:'الـ <code class="i">template</code> الـ inline كويس طالما داخل في الشاشة. بعد كده استعمل <code class="i">templateUrl</code> — النص اللي 300 سطر ده ملف لابس قناع.'}
  ]
},

/* 02 ------------------------------------------------------------------ */
{
  slug:'02-control-flow', num:'02',
  badge:{en:'Templates', ar:'التمبلتس'},
  title:{en:'The template finally got a real <code>if</code>', ar:'التمبلت أخيراً بقى فيه <code>if</code> حقيقي'},
  lead:{
    en:`<code>@if</code>, <code>@for</code> and <code>@switch</code> are built into the compiler — nothing to import, a real <code>@else</code>, and <b><code>track</code> is now mandatory</b>. That last change is the framework removing a footgun, not adding ceremony.`,
    ar:`<code>@if</code> و<code>@for</code> و<code>@switch</code> بقوا جوه الـ compiler نفسه — مفيش حاجة تعملها import، وفيه <code>@else</code> حقيقي، و<b><code>track</code> بقى إجباري</b>. التغيير الأخير ده شيل لغم من تحت رجلك، مش تعقيد زيادة.`
  },
  nodes:[
    [{id:'ts',role:{en:'holds the list',ar:'ماسك اللستة'},file:'orders.component',ext:'.ts',kind:'ts'}],
    [{id:'html',role:{en:'branches & loops',ar:'بيفرّع ويلف'},file:'orders.component',ext:'.html',kind:'html'},
     {id:'row',role:{en:'one item',ar:'عنصر واحد'},file:'order-row.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'html',a:'bottom',b:'top',label:'orders()',packet:'3 orders'},
    {id:'e2',from:'html',to:'row',a:'right',b:'left',label:'track order.id',packet:'#1042'}
  ],
  files:[
    {id:'ts',name:'orders.component.ts',lang:'ts',tag:{en:'state',ar:'الحالة'},code:[
      "export class Orders {",
      "  readonly orders = signal<Order[]>([]);",
      "  readonly loading = signal(true);",
      "",
      "  // a new array — never orders().push(o)",
      "  addFirst(o: Order) {",
      "    this.orders.update(list => [o, ...list]);",
      "  }",
      "}"]},
    {id:'html',name:'orders.component.html',lang:'html',tag:{en:'control flow',ar:'التحكم'},code:[
      "@if (orders().length) {",
      "  <ul>",
      "    @for (order of orders(); track order.id) {",
      "      <app-order-row [order]=\"order\" />",
      "    } @empty {",
      "      <li>nothing here yet</li>",
      "    }",
      "  </ul>",
      "} @else if (loading()) {",
      "  <app-spinner />",
      "} @else {",
      "  <p>No orders. <a routerLink=\"/shop\">Start one</a></p>",
      "}"]},
    {id:'row',name:'order-row.component.ts',lang:'ts',tag:{en:'the item',ar:'العنصر'},code:[
      "export class OrderRow {",
      "  readonly order = input.required<Order>();",
      "",
      "  // this state is why track matters: rebuild the row",
      "  // and the open/closed state resets to false",
      "  readonly expanded = signal(false);",
      "}"]},
    {id:'sw',name:'status-badge.component.html',lang:'html',tag:{en:'switch',ar:'التبديل'},code:[
      "@switch (order().status) {",
      "  @case ('paid')    { <span class=\"ok\">Paid</span> }",
      "  @case ('pending') { <span class=\"warn\">Pending</span> }",
      "  @case ('failed')  { <span class=\"bad\">Failed</span> }",
      "  @default          { <span>{{ order().status }}</span> }",
      "}"]}
  ],
  steps:[
    {t:{en:'The component holds the list in a signal. Note <code class="i">update</code> building a <b>new</b> array — mutating in place notifies nobody.',
        ar:'الـ component ماسك اللستة في signal. خد بالك إن <code class="i">update</code> بيبني array <b>جديدة</b> — التعديل في مكانه مبيبلّغش حد.'},
     hl:{ts:[2,6,7]}},
    {t:{en:'<code class="i">@if</code> reads the signal. There is no <code class="i">CommonModule</code> import anywhere — the compiler understands these blocks natively.',
        ar:'<code class="i">@if</code> بيقرا الـ signal. مفيش <code class="i">CommonModule</code> معمول له import في أي حتة — الـ compiler فاهم البلوكات دي أصلاً.'},
     edge:'e1',hl:{html:[1,9,11]}},
    {t:{en:'<code class="i">@for</code> walks the array. <code class="i">track order.id</code> is how Angular recognises the same order again after the list changes.',
        ar:'<code class="i">@for</code> بيلف على الـ array. و<code class="i">track order.id</code> هو اللي بيخلي أنجولار يعرف إن ده نفس الأوردر بعد ما اللستة تتغير.'},
     edge:'e2',hl:{html:[3,4]}},
    {t:{en:'With a stable key the existing row is <b>moved</b>, so <code class="i">expanded</code> survives. Track by <code class="i">$index</code> and every row after an insert is rebuilt — losing focus, scroll and state.',
        ar:'بمفتاح ثابت الصف الموجود بيتنقل مكانه، فـ <code class="i">expanded</code> بتفضل زي ما هي. لو عملت track بـ <code class="i">$index</code> كل صف بعد أي إضافة بيتبني من الأول — وبتضيّع الـ focus والـ scroll والحالة.'},
     hl:{row:[4,5,6]}},
    {t:{en:'<code class="i">@switch</code> replaces a chain of <code class="i">@if</code>. It compares with <code class="i">===</code>, so cases must match exactly.',
        ar:'<code class="i">@switch</code> بيستبدل سلسلة <code class="i">@if</code>. بيقارن بـ <code class="i">===</code>، فلازم الـ cases تطابق بالظبط.'},
     hl:{sw:[2,3,4,5]}}
  ],
  example:{
    name:{en:'A results list that survives filtering', ar:'لستة نتايج بتصمد قدام الفلترة'},
    what:{
      en:'Type in the search box and the array is replaced on every keystroke. Because each row is tracked by its id, Angular moves the rows that stayed and only builds the ones that are genuinely new — so an image already downloaded is not downloaded again.',
      ar:'اكتب في خانة البحث والـ array بتتبدّل مع كل حرف. وعشان كل صف متتبَّع بالـ id بتاعه، أنجولار بينقل الصفوف اللي فضلت وبيبني بس اللي جديد فعلاً — فالصورة اللي نزلت خلاص مبتنزلش تاني.'},
    files:[
      {id:'y1',name:'search.component.ts',lang:'ts',tag:{en:'derive, do not store',ar:'اشتق، متخزنش'},code:[
        "export class Search {",
        "  readonly query = signal('');",
        "  readonly all   = signal<Product[]>([]);",
        "",
        "  // computed, not a second signal kept in sync by hand",
        "  readonly results = computed(() => {",
        "    const q = this.query().trim().toLowerCase();",
        "    if (!q) return this.all();",
        "    return this.all().filter(p => p.title.toLowerCase().includes(q));",
        "  });",
        "}"]},
      {id:'y2',name:'search.component.html',lang:'html',tag:{en:'the list',ar:'اللستة'},code:[
        "<input [value]=\"query()\" (input)=\"query.set($any($event.target).value)\">",
        "",
        "@for (p of results(); track p.id) {",
        "  <app-product-card [product]=\"p\" />",
        "} @empty {",
        "  <p>Nothing matches “{{ query() }}”.</p>",
        "}"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">track $index</code> is right only for a list that never reorders and never has items inserted in the middle. Otherwise track the identity.',
     ar:'<code class="i">track $index</code> صح بس مع لستة عمرها ما بتترتب تاني ولا بيتحط فيها حاجة في النص. غير كده اتبع الهوية نفسها.'},
    {en:'<code class="i">@empty</code> attaches to <code class="i">@for</code>, not to <code class="i">@if</code> — and it fires for an empty array, not for <code class="i">undefined</code>.',
     ar:'<code class="i">@empty</code> بتتبع <code class="i">@for</code> مش <code class="i">@if</code> — وبتشتغل مع array فاضية، مش مع <code class="i">undefined</code>.'},
    {en:'Nested loops shadow <code class="i">$index</code>. Alias with <code class="i">let r = $index</code> on the outer loop before you need both.',
     ar:'الـ loops المتداخلة بتغطي على <code class="i">$index</code>. سمّيه باسم تاني بـ <code class="i">let r = $index</code> في اللوب البرّانية قبل ما تحتاج الاتنين.'},
    {en:'<code class="i">@if (x; as y)</code> gives you the value under a name — handy when <code class="i">x</code> is an expensive call or an <code class="i">async</code> pipe.',
     ar:'<code class="i">@if (x; as y)</code> بتديك القيمة باسم — مفيدة لما <code class="i">x</code> تكون عملية تقيلة أو <code class="i">async</code> pipe.'},
    {en:'Migrating an old codebase: <code class="i">ng generate @angular/core:control-flow</code> rewrites the whole project and drops the now-unused <code class="i">CommonModule</code> imports.',
     ar:'لو بتحوّل كود قديم: <code class="i">ng generate @angular/core:control-flow</code> بيعيد كتابة المشروع كله وبيشيل الـ <code class="i">CommonModule</code> اللي مبقاش ليه لازمة.'}
  ]
},

/* 03 ------------------------------------------------------------------ */
{
  slug:'03-signals', num:'03',
  badge:{en:'State', ar:'الحالة'},
  title:{en:'Signals: state that announces itself', ar:'الـ Signals: حالة بتبلّغ عن نفسها'},
  lead:{
    en:`A plain property is silent — change it and Angular has to ask every binding in the app whether anything moved. A signal is not silent: <b>reading registers the reader, writing notifies exactly those readers</b>. Everything modern in Angular is downstream of that one sentence.`,
    ar:`الـ property العادية ساكتة — تغيّرها وأنجولار لازم يسأل كل binding في التطبيق: اتحرك عندك حاجة؟ الـ signal مش ساكتة: <b>القراية بتسجّل مين قرا، والكتابة بتبلّغ اللي قروا بالظبط</b>. كل حاجة حديثة في أنجولار نازلة من الجملة دي.`
  },
  nodes:[
    [{id:'store',role:{en:'the source of truth',ar:'مصدر الحقيقة'},file:'cart.store',ext:'.ts',kind:'svc'}],
    [{id:'cmp',role:{en:'derives from it',ar:'بيشتق منها'},file:'cart.component',ext:'.ts',kind:'ts'},
     {id:'tpl',role:{en:'reads & writes',ar:'بيقرا ويكتب'},file:'cart.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'store',to:'cmp',a:'bottom',b:'top',label:'items()',packet:'[ 2 items ]'},
    {id:'e2',from:'cmp',to:'tpl',a:'right',b:'left',label:'{{ total() }}',packet:'340 EGP'},
    {id:'e3',from:'tpl',to:'store',a:'top',b:'right',label:'(click)="add()"',packet:'.update()'}
  ],
  files:[
    {id:'store',name:'cart.store.ts',lang:'ts',tag:{en:'writable',ar:'قابل للكتابة'},code:[
      "@Injectable({ providedIn: 'root' })",
      "export class CartStore {",
      "  // private + writable: only this class may mutate it",
      "  private readonly _items = signal<Item[]>([]);",
      "",
      "  // public + read-only: the shape everybody else sees",
      "  readonly items = this._items.asReadonly();",
      "",
      "  add(item: Item)   { this._items.update(l => [...l, item]); }",
      "  remove(id: string){ this._items.update(l => l.filter(i => i.id !== id)); }",
      "  clear()           { this._items.set([]); }",
      "}"]},
    {id:'cmp',name:'cart.component.ts',lang:'ts',tag:{en:'derived',ar:'مشتق'},code:[
      "export class Cart {",
      "  private readonly store = inject(CartStore);",
      "  readonly items = this.store.items;",
      "",
      "  // computed: lazy, cached, and it tracks its own dependencies",
      "  readonly count    = computed(() => this.items().length);",
      "  readonly subtotal = computed(() => this.items().reduce((s, i) => s + i.price, 0));",
      "  readonly vat      = computed(() => this.subtotal() * 0.14);",
      "  readonly total    = computed(() => this.subtotal() + this.vat());",
      "",
      "  add(i: Item) { this.store.add(i); }",
      "}"]},
    {id:'tpl',name:'cart.component.html',lang:'html',tag:{en:'the view',ar:'العرض'},code:[
      "<h2>Cart ({{ count() }})</h2>",
      "",
      "@for (i of items(); track i.id) {",
      "  <p>{{ i.title }} — {{ i.price }}</p>",
      "}",
      "",
      "<footer>",
      "  <span>VAT {{ vat() }}</span>",
      "  <strong>{{ total() }}</strong>",
      "</footer>"]},
    {id:'bad',name:'the mistake to avoid',lang:'ts',tag:{en:'do not',ar:'متعملش'},code:[
      "// two sources of truth that will disagree with each other",
      "readonly items = signal<Item[]>([]);",
      "readonly total = signal(0);            // ← stored, not derived",
      "",
      "addItem(i: Item) {",
      "  this.items.update(l => [...l, i]);",
      "  this.total.set(this.total() + i.price);   // and now you must never forget",
      "}",
      "",
      "// derived state stored twice is a bug with a delay fuse.",
      "// total = computed(() => items().reduce(...)) can never drift."]}
  ],
  steps:[
    {t:{en:'The store owns one writable signal and exposes it as read-only. Nobody outside can put the cart into a state the store did not sanction.',
        ar:'الـ store ماسك signal واحدة قابلة للكتابة وبيعرضها read-only. محدش بره يقدر يحط الكارت في حالة الـ store مسمحش بيها.'},
     hl:{store:[4,7]}},
    {t:{en:'The component reads <code class="i">items()</code>. That read <b>registers it as a reader</b> — nobody wired up a subscription.',
        ar:'الـ component بيقرا <code class="i">items()</code>. القراية دي <b>بتسجّله كقارئ</b> — محدش عمل subscribe ولا حاجة.'},
     edge:'e1',hl:{cmp:[3,6]}},
    {t:{en:'Each <code class="i">computed</code> is a recipe, evaluated lazily and cached until an input actually changes. <code class="i">total</code> depends on <code class="i">vat</code>, which depends on <code class="i">subtotal</code> — a graph, declared by reading.',
        ar:'كل <code class="i">computed</code> وصفة، بتتحسب وقت الحاجة وبتتخزن لحد ما مدخل يتغير فعلاً. <code class="i">total</code> بتعتمد على <code class="i">vat</code>، اللي بتعتمد على <code class="i">subtotal</code> — رسم بياني، اتعلن بمجرد القراية.'},
     edge:'e2',hl:{cmp:[7,8,9],tpl:[8,9]}},
    {t:{en:'A click calls <code class="i">store.add()</code>, which replaces the array. Every reader downstream — count, subtotal, vat, total, and only the DOM that shows them — updates. Nothing else is checked.',
        ar:'الكليك بينادي <code class="i">store.add()</code> اللي بيبدّل الـ array. وكل قارئ تحت — count وsubtotal وvat وtotal، والـ DOM اللي بيعرضهم بس — بيتحدّث. مفيش حاجة تانية بتتفحص.'},
     edge:'e3',hl:{store:[9]}},
    {t:{en:'The version on the right is the trap: <code class="i">total</code> stored as its own signal. One forgotten update and the number on screen is a lie.',
        ar:'النسخة اللي على اليمين هي الفخ: <code class="i">total</code> متخزنة كـ signal لوحدها. تحديث واحد تنساه والرقم اللي على الشاشة يبقى كذب.'},
     hl:{bad:[3,7,10,11]}}
  ],
  example:{
    name:{en:'A filter sidebar three components read', ar:'سايدبار فلترة بتقراه تلات components'},
    what:{
      en:'The sidebar writes, the result list reads, the counter reads. None of the three knows the others exist, and there is not a single subscription to unsubscribe from.',
      ar:'السايدبار بيكتب، ولستة النتايج بتقرا، والعدّاد بيقرا. ولا واحد فيهم عارف إن التانيين موجودين، ومفيش ولا subscription محتاج تلغيه.'},
    files:[
      {id:'z1',name:'filters.store.ts',lang:'ts',tag:{en:'shared',ar:'مشترك'},code:[
        "@Injectable({ providedIn: 'root' })",
        "export class Filters {",
        "  readonly brand = signal<string | null>(null);",
        "  readonly maxPrice = signal(10_000);",
        "  readonly inStock = signal(false);",
        "",
        "  readonly active = computed(() =>",
        "    [this.brand() && 'brand', this.inStock() && 'stock'].filter(Boolean).length);",
        "",
        "  reset() { this.brand.set(null); this.maxPrice.set(10_000); this.inStock.set(false); }",
        "}"]},
      {id:'z2',name:'results.component.ts',lang:'ts',tag:{en:'reader',ar:'قارئ'},code:[
        "export class Results {",
        "  private readonly f = inject(Filters);",
        "  private readonly all = inject(Catalogue).products;",
        "",
        "  readonly shown = computed(() => this.all().filter(p =>",
        "    (!this.f.brand()   || p.brand === this.f.brand()) &&",
        "    (!this.f.inStock() || p.stock > 0) &&",
        "    p.price <= this.f.maxPrice()));",
        "}"]},
      {id:'z3',name:'filter-chip.component.html',lang:'html',tag:{en:'writer',ar:'كاتب'},code:[
        "<button (click)=\"f.inStock.set(!f.inStock())\" [class.on]=\"f.inStock()\">",
        "  In stock only",
        "</button>",
        "<span>{{ f.active() }} filters active</span>"]}
    ]
  },
  gotchas:[
    {en:'<b>Forgetting the parentheses.</b> <code class="i">@if (count)</code> tests the function object, which is always truthy. <code class="i">{{ count }}</code> prints the function source.',
     ar:'<b>نسيان القوسين.</b> <code class="i">@if (count)</code> بيختبر الـ function object، وده دايماً truthy. و<code class="i">{{ count }}</code> بيطبع كود الـ function.'},
    {en:'<code class="i">.set()</code> compares by reference. <code class="i">items().push(x)</code> then <code class="i">.set(items())</code> is the same array — nothing updates. Build a new one.',
     ar:'<code class="i">.set()</code> بيقارن بالـ reference. لو عملت <code class="i">items().push(x)</code> وبعدين <code class="i">.set(items())</code> دي نفس الـ array — مفيش حاجة هتتحدث. ابني واحدة جديدة.'},
    {en:'Never <code class="i">.set()</code> what you could <code class="i">computed()</code>. If a value is a function of other values, it has no business being stored.',
     ar:'عمرك ما تعمل <code class="i">.set()</code> لحاجة كان ممكن تبقى <code class="i">computed()</code>. لو القيمة دالة في قيم تانية، مالهاش لازمة تتخزن.'},
    {en:'Values that never change after construction do not need to be signals. Wrapping everything is noise, not architecture.',
     ar:'القيم اللي مش بتتغير بعد الإنشاء مش محتاجة تبقى signals. لف كل حاجة في signal ده دوشة مش تصميم.'},
    {en:'A <code class="i">computed</code> must be pure. Calling an API or writing another signal inside one gives you a value that changes depending on who looked at it and when.',
     ar:'الـ <code class="i">computed</code> لازم تبقى نضيفة. لو نادیت API أو كتبت في signal تانية من جواها، القيمة هتتغير حسب مين بصّ عليها وإمتى.'}
  ]
},

/* 04 ------------------------------------------------------------------ */
{
  slug:'04-inputs-outputs', num:'04',
  badge:{en:'Component API', ar:'واجهة الـ component'},
  title:{en:'<code>input()</code>, <code>output()</code>, <code>model()</code>', ar:'<code>input()</code> و <code>output()</code> و <code>model()</code>'},
  lead:{
    en:`The classic <code>@Input</code> / <code>@Output</code> decorators still work — the companion deck covers them. These function-based versions are better for one concrete reason: <b>an input is a signal</b>, so it drops straight into a <code>computed</code> with no <code>ngOnChanges</code> in sight.`,
    ar:`الـ <code>@Input</code> و<code>@Output</code> القدام لسه شغالين — والملف المصاحب بيغطيهم. النسخ اللي على شكل functions أحسن لسبب واحد ملموس: <b>الـ input بقى signal</b>، فبيدخل على طول جوه <code>computed</code> من غير أي <code>ngOnChanges</code>.`
  },
  nodes:[
    [{id:'ptpl',role:{en:'binds both ways',ar:'بيربط في الاتجاهين'},file:'row-list.component',ext:'.html',kind:'html'},
     {id:'pts',role:{en:'handles the event',ar:'بيتعامل مع الحدث'},file:'row-list.component',ext:'.ts',kind:'ts'}],
    [{id:'cts',role:{en:'declares the API',ar:'بيعلن الواجهة'},file:'invoice-row.component',ext:'.ts',kind:'ts'},
     {id:'ctpl',role:{en:'emits',ar:'بيبعت'},file:'invoice-row.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'ptpl',to:'cts',a:'bottom',b:'top',label:'[line]="l"',packet:'{ qty: 2 }'},
    {id:'e2',from:'cts',to:'ctpl',a:'right',b:'left',label:'{{ label() }}',packet:'2 × EGP'},
    {id:'e3',from:'ctpl',to:'pts',a:'right',b:'bottom',label:'(remove)',packet:'id: 42'}
  ],
  files:[
    {id:'cts',name:'invoice-row.component.ts',lang:'ts',tag:{en:'the API',ar:'الواجهة'},code:[
      "import { Component, input, output, model, computed, booleanAttribute } from '@angular/core';",
      "",
      "export class InvoiceRow {",
      "  // required — the compiler refuses to let a caller omit it",
      "  readonly line = input.required<Line>();",
      "",
      "  // optional, with a default",
      "  readonly currency = input('EGP');",
      "",
      "  // renamed for the caller, and coerced on the way in",
      "  readonly compact = input(false, { alias: 'dense', transform: booleanAttribute });",
      "",
      "  // two-way: input + output in one declaration",
      "  readonly qty = model(1);",
      "",
      "  // events out",
      "  readonly remove = output<string>();",
      "",
      "  // an input is a signal, so it composes like any other",
      "  readonly label = computed(() => `${this.qty()} × ${this.currency()}`);",
      "",
      "  drop() { this.remove.emit(this.line().id); }",
      "}"]},
    {id:'ctpl',name:'invoice-row.component.html',lang:'html',tag:{en:'emitter',ar:'الباعث'},code:[
      "<span>{{ label() }}</span>",
      "",
      "<input type=\"number\" [value]=\"qty()\"",
      "       (input)=\"qty.set(+$any($event.target).value)\">",
      "",
      "<button (click)=\"drop()\">Remove</button>"]},
    {id:'ptpl',name:'row-list.component.html',lang:'html',tag:{en:'the wire',ar:'السلك'},code:[
      "@for (l of lines(); track l.id) {",
      "  <app-invoice-row",
      "    [line]=\"l\"",
      "    [(qty)]=\"l.qty\"          <!-- two-way, one binding -->",
      "    dense                     <!-- transform turns \"\" into true -->",
      "    (remove)=\"drop($event)\" />",
      "}"]},
    {id:'pts',name:'row-list.component.ts',lang:'ts',tag:{en:'handler',ar:'المعالِج'},code:[
      "export class RowList {",
      "  readonly lines = signal<Line[]>([]);",
      "",
      "  // $event is typed as string — the generic on output<string>()",
      "  drop(id: string) {",
      "    this.lines.update(l => l.filter(x => x.id !== id));",
      "  }",
      "}"]},
    {id:'route',name:'user-page.component.ts',lang:'ts',tag:{en:'bonus: routes',ar:'إضافة: الراوتس'},code:[
      "// provideRouter(routes, withComponentInputBinding())",
      "// route: { path: 'users/:id', component: UserPage }",
      "",
      "export class UserPage {",
      "  readonly id = input.required<string>();   // ← filled from :id",
      "  readonly tab = input('profile');          // ← filled from ?tab=",
      "",
      "  // the component has no idea it was routed to,",
      "  // so it also works inside a dialog and in a unit test",
      "}"]}
  ],
  steps:[
    {t:{en:'The child declares its public surface in one place: what it takes in, what it sends out, what it shares both ways.',
        ar:'الابن بيعلن واجهته العامة في مكان واحد: بياخد إيه، وبيبعت إيه، وبيتشارك إيه في الاتجاهين.'},
     hl:{cts:[5,8,11,14,17]}},
    {t:{en:'The parent binds <code class="i">[line]</code> and the value lands on a read-only signal. <code class="i">dense</code> with no value becomes <code class="i">true</code> because of <code class="i">booleanAttribute</code>.',
        ar:'الأب بيربط <code class="i">[line]</code> والقيمة بتوصل لـ signal للقراية بس. و<code class="i">dense</code> من غير قيمة بتبقى <code class="i">true</code> بفضل <code class="i">booleanAttribute</code>.'},
     edge:'e1',hl:{ptpl:[3,5]}},
    {t:{en:'Because the input is a signal, <code class="i">label</code> is just a <code class="i">computed</code> over it. No lifecycle hook, no setter, no stale copy.',
        ar:'عشان الـ input signal، <code class="i">label</code> مجرد <code class="i">computed</code> فوقها. مفيش lifecycle hook ولا setter ولا نسخة قديمة.'},
     edge:'e2',hl:{cts:[20],ctpl:[1]}},
    {t:{en:'<code class="i">[(qty)]</code> is <code class="i">model()</code> at work: the child writes its own signal and the parent value follows. One declaration instead of an input plus a carefully-named output.',
        ar:'<code class="i">[(qty)]</code> ده شغل <code class="i">model()</code>: الابن بيكتب في الـ signal بتاعته والقيمة عند الأب بتمشي وراها. إعلان واحد بدل input مع output لازم تسميه بالظبط.'},
     hl:{ctpl:[3,4],ptpl:[4]}},
    {t:{en:'Clicking Remove emits. <code class="i">$event</code> arrives typed as <code class="i">string</code> — that is the generic on <code class="i">output&lt;string&gt;()</code> paying off.',
        ar:'الكليك على Remove بيبعت. و<code class="i">$event</code> بيوصل نوعه <code class="i">string</code> — ده الـ generic بتاع <code class="i">output&lt;string&gt;()</code> بيجيب نتيجته.'},
     edge:'e3',hl:{pts:[5,6]}},
    {t:{en:'The same machinery fills inputs from the URL. With <code class="i">withComponentInputBinding()</code> a routed component reads <code class="i">id()</code> and never learns it was routed to — so it also works inside a dialog and in a test.',
        ar:'نفس الميكانيزم بيملا الـ inputs من الـ URL. مع <code class="i">withComponentInputBinding()</code> الـ component بيقرا <code class="i">id()</code> وعمره ما هيعرف إنه جاي من route — فبيشتغل كمان جوه dialog وفي التست.'},
     hl:{route:[5,6,8,9]}}
  ],
  example:{
    name:{en:'A quantity stepper inside a cart row', ar:'ستيبر الكمية جوه صف الكارت'},
    what:{
      en:'The stepper owns nothing. It renders a number, clamps it between a min and a max, and writes back through model(). The cart row stays the owner of the value, which is why the same stepper also works in the wishlist and in the admin order editor.',
      ar:'الستيبر مش مالك أي حاجة. بيعرض رقم، وبيحصره بين حد أدنى وأقصى، وبيكتب راجع من خلال model(). صف الكارت هو اللي فاضل مالك القيمة، وعشان كده نفس الستيبر شغال كمان في الـ wishlist وفي محرر الأوردرات في الأدمن.'},
    files:[
      {id:'q1',name:'qty-stepper.component.ts',lang:'ts',tag:{en:'reusable',ar:'قابل للاستخدام'},code:[
        "@Component({",
        "  selector: 'app-qty-stepper',",
        "  template: `",
        "    <button (click)=\"step(-1)\" [disabled]=\"value() <= min()\">−</button>",
        "    <output>{{ value() }}</output>",
        "    <button (click)=\"step(+1)\" [disabled]=\"value() >= max()\">+</button>",
        "  `,",
        "})",
        "export class QtyStepper {",
        "  readonly value = model.required<number>();",
        "  readonly min = input(1);",
        "  readonly max = input(99);",
        "",
        "  step(by: number) {",
        "    this.value.update(v => Math.min(this.max(), Math.max(this.min(), v + by)));",
        "  }",
        "}"]},
      {id:'q2',name:'cart-row.component.html',lang:'html',tag:{en:'owner',ar:'المالك'},code:[
        "<app-qty-stepper [(value)]=\"qty\" [max]=\"item().stock\" />",
        "",
        "<!-- qty is a signal on the cart row; the stepper writes it directly -->",
        "<span>{{ qty() * item().price }} EGP</span>"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">input()</code> has no <code class="i">.set()</code> — the parent owns the value. If the child must write it, that is <code class="i">model()</code>. If it needs a local copy that resets when the input changes, that is <code class="i">linkedSignal()</code>.',
     ar:'<code class="i">input()</code> مالهاش <code class="i">.set()</code> — الأب هو المالك. لو الابن لازم يكتب فيها، ده شغل <code class="i">model()</code>. ولو محتاج نسخة محلية بترجع لأصلها لما الـ input يتغير، ده <code class="i">linkedSignal()</code>.'},
    {en:'You cannot mix the two styles on one property. <code class="i">@Input() x</code> and <code class="i">x = input()</code> are alternatives, not layers.',
     ar:'مينفعش تخلط الأسلوبين على نفس الـ property. <code class="i">@Input() x</code> و<code class="i">x = input()</code> بدائل، مش طبقات.'},
    {en:'Signal inputs are <b>not</b> readable in the constructor either — but you no longer need to. Put the logic in a <code class="i">computed</code> and it runs when the value exists.',
     ar:'الـ signal inputs كمان <b>مش</b> بتتقرا في الـ constructor — بس مبقتش محتاج. حط المنطق في <code class="i">computed</code> وهو هيشتغل لما القيمة تبقى موجودة.'},
    {en:'<code class="i">[(x)]</code> requires a <code class="i">model()</code> named <code class="i">x</code>, or the old pair <code class="i">x</code> + <code class="i">xChange</code>. Any other naming silently does nothing.',
     ar:'<code class="i">[(x)]</code> محتاجة <code class="i">model()</code> اسمها <code class="i">x</code>، أو الثنائي القديم <code class="i">x</code> + <code class="i">xChange</code>. أي تسمية تانية مش هتعمل حاجة وهتسكت.'},
    {en:'Objects still travel by reference. Mutating <code class="i">line()</code> inside the child edits the parent’s object too — pass a copy or emit an event instead.',
     ar:'الـ objects لسه بتتنقل بالـ reference. لو عدّلت <code class="i">line()</code> جوه الابن هتكون عدّلت object الأب كمان — مرّر نسخة أو ابعت event بدل كده.'}
  ]
},

/* 05 ------------------------------------------------------------------ */
{
  slug:'05-services-di', num:'05',
  badge:{en:'Sharing state', ar:'مشاركة الحالة'},
  title:{en:'When two components need the same fact', ar:'لما اتنين components يحتاجوا نفس المعلومة'},
  lead:{
    en:`Threading a value down four levels so a cousin can see it is a smell. Put the fact in a service, mark it <code>providedIn: 'root'</code>, and both components ask for it directly. <b>One instance, one truth.</b>`,
    ar:`إنك تمرّر قيمة أربع مستويات لتحت عشان ابن العم يشوفها — دي ريحة وحشة. حط المعلومة في service، وحطلها <code>providedIn: 'root'</code>، وخلي الاتنين يطلبوها على طول. <b>نسخة واحدة، وحقيقة واحدة.</b>`
  },
  nodes:[
    [{id:'svc',role:{en:'one instance, app-wide',ar:'نسخة واحدة للتطبيق كله'},file:'session',ext:'.service.ts',kind:'svc'}],
    [{id:'header',role:{en:'reads',ar:'بيقرا'},file:'header.component',ext:'.ts',kind:'ts'},
     {id:'login',role:{en:'writes',ar:'بيكتب'},file:'login.component',ext:'.ts',kind:'ts'},
     {id:'guard',role:{en:'decides',ar:'بيقرر'},file:'auth',ext:'.guard.ts',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'login',to:'svc',a:'top',b:'bottom',label:'signIn(user)',packet:'{ user }'},
    {id:'e2',from:'svc',to:'header',a:'bottom',b:'top',label:'user()',packet:'Mona'},
    {id:'e3',from:'svc',to:'guard',a:'right',b:'top',label:'isSignedIn()',packet:'true'}
  ],
  files:[
    {id:'svc',name:'session.service.ts',lang:'ts',tag:{en:'the store',ar:'المخزن'},code:[
      "import { Injectable, signal, computed } from '@angular/core';",
      "",
      "@Injectable({ providedIn: 'root' })   // one lazily-created instance",
      "export class Session {",
      "  private readonly _user = signal<User | null>(null);",
      "",
      "  readonly user = this._user.asReadonly();",
      "  readonly isSignedIn = computed(() => this._user() !== null);",
      "  readonly initials = computed(() =>",
      "    this._user()?.name.split(' ').map(p => p[0]).join('') ?? '');",
      "",
      "  signIn(u: User) { this._user.set(u); }",
      "  signOut()       { this._user.set(null); }",
      "}"]},
    {id:'header',name:'header.component.ts',lang:'ts',tag:{en:'reader',ar:'قارئ'},code:[
      "import { Component, inject } from '@angular/core';",
      "",
      "@Component({",
      "  selector: 'app-header',",
      "  template: `",
      "    @if (session.isSignedIn()) {",
      "      <span class=\"avatar\">{{ session.initials() }}</span>",
      "      <button (click)=\"session.signOut()\">Sign out</button>",
      "    } @else {",
      "      <a routerLink=\"/sign-in\">Sign in</a>",
      "    }",
      "  `,",
      "})",
      "export class Header {",
      "  readonly session = inject(Session);   // same instance as everywhere",
      "}"]},
    {id:'login',name:'login.component.ts',lang:'ts',tag:{en:'writer',ar:'كاتب'},code:[
      "export class Login {",
      "  private readonly session = inject(Session);",
      "  private readonly router = inject(Router);",
      "  private readonly api = inject(Api);",
      "",
      "  async submit(email: string, password: string) {",
      "    const user = await this.api.signIn(email, password);",
      "    this.session.signIn(user);        // the header updates itself",
      "    this.router.navigateByUrl('/');",
      "  }",
      "}"]},
    {id:'guard',name:'auth.guard.ts',lang:'ts',tag:{en:'no constructor here',ar:'مفيش constructor هنا'},code:[
      "import { inject } from '@angular/core';",
      "import { CanActivateFn, Router } from '@angular/router';",
      "",
      "// a plain function — this is why inject() exists",
      "export const authGuard: CanActivateFn = (route, state) => {",
      "  if (inject(Session).isSignedIn()) return true;",
      "  return inject(Router).createUrlTree(['/sign-in'], {",
      "    queryParams: { next: state.url },",
      "  });",
      "};"]}
  ],
  steps:[
    {t:{en:'The service holds one private writable signal and publishes read-only views of it. Everything public here is either read-only or a method.',
        ar:'الـ service ماسك signal واحدة private قابلة للكتابة، وبينشر منها نسخ للقراية بس. كل حاجة public هنا يا read-only يا method.'},
     hl:{svc:[3,5,7,8]}},
    {t:{en:'Sign-in calls a method on the service. This is the only place in the app that can set a user.',
        ar:'تسجيل الدخول بينادي method على الـ service. وده المكان الوحيد في التطبيق اللي يقدر يحط user.'},
     edge:'e1',hl:{login:[7,8]}},
    {t:{en:'The header never subscribed to anything. It read <code class="i">isSignedIn()</code> in its template, so it is a reader — and it re-renders on its own.',
        ar:'الهيدر عمره ما عمل subscribe لحاجة. هو قرا <code class="i">isSignedIn()</code> في تمبلته، فبقى قارئ — وبيترندر لوحده.'},
     edge:'e2',hl:{header:[6,7,15]}},
    {t:{en:'The guard is a function with no class around it. <code class="i">inject()</code> works there because the router calls it inside an injection context.',
        ar:'الـ guard function من غير كلاس حواليها. و<code class="i">inject()</code> شغالة هناك عشان الراوتر بينادیها جوه injection context.'},
     edge:'e3',hl:{guard:[5,6,7]}}
  ],
  example:{
    name:{en:'Toast messages fired from anywhere', ar:'رسايل التوست بتتبعت من أي حتة'},
    what:{
      en:'A failed save in a dialog, a finished upload in a background task, a network error in an interceptor — all three need the same strip of toasts at the top of the screen, and none of them has a parent-child relationship with it.',
      ar:'حفظ فشل جوه dialog، ورفع خلص في مهمة في الخلفية، وايرور شبكة في interceptor — التلاتة محتاجين نفس شريط التوست فوق في الشاشة، ومفيش ولا واحد فيهم بينه وبينه علاقة أب وابن.'},
    files:[
      {id:'t1',name:'toast.service.ts',lang:'ts',tag:{en:'anyone can call',ar:'أي حد ينادي'},code:[
        "@Injectable({ providedIn: 'root' })",
        "export class Toasts {",
        "  private readonly _all = signal<Toast[]>([]);",
        "  readonly all = this._all.asReadonly();",
        "",
        "  show(text: string, kind: Toast['kind'] = 'info', ms = 4000) {",
        "    const id = crypto.randomUUID();",
        "    this._all.update(l => [...l, { id, text, kind }]);",
        "    setTimeout(() => this.dismiss(id), ms);",
        "  }",
        "",
        "  dismiss(id: string) { this._all.update(l => l.filter(t => t.id !== id)); }",
        "}"]},
      {id:'t2',name:'error.interceptor.ts',lang:'ts',tag:{en:'caller — not a component',ar:'المنادي — مش component'},code:[
        "export const errorInterceptor: HttpInterceptorFn = (req, next) => {",
        "  const toasts = inject(Toasts);",
        "  return next(req).pipe(",
        "    catchError(err => {",
        "      toasts.show('Could not reach the server.', 'error');",
        "      return throwError(() => err);",
        "    }),",
        "  );",
        "};"]},
      {id:'t3',name:'toast-strip.component.html',lang:'html',tag:{en:'the only renderer',ar:'الراسم الوحيد'},code:[
        "@for (t of toasts.all(); track t.id) {",
        "  <div class=\"toast\" [class]=\"t.kind\" role=\"status\">",
        "    {{ t.text }}",
        "    <button (click)=\"toasts.dismiss(t.id)\" aria-label=\"Dismiss\">×</button>",
        "  </div>",
        "}"]}
    ]
  },
  gotchas:[
    {en:'<code class="i">inject()</code> only works in an <b>injection context</b>: a field initializer, the constructor body, or a factory. Inside <code class="i">ngOnInit</code> or a click handler you get <b>NG0203</b>.',
     ar:'<code class="i">inject()</code> بتشتغل بس جوه <b>injection context</b>: عند تعريف الـ field، أو جوه الـ constructor، أو في factory. جوه <code class="i">ngOnInit</code> أو handler لكليك هيطلعلك <b>NG0203</b>.'},
    {en:'A service listed in a component’s <code class="i">providers: []</code> is created <b>per component instance</b>. Two siblings then hold two different copies and neither sees the other’s writes.',
     ar:'الـ service اللي بتتحط في <code class="i">providers: []</code> بتاعة component بتتعمل <b>لكل نسخة من الـ component</b>. فأخوين بيبقى عندهم نسختين مختلفتين وولا واحد بيشوف كتابة التاني.'},
    {en:'That per-component behaviour is a feature when you want it — a form state service scoped to one dialog, for instance. Just make it deliberate.',
     ar:'السلوك ده لكل component بيبقى ميزة لما تكون عايزه — زي service لحالة فورم مربوطة بـ dialog واحد. المهم يبقى قرار مقصود.'},
    {en:'Keep the writable signal <code class="i">private</code> and publish <code class="i">asReadonly()</code>. Otherwise any component in the app can <code class="i">.set()</code> your state from anywhere.',
     ar:'خلي الـ signal القابلة للكتابة <code class="i">private</code> وانشر <code class="i">asReadonly()</code>. غير كده أي component في التطبيق يقدر يعمل <code class="i">.set()</code> لحالتك من أي مكان.'},
    {en:'Need <code class="i">inject()</code> later than construction? Capture <code class="i">const inj = inject(Injector)</code> up front, then use <code class="i">runInInjectionContext(inj, () =&gt; …)</code>.',
     ar:'محتاج <code class="i">inject()</code> بعد الإنشاء؟ خزّن <code class="i">const inj = inject(Injector)</code> من الأول، وبعدين استعمل <code class="i">runInInjectionContext(inj, () =&gt; …)</code>.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'lifecycle',
  badge:{en:'Timing', ar:'التوقيت'},
  title:{en:'Lifecycle: what runs, and when', ar:'دورة الحياة: إيه اللي بيشتغل، وإمتى'},
  lead:{
    en:`A component is created, rendered, updated many times, then destroyed. Older Angular gave you eight hooks for those moments. Modern Angular quietly replaced most of them — you now need <b>three</b>, and one of those is only for talking to code that is not Angular.`,
    ar:`الـ component بيتعمل، وبيترندر، وبيتحدّث مرات كتير، وبعدين بيتدمّر. أنجولار القديم كان بيديك تمن hooks للحظات دي. وأنجولار الحديث استبدل معظمهم في هدوء — دلوقتي محتاج <b>تلاتة</b> بس، وواحد فيهم بس عشان تكلّم كود مش أنجولار.`
  },
  nodes:[
    [{id:'cmp',role:{en:'declares the hooks',ar:'بيعلن الـ hooks'},file:'chart-panel',ext:'.ts',kind:'ts'}],
    [{id:'tpl',role:{en:'first paint',ar:'أول رسمة'},file:'chart-panel',ext:'.html',kind:'html'},
     {id:'lib',role:{en:'needs a real DOM',ar:'محتاج DOM حقيقي'},file:'chart-lib',ext:'.js',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'cmp',to:'tpl',a:'bottom',b:'top',label:'ngOnInit → render',packet:'HTML'},
    {id:'e2',from:'tpl',to:'lib',a:'right',b:'left',label:'afterNextRender',packet:'<canvas>'},
    {id:'e3',from:'lib',to:'cmp',a:'top',b:'right',label:'onDestroy → chart.destroy()',packet:'cleanup'}
  ],
  files:[
    {id:'cmp',name:'chart-panel.ts',lang:'ts',tag:{en:'the three you need',ar:'التلاتة اللي تحتاجهم'},code:[
      "import { Component, inject, input, computed, effect,",
      "         DestroyRef, afterNextRender, ElementRef, viewChild } from '@angular/core';",
      "",
      "export class ChartPanel {",
      "  readonly data = input.required<Point[]>();",
      "  private readonly box = viewChild.required<ElementRef>('canvas');",
      "",
      "  // 1. DERIVED VALUE → not a hook at all, just a computed",
      "  readonly max = computed(() => Math.max(...this.data().map(p => p.y)));",
      "",
      "  constructor() {",
      "    // 2. TALKING TO NON-ANGULAR CODE that needs a real element.",
      "    //    afterNextRender never runs on the server — it is SSR-safe.",
      "    afterNextRender(() => {",
      "      const chart = new ChartLib(this.box().nativeElement);",
      "",
      "      // 3. CLEANUP",
      "      inject(DestroyRef).onDestroy(() => chart.destroy());",
      "    });",
      "",
      "    // reacting to a changed input: an effect, not ngOnChanges",
      "    effect(() => console.debug('data changed', this.data().length));",
      "  }",
      "}"]},
    {id:'old',name:'what each old hook became',lang:'ts',tag:{en:'the translation',ar:'الترجمة'},code:[
      "// ngOnChanges(changes)      → a computed() over the input signal,",
      "//                             or an effect() if you need a side effect",
      "//",
      "// ngOnInit()                → a field initializer, or the constructor.",
      "//                             it existed because @Input values were not",
      "//                             ready in the constructor. input() signals are.",
      "//",
      "// ngAfterViewInit()         → afterNextRender(), which is also SSR-safe",
      "//",
      "// ngOnDestroy()             → inject(DestroyRef).onDestroy(fn)",
      "//                             works in services and functions too",
      "//",
      "// ngDoCheck()               → you almost certainly do not want this",
      "// ngAfterContentChecked()   → nor this",
      "",
      "// all of them still work. none of them are wrong. you just need",
      "// far fewer of them than a 2021 tutorial will tell you."]},
    {id:'clean',name:'cleanup, three ways',lang:'ts',tag:{en:'pick one',ar:'اختار واحدة'},code:[
      "// 1. the classic — still perfectly fine",
      "export class Timer implements OnDestroy {",
      "  private id = setInterval(() => this.tick(), 1000);",
      "  ngOnDestroy() { clearInterval(this.id); }",
      "}",
      "",
      "// 2. DestroyRef — works anywhere inject() works, including services",
      "export class Timer2 {",
      "  constructor() {",
      "    const id = setInterval(() => this.tick(), 1000);",
      "    inject(DestroyRef).onDestroy(() => clearInterval(id));",
      "  }",
      "}",
      "",
      "// 3. for observables, the one-liner that replaces all of it",
      "this.socket$.pipe(takeUntilDestroyed()).subscribe(…);"]},
    {id:'tpl',name:'chart-panel.html',lang:'html',tag:{en:'the element',ar:'العنصر'},code:[
      "<h3>{{ data().length }} points, max {{ max() }}</h3>",
      "",
      "<!-- #canvas is what viewChild.required('canvas') looks for -->",
      "<canvas #canvas width=\"600\" height=\"280\"></canvas>"]},
    {id:'lib',name:'why afterNextRender exists',lang:'ts',tag:{en:'the reason',ar:'السبب'},code:[
      "// a chart library measures the element to decide how to draw.",
      "// in the CONSTRUCTOR the element does not exist yet → crash.",
      "// on the SERVER it never exists at all → crash.",
      "//",
      "// afterNextRender solves both: it runs once, in the browser only,",
      "// after Angular has put the element in the page.",
      "",
      "const chart = new ChartLib(el);   // el is guaranteed real here"]}
  ],
  steps:[
    {t:{en:'A value derived from an input is not a lifecycle problem. <code class="i">computed()</code> recalculates whenever the input changes — no <code class="i">ngOnChanges</code>, no stale copy.',
        ar:'القيمة المشتقة من input مش مشكلة دورة حياة. <code class="i">computed()</code> بتعيد الحساب كل ما الـ input يتغير — من غير <code class="i">ngOnChanges</code> ومن غير نسخة قديمة.'},
     hl:{cmp:[8,9]}},
    {t:{en:'Angular renders the template. Only now does the <code class="i">&lt;canvas&gt;</code> element exist in the page — which is why the constructor is too early to touch it.',
        ar:'أنجولار بيرندر التمبلت. ودلوقتي بس عنصر <code class="i">&lt;canvas&gt;</code> بقى موجود في الصفحة — وعشان كده الـ constructor بدري أوي إنك تلمسه.'},
     edge:'e1',hl:{tpl:[3,4]}},
    {t:{en:'<code class="i">afterNextRender</code> runs once, after that paint, and <b>only in the browser</b>. It is the safe place to hand a real element to a non-Angular library.',
        ar:'<code class="i">afterNextRender</code> بيشتغل مرة واحدة بعد الرسمة دي، و<b>في البراوزر بس</b>. وده المكان الآمن إنك تدي عنصر حقيقي لمكتبة مش أنجولار.'},
     edge:'e2',hl:{cmp:[14,15],lib:[5,6]}},
    {t:{en:'Anything you start, you stop. <code class="i">inject(DestroyRef).onDestroy()</code> registers the teardown right next to the setup, so the two can never drift apart.',
        ar:'أي حاجة بتشغّلها، بتقفلها. <code class="i">inject(DestroyRef).onDestroy()</code> بتسجّل التنضيف جنب التشغيل بالظبط، فمستحيل الاتنين يفترقوا.'},
     edge:'e3',hl:{cmp:[18],clean:[9,10,11,12]}},
    {t:{en:'The old hooks all still work — nothing was removed. You simply need far fewer of them than an older tutorial will tell you.',
        ar:'الـ hooks القديمة كلها لسه شغالة — محدش شال حاجة. إنت بس محتاج أقل منهم بكتير مما أي شرح قديم هيقولك.'},
     hl:{old:[1,5,8,10]}}
  ],
  example:[
    {
      name:{en:'A map that must not leak', ar:'خريطة ممنوع تسرّب'},
      what:{
        en:'Map and chart libraries attach global listeners and animation loops. Forget to destroy one and every visit to the page adds another running copy — the classic "app gets slower the longer you use it" bug.',
        ar:'مكتبات الخرايط والرسوم بتعلّق listeners عامة وحلقات أنيميشن. وتنسى تدمّر واحدة، فكل مرة تدخل الصفحة بتضيف نسخة شغالة تانية — وده باج «التطبيق بيبطأ كل ما تستعمله» الكلاسيكي.'},
      files:[
        {id:'m1',name:'map-view.ts',lang:'ts',tag:{en:'setup + teardown together',ar:'التشغيل والتنضيف مع بعض'},code:[
          "export class MapView {",
          "  private readonly host = viewChild.required<ElementRef>('map');",
          "",
          "  constructor() {",
          "    afterNextRender(() => {",
          "      const map = new MapSdk(this.host().nativeElement);",
          "      const onResize = () => map.invalidateSize();",
          "      window.addEventListener('resize', onResize);",
          "",
          "      inject(DestroyRef).onDestroy(() => {",
          "        window.removeEventListener('resize', onResize);",
          "        map.destroy();",
          "      });",
          "    });",
          "  }",
          "}"]}
      ]
    },
    {
      name:{en:'Reacting to an input, without ngOnChanges', ar:'تتفاعل مع input، من غير ngOnChanges'},
      what:{
        en:'The old reflex is to implement ngOnChanges and check which key changed. With signal inputs, a computed covers the value case and an effect covers the side-effect case — and neither can fire for the wrong input.',
        ar:'رد الفعل القديم إنك تعمل ngOnChanges وتشوف أنهي مفتاح اتغير. ومع الـ signal inputs، الـ computed بتغطي حالة القيمة والـ effect بتغطي حالة الـ side effect — وولا واحد فيهم ممكن يشتغل بسبب input غلط.'},
      files:[
        {id:'n1',name:'the old reflex',lang:'ts',tag:{en:'avoid',ar:'تجنّب'},code:[
          "@Input() userId!: string;",
          "@Input() theme = 'light';",
          "",
          "ngOnChanges(changes: SimpleChanges) {",
          "  if (changes['userId']) {          // stringly-typed, easy to misspell",
          "    this.load(this.userId);         // and it also runs on the FIRST set",
          "  }",
          "}"]},
        {id:'n2',name:'the replacement',lang:'ts',tag:{en:'do this',ar:'اعمل كده'},code:[
          "readonly userId = input.required<string>();",
          "readonly theme  = input('light');",
          "",
          "// a value derived from the input",
          "readonly user = httpResource<User>(() => `/api/users/${this.userId()}`);",
          "",
          "// a side effect caused by the input — and ONLY by this input,",
          "// because theme() is never read in here",
          "constructor() {",
          "  effect(() => analytics.track('user_viewed', { id: this.userId() }));",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'The constructor runs before <b>anything</b> is rendered. Reading <code class="i">viewChild()</code> or touching the DOM there gives you <code class="i">undefined</code>.',
     ar:'الـ constructor بيشتغل قبل ما <b>أي حاجة</b> تترسم. وقراية <code class="i">viewChild()</code> أو لمس الـ DOM هناك هتديك <code class="i">undefined</code>.'},
    {en:'<code class="i">ngAfterViewInit</code> also runs on the server during SSR, where there is no DOM. <code class="i">afterNextRender</code> does not — prefer it.',
     ar:'<code class="i">ngAfterViewInit</code> بيشتغل كمان على السيرفر أثناء الـ SSR، واللي مفيهوش DOM. أما <code class="i">afterNextRender</code> فلأ — فضّله.'},
    {en:'Lifecycle hooks are matched <b>by name</b>. Misspell <code class="i">ngOnInit</code> as <code class="i">ngOninit</code> and it silently never runs. Implementing <code class="i">OnInit</code> makes the compiler check it.',
     ar:'الـ lifecycle hooks بتتطابق <b>بالاسم</b>. لو كتبت <code class="i">ngOnInit</code> غلط <code class="i">ngOninit</code> مش هتشتغل وهتسكت. ولو عملت <code class="i">implements OnInit</code> الـ compiler هيتأكد.'},
    {en:'Anything you start — a timer, a listener, a subscription, a third-party widget — you stop. This is where memory leaks in Angular come from.',
     ar:'أي حاجة بتبدأها — تايمر أو listener أو subscription أو ويدجت خارجية — بتقفلها. ومن هنا بتيجي تسريبات الذاكرة في أنجولار.'},
    {en:'<code class="i">afterNextRender</code> runs once; <code class="i">afterEveryRender</code> runs after every single render. The second one is a performance trap unless you genuinely need it.',
     ar:'<code class="i">afterNextRender</code> بيشتغل مرة؛ و<code class="i">afterEveryRender</code> بيشتغل بعد كل رندر. والتاني ده فخ أداء إلا لو محتاجه فعلاً.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'directives-and-pipes',
  badge:{en:'Extending templates', ar:'توسيع التمبلتس'},
  title:{en:'Writing your own directives and pipes', ar:'تكتب directives وpipes بنفسك'},
  lead:{
    en:`A component brings its own markup. A <b>directive</b> adds behaviour to markup that already exists. A <b>pipe</b> transforms a value on its way to the screen. Between them they remove most of the repetitive code from your templates.`,
    ar:`الـ component بييجي بالماركب بتاعه. والـ <b>directive</b> بيضيف سلوك لماركب موجود أصلاً. والـ <b>pipe</b> بيحوّل قيمة وهي في طريقها للشاشة. والاتنين مع بعض بيشيلوا معظم الكود المتكرر من التمبلتس بتاعتك.`
  },
  nodes:[
    [{id:'dir',role:{en:'adds behaviour',ar:'بيضيف سلوك'},file:'autofocus',ext:'.directive.ts',kind:'cfg'},
     {id:'pipe',role:{en:'transforms a value',ar:'بيحوّل قيمة'},file:'time-ago',ext:'.pipe.ts',kind:'cfg'}],
    [{id:'cmp',role:{en:'imports both',ar:'بيعمل import للاتنين'},file:'feed.component',ext:'.ts',kind:'ts'},
     {id:'tpl',role:{en:'uses both',ar:'بيستخدم الاتنين'},file:'feed.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'cmp',to:'tpl',a:'right',b:'left',label:'imports: [Autofocus, TimeAgo]',packet:'both'},
    {id:'e2',from:'dir',to:'tpl',a:'bottom',b:'top',label:'appAutofocus',packet:'focus()'},
    {id:'e3',from:'pipe',to:'tpl',a:'bottom',b:'right',label:'| timeAgo',packet:'"2h ago"'}
  ],
  files:[
    {id:'dir',name:'autofocus.directive.ts',lang:'ts',tag:{en:'behaviour',ar:'سلوك'},code:[
      "import { Directive, ElementRef, inject, input, afterNextRender } from '@angular/core';",
      "",
      "@Directive({",
      "  selector: '[appAutofocus]',      // an ATTRIBUTE selector, in brackets",
      "})",
      "export class Autofocus {",
      "  readonly delay = input(0, { alias: 'appAutofocus' });",
      "  private readonly el = inject(ElementRef<HTMLElement>);",
      "",
      "  constructor() {",
      "    afterNextRender(() => {",
      "      setTimeout(() => this.el.nativeElement.focus(), this.delay());",
      "    });",
      "  }",
      "}"]},
    {id:'dir2',name:'a directive with host bindings',lang:'ts',tag:{en:'the host object',ar:'كائن الـ host'},code:[
      "@Directive({",
      "  selector: '[appTooltip]',",
      "  host: {",
      "    '[attr.aria-describedby]': 'id',      // bind an attribute",
      "    '[class.has-tooltip]': 'true',        // bind a class",
      "    '(mouseenter)': 'show()',             // listen to an event",
      "    '(mouseleave)': 'hide()',",
      "    '(focusin)': 'show()',",
      "    '(focusout)': 'hide()',",
      "  },",
      "})",
      "export class Tooltip {",
      "  readonly appTooltip = input.required<string>();   // the text",
      "  protected readonly id = 'tt-' + Math.random().toString(36).slice(2);",
      "",
      "  show() { /* … */ }",
      "  hide() { /* … */ }",
      "}"]},
    {id:'pipe',name:'time-ago.pipe.ts',lang:'ts',tag:{en:'transformation',ar:'تحويل'},code:[
      "import { Pipe, PipeTransform } from '@angular/core';",
      "",
      "@Pipe({ name: 'timeAgo' })     // pure by default — this is what you want",
      "export class TimeAgo implements PipeTransform {",
      "",
      "  transform(value: string | Date, now: Date = new Date()): string {",
      "    const then = new Date(value).getTime();",
      "    const mins = Math.round((now.getTime() - then) / 60000);",
      "",
      "    if (mins < 1)    return 'just now';",
      "    if (mins < 60)   return `${mins}m ago`;",
      "    if (mins < 1440) return `${Math.round(mins / 60)}h ago`;",
      "    return `${Math.round(mins / 1440)}d ago`;",
      "  }",
      "}"]},
    {id:'cmp',name:'feed.component.ts',lang:'ts',tag:{en:'wiring',ar:'التوصيل'},code:[
      "@Component({",
      "  selector: 'app-feed',",
      "  imports: [Autofocus, TimeAgo, Tooltip],   // same array as components",
      "  templateUrl: './feed.component.html',",
      "})",
      "export class Feed {",
      "  readonly posts = signal<Post[]>([]);",
      "}"]},
    {id:'tpl',name:'feed.component.html',lang:'html',tag:{en:'usage',ar:'الاستخدام'},code:[
      "<!-- the directive is just an attribute on a normal element -->",
      "<input appAutofocus placeholder=\"Search the feed\">",
      "<input [appAutofocus]=\"300\" placeholder=\"…after 300ms\">",
      "",
      "@for (p of posts(); track p.id) {",
      "  <article>",
      "    <h3 [appTooltip]=\"p.summary\">{{ p.title }}</h3>",
      "",
      "    <!-- the pipe transforms on the way to the screen -->",
      "    <time>{{ p.publishedAt | timeAgo }}</time>",
      "",
      "    <!-- built-in pipes chain left to right -->",
      "    <span>{{ p.price | currency:'EGP' }}</span>",
      "    <span>{{ p.title | slice:0:40 | uppercase }}</span>",
      "  </article>",
      "}"]}
  ],
  steps:[
    {t:{en:'A directive uses an <b>attribute selector</b> — <code class="i">\'[appAutofocus]\'</code> in square brackets. That is why you apply it by writing the attribute on any existing element.',
        ar:'الـ directive بيستخدم <b>attribute selector</b> — <code class="i">\'[appAutofocus]\'</code> بين قوسين مربعين. وعشان كده بتطبقه بإنك تكتب الخاصية دي على أي عنصر موجود.'},
     hl:{dir:[3,4]}},
    {t:{en:'It injects <code class="i">ElementRef</code> to reach the element it was placed on. A directive never creates markup — it only acts on markup that is already there.',
        ar:'وبيعمل inject لـ <code class="i">ElementRef</code> عشان يوصل للعنصر اللي هو عليه. والـ directive عمره ما بيعمل ماركب — هو بيتصرف بس في ماركب موجود أصلاً.'},
     edge:'e2',hl:{dir:[8,11,12],tpl:[2,3]}},
    {t:{en:'The <code class="i">host</code> object is how a directive binds classes, attributes and events on its element — all in one readable block, with no decorators.',
        ar:'كائن الـ <code class="i">host</code> هو إزاي الـ directive بيربط classes وattributes وevents على عنصره — كلهم في بلوك واحد مقروء، من غير decorators.'},
     hl:{dir2:[3,4,5,6,7]}},
    {t:{en:'A pipe is a class with one method, <code class="i">transform</code>. Whatever it returns is what appears on screen. Extra arguments come after a colon in the template.',
        ar:'الـ pipe كلاس فيه method واحدة، <code class="i">transform</code>. واللي بترجّعه هو اللي بيظهر على الشاشة. والمعاملات الزيادة بتيجي بعد نقطتين في التمبلت.'},
     edge:'e3',hl:{pipe:[6,10,11,12],tpl:[10]}},
    {t:{en:'Both go in the same <code class="i">imports</code> array as components. To Angular there is no difference — they are all just things a template is allowed to use.',
        ar:'الاتنين بيتحطوا في نفس مصفوفة <code class="i">imports</code> بتاعة الـ components. وبالنسبة لأنجولار مفيش فرق — كلهم حاجات التمبلت مسموح له يستخدمها.'},
     edge:'e1',hl:{cmp:[3]}}
  ],
  example:[
    {
      name:{en:'A directive that stops double submits', ar:'directive بيمنع الإرسال المزدوج'},
      what:{
        en:'Users double-click buttons. Without protection that is two orders, two payments, two emails. This directive disables the button for the duration of whatever it triggered, and works on every button in the app without touching any of them.',
        ar:'المستخدمين بيدوسوا الزراير مرتين. ومن غير حماية ده بيبقى أوردرين ودفعتين وإيميلين. الـ directive ده بيعطّل الزرار طول مدة اللي شغّله، وبيشتغل على كل زرار في التطبيق من غير ما تلمس ولا واحد فيهم.'},
      files:[
        {id:'d1',name:'busy.directive.ts',lang:'ts',tag:{en:'reusable everywhere',ar:'شغال في كل حتة'},code:[
          "@Directive({",
          "  selector: 'button[appBusy]',        // only on <button>, not on anything",
          "  host: {",
          "    '[disabled]': 'busy() || disabledInput()',",
          "    '[class.is-busy]': 'busy()',",
          "    '(click)': 'run($event)',",
          "  },",
          "})",
          "export class Busy {",
          "  readonly appBusy = input.required<() => Promise<unknown>>();",
          "  readonly disabledInput = input(false, { alias: 'disabled' });",
          "  protected readonly busy = signal(false);",
          "",
          "  async run(e: Event) {",
          "    if (this.busy()) { e.preventDefault(); return; }",
          "    this.busy.set(true);",
          "    try { await this.appBusy()(); }",
          "    finally { this.busy.set(false); }",
          "  }",
          "}"]},
        {id:'d2',name:'checkout.component.html',lang:'html',tag:{en:'usage',ar:'الاستخدام'},code:[
          "<button [appBusy]=\"placeOrder\">Place order</button>",
          "",
          "<!-- disabled while the promise is pending, re-enabled after,",
          "     and a second click during that window does nothing -->"]}
      ]
    },
    {
      name:{en:'Why a pipe beats a method in the template', ar:'ليه الـ pipe أحسن من method في التمبلت'},
      what:{
        en:'A method in a template is called on every single change detection run — hundreds of times a second in a busy app. A pure pipe caches its result and only recomputes when its input actually changes.',
        ar:'الـ method اللي في التمبلت بتتنادى في كل دورة change detection — مئات المرات في الثانية في تطبيق مشغول. أما الـ pure pipe فبيخزّن نتيجته ومبيعيدش الحساب غير لما مدخله يتغير فعلاً.'},
      files:[
        {id:'p1',name:'the slow version',lang:'html',tag:{en:'avoid',ar:'تجنّب'},code:[
          "@for (p of posts(); track p.id) {",
          "  <time>{{ formatTimeAgo(p.publishedAt) }}</time>",
          "}",
          "",
          "<!-- 500 posts × every change detection run = 500 calls, every time -->"]},
        {id:'p2',name:'the pipe version',lang:'html',tag:{en:'do this',ar:'اعمل كده'},code:[
          "@for (p of posts(); track p.id) {",
          "  <time>{{ p.publishedAt | timeAgo }}</time>",
          "}",
          "",
          "<!-- computed once per post, cached until publishedAt changes -->"]}
      ]
    }
  ],
  gotchas:[
    {en:'A directive selector needs brackets: <code class="i">\'[appThing]\'</code>. Writing <code class="i">\'appThing\'</code> makes it match an <code class="i">&lt;appThing&gt;</code> <b>element</b> instead, and it will never apply.',
     ar:'الـ directive selector محتاج أقواس: <code class="i">\'[appThing]\'</code>. لو كتبت <code class="i">\'appThing\'</code> هيطابق <b>عنصر</b> <code class="i">&lt;appThing&gt;</code> بدل كده، وعمره ما هيتطبق.'},
    {en:'Prefix your selectors — <code class="i">appThing</code>, not <code class="i">thing</code>. Unprefixed names collide with future HTML attributes and with other libraries.',
     ar:'حط بادئة للـ selectors — <code class="i">appThing</code> مش <code class="i">thing</code>. الأسماء من غير بادئة بتتصادم مع خصائص HTML مستقبلية ومع مكتبات تانية.'},
    {en:'Pipes are <b>pure</b> by default: they recompute only when the input reference changes. Mutating an array in place means the pipe never re-runs.',
     ar:'الـ pipes <b>pure</b> بشكل افتراضي: بتعيد الحساب بس لما الـ reference بتاع المدخل يتغير. فالتعديل في مكان الـ array معناه إن الـ pipe عمره ما هيشتغل تاني.'},
    {en:'<code class="i">pure: false</code> makes a pipe run on every change detection cycle. It is almost always the wrong fix — restructure the data instead.',
     ar:'<code class="i">pure: false</code> بتخلي الـ pipe يشتغل في كل دورة change detection. وده تقريباً دايماً الحل الغلط — أعد ترتيب الداتا بدل كده.'},
    {en:'Both must be listed in <code class="i">imports</code>. A missing pipe gives you <b>NG0302: The pipe could not be found</b>; a missing directive gives you nothing at all — it just silently does not apply.',
     ar:'الاتنين لازم يكونوا في <code class="i">imports</code>. الـ pipe الناقص بيديك <b>NG0302: The pipe could not be found</b>؛ أما الـ directive الناقص فمش بيديك حاجة خالص — بيسكت ومبيتطبقش.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'content-projection',
  badge:{en:'Wrappers', ar:'الأغلفة'},
  title:{en:'<code>&lt;ng-content&gt;</code>: markup that comes from outside', ar:'<code>&lt;ng-content&gt;</code>: ماركب جاي من بره'},
  lead:{
    en:`Sooner or later you want a card, a modal or a tab strip — a component whose <em>contents</em> are supplied by whoever uses it. That is content projection, and it is what makes a component library possible instead of forty nearly-identical components.`,
    ar:`عاجلاً أو آجلاً هتحتاج card أو modal أو tabs — component <em>محتواه</em> بييجي من اللي بيستخدمه. ده اسمه content projection، وهو اللي بيخلي عمل مكتبة components ممكن، بدل أربعين component شبه بعض.`
  },
  nodes:[
    [{id:'outer',role:{en:'supplies the markup',ar:'بيوفر الماركب'},file:'settings.component',ext:'.html',kind:'html'}],
    [{id:'panel',role:{en:'has the slots',ar:'فيه الفتحات'},file:'panel.component',ext:'.html',kind:'html'},
     {id:'pts',role:{en:'the wrapper',ar:'الغلاف'},file:'panel.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'outer',to:'panel',a:'bottom',b:'top',label:'<app-panel> … </app-panel>',packet:'markup'},
    {id:'e2',from:'panel',to:'pts',a:'right',b:'left',label:'select="[slot=actions]"',packet:'buttons'}
  ],
  files:[
    {id:'pts',name:'panel.component.ts',lang:'ts',tag:{en:'the wrapper class',ar:'كلاس الغلاف'},code:[
      "@Component({",
      "  selector: 'app-panel',",
      "  templateUrl: './panel.component.html',",
      "  styleUrl: './panel.component.css',",
      "})",
      "export class Panel {",
      "  readonly title = input.required<string>();",
      "  readonly collapsible = input(false);",
      "  protected readonly open = signal(true);",
      "",
      "  // the wrapper owns the CHROME. it knows nothing about the",
      "  // content, which is exactly why it works for every screen.",
      "}"]},
    {id:'panel',name:'panel.component.html',lang:'html',tag:{en:'the slots',ar:'الفتحات'},code:[
      "<section class=\"panel\">",
      "  <header>",
      "    <h2>{{ title() }}</h2>",
      "",
      "    <!-- named slot: only elements with slot=\"actions\" land here -->",
      "    <div class=\"actions\">",
      "      <ng-content select=\"[slot=actions]\" />",
      "    </div>",
      "",
      "    @if (collapsible()) {",
      "      <button (click)=\"open.set(!open())\">{{ open() ? '−' : '+' }}</button>",
      "    }",
      "  </header>",
      "",
      "  @if (open()) {",
      "    <div class=\"body\">",
      "      <!-- the default slot: everything not claimed above -->",
      "      <ng-content />",
      "    </div>",
      "  }",
      "</section>"]},
    {id:'outer',name:'settings.component.html',lang:'html',tag:{en:'the caller',ar:'المستخدم'},code:[
      "<app-panel title=\"Notifications\" [collapsible]=\"true\">",
      "",
      "  <!-- this button is claimed by select=\"[slot=actions]\" -->",
      "  <button slot=\"actions\" (click)=\"reset()\">Reset</button>",
      "",
      "  <!-- everything else falls into the default <ng-content /> -->",
      "  <p>Choose how you would like to hear from us.</p>",
      "  <app-toggle [(checked)]=\"email\">Email</app-toggle>",
      "  <app-toggle [(checked)]=\"sms\">SMS</app-toggle>",
      "",
      "</app-panel>"]},
    {id:'containers',name:'ng-container and ng-template',lang:'html',tag:{en:'the other two',ar:'الاتنين التانيين'},code:[
      "<!-- ng-container: group elements WITHOUT adding a wrapper div. -->",
      "<!-- it disappears completely from the rendered output.        -->",
      "<ng-container *ngIf=\"ready\">",
      "  <td>one</td>",
      "  <td>two</td>          <!-- a <div> here would break the table -->",
      "</ng-container>",
      "",
      "<!-- ng-template: markup that is NOT rendered until something -->",
      "<!-- renders it. it is a recipe, not an element.              -->",
      "<ng-template #empty>",
      "  <p class=\"muted\">Nothing here yet.</p>",
      "</ng-template>",
      "",
      "@if (rows().length) {",
      "  @for (r of rows(); track r.id) { <app-row [row]=\"r\" /> }",
      "} @else {",
      "  <ng-container *ngTemplateOutlet=\"empty\" />",
      "}"]}
  ],
  steps:[
    {t:{en:'The wrapper owns only the <b>chrome</b> — a border, a heading, a collapse button. It deliberately knows nothing about what goes inside.',
        ar:'الغلاف بيملك <b>الإطار</b> بس — برواز وعنوان وزرار طي. وهو مقصود إنه ميعرفش حاجة عن اللي جواه.'},
     hl:{pts:[7,8,11,12]}},
    {t:{en:'The caller writes ordinary markup <em>between</em> the tags. That markup belongs to the caller — its bindings, its event handlers, its component all still work exactly as if the wrapper were not there.',
        ar:'المستخدم بيكتب ماركب عادي <em>بين</em> الـ tags. والماركب ده بتاعه هو — الـ bindings والـ handlers والـ component بتوعه كلهم شغالين كأن الغلاف مش موجود أصلاً.'},
     edge:'e1',hl:{outer:[1,7,8,9]}},
    {t:{en:'<code class="i">&lt;ng-content select="…"&gt;</code> claims specific elements. Anything matching that CSS selector is pulled into that slot instead of the default one.',
        ar:'<code class="i">&lt;ng-content select="…"&gt;</code> بيحجز عناصر معينة. وأي حاجة مطابقة للـ CSS selector ده بتتسحب للفتحة دي بدل الافتراضية.'},
     edge:'e2',hl:{panel:[6,7,8],outer:[4]}},
    {t:{en:'A bare <code class="i">&lt;ng-content /&gt;</code> catches everything left over. Order matters: named slots claim first, the default takes the rest.',
        ar:'الـ <code class="i">&lt;ng-content /&gt;</code> الفاضي بياخد كل اللي فاضل. والترتيب مهم: الفتحات المسماة بتحجز الأول، والافتراضية بتاخد الباقي.'},
     hl:{panel:[16,17,18]}},
    {t:{en:'<code class="i">ng-container</code> groups elements without adding a <code class="i">div</code> — essential inside tables and flex layouts. <code class="i">ng-template</code> holds markup that is not rendered until something asks for it.',
        ar:'<code class="i">ng-container</code> بيجمّع عناصر من غير ما يضيف <code class="i">div</code> — وده ضروري جوه الجداول وتخطيطات الـ flex. و<code class="i">ng-template</code> بيمسك ماركب مش بيترسم لحد ما حاجة تطلبه.'},
     hl:{containers:[3,4,5,6,10,11,12]}}
  ],
  example:[
    {
      name:{en:'One modal, every dialog in the app', ar:'modal واحد، وكل الـ dialogs في التطبيق'},
      what:{
        en:'The modal owns the backdrop, the escape key, the focus trap and the close button — the parts that are hard and identical every time. The content and the footer buttons come from whoever opened it.',
        ar:'الـ modal بيملك الخلفية، وزرار الخروج، وحصر الـ focus، وزرار الإغلاق — الأجزاء الصعبة واللي بتتكرر بنفس الشكل كل مرة. والمحتوى وزراير الفوتر بييجوا من اللي فتحه.'},
      files:[
        {id:'mo1',name:'modal.component.html',lang:'html',tag:{en:'three slots',ar:'تلات فتحات'},code:[
          "<div class=\"backdrop\" (click)=\"dismiss.emit()\"></div>",
          "",
          "<div class=\"modal\" role=\"dialog\" aria-modal=\"true\" cdkTrapFocus>",
          "  <header>",
          "    <ng-content select=\"[slot=title]\" />",
          "    <button (click)=\"dismiss.emit()\" aria-label=\"Close\">×</button>",
          "  </header>",
          "",
          "  <div class=\"content\"><ng-content /></div>",
          "",
          "  <footer><ng-content select=\"[slot=footer]\" /></footer>",
          "</div>"]},
        {id:'mo2',name:'delete-user.component.html',lang:'html',tag:{en:'one caller',ar:'مستخدم واحد'},code:[
          "<app-modal (dismiss)=\"close()\">",
          "  <h2 slot=\"title\">Delete {{ user().name }}?</h2>",
          "",
          "  <p>This cannot be undone. Their orders will be kept.</p>",
          "",
          "  <button slot=\"footer\" (click)=\"close()\">Cancel</button>",
          "  <button slot=\"footer\" class=\"danger\" (click)=\"confirm()\">Delete</button>",
          "</app-modal>"]}
      ]
    }
  ],
  gotchas:[
    {en:'Projected content is created by the <b>caller</b>, not the wrapper. Its bindings and event handlers belong to the caller&rsquo;s component — which is usually exactly what you want.',
     ar:'المحتوى المُسقط بيتعمل بواسطة <b>المستخدم</b> مش الغلاف. والـ bindings والـ handlers بتوعه بتاعة component المستخدم — وده عادةً بالظبط اللي إنت عايزه.'},
    {en:'Content is projected <b>once</b>. Putting <code class="i">&lt;ng-content /&gt;</code> in two places does not duplicate it — only the last one receives it. Use <code class="i">ng-template</code> + <code class="i">ngTemplateOutlet</code> if you genuinely need it twice.',
     ar:'المحتوى بيتسقط <b>مرة واحدة</b>. ولو حطيت <code class="i">&lt;ng-content /&gt;</code> في مكانين مش هيتكرر — الأخير بس هو اللي هياخده. استعمل <code class="i">ng-template</code> مع <code class="i">ngTemplateOutlet</code> لو محتاجه مرتين فعلاً.'},
    {en:'Content inside a <code class="i">&#64;if</code> that is false is <b>still created</b> by the caller, just not displayed. For expensive content, project a template instead.',
     ar:'المحتوى اللي جوه <code class="i">&#64;if</code> بقيمة false <b>لسه بيتعمل</b> من المستخدم، بس مش بيتعرض. ولو المحتوى تقيل، اسقط template بدل كده.'},
    {en:'<code class="i">select</code> takes a CSS selector: <code class="i">[slot=actions]</code>, <code class="i">.danger</code>, <code class="i">h2</code>. It matches only <b>direct children</b> of the component tag.',
     ar:'<code class="i">select</code> بياخد CSS selector: <code class="i">[slot=actions]</code> أو <code class="i">.danger</code> أو <code class="i">h2</code>. وبيطابق <b>الأبناء المباشرين</b> بس لتاج الـ component.'},
    {en:'Styling projected content from the wrapper does not work by default — encapsulation scopes styles to the component that <em>declared</em> the markup. Style it from the caller.',
     ar:'تنسيق المحتوى المُسقط من الغلاف مش بيشتغل افتراضياً — التغليف بيحصر الاستايلات على الـ component اللي <em>أعلن</em> الماركب. نسّقه من عند المستخدم.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'component-styles',
  badge:{en:'CSS', ar:'الاستايل'},
  title:{en:'Why your CSS does not reach the child', ar:'ليه الـ CSS بتاعك مش واصل للابن'},
  lead:{
    en:`Styles written in a component file apply to <b>that component only</b>. This surprises everybody once, and it is deliberate: it is what stops a class named <code>.card</code> in one feature from silently restyling a different <code>.card</code> across the app.`,
    ar:`الاستايلات اللي بتتكتب في ملف component بتتطبق على <b>الـ component ده بس</b>. ودي بتفاجئ الكل مرة، وهي مقصودة: هي اللي بتمنع class اسمه <code>.card</code> في فيتشر إنه يغيّر شكل <code>.card</code> تاني في التطبيق من غير ما حد ياخد باله.`
  },
  nodes:[
    [{id:'css',role:{en:'scoped to one component',ar:'محصور في component واحد'},file:'card.component',ext:'.css',kind:'cfg'},
     {id:'global',role:{en:'applies everywhere',ar:'بيتطبق في كل حتة'},file:'styles',ext:'.css',kind:'cfg'}],
    [{id:'tpl',role:{en:'gets the attribute',ar:'بياخد الخاصية'},file:'card.component',ext:'.html',kind:'html'},
     {id:'ts',role:{en:'binds classes',ar:'بيربط classes'},file:'card.component',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'css',to:'tpl',a:'bottom',b:'top',label:'[_ngcontent-abc]',packet:'scoped'},
    {id:'e2',from:'global',to:'tpl',a:'bottom',b:'right',label:'no scoping',packet:'global'},
    {id:'e3',from:'ts',to:'tpl',a:'left',b:'right',label:'[class.sold-out]',packet:'toggle'}
  ],
  files:[
    {id:'css',name:'card.component.css',lang:'html',tag:{en:'scoped',ar:'محصور'},code:[
      "/* this .title only ever matches inside THIS component */",
      ".title { font-size: 18px; font-weight: 700; }",
      "",
      "/* :host = the <app-card> element itself */",
      ":host { display: block; border: 1px solid #ddd; border-radius: 8px; }",
      "",
      "/* react to a class on the host element */",
      ":host(.featured) { border-color: gold; }",
      "",
      "/* react to something ABOVE you in the page */",
      ":host-context(.dark-theme) .title { color: white; }",
      "",
      "/* ::ng-deep pierces into a child component. it is deprecated,",
      "   it leaks globally, and it is almost always the wrong answer. */",
      "::ng-deep .third-party-widget { padding: 0; }"]},
    {id:'out',name:'what the browser actually receives',lang:'html',tag:{en:'the mechanism',ar:'الميكانيزم'},code:[
      "<!-- Angular rewrites your CSS and stamps an attribute on the HTML -->",
      "",
      "<app-card _nghost-abc123>",
      "  <h3 class=\"title\" _ngcontent-abc123>Wireless mouse</h3>",
      "</app-card>",
      "",
      "<style>",
      "  .title[_ngcontent-abc123] { font-size: 18px; }",
      "  [_nghost-abc123]        { display: block; }",
      "</style>",
      "",
      "<!-- that attribute is the whole trick. no Shadow DOM,",
      "     no build magic — just a very specific selector. -->"]},
    {id:'ts',name:'card.component.ts',lang:'ts',tag:{en:'dynamic classes',ar:'classes متغيرة'},code:[
      "@Component({",
      "  selector: 'app-card',",
      "  templateUrl: './card.component.html',",
      "  styleUrl: './card.component.css',",
      "  // ViewEncapsulation.Emulated is the default and what you want.",
      "  // .None removes scoping entirely — the styles become global.",
      "  // .ShadowDom uses real Shadow DOM (real isolation, real limits).",
      "})",
      "export class Card {",
      "  readonly product = input.required<Product>();",
      "  readonly soldOut = computed(() => this.product().stock === 0);",
      "}"]},
    {id:'tpl',name:'card.component.html',lang:'html',tag:{en:'binding styles',ar:'ربط الاستايلات'},code:[
      "<!-- one class, toggled by a boolean -->",
      "<h3 class=\"title\" [class.sold-out]=\"soldOut()\">{{ product().title }}</h3>",
      "",
      "<!-- several at once, from an object -->",
      "<div [class]=\"{ featured: product().featured, cheap: product().price < 100 }\">",
      "",
      "<!-- a single style property, with the unit built into the binding -->",
      "<div class=\"bar\" [style.width.%]=\"product().stock\"></div>",
      "<div [style.color]=\"product().featured ? 'gold' : null\"></div>",
      "",
      "<!-- static and bound classes merge — you do not lose \"title\" -->"]},
    {id:'global',name:'src/styles.css',lang:'html',tag:{en:'the global file',ar:'الملف العام'},code:[
      "/* fonts, resets, CSS variables and utility classes go HERE, */",
      "/* because they genuinely need to apply everywhere.          */",
      "",
      ":root {",
      "  --brand: #e40035;",
      "  --radius: 8px;",
      "}",
      "",
      "*, *::before, *::after { box-sizing: border-box; }",
      "body { margin: 0; font-family: system-ui, sans-serif; }",
      "",
      "/* component CSS can then use the variables — custom properties",
      "   inherit through the DOM and ignore encapsulation entirely. */"]}
  ],
  steps:[
    {t:{en:'Angular rewrites your component CSS, adding a unique attribute selector, and stamps that same attribute onto the elements in your template.',
        ar:'أنجولار بيعيد كتابة الـ CSS بتاع الـ component، وبيضيف attribute selector فريد، وبيختم نفس الخاصية دي على العناصر اللي في التمبلت.'},
     edge:'e1',hl:{out:[3,4,8,9]}},
    {t:{en:'That is the entire mechanism. Your <code class="i">.title</code> rule literally cannot match an element in another component, because that element does not carry the attribute.',
        ar:'ده الميكانيزم كله. قاعدة <code class="i">.title</code> بتاعتك حرفياً مستحيل تطابق عنصر في component تاني، لإن العنصر ده مش شايل الخاصية.'},
     hl:{css:[1,2]}},
    {t:{en:'<code class="i">:host</code> targets the component&rsquo;s own tag — the <code class="i">&lt;app-card&gt;</code> element. Without it a component has no box of its own to style.',
        ar:'<code class="i">:host</code> بيستهدف تاج الـ component نفسه — عنصر <code class="i">&lt;app-card&gt;</code>. ومن غيره الـ component مالوش صندوق خاص بيه تنسّقه.'},
     hl:{css:[4,5,7,8]}},
    {t:{en:'Anything that must apply everywhere — fonts, resets, CSS variables — goes in <code class="i">src/styles.css</code>. Variables are the clean way through the boundary: they inherit and ignore encapsulation.',
        ar:'أي حاجة لازم تتطبق في كل حتة — الخطوط والـ resets ومتغيرات CSS — بتروح <code class="i">src/styles.css</code>. والمتغيرات هي الطريقة النضيفة لعبور الحاجز: بتتوارث وبتتجاهل التغليف.'},
     edge:'e2',hl:{global:[4,5,6,7]}},
    {t:{en:'For classes that change at runtime, bind them. <code class="i">[class.x]</code> toggles one, <code class="i">[class]</code> takes an object, and <code class="i">[style.width.%]</code> puts the unit in the binding.',
        ar:'وللـ classes اللي بتتغير وقت التشغيل، اربطها. <code class="i">[class.x]</code> بيبدّل واحدة، و<code class="i">[class]</code> بياخد object، و<code class="i">[style.width.%]</code> بيحط الوحدة في الربط.'},
     edge:'e3',hl:{tpl:[2,5,8]}},
    {t:{en:'<code class="i">ViewEncapsulation.Emulated</code> is the default and the one you want. <code class="i">None</code> makes the styles global — permanently, for the whole app.',
        ar:'<code class="i">ViewEncapsulation.Emulated</code> هي الافتراضية واللي إنت عايزها. و<code class="i">None</code> بتخلي الاستايلات عامة — بشكل دائم، للتطبيق كله.'},
     hl:{ts:[5,6,7]}}
  ],
  example:[
    {
      name:{en:'Theming without ::ng-deep', ar:'تغيير الثيم من غير ::ng-deep'},
      what:{
        en:'The instinct when a child component looks wrong is to reach in with ::ng-deep. Custom properties do the job properly: the child declares what is themeable, and any ancestor can set it — including a caller in a different library.',
        ar:'أول رد فعل لما component ابن يبقى شكله غلط هو إنك تمد إيدك بـ ::ng-deep. والمتغيرات المخصصة بتعمل الشغل صح: الابن بيعلن إيه اللي قابل للتغيير، وأي أب يقدر يحطه — حتى لو من مكتبة تانية.'},
      files:[
        {id:'th1',name:'button.component.css',lang:'html',tag:{en:'the child declares',ar:'الابن بيعلن'},code:[
          ":host {",
          "  /* a default that any ancestor may override */",
          "  --btn-bg: var(--brand, #333);",
          "  --btn-radius: var(--radius, 6px);",
          "",
          "  background: var(--btn-bg);",
          "  border-radius: var(--btn-radius);",
          "}"]},
        {id:'th2',name:'checkout.component.css',lang:'html',tag:{en:'the parent sets',ar:'الأب بيحدد'},code:[
          "/* no ::ng-deep, no !important, no fighting specificity */",
          ".danger-zone {",
          "  --btn-bg: #c00;",
          "  --btn-radius: 2px;",
          "}",
          "",
          "/* every app-button inside .danger-zone picks this up,",
          "   and nothing outside it is affected. */"]}
      ]
    }
  ],
  gotchas:[
    {en:'A style in a component file will <b>never</b> reach inside a child component. That is the feature, not a bug — reach for CSS custom properties instead.',
     ar:'الاستايل اللي في ملف component <b>عمره ما</b> هيوصل جوه component ابن. ودي ميزة مش باج — استعمل CSS custom properties بدالها.'},
    {en:'<code class="i">::ng-deep</code> is deprecated and it leaks: once a rule pierces the boundary it applies <b>globally</b>, not just under your component. Scope it with <code class="i">:host</code> at minimum.',
     ar:'<code class="i">::ng-deep</code> مهجورة وبتسرّب: أول ما قاعدة تخترق الحاجز بتتطبق <b>عالمياً</b>، مش تحت الـ component بتاعك بس. على الأقل احصرها بـ <code class="i">:host</code>.'},
    {en:'<code class="i">ViewEncapsulation.None</code> makes that component&rsquo;s styles global, permanently, for the whole app. It is almost never the right answer.',
     ar:'<code class="i">ViewEncapsulation.None</code> بتخلي استايلات الـ component دي عامة، بشكل دائم، للتطبيق كله. وتقريباً عمرها ما بتكون الحل الصح.'},
    {en:'A component has no display value of its own — a custom element is <code class="i">display: inline</code> by default. Set <code class="i">:host { display: block }</code> or your widths and margins will be ignored.',
     ar:'الـ component مالوش قيمة display خاصة بيه — العنصر المخصص بيبقى <code class="i">display: inline</code> افتراضياً. حط <code class="i">:host { display: block }</code> وإلا العرض والهوامش هيتجاهلوا.'},
    {en:'<code class="i">[style.color]="null"</code> removes the inline style. Returning <code class="i">\'\'</code> or <code class="i">undefined</code> also works — but <code class="i">null</code> is the explicit way to say “no value”.',
     ar:'<code class="i">[style.color]="null"</code> بتشيل الاستايل الـ inline. و<code class="i">\'\'</code> أو <code class="i">undefined</code> بيشتغلوا كمان — بس <code class="i">null</code> هي الطريقة الصريحة اللي معناها «مفيش قيمة».'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'template-syntax',
  badge:{en:'The template', ar:'التمبلت'},
  title:{en:'Template syntax and interpolation', ar:'صياغة التمبلت والـ interpolation'},
  lead:{
    en:`An Angular template is HTML with four extra powers. <code>{{ }}</code> prints a value, <code>[ ]</code> sets a property, <code>( )</code> listens for an event, and <code>#name</code> labels an element. <b>Everything else you will ever write in a template is one of those four.</b>`,
    ar:`تمبلت أنجولار ده HTML بأربع قدرات زيادة. <code>{{ }}</code> بتطبع قيمة، و<code>[ ]</code> بتحط property، و<code>( )</code> بتسمع لحدث، و<code>#name</code> بتسمّي عنصر. <b>وأي حاجة تانية هتكتبها في تمبلت هي واحدة من الأربعة دول.</b>`
  },
  nodes:[
    [{id:'ts',role:{en:'holds the values',ar:'ماسك القيم'},file:'profile.component',ext:'.ts',kind:'ts'}],
    [{id:'tpl',role:{en:'reads them',ar:'بيقراهم'},file:'profile.component',ext:'.html',kind:'html'},
     {id:'dom',role:{en:'what the browser shows',ar:'اللي البراوزر بيعرضه'},file:'rendered',ext:'.html',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'tpl',a:'bottom',b:'top',label:'{{ name() }}',packet:'"Mona"'},
    {id:'e2',from:'tpl',to:'dom',a:'right',b:'left',label:'rendered',packet:'Mona'}
  ],
  files:[
    {id:'ts',name:'profile.component.ts',lang:'ts',tag:{en:'the source',ar:'المصدر'},code:[
      "export class Profile {",
      "  readonly name = signal('Mona');",
      "  readonly age = signal(30);",
      "  readonly user = signal<User | null>(null);",
      "  readonly tags = signal(['admin', 'beta']);",
      "",
      "  greet() { return `Hello ${this.name()}`; }",
      "}"]},
    {id:'tpl',name:'profile.component.html',lang:'html',tag:{en:'the four powers',ar:'الأربع قدرات'},code:[
      "<!-- 1. INTERPOLATION — print a value as text -->",
      "<h1>{{ name() }}</h1>",
      "<p>{{ age() }} years old</p>",
      "",
      "<!-- expressions are allowed. simple ones. -->",
      "<p>{{ age() + 1 }}</p>",
      "<p>{{ name().toUpperCase() }}</p>",
      "<p>{{ age() >= 18 ? 'adult' : 'minor' }}</p>",
      "<p>{{ tags().join(', ') }}</p>",
      "",
      "<!-- safe navigation: no crash when user() is null -->",
      "<p>{{ user()?.email }}</p>",
      "<p>{{ user()?.email ?? 'no email on file' }}</p>",
      "",
      "<!-- 2. PROPERTY BINDING — set a real DOM property -->",
      "<img [src]=\"user()?.avatar\" [alt]=\"name()\">",
      "",
      "<!-- 3. EVENT BINDING — listen -->",
      "<button (click)=\"name.set('Karim')\">Rename</button>",
      "",
      "<!-- 4. TEMPLATE REFERENCE — label an element -->",
      "<input #box>",
      "<button (click)=\"name.set(box.value)\">Use what I typed</button>"]},
    {id:'let',name:'@let — a variable in the template',lang:'html',tag:{en:'stop repeating yourself',ar:'بطّل تكرار'},code:[
      "<!-- without @let, this calls the same thing four times -->",
      "@if (user()?.subscription?.plan === 'pro') {",
      "  <p>{{ user()?.subscription?.renewsAt }}</p>",
      "}",
      "",
      "<!-- with @let, once -->",
      "@let sub = user()?.subscription;",
      "",
      "@if (sub?.plan === 'pro') {",
      "  <p>Renews {{ sub?.renewsAt }}</p>",
      "  <p>{{ sub?.seats }} seats</p>",
      "}",
      "",
      "<!-- @let is read-only and scoped to the block it is declared in -->"]},
    {id:'no',name:'what templates deliberately cannot do',lang:'html',tag:{en:'the limits',ar:'الحدود'},code:[
      "<!-- these are NOT available in a template, on purpose: -->",
      "",
      "{{ window.location.href }}     <!-- ✗ no global objects -->",
      "{{ new Date() }}               <!-- ✗ no 'new' -->",
      "{{ x = 5 }}                    <!-- ✗ no assignment (except in events) -->",
      "{{ x++ }}                      <!-- ✗ no increment -->",
      "{{ a || b ? c : d }}           <!-- ✓ allowed but unreadable — move it out -->",
      "",
      "<!-- the rule: a template EXPRESSION must be cheap and side-effect",
      "     free, because it runs on every change detection pass.",
      "     an EVENT handler may do anything — it runs once, on the event. -->"]},
    {id:'dom',name:'what the browser ends up with',lang:'html',tag:{en:'the result',ar:'النتيجة'},code:[
      "<h1>Mona</h1>",
      "<p>30 years old</p>",
      "<p>31</p>",
      "<p>MONA</p>",
      "<p>adult</p>",
      "<p>admin, beta</p>",
      "<p>no email on file</p>",
      "",
      "<!-- plain HTML. nothing Angular-specific survives to the page. -->"]}
  ],
  steps:[
    {t:{en:'<code class="i">{{ }}</code> takes whatever is inside, converts it to a string, and puts it in the page as <b>text</b>. It can never inject HTML — that is your first line of XSS defence.',
        ar:'<code class="i">{{ }}</code> بتاخد اللي جواها، وتحوّله لنص، وتحطه في الصفحة كـ<b>نص</b>. ومستحيل تحقن HTML — ودي أول خط دفاع ليك ضد الـ XSS.'},
     edge:'e1',hl:{tpl:[2,3]}},
    {t:{en:'Expressions are allowed, and they should stay small. Anything longer than a line belongs in a <code class="i">computed()</code> where it can be named, cached and tested.',
        ar:'التعبيرات مسموحة، ولازم تفضل صغيرة. وأي حاجة أطول من سطر مكانها <code class="i">computed()</code> اللي تقدر تسمّيها وتخزّنها وتختبرها.'},
     hl:{tpl:[6,7,8,9]}},
    {t:{en:'<code class="i">?.</code> is essential here. A template runs many times, sometimes before your data has arrived — without it you get <code class="i">Cannot read properties of null</code>.',
        ar:'<code class="i">?.</code> ضرورية هنا. التمبلت بيشتغل مرات كتير، وأحياناً قبل ما الداتا توصل — ومن غيرها هيطلعلك <code class="i">Cannot read properties of null</code>.'},
     hl:{tpl:[12,13]}},
    {t:{en:'<code class="i">@let</code> names a value once so the template stops repeating a long chain. It is read-only and scoped to its block.',
        ar:'<code class="i">@let</code> بتسمّي قيمة مرة واحدة فالتمبلت يبطّل يكرر سلسلة طويلة. وهي للقراية بس ومحصورة في البلوك بتاعها.'},
     hl:{let:[7,9,10,11]}},
    {t:{en:'Templates deliberately cannot reach globals, use <code class="i">new</code>, or assign. That restriction is what lets Angular type-check and optimise them.',
        ar:'التمبلتس مقصود إنها متوصلش للـ globals، ولا تستخدم <code class="i">new</code>، ولا تعمل إسناد. والقيد ده هو اللي بيخلي أنجولار يقدر يتحقق من الأنواع ويحسّنها.'},
     edge:'e2',hl:{no:[3,4,5,10,11]}},
    {t:{en:'All of it reads from one ordinary class. The template is a view of the component, never a place to keep state.',
        ar:'كل ده بيتقرا من كلاس عادي واحد. والتمبلت عرض للـ component، مش مكان تحتفظ فيه بحالة.'},
     hl:{ts:[2,3,7]}},
    {t:{en:'And what the browser finally holds is plain HTML — nothing Angular-specific survives into the page.',
        ar:'واللي البراوزر بيمسكه في الآخر HTML عادي — مفيش حاجة خاصة بأنجولار بتوصل للصفحة.'},
     hl:{dom:[1,2,9]}}
  ],
  example:[
    {
      name:{en:'Moving logic out of the template', ar:'تطلّع المنطق بره التمبلت'},
      what:{
        en:'The template version works, but it is unreadable, untestable, and recalculated on every change detection pass. The computed version is named, cached, and can be unit tested in three lines.',
        ar:'نسخة التمبلت شغالة، بس مش مقروءة ولا قابلة للاختبار وبتتحسب في كل دورة change detection. أما نسخة الـ computed فمسماة ومخزّنة وتقدر تختبرها في تلات سطور.'},
      files:[
        {id:'x1',name:'in the template',lang:'html',tag:{en:'avoid',ar:'تجنّب'},code:[
          "<span [class.warn]=\"order()?.items?.length > 5 && order()?.total > 1000\">",
          "  {{ order()?.items?.length > 5 ? 'Bulk order' : 'Standard' }} —",
          "  {{ (order()?.total ?? 0) * 1.14 | currency:'EGP' }}",
          "</span>"]},
        {id:'x2',name:'in the class',lang:'ts',tag:{en:'do this',ar:'اعمل كده'},code:[
          "readonly isBulk   = computed(() => (this.order()?.items?.length ?? 0) > 5);",
          "readonly needsApproval = computed(() => this.isBulk() && (this.order()?.total ?? 0) > 1000);",
          "readonly withVat  = computed(() => (this.order()?.total ?? 0) * 1.14);",
          "readonly label    = computed(() => this.isBulk() ? 'Bulk order' : 'Standard');",
          "",
          "// template becomes:",
          "// <span [class.warn]=\"needsApproval()\">",
          "//   {{ label() }} — {{ withVat() | currency:'EGP' }}",
          "// </span>"]}
      ]
    }
  ],
  gotchas:[
    {en:'<code class="i">{{ }}</code> outputs <b>text</b>, always. To render HTML you need <code class="i">[innerHTML]</code>, which Angular sanitizes — and which you should be nervous about.',
     ar:'<code class="i">{{ }}</code> بتطلّع <b>نص</b>، دايماً. وعشان ترندر HTML محتاج <code class="i">[innerHTML]</code>، واللي أنجولار بيعقّمه — واللي المفروض تبقى قلقان منه.'},
    {en:'Calling a <b>method</b> in a template runs it on every change detection pass. Use a <code class="i">computed()</code> or a pipe instead.',
     ar:'إنك تنادي <b>method</b> في تمبلت بيشغّلها في كل دورة change detection. استعمل <code class="i">computed()</code> أو pipe بدل كده.'},
    {en:'Signals need parentheses: <code class="i">{{ name() }}</code>. <code class="i">{{ name }}</code> prints the function source and <code class="i">&#64;if (name)</code> is always true.',
     ar:'الـ signals محتاجة قوسين: <code class="i">{{ name() }}</code>. أما <code class="i">{{ name }}</code> فبتطبع كود الدالة، و<code class="i">&#64;if (name)</code> بتبقى true دايماً.'},
    {en:'Template expressions cannot see globals. If you need <code class="i">Math.max</code> or <code class="i">window</code>, expose it as a class field or a computed.',
     ar:'تعبيرات التمبلت مش شايفة الـ globals. لو محتاج <code class="i">Math.max</code> أو <code class="i">window</code>، اعرضهم كحقل في الكلاس أو computed.'},
    {en:'<code class="i">&#64;let</code> cannot be reassigned and is only visible after its declaration, inside the same block. It is a name, not a variable.',
     ar:'<code class="i">&#64;let</code> مينفعش تتغير وبتبقى ظاهرة بس بعد تعريفها، وجوه نفس البلوك. هي اسم، مش متغير.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'data-binding',
  badge:{en:'The four bindings', ar:'الأربع روابط'},
  title:{en:'Property, attribute, event and two-way binding', ar:'ربط الـ property والـ attribute والحدث والاتجاهين'},
  lead:{
    en:`<code>[x]</code> sends data <b>in</b>, <code>(x)</code> sends events <b>out</b>, and <code>[(x)]</code> does both. Getting these four right — and knowing when a property is not an attribute — removes most of the "why is my binding not working" questions.`,
    ar:`<code>[x]</code> بتبعت داتا <b>جوه</b>، و<code>(x)</code> بتبعت أحداث <b>بره</b>، و<code>[(x)]</code> بتعمل الاتنين. وإتقان الأربعة دول — ومعرفة إمتى الـ property مش attribute — بيشيل معظم أسئلة «ليه الـ binding مش شغال».`
  },
  nodes:[
    [{id:'ts',role:{en:'owns the state',ar:'صاحب الحالة'},file:'form.component',ext:'.ts',kind:'ts'}],
    [{id:'tpl',role:{en:'binds it',ar:'بيربطها'},file:'form.component',ext:'.html',kind:'html'},
     {id:'el',role:{en:'the element',ar:'العنصر'},file:'input',ext:' element',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'tpl',a:'bottom',b:'top',label:'[value]="term()"',packet:'"mouse"'},
    {id:'e2',from:'tpl',to:'el',a:'right',b:'left',label:'DOM property',packet:'set'},
    {id:'e3',from:'el',to:'ts',a:'top',b:'right',label:'(input)="term.set(…)"',packet:'"mousepad"'}
  ],
  files:[
    {id:'tpl',name:'form.component.html',lang:'html',tag:{en:'all four',ar:'الأربعة'},code:[
      "<!-- 1. PROPERTY BINDING — value goes IN -->",
      "<input [value]=\"term()\">",
      "<button [disabled]=\"!canSubmit()\">Save</button>",
      "<img [src]=\"avatarUrl()\">",
      "",
      "<!-- no brackets = a literal STRING, not your variable -->",
      "<input value=\"term()\">        <!-- shows the text: term() -->",
      "",
      "<!-- 2. ATTRIBUTE BINDING — for things that are NOT DOM properties -->",
      "<button [attr.aria-label]=\"'Delete ' + name()\">×</button>",
      "<td [attr.colspan]=\"span()\"></td>",
      "<div [attr.data-id]=\"id()\"></div>",
      "",
      "<!-- 3. EVENT BINDING — data comes OUT -->",
      "<button (click)=\"save()\">Save</button>",
      "<input (input)=\"term.set($any($event.target).value)\">",
      "<form (submit)=\"onSubmit($event)\">",
      "<div (keydown.escape)=\"close()\">          <!-- key modifiers -->",
      "<button (click)=\"remove(item.id); $event.stopPropagation()\">",
      "",
      "<!-- 4. TWO-WAY — in and out, on one line -->",
      "<app-rating [(stars)]=\"score\" />"]},
    {id:'attr',name:'property vs attribute',lang:'html',tag:{en:'the confusing one',ar:'الملخبطة'},code:[
      "<!-- an ATTRIBUTE is what you write in the HTML source.",
      "     a PROPERTY is what lives on the DOM object at runtime.",
      "     most of the time they match, and [x] binds the PROPERTY. -->",
      "",
      "<input [value]=\"name()\">          <!-- ✓ .value IS a DOM property -->",
      "<button [disabled]=\"busy()\">      <!-- ✓ .disabled IS a property -->",
      "",
      "<!-- but these have NO matching DOM property, so you need attr. -->",
      "<td [attr.colspan]=\"2\">           <!-- ✓ -->",
      "<td [colspan]=\"2\">                <!-- ✗ NG0303: can't bind to colspan -->",
      "",
      "<button [attr.aria-label]=\"…\">    <!-- ✓ all aria-* are attributes -->",
      "<div [attr.data-test]=\"…\">        <!-- ✓ all data-* are attributes -->",
      "",
      "<!-- rule of thumb: if the error says \"can't bind to X\",",
      "     try [attr.X] — you have found an attribute-only case. -->"]},
    {id:'ts',name:'form.component.ts',lang:'ts',tag:{en:'the other side',ar:'الناحية التانية'},code:[
      "export class Form {",
      "  readonly term = signal('');",
      "  readonly score = signal(3);",
      "  readonly busy = signal(false);",
      "  readonly canSubmit = computed(() => this.term().length > 2 && !this.busy());",
      "",
      "  // $event in a (click) is a MouseEvent",
      "  // $event in a (submit) is a SubmitEvent",
      "  // $event from a custom output is whatever you declared",
      "  onSubmit(e: Event) {",
      "    e.preventDefault();",
      "    this.save();",
      "  }",
      "",
      "  save() { /* … */ }",
      "}"]},
    {id:'two',name:'what [(x)] actually expands to',lang:'html',tag:{en:'no magic',ar:'مفيش سحر'},code:[
      "<!-- you write this -->",
      "<app-rating [(stars)]=\"score\" />",
      "",
      "<!-- Angular compiles it to exactly this -->",
      "<app-rating [stars]=\"score\" (starsChange)=\"score.set($event)\" />",
      "",
      "<!-- which is why the naming rule is absolute:",
      "     an input called  stars",
      "     an output called starsChange",
      "     …or one model() called stars, which declares both. -->",
      "",
      "<!-- the box looks like a banana in a box — [( )] — which is",
      "     genuinely how the Angular team remembers the order. -->"]}
  ],
  steps:[
    {t:{en:'Square brackets mean “<b>evaluate this as an expression</b>”. Without them the value is a literal string — the single most common binding mistake there is.',
        ar:'الأقواس المربعة معناها «<b>احسب ده كتعبير</b>». ومن غيرها القيمة بتبقى نص حرفي — وهي أشهر غلطة في الـ binding على الإطلاق.'},
     edge:'e1',hl:{tpl:[2,3,7]}},
    {t:{en:'A property binding sets a <b>DOM property</b>, not the HTML attribute. That is why <code class="i">[value]</code> works and <code class="i">[colspan]</code> does not — <code class="i">colspan</code> has no property.',
        ar:'الـ property binding بتحط <b>DOM property</b>، مش HTML attribute. وعشان كده <code class="i">[value]</code> شغالة و<code class="i">[colspan]</code> لأ — <code class="i">colspan</code> مالهاش property.'},
     edge:'e2',hl:{attr:[5,9,10]}},
    {t:{en:'For those, <code class="i">[attr.x]</code> writes the actual attribute. Everything <code class="i">aria-*</code> and <code class="i">data-*</code> goes through it.',
        ar:'ولدول، <code class="i">[attr.x]</code> بتكتب الـ attribute الحقيقي. وكل حاجة <code class="i">aria-*</code> و<code class="i">data-*</code> بتعدي منها.'},
     hl:{attr:[12,13,15,16]}},
    {t:{en:'Parentheses listen. <code class="i">$event</code> is whatever the source emits — a <code class="i">MouseEvent</code> from a click, or your own typed value from a custom <code class="i">output()</code>.',
        ar:'الأقواس العادية بتسمع. و<code class="i">$event</code> هو اللي المصدر بيبعته — <code class="i">MouseEvent</code> من الكليك، أو قيمتك المكتوبة بنوعها من <code class="i">output()</code> بتاعك.'},
     edge:'e3',hl:{tpl:[15,16,18],ts:[7,8,9]}},
    {t:{en:'<code class="i">[(x)]</code> is not a third kind of binding. It compiles to a property binding plus an event binding, which is why the <code class="i">xChange</code> naming is not negotiable.',
        ar:'<code class="i">[(x)]</code> مش نوع تالت من الـ binding. بتتحوّل لـ property binding مع event binding، وعشان كده تسمية <code class="i">xChange</code> مش قابلة للنقاش.'},
     hl:{two:[2,5,7,8,9]}}
  ],
  example:[
    {
      name:{en:'A search field, bound both ways by hand', ar:'خانة بحث، مربوطة في الاتجاهين بالإيد'},
      what:{
        en:'Two-way binding on a native input is just a property binding plus an event binding written out. Seeing it explicitly once makes ngModel and model() stop feeling like magic.',
        ar:'الـ two-way على input عادي هو مجرد property binding مع event binding مكتوبين بالطول. وشوفها صريحة مرة بيخلي ngModel و model() يبطلوا يحسوا إنهم سحر.'},
      files:[
        {id:'sb1',name:'search.component.html',lang:'html',tag:{en:'explicit',ar:'صريح'},code:[
          "<input",
          "  [value]=\"term()\"",
          "  (input)=\"term.set($any($event.target).value)\"",
          "  [attr.aria-label]=\"'Search ' + section()\"",
          "  placeholder=\"Search…\">",
          "",
          "<button [disabled]=\"!canSubmit()\" (click)=\"search()\">Go</button>",
          "",
          "@if (term()) {",
          "  <button (click)=\"term.set('')\" aria-label=\"Clear\">×</button>",
          "}",
          "",
          "<p>{{ results().length }} results for “{{ term() }}”</p>"]},
        {id:'sb2',name:'search.component.ts',lang:'ts',tag:{en:'one signal drives it all',ar:'signal واحدة بتحرّك كله'},code:[
          "export class Search {",
          "  readonly term = signal('');",
          "  readonly section = input('products');",
          "",
          "  readonly canSubmit = computed(() => this.term().trim().length > 1);",
          "  readonly results = computed(() => this.filter(this.term()));",
          "",
          "  // the input's value, the button's disabled state, the clear",
          "  // button's visibility and the result count all read one signal.",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>Missing brackets.</b> <code class="i">value="term()"</code> puts the literal text <code class="i">term()</code> in the box. If a binding shows your code on screen, this is why.',
     ar:'<b>الأقواس الناقصة.</b> <code class="i">value="term()"</code> بتحط النص الحرفي <code class="i">term()</code> في الخانة. لو binding بيوريك كودك على الشاشة، ده السبب.'},
    {en:'<b>NG0303: Can&rsquo;t bind to X</b> means either the property does not exist (try <code class="i">[attr.X]</code>) or the component that declares it is missing from <code class="i">imports</code>.',
     ar:'<b>NG0303: Can&rsquo;t bind to X</b> معناها يا إن الـ property مش موجودة (جرّب <code class="i">[attr.X]</code>) يا إن الـ component اللي بيعلنها ناقص من <code class="i">imports</code>.'},
    {en:'<code class="i">$event.target</code> is typed as <code class="i">EventTarget</code>, which has no <code class="i">.value</code>. Use <code class="i">$any($event.target).value</code>, or better, a template reference: <code class="i">#box</code> then <code class="i">box.value</code>.',
     ar:'<code class="i">$event.target</code> نوعها <code class="i">EventTarget</code>، ومالهاش <code class="i">.value</code>. استعمل <code class="i">$any($event.target).value</code>، أو الأحسن، template reference: <code class="i">#box</code> وبعدين <code class="i">box.value</code>.'},
    {en:'Two-way binding needs an exact name pair — <code class="i">x</code> and <code class="i">xChange</code> — or a single <code class="i">model()</code>. Any other naming fails silently.',
     ar:'الـ two-way محتاج زوج أسماء بالظبط — <code class="i">x</code> و<code class="i">xChange</code> — أو <code class="i">model()</code> واحدة. وأي تسمية تانية بتفشل في صمت.'},
    {en:'An event handler may do real work; a template expression may not. <code class="i">(click)="x = 5"</code> is fine. <code class="i">{{ x = 5 }}</code> is an error.',
     ar:'معالج الحدث ينفع يعمل شغل حقيقي؛ أما تعبير التمبلت فلأ. <code class="i">(click)="x = 5"</code> عادي. و<code class="i">{{ x = 5 }}</code> ايرور.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'built-in-directives',
  badge:{en:'Batteries included', ar:'جاهزة معاك'},
  title:{en:'<code>ngClass</code>, <code>ngStyle</code> and friends', ar:'<code>ngClass</code> و <code>ngStyle</code> وأخواتهم'},
  lead:{
    en:`Angular ships directives for the things every app needs. Two of them — <code>ngClass</code> and <code>ngStyle</code> — are so common that Angular built <b>simpler replacements directly into the template syntax</b>, and most modern code no longer imports them at all.`,
    ar:`أنجولار بييجي معاه directives للحاجات اللي كل تطبيق محتاجها. واتنين منهم — <code>ngClass</code> و<code>ngStyle</code> — شائعين لدرجة إن أنجولار عمل <b>بدائل أبسط جوه صياغة التمبلت نفسها</b>، ومعظم الكود الحديث مبقاش بيعمل لهم import أصلاً.`
  },
  nodes:[
    [{id:'ts',role:{en:'computes the state',ar:'بيحسب الحالة'},file:'badge.component',ext:'.ts',kind:'ts'}],
    [{id:'old',role:{en:'needs an import',ar:'محتاج import'},file:'ngClass / ngStyle',ext:'',kind:'cfg'},
     {id:'new',role:{en:'built into the syntax',ar:'جوه الصياغة'},file:'[class] / [style]',ext:'',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'old',a:'bottom',b:'top',label:'CommonModule',packet:'import'},
    {id:'e2',from:'ts',to:'new',a:'bottom',b:'top',label:'no import',packet:'built-in'}
  ],
  files:[
    {id:'new',name:'the modern way — nothing to import',lang:'html',tag:{en:'prefer this',ar:'فضّل ده'},code:[
      "<!-- one class, toggled by a boolean -->",
      "<span [class.sold-out]=\"stock() === 0\">Wireless mouse</span>",
      "",
      "<!-- several, from an object — true means \"apply it\" -->",
      "<span [class]=\"{ featured: isFeatured(), cheap: price() < 100 }\">",
      "",
      "<!-- from a string or an array -->",
      "<span [class]=\"'badge ' + kind()\">",
      "<span [class]=\"['badge', kind()]\">",
      "",
      "<!-- one style property, with the unit IN the binding -->",
      "<div [style.width.%]=\"percent()\"></div>",
      "<div [style.font-size.px]=\"size()\"></div>",
      "<div [style.color]=\"isError() ? 'red' : null\"></div>",
      "",
      "<!-- several styles from an object -->",
      "<div [style]=\"{ width: percent() + '%', opacity: faded() ? 0.5 : 1 }\">",
      "",
      "<!-- static and bound merge — you never lose \"badge\" -->",
      "<span class=\"badge\" [class.new]=\"isNew()\">"]},
    {id:'old',name:'ngClass / ngStyle — the older API',lang:'html',tag:{en:'still works',ar:'لسه شغال'},code:[
      "<!-- requires: imports: [NgClass, NgStyle] -->",
      "",
      "<span [ngClass]=\"{ 'sold-out': stock() === 0, featured: isFeatured() }\">",
      "<span [ngClass]=\"['badge', kind()]\">",
      "<span [ngClass]=\"'badge ' + kind()\">",
      "",
      "<div [ngStyle]=\"{ 'width.%': percent(), color: colour() }\">",
      "",
      "<!-- these are not deprecated and not wrong. they are simply",
      "     an import and a few characters more than [class] / [style],",
      "     which do the same job. new code rarely needs them. -->"]},
    {id:'others',name:'the other built-ins worth knowing',lang:'html',tag:{en:'the useful ones',ar:'المفيدين'},code:[
      "<!-- NgOptimizedImage — lazy loading, sizing and preload hints,",
      "     for free. use it for every <img> that is not an icon.        -->",
      "<img ngSrc=\"/hero.jpg\" width=\"1200\" height=\"600\" priority>",
      "<img ngSrc=\"/thumb.jpg\" width=\"200\" height=\"200\">   <!-- lazy by default -->",
      "",
      "<!-- NgTemplateOutlet — render a template you captured earlier -->",
      "<ng-container *ngTemplateOutlet=\"rowTpl; context: { $implicit: row }\" />",
      "",
      "<!-- NgComponentOutlet — render a component chosen at runtime -->",
      "<ng-container *ngComponentOutlet=\"widgetFor(type())\" />",
      "",
      "<!-- RouterLink / RouterLinkActive — navigation without a reload -->",
      "<a routerLink=\"/orders\" routerLinkActive=\"active\">Orders</a>",
      "",
      "<!-- *ngIf, *ngFor and *ngSwitch still exist, but @if / @for / @switch",
      "     replaced them: no import, real @else, and mandatory track. -->"]},
    {id:'ts',name:'badge.component.ts',lang:'ts',tag:{en:'derive, then bind',ar:'اشتق، وبعدين اربط'},code:[
      "export class Badge {",
      "  readonly product = input.required<Product>();",
      "",
      "  // decide in the CLASS, bind a name in the template",
      "  readonly stock      = computed(() => this.product().stock);",
      "  readonly isFeatured = computed(() => this.product().featured);",
      "  readonly isNew      = computed(() => daysSince(this.product().addedAt) < 7);",
      "  readonly kind       = computed(() =>",
      "    this.stock() === 0 ? 'danger' : this.isNew() ? 'success' : 'neutral');",
      "",
      "  // a template full of && and ?: is a class that is missing a computed",
      "}"]}
  ],
  steps:[
    {t:{en:'<code class="i">[class.name]</code> toggles a single class from a boolean. It is the one you will use most, and it needs no import at all.',
        ar:'<code class="i">[class.name]</code> بتبدّل class واحدة من boolean. وهي اللي هتستخدمها أكتر حاجة، ومش محتاجة أي import.'},
     edge:'e2',hl:{new:[1,2]}},
    {t:{en:'Pass an object for several at once. Static <code class="i">class="badge"</code> and a binding on the same element <b>merge</b> — you never lose the static one.',
        ar:'مرّر object لكذا واحدة مرة واحدة. والـ <code class="i">class="badge"</code> الثابتة والـ binding على نفس العنصر <b>بيندمجوا</b> — وعمرك ما بتضيّع الثابتة.'},
     hl:{new:[5,19,20]}},
    {t:{en:'Styles take the <b>unit in the binding</b> — <code class="i">[style.width.%]</code>. That is why you almost never build style strings by hand.',
        ar:'الاستايلات بتاخد <b>الوحدة في الربط</b> — <code class="i">[style.width.%]</code>. وعشان كده تقريباً عمرك ما هتبني نصوص استايل بإيدك.'},
     hl:{new:[12,13,14]}},
    {t:{en:'<code class="i">ngClass</code> and <code class="i">ngStyle</code> do the same job but must be imported. They are not deprecated — just longer.',
        ar:'<code class="i">ngClass</code> و<code class="i">ngStyle</code> بيعملوا نفس الشغل بس لازم تعمل لهم import. ومش مهجورين — بس أطول.'},
     edge:'e1',hl:{old:[1,3,7]}},
    {t:{en:'Decide in the class, bind a <b>name</b> in the template. A template full of <code class="i">&&</code> and <code class="i">?:</code> is a class that is missing a <code class="i">computed</code>.',
        ar:'قرر في الكلاس، واربط <b>اسم</b> في التمبلت. والتمبلت المليان <code class="i">&&</code> و<code class="i">?:</code> ده كلاس ناقصه <code class="i">computed</code>.'},
     hl:{ts:[5,6,7,8,9,11]}},
    {t:{en:'Beyond those two, the built-ins worth knowing are <code class="i">NgOptimizedImage</code>, the outlets, and the router directives.',
        ar:'وغير الاتنين دول، الجاهزين اللي يستاهلوا معرفة هما <code class="i">NgOptimizedImage</code> والـ outlets وdirectives الراوتر.'},
     hl:{others:[1,3,7,13]}}
  ],
  example:[
    {
      name:{en:'A status pill with four states', ar:'شارة حالة بأربع حالات'},
      what:{
        en:'One computed decides the state; the template just binds it. Adding a fifth state later is one line in the class and zero changes to the template — which is the whole point of keeping logic out of the markup.',
        ar:'computed واحدة بتقرر الحالة؛ والتمبلت بيربطها وخلاص. وإضافة حالة خامسة بعدين سطر واحد في الكلاس وصفر تغيير في التمبلت — وده كل الهدف من إنك تطلّع المنطق بره الماركب.'},
      files:[
        {id:'sp1',name:'status-pill.component.ts',lang:'ts',tag:{en:'one decision',ar:'قرار واحد'},code:[
          "type Kind = 'paid' | 'pending' | 'failed' | 'refunded';",
          "",
          "export class StatusPill {",
          "  readonly status = input.required<Kind>();",
          "",
          "  readonly label = computed(() => ({",
          "    paid: 'Paid', pending: 'Awaiting payment',",
          "    failed: 'Payment failed', refunded: 'Refunded',",
          "  }[this.status()]));",
          "",
          "  readonly needsAttention = computed(() => this.status() === 'failed');",
          "}"]},
        {id:'sp2',name:'status-pill.component.html',lang:'html',tag:{en:'no logic',ar:'من غير منطق'},code:[
          "<span class=\"pill\"",
          "      [class]=\"'pill--' + status()\"",
          "      [class.pulse]=\"needsAttention()\"",
          "      [attr.aria-label]=\"label()\">",
          "  {{ label() }}",
          "</span>"]}
      ]
    }
  ],
  gotchas:[
    {en:'A class name with a dash needs quotes inside an object: <code class="i">{ \'sold-out\': true }</code>. Without them it is a syntax error.',
     ar:'اسم الـ class اللي فيه شرطة محتاج علامات تنصيص جوه الـ object: <code class="i">{ \'sold-out\': true }</code>. ومن غيرها بيبقى syntax error.'},
    {en:'<code class="i">[class]</code> with a string <b>replaces</b> the bound classes each time. If you are building a string with <code class="i">+</code>, an object is almost always clearer.',
     ar:'<code class="i">[class]</code> بنص بت<b>ستبدل</b> الـ classes المربوطة كل مرة. ولو بتبني نص بـ <code class="i">+</code>، الـ object تقريباً دايماً أوضح.'},
    {en:'Setting a style to <code class="i">null</code> removes it. Setting it to <code class="i">\'\'</code> or <code class="i">undefined</code> also works, but <code class="i">null</code> is the explicit way to say “no value”.',
     ar:'لما تحط استايل بـ <code class="i">null</code> بيتشال. و<code class="i">\'\'</code> أو <code class="i">undefined</code> بيشتغلوا كمان، بس <code class="i">null</code> هي الطريقة الصريحة اللي معناها «مفيش قيمة».'},
    {en:'<code class="i">NgOptimizedImage</code> requires <code class="i">width</code> and <code class="i">height</code>, and refuses to work without them. That is deliberate — it is what prevents layout shift.',
     ar:'<code class="i">NgOptimizedImage</code> محتاج <code class="i">width</code> و<code class="i">height</code>، ومش بيشتغل من غيرهم. وده مقصود — هو اللي بيمنع الـ layout shift.'},
    {en:'Forgetting <code class="i">NgClass</code> in <code class="i">imports</code> fails <b>silently</b> — the attribute is just ignored. This is a good reason to prefer <code class="i">[class]</code>, which cannot be forgotten.',
     ar:'نسيان <code class="i">NgClass</code> في <code class="i">imports</code> بيفشل <b>في صمت</b> — الخاصية بتتجاهل وخلاص. وده سبب كويس إنك تفضّل <code class="i">[class]</code>، اللي مستحيل تنساها.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'built-in-pipes',
  badge:{en:'Formatting', ar:'التنسيق'},
  title:{en:'Built-in pipes: date, currency, async and the rest', ar:'الـ pipes الجاهزة: التاريخ والعملة والـ async والباقي'},
  lead:{
    en:`A pipe formats a value on its way to the screen without touching the value itself. Angular ships about a dozen. Two of them — <code>date</code> and <code>currency</code> — save you from an entire category of locale bugs, and one of them, <code>async</code>, is a subscription that cleans up after itself.`,
    ar:`الـ pipe بتنسّق قيمة وهي في طريقها للشاشة من غير ما تلمس القيمة نفسها. وأنجولار بييجي بحوالي اتناشر. واتنين منهم — <code>date</code> و<code>currency</code> — بيوفروا عليك نوعية كاملة من باجات اللغة والمنطقة، وواحد منهم، <code>async</code>، هو subscription بينضّف ورا نفسه.`
  },
  nodes:[
    [{id:'ts',role:{en:'raw values',ar:'قيم خام'},file:'order.component',ext:'.ts',kind:'ts'}],
    [{id:'tpl',role:{en:'pipes them',ar:'بيمررها'},file:'order.component',ext:'.html',kind:'html'},
     {id:'out',role:{en:'what people read',ar:'اللي الناس بتقراه'},file:'rendered',ext:'.html',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'ts',to:'tpl',a:'bottom',b:'top',label:'1699.5',packet:'number'},
    {id:'e2',from:'tpl',to:'out',a:'right',b:'left',label:"| currency:'EGP'",packet:'EGP 1,699.50'}
  ],
  files:[
    {id:'tpl',name:'order.component.html',lang:'html',tag:{en:'the ones you will use',ar:'اللي هتستخدمهم'},code:[
      "<!-- DATE — a format string, not a library -->",
      "{{ order().placedAt | date }}                    <!-- Aug 21, 2026 -->",
      "{{ order().placedAt | date:'short' }}            <!-- 8/21/26, 9:14 PM -->",
      "{{ order().placedAt | date:'dd/MM/yyyy' }}       <!-- 21/08/2026 -->",
      "{{ order().placedAt | date:'HH:mm' }}            <!-- 21:14 -->",
      "{{ order().placedAt | date:'medium':'+0200' }}   <!-- with a timezone -->",
      "",
      "<!-- CURRENCY — code, display, digits -->",
      "{{ total() | currency:'EGP' }}                   <!-- EGP 1,699.50 -->",
      "{{ total() | currency:'USD':'symbol' }}          <!-- $1,699.50 -->",
      "{{ total() | currency:'EGP':'code':'1.0-0' }}    <!-- EGP 1,700 -->",
      "",
      "<!-- NUMBER and PERCENT — digitsInfo is min-int.min-frac-max-frac -->",
      "{{ weight() | number:'1.2-2' }}                  <!-- 3.50 -->",
      "{{ rate() | percent:'1.0-1' }}                   <!-- 12.5% -->",
      "",
      "<!-- TEXT -->",
      "{{ name() | uppercase }}   {{ name() | lowercase }}",
      "{{ name() | titlecase }}                         <!-- Mona Hassan -->",
      "{{ description() | slice:0:120 }}…",
      "",
      "<!-- JSON — for debugging only, but genuinely useful -->",
      "<pre>{{ order() | json }}</pre>",
      "",
      "<!-- pipes chain, left to right -->",
      "{{ title() | slice:0:40 | uppercase }}"]},
    {id:'async',name:'the async pipe',lang:'html',tag:{en:'the special one',ar:'المميز'},code:[
      "<!-- async subscribes, unwraps the value, and UNSUBSCRIBES",
      "     when the component is destroyed. no ngOnDestroy needed. -->",
      "",
      "@if (user$ | async; as user) {",
      "  <h1>{{ user.name }}</h1>",
      "}",
      "",
      "<!-- it works with Promises too -->",
      "{{ config | async }}",
      "",
      "<!-- ✗ THE CLASSIC BUG: each | async is a SEPARATE subscription. -->",
      "<h1>{{ (user$ | async)?.name }}</h1>",
      "<p>{{ (user$ | async)?.email }}</p>       <!-- two HTTP requests! -->",
      "",
      "<!-- ✓ subscribe once, name it with 'as' -->",
      "@if (user$ | async; as user) {",
      "  <h1>{{ user.name }}</h1>",
      "  <p>{{ user.email }}</p>",
      "}",
      "",
      "<!-- ✓✓ or skip it entirely: toSignal() has none of this problem -->",
      "<!-- readonly user = toSignal(this.user$); → {{ user()?.name }} -->"]},
    {id:'ts',name:'order.component.ts',lang:'ts',tag:{en:'raw values only',ar:'قيم خام بس'},code:[
      "export class OrderView {",
      "  readonly order = input.required<Order>();",
      "",
      "  // keep the RAW value in the class. never store a formatted string —",
      "  // formatting is a view concern and it changes with the locale.",
      "  readonly total = computed(() => this.order().items",
      "    .reduce((s, i) => s + i.price * i.qty, 0));",
      "",
      "  // if you need the same formatting in TypeScript (an export,",
      "  // a PDF, an email), inject the same functions the pipes use:",
      "  private readonly locale = inject(LOCALE_ID);",
      "  csvRow() {",
      "    return `${formatDate(this.order().placedAt, 'yyyy-MM-dd', this.locale)},`",
      "         + `${formatCurrency(this.total(), this.locale, 'EGP')}`;",
      "  }",
      "}"]},
    {id:'locale',name:'making them speak your language',lang:'ts',tag:{en:'app.config.ts',ar:'app.config.ts'},code:[
      "import { registerLocaleData } from '@angular/common';",
      "import localeArEg from '@angular/common/locales/ar-EG';",
      "import { LOCALE_ID } from '@angular/core';",
      "",
      "registerLocaleData(localeArEg);",
      "",
      "providers: [",
      "  { provide: LOCALE_ID, useValue: 'ar-EG' },",
      "  { provide: DEFAULT_CURRENCY_CODE, useValue: 'EGP' },",
      "]",
      "",
      "// now every date, currency, number and percent pipe in the app",
      "// formats for Egypt — dates, decimal separators, month names."]}
  ],
  steps:[
    {t:{en:'A pipe takes the value on its left and returns a new one. It never modifies the original — <code class="i">total()</code> is still the number <code class="i">1699.5</code> in your class.',
        ar:'الـ pipe بتاخد القيمة اللي على شمالها وترجّع واحدة جديدة. وعمرها ما بتعدّل الأصلية — <code class="i">total()</code> لسه الرقم <code class="i">1699.5</code> في الكلاس بتاعك.'},
     edge:'e1',hl:{ts:[4,5,6,7]}},
    {t:{en:'Arguments come after a colon, in order. <code class="i">date:\'dd/MM/yyyy\'</code>, <code class="i">currency:\'EGP\':\'code\':\'1.0-0\'</code> — position matters, so read the docs for the ones you use often.',
        ar:'المعاملات بتيجي بعد نقطتين، بالترتيب. <code class="i">date:\'dd/MM/yyyy\'</code> و<code class="i">currency:\'EGP\':\'code\':\'1.0-0\'</code> — الترتيب مهم، فاقرا الدوكس للي بتستخدمهم كتير.'},
     edge:'e2',hl:{tpl:[4,10,11]}},
    {t:{en:'<code class="i">digitsInfo</code> reads as <code class="i">min-integer.min-fraction-max-fraction</code>. <code class="i">\'1.2-2\'</code> means at least one digit before the point and exactly two after.',
        ar:'<code class="i">digitsInfo</code> بتتقري <code class="i">min-integer.min-fraction-max-fraction</code>. و<code class="i">\'1.2-2\'</code> معناها رقم واحد على الأقل قبل العلامة العشرية وبالظبط اتنين بعدها.'},
     hl:{tpl:[14,15]}},
    {t:{en:'<code class="i">async</code> is not a formatter — it subscribes to an observable, hands you the value, and unsubscribes on destroy. It is the only pipe with a lifecycle.',
        ar:'<code class="i">async</code> مش منسّق — هو بيعمل subscribe لـ observable، ويديك القيمة، ويلغي الاشتراك عند التدمير. وهو الـ pipe الوحيد اللي ليه دورة حياة.'},
     hl:{async:[1,2,4,5]}},
    {t:{en:'One <code class="i">LOCALE_ID</code> provider makes every date, number and currency in the app format for your region — including Arabic month names and Egyptian pound formatting.',
        ar:'provider واحد لـ <code class="i">LOCALE_ID</code> بيخلي كل تاريخ ورقم وعملة في التطبيق يتنسّق لمنطقتك — بما فيها أسامي الشهور بالعربي وتنسيق الجنيه المصري.'},
     hl:{locale:[5,8,9]}}
  ],
  example:[
    {
      name:{en:'One invoice row, formatted for two countries', ar:'صف فاتورة واحد، منسّق لبلدين'},
      what:{
        en:'The class stores numbers and Date objects. The template formats them. Switching the app from Egypt to Germany is one provider change — and no component file is touched, because none of them ever stored a formatted string.',
        ar:'الكلاس بيخزّن أرقام وكائنات Date. والتمبلت بينسّقهم. وتحويل التطبيق من مصر لألمانيا تغيير provider واحد — ومفيش ولا ملف component بيتلمس، لإن ولا واحد فيهم خزّن نص منسّق أصلاً.'},
      files:[
        {id:'iv1',name:'invoice-row.component.html',lang:'html',tag:{en:'one template',ar:'تمبلت واحد'},code:[
          "<tr>",
          "  <td>{{ line().description | titlecase }}</td>",
          "  <td>{{ line().qty | number }}</td>",
          "  <td>{{ line().unitPrice | currency }}</td>",
          "  <td>{{ line().vatRate | percent:'1.0-0' }}</td>",
          "  <td>{{ lineTotal() | currency }}</td>",
          "  <td>{{ line().dueAt | date:'mediumDate' }}</td>",
          "</tr>"]},
        {id:'iv2',name:'the two configurations',lang:'ts',tag:{en:'one line each',ar:'سطر لكل واحدة'},code:[
          "// Egypt",
          "{ provide: LOCALE_ID, useValue: 'ar-EG' },",
          "{ provide: DEFAULT_CURRENCY_CODE, useValue: 'EGP' },",
          "// → ٢١‏/٠٨‏/٢٠٢٦    ج.م.‏ ١٬٦٩٩٫٥٠",
          "",
          "// Germany",
          "{ provide: LOCALE_ID, useValue: 'de-DE' },",
          "{ provide: DEFAULT_CURRENCY_CODE, useValue: 'EUR' },",
          "// → 21.08.2026     1.699,50 €",
          "",
          "// same template. same component. same data."]}
      ]
    }
  ],
  gotchas:[
    {en:'Every built-in pipe except <code class="i">async</code> and <code class="i">json</code> needs its locale registered for anything other than <code class="i">en-US</code>, or you get <b>NG0701: Missing locale data</b>.',
     ar:'كل pipe جاهز غير <code class="i">async</code> و<code class="i">json</code> محتاج الـ locale بتاعه يتسجّل لأي حاجة غير <code class="i">en-US</code>، وإلا هيطلعلك <b>NG0701: Missing locale data</b>.'},
    {en:'<b>Two <code class="i">| async</code> on the same observable = two subscriptions = two HTTP requests.</b> Use <code class="i">&#64;if (x$ | async; as x)</code>, or <code class="i">toSignal()</code> and stop worrying.',
     ar:'<b>اتنين <code class="i">| async</code> على نفس الـ observable = اشتراكين = ريكويستين HTTP.</b> استعمل <code class="i">&#64;if (x$ | async; as x)</code>، أو <code class="i">toSignal()</code> وارتاح.'},
    {en:'The <code class="i">date</code> pipe wants a <code class="i">Date</code>, a number, or an ISO string. A non-ISO string like <code class="i">"21/08/2026"</code> throws.',
     ar:'الـ <code class="i">date</code> pipe عايز <code class="i">Date</code> أو رقم أو نص ISO. وأي نص مش ISO زي <code class="i">"21/08/2026"</code> بيرمي ايرور.'},
    {en:'Never store a formatted string in your state. Keep the number and the <code class="i">Date</code>; format at the edge, in the template.',
     ar:'عمرك ما تخزّن نص منسّق في الحالة بتاعتك. خلي الرقم والـ <code class="i">Date</code>؛ ونسّق عند الحافة، في التمبلت.'},
    {en:'Need the same formatting in TypeScript? Import <code class="i">formatDate</code>, <code class="i">formatCurrency</code> and <code class="i">formatNumber</code> from <code class="i">&#64;angular/common</code> — same engine, callable from a class.',
     ar:'محتاج نفس التنسيق في TypeScript؟ اعمل import لـ <code class="i">formatDate</code> و<code class="i">formatCurrency</code> و<code class="i">formatNumber</code> من <code class="i">&#64;angular/common</code> — نفس المحرك، وبتتنادى من كلاس.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'basic-routing',
  badge:{en:'More than one page', ar:'أكتر من صفحة'},
  title:{en:'Routing and navigation, the basics', ar:'الـ routing والتنقل، الأساسيات'},
  lead:{
    en:`Your app has one HTML file, so a "page" is a component the router swaps into a hole in your layout. Three things do all of it: a <b>routes array</b>, a <b>&lt;router-outlet /&gt;</b>, and <b>routerLink</b> on your links.`,
    ar:`تطبيقك فيه ملف HTML واحد، فالـ «صفحة» دي component الراوتر بيبدّله في فتحة في التخطيط بتاعك. وتلات حاجات بيعملوا ده كله: <b>مصفوفة routes</b>، و<b>&lt;router-outlet /&gt;</b>، و<b>routerLink</b> على اللينكات.`
  },
  nodes:[
    [{id:'routes',role:{en:'the URL map',ar:'خريطة الـ URL'},file:'app.routes',ext:'.ts',kind:'cfg'},
     {id:'shell',role:{en:'has the hole',ar:'فيه الفتحة'},file:'app',ext:'.html',kind:'html'}],
    [{id:'page',role:{en:'gets swapped in',ar:'بيتحط مكانها'},file:'about',ext:'.ts',kind:'ts'},
     {id:'ts',role:{en:'navigates in code',ar:'بينقل بالكود'},file:'login',ext:'.ts',kind:'ts'}]
  ],
  edges:[
    {id:'e1',from:'shell',to:'routes',a:'left',b:'right',label:'routerLink="/about"',packet:'/about'},
    {id:'e2',from:'routes',to:'page',a:'bottom',b:'top',label:'loadComponent',packet:'About'},
    {id:'e3',from:'ts',to:'routes',a:'top',b:'bottom',label:'router.navigate()',packet:'/orders/42'}
  ],
  files:[
    {id:'routes',name:'app.routes.ts',lang:'ts',tag:{en:'the map',ar:'الخريطة'},code:[
      "import { Routes } from '@angular/router';",
      "",
      "export const routes: Routes = [",
      "  // '' is the home page. pathMatch: 'full' or it matches everything.",
      "  { path: '', pathMatch: 'full', redirectTo: 'products' },",
      "",
      "  {",
      "    path: 'products',",
      "    title: 'Products',        // sets the browser tab title. free a11y win.",
      "    loadComponent: () => import('./pages/products').then(m => m.Products),",
      "  },",
      "",
      "  // :id is a PARAMETER — /products/42 puts '42' into it",
      "  {",
      "    path: 'products/:id',",
      "    loadComponent: () => import('./pages/product-detail').then(m => m.ProductDetail),",
      "  },",
      "",
      "  { path: 'about', loadComponent: () => import('./pages/about').then(m => m.About) },",
      "",
      "  // ** matches anything left over. it MUST be last.",
      "  { path: '**', loadComponent: () => import('./pages/not-found').then(m => m.NotFound) },",
      "];"]},
    {id:'shell',name:'app.html',lang:'html',tag:{en:'the layout',ar:'التخطيط'},code:[
      "<nav>",
      "  <!-- routerLink does NOT reload the page. href does. -->",
      "  <a routerLink=\"/products\" routerLinkActive=\"active\">Products</a>",
      "  <a routerLink=\"/about\" routerLinkActive=\"active\">About</a>",
      "",
      "  <!-- exact match, so /products does not stay highlighted",
      "       when you are on /products/42 -->",
      "  <a routerLink=\"/\" routerLinkActive=\"active\"",
      "     [routerLinkActiveOptions]=\"{ exact: true }\">Home</a>",
      "",
      "  <!-- building a URL from parts -->",
      "  <a [routerLink]=\"['/products', product.id]\">{{ product.name }}</a>",
      "",
      "  <!-- with a query string: /products?sort=price&page=2 -->",
      "  <a routerLink=\"/products\" [queryParams]=\"{ sort: 'price', page: 2 }\">",
      "    Cheapest first",
      "  </a>",
      "</nav>",
      "",
      "<!-- THE HOLE. the matched component is rendered right here. -->",
      "<router-outlet />",
      "",
      "<footer>Always visible, on every page.</footer>"]},
    {id:'page',name:'product-detail.ts',lang:'ts',tag:{en:'reading the URL',ar:'قراية الـ URL'},code:[
      "@Component({ selector: 'app-product-detail', template: `…` })",
      "export class ProductDetail {",
      "  // with withComponentInputBinding(), the router fills these",
      "  // straight from the URL. no ActivatedRoute needed.",
      "  readonly id = input.required<string>();      // from  /products/:id",
      "  readonly sort = input('name');               // from  ?sort=",
      "",
      "  // and because it is a signal, navigating from /products/1",
      "  // to /products/2 just changes the value — the component",
      "  // is reused and everything downstream updates.",
      "  readonly product = computed(() => this.lookup(this.id()));",
      "}"]},
    {id:'ts',name:'login.ts',lang:'ts',tag:{en:'navigating in code',ar:'التنقل بالكود'},code:[
      "export class Login {",
      "  private readonly router = inject(Router);",
      "",
      "  async submit() {",
      "    await this.api.signIn(this.email(), this.password());",
      "",
      "    // a plain URL",
      "    this.router.navigateByUrl('/products');",
      "",
      "    // built from parts — safer, it escapes for you",
      "    this.router.navigate(['/products', id]);",
      "",
      "    // with a query string",
      "    this.router.navigate(['/products'], { queryParams: { sort: 'price' } });",
      "",
      "    // replace history so Back does not return to the login form",
      "    this.router.navigate(['/'], { replaceUrl: true });",
      "  }",
      "}"]},
    {id:'cfg',name:'app.config.ts',lang:'ts',tag:{en:'turning it on',ar:'التشغيل'},code:[
      "import { provideRouter, withComponentInputBinding } from '@angular/router';",
      "",
      "providers: [",
      "  provideRouter(routes, withComponentInputBinding()),",
      "]",
      "",
      "// and in app.ts:",
      "// imports: [RouterOutlet, RouterLink, RouterLinkActive]",
      "//",
      "// these three are components/directives like any other —",
      "// forget them and the tags silently do nothing."]}
  ],
  steps:[
    {t:{en:'The routes array maps a <b>URL path</b> to a component. Order matters: <code class="i">**</code> must be last, or it swallows everything below it.',
        ar:'مصفوفة الـ routes بتربط <b>مسار URL</b> بـ component. والترتيب مهم: <code class="i">**</code> لازم تبقى الأخيرة، وإلا هتبلع كل اللي تحتها.'},
     hl:{routes:[5,8,15,22]}},
    {t:{en:'Clicking a <code class="i">routerLink</code> changes the URL <b>without reloading</b>. Using <code class="i">href</code> instead reloads the whole app and you lose every signal, every form, every scroll position.',
        ar:'الكليك على <code class="i">routerLink</code> بيغيّر الـ URL <b>من غير إعادة تحميل</b>. أما لو استعملت <code class="i">href</code> فالتطبيق كله بيتحمّل من الأول وبتضيّع كل signal وكل فورم وكل مكان scroll.'},
     edge:'e1',hl:{shell:[2,3,4]}},
    {t:{en:'The router finds the match, downloads that component&rsquo;s chunk, and renders it inside <code class="i">&lt;router-outlet /&gt;</code>. Everything outside the outlet stays exactly as it was.',
        ar:'الراوتر بيلاقي المطابقة، وينزّل الـ chunk بتاع الـ component ده، ويرسمه جوه <code class="i">&lt;router-outlet /&gt;</code>. وكل حاجة بره الـ outlet بتفضل زي ما هي بالظبط.'},
     edge:'e2',hl:{shell:[21],routes:[10]}},
    {t:{en:'A <code class="i">:id</code> in the path becomes an input on the component. Navigating between two ids <b>reuses</b> the component and just changes the signal.',
        ar:'الـ <code class="i">:id</code> اللي في المسار بتبقى input في الـ component. والتنقل بين id واتنين <b>بيعيد استخدام</b> الـ component ويغيّر الـ signal بس.'},
     hl:{page:[5,6,10,11]}},
    {t:{en:'To navigate from code — after a login, after a save — inject <code class="i">Router</code> and call <code class="i">navigate</code>. Use <code class="i">replaceUrl</code> when Back should not return to where you were.',
        ar:'وعشان تنقل من الكود — بعد تسجيل دخول أو بعد حفظ — اعمل inject لـ <code class="i">Router</code> ونادي <code class="i">navigate</code>. واستعمل <code class="i">replaceUrl</code> لما زرار الرجوع مايرجعش لمكانك القديم.'},
     edge:'e3',hl:{ts:[8,11,16]}},
    {t:{en:'None of it works until <code class="i">provideRouter</code> is in your providers and the three router directives are in <code class="i">imports</code>.',
        ar:'ومفيش حاجة فيهم هتشتغل قبل ما <code class="i">provideRouter</code> يكون في الـ providers والتلات directives بتوع الراوتر في <code class="i">imports</code>.'},
     hl:{cfg:[4,8,9]}}
  ],
  example:[
    {
      name:{en:'A master–detail flow', ar:'قايمة وتفاصيل'},
      what:{
        en:'The most common shape in any app: a list, and a page for one item. The list links by id, the detail page reads that id as an input, and the browser Back button works correctly with no code at all.',
        ar:'أشهر شكل في أي تطبيق: لستة، وصفحة لعنصر واحد. اللستة بتعمل لينك بالـ id، وصفحة التفاصيل بتقرا الـ id ده كـ input، وزرار الرجوع في البراوزر شغال صح من غير ولا سطر كود.'},
      files:[
        {id:'md1',name:'products.html',lang:'html',tag:{en:'the list',ar:'اللستة'},code:[
          "@for (p of products(); track p.id) {",
          "  <a [routerLink]=\"['/products', p.id]\" class=\"card\">",
          "    <h3>{{ p.name }}</h3>",
          "    <span>{{ p.price | currency:'EGP' }}</span>",
          "  </a>",
          "}"]},
        {id:'md2',name:'product-detail.html',lang:'html',tag:{en:'the detail',ar:'التفاصيل'},code:[
          "@if (product(); as p) {",
          "  <a routerLink=\"/products\">← All products</a>",
          "  <h1>{{ p.name }}</h1>",
          "  <p>{{ p.description }}</p>",
          "  <button (click)=\"addToCart(p)\">Add to cart</button>",
          "} @else {",
          "  <p>That product does not exist. <a routerLink=\"/products\">Go back</a></p>",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>Use <code class="i">routerLink</code>, never <code class="i">href</code></b>, for links inside your app. <code class="i">href</code> triggers a full page reload and throws away all your state.',
     ar:'<b>استعمل <code class="i">routerLink</code> مش <code class="i">href</code></b> للينكات جوه تطبيقك. <code class="i">href</code> بيعمل إعادة تحميل كاملة وبيرمي كل حالتك.'},
    {en:'<code class="i">RouterOutlet</code>, <code class="i">RouterLink</code> and <code class="i">RouterLinkActive</code> must be in <code class="i">imports</code>. A missing <code class="i">RouterLink</code> makes the anchor do nothing at all — silently.',
     ar:'<code class="i">RouterOutlet</code> و<code class="i">RouterLink</code> و<code class="i">RouterLinkActive</code> لازم يكونوا في <code class="i">imports</code>. ولو <code class="i">RouterLink</code> ناقص اللينك مش هيعمل حاجة خالص — وفي صمت.'},
    {en:'<code class="i">path: \'\'</code> without <code class="i">pathMatch: \'full\'</code> matches <b>every</b> URL, so nothing below it is ever reached.',
     ar:'<code class="i">path: \'\'</code> من غير <code class="i">pathMatch: \'full\'</code> بتطابق <b>كل</b> URL، فمفيش حاجة تحتها بتتوصل أبداً.'},
    {en:'Paths have no leading slash in the routes array (<code class="i">\'about\'</code>), but links do (<code class="i">routerLink="/about"</code>). Mixing them up is a very common first-day bug.',
     ar:'المسارات مفيهاش شرطة في الأول في مصفوفة الـ routes (<code class="i">\'about\'</code>)، لكن اللينكات فيها (<code class="i">routerLink="/about"</code>). وخلطهم باج شائع جداً أول يوم.'},
    {en:'<code class="i">routerLinkActive</code> matches by prefix. Add <code class="i">[routerLinkActiveOptions]="{ exact: true }"</code> to your home link or it stays highlighted everywhere.',
     ar:'<code class="i">routerLinkActive</code> بتطابق بالبادئة. ضيف <code class="i">[routerLinkActiveOptions]="{ exact: true }"</code> للينك الرئيسية وإلا هتفضل منوّرة في كل حتة.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'template-driven-forms',
  badge:{en:'The quick way', ar:'الطريقة السريعة'},
  title:{en:'Template-driven forms with <code>ngModel</code>', ar:'الفورمز من التمبلت بـ <code>ngModel</code>'},
  lead:{
    en:`Angular has two form systems. This is the small one: you declare the form in the <b>HTML</b> and Angular builds the model for you. It is the right choice for a login box or a contact form — and the wrong one the moment validation gets interesting.`,
    ar:`أنجولار عنده نظامين للفورمز. وده الصغير: بتعلن عن الفورم في الـ <b>HTML</b> وأنجولار بيبني الموديل بدالك. وهو الاختيار الصح لخانة تسجيل دخول أو فورم تواصل — والغلط أول ما الـ validation تبقى معقدة.`
  },
  nodes:[
    [{id:'tpl',role:{en:'declares the form',ar:'بيعلن الفورم'},file:'contact.component',ext:'.html',kind:'html'}],
    [{id:'ts',role:{en:'holds the model',ar:'ماسك الموديل'},file:'contact.component',ext:'.ts',kind:'ts'},
     {id:'ng',role:{en:'builds it for you',ar:'بيبنيه لك'},file:'FormsModule',ext:'',kind:'cfg'}]
  ],
  edges:[
    {id:'e1',from:'ng',to:'tpl',a:'top',b:'right',label:'ngForm + ngModel',packet:'auto'},
    {id:'e2',from:'tpl',to:'ts',a:'bottom',b:'top',label:'[(ngModel)]="model.email"',packet:'two-way'},
    {id:'e3',from:'tpl',to:'ts',a:'left',b:'left',label:'(ngSubmit)="send(f)"',packet:'submit'}
  ],
  files:[
    {id:'tpl',name:'contact.component.html',lang:'html',tag:{en:'the form lives here',ar:'الفورم عايش هنا'},code:[
      "<!-- #f=\"ngForm\" hands you the form object Angular built -->",
      "<form #f=\"ngForm\" (ngSubmit)=\"send(f)\">",
      "",
      "  <label>",
      "    Name",
      "    <input name=\"name\" [(ngModel)]=\"model.name\" required minlength=\"2\"",
      "           #name=\"ngModel\">",
      "  </label>",
      "",
      "  @if (name.touched && name.invalid) {",
      "    <p class=\"err\">",
      "      @if (name.errors?.['required']) { Please tell us your name. }",
      "      @else if (name.errors?.['minlength']) { At least 2 characters. }",
      "    </p>",
      "  }",
      "",
      "  <label>",
      "    Email",
      "    <input name=\"email\" type=\"email\" [(ngModel)]=\"model.email\" required email",
      "           #email=\"ngModel\">",
      "  </label>",
      "",
      "  @if (email.touched && email.invalid) {",
      "    <p class=\"err\">A valid email address, please.</p>",
      "  }",
      "",
      "  <textarea name=\"message\" [(ngModel)]=\"model.message\" required></textarea>",
      "",
      "  <button [disabled]=\"f.invalid || sending()\">Send</button>",
      "</form>"]},
    {id:'ts',name:'contact.component.ts',lang:'ts',tag:{en:'just a plain object',ar:'مجرد object عادي'},code:[
      "import { FormsModule, NgForm } from '@angular/forms';",
      "",
      "@Component({",
      "  selector: 'app-contact',",
      "  imports: [FormsModule],          // ← without this, ngModel does nothing",
      "  templateUrl: './contact.component.html',",
      "})",
      "export class Contact {",
      "  readonly sending = signal(false);",
      "",
      "  // no FormGroup, no FormControl. a plain object.",
      "  model = { name: '', email: '', message: '' };",
      "",
      "  async send(f: NgForm) {",
      "    if (f.invalid) { f.form.markAllAsTouched(); return; }",
      "",
      "    this.sending.set(true);",
      "    try {",
      "      await this.api.contact(this.model);",
      "      f.resetForm();               // clears values AND touched/dirty state",
      "    } finally {",
      "      this.sending.set(false);",
      "    }",
      "  }",
      "}"]},
    {id:'state',name:'the state every control carries',lang:'html',tag:{en:'useful for styling',ar:'مفيدة للتنسيق'},code:[
      "<!-- Angular puts these classes on every ngModel input -->",
      "",
      "ng-valid     ng-invalid      <!-- does it pass its validators? -->",
      "ng-touched   ng-untouched    <!-- has it been focused and left? -->",
      "ng-dirty     ng-pristine     <!-- has the value been changed? -->",
      "",
      "<!-- so you can style errors with CSS alone: -->",
      "<style>",
      "  input.ng-invalid.ng-touched { border-color: crimson; }",
      "  input.ng-valid.ng-dirty     { border-color: seagreen; }",
      "</style>",
      "",
      "<!-- show errors on TOUCHED, not on invalid. nobody wants to be",
      "     told their email is wrong after typing the first letter. -->"]},
    {id:'which',name:'which form system?',lang:'ts',tag:{en:'the decision',ar:'القرار'},code:[
      "// TEMPLATE-DRIVEN — ngModel, FormsModule",
      "//   ✓ a login box, a contact form, a search filter",
      "//   ✓ fewer moving parts, quicker to write",
      "//   ✗ validation logic lives in HTML attributes",
      "//   ✗ hard to unit test — you need the DOM",
      "//   ✗ no cross-field rules without pain",
      "//   ✗ the model is not strongly typed",
      "",
      "// REACTIVE — FormGroup, FormBuilder, ReactiveFormsModule",
      "//   ✓ anything with real validation rules",
      "//   ✓ fields that appear based on other fields",
      "//   ✓ fully typed, testable without a browser",
      "//   ✗ more code up front",
      "",
      "// Rule of thumb: under five fields and no cross-field rules,",
      "// template-driven is fine. Anything else, go reactive."]}
  ],
  steps:[
    {t:{en:'<code class="i">FormsModule</code> in <code class="i">imports</code> is what activates <code class="i">ngModel</code>. Without it, the attribute is silently ignored and nothing binds.',
        ar:'<code class="i">FormsModule</code> في <code class="i">imports</code> هو اللي بيشغّل <code class="i">ngModel</code>. ومن غيره الخاصية بتتجاهل في صمت ومفيش حاجة بتترابط.'},
     edge:'e1',hl:{ts:[5]}},
    {t:{en:'Every <code class="i">ngModel</code> needs a <code class="i">name</code> attribute. That name is how Angular registers the control on the form it is building for you.',
        ar:'كل <code class="i">ngModel</code> محتاجة خاصية <code class="i">name</code>. والاسم ده هو إزاي أنجولار بيسجّل الـ control في الفورم اللي بيبنيه بدالك.'},
     hl:{tpl:[6,19]}},
    {t:{en:'<code class="i">[(ngModel)]</code> is ordinary two-way binding into a plain object. There is no <code class="i">FormControl</code> anywhere in your class.',
        ar:'<code class="i">[(ngModel)]</code> ده two-way binding عادي في object عادي. ومفيش <code class="i">FormControl</code> في أي حتة في الكلاس بتاعك.'},
     edge:'e2',hl:{tpl:[6],ts:[12]}},
    {t:{en:'<code class="i">#name="ngModel"</code> gives you that control&rsquo;s state — <code class="i">touched</code>, <code class="i">invalid</code>, <code class="i">errors</code> — so you can show a message at the right moment.',
        ar:'<code class="i">#name="ngModel"</code> بتديك حالة الـ control ده — <code class="i">touched</code> و<code class="i">invalid</code> و<code class="i">errors</code> — فتقدر توري الرسالة في الوقت المناسب.'},
     hl:{tpl:[7,10,12,13]}},
    {t:{en:'<code class="i">#f="ngForm"</code> gives you the whole form. <code class="i">f.invalid</code> disables the button; <code class="i">f.resetForm()</code> clears values <b>and</b> the touched state.',
        ar:'<code class="i">#f="ngForm"</code> بتديك الفورم كله. <code class="i">f.invalid</code> بتعطّل الزرار؛ و<code class="i">f.resetForm()</code> بتمسح القيم <b>و</b>حالة الـ touched.'},
     edge:'e3',hl:{tpl:[2,29],ts:[14,19]}},
    {t:{en:'Angular puts state classes on every control, so you can style errors in CSS alone.',
        ar:'أنجولار بيحط classes للحالة على كل control، فتقدر تنسّق الأخطاء بـ CSS لوحدها.'},
     hl:{state:[3,4,5,8,9]}},
    {t:{en:'And the deciding rule: under five fields with no cross-field rules, stay here. Anything more, go reactive.',
        ar:'والقاعدة الفاصلة: أقل من خمس حقول ومفيش قواعد بين الحقول، فضل هنا. وأي حاجة أكتر، حوّل لـ reactive.'},
     hl:{which:[1,2,9,15,16]}}
  ],
  example:[
    {
      name:{en:'A login box — the right job for this', ar:'خانة تسجيل دخول — الشغلانة الصح ليها'},
      what:{
        en:'Two fields, two simple rules, no cross-field validation, no dynamic structure. Reaching for FormBuilder here would be three times the code for exactly the same result.',
        ar:'حقلين، وقاعدتين بسيطتين، ومفيش validation بين الحقول، ومفيش بنية متغيرة. واستخدام FormBuilder هنا هيبقى تلات أضعاف الكود لنفس النتيجة بالظبط.'},
      files:[
        {id:'lg1',name:'login.component.html',lang:'html',tag:{en:'six lines of form',ar:'ست سطور فورم'},code:[
          "<form #f=\"ngForm\" (ngSubmit)=\"signIn()\">",
          "  <input name=\"email\" type=\"email\" [(ngModel)]=\"email\" required email",
          "         autocomplete=\"email\" placeholder=\"Email\">",
          "",
          "  <input name=\"password\" type=\"password\" [(ngModel)]=\"password\" required",
          "         minlength=\"8\" autocomplete=\"current-password\" placeholder=\"Password\">",
          "",
          "  @if (error()) { <p class=\"err\" role=\"alert\">{{ error() }}</p> }",
          "",
          "  <button [disabled]=\"f.invalid || busy()\">",
          "    {{ busy() ? 'Signing in…' : 'Sign in' }}",
          "  </button>",
          "</form>"]},
        {id:'lg2',name:'login.component.ts',lang:'ts',tag:{en:'no form objects',ar:'مفيش كائنات فورم'},code:[
          "export class Login {",
          "  email = '';",
          "  password = '';",
          "",
          "  readonly busy = signal(false);",
          "  readonly error = signal('');",
          "",
          "  async signIn() {",
          "    this.busy.set(true);",
          "    this.error.set('');",
          "    try {",
          "      await this.auth.signIn(this.email, this.password);",
          "      this.router.navigate(['/'], { replaceUrl: true });",
          "    } catch {",
          "      this.error.set('That email and password do not match.');",
          "    } finally {",
          "      this.busy.set(false);",
          "    }",
          "  }",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<code class="i">ngModel</code> inside a <code class="i">&lt;form&gt;</code> <b>requires</b> a <code class="i">name</code> attribute, or Angular throws at runtime. Outside a form, use <code class="i">[ngModelOptions]="{ standalone: true }"</code>.',
     ar:'<code class="i">ngModel</code> جوه <code class="i">&lt;form&gt;</code> <b>محتاجة</b> خاصية <code class="i">name</code>، وإلا أنجولار هيرمي ايرور وقت التشغيل. وبره الفورم، استعمل <code class="i">[ngModelOptions]="{ standalone: true }"</code>.'},
    {en:'Forgetting <code class="i">FormsModule</code> fails silently — the input renders, nothing binds, and there is no error message.',
     ar:'نسيان <code class="i">FormsModule</code> بيفشل في صمت — الـ input بيترسم، ومفيش حاجة بتترابط، ومفيش رسالة خطأ.'},
    {en:'<code class="i">ngModel</code> updates on every keystroke. For a search box use <code class="i">[ngModelOptions]="{ updateOn: \'blur\' }"</code> or debounce it.',
     ar:'<code class="i">ngModel</code> بتتحدث مع كل حرف. ولخانة بحث استعمل <code class="i">[ngModelOptions]="{ updateOn: \'blur\' }"</code> أو اعمل لها debounce.'},
    {en:'<code class="i">f.reset()</code> clears values but leaves the form looking touched. <code class="i">f.resetForm()</code> clears both — use that one.',
     ar:'<code class="i">f.reset()</code> بتمسح القيم بس بتسيب الفورم شكله touched. أما <code class="i">f.resetForm()</code> فبتمسح الاتنين — استعمل دي.'},
    {en:'The moment you need a rule about <b>two</b> fields together — password confirmation, date ranges — stop and switch to reactive forms. Template-driven cannot express it cleanly.',
     ar:'أول ما تحتاج قاعدة بتخص <b>حقلين</b> مع بعض — تأكيد الباسورد أو نطاق تواريخ — قف وحوّل لـ reactive forms. الطريقة دي مش بتعبّر عنها بشكل نضيف.'}
  ]
},

/* ---------------------------------------------------------------- */
{
  slug:'http-basics',
  badge:{en:'Talking to a server', ar:'الكلام مع سيرفر'},
  title:{en:'<code>HttpClient</code>: GET, POST and the rest', ar:'<code>HttpClient</code>: GET و POST والباقي'},
  lead:{
    en:`One service handles every request your app makes. It returns an <b>Observable</b>, which means nothing happens until somebody subscribes — the single most confusing thing about it on day one, and the reason your API call "did not fire".`,
    ar:`service واحدة بتتعامل مع كل ريكويست في تطبيقك. وبترجّع <b>Observable</b>، ومعناها إن مفيش حاجة بتحصل لحد ما حد يعمل subscribe — وده أكتر حاجة ملخبطة فيها أول يوم، وسبب إن نداء الـ API بتاعك «مشتغلش».`
  },
  nodes:[
    [{id:'cfg',role:{en:'switches it on',ar:'بيشغّلها'},file:'app.config',ext:'.ts',kind:'cfg'},
     {id:'svc',role:{en:'owns the URLs',ar:'صاحب الـ URLs'},file:'product.service',ext:'.ts',kind:'svc'}],
    [{id:'cmp',role:{en:'asks for data',ar:'بيطلب داتا'},file:'products.component',ext:'.ts',kind:'ts'},
     {id:'tpl',role:{en:'shows 3 states',ar:'بيعرض 3 حالات'},file:'products.component',ext:'.html',kind:'html'}]
  ],
  edges:[
    {id:'e1',from:'cfg',to:'svc',a:'right',b:'left',label:'provideHttpClient()',packet:'HttpClient'},
    {id:'e2',from:'cmp',to:'svc',a:'top',b:'bottom',label:'getAll()',packet:'GET /api'},
    {id:'e3',from:'cmp',to:'tpl',a:'right',b:'left',label:'products()',packet:'Product[]'}
  ],
  files:[
    {id:'cfg',name:'app.config.ts',lang:'ts',tag:{en:'one line, once',ar:'سطر واحد، مرة'},code:[
      "import { provideHttpClient, withFetch } from '@angular/common/http';",
      "",
      "export const appConfig: ApplicationConfig = {",
      "  providers: [",
      "    provideHttpClient(withFetch()),",
      "    provideRouter(routes),",
      "  ],",
      "};",
      "",
      "// forget this and you get:",
      "// NG0201: No provider for _HttpClient found"]},
    {id:'svc',name:'product.service.ts',lang:'ts',tag:{en:'all four verbs',ar:'الأربع أفعال'},code:[
      "import { HttpClient, HttpParams } from '@angular/common/http';",
      "",
      "@Injectable({ providedIn: 'root' })",
      "export class ProductService {",
      "  private readonly http = inject(HttpClient);",
      "  private readonly base = '/api/products';",
      "",
      "  // the <Product[]> is what makes the response typed",
      "  getAll(search = '', page = 1) {",
      "    const params = new HttpParams()          // escapes for you",
      "      .set('q', search)",
      "      .set('page', page);",
      "    return this.http.get<Product[]>(this.base, { params });",
      "  }",
      "",
      "  getOne(id: string) {",
      "    return this.http.get<Product>(`${this.base}/${id}`);",
      "  }",
      "",
      "  create(draft: NewProduct) {",
      "    return this.http.post<Product>(this.base, draft);",
      "  }",
      "",
      "  update(id: string, patch: Partial<Product>) {",
      "    return this.http.patch<Product>(`${this.base}/${id}`, patch);",
      "  }",
      "",
      "  remove(id: string) {",
      "    return this.http.delete<void>(`${this.base}/${id}`);",
      "  }",
      "}"]},
    {id:'cmp',name:'products.component.ts',lang:'ts',tag:{en:'three ways to consume',ar:'تلات طرق للاستخدام'},code:[
      "export class Products {",
      "  private readonly api = inject(ProductService);",
      "",
      "  // ── 1. the simplest modern way ─────────────────────────────",
      "  readonly products = toSignal(this.api.getAll(), { initialValue: [] });",
      "",
      "  // ── 2. the async pipe, no signal at all ────────────────────",
      "  readonly products$ = this.api.getAll();",
      "  //  template:  @if (products$ | async; as list) { … }",
      "",
      "  // ── 3. subscribe by hand — only when you must ──────────────",
      "  readonly list = signal<Product[]>([]);",
      "  readonly loading = signal(true);",
      "  readonly error = signal('');",
      "",
      "  constructor() {",
      "    this.api.getAll().subscribe({",
      "      next: p => { this.list.set(p); this.loading.set(false); },",
      "      error: e => { this.error.set('Could not load'); this.loading.set(false); },",
      "    });",
      "  }",
      "",
      "  // a POST is an EVENT, so subscribing by hand is correct here",
      "  save(draft: NewProduct) {",
      "    this.api.create(draft).subscribe(created => {",
      "      this.list.update(l => [created, ...l]);",
      "    });",
      "  }",
      "}"]},
    {id:'lazy',name:'why nothing happened',lang:'ts',tag:{en:'the day-one bug',ar:'باج أول يوم'},code:[
      "// ✗ this makes NO request. none. the network tab is empty.",
      "this.http.get<Product[]>('/api/products');",
      "",
      "// an Observable is a RECIPE, not a running task. it is COLD:",
      "// it does nothing until something subscribes to it.",
      "",
      "// ✓ any of these actually fire it:",
      "this.http.get<Product[]>('/api/products').subscribe(p => …);",
      "toSignal(this.http.get<Product[]>('/api/products'));",
      "await firstValueFrom(this.http.get<Product[]>('/api/products'));",
      "// …or  {{ products$ | async }}  in a template",
      "",
      "// ALSO: each subscribe fires a SEPARATE request.",
      "// two | async on the same observable = two GETs."]},
    {id:'tpl',name:'products.component.html',lang:'html',tag:{en:'the three states',ar:'التلات حالات'},code:[
      "@if (loading()) {",
      "  <app-skeleton />",
      "} @else if (error()) {",
      "  <p class=\"err\">{{ error() }} <button (click)=\"retry()\">Try again</button></p>",
      "} @else if (!list().length) {",
      "  <p>No products yet.</p>",
      "} @else {",
      "  @for (p of list(); track p.id) { <app-product-card [product]=\"p\" /> }",
      "}"]}
  ],
  steps:[
    {t:{en:'<code class="i">provideHttpClient()</code> in <code class="i">app.config.ts</code> is the whole setup. Without it, injecting <code class="i">HttpClient</code> throws <b>NG0201</b>.',
        ar:'<code class="i">provideHttpClient()</code> في <code class="i">app.config.ts</code> ده كل الإعداد. ومن غيره، عمل inject لـ <code class="i">HttpClient</code> بيرمي <b>NG0201</b>.'},
     edge:'e1',hl:{cfg:[5,10,11]}},
    {t:{en:'Keep URLs in a <b>service</b>, never in a component. When the API path changes you edit one file, and the same call is reusable from three screens.',
        ar:'خلي الـ URLs في <b>service</b>، مش في component. ولما مسار الـ API يتغير بتعدّل ملف واحد، ونفس النداء بيبقى قابل للاستخدام من تلات شاشات.'},
     hl:{svc:[6,9,13]}},
    {t:{en:'The <code class="i">&lt;Product[]&gt;</code> generic is what gives you autocomplete on the response. It is a <b>promise about the shape</b>, not a runtime check — the server can still send you anything.',
        ar:'الـ generic <code class="i">&lt;Product[]&gt;</code> هو اللي بيديك اقتراحات على الرد. وهو <b>وعد بالشكل</b>، مش تحقق وقت التشغيل — السيرفر لسه ممكن يبعتلك أي حاجة.'},
     edge:'e2',hl:{svc:[8,13,17]}},
    {t:{en:'<b>Nothing happens until you subscribe.</b> An Observable is a recipe. This is the number one “my API call is not firing” cause.',
        ar:'<b>مفيش حاجة بتحصل لحد ما تعمل subscribe.</b> الـ Observable وصفة. وده السبب رقم واحد لـ «نداء الـ API مش بيشتغل».'},
     hl:{lazy:[1,2,4,5,8]}},
    {t:{en:'For <b>reading</b>, <code class="i">toSignal()</code> subscribes and unsubscribes for you. For <b>writing</b> — a POST caused by a click — subscribing by hand is correct.',
        ar:'للـ<b>قراية</b>، <code class="i">toSignal()</code> بيعمل subscribe ويلغيه بدالك. وللـ<b>كتابة</b> — POST بسبب كليك — الـ subscribe اليدوي هو الصح.'},
     edge:'e3',hl:{cmp:[5,24,25,26]}},
    {t:{en:'Whichever way you consume it, the template still owes the reader three states: loading, error, and empty.',
        ar:'وبأي طريقة تستهلكها، التمبلت لسه مدين للقارئ بتلات حالات: تحميل، وخطأ، وفاضي.'},
     hl:{tpl:[1,3,5,7]}}
  ],
  example:[
    {
      name:{en:'A full create-and-refresh flow', ar:'دورة كاملة: إنشاء وتحديث'},
      what:{
        en:'Read with toSignal, write with subscribe, then update the list locally instead of refetching. This is the shape of nearly every form-plus-list screen you will ever build.',
        ar:'اقرا بـ toSignal، واكتب بـ subscribe، وبعدين حدّث اللستة محلياً بدل ما تجيب من الأول. ودي شكل تقريباً كل شاشة فورم مع لستة هتبنيها في حياتك.'},
      files:[
        {id:'cf1',name:'products.component.ts',lang:'ts',tag:{en:'read + write',ar:'قراية + كتابة'},code:[
          "export class Products {",
          "  private readonly api = inject(ProductService);",
          "  private readonly toasts = inject(Toasts);",
          "",
          "  readonly list = signal<Product[]>([]);",
          "  readonly busy = signal(false);",
          "",
          "  constructor() {",
          "    this.api.getAll().subscribe(p => this.list.set(p));",
          "  }",
          "",
          "  add(draft: NewProduct) {",
          "    this.busy.set(true);",
          "    this.api.create(draft).subscribe({",
          "      next: created => {",
          "        // put it straight in the list — no second GET needed",
          "        this.list.update(l => [created, ...l]);",
          "        this.toasts.show('Product added');",
          "      },",
          "      error: () => this.toasts.show('Could not save', 'error'),",
          "      complete: () => this.busy.set(false),",
          "    });",
          "  }",
          "",
          "  remove(id: string) {",
          "    this.api.remove(id).subscribe(() =>",
          "      this.list.update(l => l.filter(p => p.id !== id)));",
          "  }",
          "}"]}
      ]
    }
  ],
  gotchas:[
    {en:'<b>No subscribe, no request.</b> If your network tab is empty, nothing subscribed to the observable.',
     ar:'<b>مفيش subscribe، مفيش ريكويست.</b> لو تاب الشبكة فاضي، يبقى محدش عمل subscribe للـ observable.'},
    {en:'Each <code class="i">subscribe</code> fires its <b>own</b> request. Two <code class="i">| async</code> on the same observable means two identical GETs.',
     ar:'كل <code class="i">subscribe</code> بيطلق ريكويست <b>خاص بيه</b>. واتنين <code class="i">| async</code> على نفس الـ observable معناها GET-ين متطابقين.'},
    {en:'The generic <code class="i">&lt;Product&gt;</code> is a compile-time promise, not validation. If the server changes its response shape, TypeScript will not notice.',
     ar:'الـ generic <code class="i">&lt;Product&gt;</code> وعد وقت الكمبايل، مش تحقق. ولو السيرفر غيّر شكل الرد، TypeScript مش هياخد باله.'},
    {en:'Build query strings with <code class="i">HttpParams</code>, not string concatenation — it escapes values, so a search for <code class="i">"a&b"</code> does not break the URL.',
     ar:'ابني الـ query strings بـ <code class="i">HttpParams</code>، مش بلصق النصوص — هو بيهرّب القيم، فالبحث عن <code class="i">"a&b"</code> مش هيكسر الـ URL.'},
    {en:'An <code class="i">HttpClient</code> call completes after one value, so a forgotten <code class="i">subscribe</code> is not a memory leak — but it is still an untracked side effect. Prefer <code class="i">toSignal</code>.',
     ar:'نداء <code class="i">HttpClient</code> بيقفل بعد قيمة واحدة، فالـ <code class="i">subscribe</code> اللي نسيته مش تسريب ذاكرة — بس لسه side effect مش متتبَّع. فضّل <code class="i">toSignal</code>.'}
  ]
},

]};
